# Form-Native JIT Optimization Track

Goal: make the optimizer fully Form-native and witnessed, with no new runtime
meaning living in C lowering. The C seed may carry host install/call plumbing
while it shrinks, but this track treats Form recipes, Form emitters, and Form
receipts as the source of truth.

## Invariants

- The optimizer proper is Form: decide, profile, specialize, lower, guard,
  deopt, melt, and witness.
- The walker remains the semantic oracle. Every native specialization needs a
  parity witness or a named pending receipt.
- Per-instance observations must collapse into stable numeric categories before
  they guide native code.
- A specialization must have a guard, fallback, and deopt/melt receipt.
- Form source must pass a static analyzer gate for facts that can be proven
  before execution or JIT lowering: missing source attribution, unbound symbols,
  arity mismatches, literal div/mod by zero, literal bounds faults, and static
  null/empty dereferences.
- A direct native access must preserve runtime fault semantics: bounds checks,
  null/nothing checks, div/mod-by-zero checks, type/shape checks, and structured
  exceptions with stack trace plus file/line/column/span attribution.
- Container access is not claimed direct unless the representation receipt proves
  the access path: cons head/tail, indexed buffer/vector, dict/hash bucket, or
  ordered tree path.
- CPU/GPU register wins are claims only after a Form emitter/witness names the
  register lane, spill/rooting behavior, and parity result.

## Rungs

1. Profile receipt shape and numeric category collapse.
   - Status: landed in `observe/jit-profile-receipt.fk`.
   - Witness: `observe/tests/jit-profile-receipt-band.fk` returns 127.
   - Scope: plain `fkwu --src` Form witness. The opt-in C-wire `FK_JIT=1`
     path is not counted as completion for this rung.
   - Proves: per-instance receipts can collapse to category signatures; masks
     name crystallize, inline, stack, field, index, hash, tree, CPU, and GPU
     optimization eligibility.

2. Category-fed tiering.
   - Status: landed in `observe/jit-tier-policy.fk`.
   - Witness: `observe/tests/jit-tier-policy-band.fk` returns 1023.
   - Scope: plain `fkwu --src` Form witness. It is policy, not native lowering.
   - Proves: `observe/jit-decision.fk`, profile/category masks, and runtime
     fault attribution compose so receipts choose baseline/profile, hold,
     crystallize, specialize, deopt, or melt.

3. Runtime checks and source-attributed exceptions.
   - Status: landed in `observe/jit-runtime-fault.fk`.
   - Witness: `observe/tests/jit-runtime-fault-band.fk` returns 255.
   - Scope: plain `fkwu --src` Form witness. Runtime throwing in the native
     emitter is still pending.
   - Proves: bounds, null-ref, div-by-zero, type/shape, and parity-checkable
     exception receipts with stack trace and full source attribution are part of
     the JIT contract.

4. Inlining and call tracking.
   - Status: landed in `observe/jit-inline-policy.fk`.
   - Witness: `observe/tests/jit-inline-policy-band.fk` returns 1023.
   - Scope: plain `fkwu --src` Form witness. It is inline policy, not native
     machine-code emission.
   - Proves: monomorphic calls can inline, low-polymorphic calls require guarded
     inline, megamorphic calls fall back, budgets/depth/source signatures gate
     the choice, and deopt wins when present.

5. Static analyzer pre-execution/pre-JIT gate.
   - Status: landed in `observe/form-static-analyzer.fk`.
   - Witness: `observe/tests/form-static-analyzer-band.fk` returns 1023.
   - Scope: plain `fkwu --src` Form witness over a lowered AST row shape. Parser
     integration is still pending.
   - Proves: the gate catches unbound symbols, arity mismatches, literal
     div/mod-by-zero, literal bounds failure, static empty deref, and missing
     source attribution before execute/JIT admission.

6. Stack and frame collapse.
   - Status: landed in `observe/jit-stack-frame.fk`.
   - Witness: `observe/tests/jit-stack-frame-band.fk` returns 255.
   - Scope: plain `fkwu --src` Form witness. It is frame-collapse policy and
     receipt shape, not runtime stack rewriting.
   - Proves: tail-loop, frame collapse, spill fallback, root-map requirement,
     source signature match, deopt-on-guarded profile, and parity receipt.

7. Field access.
   - Status: landed in `observe/jit-representation-specialization.fk`.
   - Witness: `observe/tests/jit-representation-specialization-band.fk`
     returns 1023.
   - Scope: plain `fkwu --src` Form witness. It is direct-access admission
     policy, not emitted load/store code.
   - Proves: shape/layout ID, field slot guard, fallback requirement, source
     signature match, and source-attributed type fault receipt.

8. Array and list access.
   - Status: landed in `observe/jit-representation-specialization.fk`.
   - Witness: `observe/tests/jit-representation-specialization-band.fk`
     returns 1023.
   - Scope: plain `fkwu --src` Form witness. It separates cons head/tail from
     indexed array/buffer admission.
   - Proves: list head/tail direct path, array indexed/range path, bounds
     receipt, source signature guard, and fallback for unsafe shapes.

9. Dict and hashmap access.
   - Status: landed in `observe/jit-representation-specialization.fk`.
   - Witness: `observe/tests/jit-representation-specialization-band.fk`
     returns 1023.
   - Scope: plain `fkwu --src` Form witness. It is lookup-admission policy.
   - Proves: key category, layout/table version, hashmap bucket path, collision
     fallback, source signature guard, and source-attributed fault requirement.

10. Red-black tree and ordered map access.
   - Status: landed in `observe/jit-representation-specialization.fk`.
   - Witness: `observe/tests/jit-representation-specialization-band.fk`
     returns 1023.
   - Scope: plain `fkwu --src` Form witness. It is ordered-path admission
     policy.
   - Proves: orderable-key receipt, ordered path guard, fallback, and
     source-attributed type/comparator fault requirement.

11. CPU register and SIMD lowering.
   - Status: landed in `observe/jit-register-lowering.fk`.
   - Witness: `observe/tests/jit-register-lowering-band.fk` returns 511.
   - Scope: plain `fkwu --src` Form witness. It is register allocation/lowering
     policy, not machine-code emission.
   - Proves: virtual register pressure, physical register budget, scalar/SIMD
     choice, spill/root maps, fault maps, source-attributed runtime fault gate,
     deopt, and parity receipt.

12. GPU register and memory lowering.
    - Status: landed in `observe/jit-register-lowering.fk`.
    - Witness: `observe/tests/jit-register-lowering-band.fk` returns 511.
    - Scope: plain `fkwu --src` Form witness. It is GPU register/memory policy,
      not PTX/GLSL/MSL emission.
    - Proves: GPU register class, occupancy, shared memory, coalesced access,
      precision gate, fault map, source attribution, and parity receipt.

13. Deopt, melt, invalidation, and code cache.
    - Status: landed in `observe/jit-deopt-cache.fk`.
    - Witness: `observe/tests/jit-deopt-cache-band.fk` returns 511.
    - Scope: plain `fkwu --src` Form witness. It is cache/deopt policy, not
      runtime dispatch.
    - Proves: install, keep, invalidate, attributed deopt, invalidation-to-rewalk,
      stale melt, fallback requirement, and source signature matching.

13a. Grouped JIT policy spine.
    - Status: landed in `observe/tests/jit-policy-front-sweep-band.fk`,
      `observe/tests/jit-policy-access-sweep-band.fk`, and
      `observe/tests/jit-policy-cache-sweep-band.fk`.
    - Witness: returns 31, 7, and 15 respectively.
    - Scope: grouped composed policy witness. Native emission/dispatch remains
      pending. One mega-concatenated sweep currently exceeds the `fkwu --src`
      256-symbol table and is not claimed.
    - Proves: profile receipts, runtime faults, tiering, inlining, static
      analysis, stack/frame, representation specialization, register lowering,
      and deopt/cache policies can coexist in one source-runner pass.

14. Compact Form-native emitter and guarded dispatch bundle.
    - Status: landed in `observe/jit-emitter-bundle.fk`.
    - Witness: `observe/tests/jit-emitter-bundle-band.fk` returns 8191.
    - Scope: Form-native emitter/dispatch receipt model. It consumes the
      witnessed policy summaries without concatenating the entire policy spine,
      avoiding the current 256-symbol `--src` envelope.
    - Proves: complete policy receipts admit emission; emitted blocks carry
      guard, fallback, deopt-cache, runtime-fault-map, and source-map facts;
      guard-passing dispatch selects native; guard failure deopts to walker;
      runtime failure returns a source-attributed exception; invalidation rewalks;
      incomplete policy/source/fault maps block emission.

15. Form-native JIT IR lowering and interpreter.
    - Status: landed in `observe/jit-native-ir.fk`.
    - Witness: `observe/tests/jit-native-ir-band.fk` returns 16383.
    - Scope: executable Form-owned IR lowering target and interpreter. It is not
      host machine code.
    - Proves: emitted programs carry source-attributed instructions; array
      lowering includes bounds checks; field lowering includes null checks; div
      lowering includes div-zero checks; guard-passing execution selects native;
      guard failure deopts to walker; runtime failure returns a source-attributed
      exception; invalidation rewalks; bad attribution is rejected; native/walker
      parity is witnessed for the passing sample.

16. Form-owned host dispatch packet and cache integration.
    - Status: landed in `observe/jit-host-dispatch.fk`.
    - Witness: `observe/tests/jit-host-dispatch-band.fk` returns 8191.
    - Scope: Form-owned install/lookup/invalidate/execute contract over host
      dispatch packets. It is not host machine code execution.
    - Proves: CPU/GPU Form-IR packets install; foreign `c-lowering` packets are
      rejected; packets missing fault maps are rejected; matching valid packets
      hit the dispatch cache; signature mismatch and invalidation miss; cache
      hits execute through the Form IR interpreter; guard failure deopts; runtime
      failure returns a source-attributed exception; invalidated entries rewalk.

17. Form-owned backend emission bundle.
    - Status: landed in `observe/jit-backend-emission.fk`.
    - Witness: `observe/tests/jit-backend-emission-band.fk` returns 8191.
    - Scope: backend emission metadata and execution simulation over compact
      dispatch packet summaries. It is not loaded host machine code.
    - Proves: x64, arm64, and PTX backend bundles are admitted from Form-owned
      packets; incompatible target emission is rejected; foreign `c-lowering`
      packets and backend owners are rejected; missing deopt metadata is
      rejected; code size follows lowered IR op count; guard/runtime passing
      execution selects native; guard failure deopts; runtime failure selects
      exception.

18. Form-owned backend byte/op stream emission.
    - Status: landed in `observe/jit-backend-bytes.fk`.
    - Witness: `observe/tests/jit-backend-bytes-band.fk` returns 16383.
    - Scope: deterministic backend payload generation as Form data. It is not
      loaded or executed host machine code.
    - Proves: x64 byte payload, arm64 op-word payload, and PTX-like op-code
      payload emit only from Form-owned requests with source, guard, fault,
      deopt, root, and ABI maps; x64 payload has stable prologue, guard branch,
      and return bytes; arm64/PTX-like payloads have stable op streams; foreign
      `c-lowering`, missing deopt metadata, and missing source metadata reject.

19. Form-owned loader/executor contract.
    - Status: landed in `observe/jit-loader-contract.fk`.
    - Witness: `observe/tests/jit-loader-contract-band.fk` returns 16383.
    - Scope: load/execute contract over Form-emitted payloads. It is not an
      `mmap`/W^X host primitive and not a function pointer call.
    - Proves: safe Form-owned payloads load into sealed executable non-writable
      slots; foreign `c-lowering`, missing deopt metadata, and missing source
      metadata reject; writable/unsealed slots are unsafe; guard/runtime/parity
      passing execution selects native; guard failure deopts; runtime failure
      returns an exception kind; invalidation rewalks; parity failure deopts.

19a. Merge checkpoint and existing runtime-door audit.
    - Status: landed in
      `receipts/2026-06-30-form-native-jit-merge-checkpoint.md`.
    - Witness: required bootstrap and every Form JIT band above passed on this
      checkout.
    - Scope: no C-seed changes. The existing `fk_native_call`,
      `fk_native_call_args`, and `fk_nat_install` host membrane is named as the
      already-present install/call door. The opt-in `FK_JIT=1` C
      proof-of-concept lowerer is explicitly not counted as this track's
      completion witness; on this Mac checkout it crystallized simple probes but
      returned `nothing`, so the next step must not depend on that C lowerer as
      truth.

19b. Form-owned native admission gate.
    - Status: landed in `observe/jit-native-admission.fk`.
    - Witness: `observe/tests/jit-native-admission-band.fk` returns 131071.
    - Scope: executable Form admission chain; not host machine execution.
    - Proves: static analyzer success, Form-owned backend bytes, loader payload
      safety, sealed executable slot, native/deopt/exception/rewalk routing,
      walker fallback value, runtime exception kind, parity deopt, invalidation
      rewalk, and rejection of static div-zero source, foreign `c-lowering`,
      missing source maps, and unsafe generation-zero loads.

19c. Form-emitted carrier ABI byte bridge.
    - Status: landed in `observe/jit-carrier-abi.fk`.
    - Witness: `observe/tests/jit-carrier-abi-band.fk` returns 32767 on this
      Mac checkout.
    - Scope: Form-emitted byte equivalence to the existing `native_call_test`
      payload and live carrier status classification. It is not a general
      arbitrary-byte source path into `fk_native_call`.
    - Proves: `model/form-asm-x64.fk` emits the exact Win64 and SysV `f(a)=a+1`
      carrier byte images, carries source/map metadata, rejects missing source
      metadata, keeps foreign `c-lowering` visible as foreign, and classifies
      the live host carrier as complete only if `native_call_test` returns
      `arg+1`. On this host the carrier returns `-1`, so completion remains
      pending rather than overclaimed.

19d. Backend emits carrier-compatible payloads.
    - Status: landed in `observe/jit-backend-carrier-payload.fk`.
    - Witness: `observe/tests/jit-backend-carrier-payload-band.fk` returns 32767.
    - Scope: Form backend payload generation through the x64 encoder and loader
      action contract. It still does not pass arbitrary byte lists into
      `fk_native_call` at runtime.
    - Proves: Win64 and SysV carrier payload bytes are generated through
      `model/form-asm-x64.fk`, wrapped as safe backend byte outputs, admitted as
      safe loader payloads, loaded into safe slots, and routed through native,
      deopt, exception, rewalk, parity-deopt, and metadata-rejection paths.

19e. Form x64 checked-access payloads.
    - Status: landed in `observe/jit-checked-access-payload.fk`.
    - Witness: `observe/tests/jit-checked-access-payload-band.fk` returns
      2097151.
    - Scope: concrete Form-emitted x64 byte fragments for checked direct access.
      It still does not install or execute those byte lists through
      `fk_native_call`.
    - Proves: indexed array/list-slot loads carry null and bounds fault exits;
      field-slot loads carry null fault exits; signed division carries a
      div-by-zero fault exit; all three are admitted through the loader contract
      only with source, guard, fault, deopt, root, ABI, safepoint, and exception
      maps; fault exits are tied to source-attributed exception receipts; native,
      deopt, exception, rewalk, parity-deopt, and metadata-rejection paths remain
      witnessed.

19f. Exact Form byte-list membrane.
    - Status: landed in `observe/jit-byte-list-membrane.fk`.
    - Witness: `observe/tests/jit-byte-list-membrane-band.fk` returns
      134217727.
    - Scope: exact byte-list preservation and validation before host install.
      It still does not call `fk_nat_install`, `fk_native_call`, or a host
      function pointer.
    - Proves: Form-emitted carrier bytes, checked array/list bytes, checked
      field bytes, checked div bytes, and arbitrary valid Form byte lists are
      admitted only when every byte is in 0..255 and full source, guard, fault,
      deopt, root, ABI, relocation, safepoint, and exception metadata exists;
      loaded slots preserve exact bytes; unsafe generation-zero and
      writable/unsealed slots reject; native, deopt, exception, rewalk,
      parity-deopt, and malformed-byte rejection paths remain witnessed.

19g. Hot source to exact byte pipeline.
    - Status: landed in `observe/jit-source-byte-pipeline.fk`.
    - Witness: `observe/tests/jit-source-byte-pipeline-band.fk` returns
      2097151.
    - Scope: one source-runner pass from static-analyzed hot source plan to
      exact Form-emitted x64 bytes and sealed load/action routing. It uses a
      compact byte membrane so the static analyzer and pipeline fit in one
      `fkwu --src` envelope; the fuller exact byte-list membrane remains
      separately witnessed in `observe/jit-byte-list-membrane.fk`.
    - Proves: hot source plans must pass static analysis before byte admission;
      `arg+1`, checked array/list, checked field, and checked div lowerings emit
      exact valid bytes; loaded slots preserve exact bytes; static div-zero,
      cold source, foreign owner, missing source/deopt metadata, and
      generation-zero loads reject; native, deopt, exception, rewalk,
      parity-deopt, bounds-branch, and source-attributed fault paths remain
      witnessed.

19h. Host carrier handoff status.
    - Status: landed in `observe/jit-host-handoff.fk`.
    - Witness: `observe/tests/jit-host-handoff-band.fk` returns 524287.
    - Scope: handoff from admitted hot-source `arg+1` bytes to the existing
      `native_call_test` carrier status. It still does not pass arbitrary Form
      byte lists into `fk_native_call` or `fk_nat_install`.
    - Proves: the source pipeline's exact SysV `arg+1` bytes match the carrier
      ABI image; live carrier results are classified as correct, unavailable, or
      mismatch; only a correct carrier status can claim native completion;
      unavailable carrier status deopts; mismatched carrier status rejects;
      checked-array bytes, cold source, and static div-zero source reject before
      handoff; guard, runtime fault, invalidation, parity, byte-preservation,
      and valid-byte paths remain witnessed.

19i. Source-byte cache/deopt/melt lifecycle.
    - Status: landed in `observe/jit-source-cache-lifecycle.fk`.
    - Witness: `observe/tests/jit-source-cache-lifecycle-band.fk` returns
      65535.
    - Scope: compact cache lifecycle over admitted hot-source exact bytes. It is
      not host executable cache mutation and still does not call
      `fk_nat_install` or `fk_native_call`.
    - Proves: admitted source-byte cache entries install and keep; guard failure
      deopts only with attributed fallback; runtime failure routes to attributed
      exception; cache/runtime invalidation rewalks; parity failure deopts; stale
      low-hit entries melt; source-signature mismatch, missing fallback, cold
      source, melted reuse, and unattributed runtime exceptions reject or rewalk;
      byte preservation and source attribution remain witnessed.

19j. Compact native dispatch sweep.
    - Status: landed in `observe/jit-native-dispatch-sweep.fk`.
    - Witness: `observe/tests/jit-native-dispatch-sweep-band.fk` returns 16383.
    - Scope: compact receipt sweep over the source-byte pipeline, host handoff,
      and source-cache lifecycle bands. The full concatenation exceeds the
      current source-runner envelope, so the detailed source cells remain the
      authority and this sweep verifies their receipt totals and joint dispatch
      outcomes.
    - Proves: source-byte, host-handoff, and cache-lifecycle receipts compose;
      passing guard/runtime/parity/carrier/non-stale state selects native; guard
      and parity failures deopt; runtime failure selects exception; invalidation
      rewalks; carrier unavailability deopts; stale cache melts; wrong totals,
      missing source metadata, and missing map metadata reject.

19k. Host install intent contract.
    - Status: landed in `observe/jit-host-install-intent.fk`.
    - Witness: `observe/tests/jit-host-install-intent-band.fk` returns 131071.
    - Scope: Form-owned intent envelope for the future `fk_nat_install` /
      `fk_native_call` bridge. It still does not call the host function pointer
      or install arbitrary byte lists.
    - Proves: native completion requires exact-byte and dispatch receipts,
      source/maps, W^X sealing, executable and non-writable slot state, argument
      maps, cache state, carrier status, and positive generation; guard failure
      and parity failure deopt; runtime failure selects exception; invalidation
      rewalks; stale cache melts; unavailable carrier deopts; missing receipts,
      source/maps, argument maps, cache state, sealing, non-writable status, or
      generation reject.

19l. Native call plan contract.
    - Status: landed in `observe/jit-native-call-plan.fk`.
    - Witness: `observe/tests/jit-native-call-plan-band.fk` returns 2097151.
    - Scope: compact Form-owned readiness contract for the future
      `fk_native_call` / `fk_native_call_args` bridge. It still does not call a
      host function pointer or install arbitrary byte lists.
    - Proves: static analyzer, runtime-fault, source-byte, native-dispatch, and
      host-install-intent receipts must all be exact before call readiness;
      native completion also requires argument maps, source stack attribution,
      exception maps, ABI agreement, callee slot state, carrier status, and
      positive generation; guard/parity failures deopt; runtime failure selects
      exception; invalidation rewalks; stale cache melts; unavailable carrier
      deopts; missing args, stack, exception map, ABI, callee slot, or bad
      receipts reject.

19m. Full-track Form receipt sweep.
    - Status: landed in `observe/jit-full-track-sweep.fk`.
    - Witness: `observe/tests/jit-full-track-sweep-band.fk` returns 524287.
    - Scope: compact ledger over the full JIT track to date. It composes the
      policy/front-end, access/register/cache, emitter/IR/backend/loader, byte
      membrane, source-byte, dispatch, install-intent, and native-call-plan
      receipts, while explicitly keeping host arbitrary-byte execution pending.
    - Proves: all Form-owned receipts compose; guard failure deopts; runtime
      failure selects exception; invalidation rewalks; parity failure deopts;
      carrier unavailability deopts; stale cache melts; malformed totals,
      missing source metadata, and missing map metadata reject; a passing native
      path returns pending rather than claiming host execution until arbitrary
      Form-emitted bytes actually install and call through the host membrane.

19n. Host membrane readiness audit.
    - Status: landed in `observe/jit-host-membrane-readiness.fk`.
    - Witness: `observe/tests/jit-host-membrane-readiness-band.fk` returns
      524287.
    - Scope: compact live audit of the remaining host install/call membrane.
      It observes the existing `native_call_test` carrier and does not change
      the C seed.
    - Proves: full-track, native-call-plan, and byte-list membrane receipts are
      exact; the current hardcoded test carrier is admissible but not enough for
      completion; current native path stays pending; a future membrane must add
      arbitrary Form byte-list ingress, argument ingress, W^X/sealed state,
      source-attributed exceptions, deopt/melt routing, parity, and positive
      generation; guard/parity failures deopt; runtime failure selects
      exception; invalidation rewalks; stale cache melts; carrier mismatch and
      bad receipts reject.

19o. Byte ingress and native args bridge.
    - Status: landed in `observe/jit-byte-ingress-args.fk`.
    - Witness: `observe/tests/jit-byte-ingress-args-band.fk` returns 2097151.
    - Scope: Form-owned packet contract for feeding the existing internal
      `fk_native_call_args` / `fk_nat_install` host door. It still does not
      expose arbitrary byte-list host execution.
    - Proves: byte membrane, native-call-plan, host-membrane, and args-vector
      payload receipts compose; exact arg-vector byte images, byte counts, ABI,
      tagged argument vectors, rooted frames, source maps, exception maps, deopt
      metadata, W^X/sealed state, and positive generation are required; SysV
      and Win64 one-arg packets plus SysV two-arg packets are admitted; the old
      register-argument carrier payload rejects; without host ingress the packet
      remains pending; with ingress a passing path selects native; guard/parity
      failures deopt; runtime failure selects exception; invalidation rewalks;
      stale cache melts; byte-count mismatch, invalid bytes, untagged args, too
      many args, undersized frames, foreign owner, bad ABI, missing source,
      missing W^X, or bad receipts reject.

19p. Post-ingress full-track sweep.
    - Status: landed in `observe/jit-post-ingress-sweep.fk`.
    - Witness: `observe/tests/jit-post-ingress-sweep-band.fk` returns 1048575.
    - Scope: compact ledger after the byte-ingress/native-args bridge. It keeps
      host arbitrary-byte execution pending.
    - Proves: full-track, host-membrane, byte-ingress, call-plan,
      byte-membrane, and host-handoff receipts compose; the current Form-side
      post-ingress state is ready but incomplete until host ingress is wired;
      current passing native path stays pending; future host-ingress path
      selects native; guard/parity failures deopt; runtime failure selects
      exception; invalidation rewalks; stale cache melts; carrier unavailable
      deopts; bad receipts, missing source/maps, or missing byte-ingress state
      reject.

19q. Args-vector native payload ABI.
    - Status: landed in `observe/jit-args-vector-payload.fk`.
    - Witness: `observe/tests/jit-args-vector-payload-band.fk` returns 2097151.
    - Scope: exact byte witness for the existing `fk_native_call_args` ABI. It
      still does not expose arbitrary byte-list host execution.
    - Proves: SysV and Win64 payloads load tagged Form arguments through the
      `long long *args` vector (`[rdi]` or `[rcx]`) rather than the old direct
      register carrier ABI; one- and two-argument payload byte images are exact;
      old register-argument payloads reject for this membrane; safe payloads
      remain pending without host ingress; future host-ingress paths route
      native/deopt/exception/rewalk/melt; malformed bytes, bad ABI, missing
      source metadata, missing W^X state, or bad post-ingress receipts reject.

19r. Host W^X install audit.
    - Status: landed in `observe/jit-host-wx-audit.fk`.
    - Witness: `observe/tests/jit-host-wx-audit-band.fk` returns 1048575.
    - Scope: Form-side audit of the existing host install memory policy. It
      still does not expose arbitrary byte-list host execution. The POSIX host
      carrier was repaired in `runtime/fkwu-uni.c` to map RW, copy bytes, then
      flip RX with `mprotect` on x64 and to return unavailable on non-x64
      POSIX builds; this is carrier-only and recorded as a shrink target in
      `receipts/2026-06-30-form-native-jit-posix-wx-carrier-repair.md`.
    - Proves: byte-ingress, args-vector, host-membrane, and post-ingress
      receipts compose; current Win64 and POSIX x64 write-then-execute memory is
      W^X-safe; current non-x64 POSIX x64-payload execution remains unavailable;
      old POSIX RWX memory is rejected; native/deopt/exception/rewalk/melt
      routes are specified only after W^X, arbitrary byte ingress, argument
      vectors, source maps, exception maps, deopt metadata, parity, and positive
      generation are present; bad receipts, missing metadata, missing executable
      state, or unknown platforms reject readiness.

19s. POSIX W^X carrier repair.
    - Status: landed in `runtime/fkwu-uni.c` and
      `receipts/2026-06-30-form-native-jit-posix-wx-carrier-repair.md`.
    - Witness: checkout bootstrap still returns 42, 55, and 11111;
      `observe/tests/jit-host-wx-audit-band.fk` returns 1048575; on this
      non-x64 POSIX checkout `(native_call_test 41)` returns `-1` instead of
      trapping.
    - Scope: carrier-only C-seed repair with an explicit shrink receipt. It
      maps POSIX native pages RW, copies bytes, flips RX with `mprotect`, and
      returns unavailable on non-x64 POSIX builds for x64 payloads. It adds no C
      lowering rule and does not expose arbitrary Form byte-list host execution.
    - Proves: the host executable-page membrane now matches the Form W^X
      contract on POSIX x64 while preserving honest unavailability on this
      Apple Silicon checkout.

19t. Host ingress readiness after W^X.
    - Status: landed in `observe/jit-host-ingress-readiness.fk`.
    - Witness: `observe/tests/jit-host-ingress-readiness-band.fk` returns
      1048575.
    - Scope: compact Form witness for post-repair host ingress readiness. It
      still does not expose arbitrary Form byte-list host execution.
    - Proves: byte-ingress, args-vector, host-W^X, host-membrane, and
      post-ingress receipts compose; live carrier status is admissible if
      correct or unavailable; missing arbitrary byte ingress remains pending;
      a fully described ingress packet is ready at the Form metadata level;
      unavailable executable architecture deopts; this non-x64 checkout is not a
      complete native carrier; future matching-architecture ingress routes
      native/deopt/exception/rewalk/melt; carrier mismatch and bad receipts
      reject.

19u. Full-stack runtime fault attribution.
    - Status: landed in `observe/jit-runtime-stack-attribution.fk`.
    - Witness: `observe/tests/jit-runtime-stack-attribution-band.fk` returns
      1023.
    - Scope: Form-only tightening of the runtime exception contract. It is not
      kernel-level throwing from installed host machine code.
    - Proves: every stack frame in a native runtime exception must carry full
      source attribution; empty stacks, missing caller attribution, and throwing
      source/frame mismatches reject; native/walker parity compares every frame's
      function, call site, and source, not only stack depth; bounds and
      div-by-zero exceptions share the same full-stack contract.

19v. Host ingress exception bridge.
    - Status: landed in `observe/jit-host-exception-bridge.fk`.
    - Witness: `observe/tests/jit-host-exception-bridge-band.fk` returns
      524287.
    - Scope: Form-only composition of host ingress readiness with full-stack
      runtime fault attribution. It is not arbitrary byte-list host execution.
    - Proves: host-ingress, runtime-stack, runtime-fault, and checked-access
      receipts compose; future matching-architecture runtime failure may route
      to exception only with complete full-stack attribution; source/throwing
      frame mismatches, missing full-stack requirement, missing source or
      exception maps, generation zero, and bad receipts reject; unavailable
      architecture deopts before claiming native/exception, while guard,
      invalidation, stale-cache, and parity paths still route correctly.

19w. Bootstrap exit replacement-carrier boundary.
    - Status: landed in `observe/jit-bootstrap-exit-carrier.fk`.
    - Witness: `observe/tests/jit-bootstrap-exit-carrier-band.fk` returns
      1048575.
    - Scope: Form-only boundary after the host exception bridge. It makes the
      temporary C bootstrap an explicit no-growth path for new JIT meaning and
      records the current Rust walker as proof-only rather than a native host
      carrier.
    - Proves: host-exception, host-ingress, byte-ingress, args-vector,
      runtime-stack, and checked-access receipts compose; future Rust and
      Form-native replacement carriers are admissible only with arbitrary byte
      ingress, argument vectors, W^X, source maps, exception maps, deopt maps,
      parity, full stack attribution, and positive generation; current
      proof-only Rust remains pending; C-bootstrap growth rejects; missing
      arbitrary byte ingress, stack attribution, or generation stays pending;
      no replacement contract rejects; complete replacement carriers route
      native/deopt/exception/rewalk/melt, unavailable-architecture deopt, and
      mismatch reject cases.

19x. Hot-recipe native witness sweep.
    - Status: landed in `observe/jit-native-witness-sweep.fk`.
    - Witness: `observe/tests/jit-native-witness-sweep-band.fk` returns
      8388607.
    - Scope: compact Form-only sweep for the full optimizer objective after the
      bootstrap-exit boundary. It does not claim host machine-code execution.
    - Proves: profile/category collapse, tiering, inlining, static analysis,
      stack/frame collapse, field/list/array/dict/hash/tree access, checked
      payloads, CPU/GPU register lowering, deopt cache, Form IR, host dispatch,
      backend byte streams, loader/native admission, byte ingress, host
      exception bridge, bootstrap-exit, post-ingress, and self-host completion
      receipts compose as one hot-recipe admission surface; current non-live
      replacement-carrier state remains pending; a future live replacement carrier routes
      native/deopt/exception/rewalk/melt; C-bootstrap growth rejects; missing
      category collapse, dict access, GPU lane, runtime checks, source/maps, or
      positive generation rejects readiness.

19as. Self-host completion sweep.
    - Status: landed in `observe/jit-self-host-completion-sweep.fk`.
    - Witness: `observe/tests/jit-self-host-completion-sweep-band.fk` returns
      8388607 under `./fkwu --src`.
    - Scope: Form-native receipt/route sweep that composes the self-host
      compiler, profile runtime, runtime loop, ingress runtime, native-call
      bridge, host-membrane bridge, final-native gate, field/list/array live
      evidence, dict/hash/tree container live evidence, and compact
      dict/hash/tree container live-slot runtime. It is now required by the
      top-level native witness sweep. It does not add C/Rust or call host
      machine code.
    - Proves: the optimizer path can require both self-host access families
      plus the self-host runtime loop and container live slots before
      native-witness admission; current missing carrier/install/call evidence
      remains pending; a future complete proof routes native, guard-deopt,
      runtime-exception, invalidation-rewalk, stale-melt, and parity-deopt;
      missing field or container families, container slots, the runtime loop,
      C-growth, missing carrier/install/call, zero generation, bad receipts,
      source, or maps reject before completion.

19bd. Container live-slot runtime.
    - Status: landed in `model/jit-container-live-slot-runtime.fk`.
    - Witness: `model/tests/jit-container-live-slot-runtime-band.fk` returns
      1048575 under `./fkwu --src`.
    - Scope: compact Form live-slot contract for dict, hashmap, and red-black
      tree replacement payloads. It names CPU/GPU target facts, byte counts,
      source/maps, carrier/install/call evidence, exception/deopt/melt/parity
      proofs, and generation without composing the full container stack through
      the current late-definition source-runner limit.
    - Proves: dict/hash/tree slots are contract-ready with the expected byte
      lengths; incomplete carrier/install/call facts stay pending; complete
      slots route native, guard-deopt, runtime-exception, invalidation-rewalk,
      parity-deopt, and stale-melt; bad byte counts, invalid bytes, array-like
      ops, missing source, zero generation, missing carrier, missing install,
      or missing call reject before live completion.

19y. Replacement carrier runtime integration contract.
    - Status: landed in `observe/jit-replacement-runtime-integration.fk`.
    - Witness: `observe/tests/jit-replacement-runtime-integration-band.fk`
      returns 134217727.
    - Scope: Form-only install/call lifecycle contract between the hot-recipe
      native sweep and final runtime integration. It does not install or call
      host machine code.
    - Proves: native-witness, bootstrap-exit, host-exception, runtime-stack,
      byte-ingress, and loader receipts compose; Form-owned payloads require
      exact byte counts, valid bytes, known ABI, tagged args, source/guard/
      fault/deopt/root maps, and positive generation; replacement slots require
      no C growth, W^X, executable, non-writable, sealed, installed, valid,
      generationed state; call frames require rooted args, full-stack source
      attribution, exception maps, and parity; pending slots stay pending;
      complete replacement runtime routes native/deopt/exception/rewalk/melt,
      unavailable-architecture deopt, and mismatch reject; C-bootstrap growth,
      malformed bytes, foreign owners, missing maps, writable/stale slots, and
      incomplete exception stacks reject readiness.

19z. Source recipe to replacement runtime contract.
    - Status: landed in `observe/jit-source-replacement-runtime.fk`.
    - Witness: `observe/tests/jit-source-replacement-runtime-band.fk` returns
      33554431.
    - Scope: Form-only source-to-runtime pipeline contract. It does not install
      or call host machine code. It now requires the source-runtime
      orchestrator receipt before source replacement can pass.
    - Proves: native-witness, replacement-runtime, static-analyzer,
      source-byte, backend-bytes, runtime-stack, and source-runtime
      orchestrator receipts compose; hot source recipes require source
      attribution, static clean status, profile receipt, numeric category
      collapse, tiering, inline/frame/access/register facts, and positive
      generation; Form lowering requires Form ownership, IR, backend, exact
      byte counts, valid bytes, source/maps, payload readiness, and positive
      generation; replacement runtime requires no-C-growth, W^X, frame, stack,
      exception, deopt, melt, parity, and positive generation; current non-live
      runtime stays pending; future live replacement runtime routes
      native/deopt/exception/rewalk/melt; C-bootstrap growth, missing
      source/static/category/backend/stack/exception/orchestrator facts, bad
      byte counts, bad receipts, and foreign lowering owners reject readiness.

19aa. Final native completion audit gate.
    - Status: landed in `observe/jit-final-native-gate.fk`.
    - Witness: `observe/tests/jit-final-native-gate-band.fk` returns 1048575.
    - Scope: Form-only completion audit gate. It composes the full track through
      source-to-replacement-runtime and explicitly separates contract readiness
      from live native execution.
    - Proves: native-witness, replacement-runtime, source-replacement-runtime,
      host-exception, and bootstrap-exit receipts compose; the current track is
      contract-ready but not complete because live replacement-carrier execution
      of Form-emitted bytes is still absent; future live-native evidence routes
      native/deopt/exception/rewalk/melt; live-but-no-native evidence is not
      completion; C-growth, missing lowering/runtime/source-attributed
      exceptions/generation, bad receipts, missing source maps, and missing
      metadata maps reject readiness.

19ab. Live replacement-carrier evidence suite.
    - Status: landed in `observe/jit-live-execution-evidence.fk`.
    - Witness: `observe/tests/jit-live-execution-evidence-band.fk` returns
      16777215.
    - Scope: Form-only decomposition of the final live execution flag. It does
      not install or call host machine code. It now requires the
      carrier/install/call evidence ledger before live-execution evidence can
      pass.
    - Proves: final-native-gate, source-replacement-runtime, and
      replacement-runtime receipts compose with the carrier/install/call
      ledger; current evidence is contract-ready but pending because live
      carrier/install/call/exception/deopt/melt proofs are absent; complete
      live evidence routes native/deopt/exception/rewalk/melt; missing carrier,
      install, call, exception, deopt, melt, or parity proof is not completion;
      C growth, zero generation, bad carrier evidence, bad receipts, missing
      source maps, and missing metadata maps reject readiness.

19at. Live runtime integration bridge.
    - Status: landed in `observe/jit-live-runtime-integration.fk`.
    - Witness: `observe/tests/jit-live-runtime-integration-band.fk` returns
      4194303 under `./fkwu --src`.
    - Scope: Form-only bridge from self-host completion, native witness,
      source/replacement runtime, final-native gate, live-execution evidence,
      runtime-fault, and full-stack attribution receipts into one live runtime
      contract. It does not add C/Rust or call host machine code.
    - Proves: live runtime readiness now requires bounds/null/div/type fault
      semantics and complete source-attributed stack traces alongside the
      self-host compiler path and live carrier evidence; current missing
      carrier/install/call evidence remains pending; a future complete proof
      routes native, guard-deopt, runtime-exception, invalidation-rewalk,
      stale-melt, and parity-deopt; missing live evidence, stack attribution,
      runtime-fault maps, no-C-growth, carrier/install/call, generation,
      receipts, source, or maps reject before runtime completion.

19au. Rung 20 readiness audit.
    - Status: landed in `observe/jit-rung20-readiness.fk`.
    - Witness: `observe/tests/jit-rung20-readiness-band.fk` returns
      67108863 under `./fkwu --src`.
    - Scope: final Form-only readiness audit before claiming rung 20. It
      composes native witness, self-host completion, replacement runtime,
      source runtime, final-native gate, live-execution evidence, and live
      runtime safety receipts, and carrier/install/call evidence, then keeps
      completion pending until actual carrier/install/call evidence is present.
      It does not add C/Rust or call host machine code.
    - Proves: all current optimizer, runtime-safety, source-attribution, and
      self-host receipts compose into one rung-20 readiness contract; current
      missing carrier/install/call evidence remains pending; a future complete
      proof routes native, guard-deopt, runtime-exception, invalidation-rewalk,
      stale-melt, and parity-deopt; missing source/lowering/runtime/safety/
      self-host/live/no-C-growth facts, carrier/install/call, generation,
      receipts, source, or maps reject before completion.

19av. Carrier/install/call evidence ledger.
    - Status: landed in `observe/jit-carrier-install-call-evidence.fk`.
    - Witness: `observe/tests/jit-carrier-install-call-evidence-band.fk`
      returns 134217727 under `./fkwu --src`.
    - Scope: Form-only ledger for the final host membrane facts still missing
      from rung 20: host install intent, native-call plan, byte ingress with
      args vectors, host membrane readiness, host W^X audit, and args-vector
      payload receipts, plus the live carrier probe, concrete install/call
      attempt packet, concrete dylib carrier ABI contract, and Form-owned
      dylib slot runtime, and profile-derived dylib slot runtime. It does not
      add C/Rust or call arbitrary host machine code.
    - Proves: the install/call side is contract-ready only with byte ingress,
      tagged args, W^X, source/maps, no-C-growth, positive generation, and an
      admissible live carrier status; the install/call attempt, dylib carrier
      ABI, dylib slot runtime, and profile-derived slot bridge are now explicit
      Form packets, but current carrier/install/call evidence remains pending
      until host arbitrary-byte ingress, install, and call facts are live; a future complete proof routes native, guard-deopt,
      runtime-exception, invalidation-rewalk, stale-melt, and parity-deopt;
      missing bytes, args, W^X, live carrier probe, install/call attempt,
      dylib ABI, slot runtime, profile-slot bridge, carrier, install, call,
      no-C-growth, generation, receipts, source, or maps reject before
      completion.

19bc. Profile-derived dylib slot runtime.
    - Status: landed in `observe/jit-profile-dylib-slot-runtime.fk`.
    - Witness: `observe/tests/jit-profile-dylib-slot-runtime-band.fk` returns
      524287 under `./fkwu --src`.
    - Scope: compact Form bridge from profile/category receipts through the
      self-host compiler and ingress readiness into dylib slot eligibility. It
      keeps the source-runner surface smaller than the full install/call slot
      cell while depending on that slot runtime by receipt. It does not add
      C/Rust or call arbitrary host machine code.
    - Proves: hot direct field, list-head, and array profiles derive exact
      args-vector bytes, stable profile signatures, safe slot records, and
      native/deopt/exception/rewalk/melt routing; warm profiles, hash profiles,
      invalid slots, bad signatures, stale slot-runtime receipts, missing
      source, guard failure, runtime fault, invalidation, parity failure,
      unavailable carrier, carrier fault, and carrier mismatch reject or route
      through the correct non-native path.

19bb. Dylib slot runtime.
    - Status: landed in `observe/jit-dylib-slot-runtime.fk`.
    - Witness: `observe/tests/jit-dylib-slot-runtime-band.fk` returns 262143
      under `./fkwu --src`.
    - Scope: Form-owned slot table over the dylib carrier ABI. It converts
      concrete install/call attempts into carrier install records and carrier
      call records, binds them to generationed cache signatures, and validates
      hits before routing through the dylib carrier. It does not add C/Rust or
      call arbitrary host machine code.
    - Proves: checked field and array install/call attempts can become safe
      dylib slots; slot signatures, cache hits, valid/callable/source/mapped
      state, carrier ownership, install safety, tagged args, and generation are
      required; invalid slots, missing callability, wrong owner carriers, and
      signature misses refuse native; successful status routes native, guard
      failure deopts, runtime faults throw, invalidation rewalks, stale slots
      melt, parity failure deopts, unavailable carriers deopt, faults throw,
      and mismatches reject.

19ba. Dylib carrier ABI packet.
    - Status: landed in `observe/jit-dylib-carrier-abi.fk`.
    - Witness: `observe/tests/jit-dylib-carrier-abi-band.fk` returns 2097151
      under `./fkwu --src`.
    - Scope: concrete Form-owned ABI for a future `libform_jit_carrier.dylib`:
      `fk_jit_install`, `fk_jit_call`, `fk_jit_drop`,
      `fk_jit_flush_icache`, and `fk_jit_status_v1`. The carrier may own OS
      memory permissions and the function-pointer jump; Form owns the emitted
      bytes, tagged arg vectors, source/fault/deopt maps, routing, and the
      no-C-growth proof.
    - Proves: checked field and array byte images are admissible install
      payloads; byte counts, W^X sealed executable pages, non-writable execute
      slots, source/fault/deopt maps, tagged args, callable slots, exact symbol
      names, Form backend ownership, generation, and no-C-growth are required;
      unavailable carriers deopt, fault statuses throw through the exception
      route, mismatches reject, and stale/invalidated/native/deopt paths stay
      explicit.

19az. Install/call attempt packet.
    - Status: landed in `observe/jit-install-call-attempt.fk`.
    - Witness: `observe/tests/jit-install-call-attempt-band.fk` returns
      16777215 under `./fkwu --src`.
    - Scope: Form-only concrete packet for the remaining install/call boundary.
      It names the Form-owned byte image, args-vector ABI, W^X/sealed install
      state, live carrier status, source attribution, exception/deopt/melt
      maps, parity, no-C-growth, and generation. It does not add C/Rust or call
      arbitrary host machine code.
    - Proves: the current attempt packet is shaped and contract-ready but
      pending because arbitrary byte ingress, installed slot, and callable slot
      are not live; checked field and array byte images emitted by the Form
      assembler are admissible attempt payloads; a future complete attempt routes native, guard-deopt,
      runtime-exception, invalidation-rewalk, stale-melt, parity-deopt, and
      unavailable-carrier deopt; carrier mismatch, malformed bytes, bad count,
      bad ABI, untagged args, missing W^X, missing install/call facts, or bad
      receipts reject before completion.

19ay. Live carrier probe.
    - Status: landed in `observe/jit-live-carrier-probe.fk`.
    - Witness: `observe/tests/jit-live-carrier-probe-band.fk` returns 1048575
      under `./fkwu --src`.
    - Scope: Form-only live observation of the existing `native_call_test`
      carrier. It accepts correct execution or honest unavailability, rejects
      mismatches, and does not expose arbitrary byte ingress or add C/Rust.
    - Proves: the current carrier status is admissible and routes either native
      on correct execution or deopt on unavailable architecture; mismatch,
      missing source/maps/W^X, or zero generation rejects; future correct
      carrier status routes native, guard-deopt, runtime-exception,
      invalidation-rewalk, stale-melt, and parity-deopt.

19aw. Self-host JIT runtime loop.
    - Status: landed in `model/jit-self-host-runtime-loop.fk`.
    - Witness: `model/tests/jit-self-host-runtime-loop-band.fk` returns
      524287 under `./fkwu --src`.
    - Scope: compact Form-native runtime loop for the JIT compiler compiling
      and admitting its own hot checked-access paths. It derives hot field,
      cons-list, and array plans from profile receipts, emits stage1 user
      bytes, re-enters the compiler as a compiler-subject plan for stage2
      bytes, requires stage1/stage2 byte-probe equality, then gates ingress,
      native-call readiness, cache state, final readiness, and live
      carrier/install/call evidence. It does not add C/Rust or call host
      machine code.
    - Proves: the self-host fast path is now an explicit loop rather than only
      a receipt summary; current missing carrier/install/call evidence remains
      pending; a future complete loop routes native, guard-deopt,
      runtime-exception, invalidation-rewalk, stale-melt, and parity-deopt;
      warm profiles, hash/container profiles on this checked-access loop,
      missing ingress, missing native-call args, missing final readiness,
      invalid cache state, or missing live call proof reject before completion.

19ax. Source runtime orchestrator.
    - Status: landed in `model/jit-source-runtime-orchestrator.fk`.
    - Witness: `model/tests/jit-source-runtime-orchestrator-band.fk` returns
      1048575 under `./fkwu --src`.
    - Scope: compact Form-native selector joining the concrete profile-derived
      runtime families: checked field/list/array access, dict/hashmap/red-black
      tree containers, and the self-host JIT runtime loop. It does not add
      C/Rust or call host machine code.
    - Proves: source-runtime handoff can require exact checked-access,
      container-profile, and self-host-loop receipts before any native route is
      admitted; current non-live paths stay pending; future live paths route
      native, guard-deopt, runtime-exception, invalidation-rewalk, and
      stale-melt; missing source/category/register/backend/stack/exception/
      install/generation facts or bad self-host receipts reject readiness.

19ac. Self-hosting Form JIT compiler lowerer.
    - Status: landed in `model/jit-self-host-compiler.fk`.
    - Witness: `model/tests/jit-self-host-compiler-band.fk` returns 8191 under
      `./fkwu --src`.
    - Scope: Form-native compiler implementation over `model/form-asm-x64.fk`.
      It does not add C or Rust code and does not install or call host machine
      code.
    - Proves: hot pure Form plans lower to args-vector x64 byte payloads for
      add1, add2, checked array access, checked field access, and checked
      division; cold, impure, foreign, source-missing, and generation-zero
      plans do not emit; compiler-subject plans re-enter the same lowering
      path, so stage2 bytes for the JIT compiler's own array/div lowering
      probes match stage1. This is the first explicit rung for the JIT compiler
      compiling its own hot compiler paths on the bootstrapped `fkwu` surface.

19ad. Container lookup compiler lowering.
    - Status: landed in `model/jit-container-lowering.fk`.
    - Witness: `model/tests/jit-container-lowering-band.fk` returns 2047 under
      `./fkwu --src`.
    - Scope: compact Form-native compiler implementation for dict/hashmap/
      red-black-tree lookup specialization. It avoids C/Rust and does not load
      the larger x64 byte prelude.
    - Proves: hot pure dict, hashmap, and red-black-tree plans lower into
      stable compiler-owned native operation streams; cold, impure, foreign,
      source-missing, and generation-zero plans do not emit; compiler-subject
      hashmap/tree plans re-enter the same lowering path and match user-plan
      probes. This moves the container specialization surface beyond policy
      receipts into compiler output data that later backend byte lanes can
      expand.

19ae. Container backend schedule lowering.
    - Status: landed in `model/jit-container-backend.fk`.
    - Witness: `model/tests/jit-container-backend-band.fk` returns 4095 under
      `./fkwu --src`.
    - Scope: compact Form-native backend scheduler for dict/hashmap/
      red-black-tree lookup streams. It consumes `model/jit-container-lowering`
      output and assigns CPU/GPU register classes and lanes with guard, fault,
      deopt, source, map, and generation metadata. It does not add C/Rust or
      call host machine code.
    - Proves: dict/hash/tree streams schedule on CPU; hash/tree streams also
      schedule on GPU lane plans; invalid targets/ops, missing deopt, empty
      streams, and zero generation reject; compiler-subject schedules re-enter
      the same backend scheduler and match user schedule probes. This moves
      container specialization from compiler-owned op streams into backend
      scheduling data ready for later byte expansion.

19af. Container backend byte-plan admission.
    - Status: landed in `model/jit-container-byteplan.fk`.
    - Witness: `model/tests/jit-container-byteplan-band.fk` returns 4095 under
      `./fkwu --src`.
    - Scope: compact Form-native byte-plan layer for dict/hashmap/red-black-tree
      lookup schedules. It consumes `model/jit-container-backend` schedules and
      emits encoded payload rows with code-size accounting, ABI, relocation,
      safepoint, exception, deopt, root, source, maps, and generation metadata.
      It does not add C/Rust or call host machine code.
    - Proves: CPU dict/hash and GPU red-black-tree schedules expand into
      deterministic Form-owned payload streams; empty schedules, missing deopt,
      empty backend streams, zero generation, missing relocation, and empty
      payloads reject; compiler-subject byteplans re-enter the same byte-plan
      stage and match user byteplan probes. This moves container specialization
      from schedule metadata into admitted backend byte-plan data.

19ag. Container byte-plan loader bridge.
    - Status: landed in `model/jit-container-loader.fk`.
    - Witness: `model/tests/jit-container-loader-band.fk` returns 8191 under
      `./fkwu --src`.
    - Scope: compact Form-native bridge from container byteplans into the
      existing `observe/jit-loader-contract.fk` payload/load/execute contract.
      It passes code-size, target, source, guard, fault, deopt, root, ABI,
      relocation, safepoint, exception, and generation metadata without adding
      C/Rust or calling host machine code.
    - Proves: CPU dict/hash and GPU red-black-tree byteplans become accepted
      loader payloads; missing relocation, empty byteplans, zero generation, and
      missing deopt reject before load; accepted plans route native, deopt,
      exception, and rewalk actions through the existing Form loader contract.
      This connects the container-specialized backend path to the repository's
      native loader membrane as executable Form control data.

19ah. Source container runtime pipeline.
    - Status: landed in `model/jit-container-source-runtime.fk`.
    - Witness: `model/tests/jit-container-source-runtime-band.fk` returns
      65535 under `./fkwu --src`.
    - Scope: compact Form-native source-to-runtime pipeline for dict/hashmap/
      red-black-tree container recipes. It starts from source-level hot recipe
      facts, checks profile/category collapse, tiering, inline/frame/access/
      register facts, static cleanliness, source/maps, and generation, then
      lowers through the existing container compiler/backend/byteplan/loader
      path. It does not add C/Rust or call host machine code.
    - Proves: accepted source recipes load for CPU dict/hash and GPU tree;
      source runtime actions route native, guarded deopt, runtime exception,
      invalidation rewalk, stale melt, and parity deopt; cold source, missing
      static analysis, missing category collapse, missing register facts, and
      zero generation reject before native dispatch. This is the first
      container-specialized path that begins at hot source recipe facts and
      reaches runtime action selection through Form implementation data.

19ai. Profile-derived container runtime pipeline.
    - Status: landed in `model/jit-container-profile-runtime.fk`.
    - Witness: `model/tests/jit-container-profile-runtime-band.fk` returns
      32767 under `./fkwu --src`.
    - Scope: compact Form-native planner that derives container source-runtime
      recipes from existing profile/category receipts plus explicit static,
      tier, and inline admission tickets before entering the container
      compiler/backend/byteplan/loader path. It does not add C/Rust or call
      host machine code.
    - Proves: dict/hashmap/red-black-tree receipts derive accepted runtime
      recipes, including GPU tree targeting; accepted profile-derived plans
      route native, guard deopt, runtime exception, invalidation rewalk, stale
      melt, and parity deopt; static admission failures, non-container category
      receipts, non-specializing tier decisions, missing inline guard, and zero
      generation reject before dispatch. This replaces hand-fed source facts
      with receipt-derived facts carried by compact Form admission tickets.

19aj. Profile container replacement-runtime bridge.
    - Status: landed in `model/jit-container-replacement-runtime.fk`.
    - Witness: `model/tests/jit-container-replacement-runtime-band.fk` returns
      255 under `./fkwu --src`.
    - Scope: compact Form-native bridge from profile-derived container plans
      into replacement-runtime payload-readiness control. It mirrors the
      separately witnessed replacement-runtime byte/readiness invariants while
      staying small enough to compose with the profile container path under
      `fkwu --src`. It converts admitted container byteplans into
      replacement-compatible byte lists and checks installed/generation/source/
      map readiness. It does not add C/Rust or call host machine code.
    - Proves: dict/hashmap/red-black-tree profile plans become replacement
      payloads; GPU tree plans reach replacement readiness; pending slots,
      bad static admission, zero generation, and non-container categories reject
      before replacement handoff. Lifecycle routing remains witnessed by the
      profile-runtime and replacement-runtime integration bands.

19ak. Profile checked-access runtime bridge.
    - Status: landed in `model/jit-access-profile-runtime.fk`.
    - Witness: `model/tests/jit-access-profile-runtime-band.fk` returns
      65535 under `./fkwu --src`.
    - Scope: compact Form-native planner that derives field, cons-list slot,
      and indexed array checked-access payload requests from existing profile
      receipts plus explicit static/inline admission facts, then routes them through
      the existing checked-access payload and loader contract. It does not add
      C/Rust or call host machine code.
    - Proves: hot field/list/array profile receipts become replacement-ready
      checked-access payloads with exact Form-emitted checked field and checked
      array/list bytes; native, guard-deopt, runtime-exception,
      invalidation-rewalk, stale-melt, and parity-deopt actions route through
      the existing Form loader contract; warm profiles, hash/container
      profiles, bad static tickets, zero generation, and missing inline guards
      reject before native dispatch.

19al. Profile-derived self-host compiler bridge.
    - Status: landed in `model/jit-self-host-profile-runtime.fk`.
    - Witness: `model/tests/jit-self-host-profile-runtime-band.fk` returns
      4095 under `./fkwu --src`.
    - Scope: compact Form-native bridge from profile/category receipts into
      `model/jit-self-host-compiler.fk`. It derives compiler plans for hot
      field, cons-list, and array access from receipts instead of hand-fed
      source rows, then requires compiler-subject stage2 lowering to match
      stage1 byte probes. It does not add C/Rust or call host machine code.
    - Proves: hot profile receipts produce safe self-host compiler plans and
      exact args-vector checked field/list/array bytes; compiler-subject
      field/array plans re-enter the same lowering path with matching stage2
      bytes; warm profiles, hash/container profiles, missing static admission,
      missing source maps, and zero generation reject before compiler emission.

19am. Self-host compiler ingress-runtime bridge.
    - Status: landed in `model/jit-self-host-ingress-runtime.fk`.
    - Witness: `model/tests/jit-self-host-ingress-runtime-band.fk` returns
      524287 under `./fkwu --src`.
    - Scope: compact Form-native runtime-admission bridge for bytes emitted by
      the profile-derived self-host compiler path. It validates exact
      args-vector checked field/list/array byte shapes, ingress readiness,
      W^X/install state, and runtime action routing. It does not add C/Rust or
      call `fk_nat_install`/`fk_native_call`.
    - Proves: profile-derived self-host compiler bytes for field, cons-list,
      and array access become ingress-ready runtime payloads only with source,
      maps, positive generation, ingress, W^X, and installed-slot facts;
      native, guard-deopt, runtime-exception, invalidation-rewalk, stale-melt,
      and parity-deopt actions route in Form; warm profiles, hash/container
      profiles, bad static/source admission, missing ingress, missing W^X, and
      uninstalled slots reject before native dispatch.

19an. Self-host native-call runtime bridge.
    - Status: landed in `model/jit-self-host-native-call-runtime.fk`.
    - Witness: `model/tests/jit-self-host-native-call-runtime-band.fk` returns
      16383 under `./fkwu --src`.
    - Scope: compact Form-native bridge from profile-derived self-host ingress
      readiness into `observe/jit-native-call-plan.fk`. It gates native-call
      readiness/action routing on exact self-host compiler bytes, ingress,
      W^X, installed-slot facts, source/maps, and positive generation. It does
      not install or call host machine code.
    - Proves: profile-derived self-host field/list/array bytes can enter the
      native-call plan only after the self-host ingress bridge is ready; native,
      guard-deopt, runtime-exception, invalidation-rewalk, stale-melt,
      parity-deopt, and no-carrier deopt route through the existing native-call
      contract; warm profiles, missing ingress, missing native-call args, and
      zero generation reject before native dispatch.

19ao. Self-host host-membrane runtime bridge.
    - Status: landed in `model/jit-self-host-host-membrane-runtime.fk`.
    - Witness: `model/tests/jit-self-host-host-membrane-runtime-band.fk`
      returns 32767 under `./fkwu --src`.
    - Scope: compact Form-native bridge from profile-derived self-host
      native-call readiness into the host-membrane status/action contract
      separately witnessed by `observe/jit-host-membrane-readiness.fk`. It keeps
      the current host membrane pending unless byte ingress, args-vector
      ingress, W^X, source/exceptions, deopt/melt, parity, and generation facts
      are present, and it still does not add C/Rust or call arbitrary host
      bytes.
    - Proves: current live carrier status is admissible but pending behind the
      incomplete host membrane; a future complete membrane routes native,
      guard-deopt, runtime-exception, invalidation-rewalk, stale-melt,
      parity-deopt, unavailable-carrier deopt, and mismatch rejection; warm
      profiles and membranes missing byte ingress, args-vector ingress, or W^X
      remain blocked before native dispatch.

19ap. Self-host final-native-gate bridge.
    - Status: landed in `model/jit-self-host-final-native-gate.fk`.
    - Witness: `model/tests/jit-self-host-final-native-gate-band.fk` returns
      131071 under `./fkwu --src`.
    - Scope: compact Form-native bridge from the profile-derived self-host
      host-membrane path into the final-native-gate contract separately
      witnessed by `observe/jit-final-native-gate.fk`. It preserves the
      distinction between contract-ready and live-native-complete: the current
      incomplete host membrane remains pending rather than complete. It does
      not add C/Rust or call host machine code.
    - Proves: self-host field/list/array compilation reaches final-gate
      contract readiness with source, lowering, runtime, parity, exceptions,
      no-C-growth, witness, and generation facts; a future complete host
      membrane routes native, deopt, exception, rewalk, melt, unavailable, and
      mismatch outcomes; warm profiles, missing lowering/runtime/exceptions,
      C-growth, and zero generation reject before completion.

19aq. Self-host live-execution evidence bridge.
    - Status: landed in `model/jit-self-host-live-evidence.fk`.
    - Witness: `model/tests/jit-self-host-live-evidence-band.fk` returns
      262143 under `./fkwu --src` when the compact live-evidence helper is
      loaded before `model/jit-self-host-final-native-gate.fk`.
    - Scope: compact Form-native bridge from the self-host final-native-gate
      path into the live-execution proof-suite contract separately witnessed by
      `observe/jit-live-execution-evidence.fk`. It keeps live execution pending
      until carrier, install, call, exception, deopt, melt, parity, source,
      no-C-growth, and generation proofs are all present.
    - Proves: the self-host chain reaches live-evidence contract readiness;
      current missing carrier/install/call evidence remains pending; a future
      complete proof suite routes native, deopt, exception, rewalk, melt, and
      parity-deopt; missing carrier/install/call/exception/deopt/melt/parity,
      C-growth, and zero generation reject before live completion.

19ar. Self-host container live-evidence bridge.
    - Status: landed in `model/jit-self-host-container-live-evidence.fk`.
    - Witness: `model/tests/jit-self-host-container-live-evidence-band.fk`
      returns 4194303 under `./fkwu --src`.
    - Scope: compact Form-native bridge from profile-derived dict/hashmap/
      red-black-tree container replacement plans into the live-evidence proof
      shape. It reuses the existing container lowering/backend/byteplan/loader
      and compact replacement-runtime readiness cells; because the current
      source-runner envelope can drop late definitions after the full container
      stack, the band performs the live route matrix directly over already-live
      predicates. It does not add C/Rust or call host machine code.
    - Proves: dict, hashmap, and GPU red-black-tree profile receipts produce
      installed replacement-ready payloads with stable Form-owned byte shapes;
      current missing carrier/install/call evidence remains pending; a future
      complete container proof suite routes native, guard-deopt,
      runtime-exception, invalidation-rewalk, stale-melt, and parity-deopt;
      missing carrier/install/call, uninstalled slots, bad static tickets, zero
      generation, and non-container array plans reject before live completion.

20. Full Form-native JIT witnessed.
    - Status: pending.
    - Needs: runtime integration so hot source recipes lower through the
      Form-owned IR/backend bundles/byte streams into the existing host
      install/call membrane, dispatch native, throw source-attributed runtime
      exceptions, deopt/melt safely, and pass the repo's native witness sweep
      without C-lowering as the optimizer source.
