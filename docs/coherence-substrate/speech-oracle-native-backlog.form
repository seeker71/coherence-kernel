# speech-oracle-native-backlog.form -- remaining oracle-held speech gaps

(speech-oracle-native-backlog

  (body-state
    (native-sema-voice-organs 6)
    (selected-arms 4)
    (major-form-components 14)
    (native-neural-weight-parameters-admitted 0)
    (c-seed-growth 0)
    (meaning "native Form speech body is present; neural weight recipe-data has not been admitted"))

  (gaps
    (live-open-dictation
      (local-oracle "4/4 = 100%, WER 0")
      (native "0/4 = 0%, WER 100")
      (next "train-live-segmented-open-asr-source")
      (target "native-segmented-acoustic-learning")
      (route "oracle-guide"))

    (sema-live-voice
      (local-oracle "1/1 = 100%, WER 0; witnessed host teacher sample")
      (native "0/1 = 0%, WER 100")
      (next "render-and-oracle-next")
      (target "text-conditioned-acoustic-vocoder-candidate-search")
      (route "oracle-guide")))

  (neural-weight-backlog
    (admitted 0)
    (pending-lanes 2)
    (native-open-asr "pending recipe-data")
    (native-acoustic-vocoder "pending recipe-data")
    (admission-rule "consented local recipe-data only"))

  (rule "local oracle teaches until native receipts win")

  (runnable
    (concat
      learn/speech-oracle-native-backlog.fk
      learn/tests/speech-oracle-native-backlog-band.fk)
    (verdict 32767)))
