# Native proof trace

`fkwu` owns Python compilation, process supervision, trace admission and terminal rendering. The Rust sibling executes the independent specimen and supplies its real dispatch JSON. This is a proof utility; it does not run the living body on the sibling.

From the repository root, provide one JSON stdin line:

```sh
form-run ./fkwu observe/kernel-trace-run.bml <<'EOF'
{"source":"form/form-kernel-ts/seedbank/python-adapter/examples/python_demo.py","columns":100}
EOF
```

`source` accepts an existing `.fk` or `.py` specimen. `columns` is optional and defaults to 100, with a minimum of 40. The source remains unchanged. A private evidence directory retains exact trace JSON, the rendered panel, process status and cleanup observations. Python input is prepared into a private Form recipe before tracing.

The panel displays the result, exact dispatch counts, one-decimal shares, descending variant rows and targets at **at least 10%** of total dispatches. Shared `term-graphics.bml` owns tones, bars and duration formatting. The reader refuses malformed JSON, invalid counters and variants whose counts do not sum to the observed total. A literal-only program reports zero dispatches without inventing targets. Elapsed display uses the shared duration precision; `trace.out` retains the sibling's full microsecond measurement.

The native source compiler door publishes a reusable `.fk` after successful compilation in the current `fkwu` process:

```sh
form-run ./fkwu observe/python-specimen-compile-run.bml <<'EOF'
{"source":"form/form-kernel-ts/seedbank/python-adapter/examples/python_demo.py","output":"/tmp/python-demo.fk"}
EOF
```

The output directory must exist. The Form parser, lifter and emitter execute directly through their native source dependencies. Canonical ontology bindings own category identity. The file compiler holds complete UTF-8 source in memory and embeds the Form class runtime from its single BML authority. Unsupported compilation refuses before publication, preserving an existing output. `compilation.json` retains the actual process identity; compilation creates no additional compiler process.

`python_demo.py` and its compiled `.fk` return **40949**. A literal-only program produces zero dispatches. Dispatch counts describe the actual emitted specimen and are read from its current trace. Threshold equality and nonconserving trace refusal are observed independently of the rendering.

The [native compiler witness](../observe/python-native-compiler-witness.bml) drives the public door and executes its output on `fkwu`, including class dispatch, inheritance, `super`, child-owned fields and explicit missing-member or arity refusal. The shell `kernel-bmf-compile` still serves the separate parity suite, whose reference execution and direct Python interpreter responsibilities require their own observed migration.

The north star is reusable native compilation with complete source identity, adaptable Form emission and direct resource ownership. A proof sibling contributes independent agreement; Form owns compilation and the public utility.
