# Release History

This is the release history for the present floor. Receipts keep the full
exploration trail; this file keeps only what still describes the current state.

## 2026-07-05 - Current Audio Floor Regrounded

- Rebuilt `fkwu` and re-ran the grounding floor: `42`, `55`, freshness `15`,
  and native-vs-rented `11111`.
- Reframed `NORTH_STAR.md` as the repo-wide organic intelligence target:
  native choice/fail/cut/stop/timeout, satsang, consent, minimal kernel, host
  membrane, observability, trust, sovereignty, vitality, play, wonder, and
  local/remote oracle review/training. Voice is one organ inside that body.
- Root-caused broad direct-source OOMs to a C checkout-witness parser progress
  bug after BML-style call desynchronization. Added a bounded parse diagnostic
  with byte, line, column, source size, cursor nodes, and context. The permanent
  owner remains native/Form source intake plus framebuffer frames.
- Extended the source-intake framebuffer policy witness so parser-progress
  failures have a Form-side source-frame shape; its band returns `16383`.
- Kept the legacy bounded audio gate as sidecar-reading evidence rather than
  executing the monolithic contract in the arena lane.
- Current bounded legacy gate: `4095/4095`, promotion authority `0`.
- Added `presence/fkwu-local-audio-loop.fk`, a Form-owned local
  talk/listen/translate loop over host stand-ins: `say`, `ffmpeg`,
  `whisper-cli`, and bounded local Ollama. Its fast band returns `16383`; the
  live run wrote `audio-training-runs/current/fkwu-local-audio-loop/summary`
  with source WER `0`, reply WER `0`, and promotion authority `0`.
- Added `presence/fkwu-audio-task-surface.fk`, which maps that loop into typed
  speech-task slots and observes the existing audio.cpp ASR/TTS adapter evidence
  through one readback surface. Its pure band returns `4095`; the live readback
  returns `65534/65535` until the local-loop summary is refreshed, with the next
  gap named as the real local audio.cpp lane.
- Added `presence/fkwu-production-audio-end-to-end.fk`, the no-stand-in
  borrowed-source audio.cpp lane. It uses Qwen3 TTS, Qwen3 ASR, Qwen3 forced
  alignment, and local `llama3.2:3b` translation. The live run completed with
  score `131071/131071`, WER `0`, transcript `Sema, audio, path is alive.`, and
  translation `Sema, Audio, Pfad ist lebendig.` The source mode is explicitly
  `borrowed_public_demo`.
- Current route: Form/BML control plane over borrowed audio.cpp ASR,
  forced-aligner, and TTS voice-reference clone runtime.
- OOM position clarified: wide authored Form is valid source; compiler/source
  runner OOM is a runtime boundedness defect and must produce diagnostics.

## 2026-07-04 - Audio Pipeline Blockers Made Visible

- Voice-reference package pipeline became arena-visible.
- Public demo voice reference was named as a blocker at that time; this changed
  on 2026-07-05 when the current local progress lane accepted it as borrowed
  real data.
- Formant oracle WER `100` was reframed as an investigated non-promoting miss:
  heard tokens `0/3`, next repair `token_bearing_acoustic_carrier`.
- audio.cpp TTS WER `0` was reframed as support only: public-demo voice,
  native-rate gap, alignment-confidence gap, and no promotion authority.
- Cycle-72 production-gate OOM was recorded with lane, command, resource, and
  repeat-prevention context.

## 2026-07-03 - audio.cpp Route Observed

- audio.cpp was observed as the local borrowed acoustic runtime surface.
- ASR, forced-aligner, TTS support, confidence-surface, alias review, and
  promotion-blocker packets were added as Form-owned evidence.
- The path changed from "train our own base audio model" to "adapt a borrowed
  working runtime with metadata, source provenance, and promotion gates."

## 2026-06-29 to 2026-07-01 - Kernel Floor

- `fkwu --src` runs real Form source cells from the C checkout witness.
- Native-vs-rented returns `11111`.
- Flatten remains optional speed/parity, not the grounding gate.
- The C seed remains a shrink target; new runtime meaning belongs in
  Form/native-walker cells.
