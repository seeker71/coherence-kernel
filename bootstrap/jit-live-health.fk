; jit-live-health.fk — live source -> native bytes -> installed dispatch health.
; Its source is the authority: patching it makes any older sibling artifact stale.

(defn jit-health-sum (n)
    (if (le n 0)
        0
        (add n (jit-health-sum (sub n 1)))))

(jit-health-sum 100)
