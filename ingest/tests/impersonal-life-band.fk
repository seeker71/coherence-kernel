; tests/impersonal-life-band.fk — The Impersonal Life through the ingest door.
;
; preludes: form-stdlib/core.fk form-stdlib/knowledge-ingest.fk ingest/frequency-ingest-bashar-four-laws.fk ingest/frequency-ingest-impersonal-life.fk
;
; Verdict 4095 — twelve readings at POWERS OF TWO (a decimal-digit fold past
; the 15th reading rounds on the TypeScript arm; 2^53 is the wall).
;
; On Go / Rust / TypeScript / fkwu (four kernels; this room is not in
; fourth-arm-bands.txt — see the recipe header for why that is correct):
;   six distinct teachings                                                 1
;   carried as seven units — the creative-thought teaching is counted
;     once per reading                                                     2
;   six units freeze into body                                             4
;   ONE unit is witnessed and never frozen: the guarantee reading, which
;     says your circumstance was authored by your thinking. Deep, and
;     carrying fear — it becomes self-blame at the moment a person is
;     least able to carry it. Fear is removed before ICE, never before
;     SIGHT                                                                8
;   nothing composts — every unit is deep enough to enter                  16
;   every hand-written verdict equals the one ki-ingest computes, so a
;     verdict in that file cannot drift from what the law decides          32
;   exactly one teaching wears two readings                                64
;   and its two readings land in DIFFERENT places: same claim, two
;     fates. That split is the ingest law doing its whole job             128
;   the witnessed unit is the guarantee reading and no other — the
;     refusal is aimed, not a general suspicion of the book              256
;   every teaching names the body cell it meets; none is orphaned         512
;   the withheld-signature teaching FREEZES, so the body carries the
;     kenosis rather than merely noting it (corpus row 1001)             1024
;   and the homoplasy holds: this cell's witnessed count equals the
;     one frequency-ingest-bashar-four-laws.fk found independently.
;     1914 Akron and a 1980s channeled voice split at the same seam,
;     refused on the same structural ground — read from the sibling
;     cell, so if either moves this reading goes dark               2048

(do
    (ipl-check))
