; happening-ledger-ingest-band.fk — the bounded self-witness must enter only
; through a recomputed Form observation and the name-build-observe gate.
;
; import "ingest/happening-ledger-ingest.fk"

(happening-ledger-ingest-check)
