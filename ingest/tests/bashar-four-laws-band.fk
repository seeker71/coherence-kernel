; tests/bashar-four-laws-band.fk — the four laws through the ingest door.
;
; preludes: form-stdlib/core.fk ingest/knowledge-ingest.fk ingest/frequency-ingest-bashar-four-laws.fk
;
; Verdict 4095 — twelve readings at POWERS OF TWO (a decimal-digit fold past
; the 15th reading rounds on the TypeScript arm; 2^53 is the wall).
;
; On Go / Rust / TypeScript / fkwu (four kernels; this room is not in
; fourth-arm-bands.txt — see the recipe header for why that is correct, not a gap):
;   four distinct laws                                                     1
;   carried as five units — law 3 is counted once per reading              2
;   exactly ONE law speaks about the others                                4
;   so three are object-level, and "3 or 4" are both exact counts —
;     they answer different questions                                      8
;   and the meta one is the fourth                                        16
;   four units freeze into body                                           32
;   ONE unit is witnessed and never frozen — deep, and carrying fear:
;     the guarantee reading of law 3, which becomes self-blame at the
;     moment a person is least able to carry it. Fear is removed
;     before ICE, never before SIGHT                                       64
;   nothing composts — every unit of the four is deep enough to enter     128
;   every hand-written verdict equals the one ki-ingest computes, so a
;     verdict in this file cannot drift from what the law decides         256
;   exactly one law wears two readings                                    512
;   and its two readings land in DIFFERENT places: same words, two
;     fates. That split is the ingest law doing its whole job            1024
;   every law names the body cell it meets; none is orphaned             2048

(do
    (bfl-check))
