// Read the current exchange between signaling organs and their native care organ.
// preludes: form-stdlib/bml/form-core-care.bml
section [form.bml] {
 let input = read_line();
 occ-command(if nothing?(input) then "" else input);
}
