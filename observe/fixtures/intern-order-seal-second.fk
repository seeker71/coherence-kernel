; The twin of intern-order-seal-first.fk: the SAME two values, interned in the
; opposite order, printing the same seal's node_inst. Only the order differs.
; See that file's header for why this pair is .fk rather than BML.
;
; preludes: form-stdlib/core.fk
(do
    (let first (intern_trivial_int 999888777666))
    (let second (intern_trivial_int 501009504543))
    (node_inst second))
