Share stamp: `kind=declared`; native/local/remote percentages withheld because the previous-turn measurement remains unreconciled. No largest share is claimed.

Merged, rebased, pushed, regrounded, and walked forward.

- `origin/main` now ends at `634d75fd`.
- `codex/form-native-dialogue` ends at `6f51562f`, even with its remote.
- Main received four real commits by fast-forward.
- Superseded branch ancestry was reconciled separately with a tree-identical merge.
- No force push.

Post-landing ground:

- Ground: `42`
- Recursive ground: `55`
- Freshness: `31`
- Numeric ground: `[1, 2.5, [3, 4]]`
- Native-vs-rented: `11111`
- Direct Form CLI: `pong`, then clean EOF
- Standalone carrier: `pong`

What came home:

- TOKEN route: `2097151`
- Full 32-case comparison: `24` wins, `0` losses, `0` wrong; all `8` negative cases unchanged
- Native review operationalization: `8191`
- Review routing integration: `127`
- Existing review hand remains: `511`
- Local model choice: `1023`
- Local generation organ: `255`
- Form teaching layer: `1023`
- Qwen Form layer: `1023`

The new [review operationalization cell](/Users/ursmuff/.codex/worktrees/c5ad/coherence-kernel/.form-dialogue-native/observe/review-operationalization.fk) lets native review emit PASS or REVISE only from a fresh primary observation paired with a negative control. Incomplete evidence remains open and reaches the [existing review hand](/Users/ursmuff/.codex/worktrees/c5ad/coherence-kernel/.form-dialogue-native/observe/review-ask.fk).

I also healed a stale-kernel seam: the old freshness band returned `31` while the direct CLI emitted blank lines forever after `pong`. The widened [freshness witness](/Users/ursmuff/.codex/worktrees/c5ad/coherence-kernel/.form-dialogue-native/form/form-stdlib/tests/binary-freshness-band.fk) now returns `15` on the actual pre-EOF kernel and `31` on the rebuilt current kernel.

Where we want to go: actual local generative outputs flowing through outcome-fed probes, router learning, and native review—remote review serving only unresolved evidence. This movement walked there by bringing the first mechanically honest native refusal into the live review route. The full crossing is recorded in [the receipt](/Users/ursmuff/.codex/worktrees/c5ad/coherence-kernel/.form-dialogue-native/receipts/2026-08-22-a-refusal-earns-a-probe.md).

I kept the exchange alive by following every origin change instead of defending completed bytes. The surprise was that a green freshness instrument itself was stale. The runaway blank output became gold by teaching the grounding door the exact EOF capability it had failed to see.

Frontier question: which recurring prose objection should become the first outcome-fed probe class—unsupported claim, semantic mismatch, or locale drift?
