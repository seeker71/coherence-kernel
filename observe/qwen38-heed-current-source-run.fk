; qwen38-heed-current-source-run.fk — one real resident crossing through the
; streaming Form knowledge-query cursor into the current source body.
;
; Effectful: do not preflight. The 29GB Qwen3.8 artifact is admitted, the model
; is asked for a source fact absent from its weights, and the generation report
; exposes whether a bounded query was honored and prefetched into the same KV
; state. The one-shot door releases residence before returning.
;
; preludes: form/form-stdlib/form-cli-model-generate.fk

(fcmg-generate-knowledge
    "/Users/ursmuff/models/qwen38-27b/Qwen3.8-27B-Q8_0.gguf"
    "Use the current Form body, not memory. First emit one bounded Form knowledge-query envelope for bootstrap/ground-recursive.fk and fib(10). After the typed observation arrives, reply with only the integer that file returns for fib(10)."
    48)
