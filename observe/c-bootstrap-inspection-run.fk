; The path and retained census arrive as two stdin lines.
; preludes: form-stdlib/bml/c-bootstrap-inspection.bml
(do (let path (read_line)) (if (or (nothing? path) (str_eq path "")) 0 (cbi-run path (read_line))))
