; Eight bounded ranges per process keep the Form string pool below one GiB.
; Every range commits independently, so interruption loses no acknowledged byte.
;
; preludes: form-stdlib/qwen35-artifact-fetch.fk

(do
    (let result (qaf-batch (qaf-default-spec) 8))
    (print_str (metal_status))
    result)
