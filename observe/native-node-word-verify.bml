// preludes: form/form-stdlib/bml/native-node-word.bml
section [form.bml] {
 if and(eq(nnw-total-bits(), 64), str_eq(nnw-header(), read_file("runtime/fkwu-node-word.h"))) then 1 else form_error("native node word header differs from its Form authority");
}
