; host-exec-starved-stdin-probe.fk — run me inside a fork-starved fkwu; my
; final value is (nothing? r) for a host-exec that was handed stdin bytes and
; whose sh could not launch: 1 starved, 0 unstarved (cat echoes "hello").
; The stdin lane opens a second pipe before it forks; a launch that never
; happened must still answer the axiom-1 nothing through it, never "".
;
; Driven by form/form-stdlib/tests/host-exec-stdin-band.fk through
;   ulimit -u 1; exec ./fkwu observe/host-exec-starved-stdin-probe.fk
; witnessed: 2026-09-04 -> 1 starved, 0 unstarved
(do (let r (host-exec "cat" "hello"))
    (nothing? r))
