// preflight-exec: forbidden
// One manifest FILE PATH on stdin. File reading preserves requests larger than
// the host's line buffer. Frozen native assessments; no rented fallback.
// preludes: form-stdlib/bml/form-cli-response-session.bml
section [form.bml] {
 print_str(frss-file(read_line()));
 0;
}
