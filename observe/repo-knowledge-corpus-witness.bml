// Reuse the publishing organ's live checks, without training or state writes.
// preludes: learn/repo-knowledge-corpus.bml
section [form.bml] {
 print_str(str_concat(json-emit-precise(rkc-check(nsm-json(nsm-at(rkc-home(),"current.json")))),"\n"));
}
