; metal-bandwidth-probe.fk — the Metal stream ceiling, read on fkwu rather than
; through form-cli, so the binary is not part of the question.
; preludes: native/metal/model-bandwidth.fk form-stdlib/model-registry.bml
(do
    (print (mbw-run (mr-path (mr-find "qwen38-q8"))))
    0)
