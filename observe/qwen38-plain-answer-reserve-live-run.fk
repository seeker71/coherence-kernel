; qwen38-plain-answer-reserve-live-run.fk — one genuine ordinary local turn
; through the two-ledger plain door.  The initial 24 tokens may answer directly
; or form a bounded source query; only a typed observation opens the held 32.
;
; Effectful: opens the sealed local Qwen artifact and drives this fkwu's Metal
; carrier. Run one residence at a time; do not preflight.
; preludes: form/form-stdlib/form-cli-model-generate.fk form/form-stdlib/model-registry.bml

(fcmg-generate
    (mr-path (mr-find "qwen38-q8"))
    "Use the current Form body. First emit exactly one <|form:knowledge-query|>bootstrap/ground.fk<|/form:knowledge-query|>. After the typed observation arrives, reply with only the integer that bootstrap/ground.fk returns."
    24)
