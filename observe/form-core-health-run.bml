// The core health interface, callable without admitting a model.
// preludes: form-stdlib/bml/form-core-health.bml
section [form.bml] {
 let input = read_line();
 ohc-command(if nothing?(input) then "" else input);
}
