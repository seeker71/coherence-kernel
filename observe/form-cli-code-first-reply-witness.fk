; Real native Qwen first-role check over public, synthetic two-document data.
; One admission, at most 128 generated IDs, explicit release; stdin model name.
; The returned synthetic text is caller output, not framebuffer content.
; preflight-exec: forbidden
; preludes: form-stdlib/bml/form-cli-code-repair-fixture.bml
(print (fcrf-first (read_line)))
