; One request, or "stream" followed by requests until EOF/release.
; preflight-exec: forbidden
; preludes: form-stdlib/bml/form-cli-heal.bml
(do (let request (read_line))
    (if (str_eq request "stream") (fh-stream 0)
        (print_str (fh-command request))))
