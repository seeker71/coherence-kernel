; Diagnostic reading with comments omitted and source line numbers retained.
; preludes: form-stdlib/bml/c-bootstrap-inspection.bml
(do (let path (read_line)) (if (or (nothing? path) (str_eq path "")) 0 (cbi-review-source path (read_line))))
