; Emits the retrieval-assisted prompt only after the exact source group rehashes.
; preludes: form-stdlib/form-knowledge-exec-diagnostic.fk

(do
    (let prompt (fked-rag-prompt))
    (if (nothing? prompt) 1
        (do (print_str prompt) 0)))
