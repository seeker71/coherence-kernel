; host-exec-starved-measure-probe.fk — I measure a host-exec answer WITHOUT
; asking nothing? first, on purpose. Unstarved I answer the length of a
; timestamp (bytes spoke). Fork-starved, the launch never happened, the answer
; is nothing, and str_len dies loud naming the illness — rc 1, no counterfeit
; 0. Silent error hides illness; this probe exists so a band can witness the
; illness speaking.
;
; Driven by form/form-stdlib/tests/host-exec-launch-honesty-band.fk through
;   ulimit -u 1; exec ./fkwu observe/host-exec-starved-measure-probe.fk
; witnessed: 2026-08-27 -> rc 1 starved (str_len refuses the absence),
;                          rc 0 unstarved
(str_len (host-exec "date +%s%N" ""))
