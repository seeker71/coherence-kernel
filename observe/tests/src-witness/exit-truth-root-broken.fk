; exit-truth-root-broken.fk -- fixture for src-exit-truth-band.fk:
; INTENTIONALLY broken. xt-nowhere resolves in no unit of the chain, so the
; authoritative flat compile must print its error, tally it, record it in the
; .sym lens, and exit nonzero -- all four surfaces agreeing.
; preludes: exit-truth-prov.fk exit-truth-dep.fk
(xt-nowhere)
