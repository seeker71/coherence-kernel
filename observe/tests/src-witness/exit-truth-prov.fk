; exit-truth-prov.fk -- fixture for src-exit-truth-band.fk: the provider unit.
; Standalone-compilable (0 errors alone); it exists so exit-truth-dep.fk has a
; name to resolve inside the root's flat chain but NOT alone.
(defn xt-prov-one () 1)
