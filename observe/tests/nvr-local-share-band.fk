; nvr-local-share-band.fk — eight prompts stay off the network.
;
; Verdict 1023:
;   1      eight sources still sit
;   2      "what does it say?" is 100 native
;   4      the what-batch needs no network
;   8      emit is not claimed as native
;   16     emit is nothing, not remote
;   32     the old router would have escalated emit
;   64     that remote call is saved
;   128    the jungle kit for these prompts is ready
;   256    prompt 0's what-ask does not need the net
;   512    complete pairs are still zero — no fake summaries
;
; preludes: form-stdlib/core.fk form-stdlib/form-first-router.fk form-stdlib/native-vs-rented.fk observe/nvr-metric.fk observe/nvr-corpus.fk observe/nvr-local-share.fk

(do (nvrl-check))
