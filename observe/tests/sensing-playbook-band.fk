; sensing-playbook-band.fk — the shelf, and the chooser that will not offer what is shut.
;
; preludes: form-stdlib/core.fk observe/sensing-playbook.fk
;
; Grounded 2026-07-23 00:26 on R5CW20DK17A and this host: 18 of 20 android sensors report
; `perm: n`; metal_matvec_fixture answers metal_linked=false; the camera enumerates and
; times out.
;
; Verdict 511 when, on Go / Rust / TypeScript / fkwu:
;     1  ten recipes on the shelf
;     2  three of them built with green bands
;     4  five reachable today with no permission and no subscriber
;     8  two blocked, each carrying its own measured reason
;    16  feeling MOVING offers five: insonation, cadence, bearing, altitude, heading
;    32  feeling DARK offers four
;    64  a state nobody wrote for still gets the three that suit any state
;   128  five recipes suit "any" state and only three are offered — the gap of two is
;        exactly the blocked pair, the shelf's regret made visible as a number
;   256  the gpu row's status is 0, and its summary is the reason rather than a shrug
;
; The teaching in bit 128: a catalogue becomes a wish list the moment it lists what you
; cannot take. Both blocked recipes suit "any" state perfectly and are apt for everything;
; neither is ever offered, because an option you cannot exercise is not an option, it is a
; regret. What they keep instead is their reason, so the obstruction is not rediscovered
; from scratch by whoever asks next.

(do
    (sensing-playbook-check))
