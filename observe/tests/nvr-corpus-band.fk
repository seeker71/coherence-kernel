; nvr-corpus-band.fk — eight held-out sources; no summaries; no ≥ claim.
;
; Verdict 1023:
;   1      eight sources sit
;   2      every row is held-out
;   4      complete pairs are zero
;   8      the metric floor is not met
;   16     filling native with the rented line is refused
;   32     a pair with both lines and scores is complete
;   64     scoring a pair with empty summaries does not complete it
;   128    the seed cannot earn ≥ rented
;   256    a refused launder leaves the source untouched
;   512    the first row is held-out
;
; preludes: form-stdlib/core.fk observe/native-vs-rented.fk observe/nvr-metric.fk observe/nvr-corpus.fk

(do (nvrc-check))
