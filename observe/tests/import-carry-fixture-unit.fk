; tests/import-carry-fixture-unit.fk — a unit that stands alone, so the
; import lane compiles it to a standalone .fkb image and imports it. It is
; half of import-carry-band.fk's pair: the imported half. Pure natives only,
; no preludes — its closure is exactly itself.

(do
    (defn icfu-answer () 41)
    (defn icfu-twice (x) (add x x))
    0)
