; How few comparison primitives this kernel needs. Eight digits, low to high: le is
; derivable from lt; gt is; ge is; and on numbers eq is too, so arithmetic alone would
; put the floor at one primitive; but the derived eq is NOT four-way, because three
; kernels decline to order `nothing`; while real eq on nothing IS four-way; so the
; minimal four-way-honest set is {eq, lt}, exactly two; and the kernel carries three
; today, so `le` (tag 5) is one over and is a named shrink target.
import "../comparison-primitive-reduction.fk"
; witnessed: 2026-07-28 -> 11111111, four-way (go/rust/ts all 11111111)
(comparison-reduction-check)
