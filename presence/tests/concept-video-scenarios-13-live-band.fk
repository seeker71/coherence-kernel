; concept-video-scenarios-13-live-band.fk -- 16 real scenario photographs.
; expected: 4095
; witnessed: 2026-07-18 -> 4095 after corrected aggregate tuple indices
; preludes: form/form-stdlib/core.fk presence/concept-video-scenarios-13-live.fk

(do
  (let result (concept-video-scenarios-13-live-run))
  (let carrier (nth result 1))
  (let fixtures (nth result 2))
  (let scenarios (nth result 3))
  (let targets (nth result 4))
  (let claims (nth result 5))
  (add
    (if (eq (nth carrier 1) 1) 1 0)
    (add (if (eq (nth carrier 2) 1) 2 0)
      (add (if (eq (nth fixtures 1) 16) 4 0)
        (add (if (eq (nth fixtures 3) 16) 8 0)
          (add (if (eq (nth fixtures 5) 16) 16 0)
            (add (if (eq (nth scenarios 1) 8) 32 0)
              (add (if (eq (nth targets 1) 16) 64 0)
                (add (if (eq (nth targets 4) 15) 128 0)
                  (add (if (eq (nth targets 7) 14) 256 0)
                    (add (if (eq (nth targets 11) 48) 512 0)
                      (add (if (eq (nth claims 10) 10) 1024 0)
                           (if (and (eq (nth claims 1) 6)
                                (and (eq (nth claims 3) 6)
                                     (eq (nth claims 5) 5))) 2048 0))))))))))))
)
