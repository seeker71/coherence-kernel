# Coherence Network Form application

`api.bml` is the canonical native API catalog consumed by the sibling kernel
front doors. The catalog belongs with the Form program and travels in the
kernel submodule; the consumer repository supplies deployment configuration,
not a second source copy.

The catalog was moved byte-for-byte from the former consumer-owned deployment
path. Its SHA-256 at the move boundary is
`099482334714e2394fbd57ce4045158031960257a52dec8b0e70a8d625a1c9e1`.
