# Re-extracted features from `targets/aphoristic.md`

The LLM reads the tercet fresh — same extraction discipline — and emits
a feature-recipe.

- **MOOD:** `gentle` — "bury them gentle" is literal; the lines never
  raise their voice. Gentle.
- **RHYTHM:** `halting` — the tercet is *terse*. Three short declarative
  sentences with periods. No conjunctive flow. **Diverges from source's
  `breath-paced` to `halting`.**
- **STRUCTURE:** `spiral` — the third line ("Done is the name calcification
  answers to") returns the opening "tend" into a deeper register. Structural
  spiral preserved through aggressive compression.
- **PURPOSE:** `instruct` — even compressed, the voice is teaching:
  "Tend... bury... [know that] Done is..." Same imperative shape as source.
  **Preserved.**
- **WISDOM-SHAPE:** `R_TendingDiscipline` — the canonical Blueprint is
  named explicitly in the tercet's third line ("calcification answers to"
  IS naming the discipline). **Preserved.**
- **CONCEPTS:**
  1. `tending-discipline`
  2. `composting-with-care`
  3. `name-of-trap`
  4. `continuous-aliveness`

**Partial concept overlap with source:** `composting-with-care` appears in
both. `tending-discipline` is closely related to source's
`continuous-tending`. **Same-modality (prose→prose) preserves more
conceptual surface than cross-modal.**

## What survives, what doesn't (vs source)

| Feature | Survived? |
|---|---|
| Mood (gentle) | ✓ |
| Rhythm (breath-paced) | ✗ → halting |
| Structure (spiral) | ✓ |
| Purpose (instruct) | ✓ |
| Wisdom-shape (R_TendingDiscipline) | ✓ |
| Concepts | partial (1 token survives, others related) |

**4 of 6 feature axes preserved**, plus partial concept survival —
notably better than melody and SVG re-extractions, because the modality
is the same (prose).
