; qwen4exp-flash-next-graph-jit-band.fk — emitted architecture organs.
;
; preludes: form/native/metal/qwen4exp-flash-next-graph-jit.fk form/form-stdlib/tests/iq2xs-msl-band.fk
;
; Verdict 1023. Live compilation is observed separately.

(do
    (let unit (q4gj-msl))
    (let c0 (iq2xmb-bit (eq (iq2xmb-count unit "kernel void q4_hc_repeat") 1) 1))
    (let c1 (iq2xmb-bit (eq (iq2xmb-count unit "kernel void q4_hc_group_rms (") 1) 2))
    (let c2 (iq2xmb-bit (eq (iq2xmb-count unit "kernel void q4_hc_low_silu") 1) 4))
    (let c3 (iq2xmb-bit (eq (iq2xmb-count unit "kernel void q4_hc_gated_mean") 1) 8))
    (let c4 (iq2xmb-bit (eq (iq2xmb-count unit "kernel void q4_hc_combine") 1) 16))
    (let c5 (iq2xmb-bit (eq (iq2xmb-count unit "kernel void q4_ple_signed_sqrt_gate") 1) 32))
    (let c6 (iq2xmb-bit (eq (iq2xmb-count unit "kernel void q4_ple_dilated_conv_add") 1) 64))
    (let c7 (iq2xmb-bit (iq2xmb-all (list
        (eq (iq2xmb-count unit "history[b+0u]") 1)
        (eq (iq2xmb-count unit "history[b+3u]") 1)
        (eq (iq2xmb-count unit "history[b+6u]") 1)
        (eq (iq2xmb-count unit "history[b+8u]=cur") 1))) 128))
    (let c8 (iq2xmb-bit (iq2xmb-all (list
        (eq (iq2xmb-count unit "2.0f / (1.0f + fexp(-inject[s] / float(streams)))") 1)
        (eq (iq2xmb-count unit "acc / float(streams)") 1))) 256))
    (let c9 (iq2xmb-bit (iq2xmb-all (list
        (eq (iq2xmb-count unit "q4_flash_iq2_slot4off") 1)
        (eq (iq2xmb-count unit "q4_flash_iq3_slot4") 1)
        (eq (iq2xmb-count unit "q4_flash_iq4nl_selected_slot4") 1)
        (eq (iq2xmb-count unit "q4_hc_group_rms_simd") 1)
        (eq (iq2xmb-count unit "q4_moe_route_tg") 1)
        (eq (iq2xmb-count unit "#include <metal_stdlib>") 1))) 512))
    (add c0 (add c1 (add c2 (add c3 (add c4 (add c5 (add c6 (add c7 (add c8 c9))))))))))
