(do (defn cosine (a b) (do (let dot 0.0) (do (let norm_a 0.0) (do (let norm_b 0.0) (do (let i 0) (do (let n (len a)) (do (do (defn _while_0 (dot norm_a norm_b i) (if (lt i n) (do (do (let dot (_plus dot (mul (_get a i) (_get b i)))) (let norm_a (_plus norm_a (mul (_get a i) (_get a i)))) (let norm_b (_plus norm_b (mul (_get b i) (_get b i)))) (let i (_plus i 1))) (_while_0 dot norm_a norm_b i)) (list dot norm_a norm_b i))) (let _while_1_result (_while_0 dot norm_a norm_b i)) (let dot (nth _while_1_result 0)) (let norm_a (nth _while_1_result 1)) (let norm_b (nth _while_1_result 2)) (let i (nth _while_1_result 3))) (do (let denom (mul (math_sqrt norm_a) (math_sqrt norm_b))) (if (gt denom 0.0) (do (let score (div dot denom)) (if (gt score 1.0) (do (let score 1.0) (if (lt score 0.0) (do (let score 0.0) score) score)) (if (lt score 0.0) (do (let score 0.0) score) score))) (do (let score 0.5) (if (gt score 1.0) (do (let score 1.0) (if (lt score 0.0) (do (let score 0.0) score) score)) (if (lt score 0.0) (do (let score 0.0) score) score)))))))))))) (let contributor_vec (list 0.6 0.0 0.8 0.0 0.0 0.0)) (let idea_vec (list 0.8 0.0 0.6 0.0 0.0 0.0)) (cosine contributor_vec idea_vec))
