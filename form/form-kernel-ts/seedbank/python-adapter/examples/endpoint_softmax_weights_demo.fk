(do (defn softmax_weights (scores temperature) (do (let n (len scores)) (if (eq n 0) (list) (do (let max_s (_get scores 0)) (do (let i 1) (do (do (defn _while_0 (max_s i) (if (lt i n) (do (do (if (gt (_get scores i) max_s) (let max_s (_get scores i)) false) (let i (_plus i 1))) (_while_0 max_s i)) (list max_s i))) (let _while_1_result (_while_0 max_s i)) (let max_s (nth _while_1_result 0)) (let i (nth _while_1_result 1))) (if (le temperature 0.0) (do (let result (list)) (do (let i 0) (do (do (defn _while_5 (result i) (if (lt i n) (do (do (if (eq (_get scores i) max_s) (let result (_list_append result 1.0)) (let result (_list_append result 0.0))) (let i (_plus i 1))) (_while_5 result i)) (list result i))) (let _while_6_result (_while_5 result i)) (let result (nth _while_6_result 0)) (let i (nth _while_6_result 1))) result))) (do (let exps (list)) (do (let i 0) (do (do (defn _while_1 (exps i) (if (lt i n) (do (do (let exps (_list_append exps (math_exp (div (sub (_get scores i) max_s) temperature)))) (let i (_plus i 1))) (_while_1 exps i)) (list exps i))) (let _while_2_result (_while_1 exps i)) (let exps (nth _while_2_result 0)) (let i (nth _while_2_result 1))) (do (let total 0.0) (do (let i 0) (do (do (defn _while_2 (total i) (if (lt i n) (do (do (let total (_plus total (_get exps i))) (let i (_plus i 1))) (_while_2 total i)) (list total i))) (let _while_3_result (_while_2 total i)) (let total (nth _while_3_result 0)) (let i (nth _while_3_result 1))) (if (eq total 0.0) (do (let uniform (div 1.0 n)) (do (let result (list)) (do (let i 0) (do (do (defn _while_4 (result i) (if (lt i n) (do (do (let result (_list_append result uniform)) (let i (_plus i 1))) (_while_4 result i)) (list result i))) (let _while_5_result (_while_4 result i)) (let result (nth _while_5_result 0)) (let i (nth _while_5_result 1))) result)))) (do (let result (list)) (do (let i 0) (do (do (defn _while_3 (result i) (if (lt i n) (do (do (let result (_list_append result (div (_get exps i) total))) (let i (_plus i 1))) (_while_3 result i)) (list result i))) (let _while_4_result (_while_3 result i)) (let result (nth _while_4_result 0)) (let i (nth _while_4_result 1))) result))))))))))))))))) (let scores (list 1.0 2.0 3.0)) (let temperature 1.0) (softmax_weights scores temperature))
