; session-app-room.fk — the one direct Form entry for the Sessions room.
; Finder, Explorer, and a direct fkwu run all enter the same body here.
import "core.fk"
import "session-evidence.fk"
import "session-app.fk"
import "session-app-live.fk"

(session-app-serve)
