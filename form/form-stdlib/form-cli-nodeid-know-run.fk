; form-cli-nodeid-know-run.fk — print a knowledge query answered as an offer.
;
; preludes: form-stdlib/form-cli-nodeid-know.fk

(do
    (print_str (fnk-know-speak))
    (print_str (str_concat "fnk-check=" (str_concat (int_to_str (fnk-check)) "\n")))
    0)
