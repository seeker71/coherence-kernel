//////////////////////////////////////////////////////////////////////
//
// form-cli-unique-mint.bml — high-grammar authority.
//
// What is unique in this body is not a slogan. Channel, protocol,
// axiom, satsang, nothing, timeout, trust through transparency,
// choice and undo as primitives, and embodied memory without a
// long reasoning chain — each already lives as a cell. This mill
// mints LoRA fuel from those cells. Combinatorial N. Leakage 0.
// No sealed prompt. No chat transcript. LoRA writer stays 0.
//
// Executable: form-stdlib/form-cli-unique-mint.bml
// Proven by: form-stdlib/tests/form-cli-unique-mint-band.fk
//
//////////////////////////////////////////////////////////////////////

package Form.cli.uniquemint;

import Form.kernel.*;
import Form.cli.flow.*;

enum SampleSplit [public] {
   TRAIN,
   HELDOUT
}

interface IUniqueMint [public, witness, reusable] {
   section [public] {
      const int MixA = 7;
      const int MixB = 13;
      const int NSkills = 9;
      const int LoraWriter = 0;
      const String TrustRule = "mint from unique Form cells; observe source lesson and transform lineage separately; LoRA tensors wait on lift";

      int Mix( int nI, int nMod ) [abstract];
      String Situation( String strClass, int nI, String strSplit ) [abstract];
      Object LessonIdentity( String strClass, int nI ) [abstract];
      int Leakage( Object hRows ) [abstract];
      int LessonLeakage( Object hRows ) [abstract];
      int Unique( Object hRows ) [abstract];
      int LoraWriterReady() [abstract];
   }
}

template SampleRow<TClass> [public, reusable] {
   section [public] {
      String Split() [get];
      TClass Class() [get];
      String Situation() [get];
      Object SourceId() [get];
      Object LessonId() [get];
      Object TransformId() [get];
   }
}

class FormUniqueMint [public, reusable, teach] : IUniqueMint {
   section [public] {
      const String ExecutablePath = "form/form-stdlib/form-cli-unique-mint.bml";

      int Mix( int nI, int nMod ) [abstract] {}
      String Situation( String strClass, int nI, String strSplit ) [abstract] {}
      Object LessonIdentity( String strClass, int nI ) [abstract] {}
      int Leakage( Object hRows ) [abstract] {}
      int LessonLeakage( Object hRows ) [abstract] {}
      int Unique( Object hRows ) [abstract] {}
      int LoraWriterReady() [abstract] {}
   }
}

class FormUniqueMintSelf [public, selfHosting, teach, observable] {
   section [public] {
      const String CoreName = "form-cli-unique-mint-self";
      const String Pattern = "combinatorial-mint source-overlap-visible lesson-lineage-disjoint transform-observed overlay-fit lora-identity-waits";

      bool Observable() [witness] { return true; }
      bool Executable() [witness] { return true; }
      bool Trustable() [witness] { return true; }
   }
}
