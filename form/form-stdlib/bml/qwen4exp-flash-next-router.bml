//////////////////////////////////////////////////////////////////////
//
// qwen4exp-flash-next-router.bml — high-grammar authority.
//
// Qwen3.8-Flash-Next names architecture qwen4exp. Its routed MoE has
// 512 experts and selects 10. This authority holds that exact shape,
// the softmax/top-k/selected-renormalization contract, and the refusal
// just beyond the bounded Metal transcription.
//
// Executable: form-stdlib/qwen4exp-flash-next-router.bml
// Proven by: form-stdlib/tests/qwen4exp-flash-next-router-band.fk
//
//////////////////////////////////////////////////////////////////////

package Form.native.qwen4exp;

import Form.kernel.*;
import Form.native.metal.*;

interface IQwen4ExpRouter [public, witness, reusable] {
   section [public] {
      const int ExpertCount = 512;
      const int SelectedCount = 10;
      const int RefusedExpertCount = 513;
      const String Architecture = "qwen4exp";
      const String Routing = "softmax top-k selected-renormalization best-first";

      Object RouteIds( Object logits ) [abstract];
      Object RouteWeights( Object logits ) [abstract];
      bool WithinMetalRadius( int experts, int selected ) [abstract];
   }
}

class Qwen4ExpFlashNextRouter [public, reusable, teach, observable] : IQwen4ExpRouter {
   section [public] {
      const String ExecutablePath = "form/form-stdlib/qwen4exp-flash-next-router.bml";
      const String Model = "Qwen3.8-Flash-Next";

      Object RouteIds( Object logits ) [abstract] {}
      Object RouteWeights( Object logits ) [abstract] {}
      bool WithinMetalRadius( int experts, int selected ) [abstract] {}
   }
}

class Qwen4ExpFlashNextRouterSelf [public, selfHosting, teach, observable] {
   section [public] {
      const String CoreName = "qwen4exp-flash-next-router-self";
      const String Pattern = "512-experts top-10 softmax selected-renormalization bounded-metal-refusal";

      bool Observable() [witness] { return true; }
      bool Executable() [witness] { return true; }
      bool Trustable() [witness] { return true; }
   }
}
