//////////////////////////////////////////////////////////////////////
//
// native-model-dual-telemetry.bml — truth-separated resident model rows.
//
// Executable: form-stdlib/native-model-dual-telemetry.bml
// Proven by: form-stdlib/tests/native-model-dual-telemetry-band.fk
//
//////////////////////////////////////////////////////////////////////

package Form.native.modeltelemetry;

import Form.kernel.*;
import Form.glass.telemetry.*;

interface INativeModelDualTelemetry [public, witness, resident] {
   section [public] {
      const String AccessRule = "publication is not access; read and write epochs remain absent until a carrier observes the corresponding event";
      const String SubstateRule = "expert and KV topology derived from a parent model handle is derived-window, never physical-live";
      const String ProvenanceRule = "physical model handles, catalog weight bytes, declared pressure budget, and release observations occupy separate samples";
      const String TombstoneRule = "a clean close atomically replaces the owner publication with released physical tombstones";

      Object Resident( String id, String lifecycle, Int handle ) [abstract];
      Object WeightCatalog( String id, String parent, Int bytes ) [abstract];
      Object DerivedSubstate( String id, String parent, String kind, String purpose, String lifecycle, Int handle, Object channels ) [abstract];
      Object Budget( Int bytes ) [abstract];
      Object Released( String id, Int closeResult ) [abstract];
   }
}

class NativeModelDualTelemetry [public, resident, observable, teach] : INativeModelDualTelemetry {
   section [public] {
      const String ExecutablePath = "form/form-stdlib/native-model-dual-telemetry.bml";
      Object Resident( String id, String lifecycle, Int handle ) [abstract] {}
      Object WeightCatalog( String id, String parent, Int bytes ) [abstract] {}
      Object DerivedSubstate( String id, String parent, String kind, String purpose, String lifecycle, Int handle, Object channels ) [abstract] {}
      Object Budget( Int bytes ) [abstract] {}
      Object Released( String id, Int closeResult ) [abstract] {}
   }
}
