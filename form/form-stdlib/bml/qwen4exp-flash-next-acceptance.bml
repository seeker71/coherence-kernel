//////////////////////////////////////////////////////////////////////
//
// qwen4exp-flash-next-acceptance.bml — performance and ownership law.
//
// The rates are the same-host llama.cpp b10686 measurements from the
// exact downloaded UD-Q2_K_XL artifact.  They are an oracle boundary,
// not a runtime dependency.  Form earns the crossing only when its own
// generated-token and prompt-processing rates reach ninety percent.
//
//////////////////////////////////////////////////////////////////////

package Form.native.qwen4exp;

import Form.kernel.*;
import Form.native.metal.*;

interface IQwen4ExpFlashNextAcceptance [public, witness, reusable] {
   section [public] {
      const String Architecture = "qwen4exp";
      const String Backend = "form-native-metal-jit";
      const String ForbiddenRuntime = "llama-server";
      const int RequiredPercent = 90;
      const int LlamaDecodeTokpsX1000 = 26580;
      const int LlamaPrefillTokpsX1000 = 318830;
      const int DecodeFloorTokpsX1000 = 23922;
      const int PrefillFloorTokpsX1000 = 286947;
      const int DecodeSampleTokens = 64;
      const int PrefillSampleTokens = 5632;
      const int FormDecodeTokpsX1000 = 12188;
      const int FormSequentialPrefillTokpsX1000 = 12340;
      const String CurrentStatus = "functional-native-runtime; performance-gate-failed";

      bool DecodeAccepted( int tokpsX1000 ) [abstract];
      bool PrefillAccepted( int tokpsX1000 ) [abstract];
      bool RuntimeAccepted( String backend, String dependency ) [abstract];
   }
}

class Qwen4ExpFlashNextAcceptance [public, reusable, teach, observable] : IQwen4ExpFlashNextAcceptance {
   section [public] {
      const String ExecutablePath = "form/form-stdlib/qwen4exp-flash-next-acceptance.bml";
      const String Model = "Qwen3.8-Flash-Next UD-Q2_K_XL";

      bool DecodeAccepted( int tokpsX1000 ) [abstract] {}
      bool PrefillAccepted( int tokpsX1000 ) [abstract] {}
      bool RuntimeAccepted( String backend, String dependency ) [abstract] {}
   }
}
