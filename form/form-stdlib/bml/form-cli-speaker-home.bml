//////////////////////////////////////////////////////////////////////
//
// form-cli-speaker-home.bml — high-grammar authority.
//
// One 2026-08-26 census observed 95 remote events whose recurring shape was
// the speaker picking the next tool. That count is evidence, never a target.
// That speaker comes home as Form: planner overlay names the move,
// generate carries Speaker: plus Control:, rank-1 A/B seeds from
// planner fuel. Native NL voice is still pending. fqt-lora? stays 0.
//
// Executable: form-stdlib/form-cli-speaker-home.bml
// Proven by: form-stdlib/tests/form-cli-speaker-home-band.fk
//
//////////////////////////////////////////////////////////////////////

package Form.cli.speakerhome;

import Form.kernel.*;
import Form.cli.flow.*;

interface ISpeakerHome [public, witness, reusable] {
   section [public] {
      const int ObservedRemoteEvents = 95;
      const int FqtLora = 0;
      const int NativeVoice = 0;
      const String TrustRule = "speaker is local planner; every remote denominator is re-censused";

      String Next( String strSit ) [abstract];
      int Act( String strMove ) [abstract];
      String Prompt( String strSit ) [abstract];
      int Home() [abstract];
   }
}

template SpeakerTurn<TMove> [public, reusable] {
   section [public] {
      TMove Move() [get];
      int Act() [get];
      String Prompt() [get];
   }
}

class FormSpeakerHome [public, reusable, teach] : ISpeakerHome {
   section [public] {
      const String ExecutablePath = "form/form-stdlib/form-cli-speaker-home.bml";

      String Next( String strSit ) [abstract] {}
      int Act( String strMove ) [abstract] {}
      String Prompt( String strSit ) [abstract] {}
      int Home() [abstract] {}
   }
}

class FormSpeakerHomeSelf [public, selfHosting, teach, observable] {
   section [public] {
      const String CoreName = "form-cli-speaker-home-self";
      const String Pattern = "planner-overlay speaker-line generate-door fqt-lora-stays-0";

      bool Observable() [witness] { return true; }
      bool Executable() [witness] { return true; }
      bool Trustable() [witness] { return true; }
   }
}
