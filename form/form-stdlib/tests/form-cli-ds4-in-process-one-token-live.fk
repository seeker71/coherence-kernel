; One-token admission witness for the exact inquiry. This exercises the same
; Form prompt/token/result route as form-cli-live without stdin scaffolding.
;
; preludes: form-stdlib/core.fk form-stdlib/form-cli-dsv4-local.fk

(do
  (print_str
    (fdl-render
      "does trust require the Trinity to be knownable, since knowing requires time to valid in the reasoning plane?"
      1))
  0)
