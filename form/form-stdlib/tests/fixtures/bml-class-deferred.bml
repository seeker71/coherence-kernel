// Canonical source: the thesis's method attributes (line 644) name no
// deferred; line 723 names delegate deferred calls, which the compiler
// generates. A method marked deferred runs as written, and the compiler organ
// voices that the flag is carried without a meaning.
// Answer: 3
class A : Object { section [public] { int Later() [deferred] { return 3; } } }
return new A().Later();
