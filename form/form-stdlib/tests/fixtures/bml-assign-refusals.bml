// Assignments and indexes the compiler already sees are wrong, named before
// any table exists: an assignment to a name never declared, to a const, to a
// const from inside a for, an element past the end, an element of a const, an
// element of a value that is no array, an index past the end of a literal,
// and an index into a value that is no array.
// Answer: "BML-HATI-UNSUPPORTED assign/unbound,assign/const,assign/const,index/out-of-range,assign/const,index/not-array,index/out-of-range,index/not-array"
const int k = 2;
const int j = 1;
const int[] c = #(1, 2);
int[] a = #(1, 2);
int x = 3;
y = 3;
k = 3;
for (int i = 0; i < 2; i++) { j = 5; }
a[2] = 5;
c[0] = 5;
x[0] = 1;
const int past = #(3, 5)[2];
const int scalar = k[0];
return 1;
