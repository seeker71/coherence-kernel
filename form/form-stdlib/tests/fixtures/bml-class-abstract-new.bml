// Canonical source: a class declared abstract makes no record, even when
// every method it has is implemented; new names it and is refused.
// Answer: "BML-HATI-UNSUPPORTED new/abstract-class"
class A [abstract] : Object { section [public] { int V() { return 1; } } }
return new A().V();
