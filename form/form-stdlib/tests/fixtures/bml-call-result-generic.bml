// Canonical source: a generic method's call result carries its declared
// return type, not its type parameter: Same<T> returns int, so Pick(int)
// answers.
// Answer: 1.
int Pick(int x) { return 1; }
int Pick(String s) { return 2; }
int Same<T>(T x) { return 7; }
return Pick(Same("a"));
