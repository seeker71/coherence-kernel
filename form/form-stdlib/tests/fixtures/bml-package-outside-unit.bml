// A package declaration belongs to a unit's scope: one inside a method body
// stands outside any unit and is named before any table exists.
// Answer: "BML-HATI-UNSUPPORTED package/outside-unit"
int F() { package C; return 1; }
return F();
