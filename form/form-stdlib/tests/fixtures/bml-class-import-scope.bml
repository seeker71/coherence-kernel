// Canonical source: a class reached through an import lowers its bodies in its
// own unit. K lives in package ScopeLib, which imports ScopeDeep.*; K.Get calls
// ScopeDeep's Helper, a name this unit does not import and cannot see.
// Units: bml-class-import-scope-lib.bml bml-class-import-scope-deep.bml
// Answer: 42.
import ScopeLib.*;
return new K().Get();
