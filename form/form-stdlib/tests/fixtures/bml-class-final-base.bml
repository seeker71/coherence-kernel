// Canonical source: a final class takes no subclass. C names the final B as
// its base; the unit is refused where its layout is built, and never runs.
// Answer: "BML-HATI-UNSUPPORTED class/extends-final"
class B [final] : Object { section [public] { int V() { return 1; } } }
class C : B { section [public] { } }
return new B().V();
