// A caller's parameter stays invisible to the callee's body.
// Answer on both lanes: "BML-HATI-UNSUPPORTED name/unbound"
int Inner(int n) { return n + m; }
int Outer(int m) { return Inner(m); }
return Outer(1);
