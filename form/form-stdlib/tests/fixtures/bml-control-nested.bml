// Control reached through a container that is not control, a choose branch
// holding an if, is named before any table exists.
// Answer: "BML-HATI-UNSUPPORTED control/nested"
choose { fail; } , { if (1) return 2; }
return 3;
