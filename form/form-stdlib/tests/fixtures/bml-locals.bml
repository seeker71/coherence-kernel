// Lexical consts: each const binds its own name, a later const leaves an
// earlier one standing, and the return reads them all.
// Answer: 18
// (x + 1 = 3, y = 5, a + 6 = 10)
const int x = 2;
const int y = 5;
const int a = 4;
return x + 1 + y + a + 6;
