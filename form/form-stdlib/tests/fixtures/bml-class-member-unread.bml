// Canonical source: a class member the reader cannot take no longer vanishes
// with the rest of its section: the unit answers source/unread, and the
// compiler organ voices the words it could not read.
// Answer: "BML-HATI-UNSUPPORTED source/unread"
class P : Object { section [public] { int = 3; int V() { return 1; } } }
return new P().V();
