// A parameter list whose last parameter has no name does not read. The reader
// stops at the method and says so; no partial program runs.
// Answer: "BML-HATI-UNSUPPORTED source/unread"
int Bad(int a, int) { return a; }
return 1;
