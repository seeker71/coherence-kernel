// Canonical source: a private class constant reads only in its own class's
// code; from outside, Base.Secret is refused and names its access.
// Answer: "BML-HATI-UNSUPPORTED access/private"
class Base : Object { section [public] { const int Secret [private] = 7; int Own() { return Secret; } } }
return new Base().Own() + Base.Secret;
