// The native floor: a method returns its value, and a choose whose first
// branch fails answers through the branch that succeeds.
// Answer: 15
// (Seven() = 7, then 8)
int Seven() { return 7; }
choose { fail; } , { return Seven() + 8; }
