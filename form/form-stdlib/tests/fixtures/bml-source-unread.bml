// Canonical source: a statement the reader cannot take. The reader keeps what
// it read and closes the unit with an unread mark; the lowering answers
// source/unread and voices the unread tokens through the compiler organ,
// instead of running the part before the stop without a word.
// Answer: "BML-HATI-UNSUPPORTED source/unread".
int x = 1;
int y = 1 + ;
return x;
