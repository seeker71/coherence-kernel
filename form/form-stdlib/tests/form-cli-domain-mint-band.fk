; form-cli-domain-mint-band.fk — domain cells mint diverse samples; overlay fits.
;
; Verdict 1023:
;   1..128  fdm-check (n, mix, leakage, unique, split, heldout>=9, lora-writer=0, scales)
;   256     class descriptor FormDomainMint minted (generic T)
;   512     high-grammar authority has package/interface/class/const/template
;
; preludes: form-stdlib/core.fk form-stdlib/language-model.fk
; preludes: form-stdlib/form-cli-qwen-teach-layer.fk
; preludes: form-stdlib/form-cli-domain-mint-xtal.fk
; witnessed: 2026-08-26 -> 1023; minted heldout 11/12 leakage 0; lora-writer 0

(do
    (defn fdmb-has-str? (s needle)
        (if (ge (str_find s needle 0) 0) 1 0))
    (defn fdmb-has-name? (xs name)
        (if (nil? xs) 0
            (if (str_eq (head xs) name) 1
                (fdmb-has-name? (tail xs) name))))

    (let bits (fdm-check))
    (let desc (FormDomainMint))
    (let src (read_file "form/form-stdlib/bml/form-cli-domain-mint.bml"))
    (let exe (read_file "form/form-stdlib/form-cli-domain-mint.bml"))
    (let b8 (if (and (str_eq (nth desc 1) "FormDomainMint")
                     (and (eq (fdmb-has-name? (nth desc 2) "T") 1)
                          (and (eq (fdmb-has-name? (nth desc 3) "fdm-corpus") 1)
                               (eq (fdmb-has-name? (nth desc 4) "MixA") 1))))
                256 0))
    (let b9 (if (and (eq (fdmb-has-str? src "package Form.cli.domainmint") 1)
                     (and (eq (fdmb-has-str? src "interface IDomainMint") 1)
                          (and (eq (fdmb-has-str? src "class FormDomainMint") 1)
                               (and (eq (fdmb-has-str? src "const int MixA") 1)
                                    (and (eq (fdmb-has-str? src "template SampleRow") 1)
                                         (and (eq (fdmb-has-str? exe "heldout-v3") 0)
                                              (eq (fdmb-has-str? exe "turn_started") 0)))))))
                512 0))
    (add bits (add b8 b9)))
