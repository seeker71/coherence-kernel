(do
    (print_str
        (metal_model_open
            "/Users/ursmuff/models/ds4/ds4flash-v5mx-reap25-type40-mxfp8lt-dspark-v1.gguf"
            "85445672960 86586540032 2"))
    (print_str
        (metal_model_library
            "identity"
            "#include <metal_stdlib>\nusing namespace metal;\nkernel void identity(device const float* input [[buffer(0)]], device float* output [[buffer(1)]], uint i [[thread_position_in_grid]]) { output[i] = input[i]; }\n"))
    (print_str (metal_model_close))
    0)
