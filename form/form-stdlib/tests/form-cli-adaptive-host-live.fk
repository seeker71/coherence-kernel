; Live carrier probe: host-exec must return stdout text before the adaptive
; generator can own a local or remote oracle membrane.

(do
    (let out (host-exec "tr '[:lower:]' '[:upper:]'" "local-carrier-alive"))
    (if (str_eq out "LOCAL-CARRIER-ALIVE") 19 0))
