; form-cli-model-decode-pulse-band.fk -- opaque progress is a kernel frame.
; preludes: form-stdlib/form-cli-model-session.fk

(do
    (framebuffer-clear)
    (let pulse (fcms-decode-pulse))
    (if (and (eq pulse 0)
             (eq (len (framebuffer-events)) 1)) 1 0))
