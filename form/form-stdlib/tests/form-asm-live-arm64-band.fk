; preludes: form-stdlib/core.fk form-stdlib/form-asm.fk
;
; Live ARM64 JIT carrier witness. Form emits `add w0,w0,#2; ret`: tagged
; integer 41 enters as 82 and leaves as 84, which the Value ABI renders as 42.
(native_call_bytes
    (fa-image (list (fa-add 0 0 2) (fa-ret)))
    41)
