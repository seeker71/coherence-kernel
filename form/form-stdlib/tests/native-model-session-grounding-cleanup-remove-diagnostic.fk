; Public three-process filesystem diagnostic, phase 2: expose the exact
; recursive-removal return without asking the same process for post-state.
; preludes: form-stdlib/form-fs.fk

(do
    (let root
        (fs-path-join (fs-temp-dir)
            "native-model-session-grounding-cleanup-diagnostic-1786205299000"))
    (fs-remove-tree root))
