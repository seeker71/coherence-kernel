; form-cli-organ-mint-band.fk — remaining organs mint diverse samples; overlay fits.
;
; Verdict 1023:
;   1..128  fom-check (n, mix, leakage, unique, split, heldout>=9, lora-writer=0, scales)
;   256     class descriptor FormOrganMint minted (generic T)
;   512     high-grammar authority has package/interface/class/const/template
;
; preludes: form-stdlib/core.fk form-stdlib/language-model.fk
; preludes: form-stdlib/form-cli-qwen-teach-layer.fk
; preludes: form-stdlib/form-cli-organ-mint-xtal.fk
; witnessed: 2026-08-26 -> 1023; minted heldout 11/12 leakage 0; lora-writer 0

(do
    (defn fomb-has-str? (s needle)
        (if (ge (str_find s needle 0) 0) 1 0))
    (defn fomb-has-name? (xs name)
        (if (nil? xs) 0
            (if (str_eq (head xs) name) 1
                (fomb-has-name? (tail xs) name))))

    (let bits (fom-check))
    (let desc (FormOrganMint))
    (let src (read_file "form/form-stdlib/bml/form-cli-organ-mint.bml"))
    (let exe (read_file "form/form-stdlib/form-cli-organ-mint.bml"))
    (let b8 (if (and (str_eq (nth desc 1) "FormOrganMint")
                     (and (eq (fomb-has-name? (nth desc 2) "T") 1)
                          (and (eq (fomb-has-name? (nth desc 3) "fom-corpus") 1)
                               (eq (fomb-has-name? (nth desc 4) "MixA") 1))))
                256 0))
    (let b9 (if (and (eq (fomb-has-str? src "package Form.cli.organmint") 1)
                     (and (eq (fomb-has-str? src "interface IOrganMint") 1)
                          (and (eq (fomb-has-str? src "class FormOrganMint") 1)
                               (and (eq (fomb-has-str? src "const int MixA") 1)
                                    (and (eq (fomb-has-str? src "template SampleRow") 1)
                                         (and (eq (fomb-has-str? exe "heldout-v3") 0)
                                              (eq (fomb-has-str? exe "turn_started") 0)))))))
                512 0))
    (add bits (add b8 b9)))
