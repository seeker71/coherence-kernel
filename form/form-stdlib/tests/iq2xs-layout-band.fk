; iq2xs-layout-band.fk — the BML-authored address contract for GGML type 17.
;
; preludes: form-stdlib/core.fk form-stdlib/iq2xs-layout.bml
;
; Verdict 1023: the directly cached BML self-check returns 511, including the slot4
; execution map, while the authority remains high BML. No derived source twin exists.

(do
    (let c0 (iq2x-check))
    (let c1 (if (str_eq (IQ2XSAuthorityKind) "high-bml-interface-class-const") 512 0))
    (add c0 c1))
