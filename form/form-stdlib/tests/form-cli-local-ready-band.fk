; form-cli-local-ready-band.fk — named diagnostics walk local, not remote.
;
; Verdict 1023:
;   1..128  flr-check (named marks, controls, walk, heldout)
;   256     class descriptor FormLocalReady minted (generic T)
;   512     high-grammar authority has package/interface/class/const/template
;
; preludes: form-stdlib/core.fk form-stdlib/language-model.fk
; preludes: form-stdlib/form-cli-router.fk
; preludes: ../form-cli-local-ready.bml
; witnessed: 2026-08-24 -> 1023 on fkwu, exit 0; unresolved/unbalanced walk local

(do
    (defn flrb-has-str? (s needle)
        (if (ge (str_find s needle 0) 0) 1 0))
    (defn flrb-has-name? (xs name)
        (if (nil? xs) 0
            (if (str_eq (head xs) name) 1
                (flrb-has-name? (tail xs) name))))

    (let bits (flr-check))
    (let desc (FormLocalReady))
    (let src (read_file "form/form-stdlib/bml/form-cli-local-ready.bml"))
    (let b8 (if (and (str_eq (nth desc 1) "FormLocalReady")
                     (and (eq (flrb-has-name? (nth desc 2) "T") 1)
                          (and (eq (flrb-has-name? (nth desc 3) "flr-walk") 1)
                               (eq (flrb-has-name? (nth desc 4) "UnbalancedMark") 1))))
                256 0))
    (let b9 (if (and (eq (flrb-has-str? src "package Form.cli.localready") 1)
                     (and (eq (flrb-has-str? src "interface ILocalReady") 1)
                          (and (eq (flrb-has-str? src "class FormLocalReady") 1)
                               (and (eq (flrb-has-str? src "const String UnbalancedMark") 1)
                                    (eq (flrb-has-str? src "template MissCell") 1)))))
                512 0))
    (add bits (add b8 b9)))
