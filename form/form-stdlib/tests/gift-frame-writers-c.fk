; gift-frame-writers-c.fk — one of the three writers of gift-frame-writers-band.fk.
; It gives twenty thousand "c" into the band's frame four hundred times, as fast
; as it can, so a reader meets a give in flight. Three cells rather than one
; taking a letter: the seed has no argument door, and a writer whose letter came
; from a shared file once raced its siblings and wrote an empty payload, which
; read as a corrupt frame and was my own probe lying (2026-09-07).
;
; preludes: form/form-stdlib/form-glass-gift-frame.bml

(do
    (defn gfwc-rep (n acc) (if (eq n 0) acc (gfwc-rep (sub n 1) (str_concat acc "c"))))
    (defn gfwc-loop (n h payload) (if (eq n 0) 0 (do (let s (ggf-give h payload)) (gfwc-loop (sub n 1) h payload))))
    (let payload (gfwc-rep 20000 ""))
    (let h (ggf-offer "gift-frame-writers-band.frame" 262144))
    (let done (gfwc-loop 400 h payload))
    (print (ggf-release h)))
