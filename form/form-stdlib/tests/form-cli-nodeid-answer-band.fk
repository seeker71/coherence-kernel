; form-cli-nodeid-answer-band.fk — delayed node-id pointer.
;
; Verdict 2047:
;   1    pointer kind is recipe
;   2    naming holds evaluation
;   4    cost is outside the LLM token stream
;   8    embedding keeps the pointer held
;   16   JIT is not ready before demand
;   32   JIT is ready after demand
;   64   demand keeps the recipe kind
;   128  speak names recipe held outside
;   256  held force is nothing — evaluation has not happened
;   512  demand then force of (add 2 3) is 5
;   1024 demand keeps cost outside the LLM stream
;
; preludes: form-stdlib/core.fk form-stdlib/form-cli-nodeid-answer.fk
;
; witnessed: 2026-08-30 -> 2047

(do (fnid-check))
