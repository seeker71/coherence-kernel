; Form owns the view plan. The linked Metal port performs only the irreducible
; OS/Metal calls and returns an observable receipt in the same invocation.

(do
  (print_str
    (metal_model_open
      "/Users/ursmuff/models/ds4/ds4flash-v5mx-reap25-type40-mxfp8lt-dspark-v1.gguf"
      "85445672960 86586540032 2"))
  (print_str (metal_model_close))
  0)
