; qwen35-tokenizer-band.fk — tokenizer and non-thinking chat layout extracted
; from the observed Qwen3.8 GGUF, with an exact ASCII round trip.
;
; preludes: form-stdlib/qwen35-tokenizer.fk
(do
    (defn q35-band-list-eq (a b)
        (if (eq (len a) 0) (if (eq (len b) 0) 1 0)
            (if (eq (len b) 0) 0
                (if (eq (head a) (head b))
                    (q35-band-list-eq (tail a) (tail b))
                    0))))
    (let src (eqr-of-file "/Users/ursmuff/models/qwen38-27b/Qwen3.8-27B-Q8_0.gguf" 0 40000000))
    (let ids (q35-encode src "Hello, Form."))
    (let chat (q35-chat-ids src "Hello"))
    (sum (list
        (if (q35-band-list-eq ids (list 9419 11 3268 13)) 1 0)
        (if (str_eq (tkz-decode-text src ids) "Hello, Form.") 2 0)
        (if (eq (q35-special-id src "<|endoftext|>") 248044) 4 0)
        (if (eq (q35-special-id src "<|im_start|>") 248045) 8 0)
        (if (eq (q35-special-id src "<|im_end|>") 248046) 16 0)
        (if (eq (q35-special-id src "<think>") 248068) 32 0)
        (if (eq (q35-special-id src "</think>") 248069) 64 0)
        (if (q35-band-list-eq chat (list 248045 846 198 9419 248046 198 248045 74455 198
                                           248068 271 248069 271)) 128 0))))
