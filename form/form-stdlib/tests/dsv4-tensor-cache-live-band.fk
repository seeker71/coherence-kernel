; Isolate the Form-native DS4 tensor-table cache from pipeline, buffer, and
; generation setup.  A live local-model band: the path is this body's observed
; DS4 fixture, and no model arithmetic or remote oracle is entered.
; preludes: native/metal/dsv4-decode-stack.fk
(do
  (let path "/Users/ursmuff/models/ds4-engine/gguf/DeepSeek-V4-Flash-IQ2XXS-w2Q2K-AProjQ8-SExpQ8-OutQ8-chat-v2-imatrix.gguf")
  (let src (dfd-src path))
  (let loaded (dfd-stt-cache-load path src))
  (list (nothing) 1001100001 (nth loaded 0) (len (nth loaded 1))
        (if (and (eq (nth loaded 0) 1) (eq (len (nth loaded 1)) 1328)) 1 0)))
