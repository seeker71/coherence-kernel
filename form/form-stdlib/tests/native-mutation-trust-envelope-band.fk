; tests/native-mutation-trust-envelope-band.fk -- trust residual envelope.
;
; preludes: form-stdlib/core.fk form-stdlib/native-mutation-trust-envelope.fk
;
; Band verdict: 11111 when prediction error is carried as residual,
; side-effect intents are named, choice success is explicit, and ordinary
; traffic is native-default while explicit fallback stays reversible.

(do
    (nmte-test))
