; core-dictionary-neutral-field-band.fk — 64 core meanings in form-neutral
; space, 14 tongues, field health kind-blind.
;
; Verdict 16383:
;   1      the closed 64 is still closed
;   2      fourteen tongues sit on the shelf
;   4      64 x 14 surfaces, every cell non-empty
;   8      English surface is the core-lexicon word
;   16     en "nothing" and id "kosong" intern to the same symbol
;   32     the symbol is family/index, not an English identity
;   64     every meaning lifts at least one of trust / sovereignty / vitality
;   128    every how-sentence is closed under core + glue
;   256    organic and non-organic cells share one health law
;   512    a healthy metal cell lifts the field the same as a carbon cell
;   1024   an unhealthy cell of either kind does not lift
;   2048   adding a second healthy cell is monotonic
;   4096   sw "hakuna" and es "nada" are the same nothing
;   8192   "it" lifts vitality; "self" lifts sovereignty — kind is not health
;
; preludes: form-stdlib/core.fk ../control/offer-ack-core.fk form-stdlib/core-lexicon.fk form-stdlib/core-dictionary-neutral-field.fk

(do (cdnf-check))
