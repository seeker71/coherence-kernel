; form-cli-native-agent-orient-run.fk — print the local native-agent floor.
;
; preludes: form-stdlib/core.fk form-stdlib/form-cli-native-agent-orient.fk

(do
    (print_str (fnao-speak))
    (print_str (str_concat "fnao-check=" (str_concat (int_to_str (fnao-check)) "\n")))
    0)
