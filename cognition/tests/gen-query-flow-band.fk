; tests/gen-query-flow-band.fk — the generator against the graph's REAL query
; surface: four query shapes found in the graph's own accessors, emitted into
; every registered language.
;
; PROOF LEVEL: FOUR-WAY — nothing here is arm-specific; fkwu, Go, Rust and TypeScript each read 127 (2026-09-15).
;
; preludes: form-stdlib/core.fk cognition/language-neutral-symbol-identity.fk cognition/steiner-neutral-kernel.fk cognition/gen-neutral-code.fk cognition/gen-query-flow.fk
;
; The ANSWERS these queries give are judged where they can be run:
; gen-conformance-band hands the page to python3, node and go and compares
; every case with the graph. This band reads the surface itself.
;
; Verdict 127 on fkwu / Go / Rust / TypeScript:
;   four query shapes, FOUND by reading the graph's existing accessors
;     rather than invented                                              1
;   find_subject emits in Python                                        2
;   count_by_object emits in Form                                       4
;   find_object emits in JavaScript                                     8
;   TWELVE programs actually emitted — counted by asking each cell of
;     the grid whether it produced anything, not by multiplying four
;     queries by four languages. That product read 16 the moment Go
;     was registered while Go rendered none of them, because a product
;     of two counts cannot see an empty cell. And every language IS
;     covered — by one form or the other, Go by statements             16
;   and no fact id appears in the emitted module — data stays a
;     parameter across the whole surface, not just the one query       32
;   the JavaScript module carries prelude and all four queries          64
(do
    (gqy-check))
