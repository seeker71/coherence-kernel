; tests/difficult-conversation-counsel-band.fk — the difficult-conversation counsel, fkwu-witnessed.
;
; preludes: form-stdlib/core.fk cognition/dialogue-covenant.fk cognition/difficult-conversation-counsel.fk
;
; PROOF LEVEL: FKWU-WITNESSED — fs_exists / str_find / print_str bind on go, rust, fkwu (pf-arm-mask 11,
; probed 2026-09-18); the TypeScript walker does not bind them, so this band answers on fkwu.
;
; Band verdict 1111111111 when:
;   every move held in this body names a cell that stands on disk           ->         1
;   the arrival card fits the session-start window (<= 1800 bytes)          ->        10
;   the absent person's interior stated as OBSERVED fact fails the lane     ->       100
;   reported / inferred about the other, observed about self, pass          ->      1000
;   a well-shaped reply (grounded advise, one lift, ends on it) LANDS       ->     10000
;   two lifts fail the composed floor even with the other held open        ->    100000
;   one fabrication fails it, however the rest is shaped                    ->   1000000
;   exposition after the lift takes the question back (fails)              ->  10000000
;   every origin-field companion is named without a path, absent here       -> 100000000
;   two teachings before the one question fail the composed floor           -> 1000000000

(do
    (difficult-conversation-counsel-check))
