; tests/steiner-form-codegen-band.fk — the graph writes a Form program and the
; body runs it. No template, no host-exec, no outside interpreter.
;
; PROOF LEVEL: SIBLINGS ONLY — `walk_recipe` is a registered native in Go, Rust
; and TypeScript and is ABSENT from fkwu's --src door (present in
; primitive-registry.fk, in no row of native-op-manifest.fk or the runtime
; optable). The inverse of `nothing` and `host-exec`, which fkwu has and the
; siblings lack. Declared, not discovered later as a phantom divergence.
;
; preludes: form-stdlib/core.fk cognition/language-neutral-symbol-identity.fk cognition/steiner-neutral-kernel.fk cognition/steiner-form-codegen.fk
;
; Verdict 511 on Go / Rust / TypeScript:
;   the written program agrees with the graph for the threefold soul      1
;   for the fourfold being                                               2
;   for the threefold social order                                       4
;   for the polarity                                                     8
;   and the values are the real ones: 3 members                         16
;   4 members                                                           32
;   2 members                                                           64
;   a concept with NO members: the graph writes a program that is just
;     0, and running it returns 0 — the generator does not special-case
;     emptiness, the recursion bottoms out                             128
;   the generated program is over 50 nodes — built from the graph, not
;     a constant folded by hand                                        256
(do
    (sfg-check))
