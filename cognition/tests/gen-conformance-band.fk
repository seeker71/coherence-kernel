; tests/gen-conformance-band.fk — the generated code judged by its own compiler.
;
; PROOF LEVEL: FOURTH-ARM ONLY — host-exec is fkwu-only, and running a real
; toolchain is the entire point. The graph's expected answer is computed in
; PLAIN FORM (no walk_recipe, which is Go/Rust/TS-only): a cell needing one op
; from each side of a split surface can run nowhere, which is corpus row 913
; written yesterday and walked into today.
;
; preludes: form-stdlib/core.fk cognition/language-neutral-symbol-identity.fk cognition/steiner-neutral-kernel.fk cognition/gen-neutral-code.fk cognition/gen-query-flow.fk cognition/gen-conformance.fk
;
; Verdict 255:
;   three of the four languages have a real toolchain                     1
;   and ONE does not — Form, reported UNJUDGED rather than passed,
;     because an unrun language scoring like a passing one is the
;     blindness this cell exists to end                                   2
;   every judged language matches the graph                               4
;   python: emitted, run by python3, answer compared to the data          8
;   javascript: emitted, run by node                                     16
;   go: emitted in the STATEMENT form, compiled and run by go            32
;   Form has no runner row and is honest about it                        64
;   and the expected answer is the real one, carrying node ids above
;     2^31 rather than a small count that would hide a truncation      128
(do
    (gcf-check))
