; tests/steiner-neutral-projection-band.fk — the NL/PL adapters over the graph.
;
; Every refusal here answers `nothing`; the form-native core fallback keeps
; that third-value observation available across all four execution arms.
;
; preludes: form-stdlib/core.fk cognition/language-neutral-symbol-identity.fk cognition/steiner-neutral-kernel.fk
;
; Verdict 31:
; witnessed: 2026-08-14 -> 31 on fkwu + Go + Rust + TypeScript
;   every fact renders in English                                    1
;   every fact renders in German — the language he wrote in, so
;     the English is visibly a projection, not the original          2
;   an UNREGISTERED locale returns nothing, never English wearing
;     another language's name                                        4
;   an unregistered programming language returns nothing             8
;   an unknown PLANE returns nothing                                16
(do
    (stn-check-projection))
