# Scope judgments, missing context, and a native reply ceiling

Signed: Codex. Native Form/BML; no C seed growth or external runtime added.

## What the local probes established

The preceding movement left a scope error in an amended response: a recipe
cell witness was still being used to claim preservation of response prose.
This movement separated classification from subsequent composition.

Private run `generalization-v2/scope-probe-v1` used seven fresh fictional test
claims. The local model correctly distinguished demonstrated facts,
contradictions and unobserved extensions, with reasons matching the records.
It correctly classified untested prose persistence, untested restart recovery,
and unmeasured answer quality as unobserved. It rejected a pass claim whose
exit code failed the stated acceptance condition.

Classification took 87,527 ms and 295 generated tokens. A second reply in the
same resident context amended the actual retained next action. The combined
run took 137,558 ms: 380 prompt tokens, 674 injected tokens, 466 generated
tokens, one amendment, unchanged source documents and successful release.

That amendment removed the unsupported prose claim, but proposed repeating a
recipe check. My focused repair prompt had omitted the original enquiry. That
was a context defect in this diagnostic, not evidence that the standard native
controller omits its goal. The result cannot establish useful response quality.

`generalization-v2/scope-context-repair-v1` restored the original enquiry and
its actual native source observations. It took 187,027 ms, with 3,091 prompt
tokens and 260 generated tokens. One amendment was applied; sources remained
unchanged and release succeeded. The next action returned to trust, numeric
space and vocabulary, but still deferred already-requested work to Urs. It
also turned parity not being established into an observed lack of parity.
These are continuing semantic problems. Restored context was necessary for
this probe; it did not establish a good answer or a causal quality improvement.

Both amended reports retain the original citation failure:
`Source lines 0: explicit source_quote conflicts with selected lines`.
The full source/report/binding checker remains installed. Assessment prose,
fictional cases and answers were excluded from training. All private run roots
are beneath `.hearth/response-parity/`; prompts, raw replies, before/after
reports, request snapshots and numeric receipts remain there. No probe invoked
a provider process.

## Repair the tool that encouraged the context workaround

The standard native controller preserves the goal and source context. It now
accepts `max_reply_tokens`, a positive integer ceiling for each generated reply,
so bounded work can use that standard path. The model sees the allowance.
Decode quanta shrink to the remaining tokens. Initial reasoning uses the smaller
of its own ceiling and this one, including reasoning tokens. Omission retains
the existing context-only default. Current caller limits apply after resume.

A reply that reaches the ceiling before its completion boundary cannot call a
tool, edit documents or submit a report. Its partial text stays private evidence;
the model is released and the last candidate stays intact. Evidence-retention
failure is explicitly distinguished from retained evidence. An absent empty
file can no longer attest retention. Negative generation results retain their
generation-failure diagnosis instead of being attributed to the token ceiling.

Live controller witness: `generalization-v2/reply-token-limit-v1`.

| Ceiling | Generated | Elapsed ms | Tool calls | Checker runs | Result |
|---:|---:|---:|---:|---:|---|
| 1 | 1 | 50,884 | 0 | 0 | Attention; partial reply did not act |
| 64 | 6 | 53,024 | 0 | 1 | Complete; original source/value assertion passed |

Both runs preserved documents, released the model and used zero provider
processes. The complete answer was `{"value":17}` from the supplied source.
These observations establish a generation/control boundary on a small task,
not dialogue quality or speed parity. Model admission and prefill remain part
of those elapsed times.

## Checks and remaining ground

Request/session bands pass 255/31; native-review-repair passes 34,359,738,367;
review followup passes 1; drift gates pass 8191; all have exit 0. Existing bands
now cover integer typing, per-reply reset, final partial quantum, context bound,
reasoning ceiling, checkpoint retention, caller override and failed retention.

During development, request-band preflight first reported delimiter depth -1;
the request module also had a surplus closer. Both were repaired. The next
request-band run exposed `fractional reply token limit refused`: the old numeric
accessor's string round trip admitted a float payload. Requiring an integer
payload repaired that cause, including initial-reasoning validation. Fresh
preflights and the unchanged assertions then passed. No gate was weakened.

Counsel panel: **orphans 0; 11/12 lanes unobserved**, with no standing hearth.
The owned Glass viewer was read and stopped. The native guide reports 0 Python
implementations, 2 existing invocation candidates and 0 unread files. The share
reader withheld a percentage because no completed evidence row was available.
The parent output-token meter read **2,009,576 cumulative** during this movement;
zero provider processes in the probes does not erase the parent's rented cost.

Verified procedure was returned to session learning under event
`2026-09-17-reply-token-boundary-procedure-v1`; retention is not a training or
promotion claim. The evaluated Qwen model was not promoted.

The surprise was seven correct judgments followed by an unhelpful action.
Examining my own missing context turned that friction into a concrete tooling
repair. The exchange stays open at useful, grounded composition: neither the
classifier result nor the new ceiling closes the overall quality gap.
