# The language selector means human speech

Codex, 2026-09-07. Urs corrected the object of observation: natural-language
transcripts, never validation-language source samples dressed as live meaning.

Removed the code/grammar sampling renderer. `s` now projects already-decoded
native ear transcript rows. `1` English, `2` Portuguese, `3` Persian, `4`
Indonesian follow the ear's defaults; `l` accepts code selections such as
`en+de`. The tongue and observer share one catalog. No runtime walker in Go,
Rust, TypeScript or Python is added or invoked by this path.

Original recognition and local translations have distinct kinds and source
stamps. Selection filters received text; it does not request new translations,
open the microphone or admit a model. A missing language is shown as missing.
The renderer reads no grammar/source files and calls no translator. UTF-8 wraps
retain whole characters within the existing byte-accounted canvas.

Native projection band: 8191; direct-controller band: 131071; ear axes: 32767;
full UI: 4294967295, each exit 0. The separate local tongue validation returned
4095; those fixture generations were validation, not live speech. The PTY
check covers immediate keys, typed code selection, rejection, paste and cleanup.
The existing live panel during this movement reported rows=162, asks=17;
those requests are not claimed healed by a language-selector correction.
The read-only transcript door reported rows=0 and ear=asleep. No live utterance
is inferred from that absence. The effectful ear sensor is marked execution-
forbidden in preflight; its pure producer/projection functions are banded,
and live admission is observed separately from those fixture verdicts.

The surprise was that responsive controls had made the wrong observation easier
to reach. The useful correction removes that work from every live frame and
connects the same keys to the human speech that was meant. No claim of hearing
a new live utterance is made from a fixture.
