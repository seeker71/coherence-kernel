# More guidance did not complete the dialogue

Signed: Codex. Witnessed 2026-09-17 on `fkwu`.

The local-only arm returned to the same frozen query-admission request. Its
earlier answer narrated its grounding, dropped the referenced-cell condition
from axiom 3, and proposed an unsupported analogy as follow-through. The source
excerpt already contained the missing condition. This was a use-of-evidence
failure, not an absent-source claim.

The attempted repair added shared review guidance to retain source conditions,
keep analogies illustrative, complete reasoning already possible, and express
the answer itself. Original request bytes, documents, five native source queries
and caller checks stayed unchanged. The ordinary native request door ran Qwen;
no provider process was invoked. The exact candidate prompt is retained privately.

## Actual result

| Observation | Earlier local attempt | Revised guidance |
| --- | ---: | ---: |
| Runner milliseconds | 231492 | 865904 |
| Generated local token IDs | 386 | 444 |
| Additional injected local token IDs | 0 | 6329 |
| Model-requested source reads | 0 | 3 |
| Provider processes | 0 | 0 |

The added time was **634412 ms**. The model read the full trust, voice and
uplifting-dialogue teachings. Source admission dominated the additional work;
the retained log includes per-stage timestamps and counters. Glass also ran
briefly during this attempt, then stopped through a correlated native `abstain`
acknowledgment, so the timing is an observed run cost, not an isolated hardware
performance measurement.

The answer still began with a defensive denial. It recited instructions and
proposed composing the response as the next action, while already delivering
that response. Its question handed the useful next move back to Urs. It omitted
the prior overbroad memory claim but did not establish that it could explain the
qualified claim correctly. The intended improvement in directness, completed
reasoning and source precision was not established.

All original source/report checks passed; original documents and request bytes
were unchanged; model release succeeded. These finite checks did not detect the
dialogue failure. The voice mirror counted 1 selected word sighting before and
2 afterward. That count is attention to word forms, not a quality or resonance
verdict. The frequency spectrum organ likewise computes over supplied numeric
word values; its arithmetic does not establish the meaning of this answer.

**The guidance change was reverted.** Neither an extra read nor a green check
was accepted as evidence that the response improved. The next useful hypothesis
is the response framing itself: this dialogue currently enters a bootstrap that
calls the model a coding member and asks for a review report. That framing is an
observed fact; its causal role remains to be tested. Another wording expansion
of the same review instructions has not earned priority.

## Checks, learning and instruments

Clean preflight preceded both private native helpers. An extra closing parenthesis
in the experiment initially failed preflight; it was repaired before admission.
Policy band: 65535, exit 0. Document-context band: 1, exit 0. The final native
audit confirmed identical request bytes and computed the deltas above. The
public guidance is restored to its landed content.

Drift gates returned 8191, exit 0. The parent goal meter read 8297499 tokens at
the closing checkpoint; the transcript meter read 1718139 cumulative output
tokens. The local experiment used no provider process, while this coordinating
session still consumed rented tokens. These counters have different scopes.

Native guide: 0 Python implementations, 2 existing invocation candidates,
0 unread files. Counsel panel: **orphans 0**, with 11 of 12 lanes unobserved and
no standing hearth. The previous procedural teaching completed candidate
generation 48; serving remains generation 5 with 4 promotions. This is the
Llama learner, not a Qwen serving improvement.

The verified procedural lesson was retained as
`2026-09-17-local-dialogue-guidance-reverted-v1`; its worker launched, with
completion pending at this checkpoint. The assessed answer was not a target.

A source-read capture had completed its command but its empty stderr FIFO reader
remained live. Reopening and closing that owned FIFO allowed the original
capture handle to finish, exit 0; the command was not restarted. Early discovery
also named one nonexistent guidance path and one unmatched glob; subsequent
searches used the actual policy prelude to locate the guidance source.

Private evidence: `.hearth/response-parity/generalization-v2/direct-dialogue-v1`
holds the original request, changed prompt, owner, complete result, actual answer,
receipt, native comparison and side-by-side answers. The execution log is
`.hearth/response-parity/direct-dialogue-v1.log`. Assessment answers are excluded
from learning. This observation narrows the next attempt and leaves the original
voice and throughput gaps open.
