# Route and source evidence now correlate inside an admitted image

Witnessed 2026-08-27 by Codex with Planck, Beauvoir, and Arendt/Claude.

## One movement landed; the old gap opened into a causal path

The pulse began at:

```text
51 observed / 42 ready / 9 gaps / 0 unknown / 0 invalid / 823 permille
selected: form-cli-strict-scanner-route-record-image-placement
```

That row combined request birth, digest and bucket derivation, two different
artifact reads, and two record validators.  The reads cannot honestly live in
one pure image: the validated route determines which source artifact may be
read.  The full PIF walker also cannot yet cross the existing content-addressed
call protocol, whose executor accepts only the smaller `fcatw` image type.

The fresh census therefore replaces the composite row with the causal organs
now visible.  One is ready: exact evidence correlation executes as admitted
image data.  The others remain gaps rather than disappearing inside it.

```text
58 observed / 43 ready / 15 gaps / 0 unknown / 0 invalid / 741 permille
selected: full-pif-content-addressed-call-adapter
```

The denominator rose because the former row hid the generic call adapter,
scanner birth, digest/bucket derivation, route read and validation, then the
separate source-row read and validation.  The lower ratio is not regression;
the ready count increased and the formerly invisible causal boundaries can now
receive work and evidence.

## The image admits correlation, not supplied truth

`form-cli-route-record-evidence-image-call.fk` carries a 20-value ABI and 71
rows using only the already admitted generic compact meanings: argument,
list-index, literal, string equality, integer equality, structural NodeID
equality, and multiplication.  No evaluator branch or query-specific tag was
added.

The image independently correlates:

- request kind and request-to-route key;
- exact route and source schemas;
- route-to-source family, path, source key, and entry key;
- request-key membership in the source row; and
- the source NodeID held by the entry.

The receiver-owned registry, exact OFFER, ACK, and recomputed observation stay
in the receipt.  Exact fuel 95 returns one present integer.  Fuel 94 times out
with output-count 0.  Agreement is present 1; disagreement remains present 0.
The deterministic receipt view retains absent route as `nothing`, zero parser
validity as `failure`, valid disagreement as `refused`, and passes protocol
choice/refusal/nothing/failure/timeout through unchanged.

This does not claim that upstream flags are independently born yet.  Raw BMF
byte validation, SHA-256/hex evidence, route/source parsing, filesystem reads,
and current-source authority remain outside this pure image and are named as
separate work.

## Why the next gap is the call adapter

The full PIF walker already has generic calls, conditionals, lists, byte/string
walking, sparse frames, and native NodeID construction.  The compact form-cli
call executor does not.  Adding route-specific meanings to the compact reducer
would recreate a fixed operation path.  The straight prerequisite is instead
an exact typed adapter that lets a full PIF binding cross the existing
CALL_BINDING/OFFER/ACK protocol.

Once that adapter is observed, the next image can accept the raw coordinate,
enforce byte syntax and bounds while streaming, birth category `31.2.0.98`,
and retain incomplete, trailing, malformed, timeout, nothing, and present 0/1
without a tokenizer pre-step.

## Exact evidence

```text
evidence image source preflight              0 errors / 0 unresolved
evidence image band preflight                0 errors / 0 unresolved
route/source evidence image band                               32767
CALL_BINDING/OFFER/ACK regression                              2097151
strict route image regression                                      2047
current-source effect regression                                 65535
full form-cli regression                                        2097151
health pulse preflight                         0 errors / 0 unresolved
health map                                  58 / 43 / 15 / 0 / 0 / 741
git diff --check                                               clean
```

No model, Metal, host process, HTTP, remote inference, C seed growth,
flattening, global function table, or new evaluator meaning entered this
movement.

Kept alive: acquisition authority, parsing, integrity, correlation, and
current-source execution remained distinct observations.

Most surprising teaching: the richer full PIF walker already has nearly all
scanner mechanics; the immediate seam is the call-envelope image type, not a
missing route-specific operation.

Discomfort turned to gold when the attractive single scanner/route/source row
opened into the real causal chain.  The percentage falls, while one actual
image movement becomes observable and the straight prerequisite becomes clear.
