# Resident native identity arena — Codex

Form now emits a resident 1,380-byte Darwin ARM64 image that generates semantic
u64 identities, allocates and fills native chunks, serves prefix readers and
releases its data mappings. Admission exports the getter before any data
allocation. The growth preference changes in RAM without replacing that image.
The [arena document](../docs/native-identity-arena.md) carries the current API,
ownership contract and primary-field boundary. No C source changed.

The [source-bound evidence](../docs/evidence/fkwu/native-blueprint-layout.json)
holds 104 source bindings across nine actual child executions. Every child
exits zero, has empty stderr and releases its native owners. The new witness
generates 70 coordinate rows covering all 63 structured field bits and signed
extrema. Fourteen valid-first, invalid-later batches preserve counts, maps,
completed batches, existing words and untouched spare bytes. The measured
70-row generation and 4,096-row raw append each mint zero primary nodes.

The raw producer publishes 4,113 rows in four batches across three mappings,
beginning at row 67,108,881. Earlier readers retain their prefix even after
tail writes, and an older prefix freezes at its original seven rows. Native
and adaptive getters preserve exact u64 bytes and nonzero output guards.
Generated rows above 2^56 survive the same ownership transition. Frozen
directory data remains exact after producer code and mappings release.

The repeated sample requests 33,104 arena bytes and 112 adaptive payload bytes.
These are requested extents, not RSS or a general compression/performance
claim. The organ's retirement signal observes three readers; a correlated
release response applies their release and reports zero. Final native unmaps
equal maps and requested live chunk bytes fall to zero. Closed-state
observation remains available after metadata and code unmap.

The AI review traced native register/stack discipline, coordinate validation,
publication, prefix clipping and release retry ownership. Its two cleanup
findings were repaired: root-derived state is captured before release, and a
failed admission retains a closing owner if cleanup cannot finish. Final review
found no remaining blocker within the cooperative, serialized contract.
Host-unmap failures were reviewed in source, not injected.

The witness and evidence collector preflight with zero errors, warnings or
unresolved calls. The settled-tree native multiargument ABI validation returns
`63`, exit zero. All fourteen landing drift gates return `16383`, exit zero.
The unchanged CPU/Metal layout, float, storage, accessor, directory and importer
executions remain exact.

Our Glass startup observes a **30 ms first frame**, zero invalidated images
and a **34 ms admission reading**. The bounded memory panel reports **918K
cells**, **70 MiB logical column fill** and a **7 MiB identity subset**, with
one unavailable tensor-owner allocation signal. Shared population readings do
not attribute allocations to this movement. Counsel reports zero orphans and
11 of 12 lanes unobserved because no hearth stands. Native authoring reports
zero Python implementations, two invocation candidates and zero unread files.
Share remains declared with its percentage withheld; the spend meter reports
`transcript-path-required`.

The lane census reads ice 2,576, water 28 and gas 6,978, with 14 sibling
processes. Its absent resident has `alive=0`; zero-valued KV and ice-miss fields
do not establish observed serving performance. Counsel retains that absence.

The live Glass door reported unavailable keyboard input despite the owned TTY.
After reading its startup, we stopped only our live child with
`form-run kill -TERM 34957`. Its supervisor reported live exit 143, released
its owned sensors, and the public door exited one with
`Glass supervisor ended with an error`. This deliberate stop is not a passed
check. A process reading confirms our fleet ended and the preceding fleet
remains. The separate bounded memory reading exits zero.

The verified teaching returned through the local session door. Its worker
settled with exit zero and no pending rows: 35 retained examples across 22
sessions, candidate generation 35 and serving generation 4. Candidate training
does not establish a serving improvement or a promotion.

The most surprising teaching is that admission order is an allocation
dependency: native code can allocate without minting primary nodes once its
identity and entry already stand. The uncomfortable cleanup edge became a
useful guarantee: an owner retains enough observed state to finish release
without touching memory it has already returned.

The next boundary is canonical native interning and primary handle resolution.
Arena rows remain owner-local allocations; duplicate semantic words can occupy
different rows. The C field's tagged handles, interning, direct readers and
2^26 capacity remain unchanged. Primary cutover must carry native side-table
lifetimes, collector roots and relocation; concurrent publication requires
ordered generation selection and retained readers. The current arena releases
as a whole and does not establish slot reuse.
