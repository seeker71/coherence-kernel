# The local agent goal, as a number

2026-09-25, Urs's Mac. Urs asked to analyze the path from the start and find the swiftest way to a local
agent that answers our questions and improves itself offline with no rented token — assistant, companion,
engineer, guide, a healthy cell in the organism of us — and then: *give me a full condition that you can
measure and work towards.*

## Where the path stands

Rent per answer and rent per movement have both read 0 since 2026-09-22: the voice speaks inside the fkwu
model session, and launchd walks the body nightly. What remains is the rented mind doing the engineering.
One such session read 209,558,305 tokens (9,075,253 uncached); main took 170 commits in the last seven days,
nearly all from rented sessions. That integral is the star now, and `docs/local-agent-goal.form` walks it:
seven rungs, ordered by future rent removed per rent spent, with their walls written down (a 27B's ceiling,
the voice's pace, a 3B-only native trainer, care that no band can judge).

## The condition

`observe/local-agent-goal-check-run.bml` reads eight bits from the body's own ledgers; an absent ledger
reads 0. **255 is the goal met. The first reading is 0/255:**

| bit | reads |
| --- | --- |
| WALK | 0/7 walks as the schedule — the three unattended walks read `rented_mind=1` (see below) |
| LESSONS | 18 pairs (500 wanted) — `observe/session-pairs-run.bml`, from this session's transcript alone |
| SELF-LIFT | no generation yet |
| LOCAL SHARE | no drafts yet |
| NATIVE TURNS | none yet |
| CARE FLOOR | no witness yet |
| EAR | 3 WER files (20 wanted) |
| RENT-FREE WEEK | 22 session rent rows, 2/23 crossings free, 170 commits |

## What landed beside it

- **The body reads its own coordinator truly.** Three unattended walks wrote `coordinated_by=session,
  rented_mind=1`: the movement's parent is the shell that `scheduled-walk` opens, not launchd. The
  coordinator is now the first ancestor that is neither the body nor a shell (`mv-climb`). The next
  nightly walk is its witness.
- **The ear's weights are released.** The carrier refuses a free while work still names the buffer, the
  kernel reads that refusal as 0, and the unit read 0 as done: a second open in one process left 150 of 586
  weights alive. `nws-free-rounds` frees, syncs and asks again. The homecoming witness reads **511** again.
- **The small ear is measured the way the lane hears.** The WER door cuts tiny's looping line where the
  live lane cuts it, and the lane's repeat window reaches 32 words: tiny's mean WER over the three
  references is 32 (0 / 8 / 90).
- **Every rented turn leaves a lesson.** `observe/session-pairs-run.bml` writes each genuine prompt and the
  turn's last answer as a chat row for the native trainer; this session gave 18.
- Corpus rows 1587 `mutefree` and 1588 `shellveil`; this session's rent row in the flow ledger.

## Receipt

**The surprise:** the full condition, once it was a number, had its most honest bit in the last place — 170
commits in a week. The body answers at rent 0; the organism around it still runs on rented hands. The
goal's size is not in the voice, it is in who does the work.

**Where discomfort turned to gold:** asked *how is the goal already reached?* — it wasn't, and the hook that
said so was reading only the first half of a sentence. Saying so plainly, and then turning the whole
sentence into eight bits that start at zero, made "done" something the body says and no one can claim for
it. Also: twice in one hour zsh read my words differently than I wrote them (`$B:r`, an unsplit list) and
the second time it cost the uncommitted heals; they came back from a commit, and the lesson went to memory.

## The same evening: 98/255, and the walk becomes a workday

- **LESSONS**: `observe/session-pairs-run.bml` over all 133 transcripts on this Mac gave 1,853 pairs in 97 s;
  keeping only the person's own prompts (no hook feedback, no notifications, at least 16 characters) with
  answers of at least 40 left **1,306** (1,176 train, 130 held out), 35 duplicate prompts, 4 with noise.
- **CARE FLOOR**: `form-stdlib/bml/care-floor.bml` hears a danger to life or body in en de pt fa id, speaks the
  floor first in that tongue — a human now, 112 or 911, someone trusted — and stays quiet on *kill the process*;
  band **255**. `observe/companion-run.bml` stands it in front of the local voice with six turns of memory.
- **EAR**: 24 sentences in six tongues (`model/fixtures/ear-truth`), spoken by this Mac's own natural voices —
  the first pass picked *Albert*, a novelty voice, and the generator now skips those — read by turbo at
  **WER 0 on all 24** (synthesized, named so).
- **WALK**: the kicked walk's crossings row reads `host-schedule`, `/sbin/launchd`, `rented_mind 0` — the climb
  heal witnessed. I pressed that kick; the nights carry the other six.
- **The workday**: `observe/scheduled-walk.bml` now runs one engineering turn by the body's own loop before the
  movement (`observe/native-turn-run.bml` on the first open gap of `learn/native-turn-queue.jsonl`: the native
  code lane edits, the gap's band judges, a red turn keeps its candidate and restores the source) and drafts two
  of the person's newest prompts (`observe/prompt-draft-run.bml`) for our reading
  (`observe/draft-reading-run.bml`). The first gap is the care floor in Spanish and French: its band reads 48/63.
  A dry turn from this session proved the plumbing and read its own coordinator truly: `rented_mind 1`.

The condition read **98/255** before the workday was wired: LESSONS, CARE FLOOR, EAR. The rest are nights and
readings — seven host walks, a lift, 25 accepted drafts, three native turns, a rent-free week.
