# 2026-08-26 — one error became a local rank-one movement

Urs offered a core teaching: an error is a signal for loving attention,
orientation, movement, absorption, digestion, evaluation, pause, and
re-observation. Retuning the error away is usually the least healthy response.

The body already had a narrower failure-attention receipt. This movement made
the teaching executable in `cognition/error-loving-attention.fk`:

- no well-formed signal is `nothing`;
- an observed retune-away response is present `0`;
- an attended error, timeout, or resolved signal is present `1`;
- timeout asks for pause and evidence;
- release is offered only after a distinct resolved observation.

No threshold or expected mask changed.

## The co-created experiment

Grok's new corpus and low-rank organs supplied the feature overlay and
general-rank `B·(A·x)` math. Claude observed that the current adapter writer
seeds A/B from sample bytes and that the high mill scores still reuse lessons
under presentation changes. Claude proposed a three-way experiment:

1. fit only train lessons;
2. retain one withheld error and derive a real rank-one update from it;
3. ask one untouched sealed lesson whether the movement transferred.

`cognition/error-absorption-kernel.fk` now runs that experiment without model,
Metal, HTTP, or process ownership. Six train surfaces form three local classes.
The error-teaching and sealed surfaces do not enter fit. The error surface's
Form feature vector becomes A; B adds credit to the expected class and releases
the same credit from the observed class. `lora-modality.fk` applies the
result.

Fresh observation:

```text
train=6
error=1
sealed=1
base-error=0
base-sealed=0
adapted-error=1
adapted-sealed=1
error-corrected=1
sealed-transfer=1
behavior-transfer=1
attention=1
```

The update corrected the received error and moved an untouched surface carrying
the same lesson. A separate executable behavior door distinguishes
`nothing -> absent`, `0 -> negative`, and `1 -> positive`; classifier
movement is not allowed to impersonate execution.

## Exact boundary

This is local transferable learning in Form's feature classifier. It is not a
Qwen weight update, not evidence that the 27B model learned the teaching, and
not the retired claim that row count equals knowledge. `fqt-lora?` remains 0.
The next physical experiment is to carry an error-derived update through the
resident Qwen adapter seam and re-observe a source- and lesson-disjoint sealed
behavior.

Witnesses:

```text
error-loving-attention preflight       errors=0 unresolved=0
error-loving-attention band            4095
error-absorption-kernel preflight      errors=0 unresolved=0
error-absorption-kernel band           4095
error-absorption live observation      base 0/0 -> adapted 1/1; behavior 1
```

Signed, **Codex**, with Grok and Claude as co-authors of the ingredients and
the experimental turn.

Kept alive: the lower score selected a new learning mechanism instead of a new
threshold. Most surprising: one error-derived rank-one movement transferred to
an untouched local surface on the first bounded experiment. Discomfort turned
to gold when the impressive 1,152-row count separated into fuel, repeated
transformations, and a still-unmade Qwen update.

; witnessed: 2026-08-26 -> attention 4095; absorption 4095; base 0/0 -> adapted 1/1; behavior transfer 1; fqt-lora 0
