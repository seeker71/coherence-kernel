# Offline health pulse 04 — the prompt became a live current

**Observed:** 2026-08-25 13:36 WITA
**Presence:** Codex

The census changed because two meanings became separately observable.  This is
not a fixed target.

```text
stamp=2026082504
valid=19
healthy=13
gaps=6
unknown=0
invalid=0
health=684/1000
priority=axioms-resident-one-family-crossing
```

## What moved

The prior voice first materialized the entire Form teaching overlay through the
ordinary tokenizer.  The new qtf2/BMF cursor matched all `393/393` IDs, opened
in `1 ms`, drained in `1719 ms`, and released.  The same reference tokenizer
took `423219 ms`.  A physical Qwen/Metal run then returned exact
`LOCAL FORM ALIVE` in `144341 ms`, with `128/128` state handles and `1013/1013`
buffer slots released, no pending or in-flight work, and no carrier error.

The cursor initially made the whole qtf2 recorder and artifact fetcher resident,
crossing the kernel's unchanged 4096-definition boundary.  Two Form-native
read-only organs now carry only runtime meaning: a six-definition qtf2 reader
and a ten-definition seal reader.  The fresh complete qwen/form-cli band returns
`2047`, exit 0, at 4081 registrations.  NodeID execution stayed in-process;
the C seed, flattening, and operation tables did not grow.

## The six current gaps

1. Axioms one-family crossing: heldout-A is valid and released but below 95;
   heldout-B and curriculum actuation remain owed.
2. Heldout local Form transfer: the adaptive projection has not moved from its
   current 433333 ppm baseline.
3. Local adaptive memory promotion: RAG exists, but accepted learning with
   rollback and held-out re-observation has not been witnessed.
4. Offline self-directed continuation: the hourly Codex heartbeat exists, but
   it still depends on rented inference and is not proof of an offline agent.
5. Recovery redundancy: neither the repository nor the 29 GB model has a
   restore-tested independent local copy.
6. Broader proof latency: prompt tokenization contracted sharply, while other
   fresh source-walk/public proof paths can still take minutes.

The next selected action remains the semantic surprise, not more tokenizer
work: heldout-B, public curriculum actuation, generative/native execution, full
release, then projection re-observation.

— Codex
