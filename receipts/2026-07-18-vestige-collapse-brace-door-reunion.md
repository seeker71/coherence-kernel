# The vestigial blocks fold away; the brace door names itself; rows 815/816 seat at the reunion

2026-07-18, heuristic-morse worktree, branch `claude/dreamy-poincare-de4c1a`,
continuing receipts/2026-07-18-jit-bands-closure-parity.md.

## Why both spellings existed (asked, and answered from git)

`; preludes:` (lowercase plural) and `; Prelude:` (capital singular) were BOTH
born as inert prose: the lowercase convention from 2026-06-28 (7b6e871de), the
capital blocks in the jit family 2026-06-30 (21d29e40d) — the same day another
session wrote lowercase in jit-carrier-abi (a66358c08). The resolver arrived
2026-07-05 (4ef962e7a, fk_src_collect_preludes) with a byte-exact lowercase
needle: one spelling grew ears, the other became a false affordance. Nobody
migrated it because axiom-5 numbness kept the capital families' verdicts
right-looking. Corpus row: vestige.

## The fix

- All 29 capital `; Prelude:` blocks collapsed — label and path-list lines
  removed, prose kept; the live line is now the single dep record.
- form-stdlib/i18n-parity.fk, json.fk, cache.fk, i18n.fk gained their own
  live lines. Probe witness: 82 unresolved → 3, and the survivors name a real
  kernel gap, not a header gap: `value_kind` / `read_form_binary` /
  `write_form_binary` exist only in fourth-shim.fk — fkwu has no such doors.
- A/B on audit-evidence-cells-band (pre-existing red): 86 unresolved before,
  5 after, verdict unchanged — strict improvement, no fabricated green.

## The brace door, and the redness I manufactured

The json-category-consumers-band "numbness" I flagged earlier tonight was my
own artifact. The band and its codec deps are BRACE-surface (`section
[form.bml]`); fkwu has no form.bml door at all. Its green lives in `.fkb`
caches built by the brace-capable builder. My probe deleted a cache, fkwu
misparsed curly source as sexp (164 bogus diagnostics, names like
`codec_value,` with trailing commas), and — the sharp edge — EMITTED a
stamp-valid poisoned `.fkb` over the good one, which later runs would load
silently at verdict 0. Six poisoned artifacts (codec, json-codec, the band;
.fkb+.sym each) were found by mtime and deleted. The chip was replaced with
the corrected premise (rebuild via the right builder; consider making fkwu
refuse the brace surface instead of poisoning it).

## The reunion

origin/main had moved 8 commits, including the moved-five respell (#306) of
the same 39 jit files and six corpus lineages (rows 807-814: falsing, hapax,
misnomer, scavenge, allomorph, paraphasia, exit-truth, contamination). 41
conflicts resolved: bands keep my live header + main's respelled cat prose;
deps keep the live line, the capital fossil dies. My rows 807/808 renumbered
to 815/816 per the anastomosis law, minted-note in the narration; counts
asked of the body, never asserted: [212, 816, 1, 2122122816].

## Verification (merged tree, fkwu rebuilt from merged runtime)

- 94/94 jit bands fresh: rc=0, zero unresolved-call, verdicts unchanged.
- Corpus band: 4095.
- i18n-parity probe: 3 unresolved, all naming the missing fkwu doors above.

## Most surprising teaching

The redness I reported as "pre-existing breakage" was my own probe's shadow:
deleting an artifact I could not rebuild, then letting the wrong parser
overwrite it with a stamp-valid wrong answer. The probe didn't just observe
the system — it edited it. Fresh-compile-from-source is only an honest witness
for sources the compiling binary can actually speak.

## Where discomfort turned to gold

Admitting the earlier claim was wrong — in the same conversation where it was
made — felt like undoing the work. Sitting with it instead: the retraction
became the more valuable finding (the poisoning door now lives in the fkb
memory and the corrected chip), and the false chip was withdrawn rather than
left to burn a sibling session's time on a wrong premise.

## Honest seams left open

- fkwu's missing `value_kind`/`read_form_binary`/`write_form_binary` doors —
  json/cache binary paths stay numb under fkwu until seated (or shimmed).
- The brace-surface `.fkb` caches destroyed tonight regenerate only from the
  brace-capable builder — carried in the replacement chip.
