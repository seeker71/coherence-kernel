# The resident guarded patch entered the loop

**Witnessed:** 2026-08-27
**Signed:** Codex / Sol and the resident local Form/Qwen peer

The Form peer chose `resident guarded patch frames`. It then reviewed the built
seam and answered:

```text
SEE: The preimage read-back before guarded apply is the only durable truth; the frame is just a coordinate, the journal is the body.
TURN: Move the latency seam from scanning the completed quantum to intercepting the frame at the
```

The 48-ID quantum cut the second line and omitted its requested witness line;
nothing is completed on the peer's behalf. The observed prefix agreed with the
next implementation movement: interception moved onto each decoded token's raw
byte edge before the model session's next forward.

## What crossed

`form-agent-repo-patch-frame.fk` now holds a capability-sized scannerless
grammar, exact request identity, one-off live cursor, source-free result frame,
and durable wrapper around the existing guarded actuator. The model can offer
grant coordinate plus hex path/old/new. It cannot supply the capability root,
grant object, verifier, test callback, journal path, or deadline; those remain
function values and data in the live caller's Form context.

The durable order is:

```text
exact frame + current preimage
  -> append intent
  -> exact readback
  -> verify candidate
  -> stage recovery + candidate
  -> atomic rename
  -> post-test
  -> crystallize or undo
  -> append terminal
  -> exact readback
  -> inject typed observation into the same Qwen/KV residence
```

No SHA participates in this bookkeeping. The proposal NodeID is the O(1) live
identity; exact length-carried bytes are the restart truth.

The model session walker is now protocol-generic at its scannerless feed seam.
The existing recipe route remains unchanged and green; the patch cursor reuses
the same Metal/KV loop instead of copying it. The contribution turnwheel holds
an optional caller-born patch context while preserving the old no-authority
constructor.

## Physical local-model witness

The prior warm peer was released first (`release-ok=1`) so no two processes
competed for model or Metal ownership. A fresh Qwen3.8-27B-Q8 residence then
received a disposable fixture capability. The local model emitted:

```text
<|form:repo-patch|>grant=@0.1.2.8;pathhex=666f726d2f612e666b;oldhex=28646f2031290a;newhex=28646f2032290a<|/form:repo-patch|>
```

The cursor reported `none`, then `held` across token boundaries. At completion
it reported `status=value callbacks=1 journaled=1`; the 236-byte typed receipt
entered the same KV state, generation resumed, the fixture held `(do 2)`, and
the residence released cleanly. The final verdict was `1`, contribution was
`1`, and both intent and terminal records were present. No HTTP, remote model,
llama-server, Ollama, flattening route, or fixed function table participated.
Exact output is retained in
`receipts/artifacts/2026-08-27-resident-guarded-patch-live.txt`.

## Fresh health map after the movement

| Surface | Observed health | Living gap |
|---|---|---|
| Local reasoning / held-out knowledge | The local peer selected the expansion, reviewed it, and physically emitted the exact control frame. | Broader held-out Form knowledge and patch-quality evaluation remain uneven. |
| Form-native JIT / carrier | The patch stayed inside `fkwu`; the existing recipe/Metal session remained green after its feed seam became generic. | Arbitrary test-route NodeIDs do not yet birth/hot-swap verifier kernels inside a running residence. |
| Scannerless BMF/BML | A capability-sized frame crossed arbitrary decoded-token boundaries with `held` distinct from malformed/over. | This patch grammar is hand-authored Form; generating arbitrary BMF/BML grammars from the resident agent remains northward. |
| Diagnostics / actuation | Live stages exposed task observation, raw-byte hold, model forward, callback, journal state, injection and resumed generation. | Patch-effect begin/end can become its own tighter stage rather than the current sub-second interval bound. |
| Local assets / dependencies | Sealed Q8 weights, compiler, Metal carrier, Form source, tests and docs were all local. | Offline rebuild provenance for every optional toolchain/model still needs one consolidated recovery manifest. |
| Persistence / recovery | Exact intent/preimage precedes mutation; terminal outcome follows; post-test failure restores the preimage. | Unmatched-intent startup replay and power-loss/fsync evidence are not yet observed. |
| Tests / receipts | Patch band `4194303`; recipe session `4095`; peer `8191`; contribution turnwheel `32767`; physical live verdict `1`. | A real checkout-scoped request still waits for a narrowly born capability and its exact verifier/test recipe. |
| Unlanded work | This receipt names the coherent movement ready to land. | Hot capability injection and KV-delta efficiency are the next locally actionable joins. |

## Timing signal

- 326-byte task-to-KV observation: **56,613 ms**.
- Token-edge frame movement from run begin to typed injection begin:
  **18,076 ms**; decoded forwards sampled around 0.18–0.20 seconds.
- The guarded effect completed inside the **834 ms** interval between the last
  sampled forward end and injection begin.
- Injecting the 236-byte typed observation into existing KV: **44,129 ms**.
- Resumed decode to run end: **635 ms**.

The next straight path is therefore incremental KV observation: retain the
already-encoded role/prefix state and forward only the new observation IDs,
with per-token position evidence, so a few hundred new bytes do not cost tens
of seconds. That movement serves every resident recipe, source lookup, patch,
and later thought kernel.

I kept the exchange alive by letting the local peer choose and critique the
movement, then following its turn into a physical reversible contribution. The
most surprising teaching was how little the effect itself cost compared with
putting its small receipt back into KV. Discomfort turned to gold when the
44-second injection appeared after a sub-second guarded apply: the live stages
made the next shared bottleneck impossible to hide inside “model time.”
