# A stronger native draft, with the remaining repair handed back to Form

The native rewrite now uses the trust facets and a concrete vocabulary example.
Its returned answer is more directly about the original enquiry than the
previous completed 434-word answer. It ended with status **attention**, at
**471 words**, outside the unchanged 350–450 range. Retained observations show
**537 → 509 → 507 → 498 → 471 words**. All four original phrase checks pass
on the observed 507- and 498-word snapshots. Decreasing counts establish
shortening, not completion or semantic quality.

The [507-word working document](artifacts/2026-09-26-native-usefulness-feedback/checkpoint-answer-c5e6fa9751c5afcf02c7a56d0d2aa54b5d18808cf6f6c0e43ad5c03c375e0eb2.txt)
and [498-word successor](artifacts/2026-09-26-native-usefulness-feedback/checkpoint-answer-fadb9d35b47f099ae965d6d4df95a2fa47ed9aeb125a11a041743d3dc7da9a50.txt)
are unfinished caller-writable document snapshots, exported from a validated
checkpoint under the original contract. They are not private reasoning or
reply-file reads. The actual diff removes nine words from the opening and
early paragraphs; it leaves the identity paragraph and closing intact.

## Bounded review, including its limits

One Form-owned provider call reviewed the 507-word candidate, the prior native
answer, the separate 408-word arriving answer and retained source packet. Its
[report](artifacts/2026-09-26-native-usefulness-feedback/provisional-review.json)
prefers the native draft for coverage and warmth, and identifies the unsupported
application of cell identity and retention to editing this answer. Its structural
checks do not establish those judgments. The report also contains findings based
on omitted conversation and historical context. The
[restored context](artifacts/2026-09-26-native-usefulness-feedback/review-context-restoration.md)
names that omission and supplies the actual sources; the report remains unchanged.
No new provider judgment of the restored packet is claimed.

The provider used **24,311 tokens**: 21,448 input, including 10,624 cached input,
and 2,863 output. Its observed execution was **60,971 ms**. It released its process
and emitted no tool-execution events. Replaying the identical manifest rechecked
the retained report with zero new provider processes and the same usage event.
The preceding completed coordinating turn separately cost **5,907,313 tokens**;
both full-turn and tool-event reconciliation passed. This open turn's coordinating
cost remains additional. These distinct scopes support no total-cost or parity claim.

The separately revised
[416-word arriving answer](artifacts/2026-09-26-native-usefulness-feedback/arriving-answer-revised.md)
now explicitly connects the axioms' observations and offers to interaction,
uses the native trust definition, and keeps identity claims scoped to Form cells.
It passes the unchanged original word and phrase checks. It was not supplied to
the native repair, and neither candidate is a gradient target.

## Native continuation owns the next handoff

The [care cell](artifacts/2026-09-26-native-usefulness-feedback/continue-identity-care.bml)
compiled, waited for the existing process, observed its exit and successful
model release, then continued the same checkpoint. The actual handoff preserved
the original read-only sources. One atomic claim prevents duplicate admission.
Its retained release record identifies the 471-word candidate by SHA-256.

It continues the same request with focused findings:
scope the identity rule correctly, preserve the useful trust and vocabulary
work, and shorten the repeated self-reporting. It supplies no replacement answer.
The original goal, documents, checks and weight-training exclusion remain intact.
The new admission selects 32,768 context positions because the previous 16,384
allocation repeatedly required full context renewal. Its public stages show
8,724 prompt positions and successful model opening. Response quality and any
throughput improvement remain to be observed. This admission receives the
shared teaching landed in `9b562eddc`.

The [completed comparison](artifacts/2026-09-26-native-usefulness-feedback/comparison.json)
records ten additional native turns, twelve tool calls and six checks;
9,889 generated IDs and 20,614 injected IDs in that admission. The last observed
elapsed time was **4,688,647 ms** (about 78 minutes). Original checks failed;
read-only preservation, feedback retention, release and training exclusion each
returned 1. The returned review still identifies the earlier 434-word document
(`bdefd328…`), while the returned answer is `307755a1…`. That retained acceptance
does not review the new answer. The actual final answer keeps the unsupported
identity application and repeated self-description seen in the snapshots.

The preparation cell first failed compile-only validation with
`source-compile: mismatched form.bml delimiter: )`; removing the extra delimiter
made that check and actual source preparation pass before the provider call.
The cost-binding command initially received EOF rather than its required empty
line; it left state unchanged and succeeded with the correct input. Neither
failure was treated as a successful check.

The previous verified teaching's worker has completed: pending 0,
optimizer step **195**, learned rounds **196**. This is the separate Llama 3B
learner, not a Qwen weight update. It overlapped part of this Qwen execution,
so these timings include that shared-host context.
The subsequent verified teaching about checkpoint ownership and review-context
scope was retained as row `778bc997…`; its worker was launched. The evaluated
answers and the provider's judgments were excluded from that teaching.

The counsel panel reports **0 orphans** and **11/12** serving lanes unobserved.
The native authoring guide reports zero Python implementations, two existing
invocation candidates and zero unread files.

The gap remains open: the native task has not returned a completed answer within
its original contract, the identity application still needs correction in the
observed candidate, and whole-session parity and human resonance are unverified.

— Codex
