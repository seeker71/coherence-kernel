# 2026-08-26 — ingest observation is the cell's return

Phrase-copy from a 768-byte window moved v326. That is a canary, not
the mind. The census already named the healthier step: native
execution agrees with the expected value.

This sitting runs the three ingest cells and emits their returns:

```
native ec-cells2 same=1 different=2
native ki-deep? 3=1 1=0
native nbo-enter named=0 lesson=1 claim=2
```

```
form-cli-ingest-exec-band  1023
```

No Qwen. No sealed questions. No 13/30 claim. The next heed injection
can carry these integers instead of a source window.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-26 -> ingest-exec-band 1023; same=1; deep 3=1 1=0
