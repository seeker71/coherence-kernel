# Native identity directory — Codex

Form owns a sparse directory over adaptive identity segments, with one
312-byte native image and no C source change. A stable native entry serves
full unsigned 64-bit identities across directory generations. Current and
pinned generations retain their segments; retiring a range preserves existing
readers and prevents silent handle reassignment.

The read-only primary importer accepts published node handles, groups selected
adjacent slots and publishes one complete owned generation after its source
mappings close. The importer refuses non-field homes before opening primary
field objects. The C allocator, interning path and direct identity readers
remain unchanged. Their current 2^26 capacity remains a primary-field limit.

The [held execution evidence](../docs/evidence/fkwu/native-blueprint-layout.json)
binds 102 sources and eight actual child runs. All exit zero with empty stderr
and released native owners. Directory execution covers 65 residual widths,
130 raw fields, 69 generations and sparse row 67,108,881. Primary execution
imports 25 published handles and preserves all 200 raw bytes after source
owner retirement. The CPU/Metal layout, float, accessor and storage executions
remain exact. Preflight resolves both new witnesses cleanly. The settled-tree
ABI validator returns `63`, exit zero; handwritten kernel sources have not moved.
All fourteen landing drift gates pass with mask `16383` and exit zero.

The directory reports its current generation, segments and readers, carrier
descriptor/output bytes and calls, and separate adaptive payload bytes and
codec calls. Its correlated release witness observes the retired payload
unmapped and the live payload signal reduced by exactly that block's requested
extent. Requested bytes do not claim RSS or seed metadata reclamation.

The AI review checked ARM64 branch encoding, unsigned search, Darwin object
metadata, admission, generation ownership, release retries and documentation.
No remaining blocker was found within the serialized, process-local contract.
Concurrent publication and external primary-field reset during a source lease
remain outside that contract. The current implementation and next ownership
boundary live in [the directory document](../docs/native-identity-directory.md).

The bounded Glass memory reading reports **909K cells**, **69 MiB logical
column fill**, a **6 MiB identity subset**, and one missing tensor-owner
allocation observation. These are shared population readings, not allocations
attributed to this movement. Counsel reports zero orphans and 11 of 12 lanes
unobserved because no hearth stands. Native authoring reports zero Python
implementations, two invocation candidates and zero unread files. Share remains
declared, with percentage withheld.

The live Glass reading was deliberately stopped after observation; its
supervisor reported live exit 143 and released its owned sensors. The separate
bounded memory reading completed with exit zero. Local verified teaching
settled with worker exit zero, no pending rows, candidate generation 34 and
serving generation 4. A trained candidate does not establish improved serving
capability.

The most surprising teaching is that the primary counter measures reservations,
including unfinished nodes. The useful movement came from following published
handles instead. Discomfort over incomplete resource accounting became an
executing payload signal and an exact observation of release. The next boundary
is resident native allocation and the primary producer/read path, followed by
ordered concurrent publication and collector-aware reference ownership.
