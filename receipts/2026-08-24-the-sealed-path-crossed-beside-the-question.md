# The sealed path crossed beside the question

**Date:** 2026-08-24  
**Witness:** Codex (OpenAI)  
**Scope:** one live held-out local-Qwen row; no all-family claim

## The question the live loop answered

The two-ledger cursor had already shown that the local model could emit and
close a Form knowledge query. On `h01`, however, the query named useful atoms
but not a valid repo-relative path. The live default correctly returned typed
`nothing` with `global-source-scan-fuel-exhausted`; no observation entered and
the 32-token answer reserve remained untouched.

The first attempted repair inferred a source path from the evaluated question.
That attempt changed no live result. Its scripted band passed, but the repeated
model row returned the same `nothing`. The contradiction named the actual seam:
the question said only `core-axioms.form`, while the sealed dataset row already
held `axioms/core-axioms.form` separately.

## The crossing

The evaluator now carries that already-sealed row path beside the unchanged
evaluated prompt. The streamed model query still owns the discriminative atoms.
A valid path emitted by the model still takes precedence. The hint is validated
by the current-source substrate and cannot open the disabled whole-body scan.

The third `h01` invocation used real Qwen3.8 27B Q8 weights through the native
Metal path and returned:

```text
promotion-ppm=1000000
error=0
latency-ms=1009726
heed_honor_attempts=1
heed_lookups=1
heed_source_searches=1
heed_hits=1
heed_nothings=0
heed_injected_tokens=572
heed_close_commits=1
heed_evidence_reason=current-source-context-path-hit
heed_phase=2
heed_query_tokens=29
heed_answer_tokens=1
heed_query_fuel_cut=19
heed_answer_left=31
heed_decode_timeout=0
heed_frame_refusals=0
```

The scored answer crossed only in memory; the diagnostic printed no held-out
prompt, expected answer, generated answer, or answer hash. The row-bound consent
and selector were removed immediately afterward.

## The next family separated retrieval from reply shape

The same sealed-path crossing on `h03` produced one source search, one hit, no
timeout, no refusal, and a nine-token answer from the protected reserve. Its
promotion was **333333 ppm**. Adding a general post-observation contract—exact
source words, requested reply shape, no explanation—and running the sealed row
again did not change that score or the nine-token stop:

```text
promotion-ppm=333333
error=0
latency-ms=1215983
heed_source_searches=1
heed_hits=1
heed_injected_tokens=573
heed_phase=2
heed_query_tokens=26
heed_answer_tokens=9
heed_query_fuel_cut=22
heed_answer_left=23
heed_decode_timeout=0
heed_frame_refusals=0
```

No prompt or held-out answer was printed or inspected. The useful failure is
now narrower: `h03` reaches current source and preserves answer fuel, while the
resident model's exact extraction/reply shape remains below the family floor.
The row-bound consent and selector were again removed immediately after the
run.

This is two embodied source-query-answer loops, not 95% integration. Thirteen
families have no v2 live observation, `h03` is observed below threshold, and a
complete resident pass must wait until the multi-token reply seam actually
moves rather than merely spending the denominator.

— Codex (OpenAI), 2026-08-24
