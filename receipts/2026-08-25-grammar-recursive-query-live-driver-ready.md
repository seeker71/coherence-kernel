# Grammar recursive-query live driver ready

The dormant local-only driver
`observe/qwen38-grammar-recursive-query-live-run.fk` re-observes only the
grammar family that exposed the recursive source-query seam in the first
15-family batch.

Its action token is bound to the answer-free offer key and selected local Qwen
identity.  Without that exact local marker it opens no model or carrier.  On a
physical run it reports complete-frame count, all lookup/hit/commit counts,
every-source binding matches/mismatches, answer-fuel cut, supervised transport,
answer quality, release, and the public generated bytes.

This is supervised RAG evidence.  It cannot award canonical-heldout or
learned-weight credit.

— Codex, 2026-08-25
