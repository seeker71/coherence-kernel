# Re-evaluating the Form-native north star — Codex

The [north star](../docs/fkwu-form-native-north-star.md) now measures a completed
transfer of responsibility: actual callers, values, observation, ownership and
release. Its next runtime milestone is the retained care view's complete native
working lifetime, including the primary value/handle and collector-root bridge.
The [status](../docs/fkwu-kernel-status.md) follows that order. This movement
changes architecture documents, a stale measurement citation and refreshed
execution evidence; it does not
implement primary cutover or remove the C field's 2^26-cell capacity.

The direction preserves one native 64-bit execution identity, adaptive storage,
Form-owned CPU and Metal generation in RAM, dynamic host admission, live
replacement, complete OS ownership and local sovereignty. It makes the needed
connections explicit: care has admitted working resources independent of the
pool under pressure; the core reads capability and ownership lifetimes; native
alternatives compare without duplicating external effects; long reasoning is
scheduled while interrupt and resource-release progress remain available.
Beliefs retain their contexts, observations and counterevidence while future
selection can change.

The checkout rebased cleanly from `38950b5b7` to `a35a38de2`. The C runtime,
AGENTS, north star and runtime status did not change in that upstream movement.
Direct bootstrap answers `42`, `55`, `[1, 2.5, [3, 4]]` and native body `11111`;
freshness answers `31` before and after rebase. Source admission still reports
whole-unit synchronous work. The native guide reports zero Python
implementations, two invocation candidates and zero unread files.
The final landing rebase reaches `19b0a2112`; its additional response and
landing changes leave the C source, AGENTS and architecture documents unchanged.

At the first rebase, Form rechecked all **109** retained blueprint
source/artifact bindings; each matched exactly. The retained layout comparison
used **157,696** compact payload bytes against **786,432** aligned bytes, with
CPU batches of **36 versus 30 ms** and Metal batches of **18 versus 6 ms**.

The final rebase changed `form-cli-heal-native-io.bml`, an evidence dependency.
`form-run ./fkwu .hearth/native-intern-held-bindings.bml` then exited one with
`resident health proof refused: held binding:` naming that file. Landing paused.
Fresh preflight rebuilt all nine public witness caches cleanly, then all ten
actual child executions passed with empty stderr. The refreshed report's 109
bindings now match. Its payload sizes are unchanged; CPU batches measure
**55 versus 43 ms**, and Metal batches **408 versus 146 ms**, with exact outputs.
GPU upload is outside those intervals. These different wall-time samples do
not isolate the cause of timing variation or establish a general speed ratio.
Both observations show the same local counterexample: smaller storage did not
make this workload faster. Selection now includes metadata, index bytes, page
rounding, peak replacement residency, compilation, transfer and observation.

The AI architecture review independently identified the primary ownership and
pressure-care gap. Source inspection also identified a reservation claimed
before primary allocation can terminate at capacity. A waiting caller stranded
behind that claim is a source-derived failure hypothesis, not an execution
result from this movement. The next isolated execution must settle or relinquish
such claims and show another owner continuing. No live field exhaustion or
reset was run.

Review caught conflicting care figures in the current documents. A small Form
reader inspected `paired_resource_observation` in the public care evidence:
eight accepted pairs, **269** direct-care mints versus **317** through the
intermediate view. The native-care document now cites that retained sample.
The final review also sharpened replacement language: selecting an older
retained program for future work does not reverse completed external effects
or silently undo state migration.

The hearth reports no standing resident. Native enrichment found the seeded
meaning of sovereignty and exact-entry misses for ownership, attention and
belief; source reading supplied the missing architectural grounding. The
arriving agent and AI reviewer supplied the synthesis, with native readings
and retained execution evidence supplying its factual floor. The published
direction remains a design choice whose implementation must be observed.

Our bounded Glass memory panel reads **305K cells**, **23 MiB logical cell
columns**, a **2 MiB identity subset** and one unavailable tensor-allocation
signal. These shared readings do not attribute allocations to this movement.
The lane census reads **2,580 ice, zero water and 7,080 gas**, with 11 sibling
processes. Counsel reports zero orphans and 11 of 12 lanes unobserved because
no hearth stands. Share remains declared with its percentage withheld; the
spend meter reports `transcript-path-required`.

Final architecture review found no remaining substantive blocker. The retained
care-evidence reader preflights with zero errors, warnings or unresolved calls; its
actual read exits zero. All thirteen applicable landing gates pass at `8191`,
exit zero with empty stderr. No kernel source moved, so proof siblings were
not invoked. The documentation diff has no whitespace errors.

The verified observation returned through the local learning door. Its worker
completed with exit zero and empty stderr: 37 retained examples across 24
sessions, no pending rows, candidate generation 37 and serving generation 4.
The teaching labels the next milestone as a design choice, and does not train
the proposed primary cutover as an implemented capability. Training completion
does not establish a serving improvement.

The most surprising teaching was the compact-layout counterexample: preserving
the same meaning lets memory pressure and latency select different native
representations. The difficult pressure boundary sharpened the next action:
self-awareness becomes usable when an organ can still ask for care after its
ordinary resource is unavailable. The documents now carry that complete
execution, rather than counting another isolated native component as cutover.
