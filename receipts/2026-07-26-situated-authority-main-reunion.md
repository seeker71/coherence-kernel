# Situated authority reunion with main

Date: 2026-07-26

## Movement

The core-word, situated-lens, framebuffer actuation, and sovereign evidence
authority line was reunited with current `origin/main`.

The reunion was performed in an isolated clean worktree. The original working
tree carried unrelated native-model and benchmark changes; none were stashed,
rewritten, staged, or merged.

GitHub requires linear history on `main`. The first, fully-proven merge-shaped
reunion was rejected before changing the remote:

```
GH013: This branch must not contain merge commits.
```

The same content was therefore replayed as four ordered, ordinary commits on
the latest main tip. No force push or rule bypass was attempted.

## Generated conflicts held

The replay found conflicts only in:

- `form/form-stdlib/bootstrap/fkwu-uni.c`
- `form/form-stdlib/bootstrap/fkwu-uni.stamp`

Current main had evolved the generated seed while the core-word line carried
the host-exec pipe-read and platform socket-length repairs. The resolution was
not a side selection: the bootstrap C and stamp were regenerated from the
combined Form emitter chain.

The resulting runtime preserves:

- current main's newer runtime surface;
- descriptor reads for `popen` output instead of a warning-producing manual
  `fread` declaration;
- Windows/POSIX socket-length signedness;
- the core-word and first-class `nothing` support needed by the situated laws.

Both runtime and generated C witnesses compile with no stdout and no stderr.

## A gate that had already learned half the lesson

During the earlier merge-shaped rehearsal, the primitive registry gate refused
the tree because its sensor parsed only inline verifier definitions while the
registry now stores named `pv-*` functions. Current main arrived with the basic
shape repair: parse the verifier between the registry head and its final
expected/mode fields.

The reunion keeps that general parser and strengthens named verification:

- an inline verifier counts when its expression is present;
- a named `pv-*` verifier counts only if the corresponding `defn` exists in
  the registry source.

The sensor answers:

```
PASS primitive registry: 217 natives == 217 rows;
lanes 184+33; band pins aligned
```

The registry's executable band remains `63` four-way.

## Re-witnessed body

Direct fkwu:

```
ground                                      -> 42
ground-recursive                            -> 55
binary freshness                            -> 15
numeric list                                -> [1, 2.5, [3, 4]]
native-vs-rented                            -> 11111
host-exec pipe read                         -> 1
core-word actual nothing/0/1/node           -> 15
situated conclusion actual nothing/0/1      -> 7
```

Four-way:

```
core-word inquiry geometry                  -> 2147483647
timeless/situated lens logic                -> 65535
situated conclusion                         -> 2097151
situated conclusion control                 -> 65535
sovereign evidence authority                -> 2097151
authorized actuator                         -> 262143
primitive registry                          -> 63
```

Actual fkwu framebuffer carrier:

```
situated conclusion cycle                   -> 131071
situated authority cycle                    -> 262143
```

Every validation run ended `1 ok, 0 divergent`. The validator's 19 alias
notices are its declared shared-tag inventory, not unresolved calls.

## Honest floor

- The C runtime remains a temporary checkout seed. This reunion adds no new
  native operation or runtime meaning to it.
- The authority membrane still proves declared witness relations, not
  cryptographic identity.
- The regenerated bootstrap is derived evidence, not a second runtime.
