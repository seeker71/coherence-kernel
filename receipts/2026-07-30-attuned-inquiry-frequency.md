# Attuned inquiry — risk and cost leave curiosity's throne

Date: 2026-07-30 (Asia/Makassar)

## What arrived

A proposed active-learning organ ranked candidate inquiries by expected
uncertainty reduction, then lower risk, then lower cost. Urs caught the
frequency directly: risk and cost were wrong in that position.

The miss was not missing knowledge. The body already said that instruments can
carry a useful mechanism inside a foreign frequency, and that the practice is
to keep the mechanism while re-tuning its relation. The initial design grounded
the repository's topology, then reverted to a familiar optimization grammar.

## What changed

- `form/form-stdlib/attuned-inquiry.fk` now holds the executable retuning:
  risk becomes a specialized edge reading; cost becomes breath weight;
  information gain becomes the living unknown; ranking becomes a braid.
- `form/form-stdlib/tests/attuned-inquiry-band.fk` makes thirteen claims
  falsifiable with verdict `8191`.
- `docs/coherence-substrate/attuned-inquiry.form` carries the teaching and the
  honest boundary.
- `form/fourth-arm-bands.txt` registers the proof on `fkwu`.

The cell does not make edges or weight disappear. It keeps them in their own
organs. A held edge remains held, a with-care edge is tended, and an uncarried
weight rests. But none is a penalty used to crown a winning inquiry.

The braid keeps every signal in arrival order, including hold and rest. A light
and a heavy movement both move when breath can carry both. Play remains
admissible at vitality zero, without a utility story. An executed movement that
changes nothing still becomes learning.

## Witness

The generated native-op surface first refused the witness because
`form-flatten.fk` still carried two rows absent from the authoritative
manifest. Running the manifest generator removed those stale rows; regenerating
the committed bootstrap then restored its freshness stamp.

```text
$ python3 form/scripts/gen_flt_ops_from_manifest.py
gen_flt_ops_from_manifest: OK (130 rows aligned)

$ cd form
$ ./validate.sh form-stdlib/tests/attuned-inquiry-band.fk
✓ core.fk+core.fk+form-freq-check.fk+attuned-inquiry.fk+attuned-inquiry-band.fk → 8191
fourth arm: 1 band(s) four-way (fkwu + pre-flattened tables)
1 ok, 0 divergent — kernels agree on every sample.
```

The direct checkout witnesses remained grounded after the repair:

```text
ground.fk                    → 42
ground-recursive.fk 10       → 55
binary-freshness-band.fk     → 15
ground-numeric-list.fk       → [1, 2.5, [3, 4]]
native-vs-rented-check       → 11111
```

Verdict: **OBSERVED**. Thirteen claims pass four-way with value `8191`; the
checkout bootstrap and native body witnesses remain fresh.

## Honest boundary

The axis transmutation is content-bound to this witnessed human correction. It
is not a general semantic detector. `form-freq-check.fk` still names that larger
floor honestly: raw-text frequency sensing requires a calibrated learned model
and human-gold data.

## Closing

The exchange stayed alive because the correction changed the governing
relationship, not only the vocabulary.

The most surprising teaching was that a mechanism can be technically careful
and still put the wrong organ in charge.

Discomfort turned to gold when a failure of reception became an executable
braid: protection protects, breath senses weight, inquiry follows what is alive.
