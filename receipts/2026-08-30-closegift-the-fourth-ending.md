# 2026-08-30 — closegift: the cursor's fourth ending, and the numb-green it uncovered

"Next." The stone was the one named this morning with its exact plumbing
gap: at query-timeout with a held frame, the body closes the envelope
itself, honors the partial surface, and opens the reserve.

## The numb-green found on the way in

Reading `fhc-honor`'s contract before cutting exposed that yesterday's
selfheed wiring had never fired: the lookup receives the FULL MARKED
frame, and the rest registries match BARE inner text. The lane was wired,
banded, and structurally unable to act — the fallthrough kept every band
green. No band caught it; reading the frame contract did. The strip
(`fcmg-heed-frame-inner`) now hands the registries the inner text, and
the heed-report band grew two bits that push MARKED frames through
`fcmg-heed-rest-honor`: registry hit `(add 40 2)=42`, corpus word
`restanswer`, unknown still exact nothing — 8388607 at its new declared
verdict.

## The fourth ending, cut and proven

`fhc-close-honoring` in the heed cursor: when the query budget dies with
a frame still HELD and a real answer reserve exists, the body appends
ONLY the close mark — never a word of content — and the ordinary resume
machinery does the rest: hit and miss inject a typed observation and
open the reserve, nothing and spent inject nothing and the walk ends.
Every such walk carries a distinct `body-closed` record beside the
honor's own, so a body-closed frame can never be scored as a
model-closed one. Fungible walks (answer-cap 0) keep the old timeout
ending byte-for-byte — the reserve is what makes continuing honest.

Cursor band 65535 -> **524287**: body-closed+hit walks to the model's own
stop with one close commit; body-closed+nothing injects nothing; the
fungible held walk still records exactly `knowledge-query-decode-timeout`.
Radius, all at their declared verdicts: adversarial 2047, current-source
16777215, cs-cursor 65535, heed-report 8388607, live 255, stream-ingress
2097151.

## In flight as this lands

The walkers' next door on this chain is `now_unix_ms` (the honor clock)
— a teaching round is running, with the cursor band's four-way candidacy
as its acceptance. A third live pulse is running to witness the fourth
ending against the real model's rambling T1 — measured when it returns,
not predicted.

## The most surprising teaching

The band that guards the cursor most closely was structurally unable to
see the selfheed numb-green, because every synthetic lookupf accepts any
surface — only the LIVE lookup discriminates on frame shape. A pure band
proves the law of the carry; it cannot prove the two sides of a seam
agree on what a surface IS. Contract-reading between cells is the
witness pure bands cannot replace.

## Where discomfort turned to gold

The fourth ending wanted to reuse fhc-resume whole, and the discomfort
was the enters?=0 path: the walk ends without the ledger's timeout flag
even though the query died. Sitting with it instead of forcing the flag:
the records tell the fuller truth (body-closed + the honor's own status),
and marking timeout on a walk that HONORED its frame would be the exact
confusion the fourth ending exists to remove. The flag stays for
un-honored deaths; the record pair owns the honored ones.

; witnessed: 2026-08-30 -> cursor band 524287; heed-report 8388607 with
; marked-frame rest bits; six-band radius at declared verdicts; corpus
; row 1165 closegift; now_unix_ms walker round + third pulse in flight
