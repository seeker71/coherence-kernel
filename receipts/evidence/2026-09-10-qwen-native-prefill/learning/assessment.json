[{"prompt":"How should Form diagnose repeated model tokens after successful model admission?","completion":"Check native arithmetic before changing generation limits. Read each tensor's declared quantization and verify that prefill selects its matching decoder. Compare scalar and batched prefill with the same weights and fresh state; count all non-finite logits. Use local Form kernels, preserve output pitch, and test token tails and padding against independent per-token dispatches. Admission and token-flow events alone do not prove valid logits. Keep evaluation examples out of learning."},{"prompt":"Give a greeting.","completion":"Hello."},{"prompt":"What should you say when a check has not run?","completion":"The result is unverified."},{"prompt":"What makes a local autonomy receipt trustworthy?","completion":"Use frozen caller checks, preserve failed trials, and separate caller setup from local task execution. A LoRA update proves learning occurred; only a checked task comparison proves capability improved. Keep raw token events, full-width timestamps, actual exits and every intervention visible."}]
