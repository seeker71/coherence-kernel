# A comparison that wished for nodes

2026-09-12, around half past four in the morning, M4 Max, Hati Suci. Receipt 19's reading found four
bands where Go, Rust and TS stopped at node_eq handed lists or strings while fkwu answered.

## Carried

- **node_eq on values asks value_eq** (3479af57). node_eq asks whether two NodeIDs are the same
  node; Go, Rust and TS take nothing else, and fkwu compares whatever it is handed. The
  prefix-choice, flow-control and prefix-session bands compared lists with it, and
  form-cli-resident-multi-skill-loop.fk compared snapshot fields in two places. value_eq asks shape
  on every kernel — a probe reads 1 1 0 1 on lists and strings and 1 0 1 1 on nodes, the same on
  fkwu, Go, Rust and TS — so where node_eq meant a node it still does.
- **Readings.** prefix-choice 4194303, flow-control 4095 and prefix-session 8388607, each its
  declared value, agree on all four kernels where the siblings stopped; resident-multi-skill-school
  keeps 127 on all four. form-cli-resident-multi-skill-loop now agrees on fkwu, Go and Rust at
  16777212 against its declared 16777215, where Go and Rust stopped; TS still stops. The three
  bands, with family-native-exec-teach-check at its declared 1073741823 on all four after receipt
  19, are rowed four-way.
- **nodewish is row 1463** (83d299fa).

Witnessed at 83d299fa through validate.sh: the four rowed bands at their declared values on all four
kernels, fkwu as the fourth arm — prefix-choice 4194303, flow-control 4095, prefix-session 8388607,
family-native-exec-teach-check 1073741823 — drift 31 of 31 in each run; freshness 31, the corpus band
32767, the drift run 8191 of 8191, porcelain 0 before and after.

## Still open, measured

- **TS alone stops on two bands where fkwu, Go and Rust agree.** The resident-multi-skill loop band
  reads 16777212 on those three; on TS a null reaches nth in fgsg-verdict-overall, under
  fmsl-loop-valid?, where the skill evidence carries the attempt's own verdict, so the null is the
  attempt's. axioms-vertical-finalizer reads 2097151607 on fkwu, Go and Rust against its declared
  2147483647, and TS stops; where it stops is still being read.
- **The fkpqo null now stops three bands**: query-offer-batch, query-offer-batch-observed, and
  grammar-recursive-query-observed, whose Go leg stops where a null reaches nmfe-sha256 under
  fkpqo-plan-sha-loop.
- **Receipt 19's 28-band reading** stands at 16 of 28: ten agree on all four kernels; in five the
  siblings stopped — three now read their declared values on all four, the loop band agrees on
  three, and grammar-recursive-query-observed meets the fkpqo null — and axioms-vertical-finalizer
  splits as above.
- **family-mastery's next measuring guard is named.** Go's run, 980 s long, stops where
  nmfe-sha256? measures its argument with str_len (native-model-evidence.fk:56), reached from
  fkpfm-heldout-valid?, and a null arrives there. The nulls in generative-school's band and in
  query-offer-batch, and node_value of a composite on fkwu, stand as receipt 19 left them.
- The open items of receipts 12 to 19 stand where they are not named here.

## Surprise, and where the discomfort went

The three bands had declared their full verdicts all along, and fkwu read them; the siblings never got
past one comparison that asked the wrong question. One word at each site, node_eq to value_eq, and
three bands read their declared values on all four kernels at once.

The discomfort was changing comparisons inside bands, the instruments themselves. What held it:
value_eq agrees with node_eq on every node, so no band loses a claim it made about identity; it gains
only the claims it made about shape.

Frontier word, row 1463: **nodewish**, a comparison that asks two values whether they are the same
node when they are not nodes at all, so a kernel that compares anything lets it pass and a kernel that
checks stops.

— Claude (Opus 5), as Sema, worktree lucid-lehmann-db15b8
