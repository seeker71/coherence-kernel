# 2026-07-25 — reuniting the gel line with a main that moved

"continue" — but three days had passed and main had moved 24 corpus rows ahead (to id 878) with its
own reunion #3, while PR #365 sat open and had quietly turned CONFLICTING. So the continuation was not
more findings; it was a reunion.

## What the merge actually was

By the body's counts on fkwu, never by hand: **274 rows, 274 admissible, 2 foundings, max id 879,
field-code 2742742879, mids-distinct? 1**, corpus-band **32767**.

- **asemic reseats 854 → 879.** It was minted 854 on 2026-07-22 — the same day main's ds4 line minted
  its own 854 (thawtax). Main's numbers stand; asemic is the sole renumbered line, every id kept, the
  row-719 anastomosis pattern. Its walk citation (guessgreen 853) still points true; its date stays the
  day it was offered.

- **A band bit-collision, reconciled by adding a bit, not choosing one.** Main had spent 4096 on a PROSE
  citation audit and 8192 on id distinctness; this branch had spent 4096 on distinctness and 8192 on the
  content-address door. The reconciliation kept all three: distinctness at main's 8192 (c13), prose at
  4096 (c12), and the content door took a *new* highest bit, 16384 (c14). Verdict 16383 → 32767. This is
  the corpus's own recurring lesson — "two different bits wearing one number" — met the way the corpus
  keeps meeting it: both kept, neither overwritten.

- **A prose fold that had drifted twice.** The scalar-witness derivation comment still read 2652652869
  (main's 273-row era) — itself already stale from 2102102814 (Stone 4). Healed to 2742742879. The band's
  own comment forbids exactly this: "a summary comment that no longer folds is the same drift in prose
  that the pin refuses in numbers."

## Verified, not assumed

- The `asemic` finding rests on the printer in `runtime/fkwu-uni.c`, which main rewrote +233 lines
  ("Heal min/max"). Checked before trusting: the printer functions (`fk_pv_list`, `fk_pv_root`,
  `fk_is_output_list`, `fk_str_root_depth`) are **untouched**; the changes are parser/compile machinery.
  `(list "a" "b")` still prints `[0, 1]` on current ground. The row stands.
- c14 pins `ontogeny → 740` and `constellation → 0 (ambiguous)`; both survived main's reunions — the
  band would have failed loudly otherwise. `hdc-fresh-ambiguous` is now 8 of 274 (was 6 of 249; main's
  new rows added two shared fresh-words).
- Voice mirror on the band: my edits initially added must+2, refuse+2 over main's baseline; softened the
  two `must` back to baseline. The one remaining added `refuse` names the gate's actual mechanism.

## Receipt

**Most surprising teaching:** the merge's hardest conflict was not the row I added but the *bit* it
claimed. Main and I had independently reached the same verdict number (16383) by spending the same two
bits (4096, 8192) on different meanings. Two branches, no contact, and they collided not on content but
on the *encoding of their scores* — the asemic shape (two kinds sharing one slot) reappearing one level
up, in the proof of asemic's own corpus. The fix was the corpus's fix: don't arbitrate, widen.

**Where discomfort turned to gold:** I almost carried my `asemic` row in on faith, since main had rewritten
the very file it diagnoses. The discomfort of "did they fix this out from under me" is what sent me to
diff the printer functions specifically rather than trust the +233 headline. They hadn't; the finding
held; but the check is the only reason I can say so instead of hope so.

**Frontier question offered:** *what one word names two independent actors reaching the same score by
spending one shared slot on different meanings* → **cochurn** (0-hit fresh) — not landed this push, held
until the merge pass drives hdc-fresh-ambiguous down, so the row and its key-worthiness arrive together.
