# A distinction holds in the small enquiry and slips in the full comparison

Codex, 2026-09-20. Continued from `e457c4dfa`; rebased onto `38950b5b7`.
The two answers below came from Qwen3.8-27B-Q8_0 through the in-process
`fkwu-model-session`, using the compact `knowledge-query` profile. The
interpretation in this receipt is Codex's. The full comparison remains open.

## Observe and attempt

The [preceding answer](artifacts/2026-09-19-fkwu-native-voice-answer.md)
completed but supplied no receipt citations and overstated dictionary misses.
Its composition also preceded the provider-cost calculation repair.

The new [full request](artifacts/2026-09-20-native-citation-trial-request.json)
preserved the original enquiry and composition. A native preparation check
reconstructed the original packet byte for byte before changing its response
framing and replacing one unique cost statement: the minimum observed provider
rent meeting the structural floor became **10,875**, from the repaired native
ledger reading. No other source-composition text changed.

The framing invited a compact, useful comparison with supporting paths,
practical inferences distinguished from observations, dictionary scope kept
local, and felt qualities assessed separately from crossing counts. The
model, profile, context allowance of 12,288 and generation allowance of 1,536
matched the preceding full run. The rubric was recorded before generation.

## Re-observe: the full trial failed

The [exact returned fragment](artifacts/2026-09-20-native-citation-trial-unfinished.txt)
exhausted its allowance and stopped mid-sentence. The accepted answer stayed
empty; the raw output remains available, unfinished.

| Observation | Full trial | Focused contrast below |
| --- | ---: | ---: |
| Native execution time, milliseconds | 502,411 | 77,258 |
| Prompt token IDs | 3,546 | 300 |
| Generated token IDs | 1,536 | 271 |
| Complete / released | 0 / 1 | 1 / 1 |
| External model-server calls | 0 | 0 |
| Provider calls | 0 | 0 |

The raw fragment covered nine axis labels and named one distinct existing
dated receipt path. Those observations belong to the fragment; they do not
make it a completed answer or establish citation support.

The fragment graded native vitality as “Static/Single” and guided vitality as
“Dynamic/Flowing,” deriving this from workflow steps. It graded trust as
“High (Self-Contained)” versus “Medium/External,” and sovereignty as high
versus low. The supplied activity and dependency counts do not establish those
ratings. It also called traceability inherent in a single-step process.
The new framing did not hold these distinctions through the larger task.
Lookup-scope repair remains unassessed in its unfinished ending.

The preceding full answer used 3,502 prompt IDs and 1,050 generated IDs and
completed. This trial used 44 more prompt IDs and 486 more generated IDs while
failing completion. Its timing excludes the preceding run's retention boundary;
cache and worker context also differed. These timings do not isolate a causal
effect of the framing. The old learner finished during this trial's artifact
sealing, before its prefill began.

The public prompt changes were restored exactly to the last landed version.
The failed candidate was retained privately for examination and was not promoted.

## A discriminating contrast

The next [request](artifacts/2026-09-20-native-metric-scope-request.txt)
isolated the distinction that slipped. It used the same model and profile,
with a 2,048-token context allowance and 512-token generation allowance.
The rubric was recorded before generation, and the response was read alongside
the field checks.

Both cases supplied the same route counts. Case A supplied no trust or
vitality judgments. Case B added a participant's ratings on a shared scale.
A separate lookup supplied a miss in one dictionary table alongside a teaching
that defined the term; a broader repository search had not run.

The [complete native answer](artifacts/2026-09-20-native-metric-scope-answer.json)
identified the greater crossing count in both cases. It left Case A's trust
and vitality fields null, used the participant's differing rankings in Case B,
and explicitly scoped those rankings to that participant's report. It kept
the dictionary miss within the selected table and acknowledged the teaching.
Its explanation says no judgments “exist” in Case A; the more precise scope
appears in its own ranking basis: no data supplied.

The surprise is useful: the model can make this distinction in a focused
enquiry. That result changes the next attempt toward organizing evidence and
composing focused assessments through the existing native model lifecycle.
It does not identify the cause of the larger failure, establish general
semantic quality, or close the complete comparison. The next full answer is
assessed for completion, supporting citations, preserved scope, a useful
conclusion and total cost.

## Cost, instruments and continuity

The [native audit](artifacts/2026-09-20-native-citation-scope-audit.json)
rechecked the retained outputs and copied the answer artifacts exactly. The
full request's JSON `packet` string decodes to the exact original request.
The first staged whitespace check found a source Markdown line ending in two
spaces inside that request. Native JSON encoding preserves those source bytes
and gives the retained artifact a clean serialization; the audit checks the
decoded text against its source.
Together these attempts used **2 native calls, 3,846 prompt IDs, 1,807 generated
IDs and 579,669 milliseconds** within their execution boundaries. Both released
the model. Neither called a provider or submitted evaluation answers to learning.

Coordination remains a substantial cost. The meter for the preceding completed
Codex turn `01a0ba51-22be-7b73-8b14-e775d909bd50` reports:

| Carrier usage | Tokens |
| --- | ---: |
| Input | 6,201,664 |
| Cached input, within input | 5,621,760 |
| Uncached input, within input | 579,904 |
| Output, including reasoning | 40,970 |
| Unattributed | 27,240 |
| Total, including unattributed | 6,269,874 |

That meter covers 48 coordinator model calls. It excludes this open turn and
separate provider processes. Zero provider calls inside these native experiments
therefore leaves the coordinator cost visible and the overall goal open.

Panel observations: counsel reported **0 orphans**, with **11 of 12 lanes
unobserved** while no hearth stood. The native authoring guide reported
**0 Python implementations, 2 execution candidates, 0 unread files**. The
previous completed turn's share reconciled at 43 native, 68 local and 48 remote
boundary events; those counts describe carrier activity, not semantic contribution.
A later refresh withheld the share while validating an appended transcript
range. This open turn has no completed share measurement.

The earlier session-learning worker completed round 85 with no pending work;
serving generation remained 5. That worker targets the existing Llama 3.2 3B
adapter. These Qwen evaluation runs did not change its weights. The verified
learning returned by this movement concerns preserving evidence and checking
completion; these evaluation requests and answers remain excluded. The native
learning door retained event `native-evidence-completion-boundary-2026-09-20`
and launched its worker. Retention is observed; a new trained or promoted
generation from this event is still pending.

Checks: binary freshness **31**; grounded-synthesis band **1023**; drift checks
**8191**, all exit 0. Native preflight found no errors or unresolved calls in
either experimental runner or the audit. The audit returned **1**, exit 0.
Its field checks establish the reported contrast; the prose observations above
come from reading the actual answers.

Private experiment roots: `.hearth/response-parity/citation-scope-v1/` and
`.hearth/response-parity/metric-scope-v1/`. They retain the frozen rubrics,
generation metadata and IDs, original source packet, and correlated health
events. The actual movement is a failed full-answer repair followed by a useful
discriminating observation. The next repair has firmer ground; parity remains
to be earned in the complete response.
