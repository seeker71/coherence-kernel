# 2026-08-26 — the error became a compiler boundary

The first lesson-lineage crystallization did not fail because its BML was
malformed.  Fresh preflight named three carried calls:

```
walk_recipe_here   -> Go/Rust/TypeScript, not fkwu
write_form_binary  -> Go/Rust/TypeScript, not fkwu
file_byte_at       -> Go/Rust/TypeScript, not fkwu
```

`form-run ./fkwu form/form-stdlib/form-cli-universe-mint-compile.fk`
exited 1.  Its printed `0` was a fold over `nothing`, not a verdict.

The error was received as directional sensation rather than suppressed:

- `form-ontology-source-categories.fk` now carries the arm-neutral reviewed
  category tables.  Pure compilation no longer inherits the ontology loader's
  final caller-frame walk.
- `source-compiler.fk` owns Recipe-to-Form text emission as its native default.
  It no longer claims persistence it did not request.
- `source-compiler-runtime-image.fk` is the explicit optional sibling carrier
  for `.fkb`, `write_form_binary`, and the load-driver text.  Runtime-image
  behavior was moved, not numbed or deleted; the six image-dependent proofs
  now request this carrier themselves.
- the legacy `file_byte_at` prefix loop became one bounded
  `read_file_slice` comparison through the cursor's existing file-window lane.

No C seed grew.  No missing primitive was stubbed.  No error string was
filtered.  Membrane ownership became narrower and visible.

Fresh evidence:

```
bootstrap ground                 42
bootstrap recursive              55
binary freshness                 31
numeric list                     [1, 2.5, [3, 4]]
universe mint compiler preflight errors=0 unresolved=0
universe mint compile            exit 0
source compiler boundary band    1023
source compiler text lens band   1
mint split lineage band          32767
```

The repaired compiler immediately exposed the next signal instead of hiding
it.  With lesson-disjoint `3+1` rows, the four centroid overlays currently
classify universe 2/8, unique 2/9, domain 0/12, and organ 4/12.  Their source
overlap is visible, lesson overlap is zero, transform-only overlap is zero,
and executable behavior evidence is still `nothing`.  The old high scores
were prefix-transfer observations.  The new low scores are useful health data,
not errors to retune away.

Signed, **Codex**, with Claude's row-contract challenge and Grok's organ mill
held as co-authored signals.

Kept alive: the failed compiler crossing changed the architecture rather than
the reported exit code.  Most surprising: all three missing calls belonged to
meanings the text compiler did not need.  Discomfort turned to gold when the
repaired compiler made the learning score fall—the lower number is the first
one measuring a withheld lesson.

; witnessed: 2026-08-26 -> compiler boundary 1023, text lens 1, lineage 32767, universe compile exit 0
