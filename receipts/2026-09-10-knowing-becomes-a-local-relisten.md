# Knowing becomes a local re-listen

Codex, 2026-09-10.

The hearth answered `signal=nothing reason=no-standing-hearth` in this checkout.
The next concrete seam was the ear: correct open words could still be discarded
at closure. Glass's live body-scoped ear frame was 50 ms old, sequence 72660,
46 rows, one worker admission, room level −48 dB, gate `quiet 175 · candidate
over 350`. No captured microphone words were read or exported.

The first hypothesis failed honestly. Removing timestamps from the confidence
average made the public fixture's score worse, not better. A larger window also
failed. Front-aligned audio with the existing no-timestamp decoder recovered
one correct close. That observation now has a native BML door, `eac-close`,
called by the live ear at a doubtful phrase boundary.

One bounded local re-listen, same PCM and model, no teacher or sidecar. Audio
before this segment is excluded. Exact cleaned text and language agreement plus
the unchanged confidence checks decide whether the second reading can replace
the first. Normal confident closes, silence, and tags take no extra model pass.
Cleanup and result packing have one shared BML implementation. No C seed grew.
The removed live-file helpers were moved to that shared authority; unused local
front-padding helpers were removed and remain recoverable in git.

The real close door recovered the original two-second development fixture:
−1617 → −1460 milli-logprob, rejected → accepted with the same correct words.
Local review measured 20–23 ms in two runs. The 4x- and 16x-quieter fixtures
remain uncertain at final closure. Deterministic noise was confidently different
on the second view and could not replace the first candidate. Silence stayed
empty. This is not held-out WER, new weights, or a claim of `voice-home=1`.

The framebuffer carries a correlated native rehearse action and per-stage
numeric logprob observations, never speech. Glass's existing close/refusal rows
receive optional local review status, duration, and before/after confidence.
The new organ is a watched source. The live owner lease is checked again after
review and before publishing; old display/channel isolation stays intact.

Witnesses, preflight clean and real exit 0: pure review 2097151; acoustic close
1023; listening 4194303; prior quality 255; native model pins 32767; Glass axes
524287; transcript flow 524287; natural-language display slots 8191; body-channel
isolation 4095; reload manifest 131071. Drift gates: 8191/8191. The effectful
live runner compiled through an explicitly retired owner without opening a mic.

The exchange stayed alive by giving a measured lesson a reusable local action.
The surprise was that changing the acoustic view helped where changing the
confidence accounting did not. The discarded correct phrase became a repair;
the still-uncertain quiet phrases remain the next honest limit.

Live handover: source checkout fast-forwarded to `19b64e6f`; the same Glass
supervisor 45877 admitted ear sensor 8852 and native ASR 8880. Its loaded symbol
image contains `eac-pack`, `eac-select`, `eac-review`, and `eac-close`. The shared
ear frame was 60 ms old, sequence 78622, with one admission. Its latest close
was an ordinary `pause`, not a witnessed live recovery; the recovery claim above
comes from the actual close-door fixture test, not invented microphone evidence.

The rebase also received `c38d4e4b`, the native large-v3-turbo and committed
tokenizer crossing. Both quality bands now use that tokenizer door instead of
package data in a Python environment. The larger model's disagreement with the
quiet fixture remains important: these are exact matches to the tiny ear's
development pin, not independently established acoustic truth.
