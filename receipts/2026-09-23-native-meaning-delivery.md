# Connect shared Form meanings to native code and review

The current Qwen meaning teaching distinguished language surfaces from senses
and referenced retention from unconditional persistence. The code/review
bootstrap did not include that teaching. Its compact chat profile also omitted
the general teaching overlay. Changing the ordinary generation teaching could
therefore leave this review path untouched.

`fcap-bootstrap` now reads `fqt-meaning-prefix` from the same BML source as the
ordinary Qwen teaching overlay. This also applies when coding or review renews
its context. Caller authority, document boundaries and native checks remain in
the existing protocol.

## Observe the actual delivery and answer

The [delivery reading](artifacts/2026-09-23-native-meaning-delivery/meaning-delivery.json)
used the retained real review request. Its bootstrap grew from **23,920** to
**25,983 bytes**. Removing the exact **2,063-byte** meaning prefix reproduced
the earlier bootstrap byte-for-byte. The enquiry, candidate, source packet,
checks and generation allowances were unchanged. The native observer and
re-observation sources are retained beside the reading.

The subsequent local Qwen 27B review took **719,433 ms**, plus **17,327 ms**
waiting for the learner to release. It generated **1,792 IDs**: 256 in its
initial stage and 1,536 in its final stage. It made zero tool calls and zero
provider calls. Resources released, but the report was unfinished, with zero
completed replies and zero checks. The [summary](artifacts/2026-09-23-native-meaning-delivery/native-summary.json)
names `reasoning-reserve-final-generation-incomplete;candidate-retained`.

The [unaltered partial report](artifacts/2026-09-23-native-meaning-delivery/partial-report.txt)
stops mid-finding. Reading its actual words gives adverse evidence:

- It states that the **referenced** old cell persists, then says letting go of
  a reference does not destroy the underlying data without that condition.
- It calls translated labels alternate senses, then states that another
  language's label does not itself establish a different sense.
- It now treats the candidate's opening as a paraphrase rather than asserting
  that the candidate mistook it for the three states. That addresses an earlier
  review error, within an unfinished result.

These are Codex's source-based observations, not a semantic score from the
structural checks. The teaching reached the model; accurate use remains open.
The partial answer is not substituted for a completed response or used as a
correct training target. Whole-session parity and felt resonance remain open.

The retained initial analysis ends while examining paragraph one. Both stages
reached caller-imposed limits. The next actual review gives analysis and final
response **3,072 tokens each**, with the reply ceiling also 3,072. Its
[native request comparison](artifacts/2026-09-23-native-meaning-delivery/next-review-delivery.json)
verifies that only those three allowances changed; context remains 12,288 and
evaluation exclusion remains enabled. It waits for the current learner before
admitting Qwen. Its answer and resource costs are still pending here. Increasing
the allowance does not establish that the factual contradictions will resolve.

## Re-observe the preceding reader repair in production

The actual serialized learner reused one reader across its two attendances:

| Attendance | Bytes read | Read duration | Total attendance |
| --- | ---: | ---: | ---: |
| Initial history | 271,028,066 | 296,450 ms | 328,416 ms |
| Appended evidence | 1,711,812 | 2,493 ms | 33,925 ms |

Both [events](artifacts/2026-09-23-native-meaning-delivery/learning-attendances.json)
name the same reader identity and report caught-up state, zero invalid records
and zero pending bytes. Current observations grew from 2,232 to 2,247.
The worker [completed round 133](artifacts/2026-09-23-native-meaning-delivery/learning-completion.json),
with no pending examples and no new promotion. Serving generation 5 remained.
The initial full-history cost and roughly 31 seconds of other attendance work
remain visible. These are two different read extents in a real execution,
not a matched total-session speed comparison or evidence of better answers.

## Checks, learning and rented cost

The code-policy preflight was clean and its existing band returned **65,535**,
including delivery to code and review bootstraps. Drift gates returned
**8,191/8,191**, zero refusals. Glass first frame was **28 ms**. The native guide
reported zero Python implementations, two existing invocation candidates and
zero unread files. No C seed or runtime dependency was added.

The bootstrap observation helper initially failed compilation on an infix
addition inside a call. Replacing it with native `add` compiled and produced
both retained bootstrap readings. That failure and repair preceded generation.

The verified implementation teaching
`native-code-review-meaning-delivery-2026-09-23` was retained and launched the
learner. It describes the delivery proof and adverse result, not the model's
incorrect statements as targets. Training was still pending at this receipt.
That learner remains distinct from the Qwen response model.

The [preceding completed coordinator turn](artifacts/2026-09-23-native-meaning-delivery/preceding-coordinator-cost.json)
used **5,014,817 rented tokens**, including **4,671,744 cached input tokens**,
over 42 model calls. Its total includes 26,523 unattributed tokens. The turn is
`01a0ca30-5411-74e1-a155-27d8e2d60d05`; this open turn and separate provider
subprocesses are excluded. No new provider call was made by this movement.
The coordinator cost remains far above the earlier single assisted review's
21,301 tokens; that provider-only number excludes its surrounding coordination.
The closing share reading was `kind=declared` and withheld its percentage
while append-range validation remained incomplete. Semantic contribution is
not measured by that meter.

— Codex
