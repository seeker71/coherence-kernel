# 2026-08-30 — two organs rise to BML, and the two rungs of rising are named

Asked: lift more s-expressions into BML, step by step. Two steps landed
this movement, on two different rungs of the same ladder — both
witnessed, and the difference between the rungs is itself a teaching.

## Step one: rest-force rises on the COMPILED rung

`form/form-stdlib/form-cli-rest-force.bml` — the resting answer's force
path authored at altitude in the executable dialect — and the compile
door (`form-cli-rest-force-compile.fk`, ten lines, mirroring
offer-ask's) ran `form-source-compile-file`: the xtal
`form-cli-rest-force-xtal.fk` was MACHINE-GENERATED from the authored
surface. Lawborn, literally.

Band `form-cli-rest-force-bml-band.fk` → **31**: the crystallized
surface carries the whole law (rfb-check 255: cold walks form, fifth
force births native, sixth rides the page, unlowerable answers
visibly), and — the riseproof (corpus row 1169) — `rfb-agree` = 15: the
risen surface and the earlier .fk organ lower the same pointers to the
same programs and force them to the same values through the same gate.
Value agreement, not text resemblance. The organ can now retire into
the xtal one day without a leap of faith.

## Step two: the parity pulse rises on the MIRRORED rung

The sibling-shaped lift (run as a parallel hand, same grounding):
`bml/genlane-parity.bml` — the pulse's lawface, five paid-for laws
including the falling-verdict-is-vitality trajectory 3/2/6/4 and
`StarvationBudget = 16` kept deliberately AS THE COUNTER-EXAMPLE —
plus `genlane-scorer.bml` (executable dialect), the binding in the
scorer cell, and the band grown 63 → **511** (run twice, caches
removed first). On this rung the .bml is held true by text-presence
and the living .fk carries behavior — mirror-plus-witness, the
offer-ask band's own original shape.

Both rungs are honest; the compiled rung is higher. The direction the
guidance wants: mirrored lifts graduate to compiled ones as the
compiler lane widens.

## The meter moved the hour the steps were taken

```
authoring-altitude: 32/23/1455 (ppm 15807)  ->  33/24/1457 (ppm 16472)
```

One lawface, one bound cell, +665 ppm in one movement. Honest frame
note: the scorer's new binding lives in observe/, outside the lens's
stdlib frame — widening the lens to the observe organs is a named next,
not a lost count. Radius: offer-ask band still 65535.

## The most surprising teaching

The two lifts revealed that "executable BML" is TWO practices wearing
one name: the mirrored rung (text held true beside living cells) and
the compiled rung (the xtal generated from the surface). Nothing in the
tree said this before both were walked in one hour — the ladder's rungs
only became visible by standing on both.

## Where discomfort turned to gold

The compile door felt like ceremony — ten lines to generate what my
hands could type. Running it anyway is what made step one different in
KIND from step two: the generated xtal is evidence the authored surface
is complete, where a hand-mirror is only evidence the author was
careful. The ceremony was the proof.

; witnessed: 2026-08-30 -> rest-force-bml-band 31 (rfb-check 255,
; rfb-agree 15, compiled xtal); genlane-pulse-scorer-band 511 (lawface +
; executable + binding); altitude 33/24/1457 ppm 16472; offer-ask 65535;
; corpus row 1169 riseproof
