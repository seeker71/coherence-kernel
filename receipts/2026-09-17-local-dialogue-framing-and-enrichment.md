# Separate framing, native evidence and reasoning

Signed: Codex. Witnessed 2026-09-17 on `fkwu`.

The previous guidance expansion was reverted after it added source reads and
time without completing the enquiry. This movement kept that adverse result
and tested different causes through the local-only path.

## Framing alone

The review bootstrap's opening sentence changed from a coding-member role to a
collaborator answering the person's enquiry. A native check restored that one
sentence and verified the remaining prompt was byte-identical to the frozen
baseline. Request bytes, sources, query selection and checks were unchanged.

The run completed in 224757 ms, generated 398 local token IDs, requested no
additional reads, passed the original checks and released its model. The
baseline took 231492 ms and generated 386 IDs. The answer retained its defensive
opening and postponed revising itself. This did not establish the intended
behavioral improvement. The framing change was reverted; public policy remains
at its previously landed content.

## Actual Form enrichment

With the original bootstrap restored, native `fce-command` read the exact terms
`trust resonance offer nothing identity`. Its 1350-byte attributed result became
one additional resident document and source observation. All original documents,
the enquiry and original source/report checks remained intact. An additional
source check preserved the new reading exactly. This is changed context,
explicitly recorded in the request and prompt, not an identical-input comparison.

The reading carried trust and resonance definitions, their authored relations,
an existing offer codebook entry, an absent locale row for nothing, and no exact
identity entry. It preserved the distinction between short codebook anchors and
kernel content identity. Separate native lookup also found no exact entries for
translation or vocabulary in these resources. An absent entry is not absence of
all related capability in the repository.

The enriched run completed in 237502 ms, generated 413 local token IDs, requested
no additional reads, passed the original and added checks, preserved documents
and released its model. The native audit computed 6010 ms above the original
baseline. The model used the new trust definition but continued narrating its
procedure and deferring the enquiry. Translation remained an unmarked
reinterpretation of the voice teaching rather than use of the actual codebook
mapping. Relevant evidence reached the answer cheaply in this run; useful
completion and resonance parity were not established.

## Existing native reasoning option

The next attempt preserves the enriched request and adds only
`initial_reasoning_tokens=4096`. The enquiry prompt is byte-identical; the native
reasoning profile changes the generation channel. Its allowance includes both
reasoning and the first answer. Only the completed final-channel answer can
reach the existing action/checking controller. Private generation evidence stays
outside this receipt and outside training targets.

The run completed in 665020 ms, generated 2470 local token IDs including its
private reasoning, made no further source reads, passed the unchanged checks
and released its model. The final-channel boundary and completion were both
observed. It used 427518 ms more than the non-thinking enriched run.

The answer became longer and more specific, but introduced unsupported claims.
It quoted the referenced-cell condition and then treated revision of this prose
response as guaranteed reversible, while also describing the old version as
unreferenced and composting. It turned the absent resident answer into a claim
about a cell's current composition and identity. Neither application had an
execution witness. It proposed investigating the dictionary's identity gap but
performed no additional tool action. More reasoning did not establish the
required completion, source discipline or resonance.

The next relevant attempt is an executed primitive example with its limits:
observe what a real composition change preserves, distinguish that from the
lifetime of response text, and carry those actual results into the explanation.
This follows the specific unsupported applications above; it is not a claim
that adding such a witness has already improved the answer.

## Evidence and scope

All experiments use the ordinary native request door. No provider process is
invoked by these local runs; coordination still uses rented tokens. This is a
repeated development enquiry, not held-out evidence of general parity.

Private roots are under `.hearth/response-parity/generalization-v2/`:
`review-frame-v1`, `enriched-dialogue-v1`, and `reasoned-enrichment-v1`. Each
retains its request, exact prompt, owner, result, answer and measured receipt.
The native pair audit preserves actual side-by-side answers and records changed
context separately from the original assertions. All three owned processes
completed and released. The native totals audit records **1127279 ms** of summed
runner time and **3281 generated local token IDs**, with **0 provider processes**
inside these runs. It excludes the earlier baseline, coordinator gaps and
coordinator rented tokens. None of the three attempts established the intended
dialogue improvement; no candidate prompt change or default reasoning expansion
was retained as a successful repair.

All native helpers had clean preflight. Policy band returned 65535, exit 0.
Native guide: 0 Python implementations, 2 existing invocation candidates,
0 unread files. Counsel panel: **orphans 0**, with 11 of 12 lanes unobserved and
no standing hearth. The preceding procedural teaching completed candidate 49;
serving remains generation 5 with 4 promotions. That learner updates Llama, not
the Qwen model measured here. The native Qwen rank-one fitting experiment is
separate and does not establish a language-quality training result.

Drift gates returned 8191, exit 0. At the closing checkpoint the parent goal
meter read 8409114 tokens; the transcript meter read 1754617 cumulative output
tokens. These include coordinating work and have different scopes from the
local generation counts. No minimal-rental result is claimed for this movement.

The verified procedural teaching was retained under
`2026-09-17-local-framing-enrichment-reasoning-observed-v1`; the local worker
launched, with completion pending at this checkpoint. Its target records
execution mechanics and observed limits, not an assessed answer.
