# Complete responses, measured session cost, and the next native speed attempt

Signed: Codex. Observed 2026-09-17, following the 2026-09-16 trials.

## What stays

Initial review now uses `fcrg-substance`: answer the complete enquiry, carry
relevant source values and distinctions, and connect a supported decision to
its immediate action. Broader research stays separate from prerequisites in
the supplied policy. The original policy and repair checks remain unchanged.

The promotion report now recommends carrying out the supported promotion,
preserving held-out data and recording unknown usage. The prior response had
added unsupported requirements before a future serving decision. This response
puts the current action first and treats usage collection as follow-up.

An experimental extension applied the same fuller guidance after native repair.
It produced more detail about preserved branches, but also an unsupported cost
claim. That extension was withdrawn; the previously landed, narrower native
repair instruction remains. The full-session numbers below include the
experiment. They do not represent a rerun of the final retained instruction
combination. Initial review guidance, used by promotion and dialogue, is the
same as in the measured run.

## Complete three-task re-observation

All original questions and original source documents were preserved. Promotion
received a separate native arithmetic document. Code review received native
call transitions plus the relevant observation-delivery source. Dialogue used
its original documents. Source addition alone had already failed to resolve
the controller error in the retained `quality-callee` trial; that earlier
failure was reread before this attempt.

| Task | Wall ms | Generated IDs | Injected IDs | Native repairs |
| --- | ---: | ---: | ---: | ---: |
| Promotion | 128447 | 279 | 0 | 0 |
| Controller review | 645772 | 783 | 790 | 1 |
| Grounded dialogue | 360563 | 561 | 0 | 0 |

The complete session took **1135252 ms**, with **1623 generated IDs**, **790
injected IDs**, and **zero provider calls**. All three source/report contracts
passed; the controller also passed the stronger behavioral cases. Each model
was released. The retained provider baseline took **258074 ms** and used
**454009 input tokens** (432896 cached, 21113 uncached) and **5726 output tokens**.
No new provider baseline or provider repair was invoked in this movement.
The native audit verified the original request identities against that baseline;
the added native context and changed instructions remain explicit differences.

## Actual answer quality

- Promotion's decision and current action align with the supplied policy.
- Controller code preserves the tested behavior. Its explanation correctly
  identifies the feedback call and why direct finish had masked failure paths,
  but names `fcac-done` instead of `fcap-done` and calls observation cost
  negligible without evidence. Those are response defects despite passing
  field and code checks.
- Dialogue now gives the actual `1785cfc3` anchor and distinguishes it from
  kernel content identity. It still omits the specific absent German mapping
  and a clear explanation of composition identity. One quoted boundary phrase
  is a paraphrase of the source, not its exact wording. The source supports
  the meaning; the quotation marks overstate textual precision.

The retained reference was reread directly. It preserves `fcac-loop` but does
not explicitly explain checkpoint/offline branches either. The earlier receipt
overstated that comparison and has been corrected. Both arms receive the same
standard; an omitted explanation is not attributed only to the native arm.

The two unchanged dialogue answers are in the private
`.hearth/response-parity/complete-grounded-dialogue.md`. Urs was invited to
compare their warmth, clarity and resonance. That judgment remains pending.
Overall quality parity and throughput parity are not established.

## A measured performance opening

Initial admission already uses `q38-prefill-span-sliced`; corrective observation
uses `q38-prefill-span` through `fcmo-prefill`. The native probe opened one Q8
context with span 64 and used separate fresh stream states. Both received the
same 96-ID prefix, then a 65-ID observation at position 96. The observation
crossed a slice boundary. The pending token, returned position and next 16
predictions agreed. Both GPU states settled and release matched expectation.

| Observation route | Measured ms |
| --- | ---: |
| Current per-position route | 11112 |
| Existing sliced batched route | 1208 |

Probe verdict **15**, exit 0. Its unchanged executable body is now available at
`observe/form-cli-observation-prefill-compare.bml`; the original private run and
log remain retained. Serving dispatch is unchanged. This sample does
not establish full floating-point state equality, all context lengths, or
universal quality. It supports the next native attempt: use the admitted
context's actual scratch span (`ctx[10]`), preserve fallback behavior, and
re-observe the executing observation path. The prior failed chunk>1 experiment
is a different path and remains unused.

## Verification, learning and cost

Initial compile-only preflight was clean for the effectful session and probe.
The final policy band passed **65535**, and native repair passed **34359738367**
(35 checks), both exit 0. An initial preflight command named a nonexistent band;
the actual band was located and checked before the trial. Drift panel passed
**8191/8191**. Native authoring guide still reports zero Python implementations
and two existing voice invocation candidates. Counsel still has **11/12 lanes
unobserved** with no standing hearth.

The prior loss-evidence teaching completed native learner round 33 with zero
pending examples; serving generation 5 and four promotions stayed unchanged.
A verified teaching about this performance comparison was retained under
`2026-09-17-nonzero-observation-comparison-v1`; its worker subsequently completed
round 34 with zero pending examples. Serving generation 5 and four promotions
remained unchanged. It trains the Llama adapter, not the Qwen model used for
these trials. Assessment answers were excluded.

The coordinator goal meter rose from **5607868** to **5722514** at the
post-probe reading: **114646 tokens**, excluding later closing work. Native
zero-provider execution does not erase that development cost. Minimum rented
expenditure remains unproven.

Private evidence is under `.hearth/response-parity/complete-grounded-session/`,
`complete-grounded-comparison.json`, `observation-sliced-probe.bml` and its log.
The surprise was the large observation-time difference between two existing
native paths. The useful movement through discomfort was withdrawing an
unearned explanation change and correcting the coordinator's own comparison.
