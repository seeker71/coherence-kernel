# Non-toy native multimodal expansion over public reality

Date: 2026-07-18

This increment advances five requirement-level gaps with public source bytes,
released checkpoint weights, negative interventions, and operational Form
consumers. It does not declare the 10,000 × 13 × 13 goal complete.

## Exact live movement

| Requirement lane | Before | Now | Live evidence |
|---|---:|---:|---|
| attributed semantic anchors | 8,444 / 10,000 | **9,889 / 10,000** | 1,445 revision-pinned Wiktionary definitions; 111 explicit malformed-token holes |
| arbitrary public PL understanding | 0 programs | **13 programs / 130 roles** | pinned MIT Exercism Luhn implementations, AST + mutation-sensitive semantics |
| public-content world identities | stale hand floor 28 | **850 exact union identities** | Tatoeba text + Lingua Libre human speech + Commons photographs |
| native visual learned inference | no released pixel layer | **603,136 released parameters** | complete DINOv2-L/14 patch projection, GCA, cache, map/world causality |
| native acoustic learned inference | no released waveform layer | **92,544 released parameters** | complete Whisper-tiny encoder conv1 on CC0 human speech |

The strict completion ledger remains **4/22 complete, overall 0**. Its changed
incomplete rows now read:

```text
semantic-concepts                  9889 / 10000
pl-arbitrary-source-understanding    13 / 130000
world-model-concept-identities      850 / 10000
native-acoustic-weights               0 / 1  (complete conv1, partial recognizer)
native-visual-weights                 0 / 1  (complete patch projection, partial model)
full-multimodal-cross-product          0 / 1
```

## Real semantic content, not filler

The semantic builder queried all 1,556 prior misses against revision-pinned
English Wiktionary pages. It accepted 997 exact-title definitions and 448
capitalized-title proper-name definitions. Each accepted record retains page
ID, revision ID, timestamp, English-section hash, part of speech, definition,
and definition count.

The remaining 111 anchors are published in
`model/concept-semantics-10000-wiktionary-lexical-remaining.tsv`. Examples are
subtitle/tokenization debris such as `didn`, `you-`, `chffffff`, `ofthe`, and
confusable `yöu`. They remain holes. Inventing glosses for them would hide the
actual next work: repair or replace defective members of the ranked substrate.

The production `presence/concept-10000-13-runtime.fk` now consumes method-4
meaning directly. Anchor 0, `you`, carries its pinned Wiktionary definition
through JavaScript source generation/recovery and enters the same native world
as the runtime's WordNet and morphology-backed cells.

## Thirteen real programs and adversarial meaning

The PL lane reads 13 upstream Exercism Luhn implementations at pinned Git
commits under the MIT license: Python, JavaScript, TypeScript, Java, C, C++, C#,
Go, Rust, Ruby, PHP, Swift, and Kotlin. Python source is parsed but never run.

Live Form totals:

```text
programs / source bytes                 13 / 9,558
tokens / AST nodes                  2,265 / 1,989
balanced groups / max depth            276 / 6
semantic roles                         130 / 130
real programs admitted                  13 / 13
modulo-11 mutations parsed/rejected      13 / 13
unclosed-group mutations rejected        13 / 13
```

The ten roles cover normalization, length guard, digit validation, traversal
direction, iteration, alternating selection, doubling, subtract-nine folding,
checksum accumulation, and modulo-ten validity. The modulo-11 variants remain
syntactically valid but lose checksum meaning, while an appended unclosed group
fails syntax. The central production runtime now requires these real-source
admission and negative results for its score of 255.

## Released visual weights become causal

The exact released LingBot checkpoint records carry DINOv2-L/14
`patch_embed.proj.weight [1024,3,14,14]` and bias `[1024]`: 602,112 weights plus
1,024 biases. Form validates ZIP metadata/CRC/f32 layout, decodes real video
frames, applies upstream ImageNet normalization, and executes the complete
602,112 learned multiply-add patch projection.

Four public real-place examples produce distinct full 1,024-channel tokens:

| scene | vector sum | L1 distance from loop |
|---|---:|---:|
| loop | 0.3681681310 | 0 |
| Oxford | 9.5278445758 | 220.2115189622 |
| university | 30.1378729613 | 373.3804950277 |
| courthouse | 26.5716682673 | 235.1447312592 |

An independent Node implementation matches the Form output to `1e-12`. Two
consecutive loop frames also generate a 4×3 learned spatial grid. Their GCA
output is `[0.0081609233,-0.1347291075,-0.0203584375,0.0518580872]`; zeroing
the learned tokens produces `[0,0,0,0]`, an L1 causal effect of
`0.2151065555`. Learned visual embeddings feed 64 depth/cloud/world objects,
and real-frame correspondence error moves `2,385,740 → 284,311`.

Full native visual parity remains zero because the 24 DINO transformer blocks,
released learned GCA, camera head, and DPT depth head are not all native.

## Released acoustic weights hear a human recording

The acoustic lane pins openai/whisper-tiny at revision
`169d4a4341b33bc18d8881c4b69c2e104e1cc0af` and loads the complete
`encoder.conv1 [384,80,3]` plus bias `[384]`: 92,544 released parameters.
The input is the CC0 Lingua Libre human recording M92036254, never TTS.

Form reads PCM, computes a real Hann/direct-DFT/Slaney log-mel tile, normalizes
it, applies all 92,160 weights and 384 biases, and emits all 384 channels. An
independent Node direct-DFT reference agrees within
`3.4681306715356186e-9`.

The operational neutral-path door reports:

```text
native-acoustic-live human=1 tts=0 released-parameters=92544
output-width=384 learned-activity=1 candidate-count=1
detected-id=571 detected-surface=book world-admitted=1
zero-ablation-admitted=0 native-recognizer=0
```

The transcript decoder is still host Whisper. Native conv2, transformer
encoder/decoder, tokenizer, and search remain owed, so the strict full native
acoustic row stays zero.

## Exact public world union

The public world cell constructs an actual set union and persists every member:

```text
Tatoeba text 828; human audio 8; public visual 24
text∩audio 4; text∩visual 6; audio∩visual 1; all three 1
exact union 850; persisted entities 850
```

Concrete source masks (`text=1, audio=2, visual=4`) include water 377=`7`, book
571=`3`, hospital 628=`1`, building 752=`4`, bridge 1520=`5`, and butterfly
5547=`4`. The visual gate re-hashes all 24 JPEGs and joins every target label to
its exact original classifier stream above the confidence floor. Hash or join
failure zeroes that source rather than preserving a memorized count.

## Reproduced gates

```text
ground / recursive / freshness / numeric     42 / 55 / 15 / [1,2.5,[3,4]]
native-vs-rented                              11111
semantic lexical audit                        4095
arbitrary public PL                           8191
DINO patch live / learned map                 255 / 1023
Whisper conv1 pure / human-world live         1023 / 511
public reality exact world                    4095
production lexical+arbitrary integration      255
strict incomplete completion ledger           1023
```

No Python executed. `runtime/fkwu-uni.c` did not change.

## What remains genuinely open

- replace or repair the 111 malformed ranked anchors, then regenerate affected
  10k × 13 tables without breaking stable identity;
- human review and fluent generation across 130,000 NL cells;
- full native Whisper and full native LingBot released-weight graphs;
- visual and acoustic content coverage across all 10,000 identities;
- arbitrary-program semantic diversity beyond one algorithm family and 13
  examples toward the 130,000 requirement;
- a content-proven 10,000 × 13 NL × 13 PL cross-product gate.

The new work is called by non-test presence doors, the central production
runtime, the ordinary world model, and the strict completion ledger. None of it
exists only in isolated tests or prose.
