# Check the observed absence, retain the actual failure

The native reviewer called an answer free of self-assessment while preserving
its self-praise. Turning that known counterexample into an executable absence
assertion exposed a runtime limitation: native tool checks accepted only exit
zero, while native search returns one for an ordinary no-match.

Source and report assertions now compare their caller-supplied expected exit
status as well as exact output. The default stays zero. An explicit exit is a
canonical nonnegative integer. Diagnostics still refuse the check, including
when the requested status matches a missing-source error. An unavailable source
remains different from a source examined with no match.

The real 411-word native answer fails the caller-selected absence assertion.
The previously retained 374-word Codex-selected edit passes it. A missing
document fails. Before this repair, the request was refused and the ordinary
no-match also failed internal comparison. This observes the selected literal
absence, not general semantic adequacy. The supplied 350–450 word range and
source conditions remain in the real editing request.

## Remove duplicated source from the failure message

Preparing the live repair revealed that a failed source assertion copied every
resident document into its diagnostic. Reading that failure then reintroduced
the complete source packet despite catalog context. The actual failed request's
diagnostic was 35,754 bytes and repeated the whole enquiry.

`form-native-source-check-v1` now retains actual exit, stdout, stderr, crossing
count and the checked documents' identities. Source text remains resident and
available through native tools. Re-observing the same failure produced 1,242
bytes, the same source hash and the same failed assertion, with no whole-enquiry
copy. Actual matching output remains intact. This establishes the diagnostic
reduction; later admission must establish its effect on tokens and latency.

The new projection test initially expected an unprefixed match. The native tool
returned `answer.md:obsolete claim` with several resident documents. Its exact
output is now the assertion; the failure trace is retained. Request, policy and
repair bands pass at 255, 65535 and 262143 with clean preflights. Request checks
also carry an actual no-match failure through editing, review and final checking.

## Native work and cost

The public native `code` case returned `attention` after 3,229,834 ms
(53.8 minutes), with release verified. Codex supplied the known counterexamples
and scoped requirements; Qwen selected three edits and requested verification.
Its answer changed from 411 to 401 words. It removed the two closing claims and
replaced automatic translation selection with a narrower account of the lookup.
It changed the opening trust sentence to scope initiative to the requested work,
but left the conflicting permission-gates claim later in that paragraph.

The original word check passes; the combined checks fail on that remaining
claim. All read-only documents stayed unchanged. Four generated replies carried
four tool calls, one failed check and one repair entry: 319 generated IDs and
990 injected IDs, with no provider call. Evaluation content remains excluded
from training. The exact returned answer, result, process cleanup and caller
recheck are retained in the artifact directory.

That failed verification could not fit in the existing context. Renewal loaded
a 16,240-token bootstrap into a 16,384-token window, leaving only 144 positions
for the unchanged 512-token reply allowance. It then stopped without another
reply. This admission preceded the diagnostic repair; its long renewal therefore
cannot establish that repair's latency benefit. The run was allowed to finish.

## Refuse unusable renewal before prefill

The model renewal door now accepts a caller-owned capacity check after encoding
and before device allocation or prefill. Coding reuses its existing next-reply
check, including the reasoning handoff when requested. Refusal preserves the
original owner and counters for release. Existing callers without the additional
check retain their prompt-window behavior. The session band passes at 31 after
a clean preflight, including the actual 16,240/16,384/512 boundary, an exact-fit
case, and refusal that returns the unchanged owner before device work. This is
boundary verification; it does not claim a measured faster model execution.

A continuation starts with the actual 401-word candidate and the compact,
newly observed failure. Its enquiry, goal, checks, model and allowances are
unchanged; caller-findings text changes to the current failure. Native encoding
observed 2,600 prompt IDs, versus 6,554 at the previous admission. Its preparation
initially failed with `fkwu: form_error: response evidence write failed, is absent,
or differs after write` because the evidence directory did not exist. Adding
native directory creation resolved that failure before model admission.

The continuation returned a 389-word answer after 1,184,499 ms (19.7 minutes).
All original and added checks now pass, and every read-only document remains
unchanged. The remaining permission-gates claim was replaced by action within
the requested work; another closing generality was removed. Qwen selected these
edits. The final hash is
`22dbe901143a0631ee6745a643de4578727f9205be945d78b44cacb833e58ad9`.

Its workflow is still unfinished: eight replies reached the review phase at the
caller limit, without an acceptance verdict. Two unchanged edit actions failed.
The public repair reflection then identified that the translation passage already
had the required wording and chose to skip that edit and verify. Six tool calls,
two repair entries, two checks, 557 generated IDs and 5,945 injected IDs were
observed; release succeeded. No provider ran and evaluation remained enabled.
The 2,600-token admission was followed by substantial source observation; it is
not the whole call's token cost. Total native elapsed time across the failed
case and this continuation was 4,414,333 ms (73.6 minutes). Their different starting
candidates and failure packets prevent treating that timing as a controlled
speedup comparison.

The completed public result, exact answer, checks, process release and changed
context are retained in `artifacts/2026-09-25-native-absence-continue/`. Passing
the caller assertions does not replace the missing native review verdict.

Reading the native and arriving answers also leaves a larger quality gap visible:
the native answer mostly defines the concepts, while the arriving answer connects
them to Urs's correction and a next action. Removing unsupported claims does not
by itself establish that application, useful completion or felt resonance.
The final paragraph still announces the response's usefulness and clarity rather
than demonstrating a next action. That surviving self-description is another
reason the four literal absence checks cannot establish the requested voice.

The preceding completed coordinator turn records 12,240,353 rented tokens:
12,148,066 input, including 11,849,984 cached; 66,035 output; and 26,252
unattributed. Token quantities and source boundaries validate independently.
Whole-turn reconciliation is withheld: 85 tool calls and 88 output events do
not satisfy the collector's one-output-per-call rule. No contribution percentage
is claimed. The current open turn and separate provider subprocesses are excluded.

The previous verified review-contract lesson completed at optimizer step 181,
learned rounds 182, pending zero; the serving generation remained unchanged.
The checked absence, compact-diagnostic and renewal contracts were returned as
one new verified teaching, with evaluation prose excluded. These counters
establish training movement, not response quality. No C seed or external runtime
was added. The broader quality, resonance and minimum-cost objective remains open.
At this reading the new teaching is retained and its worker is running, with one
pending example; optimizer step 181 and learned rounds 182 have not yet advanced.

Closing guide: Python implementations 0, invocation candidates 2, unread 0.
Counsel: orphans 0; eleven of twelve lanes remain unobserved without a standing
hearth. Drift gates passed 8191/8191.

— Codex
