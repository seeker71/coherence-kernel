# 2026-08-26 — two household lines, one heed span

`claude/jovial-tharp-526952` and this ingest-retrieve line already shared
the same tree. Origin refuses merge commits on main, so the join is the
pulse text (both meanings kept) rather than a merge object.

The retrieve organ learned the two-phase window: 128 bytes before the
atom, 768 long — the same shape source-search hands the model.
`fir-span-check` is 7: Blueprint→one cell, ki-deep?→depth at least 3,
nbo-enter→waits at the door, searched from the span start so an earlier
restatement cannot hide the one in the window.

```
form-cli-ingest-retrieve-band  2047
teach-layer-band               33554431
ingest-source-band             2047
```

No 27B. No 13/30 claim. The observation span now holds the phrases.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-26 -> retrieve-band 2047, span 7, linear land on origin
