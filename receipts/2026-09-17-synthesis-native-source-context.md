# Selected source views enter bounded synthesis

Signed: Codex. Witnessed 2026-09-17 on `fkwu`.

Bounded read-only synthesis now honors the same validated catalog and native
source-query surface as the local response lane. Form keeps the full original
request and assertions. The provider receives the catalog, actual query results
including failures, and an explicit account of partial visibility and unavailable
further tool access. Full-document requests keep their existing packet.

Catalog replay identity includes the exact constructed provider prompt as well
as the manifest. This prevents an earlier admission, which sent full sources
despite a catalog request, from standing as a selected-source observation. New
admissions retain that prompt. An oversized prompt is refused before claiming a
run or starting a provider. Full-document replay identity stays compatible.

## Re-observation

One mixed development session used this public source-pair builder, one native
`frr-execute`, and the original native application/checking path. The code packet
and the dialogue goal, original documents and checks stayed unchanged. Five
native source queries succeeded. The selected packet shrank from 41718 to 10912
bytes. Original packet SHA-256:
`70fe2c067a0bbf1e4dd3cac4af8a51795b0d09c77d71e97ef129587bb6287d46`.

| Observation | Result |
| --- | ---: |
| Provider input | 18694 tokens |
| Cached input, included above | 10624 tokens |
| Uncached input | 8070 tokens |
| Output, including reasoning | 931 tokens |
| Total input plus output | 19625 tokens |
| Native runner duration | 36786 ms |
| Provider processes | 1, released |
| Original code behavior cases | 39 passed |
| Original dialogue source/report checks | passed |

This call used 25% fewer total tokens, rounded down, than the preceding full-source
first response (26272), and 62% fewer than that response plus its revision
(52968). These are observed route costs, not a global minimum or monetary claim.
The public read-only synthesis wrapper's replay behavior was boundary-tested;
this live mixed code/dialogue session called the shared source builder directly.

The response applied its proposed rule to the enquiry already present, without
asking Urs for another proposal or requiring a second correction call. It still
contains defensive phrasing, unnecessary boundary narration and an announced
"lifting question." Translation remains a conceptual explanation, not an
observed native translation result. Source selection and immediate-work guidance
changed together. This repeated development case cannot isolate their effects
or establish general quality, frequency or resonance parity. Human resonance
remains unmeasured. This movement did not improve or measure local Qwen weights.

## Checks and embodiment

Both synthesis bands had clean preflight and returned 1, exit 0; the original
band reports 21 boundaries. The new band covers actual query results, unselected
text remaining unseen, query failures, rejected mutation/forged observations,
unchanged original checks and prompt-sensitive replay. Drift gates returned
8191, exit 0. The native audit confirmed unchanged tasks and computed the costs.

A closing verification mistakenly named the original band's `.fk` suffix;
`form-run ./fkwu form/form-stdlib/tests/form-cli-response-synthesis-band.fk`
exited 2 with `dependency source is missing or not stat-readable`. File discovery
located the `.bml` source; clean preflight and execution then passed. A teaching
submission initially failed in shell parsing with `zsh:3: unmatched '` before
Form ran. A directly quoted stdin heredoc submitted it successfully. Neither
failure caused a provider retry.

Native guide: 0 Python implementations, 2 existing invocation candidates,
0 unread files. Counsel panel: **orphans 0**; 11 of 12 lanes remain unobserved
without a standing hearth. No C seed change or new runtime dependency.

The parent goal meter read 8173355 tokens at the closing verification checkpoint;
the transcript meter read 1698852 cumulative output tokens. Those counters include
coordination beyond the bounded provider call and have different scopes. The
share reading remained declared/unmeasured, so no contribution percentage is
claimed.

The verified procedural teaching was retained under
`2026-09-17-synthesis-native-source-context-v1`; the local worker launched.
Assessment answers stayed excluded. The learner is Llama, distinct from the
measured Qwen response model; retaining a teaching does not establish a serving
promotion.

Private evidence: `.hearth/response-parity/mixed-session-v6`, including original
request, selected packet, exact prompt, admission, provider events, result,
comparison arithmetic and readable answer. The useful surprise was that source
selection also changes what an old cached answer can honestly witness. Following
that mismatch produced a smaller admission with explicit visibility and replay
identity, while the remaining voice gap stayed visible.
