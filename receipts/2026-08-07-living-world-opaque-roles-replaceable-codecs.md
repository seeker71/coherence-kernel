# Behavior grows opaque roles and replaceable executable surfaces

Date: 2026-08-07
Witness: direct `fkwu --src`, Form/native path
Authority: diagnostic non-promotion evidence only

## Movement

The living-world body can now carry a differentiated behavior through roles
that are identified by recurrent relations, not by inherited semantic names.
Three current observation sites independently relate two opaque coordinate
cells to exact historical bindings.  Two-source recurrence plus unanimous
admission produces two content-addressed role candidates.  Their identities
retain coordinate, evidence classes, sites, bindings, alternatives, unknowns,
witnesses, thresholds, and local derivation paths.

No role is named subject, object, input, output, left, right, cause, or effect.
Historical binding strings are inspected only at the explicit bridge from the
older hypothesis tissue.  They select an admitted opaque role but do not enter
that role's coordinate identity.  One admitted carrier then retains the exact
compatible hypothesis, its offered evaluator cell, the ordered opaque-role
sequence, and the behavior receipt.  Contradicted behavior remains present as
an alternative receipt and cannot cross the carrier membrane.

## Plural evidence and local reopening

Both roles recur at three distinct sites with two independent support sources.
One role also retains an explicit alternative; the other retains a current
silence and unknowable remainder.  No evidence class is collapsed into
support.  A later dissent against only the first coordinate changes that role's
evidence identity and makes its admitted candidate and dependent schema stale.
The second role and the earlier differentiated tissue remain current.  Thus
reopening is local to the relation that moved.

## Replaceable executable codecs

An executable codec is a separately offered relation between one admitted
opaque role and one safe local variable token.  The token is surface notation,
not role identity.  A value cell separately relates that same role to a current
scoped projection.  An application must contain exactly one current codec and
one current value per carrier role, with unique tokens, before ordinary
recurrent offers may make it current.

The witness offers two distinct codec fields for the same carrier:

- tokens `u`, `v`, yielding native source `(sub u v)`;
- tokens `p`, `q`, yielding native source `(sub p q)`.

Both bind the same new projections, `20` and `6`, and both evaluate natively to
`14`.  Their generated Recipe identities differ while their schema and carrier
identities remain exact.  No token set is preferred, promoted, or retained as
the meaning of the behavior.

The second application independently carries an explicit alternative, an
unknowable remainder, an open choice, and a sender-declared timeout whose
default condenses to an alternative acknowledgment.  Its immediate `ONE(14)`
remains distinct from pending time and from the later timed outcome.  Optional
fields may also be absent; absence does not synthesize a default.

A reverse, contradicted carrier is well shaped and remains inspectable, but it
produces no execution because it is not among the admitted schema carriers.
Raising recurrence from two offers to three likewise leaves both applications
inactive.  Revoking the sole attribution for codec `u` invalidates only the
first application and execution; the `p/q` application, schema, projections,
unknown remainder, and execution remain current.

## Proofs

- `living-world-structural-role-band.fk` -> `32767`
- `living-world-structural-recipe-band.fk` -> `32767`
- all 68 declared `living-world-*-band.fk` files -> each declared verdict
- task-start and final controls: `42`, `55`, binary freshness `15`, exact
  numeric list `[1, 2.5, [3, 4]]`, native-vs-rented `11111`
- Form evaluator `635`; offer/ack core `1023`; alternative/timeout `32767`
- preceding hypothesis tissue/application remain `32767` and `32767`

The complete sweep stayed under direct observation.  Source evaluation was
named after 60 seconds.  At five minutes the heavy recurrence artifacts were
1,958,848 bytes (`.fkb`) and 279,995 bytes (`.sym`); the sweep had advanced to
second integration with a direct stack of
`fk_run_src -> fk_walk -> fk_walk_body`.  This was native recursive evaluation,
not learning time or a model-quality score.

Static inspection classifies every quoted region in the organ.  Fifteen are
`bp` category identifiers for structural content-addressed cell shapes.  The
remaining three are the minimal Form source-codec delimiters `(`, one space,
and `)`.  The organ contains no fixed role or operation literal and no remote,
host, file, socket, model, or network route.  Fixture strings `left/right` are
historical bridge evidence; `u/v` and `p/q` are replaceability probes.  There is
no duplicate target definition, trailing target whitespace, or
`runtime/fkwu-uni.c` diff.

## Content identities

- `living-world-structural-recipe.fk`:
  `7a68ed4636c119f569a3de4b066387ba82daeda0c0b9ccda7742273d74103050`
- `living-world-structural-recipe-witness.fk`:
  `966e56d83a2bcf35865c2408a3621c964878fd8de4c51adda961d09bc27910cc`
- structural-role band:
  `1463edcb69c905a021e227d9249e54db47a347a6c0a3b02a029cc43788f10404`
- structural-recipe band:
  `faa1e839e59b7515ba6c93a76d65d018588e2132503242c58d114bb79c17f75a`

## Honest boundary and next floor

This is content-addressed structural role learning and replaceable Form-native
execution.  It is not model-weight training, movement of the frozen `733`
benchmark gain, NL-to-NL generation, native generative voice, global program
equivalence, authority expansion, or promotion.

The operator token still arrives as a consented cell through the minimal Form
evaluator interface.  Its admitted meaning is bounded to exact observed
behavior receipts, but the field does not yet generate or discriminate an
executable operator carrier from transformations alone.  The next floor is a
plural operation nursery: test all separately offered minimal executable
carriers against several consented before/after observations, retain every
behaviorally compatible or contradicted candidate, and let held-out evidence
differentiate them without making a textual operator name primary.
