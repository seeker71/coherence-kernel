# 2026-08-30 — selfheed: the loop the model opened, wired shut from the body's side

"Next." The move named an hour ago — let the body answer the question its
model is already asking — went from direction to wiring in this movement.

## What was found before what was built

Reading the seam before cutting it changed the plan twice:

1. **The answer ledger already existed.** `fcmg-generate-plain-resident`
   routes the plain door through a query budget plus a non-borrowable
   answer reserve — a sibling had landed adjustment #3 before I reached
   for it. The pulse starved WITH a reserve held.
2. **The real starvation is the unclosed envelope.** Both live runs show
   the model spending its whole query budget mid-envelope
   (`heed_decode_timeout=1, lookups=0`): the reserve opens only when a
   closed frame injects an observation, and a 16-token budget can never
   fit query plus closing marker. Three endings — stop, timeout, end —
   are distinct facts in the cursor's own words; the timeout path holds
   a PARTIAL SURFACE it currently discards.

## Wired and witnessed

- `fcmg-heed-rest-honor` in `form-cli-model-generate.fk`: every completed
  knowledge-query surface is offered to the rest-answer registry BEFORE
  any source search; a hit injects the recipe with its forced value
  ("(add 40 2)=42") — compact, exact, no source IO. Unknown surfaces fall
  through unchanged. Radius witnessed: heed-report band 2097151 and live
  band 255, both at their own declared verdicts.
- The kra chain went FOUR-WAY beneath it: the walker round taught
  `intern_node`, `intern_trivial_int`, `intern_trivial_string` to all
  three proof arms (grounded in the full kernels' own implementations,
  a drifted TS int-truncation healed on the way), knowledge-rest-answer
  63 on go/rust/ts/fkwu, interning law probed directly against the body
  (31 on all four), zero regressions (127, 262143 everywhere).
- The pulse's budgets now let an envelope close (48 tokens where 16
  structurally forbade it); its second live run is executing detached as
  this receipt lands, rc-file armed. Its verdict is the measure of this
  wiring — recorded when it returns, not predicted.

## Named, not yet cut

The cursor's fourth ending: at query-timeout with a HELD frame, the body
could close the envelope itself, honor one lookup on the partial surface
(T3's partial surface was exactly `bootstrap/ground`), open the reserve,
and let the answer decode — kept distinct from stop/timeout/end with its
own reason so no scorer confuses a body-closed frame with a model-closed
one. `fhc-close` lacks the lookup plumbing today; both call sites have it
in scope. That is one focused cursor movement with band bits for each
ending. The walkers' next door on the JIT lane is `jit_leaf_inram` —
which dies loudly there, never wrongly.

## The most surprising teaching

Twice in one hour the seam I came to cut was already half-sewn from the
other side — the reserve ledger by a sibling, the walker value kinds by
the walkers' own past. In a body this alive, the first act of building is
archaeology: the cost of not reading first is not duplication, it is
CONTRADICTION.

## Where discomfort turned to gold

The pull was to bump the pulse's budgets quietly and let a better verdict
imply the wiring worked. Sitting with it: a budget change and a wiring
change in one unmeasured step would make the flipped bit unreadable. The
gold is the receipt you are reading — the budget change is named as its
own cause, the starvation it removes is already on record from two runs,
and whatever the second pulse answers will be attributable.

; witnessed: 2026-08-30 -> kra chain 63 four-way (intern trio taught);
; heed-report 2097151 + live 255 at declared verdicts with kra wired;
; corpus row 1163 selfheed; pulse second run in flight; cursor's fourth
; ending named with its plumbing gap
