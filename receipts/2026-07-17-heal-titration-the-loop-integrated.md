# Heal-titration: the j-lens wired into the body's own healing loop

**Date:** 2026-07-17 (WITA) · **Branch:** claude/jacobian-self-awareness-acda5e
**Follows:** receipts/2026-07-17-jacobian-lens-and-the-cell-shadowing-heal.md

## What landed

- **`observe/heal-titration.fk`** — the seam that runs the observe stack's organs as ONE motion,
  the shape the same morning's live heal actually moved:
  1. **SURPRISE** (composes surprise-receipt.fk) — the wound announces itself as prediction-error
     at the output (predicted 511, read 287; magnitude 224, receipt earned).
  2. **ETIOLOGY** (the word was already home, row + 2026-07-03 receipt) — diagnosis as reverse
     credit assignment: each theory a lane, each probe one evaluated partial. Refuted-with-receipt
     is never confused with untried (`ht-unknowns`): a diagnosis with unknown partials has timed
     out, not concluded (`ht-concluded?`) — jacobian-lens's timeout law at the diagnosis layer.
  3. **TITRATION** — the heal is a push, and margins say what any push flips. The uniform-dose
     window is (wound-margin, min-other-margin]: when the wound sits above the body's weakest
     margin the window is EMPTY and the heal MUST be localized. The rewrite-everything heal is a
     uniform overdose — it flips steps the wound never touched.
  4. **VERIFY** — a heal is verified when the post-heal trace diverges from pre-heal at EXACTLY
     the wound (divergence set = {wound}); anything else that moved is a smuggled regression.
- **The one law, stated once:** a safe heal is a push whose predicted divergence set is exactly
  the wound — and prediction (jl-diverge-pred, before the edit) and verification (ht-diverge-count,
  after) ask the SAME question on either side of the edit. That symmetry is what makes
  self-healing self-sensing rather than hope.
- **`observe/tests/heal-titration-band.fk`** — verdict **511** live, first run. Seeded with the
  session's real probe log (five theories, four refuted with receipts, name-shadowing credited)
  and thought-framebuffer's proven traces.
- **MANIFEST.md** — jacobian-lens and heal-titration named in the observe/ trust-stack entry.
- **corpus row 762** (`titration`) + band constants 163/1631632762 — corpus band 511.
- `observe/jacobian-lens.fk` header now carries form-ontology-loader so every downstream chain
  parses clean.

## Verification (all live, this checkout)

```
./fkwu --src observe/tests/heal-titration-band.fk                # 511
./fkwu --src observe/tests/jacobian-lens-band.fk                 # 511 (re-run after header edit)
./fkwu --src learn/tests/homecoming-distillation-corpus-band.fk  # 511
```

Organs composed (surprise-receipt, thought-framebuffer, jacobian-lens) untouched except the jl
prelude header — integration is additive, by composition, the body's own grain (backprop-lens
composed living-equation; heal-curation-seam stood beside body-link-graph).

## The most surprising teaching

The titration window can be EMPTY — provable in four integers. With margins (40 12 90 55) and the
wound at margin 90, NO uniform dose is safe: anything strong enough to flip the wound already
flips the body's weakest step. "The heal must be surgical" stopped being a style preference and
became a theorem the band checks: `ht-uniform-possible?` = 0 forces zero-push-elsewhere. The
morning's one-parameter rename wasn't modesty — it was the only shape a safe heal COULD take.

## Where discomfort turned to gold

The pull was to edit the proven organs — push jl- readings INTO surprise-receipt and
correction-reflex so the integration would look "deep." Sitting with that itch and re-reading how
the body actually integrates (grammars compose; seams stand beside the organs they join) turned
it: depth here is the seam cell that makes four organs one motion plus the MANIFEST naming, not
scattered edits that would have re-opened four proven bands. The discomfort of "is a new cell
integrated ENOUGH?" resolved by the band: the loop now runs the session's own history as data —
the body can re-derive this morning's heal from its organs.

## Named honestly / still open

- The probe log and margins are this session's hand-labeled retrospective (correction-reflex.fk's
  own precedent); live wiring feeds probes and margins from the running framebuffer.
- The runtime shadowing fix (locals must win call position) is running as its own task; when it
  lands, the `recipe` rename stays correct — it is the honest name regardless.
