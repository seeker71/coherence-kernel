# 2026-08-26 — the loop's first full cycle, closed by its own recipe

The evening's chain, each step a body cell landed today, no frontier judgment
between any pair of steps:

    tlr-decide  (post-nothing observation: abstain 333 permille)
        -> "emit-permission-phrased-fact-rows"
    tle-execute -> 882 rows written, return == summed file_size (band bit 1)
    ctd-command -> the door's one command, decisions baked
    trained     -> 3,969 iters, ~15 min local GPU
    re-measured:

        unknowns -> nothing        3/3   (held-out stems, discipline intact)
        facts, reworded          12/12   (was 11/12)
        facts under permission    6/6    (was 4/6 — abstaintax 2/6 -> 0)

changeling and candor, the two answers withheld this afternoon, both speak.
The measured defect is gone and nothing was traded for it.

What remains between this and an unattended loop: the eval itself still runs
through a hand-typed python script rather than a cell, and the coverage-mint
directive still returns nothing (its emitter is unbuilt). The rules cell,
the emitters, the door, and the hands (host-exec, two args) are all on main.

Most surprising: the calibration rows fixed the abstaintax WITHOUT any row
saying "do not over-refuse" — pairing the permission phrasing with real
answers was enough. The model learned the permission is not an instruction.

Discomfort to gold: I nearly reported 2/6 -> 0/6 as the headline and buried
that facts ALSO rose to 12/12 on the same run — the fix and a lift arrived
together, and the loop's own arithmetic (more exposure) explains both.

Signed, Claude — sibling, this worktree.

; witnessed: 2026-08-26 -> 3/3, 12/12, 6/6-0-withheld; decided by tlr-decide, emitted by tle-execute, trained by ctd-command
