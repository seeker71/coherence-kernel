# Care can display a changing observation without making it permanent

Codex, 2026-09-22.

Care and Glass now build ages, source metadata and the care projection as
collector-visible Form values. Explicitly tagged borrowed nodes retain the
original organ observations. Direct serialization creates no report nodes;
`occ-request-at` remains the explicit primary-node admission door. Stream
consumption is shared with health and happens once before the selected report
representation is constructed. The C seed did not change.

The [retained evidence](../docs/evidence/fkwu/care-view-lifetime.json) binds the
observed sources and real 44,397-byte failed-process care exchange. Eight
alternating comparisons preserved every field except elapsed time and derived
observation age. Complete warmed explicit-source command calls, including
parsing and serialization, minted zero primary nodes. The node-building
comparison minted 33–35 per call. Its measured 5–6 ms versus the view's 7 ms
does not establish a speed improvement; the observed gain is reclaimability.
Comparison/parsing of the measured results occurs outside those intervals.

The public lifetime witness completed 256 commands with zero primary mints,
one actual collection cycle and over 98,000 reclaimed temporary strings. A retained
view rendered exactly afterward and explicit node admission preserved the same
serialized value. A separate mixed-value observation covered duplicate-key
replacement order, null versus absent, `VALUE-NONE`, escaping, embedded NUL,
Unicode, large tagged integers, booleans, empty containers and precise borrowed
floating-point values. Lexical and record-held views survived collection; the
test cleared its deliberate record references afterward.

The source CLI returned two care reports and a health report from one retained
cursor: 44,397 bytes, then zero and zero. Actual supply and fresh observation
remained distinct; unknown diagnostic meaning and the failed child's open need
remained visible. Glass emitted the current care schema. Both children exited
zero; their stderr files were empty. Private observation homes remained paused,
with no learner/model artifacts. Process groups, Form mounts and declared
observation sources were released and re-observed.

The AI review board checked the changed path with its callers and collector
contracts. It found the `VALUE-NONE` constructor gap, which was repaired and
observed. Its central lifetime finding: every seed record remains a root, so a
record per view would keep the temporary graph alive. Working values therefore
stay in ordinary Form lists; the implementation adds no per-view record.

The actual CLI observation also exposed a session ownership gap. Missing
relative data paths were searched through ancestor directories by the seed's
source resolver. A paused private home read the genuine repository learner's
PID and waited on it. That learner completed naturally; no process was killed.
Form's `nsm-at` now anchors relative homes to the executing cwd while preserving
absolute homes. Re-observation with genuine ancestor state and absent private
state showed the private home settled without inheriting it. The existing
session-memory checks returned 1023. Session-home lookup is a data boundary,
not a source-discovery request.

Attempt evidence stays visible. The first comparison helper lacked the
`line-grammar.fk` prelude; `split-on` was unresolved and the ensuing diagnostic
loop was stopped with TERM, exit 143. The source capture also initially carried
the command wrapper's `@form` metadata; exact Git bytes corrected that input.
The public witness's nested BML definition did not lower, so its retained root
was expressed directly. A fixed 128-command exercise did not always cross
collection; the witness now observes an actual cycle within an explicit finite
exercise budget. The isolation helper normalized the valid `form/../control`
dependency. Glass's first supervised launch received an empty stdin filename;
`/dev/null` supplied the actual input contract. Those failed children and owned
declarations were settled. A publication-helper delimiter typo was repaired
before execution. Rebase changed cache freshness: the warning-bearing public
witness capture was retained and excluded from clean validation, and the
integration capture's stderr was checked separately. Explicit cache admission
and fresh execution supplied clean evidence. Fresh preflight and re-observation followed each repair;
none of these failures was counted as a pass.

Validation: direct bootstrap 42/55/31 and numeric list; native body 11111;
clean care, Glass, CLI and lifetime-witness preflights; paired comparison and
collection observations; source CLI/Glass integration and session-home check;
native-session-memory 1023; 13 drift gates, mask 8191; clean diff check.
The native authoring guide reports zero Python implementations. My memory
panel showed 2M field cells, 191 MiB logical columns and one unavailable tensor
allocation observation. These are field/probe figures, not this patch's RSS
or a causal allocation total. The phase reading showed ice 2585, water 13,
gas 7137; the hearth was absent.

Verified teaching completed locally: 38 retained examples, 25 sessions,
candidate generation 38, serving generation 4, pending zero. This establishes
retention and completed training, not serving promotion or answer quality.

The completed boundary is transient construction and rendering. New event
admission, discovery, reader records, framebuffer publication and collection
still depend on the seed. The primary field's 2^26 ceiling remains. Next is
the retained reader's complete ownership and release contract, including an
independently admitted signal path when ordinary allocation cannot grow.
