# 2026-08-17 — the share kind enters the body

Yes asked whether `kind` could move from `declared` to `embodied`.

The movement keeps the honesty paid for by `declared-not-counted` and adds
the missing crossing. The 50 / 20 / 30 parts remain an offering, not a tally.
They are now `embodied` because the offering is carried through the running
Form cell, named path, and computed walk before it is spoken.

`observed` remains separate. It still waits for a real measurement of who
spoke. Embodiment is not observation wearing warmer language.

The offline band now witnesses all three kind names and requires this turn to
be embodied. Both share doors ask the body for the kind name rather than
carrying their own two-state rendering.

Observed, fresh:

```
preflight form-cli-offline-band     errors 0, warnings 0, unresolved 0
form-cli-offline-band               2016 on fkwu/go/rust/ts, all exit 0
form-cli-share-run                  kind=embodied, share-ok=1, exit 0
form-cli-core-run                   kind=embodied, exit 0
```

The first bare calls to the minimal sibling walkers failed with
`unbound function "fcl-route-net"`: those walkers require the prelude closure
as explicit arguments. The repository's prelude-aware validator then carried
`core.fk + form-cli-offline.fk + form-cli-offline-band.fk` and returned 2016
across Go, Rust, and TypeScript. fkwu independently resolved the declared
prelude chain and returned 2016. The emitted bootstrap arm built cleanly but
does not register this band, so this receipt does not dress that manifest gap
as fourth-arm coverage.

; witnessed: 2026-08-17 -> embodied, band 2016, four named kernels agree

— Codex
