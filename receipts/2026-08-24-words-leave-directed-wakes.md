# Words leave directed wakes

The observed response dictionary moved from 30 described occurrences out of
448 to 448 out of 448.  Unique-word coverage moved from 21 of 237 to 237 of
237.  The prose layer independently reaches 303 of 303 occurrences and 210 of
210 unique surfaces.  These are exact counts over the retained response and
carrier fixtures, not an estimate.

The extension keeps four kinds distinct: lexical meaning, functional relation,
carrier label, and inflected response action/state.  Repository path fragments
therefore receive neutral cells without being presented as universal natural-
language meanings.  The extension contains 270 sense cells over 249 surfaces;
20 surfaces intentionally carry more than one response sense.

`cognition/form-neutral-word-field.fk` seats thirteen non-acoustic semantic
axes: love, harmony, vitality, clarify, awe, humor, excitement, trust,
sovereignty, grace, flow, coherence, and resonance.  The English strings are
surfaces; neutral ids, core-token definitions, numeric shapes, and edges carry
the form.  All thirteen coordinates are marked **authored seed**.  They are an
inspectable starting geometry, not measured cultural truth and not hertz.

An ordered pair now computes a semantic wake containing target-minus-source
delta, adjacent-axis shear, alignment, L1 distance, and expansion.  The native
observation for love -> trust is:

```text
delta     [-60, 80, -60, 260, -20, -20, -40, 80, 120, 20, 0, 80, -20]
shear     [129, -116, 259, -216, -1, -14, 92, 44, -96, -15, 62, -89, -34]
alignment 720738
distance  860
expansion 420
```

Trust -> love retains the same alignment and distance while delta and shear
reverse and expansion becomes -420.  All 156 distinct ordered seed pairs pass
the direction witness.  The directed seed graph supports both forward and
backward queries, and each response cell has a directed edge to its field axis.

Description grammar is explicit: inflections such as `known`, `does`, and
`made` reduce through checked recipes to core/derived/domain tokens.  Existing
symbol descriptions exposed `begin` and `end` as the final two unseated words;
they were added and re-witnessed.  Symbol-description tokens outside the full
dictionary are now zero, even though eight occurrences remain outside the old
64-word core alone.

Native observations:

- `cognition/tests/form-neutral-word-field-band.fk` -> `33554431`
- `observe/tests/response-dictionary-spectrum-band.fk` -> `32767`
- native review verdict -> `PASS` (`1`)
- response missing words -> `0`
- description symbol tokens outside dictionary -> `0`

Honest boundary: the 13-axis values and response grouping are authored seeds.
Participant, locale, and corpus observations can offer successors; the current
cell does not infer universality from its own numbers.  The pre-existing
64-meaning core still has 14-language surface identity, while the new 13-axis
field has only its English rendering seated today.  Its ids and recipes are
form-neutral; reviewed locale surfaces remain the next crossing.

The surprising teaching was that crossing 95% was not blocked by rare content
words.  It was blocked by `the`, `a`, `was`, and the other words previously
called glue: frequently used relations whose meaning debt had been made
invisible by their category name.

Discomfort turned to gold twice.  The first 88% witness refused the target and
named twenty missing surfaces.  Then the closure witness found `begin` and
`end` inside symbol descriptions after response coverage was already perfect.
Both misses became cells before the claim was repeated.

Signed: Codex, 2026-08-24.
