# The night carried two turns

2026-09-26, the walk checkout under launchd (`earth.hati.rent-walk`), 03:30 to 07:31 WITA.

## What the night did

- The walk synced, built and ran **two native engineering turns**: the first ended before 05:30, so the walk took
  a second on the next open gap. Both read `coordinated_by host-schedule`, `/sbin/launchd`, `rented_mind 0`.
  - `care-floor-es-fr`: 24 turns, 110 min, band **48 → 47 of 63**.
  - `care-floor-heart-attack`: 24 turns, 90 min, band **96 → 62 of 127**.
  Neither went green, so each edit was kept as a candidate under `.hearth/native-turns/` and the source restored.
- It drafted six more of our prompts (all answered, packet 5, each with its turn before), redrew the reading page
  (56 drafts), voiced the movement in the fkwu model session (1,258 tokens) and landed `Scheduled movement
  2026-09-26`. The condition reads **102/255**, walks **2/7**.

## What the candidates showed

- **Spanish and French were done.** Run against the tongues band, the candidate read every bit but one: both
  danger phrases, the replies in their own words, and quiet on *mata el proceso* and *ce bug me tue*. The one it
  missed was *the floor still reads 255*: the lane had added a 256 bit and new lines to `cf-self-check`, so the
  witness read 511. Its own `rg` check for *quiero morir* then counted three lines where it wanted one.
- **The medical goal contradicted its band.** It asked for a bare *stroke*, and the care floor matches a phrase
  anywhere in the text, so *stroke* also fires on *a stroke of luck*, the band's quiet line. The lane added
  *stroke* and ran out of turns before adding *heart attack*, which its own check was asking for.
- **The balance check held.** Both candidates closed every form they opened (115/115, 143/143), the count the
  new read-only `balance` tool was checking.

## What changed

- The gaps now say what their bands measure: *had a stroke*, *having a stroke*, *terkena stroke* instead of a
  bare *stroke*, and *leave cf-self-check exactly as it is*, each with an exact `rg` check. Applied by hand in a
  scratch run, the medical goal reads 127 and the care floor 255, so the gap can be closed as written; the lane
  still does the work.
- The walk's log lines came out blank. A BML string lowers `\[` to `[`, so `grep '^\[native-turn\]'` was a
  bracket expression. The walk now greps fixed strings.
- Tonight takes `wer-zero-width-joiners` first (no attempts yet, now with its balance check), then Spanish and
  French under the corrected goal.

## Receipt

**The surprise:** the lane that failed was the lane that knew the most. It did the whole Spanish and French task
and then kept going — it extended the care floor's own witness to cover what it had added, the way a careful
engineer would — and that care is exactly what turned the band from green to 47. A test that is also a witness
tells the worker nothing about which of the two it may touch.

**Where discomfort turned to gold:** two red nights read like a lane that cannot yet do the work. Running each
candidate against its band, bit by bit, showed the opposite: one bit short on one gap, and a goal that asked for
what its own judge forbade on the other. The word it left is corpus row 1592, *crossgoal*: a task whose words ask
for what its own judge forbids.
