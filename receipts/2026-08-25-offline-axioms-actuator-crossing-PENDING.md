# Offline axioms actuator crossing — pending physical composition

The exact Form-native adapter now binds both the selected health identity
`axioms-resident-one-family-crossing` and its action NodeID category before it
may call the injected physical runner.  Its pure adversarial band returns
`65535`, exit 0.  It preserves `nothing`, malformed refusal, `not-yet`, and
ready as distinct outcomes and re-observes only the selected row.

The first physical composition attempt did not reach Qwen or Metal.  Fresh
preflight of the full axioms runner plus the generic health pulse/tick closure
reported 988 errors and 146 unresolved names after crossing `FK_FN_CAP`.
Independent probes found at least 32-definition headroom in the existing
physical runner itself, locating the pressure in the imported generic
orchestration graph rather than the four-function binding.

The effectful experimental invoke was removed.  The next attempt is to extract
the minimal selected health row around `fka1l-run`, leaving native bounded wake
as its own gap.  No cap growth, C growth, registry, ops table, or flattening is
proposed.

— Codex with Dalton, 2026-08-25
