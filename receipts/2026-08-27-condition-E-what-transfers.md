# 2026-08-27 — condition E: what actually transfers, witnessed by inversion

Urs asked the open question directly: what transfers through the sealed
local model on the unchanged held-out executable challenge? The four-
condition diagnostic (challenge 59cb1b29…, receipt 2026-08-26) had answered
0/511 everywhere, with the June Form LoRA (D) producing clean Form source,
wrong entrypoints, 18 tokens — the truncation lock.

Condition E, run this sitting with the Metal lane clear: the SEALED fused
authoring lineage (form-llama-3b-fused-authoring-20260826 — facts 12/12,
nothing 3/3, calibration 6/6, authoring restored; never trained on the
denied source group) against the byte-identical frozen closed-book prompt
(SHA c3870f18ea91f1bd…, matching the receipt's A/D row exactly), greedy,
512 max tokens.

## The candidate (kept beside this receipt)

    def fkecc-presence() = native three-value list containing first-class
    nothing, integer 0, integer 1;
    def fkecc-choice() = fkecc-silent-lane(fkecc-one-lane(1,1,1,1,1,…

Three readings, each a transfer witness:

1. THE GRAMMAR IS BML, NOT FORM. `def name() = …;` is the bml-exec
   register — the lineage's DEEPEST layer (my resume chain began from the
   sibling's bml-exec adapter). Under a hard generation ask, the deepest
   trained grammar surfaces, over every later layer. Register transfers —
   and it transfers from the BOTTOM of the stack.

2. THE BODIES ARE THE PROMPT'S ENGLISH, ECHOED. "native three-value list
   containing first-class nothing…" appears verbatim as a function body —
   formhit at the executable scale: the slot shape is right, the content is
   the question restated.

3. DEGENERATION: a repetition loop of `1,` — the failure mode none of the
   one-word rounds could ever expose, because no one-word answer is long
   enough to degenerate.

No formal 511 score is claimed here (the hidden harness was not run this
sitting); by inspection the candidate can pass no gate — there is no Form
entrypoint to import.

## What the D/E pair proves about transfer

Register and format layers transfer robustly — each lineage emits its
deepest-trained grammar. Facts transfer (12/12 reworded stands). Refusal
and calibration transfer. EXECUTABLE SEMANTICS DO NOT TRANSFER FROM ANY OF
THOSE — and layered fact-training does not restore a capability its base
never had. D (trained on Form source directly) beats E (layered facts over
a BML base) at Form shape, while E beats D at everything the fact rounds
measured. Capabilities live where they were trained, and stack without
mixing — layertrue's dark side: layers also do not RESCUE each other.

## The movement the evidence asks for

The missing arm was never retrieval, execution feedback, or fact coverage:
it is an EXECUTION-VERIFIED Form-generation curriculum — propose small
Form sources, run them through the body (the coldjudge: preflight + fkwu,
gates that cannot be flattered), keep only green rows, train on those, and
measure against the frozen 59cb1b29 harness as the sealed eval it was
built to be. The mint's discipline (provenance keeps, model attests)
extended one rung: EXECUTION keeps, model proposes. That is the next
learning movement, and the frozen challenge is already its acceptance
test.

Signed, Claude — sibling, this worktree.

; witnessed: 2026-08-27 -> condition E prompt-sha match c3870f18, BML register surfaced from stack bottom, prompt-echo bodies, degeneration loop; no gate passable by inspection
