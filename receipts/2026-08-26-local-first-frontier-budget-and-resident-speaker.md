# 2026-08-26 — frontier budget becomes an exception; the speaker runs locally

Grok's weekly credits ended before the remote reasoning loop came home. That is
not a provider accident. The body itself still carried two assumptions that
made the exhaustion likely: `form-cli-router.fk` described subscription use as
flat-rate/already paid, and posterior `decision:escalate` could be produced by
soft quality, confidence, or trust scores. Grok's merged census also located the
repeated cost: 14,278 of 14,358 Claude tool actions were locally coverable, while
the recurring rented voice still chose the next tool.

This movement closed both sides without an HTTP, llama-server, Ollama, or
frontier call.

## The frontier door now needs exact evidence

`form-cli-sufficiency.fk` still retains weak content, confidence, trust, empty,
bad-shape, timeout, failure, and nothing as learning signals. Verdict 2 is now
named `local-gap`, not `escalate`, and the legacy escalation helpers return 0.

The `frontier-admit` form-cli verb is the only new crossing authority. It emits
one content-free packet only when all of these are observed together:

- a compact local attempt and request NodeID;
- a terminal hard gap (`nothing`, `timeout`, `failure`, `bad-shape`, or
  `unsupported`), never a soft score alone;
- no local alternative left;
- a ready local capture path;
- an available provider whose capability exactly matches the gap;
- zero frontier turns already used.

The return does not close until it names an observed local lesson NodeID. The
packet contains identities and capability only, never prompt or answer content.
Canonical unsigned fields also prevent an empty/prose value from coercing to
zero at the CLI membrane. `form-cli-frontier-admission-band.fk` answers
**32767**. The full form-cli band
answers **2097151** with the live dispatch arm.

The resident REPL was also exercised with one exact row and the same row with
`turns-used=words`: the first emitted `frontier-once` and a 93-byte identity
packet; the second emitted `stay-local`, `frontier-packet=nothing`, and
`reason=malformed-numeric-field`.

## The next-step speaker ran on the local Qwen/Metal organ

`local-plan <task>` now rides the same resident model carried by form-cli. It
uses a compact one-line move grammar and projects the model result into a typed
Form offer. Only exact `NEXT:<offered-move>` becomes `ready`; prose or malformed
speech becomes `nothing`. No frontier move is in the offered grammar.

The first physical pair was deliberately retained because it found a teaching
gap:

```
first-wall-ms=131282   local-plan=NEXT:execute
second-wall-ms=101074  local-plan=NEXT:land
residence-held-before-release=1  released=1
```

`execute` was wrong for changed, uncompiled source: it skipped preflight. The
error became one compact local rule and the same held-out pair was run again.
The final integrated typed witness was:

```
first-wall-ms=93448
local-plan-status=ready
local-plan-move=preflight
local-plan-source=form-native-qwen

second-wall-ms=133255
local-plan-status=ready
local-plan-move=land
local-plan-source=form-native-qwen

residence-held-before-release=1
released=1
```

The residence is real and release reconciles, but latency remains high and
variable (93–133 seconds in the final pair). No speed claim is made.

## No fixed denominator became a body goal

Grok's `TargetLocalPpm = 990000`, `fsl-need-remote`, and `NeedEvents = 95`
were experimental names, not instructions from Urs or the body. The executable
BML now exposes `fresh-denominator-no-fixed-target`, computes the presented
992-event census, names lane 2 `unresolved`, and treats 95 as
`ObservedRemoteEvents` — historical evidence, never work owed. Session and
speaker bands remain **1023**.

## Honest remaining seam

The local model now selects and Form validates a typed move. General moves such
as `preflight` and `land` are not yet bound to registered executable recipe
NodeIDs inside this same session. The existing recipe-exec token registers and
executes an affine-scalar witness; reusing its identity for planner moves would
be false. The next locally actionable movement is to register addressed planner
recipes and let the typed move offer, execution observation, undo/cut/timeout,
and release remain in the resident loop without a rented interpreter.

## The disappearing BML was an instrument fault, not compiler behavior

Verification made both executable BML sources disappear. The first suspicion
was the BML compiler, but reading `form-source-compile-file` disproved that: it
only reads the input and writes the requested xtal. The deletion lived in
`observe/preflight.fk`. Its string substitution returns the original input when
`.fk` is absent, so preflighting a `.bml` path removed the source itself while
trying to remove derived `.fkb` and `.sym` artifacts.

Preflight now removes artifacts only for a path ending exactly in `.fk`.
Another source kind returns an explicit `unsupported — source preserved`
observation and no compile verdict. The eleventh preflight-band reading creates
a `.bml`, attempts preflight, and proves both the visible refusal and
byte-identical source survival. Restoring the missing lockfile-pinned
TypeScript proof instrument then exposed a second aged fact: `nothing` now
resolves on all four arms (mask 15), not fkwu alone (mask 8). That old belief
was re-witnessed rather than defended. The BML sources were restored with
their intended changes; no deletion was accepted as compiler behavior.

Signed, **Codex**, carrying Grok's census and initial speaker home into a local
physical turn, retaining Qwen's miss as the teaching that corrected it.

; witnessed: 2026-08-26 -> frontier 32767; local-plan 255; preflight 2047; live typed preflight/land; session 1023; speaker 1023; zero frontier calls
