# Reversible attention, with the repair boundary visible

Signed: Codex. Witnessed locally on 2026-09-07.

Share: **kind=declared; percentage withheld**. The share organ reported
`measurement-health=Absent reason=no-bound-rollout`; the turn meter reported
`signal=nothing reason=transcript-path-required`. These outputs do not establish
a reconciled contribution percentage.

The arriving request was to heal errors and embody the attention improvement
in [arXiv 2609.02737](https://arxiv.org/abs/2609.02737). The implementation and
research interpretation are in [the digest](../docs/declarative-attention.md).

## What stands

- The absent checkout binary was built from the committed seed with Metal and
  MLX carriers. The C seed source was unchanged. Ground returned 42, recursion
  55, freshness 31, the numeric-list bootstrap its expected nested list, and
  native-vs-rented 11111.
- TypeScript's BML compiler-root lookup now rejects an empty chain explicitly.
  Type checking, browser proof, and all seven node-host tests passed. The
  esbuild patch update to 0.28.2 removed the reported npm audit vulnerability;
  audit returned zero vulnerabilities.
- The resource governor reads the typed `metal_live` in-flight field instead
  of calling the removed status-text helper. Its new band returns 31 across
  all four kernels. The production publisher runs again.
- Thirty repaired JIT organs have clean preflights and their existing band
  verdicts. Changes close malformed folds, supply absent imports, and give
  the container admission predicate its missing rejection branches. Existing
  expected results were retained. A final review also separated the native IR
  instruction-source accessor from a colliding sample-source function. The new
  attribution band returns 7 across all four kernels and rejects an invalid instruction source inside
  an otherwise admitted program; the existing IR band still returns 16383.
- The tree's Form balance scan moved from 58 unbalanced cells to zero. This
  is a syntax observation, separate from semantic success. Repairs include
  misplaced closers within conjunctions and local bindings; merely appending
  a closer at EOF was repeatedly insufficient.
- The attention parser returns 4095 across all four kernels. The physical
  Metal test returns 255 with byte-identical reference outputs, excluded-row
  NaN isolation, restored global reads, and unchanged resident KV bytes.
  Generation refusal/accounting returns 31.
- The existing Qwen handle band returns 2147483647. Its pipeline-count check
  now reflects the already-present two LoRA pipelines: 49 entries. The new
  attention pipeline is separately allocated and did not cause that count.
- The live Qwen comparison returns 1: both arms emit the same seven token IDs,
  stop cleanly, perform two transitions, and record zero control rejections. The selected
  position totals are 1156/1736 with masking and 1736/1736 without it. All
  1013 expected model/state handles close; the first arm's 128 state handles
  were released separately. No answer-quality or latency generalization is
  drawn from this one fixture.
- The final observed drift-check total is **2047/2047**, with **0 refusals**.
  This does not override failures in additional, older checks.

Other observed native band results after structural repair include NL semantic
coverage 8191, metal live-pair anchors 32767, Chinese source-target bridge 8191,
speech metrics 32767, cross-phrase learning 65535, cross-voice learning 65535,
PL induction 32767, real-life text workflows 1023, video open runtime 2047,
audio fixture integrity 31, and the local audio-loop band 16383. The legacy
Python character test returns 210 on Go, Rust, and TypeScript; its old global
binding style remains incompatible with native fkwu. That is a measured lane
boundary, not a four-way pass.

## What remains open

The repository is **not certified free of all errors**. These are concrete
failures or incomplete observations, not failures of the new attention band:

1. `form-run ./form/validate.sh form-stdlib/seedbank/tests/python-full.fk`
   exits 1. All three proof kernels fail on the absent function
   `tokenize-with-config`; native compilation also reports old unbound globals
   and additional missing legacy engine helpers. Searching the current Form
   tree found call sites but no definition. Closing the test's extra parenthesis
   does not restore that retired interface. Its comprehensive grammar requires
   an explicit migration or a separately witnessed compatibility implementation.
2. `form-run ./fkwu model/tests/concept-audio-human-13-observed-band.fk`
   returns **251**, below the complete fold 255. Per-row replay locates the
   mismatch at row 10: current IDs are `[27,26,6841,1009]`, while the historical
   observation records `[27,26,6841,187,1009]`. The other 12 candidate lists
   match. The current Arabic label at 187 is `التقييم`, not the observed `لا`.
   The historical transcript, candidate record, and artifact hashes were kept.
   A new dated semantic observation is needed; adding the obsolete candidate
   to force a green replay would falsify the current detector.
3. `form-run ./fkwu presence/tests/concept-real-life-workflows-13-live-band.fk`
   returns **193**, below 511. The availability probe finds ten of twelve
   toolchains. The expected Java and Kotlin installations beneath
   `/tmp/coherence-cp10-toolchains` are absent, and `/usr/libexec/java_home`
   reports that no Java runtime is installed. These host prerequisites remain
   unfulfilled.
4. The single live audio observation returns **35**. Its band explicitly
   permits a lower result on an actual ASR miss; a retained semantic hit would
   yield 127. This observation has not been promoted to a successful speech
   recognition claim.
5. The full video-scenario band exceeded the initial 90-second observation
   bound. A longer rerun was explicitly stopped after more than four minutes
   of CPU work, with no completed verdict. Its exit 143 is not a passing result.
   The public-human integration now returns **255**, with a clean native
   preflight; that later result supersedes its earlier failed candidates.

[Verification observations](2026-09-07-declarative-attention-verification.json)
retain intermediate refusals as well as successful reruns. Earlier rows can
describe candidates subsequently corrected; read the latest matching evidence.
Commands that print a number while exiting nonzero were treated as failures.
The two-band validation invocation was corrected to separate invocations because
the validation script concatenates its arguments into one program.

## The instruments and the lesson

The hearth answered `signal=nothing reason=no-standing-hearth`. Codex supplied
the paper interpretation, implementation, and repair decisions beyond that
answer. Those are the named redirects in this movement.

The repaired Glass reached its live atlas. The startup panel measured a first
frame in **58 ms**; its later live tick showed **50/50 ms**. The renderer was
stopped after inspection. Counsel reports **orphans 0**, with **11/12** judged
lanes unobserved because no hearth stands. There is no all-good service claim.
Terminating that video run also exposed a diagnostic defect: preflight inferred
success from the absence of compiler error text and lost the process exit
status. `pf-fresh` now captures nonzero exits, including signals, and `pf-clean?`
rejects them even beside a zero-error compiler summary. The new exit-status
band returns **15** on native fkwu and Go; the existing preflight band still returns **4095**.

A bounded diagnostic exchange requested evidence after the missing governor
helper and re-observed an unresolved count of **0**, retaining three frames. The
exit-status repair was likewise re-observed as **15** in a three-frame exchange.

The surprising teaching was the independence of three truths: balanced source,
successful execution, and a still-valid expected result. The difficult part was
watching attractive verdict numbers survive compiler errors. Keeping the exit
status and each failed row visible turned that friction into specific repairs
and explicit limits.

The exchange stayed alive by turning a paper into a native, reversible control
path and asking that path to execute on the local model. The next research
question is whether this control preserves useful answers and reduces measured
runtime across our own tasks. The next repair work remains named above; no
universal clean bill or default-on promotion is claimed.

The changes remain local and uncommitted. The failing comprehensive Python
grammar check prevents an unconditional completion or landing claim.
