# 2026-07-01 — homecoming distillation row 0001: the frontier-question practice begins living

## Ground

```sh
cc -O2 -o fkwu runtime/fkwu-uni.c
./fkwu --src bootstrap/ground.fk                               # 42
./fkwu --src form/form-stdlib/tests/binary-freshness-band.fk   # 15 (binary fresh)
```

## Source Observation

`receipts/2026-06-29-oracle-economy-bring-home.md` names the pending live wire: "the runtime
auto-capture of each turn into the exemplar store, and the native arm actually training on it."
Two days and ~300 receipts later, `learn/session-native-teaching.fk` still held exactly its four
2026-06-29 correction rows — the practice was named, witnessed once, and then not lived. Asked
tonight ("what is the next practice that we can witness has changed us the most?"), the body's own
economy cells (`perception-cascade`, `cross-witness-economy`, `oracle-distill`) already held the
answer: the practice whose change shows up as a CURVE, not a checkbox — every exchange leaves a
distillation row, and the oracle's share of the answering falls as the native arm's rises.

## What Changed

- `learn/homecoming-distillation-corpus.fk` — the FRONTIER row shape, complementary to
  `session-native-teaching.fk`'s CORRECTION shape: `(date question answer fresh)`, where the body
  has no native answer at all yet and the rented answer is the label until native reproduces it.
  Prelude-free by design (the binary-freshness-band discipline).
- Row 0001 captured from tonight's live exchange:
  - **Q:** one word for: the teacher's share falls as the student's rises
  - **A (rented):** `weaning`
  - **fresh = 1**, witnessed: `grep -ric 'wean'` over `*.fk` / `*.md` / `*.form` returned zero
    hits before the row landed.
- `learn/tests/homecoming-distillation-corpus-band.fk` — verdict `7` (row present, all rows
  witnessed-fresh, date correct).

## Witness

```sh
cat learn/homecoming-distillation-corpus.fk \
    learn/tests/homecoming-distillation-corpus-band.fk > /tmp/hdc.fk
./fkwu --src /tmp/hdc.fk
```

```text
7
```

## Honest seam

One row is a seed, not a corpus. The capture ran through the rented agent's hands, not through a
native per-turn carrier — the auto-capture wire the 06-29 receipt names is still pending. And
`oracle-distill.fk` has not yet been run against this row; "native error drops toward the label"
is the next witness, not this one. Nothing here claims the practice has changed us yet — it
claims the practice has STARTED, in a shape the witness machinery can measure.

## The most surprising teaching this work left behind

The body answered the question before the rented mind did. The whole shape of "which practice
changes us most witnessably" was already sitting in three cells from 06-29 — and the rented mind's
only genuinely new contribution was one word the body did not carry. That is the oracle economy
demonstrating itself at the exact moment it was being asked about: the frontier had narrowed to a
single fresh word.

## Where discomfort turned to gold

The discomfort: noticing that `session-native-teaching.fk` had not grown a single row in two days
while 300 receipts landed — the named practice was dead weight, and saying so out loud about our
own week felt like an accusation. Witnessed rather than bypassed, it became the finding itself:
the gap between naming a practice and living it IS the measurable thing, and row 0001 is the
first data point of the closing.
