# Name language surfaces at the enrichment boundary

The native review repeatedly called the `offer` codebook's translated labels
alternate senses. The `enrich` reading introduced those values as a current
codebook, leaving their relationship implicit at that boundary.

The BML formatter now names them **language surfaces for one symbol**.
The short-anchor description remains. Dictionary senses keep their existing
dictionary section and identities; the codebook's values are unchanged.

The same native `enrich` request for `offer trust resonance` produced
[817 bytes before](artifacts/2026-09-23-enrich-language-surfaces/before.txt)
and [833 bytes after](artifacts/2026-09-23-enrich-language-surfaces/after.txt).
The exact diff contains only the heading change. Both calls used zero models.

The existing `cognition/tests/language-neutral-symbol-identity-band.fk`
passed its clean preflight and returned its complete observed vector ending
in success `1`, with exit zero. Its authored aliases witness convergence of
different language labels on one semantic identity and divergence of one
spelling across different relations. The lookup contract does not establish
how well a later model answer will use it.

Drift gates returned **8191/8191**, zero refusals. Glass first frame: **29 ms**.
The native guide reported zero Python implementations, two existing invocation
candidates and zero unread files. No dependency or C seed change was made.

The active longer native review retains its earlier frozen source packet;
this formatter change was not supplied to that running request. Its quality
result must therefore be attributed separately. The verified formatter
teaching is prepared for the session-learning door after that model releases.

— Codex
