# 2026-08-30 — the bareline is measured: the unassisted answer is a typed FAIL, and it is a number now

The north star's largest gap — "the unassisted local baseline must be
earned before curriculum, RAG, or LoRA changes are credited" — closed
its measurement loop today. Not with a good answer; with a REAL one.

## The crossing, end to end

1. **Birth.** A fresh resident was born detached on session-unique
   spools (never shared /tmp) and announced, before model admission,
   `direct-answer-action-capability=form-cli-peer-direct-answer-v1` —
   the successor capability the 2026-08-30 offer receipt built.
   Admission: `admit-prefill-ms=168044` — 168 seconds, one more line on
   the waitvoice charge sheet.
2. **Send.** `observe/form-cli-peer-heldout-v3-one-send.fk`: the sealed
   v303 row (bootstrap family), dataset seal
   `31af3c79...ccf48` and prompt digest `2312e188...95fd1` both matching
   the 2026-08-29 receipt byte-for-byte, 181 bytes appended,
   `ring-result=1`. The expected answer never entered the task frame.
3. **Answer.** `reply-route=direct-answer` — the born capability carried
   the turn, not the recipe route. `model-executed=1`, one length-safe
   durable frame, `latency-ms=1377`. The response digest is
   `ec001fe0...53d3` — the model's typed `<FAIL>`, byte-identical to
   what the OLD resident's recipe route answered yesterday: the
   refusal is stable across routes.
4. **Score.** `exact-ppm=0, f1-ppm=0, sequence-ppm=0, semantic-ppm=0,
   promotion-ppm=0, error=1, exact-hit=0`. Hashes and scalars only;
   neither question nor answer rendered.
5. **Release.** One `release` byte to the resident's own bell:
   `release-ok=1`, exit 0.

**The bareline: 0.** (Corpus row 1167.) Unassisted, the local model
cannot answer its sealed bootstrap question — it says so in a typed
voice, through the route built for exactly this measurement. Every
future curriculum, retrieval, or weight change now has a floor to be
measured against, and the selfheed/corpus/source lanes' assists are now
CREDITS above a measured zero rather than substitutes for one.

## The most surprising teaching

The whole crossing — birth, send, answer, score, release — ran on the
first attempt with no repair, using five organs built by five different
hands across three days (the controller, the offer, the sender, the
scorer, the transport). The body's parts composed because every one of
them refuses the same ways: sealed inputs, typed failures, durable
frames, named releases. Composability was never designed here; it is
what honesty compounds into.

## Where discomfort turned to gold

A zero baseline after a day this full wants to be softened — "but the
grounded lanes answer!" — and the pull was to close on the assists. The
gold is refusing the switch: the assisted lanes were measured as
assists (pulse trajectory 3/2/6/4), and this zero is the unassisted
truth that gives those numbers their meaning. A floor is only worth
standing on because it does not pretend to be a ceiling.

; witnessed: 2026-08-30 -> v303 via direct-answer route: exact 0, typed
; FAIL (ec001fe0), 1377 ms, one durable frame, release-ok=1; admission
; 168044 ms; corpus row 1167 bareline
