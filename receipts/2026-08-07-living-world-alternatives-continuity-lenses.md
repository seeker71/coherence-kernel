# Alternatives, timed condensation, continuity, capabilities, and live lenses

Date: 2026-08-07

This movement followed the frozen `733 ppm` diagnosis without pretending that
another verification of the order-2 session-action predictor was learning. It
grew the separate axiom-born living-world organ instead: answers may now carry
alternatives; immediate absence is distinct from expiration; continuity is
inert until re-authorized; purposes see only unanimously granted tissue; and a
bidirectional lens exchange keeps pure source, declared lenses, changes, flow,
result, choices, alternatives, and unknowable remainder visible.

## Acknowledgement and time

The four acknowledgement arms remain exactly `NOTHING`, `ZERO`, `ONE`, and
`NODE`. `OAC-ALTERNATIVE` is structured content carried by `NODE`, not a fifth
control arm. A timeout is now a sender-authored contract holding expiration,
optional default acknowledgement, and evidence. Before expiration it yields a
pending observation. At expiration it yields a distinct timed-outcome envelope
that preserves the contract, observed boundary, and condensed acknowledgement.
No declared default condenses to `NOTHING`, but that history is not identical
to immediate `NOTHING`. A declared default may be any existing arm, including
a `NODE` carrying an alternative.

Question holders can offer immediate alternatives or completed timeout
outcomes as new governed answer proposals. The prior answer and every route,
uncertainty, attribution, and effective acknowledgement remain present. A
revoked stance makes the successor stale.

## Continuity and capability privacy

`LWCN-IMAGE` contains the exact world plus a redundant manifest of all five
world fields and their sizes. Loading is modeled as inert evidence: a custodian
must offer the exact image, and every active historical source must freshly
authorize restoration. Restore begins from the exact stored snapshot, preserves
old revocations, and adds only a source-attributed activation candidate.

The active `fkwu --src` seed does not expose `read_form_binary` or
`write_form_binary`; repository binary-I/O witnesses recover those calls to
`nothing`. Therefore this receipt proves exact Recipe re-derivation and
consent-bound restoration semantics, not cross-process durability. Signatures,
atomic replacement, concurrent writers, and hostile-file admission remain
pending.

`LWCP-GRANT` has no source field; source authority is recovered only from the
active attribution edge. A shared payload enters a purpose context only when
every active holder has granted that exact purpose. Contexts preserve the
original relative field order, because reordering equal members changes the
content identity of evidence. Learning, enquiry, and future voice receive the
same minimized projection mechanism; revoking one holder's grant removes the
payload and makes the old context stale. A syntactically exact grant from a
non-holder cannot expose private tissue.

## Bidirectional live lenses

`living-world-lens-channel.fk` accepts lens payloads shaped as blueprints,
Recipes, cells, or future offered shapes. A declaration has an optional default
lens, ordered optional layers, unapplied alternatives, optional unknowable
remainder, and evidence. A directional pass retains invariant pure source,
the value arriving in that direction, every before/lens/flow/change/result
step, optional choice, and final result. An exchange may contain either, both,
or neither directional pass, plus alternatives and unknowable remainder.

Choice actions are arbitrary offered cells rather than a closed switch. The
witness uses `observe`, `be`, `do`, `integrate`, `release`, `digest`,
`transmute`, `illuminate`, and `identify`, while allowing no primary choice,
additional alternatives, and unknowable to coexist. Lens declarations and
exchanges are ordinary living-world cells; revoking a declaration makes the
exchange non-current without erasing its history.

The exact Recipe `(add 2 3)` carried by the Recipe lens executes through the
native `fkwu --src` route and returns `5`; the execution receipt binds that
result beside the exact Recipe, before-state, flow, and changes. Dynamic
in-channel `walk_recipe` is not claimed: the source seed reports it unresolved.
The separate `--feval` attempt was also rejected because it returned `1` with
nine unresolved `substring` calls.

## Native observations

```text
offer-ack-core-band                         1023
offer-ack-alternative-band                 32767
living-world-answer-offers-band            32767
living-world-continuity-band               32767
living-world-capability-context-band       32767
living-world-lens-channel-band             32767
living-world-lens-channel-recipe               5
```

The complete 22-witness offer/living-world regression remained green: organ
`65535`, tending `131071`, resonance `1023/255/255/127`, recursive transfer,
lifecycle, and compost `1023` each, experience `4095`, concept field `16383`,
and every enquiry/equivalence/contention/answer/continuity/capability/lens band
`32767`. `git diff --check` passed. `runtime/fkwu-uni.c` is unchanged. There
were no commits, pushes, PRs, model-score claims, authority promotion, or remote
oracle calls.

## Honest next floor

This is real protocol movement, not movement in the frozen daily model. The
next load-bearing seam is a callable dynamic Recipe executor inside the
Form/native channel, followed by a native generative emitter that consumes only
a consent-minimized lens context. Durable authenticated continuity, autonomous
unresolved-relation tending, and effect isolation also remain open.
