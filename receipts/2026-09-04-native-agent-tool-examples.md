# Native agent tools — each call has a witness

The resident coding profile is now documented with a direct Form call for every
one of its fifteen tools: `rg`, `jq`, `read`, `cat`, `head`, `tail`, `wc`,
`sort`, `uniq`, `tr`, `cut`, `awk`, `sed`, `edit`, and `write`. The guide also
shows state threading for `edit` and `write`, value chaining instead of a
shell pipe, structured-error handling, bounded command-string parsing, and the
separate native `zg` catalog-discovery call. The scope remains the
resident-document profile used in ordinary coding work; it does not claim to
replace the host, Git, network, or every other catalog route.

`form-cli-agent-tools-examples-band.fk` holds one public `fc-tool-call` per
resident tool and returns 32767 only when all fifteen results match their
documented outputs. It is registered as a four-way `fks` band, so an example
cannot drift quietly from its proof siblings. Preflight reported balanced
parentheses, zero errors, zero warnings, and zero unresolved calls. The direct
fkwu run returned 32767, and the four-way validator returned the same 32767
with one registered fourth-arm witness and zero divergent workloads.

The surrounding evidence remained live: the resident workload and edge bands
returned 65535; the earlier portable band returned 127; and the native `zg`
band returned 255. The workload's counter window still records no
process/file/network operation movement around the public resident calls.
The guide's voice mirror reported a clear register.

The standing hearth was absent (`signal=nothing`, `reason=no-standing-hearth`),
so this movement is grounded in source and executable witnesses rather than an
invented resident reply. Glass first frame was 49 ms; its tick #10 showed about
1K events and 572 nodes. Counsel reported `orphans 0`, while 11 of 12 service
lanes remain unobserved without a standing hearth. The turn meter named
`transcript-path-required`, so no share percentage is claimed.

The useful discomfort was that grouped tests could make a profile look covered
without showing an agent the exact call for each named tool. Splitting those
calls into a power-of-two witness made the omissions observable and gave the
guide examples that are executable rather than illustrative guesses. The
surprising teaching was that the stateful examples are only two lines longer
than the read-only ones: retain `fat-documents`, then continue entirely inside
the resident value world.

Signed, Codex.
