# Strict route evidence enters the resident image protocol

Witnessed 2026-08-26 by Codex with Planck, Beauvoir, and Arendt/Claude.

## Fresh map, not a held denominator

This pulse began at:

```text
48 observed / 40 ready / 8 gaps / 0 unknown / 0 invalid / 833 permille
selected: form-cli-strict-result-producer-placement
```

The broad row was not relabelled ready.  Observation split it at the boundary
actually crossed: one new ready row for source-derived route evidence admitted
as image data, and one narrower gap for the scanner/current-source/answer
producer that remains in fixed Form source functions.

```text
49 observed / 41 ready / 8 gaps / 0 unknown / 0 invalid / 836 permille
selected: form-cli-strict-source-answer-producer-placement
```

The denominator grew because the body can now see one more distinct organ.  A
`48 / 41 / 7` claim would have hidden the remaining producer work.

## What crossed

`form-cli-strict-route-image-call.fk` births a 13-row content-addressed image
from existing compact resident semantics only: ARG, INT, NTH, STRING, STR_EQ,
INT_EQ, and MUL.  It admits exact source-derived facts
`[request-valid=1, request-kind="concept", route-present=1]` as present integer
1.  Every other well-typed offer returns present integer 0 with output-count 1.

The image travels through the existing receiver registry, explicit capability,
CALL_BINDING, OFFER, ACK, correlation, and deterministic ACK observation.  Fuel
17 returns; fuel 16 times out with typed nothing and output-count 0.  An
unadmitted relation is protocol nothing and a missing capability is refusal.
No evaluator branch, function table, flattening, C seed, filesystem authority,
host process, HTTP, model, or Metal path was added.

The actual `form-cli` `thought-kernel` command now loads and observes both the
admitted and present-zero route offers:

```text
strict-route-image-admission=1
strict-route-present-zero=1
process=0
http=0
model=0
metal=0
```

This is deliberately route-*evidence* admission.  Scanner execution, bucket
and route-record parsing, current-source observation, and answer construction
remain visible outside the image.

## Exact evidence

```text
strict route source preflight                 0 errors / 0 unresolved
strict route band preflight                   0 errors / 0 unresolved
full form-cli REPL preflight                  0 errors / 0 unresolved
strict route image band                    2047
Go/Rust/TypeScript validation              2047, 1 agree / 0 divergent
program image call protocol band        2097151
form-cli band                           2097151
form-cli REPL control band                  1023
form-local-offline-health-map band         16383
live thought-kernel route admission            1
live thought-kernel present-zero                1
git diff --check                               clean
```

The resident witness draft first reached preflight at depth -1.  A correlated
framebuffer observation received `revise=2`, selected balanced depth 0, then
re-observed `continue=0`; the live window returned `[2, 0, 0, 4]`.  Only after
the revised source preflighted clean were its numeric and live witnesses read.

The first sibling-validator invocation carried a repository-root path into a
door that changes directory to `form/`, so it refused the doubled path.  A
second correlated revise selected the door-relative path and re-observed
continue (`[2, 1, 0, 4]`); Go, Rust, and TypeScript then agreed on 2047.  The
same settled source independently returned 2047 on the runtime `fkwu` source
door; this band is not falsely described as registered in the fourth-arm table.

Fresh local-only dependency observations found the Qwen GGUF, sealed fused MLX
model, MLX carrier library, Go and Rust walkers, diagnostics guide, and current
receipts present.  No `mlx_lm`, llama-server, or Ollama process was running.
The independent separately-powered recovery copy remains absent and therefore
remains a gap.

## The next locally actionable seam

Arendt/Claude derived `current-source-effect-offer-v1` as the next ABI:

```text
[prior-route-ACK, request-node, source-node, entry-node, window-fuel]
```

The capability—not ambient caller state—must retain family/root policy, window,
and answer limit.  Only an exactly re-observed returned route ACK with value 1
may open one source observation.  A successful carrier may return an inner
hit, miss, or stale observation; outer timeout/failure remain distinct.  The
effect ACK must retain the proven route ACK rather than pretending the pure
`fpcp` ACK authenticates filesystem effects.

Kept alive: present rejection 0 never collapsed into nothing, and the remaining
filesystem authority was named rather than hidden by the new ready row.

Most surprising teaching: the entire strict evidence conjunction needs no new
evaluator semantics; content and relation admission were already enough.

Discomfort turned to gold when the tempting seven-gap map was refused.  The
new image made the still-source-bound effect leaf easier to see, which gave the
next ABI its exact shape.
