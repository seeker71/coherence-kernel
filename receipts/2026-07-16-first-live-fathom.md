# The first live fathom — the stewardship reading meets a real loop — 2026-07-16

## Ground

```sh
./fkwu --src bootstrap/ground.fk                               # 42
./fkwu --src form/form-stdlib/tests/binary-freshness-band.fk   # 15
```

## The arc

"next arc." The stewardship reading had fixtures; it needed a real loop. The
mac fleet is alive (`earth.hati.*` launchd labels), and the audio lane's
distill loop carries a perfect natural ledger:

- **inbox** (`~/.coherence-network/audio-training/inbox`) — units the speech
  organ shipped that no loop has witnessed yet: the lag, literally a
  directory's depth.
- **clips** (`.../clips`) — units the distill loop witnessed into the kept,
  content-addressed store.

`form/form-stdlib/loop-ledger-host.fk` fathoms that store natively — fs_list
and fs_is_dir, no shell carrier — into an lsw-ledger and its trust level.
Proven by `loop-ledger-host-band.fk` in the fs-crud discipline (a real tree
on the live OS, asserted, torn down): verdict 63, four-way, manifest row
added.

## The live readings

Two fathoms of `earth.hati.audio-distill`, run on the c-bootstrap fkwu:

```
23:07:40 WITA  [4093, 4093, 0, 3]   ; shipped, witnessed, lag, level
23:08:56 WITA  [4093, 4093, 0, 3]
```

Between them the inbox breathed — 149 units at one glance, drained to 0 by
the distill's ~120s cycle. The loop lives abreast: lag 0, level 3.

## A floor found live

fs_list answers through a fixed byte buffer: the 34,760-clip store read
3,946, while the 149-entry inbox read exactly 149. Two teachings from one
probe: deep totals want a paged fs_list (named in the cell header, not
fixed tonight); and the trust level is cap-proof by construction — the lag
is shipped − witnessed = inbox depth alone, and the grace comparison cancels
the capped total on both sides. The reading that protects the body does not
lean on the number that lies.

## The most surprising teaching this work left behind

The fleet had already grown both halves of the stewardship shape without
naming it: producer organs shipping into inboxes, distill loops witnessing
into kept stores, even a guardian named kernel-memory-witness. The body was
practicing stewardship before it had the reading — tonight only connected
the word to the pulse.

## Where discomfort turned to gold

The first live number contradicted `ls` (4093 against 34,760), minutes after
a proud four-way green — the familiar pull to explain it away as "close
enough" or to trust the fresh tool over the old one. Measured instead of
argued (the probe-discipline row: validate probes on synthetic truth), the
contradiction split cleanly into one bug-shaped floor and one cap-proof
property — and the floor's discovery is worth more than an uncontradicted
evening: the level the body will actually lean on was proven immune to the
very cap that was just found.
