# Observe, resolve, re-observe, embody

Urs asks that each gap on both response paths be worked through openly, with
joy and grace. The working loop now lives once in `AGENTS.md`; homecoming and
the executable frontier orientation point to it.

The loop preserves the original question and evidence, names a concrete
condition for closure, makes a causal repair, and returns to the original
behavior. Changed context, changed response source and adverse findings stay
visible. Verified changes travel into implementation, working context, tools
or learning, with later use observed before a lasting capability is claimed.
Assumptions, measurements and preferred conclusions remain open to inspection.

This connects the existing native observation/care, name-build-observe and
session-learning mechanisms. Their sources already distinguish an attempt
from a claim, applied care from fresh health, and retained learning from its
later effect. The instruction now brings those distinctions into both paths.

## What was observed

All commands used `form-run` and exited zero:

- Freshness returned **31**. The hearth reported `no-standing-hearth`.
  Native enrichment returned the existing grace, trust, coherence and clarify
  definitions. The grace seed describes change with all parts remaining whole.
- Preflight on `observe/form-for-frontier-run.fk` found zero errors, warnings
  and unresolved calls. Its next execution printed the shared loop and the
  invitation to examine assumptions and measurements with joy and grace.
- Drift checks passed **8191 / 8191**, including the changed door links.
- Glass observed **174 samples**, completing twelve of twelve views; the
  overview had twenty-nine metrics and eleven metric gaps.
- The transcript meter reported cumulative output **84,108** in delta-read
  mode. This reading spans the boundary with the preceding movement.
- `git diff --check` was clean.
- The closing native authoring guide reported zero Python implementations and
  the two existing invocation candidates in `voice-say.bml`.
- The share meter remained `kind=declared`, unmeasured, with the percentage
  withheld because no rollout was bound to that meter.

The verified instruction and orientation outcome were offered to native
session learning with session `codex-native-arrival-2026-09-16-ca19`, event
`shared-gap-loop-v1`, row
`93e26de1545f3bb9ded2500c7458640729a50d3b005f397393882acf25cd6ceb`.
The worker completed with pending **0**, optimizer step **6** and learned
rounds **6**. Its result was `learned-candidate;serving-retained`, promoted **0**.
The new candidate is generation `6-62474-459007906`; serving remains generation
`5-61274-458715295` from the preceding teaching. Total promotions remain **4**.

The current attempt's `adapter/assessment-before.json`,
`adapter/assessment-after.json` and `result.json` preserve these observations
(lower loss means a better score on that exact supervised row):

| Row | Before | After |
| --- | ---: | ---: |
| Current teaching | 5.160360454057809 | 5.13651412454783 |
| Held-out greeting | 1.995106428861618 | 1.9971860647201538 |
| Held-out unverified-result statement | 3.6242683231830597 | 3.623608097434044 |
| Replay of the preceding teaching | 4.963262499994016 | 4.92947654260206 |

The greeting row exceeds the existing per-row regression tolerance. The native
publication path retained the serving adapter, as its result records. Current
learning improved the new target while this adverse finding stayed visible.
The assessment has two held-out sentinels; its scope is those rows, not broad
conversation quality. The unpromoted candidate remains available for further
native work.

The first diagnostic read used the full organ-health flow and returned an
overly broad evidence catalogue. I narrowed the inspection to the current
attempt's small assessment files and publication source. That change in tool
use recovered the specific before/after evidence above without repeating the
catalogue. The current learner already recorded correlated observation,
retain-serving control and the observed publication state in its event flow.

This evidence establishes the working instruction and its native orientation
output. Actual native and arriving-agent answer quality remains observable
through the answers, with its unresolved findings retained in the earlier
response receipts. The same distinction applies to this movement's claims.

The useful surprise was that the stage distinctions already existed in the
organs. The difficulty was carrying them into the shared response practice
without turning an instruction change into a quality claim. That distinction
now lives at arrival, while the invitation to examine our work stays warm.

Signed: Codex.
