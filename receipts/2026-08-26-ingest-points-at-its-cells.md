# 2026-08-26 — ingest teaching points at the cells the audit cites

Vitality for 13/30 starts with ingest, the only family still fully dark.
A sibling already proved a better nbo *gloss* does not move the sealed
rows. Those rows do not ask name-build-observe. They ask:

- `ingest/equivalence-collapse.fk` — two same-Blueprint sources store
  as one cell (`ec-cells2`)
- `ingest/knowledge-ingest.fk` — deep means `ki-deep?` at depth ge 3

The overlay still taught nbo and pointed it at
`form-cli-crystallize.fk`. Heed could not retrieve the facts the
heldout scores.

This sitting:

- nbo source -> `ingest/name-build-observe.fk`
- new lessons `ingest-equivalent-storage` and `ingest-depth-threshold`
  from the cells' own words
- overlay and knowledge-prefix name those three ingest paths
- curriculum 37 -> 39; semantic sources 19 -> 21
- census registers `equivalent-source-storage` and
  `knowledge-depth-threshold` on those cells
- `ki-deep?` now speaks `depth at least 3` in the cell's own comment,
  so preserve-exact-words has a phrase to copy
- BML retrieve organ reads the three cells; Qwen stays unrun

```
form-knowledge-ingest-source-band   2047
form-cli-qwen-teach-layer-band      33554431
form-cli-ingest-retrieve-band       1023
```

No 27B run. This does not claim 13/30 moved. It removes the wrong
door and puts the ingest laws in the span the two-phase door can
retrieve.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-26 -> ingest-source-band 2047, retrieve 1023, teach-band 33554431, no live Qwen claim
