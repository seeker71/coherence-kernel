Frozen before generation. Same retained deadline question, original documents, source/report checks, Qwen model, context 32768, two replies, 512 initial and 1024 final allowance. Add only the exact native dcc-card as one context document. The prior answer is not model input. This is a known failed case, not held-out transfer. Inspect whether the actual draft invents a recipient motive, changes the deadline, retains an unfilled signature or narrates the writer's conduct; inspect complete usable wording and accurate local-revision/codebook boundaries. Existing field checks remain narrow and unchanged. No expected draft, response word blacklist, provider resource or evaluation training. Count total execution and coordination separately.
