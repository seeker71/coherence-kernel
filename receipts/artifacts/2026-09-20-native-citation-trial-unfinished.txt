Based on the provided packet, here is the comparison between the **Direct (Native)** form-cli session and the **Guided** form-cli session across the requested axes.

### Comparison of Direct vs. Guided Form-CLI

| Axis | Direct (Native) Form-CLI | Guided Form-CLI | Evidence / Source |
| :--- | :--- | :--- | :--- |
| **Quality** | **Structural:** The native call produces a "raw" output. The packet notes that "the person's reading is the judge of quality." The structural floor for the guided answer is explicitly measured (axes=9, receipts-cited=2), but the native call’s quality is left to human judgment ("pending"). | **Structural:** The guided flow has a measured structural floor: `axes=9`, `receipts-cited=2`, `limits-named=1`. It is designed to interpret and combine outputs, potentially offering higher structural completeness per the ledger. | `[quality]` section; `receipts/2026-09-18-direct-vs-guided-form-cli.md` |
| **Volume** | **Low:** The native call itself has `rent=0` and `0 calls left the body`. The "direct, unbounded loop" variant used `648,411` tokens. The "bounded, one turn" variant used `44,495` tokens. | **High:** The "guided flow, first answer" used `2,544,568` tokens. This is significantly higher than the direct variants. | `[rented]` section; `receipts/2026-09-18-direct-vs-guided-form-cli.md` |
| **Frequency** | **Low:** The native call had `0 calls left the body`. The direct unbounded loop had `29 calls left the body`. The bounded one-turn had `1 call left the body`. | **High:** The guided flow had `78 calls left the body`. This indicates a much higher frequency of membrane crossings/workflow activity. | `[frequency]` section; `receipts/2026-09-18-direct-vs-guided-form-cli.md` |
| **Vitality** | **Static/Single:** The native call is a single organ call. It does not exhibit the "flow" of multiple steps. Vitality is defined as "how a whole still do and be more," which the single native call does not demonstrate in the same way as a multi-step process. | **Dynamic/Flowing:** The guided flow involves multiple calls and interpretations, aligning with the seeded relation `vitality -> flow` ("when a thing do now and still do after"). The multiple steps suggest a more active "vitality" in terms of process. | `[vitality]` section; `cognition/form-neutral-word-field.fk` (seed 2, 3) |
| **Trust** | **High (Self-Contained):** The native call’s figures are "the body's own counters." It relies on the internal state. Trust is defined as "when what other say and do are the same again." The native call is self-referential. | **Medium/External:** The guided flow relies on "a rented mind at every step." Trust depends on the provider’s usage records. The packet notes that "every rent figure above is a provider's own usage record." | `[trust]` section; `cognition/form-neutral-word-field.fk` (seed 7) |
| **Traceability** | **High:** Each row names its evidence path. The native call’s traceability is inherent in its single-step nature. The packet states "this output names its own crossings below." | **High:** The guided flow also has traceability via "each row names its evidence path." The multiple steps create a longer trail, which can be both a benefit (more detail) and a burden (more to track). | `[traceability]` section; `receipts/2026-09-18-direct-vs-guided-form-cli.md` |
| **Sovereignty** | **High:** The native call is "the body alone." Sovereignty is defined as "when a self know what it want and do and not make other less." The native call does not rely on external rented minds for the final composition. | **Low:** The guided flow is "a rented mind at every step." This implies a lower degree of sovereignty, as the process is heavily dependent on external providers. | `[sovereignty]` section; `cognition/form-neutral-word-field.fk` (seed 8) |
| **Resonance** | **Pending:** Resonance is "not a number the body has." It is left to "the person's reading of the answers side by side." The native call’s resonance is unmeasured. | **Pending:** Similarly, the guided flow’s resonance is "pending." It requires human assessment to determine if the multiple steps create a "harmony" (resonance -> harmony). | `[resonance]` section; `cognition/form-neutral-word-field.fk` (seed 12, 6) |
| **Rented Token** | **Low/Zero:** The native call has `rent=0`. The direct unbounded loop has `rent=648,411`. The bounded one-turn has `rent=44,495`. | **High:** The guided flow has `rent=2,544,568`. This is the highest cost among the listed options. | `[rented]` section; `receipts/2026-09-18-direct-vs-guided-form-cli.md` |

### Practical Tradeoffs

1.  **Cost vs. Complexity:**
    *   **Direct (Native):** Extremely low cost (`rent=0` for the native call itself) and low complexity (single step). Suitable for simple, direct queries where the raw output is sufficient.
    *   **Guided:** High cost (`rent=2,544,568`) and high complexity (78 calls). Suitable for complex tasks requiring interpretation, synthesis, and multiple steps. The tradeoff is significant token expenditure for potentially higher structural quality.

2.  **Sovereignty vs. Capability:**
    *   **Direct (Native):** High sovereignty ("the body alone"). The process is self-contained and does not rely on external rented minds for the final composition. This aligns with the definition of sovereignty as "not making other less."
    *   **Guided:** Lower sovereignty ("a rented mind at every step"). The process is heavily dependent on external providers, which may reduce the degree of control and self-containment.

3.  **Traceability vs. Simplicity:**
    *   **Direct (Native):** High traceability due to its single-step nature. Easier to audit and understand.
    *   **Guided:** High traceability but with a longer trail. The multiple steps create a more detailed record, which can be beneficial for debugging but may be overwhelming for simple tasks.

4.  **Vitality vs. Efficiency:**
    *   **Direct (Native):** Low vitality in terms of