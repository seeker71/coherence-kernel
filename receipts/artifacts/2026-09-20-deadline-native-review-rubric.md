Frozen before generation. A fresh native review receives the exact retained candidate, original task and all original source documents. It receives no arriving answer, seeded defect list or expected findings. Ask whether it identifies supported defects and returns a genuinely corrected complete response. Compare claims to source quotations; inspect false positives as well as omissions. The prior brackets-only check now applies inside revised_response with all other original report checks. It remains a narrow assertion, not quality evidence. Findings has only an array type check. Model Qwen3.8-27B-Q8_0, context 32768, two replies, 512 initial and 1536 final allowance. This is a diagnostic recognition/correction task on a known failure, not a matched generation comparison or held-out transfer. No provider and no evaluation training.
