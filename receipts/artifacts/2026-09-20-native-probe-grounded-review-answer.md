Based on the supplied source code and native observations, here are the concrete findings regarding accounting or correlation defects.

**1. Incorrect Calculation of `uncached_input_tokens`**
*   **Triggering Event Sequence:** A valid `thread/tokenUsage/updated` notification is received where `inputTokens` is 100, `cachedInputTokens` is 20, and `cacheWriteInputTokens` is 10. The reader processes this snapshot.
*   **Source Location:** `form/form-stdlib/bml/form-cli-provider-notification.bml`, function `fpn-report`, specifically the line defining `uncached_input_tokens`.
*   **Defect Analysis:** The code calculates `uncached_input_tokens` as `sub(fpn-num(total,"inputTokens"), fpn-num(total,"cachedInputTokens"))`. According to the `input_partition` contract, `cachedInputTokens` is a subset of `inputTokens`. However, the `cache_writes` contract states that `cacheWriteInputTokens` is reported separately and its presence does not imply it should be subtracted from uncached input. More critically, standard accounting for token usage typically defines "uncached" input as the portion of input *not* served from cache. If `cacheWriteInputTokens` represents tokens written to cache (which are part of the input but not "cached" in the sense of being retrieved from a previous cache hit, or potentially distinct from the `cachedInputTokens` retrieval count), the simple subtraction `input - cached` may misrepresent the cost-relevant uncached volume if `cacheWriteInputTokens` is intended to be a distinct billing category or if `cachedInputTokens` does not account for all non-billed input. However, the most direct defect is that the code *ignores* `cacheWriteInputTokens` in the derivation of `uncached_input_tokens` while reporting it separately. If the provider's definition of `inputTokens` includes both cached and uncached, and `cacheWriteInputTokens` is a subset of the *uncached* portion (tokens being written to cache for future use), then `input - cached` correctly yields the uncached total. But if `cacheWriteInputTokens` is a separate metric that *should* be excluded from "uncached" for pricing purposes (i.e., uncached = input - cached - cacheWrite), the current calculation overstates uncached tokens. Given the contract says "Its presence alone supplies no additive price rule or instruction to subtract it," the code's behavior is technically compliant with the *provided* contract, but the contract itself is ambiguous on whether `cacheWriteInputTokens` is a subset of `inputTokens` or additive. If `cacheWriteInputTokens` is additive to `inputTokens` (i.e., `totalInput = inputTokens + cacheWriteInputTokens`), then `inputTokens` is already the uncached portion, and subtracting `cachedInputTokens` (which is a subset of `inputTokens`? No, `cachedInputTokens` is usually a subset of total input). Let's re-read: `cachedInputTokens` is a subset of `inputTokens`. So `inputTokens` = `cached` + `uncached`. The code computes `uncached` = `inputTokens` - `cachedInputTokens`. This is mathematically consistent with the stated subset relationship. The defect is likely that `cacheWriteInputTokens` is *not* subtracted, but the contract says it *shouldn't* be. Therefore, this may not be a defect in the code relative to the contract, but a defect in the contract's clarity. However, looking at `totalTokens` validation: `ge(totalTokens, add(inputTokens, outputTokens))`. If `cacheWriteInputTokens` is part of the input cost but not included in `inputTokens`, the total might be understated. But the contract says `cacheWriteInputTokens` is optional and separate.
    *   *Correction*: A clearer defect is in the **`unattributed_tokens`** calculation.
    *   **Revised Finding 1: Misleading `unattributed_tokens` Calculation**
    *   **Triggering Event Sequence:** A notification arrives with `inputTokens`=100, `outputTokens`=50, `totalTokens`=160, and `cacheWriteInputTokens`=10.
    *   **Source Location:** `fpn-report`, line defining `unattributed_tokens`.
    *   **Defect Analysis:** The code calculates `unattributed_tokens` as `totalTokens - (inputTokens + outputTokens)`. In this example, `160 - (100 + 50) = 10`. This 10 is attributed to "unattributed." However, if `cacheWriteInputTokens` (10) is a distinct component of the total token count that is not included in `inputTokens` or `outputTokens`, then these tokens are *attributed* to cache writes, not unattributed. The code fails to subtract `cacheWriteInputTokens` from the unattributed remainder, thereby misclassifying known cache-write tokens as "unattributed." This violates the principle that reported quantities should be accounted for. The contract states `cacheWriteInputTokens` is reported separately, implying it is a known quantity. By not subtracting it from the residual, the reader reports a non-zero `unattributed_tokens` value that is actually fully explained by `cacheWriteInputTokens`.

**2. Failure to Validate `cacheWriteInputTokens` Against `inputTokens`**
*   **Triggering Event Sequence:** A notification arrives with `inputTokens`=10, `cachedInputTokens`=5, and `cacheWriteInputTokens`=20.
*   **Source Location:** `fpn-valid-usage`, specifically the `if json-object-has?(node,"cacheWriteInputTokens") then fpn-int(node,"cacheWriteInputTokens") else 1` clause.
*   **Defect Analysis:** The validation function `fpn-valid-usage` checks that `cachedInputTokens <= inputTokens` and `reasoningOutputTokens <= outputTokens`. However, it only checks that `cacheWriteInputTokens` is a non-negative integer (`fpn-int`). It does not validate that `cacheWriteInputTokens` is logically consistent with `inputTokens`. For instance, if `cacheWriteInputTokens` represents tokens written to cache from the current input, it should logically be `<= inputTokens` (or `<= inputTokens - cachedInputTokens`). If `cacheWriteInputTokens` is 20 and `inputTokens` is 10, this is a logical impossibility if cache writes are a subset of input. The reader accepts this invalid snapshot as valid, leading to corrupted cumulative state. The contract implies `cacheWriteInputTokens` is a quantity related to input, but the code lacks the correlation check to ensure it doesn't exceed the total input volume.

**3. `fpn-monotone` Check Excludes `cacheWriteInputTokens`**
*   **Triggering Event Sequence:** Two consecutive notifications are received for the same thread/turn. The first has `cacheWriteInputTokens`=10. The second has `cacheWriteInputTokens`=5. All other fields (`inputTokens`, etc.) are non-decreasing.
*   **Source Location:** `fpn-usage`, specifically the call to `fpn-monotone(nth(state,3), total, list("inputTokens","cachedInputTokens","outputTokens","reasoningOutputTokens","totalTokens"))`.
*   **Defect Analysis:** The monotonicity check ensures that cumulative quantities do not decrease. The list of keys checked for monotonicity includes `inputTokens`, `cachedInputTokens`, `outputTokens`, `reasoningOutputTokens`, and `totalTokens`, but **excludes** `cacheWriteInputTokens`. If `cacheWriteInputTokens` is a cumulative counter (as implied by the "cumulative thread usage" scope and the fact that it is part of the `total` snapshot), a decrease in this value indicates a regression or data corruption. The reader will accept a snapshot where `cacheWriteInputTokens` decreases, failing to detect a cumulative regression for this specific field. The native observations show `cacheWriteInputTokens` regressed from 5 to 4 in the probe, and the status was `observed-prefix` (not `cumulative-usage-regressed`), confirming that the reader did not flag this regression because it was not included in the monotonicity check. This is a direct correlation defect: the reader fails to enforce the cumulative invariant for a declared field.

**Actual Check Results:**
*   Preflight exit=0.
*   Band exit=0; original band output=1.
*   Native probe detected 5 regressions and 1 gap. The gap corresponds to `cacheWriteInputTokens` (status `observed-prefix`, gap 1), which aligns with Finding 3 (the reader did not flag the regression as a failure, hence the "gap" in detection logic). The other 5 fields were correctly flagged as `cumulative-usage-regressed` by the probe, but the reader's `fpn-monotone` function would have failed them if they were the only changes, but since `cacheWriteInputTokens` is excluded, a regression *only* in `cacheWriteInputTokens` would pass the reader's validation, which is the defect.