The earlier leak findings do not survive. In `fcms-open-context`, both prefill refusal paths return a session object containing the opened `ctx` and `sts` handles, rather than closing them locally. This is intentional: the comment explicitly states that physical release is deferred. The caller, `fcnv-run`, invokes `fcms-release-ok?` on the resulting session, which correctly frees the state and closes the context if the session is resident. Since the caller guarantees cleanup for all returned sessions, including refusals, no leak exists.

No concrete remaining fault is supported by this source. The code correctly separates logical status from physical ownership, and the release path handles all resource types (adapter, state, context) with expected count verification. The logic is sound and complete for the provided scope.