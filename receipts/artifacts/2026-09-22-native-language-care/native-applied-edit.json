{"tool":"edit","arguments":["form/form-stdlib/form-cli-turn-evidence-target-bind.fk","  let turn-id = read_line();\n  if nothing?(turn-id) then form_error(\"turn-evidence-target: expected one line, got EOF\") else 0;\n  if str_len(turn-id) == 0 then 0 else 0;\n  let i = 0;\n  let ok = true;\n  while i < str_len(turn-id) and ok {\n    let c = str_byte_at(turn-id, i);\n    if or(and(c >= 48, c <= 57), and(c >= 65, c <= 90), and(c >= 97, c <= 122), c == 45, c == 95, c == 46, c == 58) then 0 else ok = false;\n    i = i + 1;\n  }\n  if not(ok) then form_error(\"turn-evidence-target: invalid identifier; allowed: ASCII letters, digits, hyphen, underscore, dot, colon\");","  let turn-id = read_line();\n  if nothing?(turn-id) then form_error(\"turn-evidence-target: expected one line, got EOF\") else 0;\n  if str_len(turn-id) == 0 then 0 else 0;\n  let valid = true;\n  let i = 0;\n  while i < str_len(turn-id) and valid {\n    let c = str_byte_at(turn-id, i);\n    if or(and(c >= 48, c <= 57), and(c >= 65, c <= 90), and(c >= 97, c <= 122), c == 45, c == 95, c == 46, c == 58) then 0 else valid = false;\n    i = i + 1;\n  }\n  if not(valid) then form_error(\"turn-evidence-target: invalid identifier; allowed: ASCII letters, digits, hyphen, underscore, dot, colon\") else 0;"],"input":""}