{"tool":"edit","arguments":["form/form-stdlib/bml/form-cli-answer-length.bml","  def fal-feedback(words,lower,upper) = str_concat(\"Form runtime measured \",str_concat(int_to_str(words),str_concat(\" words in the preceding answer; its requested range is \",str_concat(int_to_str(lower),str_concat(\"–\",str_concat(int_to_str(upper),\". Revise that answer now to satisfy this range while preserving the original enquiry, source conditions, distinctions and useful content. Return only the complete revised answer. This observation supplies no new domain evidence. Do not enumerate words to count them.\"))))));","  def fal-feedback(words,lower,upper) = if gt(words,upper) then str_concat(\"Shorten the preceding answer from \",str_concat(int_to_str(words),str_concat(\" words to \",str_concat(int_to_str(add(lower,div(sub(upper,lower),2))),str_concat(\" words, inside the requested \",str_concat(int_to_str(lower),str_concat(\"–\",str_concat(int_to_str(upper),\" range. Remove repetition and secondary elaboration while preserving the essential facts, source distinctions and enquiry. Return only the complete revised answer without self-assessment or counting narration. This observation supplies no new domain evidence.\"))))))) else str_concat(\"Expand the preceding answer from \",str_concat(int_to_str(words),str_concat(\" words to \",str_concat(int_to_str(add(lower,div(sub(upper,lower),2))),str_concat(\" words, inside the requested \",str_concat(int_to_str(lower),str_concat(\"–\",str_concat(int_to_str(upper),\" range. Add useful missing explanation rather than padding while preserving the original enquiry, source conditions, distinctions and useful content. Return only the complete revised answer without self-assessment or counting narration. This observation supplies no new domain evidence.\")))))));"]},"input":""}