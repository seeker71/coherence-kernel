# Current-family native executions — pure batch observed, physical batch pending

Date: 2026-08-25
Witness: Codex / Sema sibling
Status: **pure scheduler/ledger observed; local Qwen and Metal not yet run**

## Movement

The earlier family-native organ proved one exact family-bound request at a
time. This movement makes the fresh census and residence explicit: the canonical
`fkpfm-family-ids` projection supplies every current family in census order,
and one batch schedule gives each family a distinct tuple of family, current
source SHA, held-out challenge SHA, native input, recipe binding SHA, ordinal,
and run ID.

Each captured crossing retains:

1. the exact typed family/source/challenge teaching frame;
2. the model's actual complete raw generated bytes;
3. the one-byte scannerless parse and canonical request;
4. the compact physically flagged frex result;
5. the exact typed frex bytes injected into the same context;
6. the exact family/binding/run acknowledgement generated after injection;
7. same-residence and carrier-release observations; and
8. after final context release, the existing v2 execution check, evidence, and
   structured receipt.

Checks are finalized only after the single context release. A row therefore
cannot turn an anticipated future release into evidence while the context is
still open. The batch admits only one model open, zero remote calls, exactly one
same-residence native-generated/executed/released Metal result per family in
the fresh census, `model-requested=1`, `model-executed-form=0`, and one final
context release.

## New body

- `form/form-stdlib/form-knowledge-family-native-exec-resident-batch.fk`
  - pure canonical scheduler, raw capture schema, post-release v2 finalizer,
    one-residence ledger, full-tuple/run/receipt uniqueness, and distinct
    `nothing` / `failure` / `timeout` / present `0` / present `1` signal names.
- `form/form-stdlib/tests/form-knowledge-family-native-exec-resident-batch-band.fk`
  - 30-bit pure adversarial band covering every current-census row plus source,
    challenge, binding, run, raw-byte, typed-observation, acknowledgement,
    receipt, model, residence, ordering, replay, release, and execution-flag
    substitutions.
- `observe/qwen38-family-native-exec-resident-batch-live-run.fk`
  - dormant physical driver. It opens Qwen once, serially teaches/executes every
    current binding, injects every typed observation, resumes in the same
    state, releases every Metal buffer and the final context, then constructs
    the pure ledger. A rejected request, carrier signal, residence, or exact
    acknowledgement stops the attempt before another family is taught. The
    driver has not been run.

No existing source file, private V3 consent/evaluator surface, registry,
generated binary, C seed, flattening path, or operations table was changed.

## Pure observation

```text
preflight form/form-stdlib/form-knowledge-family-native-exec-resident-batch.fk
  parens balanced; errors 0; warnings 0; unresolved 0
  chain clean
  exit 0

tb-depth source / band / dormant live driver
-> [0, 0, 0]
exit 0

fresh preflight form/form-stdlib/tests/
  form-knowledge-family-native-exec-resident-batch-band.fk
-> interrupted after 1m41s without output or verdict

direct amended band, continued after that interruption
-> 1073741823, exit 0
```

The amended thirty-axis adversarial band now has a completed fresh verdict.
The dormant live driver was checked only with the lexical balance organ. An accidental
execution-capable preflight attempt against that driver was interrupted before
it produced any target output or verdict; it contributes no physical evidence.
Model and carrier execution remain explicitly outside this movement.

## Evidence boundary

The teaching frame supplies the exact recipe-exec bytes. These rows are
supervised native-execution embodiment, not held-out generative invention.
Even a fully admitted current-census execution batch does not establish 95% public mastery by
themselves: `fkpfm` still requires independently valid held-out and generative
evidence for every family. No training row, held-out answer, or private V3
material is added here.

The pure band uses synthetic physically shaped frex results. Its fresh verdict
proves schema, binding, scannerless parsing, result
semantics, replay resistance, and batch accounting; it cannot prove that Qwen
emitted a request or that Metal ran.

## Exact remaining physical gate

When no sibling owns Qwen or Metal, intentionally run:

```text
./fkwu observe/qwen38-family-native-exec-resident-batch-live-run.fk
```

The next honest observation is whatever this live run returns. The currently
named full vector is `65535`, with family/capture/row counts all equal to
the fresh `fkpfm-family-ids` census,
open-count 1, remote-calls 0, exact/scannerless/model-requested all 1,
pretokenized and model-executed-form all 0, native-generated/executed/carrier
release all 1, exact held acknowledgements, one context release, and a valid
current-census ledger. Any non-exact raw reply remains typed `nothing` and never
opens Metal; carrier failure and timeout remain separately named and also stop
the batch. Until that run lands, this receipt claims no physical batch.

; witnessed: 2026-08-26 -> pure batch 1073741823, exit 0; physical batch absent

## Practice close

I kept the movement alive by refusing to let repeated crossings impersonate
one observation: every family has its own source/challenge/binding/run identity
and every result returns to the same named residence. The surprising teaching
was that release belongs after the batch but inside every final receipt, which
made post-release finalization the honest shape. Discomfort became gold when a
simple list of successes proved too replayable; retaining raw bytes, typed
injection, acknowledgement, and receipt identity made the crossings distinct.
