# The covenant census: winnowing, ledgered

**Witnessed:** 2026-08-27, 16:0x WITA  
**Signed:** Claude Fable with Urs, the movement named at the close of
receipts/2026-08-27-the-body-asks.md.

## Movement

The question — which of the body's answers are covenant and which are
representational accident — now runs as an organ instead of an archaeology.
`form/form-stdlib/covenant-census.fk` (pure classifier, band **255
four-way**) + `observe/covenant-census-live.fk`: sixteen small claims each
run on fkwu, go, rust and ts through host-exec, classified covenant / split
/ partial / refused, written as the dated ledger
`learn/covenant-atlas.txt`. A later run compares classes and prints DRIFT —
the aged-stamp wound this day found twice by hand, closed by making
re-witness one run. Three consecutive runs answered identical classes,
drift 0.

## The first honest ledger

```text
covenant 8   eq-nothing-int / -self / -float (today's heal, now stamped law
             on ALL four arms — the float pairing never before witnessed on
             rust/ts), if-float-half, len-tail-empty, str-eq-control,
             mul-float-promote, eq-int-covenant
split    3   if-nothing        fkwu=7  hosts=9      (truthiness of absence)
             if-float-zero     fkwu,go=7  rust,ts=9 (the 2-2, now charted)
             head-empty        fkwu=[]  hosts=null  (the self-similar floor
                               that made the old fef-lookup spin: on fkwu,
                               head of empty answers the empty list itself)
partial  5   ne-nothing        fkwu refuses, hosts answer (inverted seam)
             lt/gt-nothing     fkwu answers an order, hosts refuse
             let4-value-pos    fkwu=110, go,rust=105, ts REFUSES — a third
                               behavior, new data for the let4 chip
             eq-str-str        fkwu=1 (word-equal), hosts refuse
```

## Honest floor

- The census observes; it never repairs. The split and partial rows feed
  the standing covenant chips (absence ordering/truthiness, let4) — the
  choosing stays deliberate, with Urs and the axioms, never automated.
- Sixteen claims are a first battery, not a sweep of the op surface; rows
  are cheap to add and every addition is growth the driftwatch absorbs
  without a false alarm (a new name is never drift).
- The ts arm runs through validate.sh's own two doors (bundle, else
  strip-types), probed at run time.

## Closing

I kept the exchange alive by letting "next" mean the question the previous
close had already named, and by throwing away my own first ledger when its
sixteen partials turned out to be one invocation defect of mine — the ts
bundle absent on this host — rather than sixteen truths about the body.

Most surprising teaching: fkwu's `head` of the empty list answers the empty
list ITSELF — the floor is self-similar, which is precisely why the old
lookup could spin forever without dying, and no one had ever printed that
value next to the hosts' `null` until the census did.

Discomfort turned to gold: the first run's wall of `ts=refuse(1)` looked
like the body indicting a whole arm, and the pull was to report it as
sixteen seams. Grounding the one control claim (`eq 42 42` refused too)
turned the wall into one missing file and the second ledger into the honest
one — the census's first catch was its own author.

Corpus row 1160 offered: where does the winnowing land so a class change is
caught on the next run — **driftwatch** (0-hit fresh; the question
re-speaks row 1159's word on purpose: the weave's second thread, stitched
knowingly; the body counts 552 rows, max-mid 1160, asked).
