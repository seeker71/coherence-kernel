# Reversible concept-state readiness — witnessed 2026-07-22

## Attuned criterion

The initial sentence used compulsory universals and treated ice, water, and gas
as artifact kinds. The revised, axiom-compatible offer is:

> A stored concept may expose its current gas, water, or ice state; aliases;
> executable recipe; content identity and composition; evidence lineage;
> computed inquiry responses; transfer observations; and freshness reading.
> Missing or changing facets may keep it available as a candidate for further
> observation.

Gas, water, and ice are reversible states available to a node. Aliases, recipe,
composition, lineage, inquiries, transfer, and freshness travel with that node.

## Frequency reading

The repository does not currently produce a calibrated semantic signature for
this sentence. `form-freq-check` therefore returns its honest unmeasured reading:

```text
[state unknown=2, source none=0, fear-score 0, confidence 0, valid 0]
```

This is abstention, not a claim that the sentence is clear or contracted. A
register reading against the core axioms motivated removing “every,” “must,”
“anything,” and “established ground”: the axioms offer interfaces and reversible
states; they do not legislate this readiness profile as the sole shape.

## Native witness

```sh
./fkwu --src cognition/tests/concept-crystallization-contract-band.fk
```

```text
[nothing, 0, 1, 1001099400,
 [[1,1,1,1,1,1,1,1],2,6],
 [[1,0,1,1,1,1,1,1],1,7],
 [[1,1,1,1,1,1,0,1],1,7],
 [[1,1,1,1,1,1,1,0],1,7],
 [2000830772,2000830772,2000830772],
 3,2,1,[1,1,1,1,1],[2,0,0,0,0],8,1]
```

The eight facet positions are current state, aliases, recipe, content identity
plus composition, evidence lineage, seven-lane inquiry replay, held-out transfer,
and freshness.

- The observed profile is currently ready (`2`) and selects admit (`6`).
- Missing aliases remains candidate (`1`) and selects defer (`7`).
- Failed transfer remains candidate (`1`) and selects defer (`7`).
- Stale freshness remains candidate (`1`) and selects defer (`7`).
- Gas, water, and ice carry the same node identity: `2000830772` three times.
- Evidence is `3` observations split into `2` validation and `1` held-out.
- Axiom compatibility is `[1,1,1,1,1]` for states, cell composition,
  content identity, observed interface, and offer/ack.
- Four decisions produce eight correlated framebuffer events; final success `1`.

## Honest floor

This is one offered readiness profile over one admitted recognition relation. It
does not establish a universal ontology or a calibrated semantic frequency model.
The phase transition is represented and identity-stable here; persistence and
automatic melt/crystallize policy across the broader concept graph remain later
movements.
