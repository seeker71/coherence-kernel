# Glass fields become reachable; inspection stops competing with the publisher

Codex, 2026-09-07. Rebased onto the ear, perception and bearing-census work at
dab58d45. The local binary returned freshness 15 after rebase; rebuilding the
committed seed restored 31. This movement adds no C or runtime sidecar.

## The changes

- Six placeholder memory measurements now use native counters and shared column
  widths. Blueprint quartets are a cell subset; logical bytes remain distinct
  from RSS. Host page bytes and reclaimable headroom keep their separate meanings.
- Memory maps exclude controls/counters masquerading as allocations. Owner
  ledgers and model roots are counted once, leaves are not added twice, derived
  extents remain derived, and handles cannot become byte budgets.
- Arrows/Tab navigate distinct metrics, samples and typed observations in the
  current view. Domain-qualified selectors distinguish Metal from MLX; shared
  projection nodes remain shared. Row 150 is reachable without a 128-row ceiling.
- Retained viewports follow selection, name overflow and keep evidence visible.
  Summary-hidden selected fields get a row. Events/channels apply their filter
  and distinguish an empty selection from a dead publisher. Help fits at 80 columns.
- Frame sequences have sequence units and source clocks. Missing row clocks use
  carried publication time; existing sample clocks survive. Stale ear data does
  not claim OPEN. The new view helpers participate in source-change reload.
- Bounded inspection reads published frames without poking the owner, publishing
  competing Glass metrics or refreshing the governor. Local typed rows identify
  glass.probe. The audit includes jungle/bearing and confirms all twelve views ran.

## Witnesses

The native view band exercises all twelve views at four dimensions, exact
selection, duplicate navigation, stale clocks, evidence and reload membership.
Memory, observer, typed observation, dashboard, live UI, input, sensor rows,
transcript, events/channels and launch bands are the accompanying regressions.
The transcript fixture now supplies the ear API's lane argument: its earlier
8191 with exit 1 was rejected, then repaired to 8191 with exit 0.

Passing band scores: views 4194303; memory 131071; observer 67108863;
typed observation 2097151; dashboard 16777215; live UI 4294967295;
input 131071; sensor rows 2047; transcripts 8191; events/channels 32767;
launch 65535. Drift gates: 4095/4095, refused=0. Scores were accompanied by
exit 0, with preflight reporting zero compiler errors.

The rebased PTY witness passed fifteen immediate-key transitions, maximum
2.77 ms, plus paste/escape, language selection, evidence, signal restoration,
suspend/resume and non-TTY handling. This is terminal-carrier evidence, not a
claim that the human's existing terminal paint was inspected.

At epoch 1788786290488, the non-demo audit read 375 atlas metrics and 185 samples,
completed 12/12 views, and reported 41 memory metrics with no missing gauges
and one typed owner allocation gap. Jungle state now carried publication age
7054884 ms, matching its frame instead of -1. The separate native host reading
was 128 GiB physical memory.
Kernel rejection count, prosody pitch and display state remained unavailable;
several ear/perception/census publications were silent. Counsel reported
orphans=0 and 11/12 lanes unobserved because no hearth stood here.

These source gaps are not painted healthy. No microphone activation, model
admission or voice-home claim was made. Share remained declared/unmeasured.
Usage examples live in docs/glass-keyboard.md.

The surprising teaching: a shared projection identity is useful for bounded
memory but insufficient for selecting one reading. The difficult part became
useful when inspection itself stopped altering the stream being inspected.
That is the movement kept alive here: more reachable evidence, less disturbance.
