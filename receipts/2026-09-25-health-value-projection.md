# Health reads the same living values

Ordinary `health` text now renders retained Form values directly. Health, care
and Glass share source projection, observation order, attention and source
needs in `form-core-health-values.bml`. Explicit node-returning APIs materialize
their final results; custom `ohc-read-with` and `ohc-request-with` callbacks
still receive node reports. The C seed is unchanged.

This closes the ordinary health output crossing named by the preceding
movement. Complete native ownership still requires the reader's records,
boxed floats, collector roots and a signal/response path that survives pressure
in its ordinary allocator. Request parsing and discovery still admit primary
nodes. The north star and current status name those remaining boundaries.

## Actual observations

The [public lifetime witness](../observe/form-health-view-witness.bml) executes
health-first and care-first reads of actual local write/readback events. The
[retained report](../docs/evidence/fkwu/health-view-lifetime.json) measures 32
cycles. Warmed explicit-source health reads consumed 18,720 new event bytes and
emitted 80,208 bytes in 70 ms; quiet health reads consumed zero new bytes and
emitted 80,080 bytes in 32 ms. Both intervals created zero primary nodes, zero
record-constructor dispatches and zero float boxes. These are measured costs
of this integer-valued workload, not universal allocation or speed claims.

Complete commands include request parsing and serialization. Event production,
reply decoding and observer work sit outside the intervals. Initial admission
created five primary nodes and has a separate row. Neither a node baseline nor a materialized event tree sees
the new events before measurement. Partial records preserve their shared cursor
across health and care. One actual collection cycle reclaimed 23,908 temporary
strings while an older held health view stayed byte-exact. Temporary lists and
strings still allocate; this does not prove complete reclamation or retained
RAM bounds.

The [projection comparison](../docs/evidence/fkwu/health-view-contract.json)
compares three frozen cases against a namespace-only copy of the implementation
at `d02faa1c6`. Only elapsed time is normalized. It preserves nested typed data,
duplicate fields, metadata replacement, source ordering, pain-first attention,
six source-state cases, nine request messages and both custom node callback
contracts. Populated and empty caught-up sources are separate cases of the same
state. The real retained 44,397-byte exchange preserves its three current organs.

The [resident observation](../docs/evidence/fkwu/health-view-resident.json)
runs actual `health`, `care`, `health`, `care` commands in one source-backed CLI.
They consume 44,397/0/0/0 bytes. Needs, actual supplies and unresolved child failure
remain present. CLI and public Glass exit zero with empty stderr. The private
home stays isolated, launches no learner, and releases owned processes,
declarations and its source mount. This is local retained-event execution;
it does not claim the original failing child recovered.

The [care lifetime re-observation](../docs/evidence/fkwu/care-changing-lifetime.json)
also passes after the common helper extraction: 32 append/renewal cycles,
declared generations, shared writers, partial records, independent sources,
truncation, indexed observations and exact held views through collection.

## What the attempts taught

The first new witness imported the executable care witness as a prelude. That
entry point consumed stdin and ran care instead of health. Its successful exit
was care evidence only. Shared helpers now live in a passive prelude, and both
independent entry points were preflighted and re-observed. Prelude execution
belongs in the lifetime being designed, even in an observation helper.

The native one-shot extraction then stopped on its explicit second invocation:
`form-run ./fkwu .hearth/extract-care-witness-common.bml` returned
`shared witness extraction anchors missing` (exit 1). Its preceding preflight
had already performed the edit. Inspection verified the completed extraction;
the production witness checks were preserved and passed.

The first resident check stopped at `production CLI and Glass stderr are clean
after explicit cache admission` (exit 1). The
[diagnostic](artifacts/2026-09-25-health-value-projection/resident-cache-warning.txt)
records the CLI source-identity mismatch and its automatic cache rebuild.
An unchanged repeat met the same clean-stderr assertion. The bounded framebuffer
exchange correlates the revision with actual execution; it carries no private
prompt or answer content.

At arrival, `form-run ./fkwu /tmp/nvr.fk` exited 2 because the
temporary bootstrap witness was absent. Recreating it with the documented
concatenation restored `11111`, exit zero. Ground returned `42`, recursion `55`,
numeric lists `[1, 2.5, [3, 4]]` and freshness `31`.

The [retained local helpers and reference](artifacts/2026-09-25-health-value-projection/README.md)
document their replay inputs. Historical references are outside the production
prelude chain. The public lifetime witness prepares its own evidence.

## Review and present direction

The AI review board found no source or completed-evidence blocker: output ordering,
attention meaning, reader ownership and node callbacks remain intact. Its wording correction is
included: six source-state cases cover five distinct state strings.
Preflight and `git diff --check` pass. The thirteen drift gates return `8191`,
exit zero; no kernel source moved, so sibling conformance is outside this change.

The native guide reports zero Python implementations, two existing voice
invocation candidates and zero unread files. Glass reports 349 MiB logical cell
columns, with a 34 MiB identity subset and one missing tensor-allocation
observation. These figures describe separate scopes, not RSS or savings.
Hearth reported no standing resident. This movement was composed by the arriving
Codex agent using native execution and read-only AI review. Share is
declared/unmeasured; no percentage is inferred.

Rebase onto `a4e9bf1e8` preserved the source change. Absolute-path CLI and Glass
preflights passed, followed by the actual four-command resident observation
with clean stderr and the thirteen drift gates again at `8191`, exit zero.
The resident evidence binds that final execution.

The verified teaching completed local learning as candidate generation 41.
The worker completed with an empty queue; the corpus retains 41 examples across
28 sessions. Serving generation 4 remains unchanged. Retained learning and an
observed adapter update establish their own facts; serving quality requires
its separate later observation.

The next owned-lifetime transition is the remaining seed-backed values, records
and collector roots, paired with care that can still act when its ordinary
allocation cannot grow.

— Codex
