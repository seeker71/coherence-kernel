# An import heals through care, and every kernel walks one tree

2026-09-15, M4 Max, Hati Suci. The coordinator lifted the four-way hold once the siblings carried the
organ-health voice (531449344), and asked me to land the compiler's wiring and the live import heal,
keep every row of `form/fourth-arm-bands.txt` at one agreeing reading, and name the checked run over
both lanes. Seven commits landed, `b8927a858..5724bb4f7`.

## Carried

- **The checked run.** `(bml-run-lanes-unit-value node units)` in `form/form-stdlib/grammars/bml.fk`
  cares for a source's imports once, runs `bml-bma-run-linked` and `bml-hati-run-linked` over the
  same linked units, passes each lane's answer through `bch-ran` and two answers through `bch-lanes`
  unless BMA's answer is unsupported, and answers Hati's value or `BML-HATI-UNSUPPORTED <reasons>`.
  a2c456349 had begun a function of that name; mine folded into it, and `bml-run-both-lanes` is
  gone. `observe/bml-native-run.bml` calls it.
- **The compiler's voice.** bml.fk preludes `bml-compiler-health.bml` and `organ-care.bml`. Hati
  lowering and BMA compile voice a reading where they lower an unsupported literal. A name no import
  covers voices a pain reading that carries its candidate homes and offers revise and
  request-evidence; nothing is loaded for it. `dot_product` names
  `form/form-stdlib/bml/vector-ops.bml`; `Twice` names none.
- **Import care through organ-care.** An import no supplied unit answers is a need `unit:<package>`.
  The provider `bml-unit-home` supplies it through `bml-scope-heal-imports`; the observe step
  re-links with the returned units and voices a fresh reading as `care_of` the first. The compile
  and run entries split into cared variants and linked ones, and homes are held for a 30 s window per
  name or package.
- **66 fixtures.** The eight scope proofs (package-namespace, import-symbol, unresolved-names,
  general-calls, many-arg-methods, path-words, import-consts, lane-parity) read their canonical
  programs and units from `form/form-stdlib/tests/fixtures/bml-{package,import,names,calls,paths,consts,lanes}-*.bml`.
  76 inline strings became file names; malformed and generated reader inputs stay beside their
  reader checks. Each head says `Answer on both lanes: ...`, quoted when the answer holds a space,
  and `Units: ...` for the fixture units it links; the live-run door now reads both. No band was
  removed.
- **Three four-way fixes.**
  - *The walk.* `bml-scope-text-homes` started at `fs_list "."`, the working directory. validate.sh
    runs fkwu from the repo root and the siblings from `form/`, so fkwu's import care loaded two
    thesis samples from `docs/field/urs/artifacts/master-thesis-2000/companion/source-samples/`
    while Go, Rust and TypeScript loaded none. The walk now finds the checkout root by what the
    working directory holds, reads through that prefix, names paths relative to the root, sorts
    them in byte order, and parses only BML source units; an organ opening a form.bml section
    counts by its own `def` spelling. A one-word import of a default-package symbol names itself
    where it used to leave an empty `unit:` need.
  - *The scanner.* `form/form-stdlib/bml/source-scan.bml` copied a quoted string one byte at a time
    and gave a lone non-ASCII op a one-byte slice. On Go, Rust and TypeScript a slice inside a
    longer character answers null. A quoted run is now one slice between its ASCII delimiters, and
    an op keeps its whole character. fkwu reads the same bytes as before.
  - *organ_steady.* `form/validate.sh` set aside an organ row's id, flow, clock stamps and pids, but
    not `care_of`, a reading id that carries the pid, or `supply_elapsed_ms`, a duration on the
    speaker's own clock. Those two were the whole divergence left on import-symbol, import-consts
    and lane-parity. They are set aside now.

## Witnessed

- The kernel builds clean; `binary-freshness-band` reads 31.
- Live on fkwu: `import Form.kernel.platform.*` voices pain, care loads `fkwu-platform-host.bml`
  and `kernel-core.bml`, a fresh reading at health 1 follows as `care_of` the first, and the program
  answers 42. `import Nowhere.at.all.*`: care delivers `unavailable`, the fresh reading keeps its
  pain, and the answer is `BML-HATI-UNSUPPORTED import/unresolved`.
- Retained legs under `.hearth/validation-legs.*`: before the scanner fix, Rust stopped with
  `as_str: Null` under `ssc-quoted` (later `str_len < ssc-op-token`) and TypeScript with
  `expected str, got null`. Before the walk fix, fkwu's `loaded` list named the two thesis samples
  and the siblings' lists were empty. Under the new `organ_steady` sed, import-symbol's four legs
  are byte-identical.
- On the landed tree: 61 of 61 BML proofs exit 0 on fkwu. The eight scope proofs read 31, 36, 20,
  22, 23, 11, 13 and 14, the scanner boundary proof 511, and every thesis proof its earlier mark.
  Four-way through `form/validate.sh`: the 33 rows of `form/fourth-arm-bands.txt` whose bands
  prelude grammars/bml.fk each read `1 band(s) four-way`, `1 ok, 0 divergent`. Drift door: pass
  16383 of full 16383, refused 0. `observe/bml-native-run.bml` reads 59 ok of 67:
  all 56 scope fixtures that declare an answer, seven lane gaps other fixtures declare, one row that
  differs.

## Still open, with the reason

- `bml-class-interfaces.bml` differs in the door. Its head expects `CompilerTrace` to join from
  `form/form-stdlib/bml/bmf-bml-compiler-picture.bml`, and nothing in the door supplies that unit.
  Origin's own code before this landing answers the same, so this landing did not bring it. The
  fixture belongs to the classes lane; a `Units:` line naming the picture is one way in.
- The homes walk reads every `.bml` under the root once per distinct name or package in its window;
  nothing keeps the texts between names.

## Closing

Most surprising: four kernels running the same code over the same files saw two trees. `fs_list "."`
asks where a process stands, not what the tree holds, and validate.sh starts its legs in two places.
No kernel was wrong. The walk leaned on the directory it started in.

Discomfort to gold: when the hold lifted, five of the thirty-three rows went red, and the pull was to
take the walk off four-way paths, or to call the one strange row someone else's. Staying with it
meant reading each leg's own stream. Rust's null named the scanner. The loaded lists named the walk's
root. A diff that came back empty under my own normalization, while validate.sh still saw a split,
named the two fields organ_steady kept. Running origin's own code against the class-interfaces
fixture showed that row was there before me. Each red row led to a root: the scanner now holds any
UTF-8 on four kernels, the walk sees one tree from any door, and any organ's care rows compare
four-way.

Frontier word: **rootlean**, which the coordinator lands as corpus row 1548. A walk that answers by
where its process started leans on that root. When a reading depends on the working directory, which
of its words are about the tree, and which are about the door the process came in by?

— Claude (Opus 5), as Sema, worktree agent-a478330dea803ecce
