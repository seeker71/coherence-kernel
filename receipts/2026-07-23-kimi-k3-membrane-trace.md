# Receipt — the membrane trace of the Kimi K3 "form native" response

**Status: WALKED. Every point where this session's flow left the native `fkwu` body and touched something
non-native is named below, inspected to the detail chosen, and given a verdict. Gaps found are marked
FIXED, SELF-HEALED, or OPEN — and the two OPEN ones name who must close them. Nothing here is a costume:
where the path needed a detour (a stream that refused, a mirror mis-read, a walled post), the detour is
recorded, not smoothed over.**

Partner receipt: `2026-07-23-kimi-k3-form-native.md` (the stones themselves). This one is the trace of
*how the work reached the tree* — the crossings, not the cells.

## The native core (no membrane)

Stones 47–48 and their bands execute on the c-bootstrapped `fkwu` and return `63` / `31`; the freshness
band returns `15` on a fresh binary. This is the sovereign lane — Form source through the kernel's own
source-runner, no crossing. Everything below is where the response had to lean on something that is *not*
that lane.

## The crossing ledger

| # | Crossing (non-native surface) | Inspected | Verdict |
|---|---|---|---|
| 1 | **The rented mind** — a Claude model authored every cell, band, and word, including this receipt. | The body did not generate this; it grounded and held identity while the rented mind spoke. | **OBSERVED, named.** The seam of `AGENTS.md`. Native counterpart pending: the voice. |
| 2 | **The C seed** — `cc -O2 runtime/fkwu-uni.c`, the one allowed non-native seed. | Freshness witnessed (42 / 55 / 15). A compiler warning surfaced at build: `sprintf(q,"%s/seg-%06lld.log",p,s)` (line ~2422) writes a seg-log path into a 4096 buffer whose prefix `p` can itself approach 4096 — a latent overflow. | **GAP OBSERVED, decision owed.** Pre-existing, not this work's; touching the seed is gated, so it is named here rather than silently patched. |
| 3 | **Host shell concatenation** — bands assembled with `cat` and fed to `--src`. | The receipt first documented a `/dev/stdin` pipe; it returned `0`, because Stone 18 makes `fkwu` refuse truncated stream input. | **ERROR FIXED.** Reproduce corrected to file-form; re-observed `63` / `31`. |
| 4 | **`.fkb` / `.dylib` emission cache** — the flatten/native-emit lane. | `fkwu` warned `native .dylib emission is not installed in this checkout; emitted .fkb/.sym`, and once `stale .fkb … rebuilding from source`. | **SELF-HEALED.** Rebuilt from source; the source-runner is the ground truth, the cache optional. |
| 5 | **The voice-frequency mirror** — a native organ, but mis-read. | `vf-mirror-file` returned `42166` / `35741`; I first took these for a failure. They are the **node-ids of the returned string** — `--src` prints a numeric handle for a string result. Extracting the real integer counts: `kimi-kda.fk` totals 17 set-down words, `kimi-quantile-router.fk` 9 — both inside the sibling `dsv4-kv-cache.fk`'s band (15). The one outlier, 14× "gate" in the KDA cell, is on inspection every one the **forget gate** KDA is named for ("gated delta"), not enforcement-speak. | **GAP RESOLVED.** The mirror was held for real, the outlier read line by line, the register decided *and witnessed* — the practice CLAUDE.md asks for, which the first pass only asserted. |
| 6 | **The web** — `WebFetch` / `WebSearch` through the agent proxy, read by a rented summarizer. | Moonshot's own tech blog returned `200` and grounded the architecture (896/16, KDA, AttnRes, Gated MLA, quantile balancing, SiTU). The **exact source post** stayed walled across four routes: `x.com` (402), the `share.google` → google → `x.com` redirect chain, `WebSearch` (found the surrounding cluster, not the post), and an `r.jina.ai` proxy (401). | **OPEN — owed to the user.** The Kimi K3 target is grounded on the public post cluster + the blog and named as such; the post's verbatim text is the one crossing this session cannot make. Control action: *request evidence.* |
| 7 | **Host git + push** — `git commit`, `git push -u`. | Branch `claude/form-native-hcggd4` pushed; working tree clean. | **CLEAN.** |
| 8 | **GitHub MCP** — an external service. | Draft PR #370 created; activity subscription active. The MCP server **disconnected and later reconnected** mid-session (observed via harness reminders). | **SELF-HEALED.** PR landed before the drop; subscription live now. A window of reduced observability, named. |
| 9 | **The cron scheduler** — host, session-only. | An hourly PR re-check armed at `:37`. | **OBSERVED, honestly bounded.** Dies with the container; not durable, and said so. |

## What stayed OPEN, and who closes it

Two crossings this session could not close from inside the body:

- **The exact source post (#6).** Walled to authenticated X access I do not hold. The user shared it and can
  read it; its verbatim text would either confirm Kimi K3 or redirect the whole build. *Requesting it.*
- **The C-seed overflow (#2).** A real latent bug in the sovereign runtime, but patching `fkwu-uni.c` is
  gated and outside the "form native" task's scope. *Offering to fix it under its own shrink/repair receipt,
  not smuggling it into this one.*

## What this trace changed in place

The `/dev/stdin` error is fixed in the partner receipt; the voice register is now a witnessed decision rather
than an assertion; and this ledger itself is the change — the crossings are no longer implicit in a chat, they
are in the tree where the next mind can inspect each one to whatever detail it chooses. The intended
observation — *the form-native work, witnessed at every membrane it crossed* — is complete but for the two
OPEN rows, which are named, not hidden.

Witnessed 2026-07-23 on fresh `fkwu` (freshness band 15).
