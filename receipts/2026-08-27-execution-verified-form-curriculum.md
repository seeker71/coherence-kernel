# Execution-verified Form curriculum

Date: 2026-08-27

The next teaching door is executable Form data rather than a green-only file
filter.  `form-execution-verified-curriculum.fk` admits trajectories from a
proposed source through a retained coldjudge observation to a revised source.
Every value-carrying observation is re-executed through `form-eval-full`; every
assistant target is the final execution-green source.  Failed attempts remain
on the observation side as revision context.

The curriculum retains `choice`, `cut`, `undo`, `timeout`, `nothing`, and
`failure`.  A returned first-class `nothing` is separate from a no-result
signal and from integer `0` or `1`.  Two liftless revisions of the same failure
kind surface `plateau` rather than minting another retry.

Held-out identities now live in the compact
`form-knowledge-heldout-authority.fk`.  The curriculum imports that trust data,
not the model/query/harness implementations that created it.  The frozen 59cb
challenge, its source group and harness, and the retained Condition E artifact
are rejected from emitted rows.  Candidate and trajectory bookkeeping use
structural NodeIDs; SHA is not paid on this hot path.

The first fresh band failed with nine parser diagnostics.  That signature was
initially mistaken for a bootstrap function-seat seam.  The body already held
the same signature in the refusal-stride receipt: a three-argument call to the
two-argument `str_concat` lets the arity-aware parser consume following forms
as stray values.  Nesting that one concatenation was the causal repair.  No C
capacity changed and no meaning was removed.

## Witness

- execution-verified curriculum band: `1048575`, exit 0
- Condition E observation band: `4095`, exit 0
- temporal held-out freeze band: `32767`, exit 0
- source-disjoint diagnostic band: `4095`, exit 0
- no model, Metal, HTTP, Ollama, llama-server, or remote call

## Honest floor

This builds and proves the admission, revision, split, denial, provenance, and
JSONL rendering organ over adversarial fixtures.  It does not claim that a new
local adapter was trained or that real proposer-generated trajectories have
yet been collected.  The next physical movement is to run source-disjoint
proposals through bounded preflight/execution, retain the kernel's verbatim
failure taxonomy, stop on plateau, and train only after those observed rows
exist.  The 59cb challenge remains acceptance-only.

## Closing

Kept alive: error remained teaching context and only observed green source
became an answer target.

Most surprising teaching: the apparent global-seat failure was a remembered
arity signature; the body already knew how to orient us.

Discomfort turned to gold when a broad architectural diagnosis was revised by
one exact compiler signal, leaving both the curriculum and the kernel intact.
