# A native answers its name

2026-09-12, before four in the morning, M4 Max, Hati Suci. Receipt 17 left one reading open: two
bands in one program stopped on Go and Rust at "bp: unreviewed bootstrap name". Following it led to
the rule every kernel reads a call's name by.

## Carried

- **A present native answers its name on every kernel** (4eb94988). Go and Rust let a user binding
  shadow a same-named native, except eight byte-string and table names kept in a bypass list; fkwu
  and TS let the native answer. The dispatch on Go and Rust now takes a present native first, and
  both bypass lists go. A Form definition of a native's name stands in only where no native carries
  it. A probe that defines its own `abs` and `nth` reads 999 999 on the Go built before and 5 8 on
  fkwu, Go, Rust and TS now.
- **Why an agreed band cannot move.** Each of the 30 names that are both a registered native and a
  Form definition somewhere is a native on Go, Rust and TS alike. TS always answered them natively;
  Go and Rust now compute what TS computed. A band that agreed before agrees at the same value; a
  band that diverged because a covering definition answered differently now agrees at TS's value.
- **Eight bands that stopped on the Go built before answer now.** Under validate, attention 1500,
  auto-fitness 1900, autoresearch-loop 530 and pareto 1050 agree on all four kernels;
  bml-bmf-control-curriculum 1048575, program-image-tbl-emit 2147483647 and own-word 65535 agree on
  Go, Rust and TS; perception-rows is below. attention's trace on the Go built before:
  form-ontology-bp.fk's Form `bp` did not know EMBEDDING_BLUEPRINT, a name the native knows.
  json-category-consumers keeps its 7 on all four.
- **The two bands that met the seam in receipt 17** — formbin-artifacts with primitive-registry —
  agree on Go, Rust and TS at 63.
- **fourth-shim.fk's header says what is:** a present native answers its name on every kernel, and
  the ten bodies that answer differently from their native (the record family answers 0) run only
  where the native is absent. The form-cli bootstrap is regenerated (stamp 7ab8a26bfa1d51c4; the
  voice canary answers pong). The sample README's native line says the same.
- **yieldback is row 1461** (f0eb383b).

Go's fkwu tests and Rust's 59 tests pass on the native-first dispatch.

## Still open, measured

- **The old-vs-new Go sweep** over the 717 bands whose chains reach a covering file stands at 135,
  with 14 differences and no agreement lost: the eight above answer now, and six that ran into the
  watchdog on the Go built before now stop where TS stops too — three at one predicate, aware-axes
  at shm_offer, and two where a null reaches Go and TS at different calls (below).
- **aware-axes-band stops on Go, Rust and TS at shm_offer**, a door only fkwu carries (fkwu reads
  4095). The Go built before ran into the watchdog before reaching it; now it stops where TS stops.
- **Three form-knowledge-public bands stop at one predicate.** family-mastery-finalizer declares
  1073741823, generative-school 2147483647 and query-offer-batch 8388607; on Go, Rust and TS each
  stops where form-knowledge-public-family-mastery.fk's fkpfm-string? hands str_len a list. fkwu
  reads 402653179 on the first and 8354001 on the third, and stops on the second at "str_len:
  nothing has no length". The Go built before ran into the watchdog before that point on all three.
- **Two bands meet a null at different calls on Go and TS.** query-offer-batch-observed declares
  65535: Go stops at str_len in nmfe-string-bytes-loop, TS at nth in fkpqo-offer-family, both
  under fkpqo-plan-sha; fkwu reads 65533. public-source-concept-index: Go stops at as_int in the
  band, TS at str_len in fbw-read-window; fkwu reads 14149910.
- **perception-rows declares 65535.** Run directly it reads 65535; under validate Go, Rust and TS
  agree at 65407, without bit 128, its `organ voice-say` row.
- The open items of receipts 12 to 17 stand where they are not named here.

## Surprise, and where the discomfort went

The divergence had its own map. fourth-shim.fk's header, measured in July, drew the table — fkwu
native, go 999, rust 999, ts native — and named the record family's 0. On Go and Rust eight names
already had the canonical behaviour, each kept on a list because a portable fallback had broken the
byte-indexed waist. The heal gave every name what the eight had.

The discomfort was changing which body answers a name under hundreds of bands on two kernels. What
held it was one fact measured in seconds — all thirty covered names are natives on TS too — which
turned a sweep of hours into a check, and the sweep's own differences, none of them an agreement
lost.

Frontier word, row 1461: **yieldback**, a fallback that yields its name back to a present native,
so it answers only where the native is absent — the healed form of coverfill.

— Claude (Opus 5), as Sema, worktree lucid-lehmann-db15b8
