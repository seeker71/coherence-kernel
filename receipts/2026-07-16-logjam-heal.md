# The logjam broken — the astern lanes healed and witnessed climbing — 2026-07-16

## Ground

```sh
./fkwu --src bootstrap/ground.fk                               # 42
./fkwu --src form/form-stdlib/tests/binary-freshness-band.fk   # 15
```

## The arc

"next arc." The muster had surfaced vision and face astern (lag ≈3,590,
level 1, logs silent since ~03:42). The covenant's standing word is heal
with the body's tools — so: diagnose, heal, and let the muster witness.

## The diagnosis

- The distill scripts are healthy: both drain their ENTIRE inbox per pass
  and always end with an echo; no early gate, no target stop.
- launchd is healthy: both jobs fire every 90 s (runs 4596 / 5585, last
  exit 0).
- The only shape that fits twenty silent hours with a growing inbox: a
  **logjam** — one instance's hung child holds the StartInterval slot
  (launchd will not re-fire while an instance runs), and a killed pass
  leaves no final echo. One stuck log stops the whole river drive, and
  from the outside it looks exactly like health: loaded, exit 0, quiet.

## The heal (the body's own doors, nothing invented)

- One by-hand drain pass of face-distill (the script's own documented
  mode), started 23:47.
- `launchctl kickstart` of vision-distill, 23:51.
- launchd itself re-fired face-distill at 23:51 — proof the slot was free
  again and the door works.

## The witness — the muster read the heal live

```
                23:38 (before)          23:56 (after)
audio           lag  149  level 3       lag  149  level 3   abreast
vision          lag 3590  level 1       lag 2628  level 1   draining
face            lag 3587  level 1       lag  220  level 2   climbed a rung
```

Face's kept store grew 3,258 → 3,807 in the same window: 549 new faces
witnessed. Fresh vision log lines labelling again. The trust ladder moved
exactly as the tissue says it should: witnesses restore rungs; nothing
else does.

## Left standing, named (not fixed at midnight)

- vision-distill's oracle fallback is `/tmp/vision_classify` — the shared
  /tmp collision shape the body already knows; worth moving into the
  store's own bin.
- The logjam's root child (which grab/embed hung at 03:42) was not caught
  in the act; if it recurs, the muster will read it within a cycle — that
  is now the point of having one.

## The most surprising teaching this work left behind

A stalled loop and a healthy loop are indistinguishable to every existing
signal the fleet had — launchd state, exit codes, error logs all said
"fine." The only honest witness was the LEDGER: shipped units nobody
witnessed, piling up in a directory. The stewardship reading built tonight
from a book about loops turned out to be the only instrument on the
machine that could see this failure class at all.

## Where discomfort turned to gold

The pull at midnight was to keep hunting the hung child's corpse — to name
the exact frame it died on, twenty hours cold — before daring to touch
anything. Witnessed, that was recognized as the inspection excuse: the
blocker test (run one pass by hand) took seconds, proved the organ sound,
and the heal was two of the body's own doors. The gold is the order of
operations the evening kept teaching: make the instrument honest, read it,
act on the reading, and let the same instrument witness the act.
