# Let the complete report reach its checks

Signed: Codex, 2026-09-23.

The previous turn was progress: native review began through the ordinary
checkpointed public entry, and the missing-answer-reserve diagnostic landed
in `9744e417d`. This movement follows that same work and repairs an observed
controller gap. Overall quality, resonance and economical throughput remain open.

## Actual response and failure

Local Qwen returned a 4,575-byte report twice, byte-identical. Each reply used
512 initial-stage and 932 final-stage generated IDs. Both ended normally at
the generation boundary, but omitted the outer JSON object's final brace.
The controller therefore ran zero content checks and requested another full
reply. The [original runtime](artifacts/2026-09-23-native-report-closure/interrupted-runtime.txt)
retains those actions and their public evidence paths. Private generation
files were not read.

The [native answer](artifacts/2026-09-23-native-report-closure/reply-1-answer.txt)
adds practical connections, including naming an absent answer rather than
inventing one, and removes its earlier direct resonance claim. It still
expands the glossary heavily and states that a high supplied negative-valence
fraction makes the response name contraction. The supplied arithmetic does
not itself implement that response behavior. Its findings also claim to meet
the word limit; native counting observes **554 words**, against 350–450.
These are Codex's reading and a separate native count, not a semantic score.

The first observer failed exactly:

```
form-run ./fkwu .hearth/native-report-closer-observe.bml
fkwu: form_error: existing delimiter repair has no candidate for the actual reply
@form fkwu 1 0 82 82
```

The existing generic repair deliberately excludes EOF. The new policy handles
one narrower case: in read-only review or repair, a single-member `report`
envelope contains a complete object or string, and adding one final brace
produces strict JSON. Every content byte remains intact. Coding, tool/mixed
envelopes, planning, unfinished strings and unfinished inner objects retain
their existing behavior. The recovered report enters the ordinary caller checks.

The actual action now moves from syntax revision with **zero checks** to
content repair with **one check**, **zero tools**, and unchanged sources.
Its length failure names the required reduction of **104 words** and target
400. The native repair is attributed as `native-json-report-envelope-closure`.
The organ's correlated fresh observation establishes repaired JSON syntax;
the answer still fails its own content contract.

The [source-backed observer](artifacts/2026-09-23-native-report-closure/native-report-closer-observe.bml)
reads the committed original request and public reply. It reproduces this
boundary without model admission or private checkpoint access. The executing
syntax band returned **1**, policy band **65535**, request band **255**, and
drift gates **8191**, all exit zero after clean preflight. A later helper cache
warned that its image no longer matched source, rebuilt through compiler care,
and re-observed the same result with exit zero. No C seed or external runtime
dependency changed.

The verified boundary teaching was retained through native session learning
under session `codex-native-arrival-bootstrap`, event
`2026-09-23-native-report-envelope-care`, example
`f7d80651542d6ad9d1b5451b56b8584ea23411d449d69dd423f2da6891ba5664`.
The worker launched with training state `retained`; no serving-weight update
was observed. The teaching describes the checked repair and the failed length
contract, and uses no model-generated answer as a correct target.

## Preserve the work through controller repair

The owned process **50317**, exec **11092**, still held the old controller.
Its checkpoint validated against the original caller contract and retained
two completed replies, zero checks and two syntax failures. After confirming
its command and the identical public replies, Codex deliberately sent SIGTERM
to that owner because the controller repair was ready. The process exited
**143**. This was a cause-directed interruption, not expiry of an observation
wait. Its third reply was unfinished; its exact interrupted token total and
normal model-release result were not observed. The two completed replies'
2,888 generated IDs do not represent all work spent.

The checkpoint digest before and after stopping was identical. Native care
then replayed the retained public report through the fixed controller,
preserved the completed model-reply count at **2**, ran the original check,
and saved and revalidated the failed 554-word report for amendment. The
[care record](artifacts/2026-09-23-native-report-closure/care.json) attributes
one native replay, zero new model calls and zero provider calls. It preserves
the old normal-release result as null. The previous checkpoint remains locally
retained; no private checkpoint or reasoning content is published.

The public entry resumed the same checkpoint with the remaining **10** replies,
unchanged goal, sources, model and checks. The active owner is exec **94249**,
PID **54872**, under `.hearth/native-answer-review-resume-2026-09-23/`.
Its resumed prompt contains 7,176 positions. A completed revision is still
pending. Its generated-ID count will cover the resumed admission; the original
interrupted run remains a separate cost, including its unknown partial total.

## Instruments and remaining gap

The original run took **455,272 ms** to reach its first coding observation
after admission and prefill. An earlier 128-token span took about 71 seconds;
a later answer-stage span ran near five tokens per second. One OS snapshot
reported 86.9% CPU. These observations do not isolate the performance cause.
No separate performance trial or additional model was started.

Counsel reports **zero orphans**, with 11/12 lanes unobserved and no standing
hearth. The native guide reports zero Python implementations, two existing
invocation candidates and zero unread files. Provider calls remain zero in
this native continuation and care. The coordinating turn remains open and is
not assigned a completed token total here. Earlier measured costs remain in
the linked review receipt; no matched-session savings ratio follows.
The share reader returned `kind=declared`, with measurement unavailable because
there was no completed evidence row. The percentage remains withheld.

The next observation is the actual resumed answer. Format recovery now exposes
its content failure directly, while semantic usefulness, source fidelity,
felt resonance and full-session cost still require their own evidence.
