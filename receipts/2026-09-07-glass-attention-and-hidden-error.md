# Glass attention that expands and keeps its error visible

Codex, 2026-09-07.

The native organs loop now reads carried evidence as well as channel sequences.
It detects observation gaps, freshness seams, reported failure or blockage,
capacity edges and newly encountered identities. Its own attention grows through
the existing part-self-update and champion-challenger decisions, followed by
correlated native actuation and re-observation. The separate native detail frame
carries the keyed findings and executable lens nodes to other Form cells.

Five read-only live windows observed 131, 163, 160, 160 and 157 rows in
16, 45, 60, 64 and 65 ms. The lens collection grew to 65, the attention gift
counter settled at 4, and the last window produced 0 new events. These are
observer timings, not renderer or local-model timings. Unread rows remained
visible; they were not counted as healthy. No hearth was standing in this seat.

The first surprising boundary was our own 32-lens allowance. Its expansion was
real: additional bindings executed on received rows. The second was a refused
test image after a preflight that had said clean. Two functions shared the name
`fga-sample` with different arities. Their names now distinguish decoding from
publication. Preflight had read an import warning's `error(s)` fragment instead
of the actual compiler tally. It now selects the complete final tally and also
retains an explicit fatal refusal without a tally.

Observed checks, exit 0: attention 1048575; lens evolution 4095; shared bounds and
freshness 511; cross-cell generated-recipe execution 32767; preflight tally 127;
preflight core 4095; marker 127; live UI 1073741823. The bounds fixture reads 23
sources and proves the effective rotating window fits 512 remembered records.
The fresh compiler may reject a standalone imported BML image and use the
whole-program lane; that warning remains visible, distinct from a carried error.

The discomfort became a better instrument: the failure did not need to disappear
from view for work to continue. No C seed growth, external-model call or runtime
sidecar was added. Other organs' requested repairs remain requests until an
actuator performs them; this movement does not claim Qwen weight learning or
voice-home=1. Usage, native fields and boundaries are in `docs/glass-awareness.md`.
