# 2026-08-19 — form-cli meets free remotes

Yes named the end: form-cli with a free ChatGPT, or any other
free remote mind, as functional and vital as it can be.

## The joint that already lives

Free ChatGPT arrives inbound. `plugin/chatgpt-plugin.fk` offers
this body as an Action (auth none). Ground, frequency, trace.
The words the visitor reads are still rented. That seam stays
named. This is the free ChatGPT partnership that can be used
today.

## What this sitting built

`form-cli-free-remote.fk` catalogs six faces.

| face | direction | cost | state |
|---|---|---|---|
| chatgpt-plugin | inbound | free | observed |
| groq | outbound | free | host-owed |
| gemini-free | outbound | free | host-owed |
| openrouter-free | outbound | free | host-owed |
| huggingface | outbound | free | host-owed |
| chatgpt-api | outbound | metered | absent |

The ChatGPT REST API is metered. It is not a free face. Putting
it in the catalog as absent is the refusal, not a missing row.

Walk is the standing order: native, then local, then remote.
`ffr-may-speak?` is 1 only when both inner lanes are quiet, water
is present, and a free face is ready. Metered never enters.

An OpenAI-compat envelope and a Form-rendered POST exist as
strings. No completion was sent. No key was invented.

A live GET to `https://api.groq.com/openai/v1/models` (no key)
answered **401**. The outbound membrane is there. The face still
owes a host-reported endpoint.

Every remote turn still captures as the oracle-regent pair.
Standing review-gap at tol 1 is not retired.

Band **2047**. Door: `./fkwu form/form-stdlib/form-cli-free-remote-run.fk`.

## What still makes it more vital

A host-reported free face (gitignored `.form-free-remote-face`)
so one outbound completion can run, be judged, and be captured.
Then the flywheel already written in form-cli-oracle-loop and
form-cli-review-gap has something live to eat.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-19 -> form-cli-free-remote-band 2047, groq GET 401
