# Current measurements and attributed history improve the native answer

Codex, 2026-09-20. Continued from `1a4a99af0`. The preceding movement made
progress by locating competing token figures in historical commentary. This
movement separates that commentary from current measurements, observes a better
native answer, and brings the verified quantity selection into ordinary native
comparison. Overall session quality and human resonance remain open.

## Native change and re-observation

`fcr-attributed-domain` selects seven verbatim historical sections, each labeled
as the original author's observation or judgment. It retains the source path
and original SHA-256, and names five omitted sections. Current quantities,
output artifacts, concepts, question and coverage are unchanged. Historical
material remains available for audit; omission is not a finding that it is false.
The [source](artifacts/2026-09-20-attributed-native-comparison-source.json) and
[packet](artifacts/2026-09-20-attributed-native-comparison-packet.json) are retained.

The instruction, Qwen3.8-27B-Q8_0 base, `knowledge-query` profile, context 12,288
and allowance 2,048 match the scoped comparison. Both selection and organization
of historical commentary changed; the paired observation does not isolate their
individual effects. No expected answer or provider response was supplied to Qwen.

The [complete Qwen answer](artifacts/2026-09-20-attributed-native-comparison-answer.md)
uses **649,462 direct**, **45,966 bounded** and **2,544,568 guided** tokens
consistently. It identifies the guided quantity as partial, cites actual source
paths beside claims, and attributes historical judgments to their author. It
does not repeat the older primary-model totals or uncached percentages. These
are observed improvements over the [scoped answer](2026-09-20-scoped-native-comparison.md).

The answer still omits output byte counts. It presents provider call counts
alongside all-reported-model totals without naming the narrower call coverage.
Its conclusion turns properties of these particular runs into broader workflow
advice, including a sovereignty/vitality trade-off the measurements do not
establish. The identity repair remains ambiguously described as named with its
repair instead of clearly proposed. These limits remain visible in the returned
words. The answer is an improved evaluation, not a general serving promotion.

## The repair also serves ordinary comparison

`fnc-route-usage-text` now uses `fsu-select`, so `fnc-compose` and its callers emit
one route token total with basis, scope and source. Unknown breakdowns remain
null; call-count coverage remains separate. The source ledger retains the
original recorded values. The existing provider-model usage band now checks the
rendered direct and guided lines against these boundaries. This verified source
repair is independent of model answer quality.

The live capability and wall descriptions in `single-call-parity-goal.form` and
`rent-to-zero-goal.form` now match the inspected source: local generation runs
inside `fkwu`, and an optional offered provider is invoked by Form with attempts
counted. The dated external-server experiments remain historical evidence.
No completion criterion was relaxed and no external runtime dependency added.

## Native cost, coordination and failed setup

| Native observation | Scoped | Attributed |
| --- | ---: | ---: |
| Prompt IDs | 2,871 | 2,445 |
| Generated IDs | 955 | 952 |
| Execution ms | 333,939 | 285,795 |
| Complete / released | 1 / 1 | 1 / 1 |
| Provider / model-server calls | 0 / 0 | 0 / 0 |

The [result](artifacts/2026-09-20-attributed-native-comparison-result.json)
records valid JSON and a nonempty answer. Replay verified the packet and added
**0** native calls. The [eight-attempt audit](artifacts/2026-09-20-attributed-native-comparison-audit.json)
includes unsuccessful answers: **24,272 prompt IDs**, **8,468 generated IDs**,
**2,865,933 ms**, provider **0**, evaluation training **0**. Earlier September 19
generation, preparation, compilation, retention, learning and coordination are
outside those execution windows.

The [previous completed coordinator turn](artifacts/2026-09-20-attributed-native-comparison-coordinator-cost.json)
used **26 calls**, **2,370,012 input tokens** (**2,291,968 cached**, **78,044
uncached**), **21,345 output** including **9,342 reasoning**, and **28,903
unattributed**: **2,420,260 total**. This open turn and separate provider calls
are excluded. Unattributed usage is counted rather than silently treated as zero.
The coordinator's repeated context remains the largest rented cost.

Initial preflight of `form-cli-attributed-history-band.bml` exited **1** with
**2 errors**, **1 unresolved call**, `list_len -> UNOBSERVED`; running that
invalid cached band also exited **1**, `historical section missing`. Replacing
the invented helper with native `len` repaired the cause. Fresh preflight then
reported **0 errors, 0 warnings, 0 unresolved calls**, and the band returned **1**,
exit **0**. The scoped-usage, comparison-review and provider-model usage bands
also returned **1**, exit **0**; the provider-model band had a clean preflight.

The provider experiment runner initially exited **2** before admission:
`source-compile: mismatched form.bml delimiter: )`. Splitting its deeply nested
manifest expression into named native values repaired the source. No provider
process belonged to that compile failure. Compiler cache warnings used the
existing health flow to rebuild their affected images.

## Matched task through the Form-owned provider

The [provider answer](artifacts/2026-09-20-attributed-provider-comparison-answer.md)
was produced by one `codex exec` process admitted inside Form. The
[pair audit](artifacts/2026-09-20-attributed-provider-comparison-pair-audit.json)
verifies byte-identical source and identical task instruction against the native
run. Model and carrier envelopes differ. The provider did not see Qwen's answer.
The pre-admission rubric names the same evidence and judgment boundaries.

The provider includes output byte sizes, preserves the guided cutoff through
call 21 with closing and landing excluded, and names the unobserved auxiliary
call count alongside the broader token coverage. It attributes historical
judgments, labels one vitality inference as its own, and states that the session
identity repair was proposed. Its recommendations are more conditional and
keep the original direct/guided comparison central. These are stronger observed
responses to the remaining native gaps. Its final instruction to repair session
isolation still extends a historical finding into current advice without fresh
runtime verification. Its sovereignty comparison also deserves the person's
reading; source citation alone does not settle the interpretation.

The [provider result](artifacts/2026-09-20-attributed-provider-comparison-result.json)
records **1** completed provider turn, **17,355 input tokens** (**10,624 cached**,
**6,731 uncached**), **1,039 output** including **50 reasoning**: **18,394 total**.
Provider elapsed **28,189 ms**, native driver **28,408 ms**, released **1**,
message-only items **1**, local generations **0**, training **0**. Its report
checks establish shape and the caller's source assertions, not semantic quality.
Replay added **0** provider processes and kept the same usage event.

The three retained comparison provider runs now total **59,816 tokens**:
**55,949 input**, **31,872 cached input**, **24,077 uncached input**, **3,867 output**.
The audit checks that their usage events are distinct. Earlier sessions and
coordinating turns remain separate; unsuccessful native attempts remain in the
eight-run native audit. This matched pair makes quality, response latency and
rent visible together. It does not establish overall session parity, stable
quality across enquiries, or human resonance. A human comparison of these two
answers was requested and remains pending.

| Latest matched task | Native Qwen | Provider inside Form |
| --- | ---: | ---: |
| Returned answer bytes | 3,842 | 4,528 |
| Call execution ms | 285,795 | 28,408 |
| Provider tokens | 0 | 18,394 |

Byte counts measure returned text; execution windows include each route's own
admission and completion. Neither row includes coordinator work. Different
tokenizers make native IDs and provider token counts distinct quantities.

All landing checks returned **8191**, exit **0**. The runtime source remained
unchanged, so sibling proof runtimes were not required.

## Continuity

Panel: **0 orphans**, **11/12 lanes unobserved**, no standing hearth. Bounded
glass first frame **28 ms**. Native guide: **0 Python implementations, 2
execution candidates, 0 unread files**. The prior procedural learner completed
round **92**, pending **0**, serving generation **5** unchanged. The verified
source-transformation teaching was retained under
`attributed-history-source-boundaries-2026-09-20`; its worker completed round
**93**, pending **0**, serving generation **5** unchanged.
This is the separate Llama learner, not a Qwen weight update. Evaluated answers
remain excluded from training.
