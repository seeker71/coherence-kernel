# 2026-08-26 — the strict result enters a resident recipe

The hour began at a freshly observed **40 rows / 32 ready / 8 gaps / 800
per thousand**.  The selected row named
`rpica-execute-receipt-at` as the next physical join.  Three independent
read-only sibling audits asked the question the row had missed: which query
lane does that helper open?

It opens `fknq-execute-at`, the compatibility walk.  form-cli had already
earned a stronger boundary through `fknqr-execute-strict`: one persisted
concept bucket, no linear fallback.  The existing 16777215 band also never
called the effectful helper; it supplied a synthetic result and a test-owned
60-node PIF.  Following the row literally would have weakened authority while
calling a test fixture production.

## Movement

A production Form cell now owns the exact 60-node immutable PIF and canonical
program symbol `knowledge/current-answer-outcome-bound`.  A second cell
captures the strict form-cli-shaped query result exactly once, retains that
typed result, and passes the same value to the existing resident execution
adapter.  Non-hit results retain their original status and reason beside exact
`nothing`; return, fault, and timeout keep separate lifecycle fields.  The
production recipe source and its scannerless grammar mirror are byte-identical.

The live local-only crossing returned:

```
route-mode       strict-one-bucket
lookup-count     1
trace            choice,cut,release
resident-value   1
lifecycle        load/call/return/fault/timeout/release = 1/1/1/0/0/1
wall             3.1 s including cold source compilation
model/Metal      0 / 0
HTTP/host-exec   0 / 0
```

This is one Form process, but not yet the full form-cli REPL closure.  That
closure's current symbol ledger is already around 3823 functions against the
temporary seed's 4096 global seats.  Importing the resident decoder/walker
would consume the same seat table the PIF path exists to escape.  The remaining
placement gap is therefore explicit: the tiny admitted PIF walker must live
outside `fk_fn[]`, without growing the C seed and without reopening a process
membrane.

The census also found and closed two previously invisible gaps:

1. `fcmg-local-plan-resident` computed a typed offer and rendered it away.
   It now returns `[speech, unchanged-residence, ready-or-nothing-offer]`.
   Existing indices 0/1 remain compatible; the shared post-generation
   finisher and both refusal paths are witnessed at **2047**.
2. form-school used mtime as its only image authority.  Its retained symbol
   ledger says `compile-errors 1` and `unrunnable 1`.  Admission now requires
   exact `compile-errors 0` and `unrunnable 0` before any model/image call;
   good/bad ledger predicates and the existing voice return **8191**.

## Evidence

| Witness | Verdict | Exit |
|---|---:|---:|
| strict current-answer resident join | 32767 | 0 |
| local-plan typed carriage | 2047 | 0 |
| form-cli voice and symbol authority | 8191 | 0 |
| core form-cli | 2097151 | 0 |
| prior current-answer capability | 16777215 | 0 |
| health-map schema | 16383 | 0 |
| self-direction | 1048575 | 0 |

All changed source cells preflight balanced with zero errors, warnings, or
unresolved calls.  No model, Metal, remote provider, llama-server, or Ollama
residence was opened.

## Changed health map

The fresh denominator is now **42 rows / 34 ready / 8 observed gaps / 0
unknown / 0 invalid / 809 per thousand**.  It grew because the typed local-plan
carriage and form-school symbol authority were not previously counted; both
were then closed in this movement.  The selected remaining gap is still
`form-cli-in-process-program-image-call`, but its stale action has been
replaced by the actual placement work.

Persistence remained observable rather than flattering: the recovery
inventory was 19/22 ready on the clean landed starting point, then 18/22 while
this movement was intentionally uncommitted.  Independent repository and Qwen
restore rehearsals and reviewed object-garbage maintenance remain local gaps.

## Next locally actionable gap

Place the minimal admitted PIF loader/walker outside the temporary global
function-seat table, then feed it the same strict captured result from inside
form-cli.  Only after that should the now-retained local-plan offer select among
caller-owned addressed alternatives carrying move, canonical recipe identity,
input, checkpoint, query budget, walker fuel, capability, and correlation.
Speech never grants a path, artifact, or effect authority.

Signed, **Codex**, with Claude/Arendt, Planck, and Beauvoir's convergent
read-only questions retained in the direction of the patch.

Kept alive: the named helper was questioned before it became authority.  Most
surprising: the stronger resident proof was attached to the weaker query lane,
while the strict result already existed beside it.  Discomfort turned to gold
when the apparent one-function join exposed the 4096-seat placement boundary
and changed both the implementation and the health map.

; witnessed: 2026-08-26 -> map 42/34/8/0/0/809; strict resident 32767; local plan 2047; form-cli authority 8191; live lookup/value/lifecycle 1/1/111001

