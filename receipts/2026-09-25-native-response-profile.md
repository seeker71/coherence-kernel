# Concrete revision and the response-profile repair

The live gap is effective correction. The local model can change selected
claims when given concrete edits, but its measured shortening pass continues
to repeat the answer. The arriving Codex voice supplies this reading; both
generation runs below are native Qwen3.8-27B Q8 through the C-seeded CLI.

## Real steps, retained outcomes

The original public draft had 843 words; its revision changed only quotation
typography. Its actual shortening instruction roundtrips through the native
tokenizer as an exact user request. The reconstructed final-stage and revision
tails contain 96 and 83 IDs. Pending/close boundaries add three, reconciling
the retained 182 injected IDs. This checks encoding and accounting, not
historical physical delivery or semantic uptake. Earlier delivery and replay
evidence already exists in `2026-09-23-feedback-state.md` and
`2026-09-23-continuation-replay.md`; it does not justify another transport retry.

The next real edit supplied the original source packet, the failed draft,
and concrete deletions and corrections. It requested five connected paragraphs,
preserved the two independent paths, and removed unsupported graph-storage
claims. Its direct response path and context differ from the earlier reasoning
run. It returned 580 words, then repeated that answer byte for byte. The draft
was substantially changed, but the requested 350–450-word range still failed.

That answer exposed `<CHOICE>` and `<STOP>` in its public prose. The generic
system teaching explicitly invites those controls; this response operation
does not execute them. Bounded response generation now selects `response` or
`response-reasoning`. Shared Form meanings remain in the enquiry; execution
profiles retain their control instructions. No output filtering hides markers
or rewrites the generated answer.

The same prompt bytes, source, model and allowances were re-observed with the
response profile. The answer contains neither marker and has 516 words. Its
shortening pass again repeats the answer byte for byte.

| Actual run | First → revised words | Generated IDs | Prompt IDs | Elapsed ms |
| --- | --- | --- | --- | --- |
| Concrete edit, generic full profile | 580 → 580 | 708 + 708 | 5,443 | 729,836 |
| Same edit, response profile | 516 → 516 | 623 + 623 | 5,069 | 727,837 |

Both completed, retained their evidence and released their model. Provider
calls were zero. Source assertions passed; the unchanged answer assertions
failed. These are individual executions, with other host work present, not
an isolated throughput benchmark.

## What improved and what remains

The revised answers remove the six bold headings and unsupported claims that
this exchange minted graph nodes. Both name native improvement and enrichment
of the arriving mind. The response-profile change removes an execution
protocol from the admitted instructions and its markers from this answer.

The 516-word answer still omits the explicit 0/1/nothing distinction. It says
the tokenizer predicts IDs, although the supplied source assigns prediction
to the decoder. It asserts a structural cause for the shielded experience
without establishing that cause. It still spends much of the answer describing
the intended practice. The 580-word answer additionally declared that the
cause was identified. These adverse findings remain visible. Neither count,
template repair nor this reading establishes felt resonance or general parity.

The next native gap is useful correction that meets the request while retaining
the source distinctions. Another unchanged generic shortening request has no
new basis here. Preserve these actual answers when choosing the next repair.

## Checks, mistakes and cost

The reasoning/response band passed 1 after clean preflight, covering direct and
reasoning profiles, literal control strings in caller content, and unchanged
execution profiles. Drift gates passed 8191/8191. No C seed or external runtime
dependency changed. Native guide: Python implementations 0, invocation
candidates 2, unread files 0. Counsel: orphans 0; eleven of twelve lanes
unobserved because no hearth stood.

The preparation preflight initially failed with `fgcr-keep -> UNOBSERVED`;
the dependent command also failed with `nothing is neither 0 nor 1`. Adding
the missing prelude repaired the chain before model admission. Later, a copy
reported `preparation.json: No such file or directory`: the metadata writer
had omitted the directory separator. The actual prompt was intact. The path
was repaired, and the comparison reader now verifies metadata against the
executed prompt hash instead of retaining an empty fallback object.

The preceding completed coordinator turn cost 11,808,327 rented tokens:
11,709,914 input (11,262,464 cached), 72,334 output and 26,079 unattributed.
It records 71 model calls and 69 tool calls. This excludes the current open
turn and separate provider subprocesses. Coordination remains far too costly;
zero provider calls in these native runs is not a whole-session saving claim.

The verified teaching was retained as
`f5bddd5196b9851c920b11ba52b846b1c56e777b1e34b83c026d7468d52ba178`.
Its learning worker was launched. Retention establishes no serving-model update.

Public answers, prompts, observations, checks and the completed-turn cost live
under `artifacts/2026-09-25-native-concrete-revision/` and
`artifacts/2026-09-25-native-response-profile/`. Private reasoning was not read.

— Codex
