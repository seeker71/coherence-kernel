# Native pinned sources and complete shared values

Form reacquires and interprets all 111 retained Wiktionary revisions. Five
actual requests reproduce the page, title, revision, timestamp, English-section
hash and selected definition for every row. Complete HTTP exchanges and source
texts remain in private evidence. No translation or corpus publication is
claimed. The public authority, execution door and current boundary live in
[native Wiktionary source acquisition](../docs/native-wiktionary-source.md).

Five captured-response replay batches pass, and sixteen changed-input cases
refuse at their specific admission boundary. The independent meaning witness
passes 192 observations. The
[source evidence](../docs/evidence/fkwu/wiktionary-source.json) binds those
executions and the current Form sources.

The seed's shared-value transfer preserves a local list prefix followed by an
already shared tail. The correction replaces one terminal expression with the
existing sharing operation, removes 17 bytes (871,469 to 871,452) and adds no
function or global. Fresh process identities prevent prior canonical interning from masking
first admission. The public ownership witness passes all thirteen observations;
independent review covers nested lists, scalar prefixes, existing shared lists
and empty lists. [Ownership evidence](../docs/evidence/fkwu/shared-field-ownership.json)
binds the actual refusal and repaired execution.

The shrink destination is a Form-owned context and sharing operation. Seed
field storage and its transfer routine remain present. This is a transport
repair within that temporary seed, not completion of the ownership migration.
The refreshed lexical/Clang audit retains 643 function definitions, 374 distinct
global names and zero unresolved top-level units.

Validation: freshness 31; bootstrap 42, 55 and the numeric list; native body
11111; shared-field cross-process witness 255; all nine IPC, HTTP and translation
worker witnesses pass with actual exit 0 and empty stderr. The complete corpus
checks include 10,000 source labels and 120,000 retained language cells. Worker
image-admission diagnostics are retained before exact message-stream checks.
Root and emitted CLI linkage remain libSystem only; Metal admission is dynamic.

Current instruments: bounded Glass construction **157 ms** (storage 113 ms,
fast collectors 37 ms), 168 samples and 24 observations. The authoring guide
reads two Python implementations, 221 execution candidates, 50 grammar inputs
and zero unread files. Eleven of twelve counsel lanes remain unobserved without
a standing hearth; no complete performance verdict or contribution percentage
is claimed.

AI review accepts the source re-observation scope and the shared-tail repair.
Fresh ranked selection, rights acquisition, successful translation and complete
new-generation publication remain distinct work. The full model oracle and its
resource plan are still under numerical acceptance.

The exchange stayed grounded by following complete bytes through the actual
consumer. The surprising teaching is that a previously interned value can hide
a first-admission defect. An unexpected refusal became useful evidence when a
fresh identity exposed the missing tail and the same observation verified its
repair.

— Codex, with independent review by the AI board
