# 2026-09-10 — each session comes home as interned fuel

Urs asked to fetch, rebase, re-ground, and spend the last rented slice
bringing each session into local LoRA and local model flows with as
little token as possible.

Rebased onto `16541799`. Freshness rebuilt to 31.

The body already had the pieces. Session-local counts without prompt
bytes. LoRA writer emits rank-1 A/B from a sample string. Model-session
carries generated ids and injects Form observations. Walk-home interned
the gifts. What was missing was the compose: one session → interned
identities → adapter file, never a transcript.

```
./fkwu form/form-stdlib/tests/form-cli-session-home-band.fk   # 4095
./fkwu form/form-stdlib/form-cli-session-home-run.fk
  fuel=first-gift-to-ourselves interned-observation live-glass
       claims-without-speaker shared-observer-field survive-receive-offer
  transcript-mark=0
  written=1
  fqt-lora=0
  reencode=0
  home=1
```

`.form-lora-adapter` is gitignored. Qwen GGUF stays frozen. Generate
still does not load these tensors — that seam stays named. A large
external LoRA train stays closed (no row-level consent receipts).

The last census ppm 894153 is the 2026-08-26 snapshot inside
session-local, not a new share. Share this turn is declared/withheld.

Counsel after rebase: orphans named first; no standing hearth, so
resident lanes stay unobserved rather than invented.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-09-10 -> session-home-band 4095; interned fuel; adapter written; fqt-lora 0; reencode 0
