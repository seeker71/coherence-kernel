# 2026-09-03 — the gate speaks Form (owntongue)

The same hour the op-addition recipe learned its fourth mirror
(receipts/2026-09-03-the-recipe-is-a-mirror-too.md), the heal was corrected at
its root: the recipe I had written sent the next hand to a Python gate. "No
python, no bash, no non-form!" — and the body had already said so itself, in
validate.sh phase 0's own words: the Form-owned core is shell/Python free,
auxiliaries stay named. A gate that guards the body's native surface while
speaking a rented tongue re-creates the dependence it exists to end.

The gate is now Form-bodied:

- gate/native-surface-gate.bml — BML altitude, gate/ home, `nvs-` names
  (the first prefix tried, `nsg-`, was already worn by
  neutral-symbol-grammar-band.fk; the frequency reflex caught it before a
  collision landed). Same judgments as the python it replaces: flt-ops rows
  parsed from form-flatten.fk, arm-slots bounds, one-tag-one-arity with
  same-arity aliases counted, nullary/unary/ternary arm coverage against
  fkc-flat, quad and C-walker warning lanes. Runs from repo root:
  `./fkwu gate/native-surface-gate.bml`, answers native 1/0 after its named
  verdict line — the structural gate's own explicit-result idiom.
- form/validate.sh phase 0 rewired through that idiom (output echoed, last
  line must be `1`, refusal exits nonzero); the python invocation and
  form/scripts/validate_fkwu_native_surface.py itself are gone.
- All four recipe texts (gen-source-walker-table.fk, gen-source-walker.fk,
  native-op-manifest.fk, and the emitted fkwu-optable.h comment — regenerated
  again through its own two-call lane) now name the Form gate.
- Corpus rows: mapmirror's close healed to name the Form door; owntongue
  takes the correction home. Both re-seated at reunion the same night —
  mapmirror to 1247 (main's tailkeep took 1246), owntongue to 1248 — the
  row-719 anastomosis pattern, twice in one night, every row kept.

## Witnesses

- first run of the Form gate against the live tree: exact number equality
  with the python — `OK (152 flt-ops rows, max_tag=255, arm_slots=256,
  aliases=19, warnings=0)`, native 1
- teeth on synthetic truth: the 197 arm hidden -> `error: method_define:
  ternary tag 197 missing fkc-tri2 / explicit fkc-flat arm`, FAIL, native 0;
  restore diff empty
- regen: 19612 / 6579; fkwu-optable.h diff = one comment line
- carrier parse witnessed inline: last-line extraction of the gate's output
  yields `1`; `bash -n form/validate.sh` clean
- no residual validate_fkwu_native_surface references in validate.sh or
  form/scripts/; the two comment mentions that remain are history explaining
  present rows
- learn/homecoming-distillation-corpus.fk evaluates 0 with rows 1247+1248

Most surprising: the refusal I needed was already written into the file I was
editing. validate.sh phase 0 opens by declaring the tree cannot claim a
shell/Python-free Form-owned body while an unclassified script sits outside
its boundaries — the covenant was standing one screen above the python call I
had just blessed into four recipe texts. The correction did not add a new
principle; it made me read the one the body already held.

Discomfort turned to gold at the die-door probe. I wanted the gate to exit
nonzero and went hunting for a hard-stop: `(head (list))` answered `[]` rc0,
`(str_byte_at "" 0)` answered -1 rc0 — the dramatic door I imagined does not
exist in this body. Sitting with that instead of forcing one revealed the
body's actual idiom one function above: the structural gate answers an
explicit native 1/0 and lets the named process carrier own the exit code.
The gate got honest teeth by borrowing the body's own hinge, not by adding a
foreign slam. And the mutation test paid immediately: the first red line the
new gate ever printed named method_define 197 — the exact wound the whole
movement began with.
