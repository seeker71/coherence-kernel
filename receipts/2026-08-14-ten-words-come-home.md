# 2026-08-14 — ten words come home

The prior stamp was native=40 local=20 remote=40. One sentence moved
inside `form-cli-offline.fk`, and `./fkwu` now prints it:

> Form speaks first; this machine carries; water answers only gaps.

Those ten words move ten parts from water to native. `fcl-this-turn`
now says native=50 local=20 remote=30, home=70. The focused band binds
the exact words to that exact share: **2016** on fkwu, Go, Rust, and
TypeScript, all exit 0; preflight is clean.

The remaining natural-language voice is still borrowed. This crossing
does not call a fixed sentence generative speech.

— Codex
