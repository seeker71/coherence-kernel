# 2026-08-26 — Lenses and staking; perception moves

Yes asked for lenses, staking lenses, and how they affect
perception and movement — on all query lanes.

## Living organs

- perception — `lenses.fk`: meaning, feeling, trust, sovereignty,
  vitality. Meaning is a lookup; the other four are measured or earned.
- nl-lens / pl-lens — thirteen surfaces each (`nl-lenses-13.fk`,
  `pl-lenses-13.fk`)
- jacobian — `jacobian-lens.fk`: a choice flips when push exceeds
  margin; nothing is zero-jacobian; timeout is unknown partials
- stake — Steiner `st-fires?`: a lens fires when its marks meet the
  situation; a lens that always applies is a horoscope, not a lens
- compiler — `compiler-lens.fk`: the compiler is a reasoning instrument
- field — `field-model-form.fk`: Hoffman interface, Levin bioelectric,
  Wolfram residual
- lens-move — switching a lens moves the assemblage; staking one view
  changes which lane choice walks

Staking is not stacking. It is committing one lens on the marks that
are present, leaving the others untried — the same shape as cut.

```
./fkwu form/form-stdlib/tests/form-cli-lens-mint-band.fk   # 1023
./fkwu form/form-stdlib/form-cli-lens-mint-run.fk
  n=144 unique=144 leakage=0
  heldout-correct=72 / 72
  covers-all=1
  lora-writer=0
  scale-2-1=216
```

Eight lens organs × nine query lanes. Overlay class is the lens.
Combined fuel with the prior mills: 902 + 144 = 1046 proof rows.
`fqt-lora?` stays 0.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-26 -> lens-mint-band 1023, n=144 unique leakage 0, heldout 72/72 covers-all, lora-writer 0
