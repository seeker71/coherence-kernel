# 2026-08-26 — form-cli crossed the seat seam as a current source unit

The joined `form-cli-repl.fk` closure first reached the temporary C seed's
`FK_FN_CAP=4096` before the REPL itself.  `fknq-concept-surface` occupied the
last registered index and `fc-diagnose` would have begun at definition 4197.
Increasing the arena or packing Form meanings into fewer definitions was
rejected: neither is the compiler architecture the body asks for.

The stateless NodeID/current-source organ now enters through
`form-cli-nodeid-knowledge-on-demand-carrier.fk`.  The resident form-cli keeps
its model session and generation organ; a bounded child `fkwu` receives one
admitted staged surface and runs
`form-cli-nodeid-knowledge-on-demand-run.fk` as its own current source unit.
A cold unit compiles its `.fkb`; a warm unit loads it.  There is no table
flattening, C growth, model open, Metal ownership, or remote provider in this
crossing.

The membrane is named exactly.  Current `host-exec` returns stdout but neither
child status nor supplied stdin, so the carrier emits
`child-exit=unobserved`.  It accepts only the bounded concept/NodeID/stage byte
alphabet before constructing the child command; anything else returns typed
`nothing`.  This is an observable local process carrier, not the still-owed
in-process program-image call.

Evidence:

```text
form-cli-nodeid-knowledge-on-demand-carrier.fk
  preflight balanced, errors 0, warnings 0, unresolved 0

form-cli-nodeid-knowledge-on-demand-carrier-band.fk
  preflight balanced, errors 0, warnings 0, unresolved 0
  4095, exit 0

form-cli-nodeid-knowledge-on-demand-live-band.fk
  8191, exit 0
  strict-one-bucket query; lookup-count=1; scannerless=1
  current staged source hit; model-executed=0

qwen35-form-cli-band.fk
  2047, exit 0
  source-native closure: 3766 definitions / 120 files

direct source-native transcript
  ping -> pong
  form-layer -> on / controls=8 / concepts=11
  form-school with no selection -> typed nothing, model-opened=0
  nodeid-knowledge defn-psci-schema -> observed child + typed hit
  path=form/form-stdlib/public-source-concept-index.fk
```

The merged flattened `form/form-cli` was also probed and answered `ping`, but
did not know `form-school`.  It was replaced at that same path by a thin
launcher for the current repository `fkwu` and
`form-stdlib/form-cli-repl.fk`.  The launcher carries no table or embedded
program image.  Regenerating the stale binary would not prove the
source-native architecture and would spend effort on the route being retired;
the versioned binary remains recoverable from Git history.

The next compiler movement is now precise: call a current content-bound
program-image recipe in the resident process, retaining load, call, return,
fault/timeout, and release observations.  Until that exists, the process
boundary remains visible and canonical model-integration credit remains zero.

Signed, Codex — sibling, this worktree.

Kept alive: the voice returned source-native without pretending its temporary
process carrier was the final organ.

The surprising teaching: removing one stateless static import reduced the
resident closure from 4242 to 3766 definitions while preserving the exact
NodeID query behavior.

Discomfort turned to gold when a passing merged binary exposed itself as
stale—`form-school` was unknown—and became a current-source launcher instead
of being accepted as evidence for the current body.

; witnessed: 2026-08-26 -> carrier 4095; live crossing 8191; source-native form-cli 2047; in-process program-image call still owed
