# Rebase converged the live Form homecoming

Date: 2026-08-25
Witness: Codex
State: observed for the pure/source and previously-live carrier surfaces named below; physical axioms one-family crossing remains pending

The local reasoning homecoming was rebased onto the current `origin/main`
without choosing between two useful native meanings.  The Metal registry now
contains 47 pipelines: the cooperative GQA path remains at 39, linear span
occupies 40 through 44, and the two folded-recurrence alternatives occupy 45
and 46.  The dense registry band returned `2147483647`, exit 0.  No C seed,
flattening path, or operation table grew.

The Qwen teaching layer also retained the compatible meaning found across the
rebase: BML is executable source and authority; needed CPU, GPU, Metal, and MLX
work may be generated on demand or ahead of time; repeated hot and pure use may
earn cached crystallization; cold use or lost purity dissolves that
specialization back to revisable Form.  This is a lifecycle teaching, not a
gate on generation.  The complete teaching band returned `33554431`, exit 0.
An attempted change to the semantic centroid rows lost exactly the heldout
correct-count bit (`524288`), so those vectors were restored and the teaching
was kept in the overlay and pair glossary where the complete band returned.

Fresh post-rebase witnesses:

- direct source ground `42`, recursion `55`, binary freshness `31`, numeric
  list `[1, 2.5, [3, 4]]`, and native-vs-rented `11111`;
- live BMF prompt cursor `2047`, qtf2 `65535`, artifact seal reader `63`, full
  qwen/form-cli `2047`, and form-cli `1048575`;
- generation source attestation `PASS`;
- current-source `16777215`, heed report `2097151`, resident loop `16777215`,
  resident school `127`, native control `65535`, and replacement `262143`;
- qlslc layout `32767` and category coherence `7`.

The current qwen/form-cli `.fkb` v5 image directly reports 4087 registered
functions and a sealed image.  Re-running its source band returned `2047`, exit
0, leaving nine honest slots below the unchanged 4096 capacity.  The hourly
health surface now carries that observed number rather than its pre-rebase
4081 witness.

The live carrier observation remains the earlier exact `LOCAL FORM ALIVE`
crossing: 393/393 streamed prompt token IDs, `prompt_cursor=bmf-qtf2-live-v1`,
128/128 released states, zero pending/in-flight buffers, and exit 0.  This
receipt does not turn the pure axioms one-family crossing into a physical
claim; that crossing remains the next observed gap.

I kept this movement alive by letting the rebase preserve both useful native
paths and then asking the complete bands to judge the combined body.  The most
surprising teaching was that adding only a few registry meanings consumed six
of the fifteen resident function slots.  Discomfort turned to gold when the
centroid experiment lost one precise heldout bit: restoring the classifier and
placing the new meaning in the teaching surface made the boundary visible.

— Codex
