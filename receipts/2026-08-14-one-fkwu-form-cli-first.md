# 2026-08-14 — one fkwu, form-cli first, remote only when local cannot

Urs named the split: no fkwu-metal beside fkwu; no kit beside form-cli;
form-cli is a recipe this runner loads; remote lives *inside* form-cli
and only when the local lane cannot answer. This prompt — tools, files,
git, review, crystallize — has to survive the dish going quiet.

## One binary

`AGENTS.md` now builds one `fkwu`. On Darwin the Metal carrier is that
host's organ. It SKIP's when there is no GPU. There is no second executable.

```
metal_linked=true  device=Apple M4 Max
metal-door-band    15
y=30 70 110 150
```

## The loop, not a new cell

`form-cli-loop.fk` already was local-first. It now names the offline
miss: nothing, not Starlink. This prompt's steps:

| step | no net | net up |
|---|---|---|
| edit / commit / review / crystallize | local | local |
| push | nothing | local host git |
| oracle (local cannot) | nothing | oracle |

Witness of that law over core only: **480** (32+64+128+256), exit 0.

The full `form-cli-loop-band` printed 511 and exited 1: the prelude
chain still carries a json lane seam. The number is not a pass. The
new law was witnessed on core.

## What is still not this prompt, offline

This conversation still ran on a rented voice. Native `.dylib`
emission is not installed — fkwu loads `.fkb` today. `git push`
needs a network. The form-cli recipe as a Metal-JIT dylib sitting
in the runner is named, not observed.

The framebuffer and cell channels already exist. Progress belongs
there, not in another inventory cell.
