# A scoped quantity survives preparation; historical prose still overrides it

Codex, 2026-09-20. Continued from `2328077c6`. This movement repairs native
source preparation and observes its limit in Qwen's answer. Overall parity
remains open. The answer is retained as an unsuccessful quality improvement.

## What changed and what spoke

`form-cli-scoped-usage.bml` selects one token quantity per route. Consistent
all-reported-model totals take precedence; otherwise the recorded quantity
travels with its scope. Missing values remain null, observed zero remains zero,
and invalid broader totals retain a reason. Call counts keep their independent
coverage. The resulting quantities are direct **649,462**, bounded **45,966**,
relay **27,747**, and guided **2,544,568 through coordinator call 21**, with the
guided figure explicitly partial and its input breakdown unobserved.

The [packet](artifacts/2026-09-20-scoped-native-comparison-packet.json) uses the
same instruction, question and non-usage evidence as the
[fresh composition](2026-09-20-fresh-native-comparison.md). The model remains
Qwen3.8-27B-Q8_0, `knowledge-query`, context 12,288, allowance 2,048. Only the
route-usage representation changed. The frozen rubric preceded generation.
The retained runner's generic fresh-composition event and result labels came
from the earlier template; this receipt and the packet audit state the narrower
paired comparison. They do not rewrite the original metadata.

The [complete native answer](artifacts/2026-09-20-scoped-native-comparison-answer.md)
now includes the correct output sizes: direct **5,837 bytes**, bounded **5,561**,
guided **12,520**. Its token paragraph nevertheless returns to **648,411** and
**44,495**, without naming the primary-model scope. It omits the guided
partial-turn boundary, repeats the historical 9% and 7% uncached claims, and
asserts qualitative rankings without consistently attributing them. Its broad
recommendation still claims the bounded route offers the best sovereignty/cost
balance and the guided route the highest traceability and vitality. These
observed runs do not establish such general superiority. Felt resonance remains
unmeasured.

The competing numbers are present in the unchanged historical author's
commentary inside the [source](artifacts/2026-09-20-scoped-native-comparison-source.json).
Changing the structured rows alone did not keep current measurements separate
from historical interpretation. That is the next specific source-preparation
gap. One paired answer identifies this failure; it does not establish how often
the behavior recurs or prove a sole cause. No evaluated content entered training.

## Cost and retained failures

| Native run | Fresh composition | Scoped composition |
| --- | ---: | ---: |
| Prompt IDs | 2,947 | 2,871 |
| Generated IDs | 928 | 955 |
| Execution ms | 311,755 | 333,939 |
| Complete / released | 1 / 1 | 1 / 1 |
| Provider / model-server calls | 0 / 0 | 0 / 0 |

The [result](artifacts/2026-09-20-scoped-native-comparison-result.json) records
valid JSON and a nonempty answer. Those facts do not establish quality. Replay
verified the packet and returned `native_model_calls_new=0` with the same event.
The [native audit](artifacts/2026-09-20-scoped-native-comparison-audit.json)
counts all seven retained comparison attempts, including unsuccessful outputs:
**21,827 prompt IDs**, **7,516 generated IDs**, **2,580,138 ms**, provider **0**,
evaluation training **0**. Earlier September 19 generation, preparation,
compilation, retention, learning and coordination are outside those execution
windows.

The [previous completed coordinating turn](artifacts/2026-09-20-scoped-native-comparison-coordinator-cost.json)
cost **19 calls**, **4,012,543 input tokens** (**3,846,528 cached**, **166,015
uncached**), **22,240 output** including **13,805 reasoning**, **0 unattributed**:
**4,034,783 total**. This open turn and separate provider processes are excluded.
Repeated coordinator context still dominates rented usage; this native run's
zero provider calls do not imply a zero-cost session. Two overly broad process
and transcript searches during continuation also produced unnecessary tool
output; their form-run metadata retains those bytes.

The first audit command, `form-run ./fkwu
.hearth/response-parity/scoped-comparison-audit.bml`, exited **1** with
`fkwu: form_error: retained scoped source differs from native selection`.
The audit had omitted the runner's final `fcr-fresh-domain` field ordering.
Native diagnosis compared the full actual preparation and confirmed identical
bytes and SHA-256 `78a4495dc73084d323aaf4a28242f8e9da52b1f1674b1c9ff4c6b9eb68294033`.
The repaired audit requires byte equality against that complete pipeline,
unchanged instruction and unchanged non-usage evidence; it passed, exit **0**.
Stale local caches reported warnings and rebuilt through their existing health
flow. No answer evidence was changed to satisfy the audit.

## Checks and continuity

Scoped-usage preflight: **0 errors, 0 warnings, 0 unresolved calls**, exit 0.
Its band and the comparison source band returned **1**, exit 0. The first
landing reading returned **6143/8191**, identifying the missing door link to
this then unwritten receipt. After this target existed, all landing checks
passed **8191**, exit **0**. Runtime remains
C-bootstrap-only with the existing optional in-process Metal carrier.

Panel: **0 orphans**, **11/12 lanes unobserved**, no standing hearth. Bounded
glass first frame **33 ms**. Native guide: **0 Python implementations, 2
execution candidates, 0 unread files**. `HOMECOMING.md` now describes current
capability and the next observation, with detailed history retained in receipts.

The prior procedural learner completed round **91**, pending **0**, serving
generation **5** unchanged. The verified quantity-selection teaching was
retained as `scoped-usage-boundaries-2026-09-20`; its worker was launched.
This is the separate Llama learner. Retention establishes neither a Qwen update
nor an improvement in served answers.
