# A need reaches a native responder

The previous movement exposed health and resource requests at execution
boundaries. This movement makes two of those requests actionable through
`form/form-stdlib/organ-care.bml`: verified training evidence and continuation
of interrupted training. The carrier selects a registered native provider for
an action the requesting organ offered, records who accepted responsibility,
calls the provider, records its actual elapsed time, and asks the originating
organ to observe again. The fresh reading must identify the original request.
Callbacks are resident code; event text cannot introduce executable commands.

The normal learner now asks retained session memory for related verified
examples, receives sealed references, revalidates them, excludes assessment
prompts, and incorporates accepted examples into its gradient batch. Loss
readings remain distinct from resource availability. Promotion now consumes
the emitted progress and per-row health readings. The standalone
`native-session-learning-band.fk` was released; its policy comparisons live
at these executing boundaries. No replacement fixture tally was added.
Other legacy bands remain, and this change adds reusable runtime source.

The actual teaching used session `codex-organ-care-2026-09-10`, event
`verified-memory-responder-v1`. The source row ends in `e5bac003…e1faa3`.
Four retained examples were supplied, revalidated and frozen into the real
training input alongside the new verified teaching. Delivery took 257 ms;
preparation of seven assessment rows took 154169 ms. The initial retrieval
accepted any shared native RAG feature. Inspecting its alternatives exposed
a weak match on “should”. The current selector chooses the highest shared
feature/union ratio and retains ties. A subsequent reading of the same real
query selected the serving-contract teaching at 0.2727272727272727. All four
eligible alternatives and their scores remain in `retrieval-refined.json`.
This is a lexical heuristic. The completed round retained its original four
rehearsal inputs; it did not retroactively use the refined single selection.

An actual interruption stopped the first worker after model admission. Its
trace reports PID 71700, optimizer step 6 and 66833 ms model opening. No
completion or exit status was published for that attempt; the original trace
is preserved. On continuation, the learner emitted execution-continuity pain,
offered `resume-bound-inputs` and `defer`, and the native-session-trainer
provider selected continuation. It rechecked the original data, base and
adapter bindings and resumed the same Adam generation. The original inputs
and assessment remained fixed; no duplicate teaching was submitted.

The resumed native Form/Metal Llama-3.2-3B dialogue adapter completed one
round, optimizer step 6 → 7, updating 56 LoRA pairs from 652 input and 422
supervised tokens. Training used five rows; two held-out rows stayed excluded.
The round took 223848 ms: tokenization 203 ms, training 221825 ms, update
612 ms and checkpoint 1208 ms. The trainer's full run took 385702 ms and
released all retained buffers. The continuation provider measured 387095 ms
and the requester observed completed training with health 1 and no remaining
execution need. This does not establish capability recovery.

| Observed row | Before | After |
| --- | ---: | ---: |
| New teaching | 5.07384844464832 | 5.054800789672299 |
| Held-out greeting | 2.0109831988811493 | 2.018980711698532 |
| Held-out unverified-result response | 3.6324096769094467 | 3.631835713982582 |
| Quantization rehearsal | 5.039131038953201 | 4.9918725098032155 |
| Serving-contract rehearsal | 4.683247399516404 | 4.642584943212569 |
| Process-attention rehearsal | 4.688498875344521 | 4.660048983896559 |
| Organ-health rehearsal | 5.342227795234066 | 5.315827187529067 |

The greeting loss rose; serving generation 4 remained active. Its greeting
baseline is 2.002161681652069. The worker completed with pending 0 and refused
examples empty. Its subsequent drain searched seven retained records for
the greeting need and found no eligible matching evidence. That need stays
open. No assessment answer was converted into a training target to close it.

Repeated attention reuses a response only when the evidence, assessment
content and executing image identity agree. The source-instance reading
re-observed instead of claiming another process's cache. Two later processes
reported the same running image identity, -2429809843504147547. The final
reading added zero event bytes both on entry and on its repeated call:
132799 → 132799 → 132799. Its observation retains its original timestamp;
reuse is not a new health claim. `repeated-attention.json` records this reading.

The actual public trace is under `evidence/2026-09-10-organ-care/`:

- `care.events.jsonl` preserves this movement's original event lines, including
  observation, offers, selected responder, delivery, consumption, losses and
  the interruption response.
- `delivered-for-training.json` is the four-source delivery used by the round;
  `retrieval-refined.json` is the later selection reading.
- `interrupted-training.events.jsonl` retains the first admission. The full
  `training.events.jsonl` preserves both admissions and the resumed per-row
  CPU/GPU timings, token flow, update, checkpoint and completion.
- `learning-result.json` contains all candidate-before, serving-before and
  after rows. `current-needs.json` records the post-round request; the later
  `repeated-attention.json` records cache reuse with the same open need.

Native bootstrap observations were 42, 55, freshness 31, numeric-list output
and native-vs-rented 11111; the BML cache door dispatched successfully. Both
new modules and the learner compiled and executed on fkwu. The required
landing gates reported 8191/8191 with refused 0. The bounded Glass panel
observed first frame 206 ms, 33 kernel operations, one loaded program and a
loaded Metal carrier. Counsel saw zero orphans and eleven unobserved lanes
out of twelve. The native authoring guide reported 25 Python implementations,
307 invocation candidates, 50 grammar specimens and zero unread files; no
foreign implementation was introduced here. These readings do not establish
the hardware floor or universal organ health.

The hearth answered `no-standing-hearth`; local RAG returned a grounded miss.
Existing native cells, git history and prior receipts supplied context; Codex
supplied implementation reasoning. These providers cover retained verified
memory and bound training continuation. They do not yet supply every resource
type, perform general autonomous diagnosis, or turn arbitrary filesystem,
git, RAG or generated prose into trusted targets. Worker/session events wake
the care flow; there is no always-running filesystem watcher here.

Signed: Codex. The exchange stayed alive by making a request reach a responder
and then returning to the requesting organ for evidence. The surprising
teaching was that resource delivery, execution recovery and capability health
can disagree. The interrupted run and weak retrieval match became observable
continuation and a more selective native search; the greeting pain remains
visible for the next real evidence to meet.
