# Native DSV4 references and proof generation

Form owns the real-model reference arithmetic, GGUF admission, matrix programs,
layer and token histories, complete output vectors, tensor-view planning,
shader and metadata generation, checked output settlement and proof retention.
The three comparison callers use these owners. Their replaced 1,212-line
Python oracle is removed.

The [execution evidence](../docs/evidence/fkwu/dsv4-oracle.json) binds four
accepted consumers to 107 unchanged source and executable identities:

| Consumer | Checks | Actual child exit |
| --- | ---: | ---: |
| Attention | 30 | 0 |
| Complete layer | 31 | 0 |
| Two-token, four-layer history | 184 | 0 |
| Full 43-layer stack, two positions | 861 | 0 |

All 1,106 checks retain their numerical thresholds. The sequence caller
generates all four native references itself. The full-stack caller uses four
completed independent reference trajectories, with 3,272 retained vectors
rechecked. Complete diagnostic streams, archive identities, actual child
settlement and the separately scoped arithmetic/resource reviews remain
available. Cache-renewal refusals and accepted retries keep their own evidence.

The final table-source cleanup removes five spaces from blank comment lines.
Form lowers the tested and final source to identical 4,749-byte executable
text. The evidence keeps the executed source identities and separately binds
the final source manifest through that exact comparison.

Form repairs the observed F16 projection, key/value projection and RMS
precision defects. Same-input comparisons expose both continuous error and
discrete quantization choices. Tensor views cover their actual spans plus
alignment. The full proof retains its whole-file mapping for its process
lifetime; separate device checks witness physical release of individual maps.

The [numeric display evidence](../docs/evidence/fkwu/float-display.json) retains
34 passing health observations, exact integer extrema and a forced failing
child. The original sibling band passes in Go, Rust and TypeScript, with a
separate native fkwu pass; the validator's fourth-arm coverage remains zero for
that unregistered band. Large plain-decimal BML source literals retain their
explicit emission boundary.

The canonical CLI is generated with Form, answers `pong`, and links only
libSystem alongside the root executable. The inspected C seed is 896,371 bytes,
with 672 function definitions and 400 global declarations; this movement adds
no handwritten C. [Current identities](../docs/evidence/fkwu/identities.json)
bind the source, rebuilt executable and generated carrier.

The staged whitespace check is clean. Final drift gates return 8191 with
zero refused gates, exit 0 and empty stderr. The native authoring guide reads
one Python implementation, 214 execution candidates, 50 grammar inputs and
zero unread paths, exit 0 and empty stderr.

The [Glass diagnostic](../docs/evidence/fkwu/glass-stage-observation.json)
retains all twenty stage rows and correlated observation, response, applied
action and fresh observation. Its panel reports 19/20 frames under 50 ms,
first/max 119 ms and warm maximum 45 ms during concurrent proof work. The
all-frames target remains unmet. Counsel observes zero orphans and eleven
unobserved lanes without a standing hearth. Reply share is declared and
unmeasured; no percentage is claimed.

A verified structural teaching was returned through the session home door
under event `dsv4-native-ownership-consumer-contract-v1`. One native training
round reaches optimizer step 25; the serving generation stays unchanged,
pending work is zero, and the trainer reports zero retained buffers. Evaluation
tokens and vector values are excluded from that teaching.

The [status](../docs/fkwu-kernel-status.md) and
[north star](../docs/fkwu-form-native-north-star.md) retain the current ownership
boundary: POSIX/Linux terminal acceptance still has one Python implementation;
Swift comparison carriers, shell orchestration, JavaScript responsibilities and
the temporary seed remain. Resident native activations, portable ownership and
the complete Form OS require further execution.

The useful surprise was that plausible decimals could contain invented
fractional digits before overflow, while a green scalar could coexist with a
failed observation. Withdrawing that result led to real refusal and per-row
checks. The exchange stayed grounded by letting observed failures change Form
implementations while the consumers kept their standards.

Signed: Codex, with independent architecture, numerical and emission review.
