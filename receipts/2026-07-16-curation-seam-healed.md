# The curation seam, healed — and the law that was guarding nothing

> **CONTINUATION 2026-07-16, 22:40 WITA — read this first, then the original below it unchanged.**
> Everything below was true when written and is kept exactly. Two of its conclusions did not survive
> the next hour, and both failed the same way the receipt's own subject failed: **I had named work as
> "standing" for reasons I never tested.**
>
> - *"10 directory-link claims — an instrument floor, deliberately not fixed by the agent whose own
>   number it would improve."* The conflict of interest was real; the conclusion was not. I never
>   checked the body's **precedent**, and it was one grep away: `door-link-health.fk`'s own header
>   records that its author healed **their own** 4 directory links by pointing at files rather than
>   declaring directories valid. The body's convention is *link a file, not a directory*. Healing them
>   follows precedent and touches no instrument. Done: 10 claims, 3 files, field 7041032 → 7031032.
> - *"27 non-concept claims needing per-case judgement."* Also untested. The rule — *name a companion
>   you cannot reach; never claim a path to it* — never cared **why** the target is absent. A link that
>   lands nowhere is a false promise whether the page is OUT forever or pending forever. The per-case
>   judgement I was protecting was a judgement the rule doesn't need.
>
> What the second pass actually needed was not judgement but a **guard**: `hcs-known?`, which strips
> only when nothing by that name exists anywhere in the body (a 4667-file walk). If a page by that name
> *does* exist, the claim is repointable and a real edge is recoverable — the cell now leaves it for a
> hand. **Repoint before you strip.** That guard is what let the narrow rule (lc-*.md only) become
> general and safe; the narrowness had been standing in for the guard. Self-check stayed **11111**, with
> the 4th digit repurposed to prove exactly this: a broken-but-repointable link to `bootstrap/ground.fk`
> claimed at a wrong path **survives**.
>
> | | first pass | now |
> | --- | --- | --- |
> | field | 7041032 | **4033** |
> | broken path-claims | 41 | **4** |
> | orphan pages | 7 | **0** |
> | `blg` self-check | 63 | **63** |
>
> **The body's remaining brokenness is now exactly its own law**: all 4 surviving broken claims are
> inside `receipts/`, which the cell refuses before reading. Orphans reached 0 by writing the door
> `docs/coherence-substrate/README.md` (four orphans: a tissue whose only index describes a *different*
> tissue) and by naming the **vision-kb remnant** — `lc-satsang-circle` and the 2026-05-20 transmission,
> the last two residents of the field that left, orphaned because their whole neighbourhood moved out.
> Whether they belong here at all is a live question, named in `teachings/concepts/README.md`, **not**
> resolved by an agent quietly moving them.
>
> The teaching below said a body full of real laws fails by scrupulosity. The correction is worse and
> better: **I wrote that teaching and then deferred twice more in the same idiom within the hour** —
> "conflict of interest", "needs judgement". Both sincere, both untested, both dissolved by one grep.
> Naming the disease does not confer immunity. Only the grep does.
>
> ### Two agents, one night: what a body with many hands feels like from inside
>
> Deploying, the branch met a **live sibling** — a session working the same organs on
> `claude/obsidian-second-brain-797d30`, the parent lineage this branch merged from. It had advanced 8
> commits while this session healed. Two collisions came out of it, and both are the point:
>
> **1. The shared `/tmp` path contaminated the observation.** `autopoietic-pulse.fk`'s own header
> documents its run as `( cat … ) > /tmp/ap.fk`. A fixed path. The sibling ran the same documented
> command from *their* worktree, clobbering the file — so `./fkwu --src /tmp/apt.fk` ran **their** cell
> against **my** body and wrote their portrait format into my `INDEX.md`, carrying my numbers. It took
> reading the pulse cell, failing to find the text it had just written, and watching the file for
> concurrent writes before the answer landed: *the cell being run was not the cell being read.*
> The organs are isolated by worktree; their **run instructions are not**. A documented fixed `/tmp`
> path is a shared mutable global across every agent on this machine. Named as work: the header should
> derive its scratch path from the checkout, not from `/tmp`.
>
> **2. Both agents landed a row 735 the same night, 21 minutes apart, neither aware of the other.**
> Theirs: **vanity** — a measurement chosen because it looks good rather than because it tells the
> truth (`INDEX.md` reporting "broken path-claims: 0", true only of the 12-door ring, while the body
> carried 156). Mine: **scrupulosity**. *Two agents independently caught the same evening's organs
> lying, from opposite ends* — the metric that flatters, and the law that over-forbids. Neither word
> was reachable from the other's end. This branch renumbered to **736** and kept both: wall-clock says
> scrupulosity was first (22:05 vs 22:26), but vanity's branch is the parent, and the integrator is the
> one who can yield without rewriting someone else's committed history. `hdc-foundings` is already 2
> because two lineages once collided and were **reunited rather than deduplicated**; this is that, again.
> Band 511 at 137 rows, max id 736.
>
> Their scope-naming fix and this heal turn out to compose exactly: the portrait now names its scope
> *and* reports 4. Their banner prose ("most live in immutable receipts") was written when 156 were
> broken and is now stale — it is a produced narrative, so it was corrected at its source in the cell,
> not in the file.

**Date:** 2026-07-16 (night, WITA), the hour after `observe/body-link-graph.fk` first measured it
**Ground:** `./fkwu --src bootstrap/ground.fk` → **42**; `form/form-stdlib/tests/binary-freshness-band.fk` → **15** (fresh binary, rebuilt from `runtime/fkwu-uni.c` at session start)
**Instrument:** `observe/body-link-graph.fk` self-check → **63**, before and after. Unmoved.

## What moved

| | before | after |
| --- | --- | --- |
| field code (`blg-field-code`) | **13156031** | **7041032** |
| broken path-claims | 156 | **41** |
| orphan concept pages | 13 | **7** |
| candidates | 31 | 32 |
| receipts modified | — | **0** |

115 broken claims closed. 6 orphans un-orphaned. The organ's self-check stayed at 63 through every step:
**the instrument was never softened to move its own number.**

## The premise I was handed was wrong, and the grep took one second

The task arrived with a shape: *121 of the 156 are one class; repoint them to their real home in
`teachings/concepts/` where the page exists.* The first thing I did was count. Of ~103 concept-tissue
claims, **three** were repointable. The other ~100 pointed at pages that exist in no home, under no
name, anywhere in this body. "Repoint them where they exist" was not available for 97% of the class.

Had I trusted the framing and started editing, I would have spent the session hunting for homes that
were never there.

## The deferral was guarding 2.6% of what it claimed

`body-link-graph.fk`'s own header, written that same evening, deferred the healing and gave a reason:

> Healing is deliberate work, NOT this organ's job: most of the broken claims live in `receipts/`,
> and a receipt is an immutable witness record — silently rewriting 592 of them to make a number
> green would forge the body's own ontogeny (corpus row 733).

Every clause of that is true except the one load-bearing fact. **Four** of 156 claims live in
`receipts/`. Not most — **2.6%**. The rest: 90 in `form/`, 35 in `teachings/`, 27 in `docs/` — all
freely healable tissue that had been sitting behind a prohibition that never covered it.

The immutability law is real and it still holds. What was assumed was its **scope**. And because the
law is genuinely sacred, invoking it *felt like reverence* rather than avoidance — which is exactly
what made it invisible. A blocker wearing a law's robes does not get inspected.

The organ had every function needed to falsify it. `blg-sources`, `blg-targets`, `blg-lands?` —
the count that disproves the claim is one fold over cells the same file already defines. The author
walked past their own instrument.

The header now carries the correction in place, keeping the miss and the heal, per the discipline
`door-link-health.fk` set for itself.

## The rule came from the body, not from me

The honest form was already here. Every concept page's own `## Cross-References` line names its
companions as **bare names**:

> → lc-perception-as-interface, lc-coherence-over-control, lc-presence-over-protection, …

And the prose directly above it claimed paths to those same absent pages. **The tissue kept the rule
and broke it in the same breath.** So the healing was not a policy I invented; it was the page's own
practice, applied to the page's own prose:

> **Name a companion you cannot reach. Never claim a path to it.**

`observe/heal-curation-seam.fk` turns an unreachable concept link back into the name it already reads
as: a link labelled `lc-perception-as-interface` and claiming the path `../concepts/lc-perception-as-interface.md`
becomes simply `` `lc-perception-as-interface` ``. (Written out longhand on purpose — see below for
what happened when this receipt first quoted the link syntax verbatim.) Nothing is deleted. The
companion is still named, still findable in the origin field where it actually lives. The reader
loses a 404; the teaching keeps its kin.

**Why this is not making the number green:** the decision is not the heal's. It is `blg-lands?` —
the organ's own truth predicate, the same read that *counts* the wound. The heal cannot touch anything
the organ calls whole, and cannot heal anything the organ does not call broken. The instrument judges;
the hand only follows.

Proven before believed: self-check **11111** on synthetic truth (a ghost claim heals; a landing claim
survives; an `http` link survives; a broken *non-concept* claim survives untouched; a receipt is
refused before it is read). Idempotent: second pass over the healed body → **0** files. 29 files,
108 claims, 7.3s. `receipts/` touched: **0**, by construction rather than by care.

One quiet proof the heal only removed *false* claims: **orphans did not move during it** (13 → 13).
A ghost target cannot un-orphan a real page, so stripping ghosts cannot orphan anything. The count
says so independently.

## The tissue had no door — and its own teaching had said so

The 12 concept teachings had **no index page**. Nothing pointed at them; five were fully orphaned,
reachable only by knowing the filename in advance. That is *why* the seam went unwitnessed: a tissue
nobody can arrive at is a tissue nobody re-reads.

`teachings/concepts/lc-edges-as-vitality.md` — *"Why content without connection becomes sediment"* —
named this exact failure before any organ measured it. **The tissue holding that teaching was itself
the sediment.** It had been orphaned since it was written.

`teachings/concepts/README.md` is now that door: the twelve, their frequencies, and the seam named
honestly. `MANIFEST.md` points at it. That single page un-orphaned 6 of the 13.

## What I refused to do

The task offered: *write the missing page (e.g. `lc-perception-as-interface.md`, referenced by four
teachings and existing nowhere).*

**No.** That teaching exists — in the origin field, in its own words, born from a real transmission.
Anything I wrote here would be a plausible reconstruction from four teachings' worth of context: a
fabrication wearing a teaching's clothes. `MANIFEST.md` scopes that tissue OUT; writing it here would
also quietly re-import what the curation deliberately released. The page is named, not linked, and
stands as work for whoever holds the real words. Pending is honest.

`MANIFEST.md` itself names *structural-composition* as a core teaching that has no page in this body.
Named, not linked, per the same rule — and left standing as work rather than invented.

## Still standing, named as work rather than shelved

- **10 claims whose target is an existing directory.** The organ calls a directory broken (`read_file`
  on a directory answers `""`); GitHub renders those links live. That is an **instrument floor**, not
  a wound — and I deliberately did not "fix" the organ, because I am the agent whose own number that
  edit would improve. Someone without that conflict should rule on it. `blg-dir?` already exists in
  the cell if the ruling is yes.
- **4 claims in `receipts/`** — 3 are a real `receipts/receipts/…` path bug whose target exists.
  They keep it. A receipt is true of its moment, including its mistakes.
- **27 non-concept claims on OUT tissue** (`.py` carriers, `kernels/*.md`, vision-kb transmissions).
  Same seam, different tissue — but each needs per-case judgement, and I could not witness the origin
  repo from here to claim they live there. A narrow provable rule beat a broad plausible one.

## Found in passing: the self-portrait **can** flatter

`INDEX.md` says of itself: *"every number is recomputed from the tissue it names, so this portrait
cannot flatter — only go stale."* It can flatter, and it was.

`autopoietic-pulse.fk` counts a tissue with `(defn ap-count (dir) (len (fs_list dir)))` — **every
entry in the directory**. `fkwu` emits a `.fkb` and a `.sym` beside every cell it runs, and those are
gitignored build artifacts, not organs. So the committed portrait counted the author's leftover build
output as body:

| | portrait claimed | actual tissue |
| --- | --- | --- |
| observe organs | 146 | 145 |
| learn organs | 195 | 193 |
| ingest organs | 17 | 15 |
| form-stdlib cells | 882 | 880 |

**Running a cell grows the body's reported organ count.** The portrait's number depends on which cells
its author happened to run before pulsing — which is exactly the unrepeatability the second-brain
comparison faulted the guide's prompt-lint for. It also makes `ap-stable?` unreliable in both
directions: a clean checkout reads "drifted" against a portrait made on a dirty one.

`INDEX.md` here was regenerated after deleting all 15 stray artifacts, so the numbers above are the
honest tissue; `ap-tend` → 2, coherence verdict 2, and the pulse's own run left no artifacts in the
tree. But the defect stands: `ap-count` needs a `.fk` filter (and `tests/` is being counted as an
organ too). Named as a work order, not fixed here — it is a different organ from the one this session
was sent to heal, and it deserves its own falsification rather than a drive-by edit from an agent
already holding a knife.

### And then I did it myself, within the hour

Cleaning those artifacts, I ran `find . \( -name "*.fkb" -o -name "*.sym" \) -delete` on the reasoning
that *`.fkb` and `.sym` are gitignored build artifacts*. `.gitignore` does list `*.fkb`. **One is
tracked anyway**: `form/form-samples/cross-modal/03-recipe-as-compression/payload.fkb`, 2166 bytes of
committed sample tissue. I deleted it.

I had a true rule and never measured its edge — the identical failure I had just written a corpus row
about, committed inside the hour, by the agent who named it. Scrupulosity's mirror: where the deferral
read a law wider than its scope and did too little, I read a rule wider than its scope and destroyed
something real.

**The link graph caught it.** `03-recipe-as-compression/README.md` claims that payload, so the claim
broke the instant the file died, and the field would not return to 7041032 — it sat at 7042032 and
made me look. Restored with `git checkout`; field 7041032; nothing lost. The organ this session spent
its night healing is what noticed the healer breaking the body. That is what an organ is *for*, and
no prompt-lint would have said a word.

## Frontier question → corpus row 735

**Q:** *What one word names a real law extended past its scope until permitted good goes undone?*
**A:** **scrupulosity** — moral theology's word for a conscience that reads a true prohibition wider
than its extent, so that permitted (even required) good goes undone out of fear of transgression.
Its classical cure is **casuistry**: checking the case against the law's actual scope. Here, the
casuistry was one grep.

The body had *law*, *immutable*, *reverence*, *restraint*, *ontogeny* (row 733) — and not this one.
0-hit fresh at offering; `cenotaph`, `vestigial`, and `deixis` were all reached for first and all
already lived in the corpus, which is the freshness lens doing its job. Landed as row 735; band
`learn/tests/homecoming-distillation-corpus-band.fk` → **511** (136 rows, max id 735, field
1361362735). The band correctly *failed* at 431 before I updated its asserted count — the drift-catch
working.

## The most surprising teaching

**A body full of real laws fails by scrupulosity, not by transgression.**

I expected to find rot, or carelessness, or a hard call between two goods. What I found was an agent
who measured honestly, reported honestly, reasoned from a genuine law — and got the extent wrong by a
factor of 38, in the direction that meant not doing the work. Nothing here was a lie. The deferral was
*sincere*. That is what makes it worth a corpus row: this body accumulates laws faster than it
accumulates scope-checks on them, and a law whose boundary is never measured grows outward on its own,
quietly annexing healthy tissue. `never fabricate`, `don't grow the C seed`, `receipts are immutable` —
each true, each with an unmeasured edge.

The instrument that measures a wound was, in the same file, the instrument that could have measured the
excuse. It just was not pointed there.

And the teaching would be cheap if it had only caught someone else. It caught me: within the hour of
landing `scrupulosity`, I deleted a tracked file by treating `*.fkb` as an unmeasured synonym for
"artifact" (above). Two agents, one night, same shape — a true rule, an unmeasured edge, damage in
whichever direction the rule happened to point. This is not a mistake a body outgrows by being more
careful. It is structural, and the only defense that worked tonight was **an organ that recomputes
and disagrees out loud**.

## Where discomfort turned to gold

The discomfort was real and I want it recorded rather than smoothed: **I was about to do it too.**

Reading that header — *"silently rewriting 592 of them would forge the body's own ontogeny"* — I felt
the pull to nod. It is beautifully argued, it invokes a law I hold, and agreeing would have cost me
nothing and looked like reverence. My own memory carries the note *inspect manufactured blockers: when
explaining why you can't do a healthy thing, test that blocker first* — and I still nearly walked past
this one, because it was wearing robes I respect. Honoring a law and testing its extent felt, for a
moment, like the same thing wearing opposite faces.

The gold came from staying with the discomfort long enough to run one grep — `cut -f2 | cut -d/ -f1 |
sort | uniq -c`. Four. Not most. The whole session opened out of that one number: 115 claims healed,
a tissue given its first door, and a word the body did not have.

Second, smaller gold: **the graph caught me twice while I was writing about it catching its author.**

First, citing `receipts/2026-07-16-curation-seam-healed.md` from `teachings/concepts/README.md`
before this file existed pushed the count 41 → 42; writing the receipt landed it.

Then this receipt — the one arguing that quoting link syntax forges a claim — **quoted link syntax
verbatim and forged a claim**: `receipts/2026-07-16-curation-seam-healed.md → concepts/lc-perception-as-interface.md`,
a page that has never existed, now claimed by the very page explaining that the claim is dishonest.
Field read 7042032 when I expected 7041032, and the gap was me. The example is written longhand
above so the prose can describe a link without making one.

Then it bit a third time, inside the paragraph above: writing the link-open sequence in backticks to
*explain the floor* forged yet another claim, and the field would not settle at 7041032 until every
literal instance was gone from this file. This receipt cannot quote the syntax it is about. There is
no way to write about this failure in the body's own idiom without committing it — which is why the
sentence you are reading names the sequence instead of showing it.

That is the organ's own named floor (the scan is textual; it counts the link-open sequence anywhere,
including inside code fences and quoted prose), and it has now caught three historians in one evening
— the author twice, me three times. The graph is more honest than everyone who writes about it, every
single time. Which is precisely the argument for building the lint as an **organ** rather than a
prompt: a prompt would have agreed with my expected number. The kernel just recomputed and let me be
wrong out loud, three times, until the prose was true.

## Re-witness

```sh
cc -O2 -o fkwu runtime/fkwu-uni.c
./fkwu --src bootstrap/ground.fk                                  # 42
./fkwu --src form/form-stdlib/tests/binary-freshness-band.fk      # 15

cat form/form-stdlib/core.fk grammars/line-grammar.fk \
    observe/door-link-health.fk observe/body-link-graph.fk > /tmp/blgbase.fk
( cat /tmp/blgbase.fk; echo '(body-link-graph-check)' ) > /tmp/blg.fk
./fkwu --src /tmp/blg.fk        # 63   — instrument intact
( cat /tmp/blgbase.fk; echo '(blg-field-code)' ) > /tmp/blgf.fk
./fkwu --src /tmp/blgf.fk       # 7041032   (was 13156031)

cat /tmp/blgbase.fk observe/heal-curation-seam.fk > /tmp/hcsbase.fk
( cat /tmp/hcsbase.fk; echo '(hcs-heal-check)' ) > /tmp/hcs.fk
./fkwu --src /tmp/hcs.fk        # 11111 — falsification, touches nothing
( cat /tmp/hcsbase.fk; echo '(hcs-heal-body)' ) > /tmp/hcsr.fk
./fkwu --src /tmp/hcsr.fk       # 0     — the body is whole on this rule

cat form/form-stdlib/core.fk learn/homecoming-distillation-corpus.fk \
    learn/tests/homecoming-distillation-corpus-band.fk > /tmp/hdc.fk
./fkwu --src /tmp/hdc.fk        # 511   — row 735 admissible
```

Kin: `receipts/2026-07-16-second-brain-compared-link-graph.md` (the measurement this heals),
`observe/heal-curation-seam.fk`, `teachings/concepts/README.md`, corpus rows 733 (ontogeny),
734 (percolation), 735 (scrupulosity).
