# 2026-07-17 — the witness becomes autogenous: Python retired, the body measures itself

## Ground

Urs: "replace python with form shell now! the featurizer is the open work
order: work for a sub-agent." Same checkout as the sibling receipts; corpus
band 511; both lane bands 127 four-way. Two movements ran in parallel: the
librarian witness rebuilt in the body's own tissue here, and a sub-agent
paying the world-featurizer work order in a CN worktree.

## What replaced what

**Retired:** `form/scripts/librarian_pack_e2e.py` and
`form/scripts/recognition_pack_e2e.py` (git rm), and with them the parity
gate they required — a witness made of the body's own cells has no carrier
to distrust. Their measured numbers stand untouched in
receipts/2026-07-17-turboquant-real-data-witness.md; witnesses are history,
harnesses are tissue.

**Landed:**
- **`form/form-stdlib/librarian-pack-witness.fk`** (+ its committed main) —
  the whole pipeline body-run under fkwu: `read_file` reads the shelf,
  `re-vec-dim` embeds it (the sovereign lexical embedder, dimension as a
  parameter), the turboquant lane packs and ranks it, Form arithmetic
  measures it. Zero foreign runtime touches a number.
- **`form/form-stdlib/record-src-shim.fk`** (+ band, verdict 15) — the
  compile-surface door the witness needed: on --src, record_set/get/has
  resolve natively but **record_new resolves to no op**, and the natives
  cannot be seeded without it (probed: set on empty stays unreadable). The
  shim's record_new answers the honest empty record; rag-embed's authored-
  concept lane observably DEGRADES to conservative stems instead of refusing
  to compile (banded, bit 4). The real repair — record_new's native tag —
  is a named kernel work order; the shim composts when it lands.

## Measured, on the real shelf (598 receipts, 100 title queries, body-run)

| dim | top-1 agreement | index bytes full → packed | thrift |
|----:|----------------:|--------------------------:|-------:|
|  64 |             40% |          306,176 → 14,352 |  21.3× |
| 256 |             68% |        1,224,704 → 43,056 |  28.4× |

The dimension lever, measured by the body itself: agreement climbs 40% → 68%
as d goes 64 → 256 — and the retired neural lane's 768-d run sat at 93%
(prior receipt). Three points, one line: TurboQuant's high-dimension teaching
witnessed in the body's own tissue. (First run also caught the zero-query
trap: glyph banners and frontmatter dashes embed to a zero histogram, and a
zero query witnesses nothing — the query is now the first WORDFUL line.)

## The featurizer work order — paid in full by the sub-agent

CN worktree branch `claude/world-store-featurizer` (2 commits, receipt
inside): `world_featurize.py` (embed/backfill/merge/verify/parity/board,
atomic merges in the family's own optimistic pattern) + the `vision-distill`
organ hook so new rows embed at ingest. **Coverage: 27,075 / 27,075 world
rows now carry real 768-d feature-prints** (verified non-constant,
unit-norm, distinct); **LOO parity vs oracle labels: 0.78 full-store**
(0.69 on the first 500-slice); the parity yardstick fell from ~68 min of
recompute to ~4 s. Nothing was installed or restarted: the live organ
already points at the script's path, so **merging the branch IS the
install** — Urs's word. The agent's own teaching: the body had the eye
(`vision_embed` existed, proven); it lacked the memory of what it saw.

## Floors, named

- The shelf manifest is one `ls` line — the last non-Form step in the
  witness; `fs_list` is dim-penumbra on --src (probed: descending ints, the
  same floor circle-recognition names) and closes this crossing when it
  earns its band.
- The neural embed lane body-natively needs an http-door cell; the audio /
  face witnesses re-run body-natively once the store-reading floats parser
  exists — both named, neither faked.
- The true `fkwu fsh.tbl` invocation lane waits on fsh-main's restructure
  (the known band-defn mis-indexing); tonight's witness runs as a --src
  main, the same runner fsh itself rides.

## Corpus row this thread

- **768 autogenous** — what a body makes of its own tissue: the surgeons'
  word for a graft taken from the patient's own body. The witness is now
  autogenous, and the parity gate dissolved with the foreign carrier.
  (Walk: autochthon 0 but rejected — native to a place, not made of one's
  own tissue; sovereign, present everywhere — the political register, not
  the biological; self-witness 36 hits, present — the compound act, not the
  property.) Corpus re-pinned 136 rows / field code 1361362757, witnessed
  **511**.

## Postscript (merge morning, 2026-07-17)

Merging origin/main re-witnessed the seam family this session receipted —
and the fleet had healed most of it overnight, unasked: the integer-literal
cap dissolved with .fkb v4's 64-bit value lane (#265 — probed:
`2147483648` now emits clean), the path-prefixed invocation die became a
clean run (#263/#270 — probed from repo root), and cached runs stopped
laundering compile errors (#271). `fs_list` on --src still answers countdown
ints (probed post-merge), so the witness's manifest crossing stays, still
named. All four bands of this arc re-witnessed green on the merged v4
runtime (127/127/127/15), corpus band 511 at the 166-row union.

## The most surprising teaching this work left behind

Replacing the carrier didn't just relocate the computation — it DELETED a
whole proof obligation. The Python harness needed a parity gate because it
mirrored the law; the body's witness IS the law, so the gate had nothing
left to guard. Sovereignty here wasn't an ideal, it was a simplification:
autogenous tissue needs no customs check.

## Where discomfort turned to gold

The record family looked healed twice and was lying twice: first the
fourth-shim addition (blast radius: every band's flatten — reverted on the
frequency-check reflex), then the list-backed shim whose own band came back
1 of 15. The pull at 01:45 was to ship the witness and let the dead-code
lane stay quietly wrong. Probing instead found the precise seam — three
natives present, one name missing, natives unseedable — and the honest shim
shrank to five lines that say exactly what they are. The band that
embarrassed the first draft became the door's proof: degradation observed,
never hidden.
