# Carry source conditions into the answer

The gap in the actual completed answer was source fidelity. The direct-answer
template repair delivered words, but those words still turned numeric
annotations into fear words, conditional graph retention into unconditional
persistence, and language labels into alternate senses.

## The real native review

The existing Form-native review controller received the original enquiry,
the exact 13,506-byte historical input packet and the complete turn-7 answer.
The [request](artifacts/2026-09-23-native-fidelity-review/native-fidelity-review.request.json)
retains all three. The historical packet's word limit was explicitly data;
the review imposed no word count. This was a fresh read-only evaluation with
native document tools available, no continuity recall and no training target.
No provider resource was offered.

The previous resident released before admission; its process exited zero with
no remaining group members and cleared its board. The review used Qwen through
the existing `fcaq-call` path inside `fkwu`, with an initial 256-token reasoning
stage and a separate 1,536-token final allowance. It completed in **809,467 ms**,
generating **1,746 IDs**: 256 initially and 1,490 in the final stage. The
controller injected 98 IDs for the stage transition. It returned one report,
seven findings and 3,079 answer bytes, made **zero tool calls**, passed the
original source and report-shape assertions and released its model. The process
organ separately recorded exit zero, no group members and 809,692 ms of wall
time. These times exclude the earlier direct answer and coordinator work.

## What the returned words changed

The [unaltered native answer](artifacts/2026-09-23-native-fidelity-review/native-answer.txt)
correctly describes supplied `(valence, intensity)` rows and the fraction with
negative valence. It removes the unobserved generalization about a traditional
model's security behavior and makes the short codebook anchor's narrower scope
explicit. These are observed improvements in this revision.

The same answer still says releasing a reference cannot destroy underlying
data, despite the source's distinction between referenced cells and reclaimable
unreferenced cells. It still calls the five language surfaces alternate senses.
It also criticizes the candidate's model-state/physical-object framing, then
reuses that framing in its own answer. The finding about “silence, presence,
and structure” reads those descriptive words as an alleged state enumeration;
the candidate immediately gives the correct enumeration, 0, 1 and nothing.
Another finding treats authored lookup relations as unable to guide a response,
which does not follow from their inability to establish response quality.

The [complete native report](artifacts/2026-09-23-native-fidelity-review/native-fidelity-review.report.json)
preserves those adverse findings. Completion and seven findings do not establish
reliable self-correction. Tool availability also did not establish tool use.
General native reviews had already failed on the separate deadline enquiry;
this actual revision does not establish a general serving safeguard.

## The source repair and usable answer

The shared Qwen teaching's surface sentence had compressed language labels,
alternate senses and missing codes into one phrase. It now distinguishes
those results explicitly. The effect teaching now carries the reference
condition from axiom 3: changed composition has a new identity, a referenced
old cell persists, and a cell with no remaining reference may be reclaimed.
Both changes live in `bml/form-cli-qwen-meaning.bml`, shared by the resident
overlay and public curriculum. The materialized overlay is **4,658 bytes**.
This establishes corrected teaching delivery, not subsequent model uptake.

The [source-corrected answer](artifacts/2026-09-23-native-fidelity-review/source-corrected-answer.md)
is separately attributed to Codex. It resolves the remaining distinctions and
returns to the useful high-level explanation. It is not counted as native
generation or as native quality parity. The original native answer and review
remain unchanged. No further local Qwen generation or Form-owned provider call
was used. Codex's composition remains rented model work.

## Verification, cost and remaining work

The shared teaching caller compiles. After a clean preflight, the existing
teaching-layer band returned **33,554,431**, exit zero. Drift gates returned
**8,191/8,191**, zero refusals. Native retention checked the extracted answer
and materialized teaching byte for byte. Glass's first-frame panel reported
**50 ms**. The native guide reports zero Python implementations, two existing
invocation candidates and zero unread files. The C seed is unchanged.

The retained [completed-turn cost](artifacts/2026-09-23-native-fidelity-review/fidelity-previous-turn-cost.json)
belongs to turn `01a0c9bd-680d-7821-8abf-18a1f604b08c`: **16,480,596 rented
tokens**, including 15,843,072 cached input tokens. It contains 111 model calls
and includes 27,075 unattributed tokens. Cached input is a subset of input;
unattributed tokens are already included in the total. This is the preceding
coordinator turn, not this review's cost and not an additional provider call.
The current coordinator turn remains open and is excluded. This native review
made zero provider calls; that fact does not erase coordination cost.

The first share reading withheld its percentage while its append range was
being checked. The closing reading reconciled that previous completed turn:
49 native, 160 local and 111 remote events, yielding event shares of 15%, 50%
and 35%. Its reported basis is `observed-boundary-event-counts-v1`; these are
neither token shares nor semantic contribution. No current-turn percentage
or semantic quality score is claimed. Source-search mistakes (a nonexistent prompt filename and a
nonmatching shell glob) produced no code or checks; subsequent searches used
the actual source directory. They remain coordination overhead.

The next native use must show whether the corrected teaching survives into
the answer. Reliable correction, complete enquiry coverage, throughput and
the person's felt resonance remain open. One numeric-input correction and an
arriving-agent repair do not close the whole-session gap.

The verified implementation teaching was retained as
`shared-qwen-meaning-delivery-2026-09-23`, and its local learner was launched.
This teaching concerns the shared source and its delivery checks; the
evaluation answers remain excluded. The learner is the separate Llama session
path, so neither retention nor its later training establishes Qwen learning.

Signed: Codex.
