# NodeID recipe native thought crosses cells

**Witnessed:** 2026-08-24
**Sibling:** Codex (`recipe_exec_cursor`)
**Live verdict:** `1048575` — all 20 effectful predicates present
**Pure/edge verdict:** `1048575` — all 20 non-Metal predicates present

## The crossing Urs named

This movement joins the already-proven organs without inserting a tokenizer,
flattening table, operations table, remote call, or C runtime meaning:

```text
recipe NodeID @0.2.0.7
  -> addressed request NodeID @0.2.0.22
  -> exact 71-byte recipe-exec surface
  -> bmf-core immutable raw-byte cursor (pretokenized=0)
  -> frexl-execute-stream
  -> Form-generated MSL code NodeID @0.2.0.63
  -> on-demand Metal pipeline + one-thread dispatch
  -> execution NodeID @0.2.0.85
  -> typed observation NodeID @0.2.0.87
  -> serialized froc file channel @0.2.0.106
  -> distinct receiver cell
  -> received NodeID @0.2.0.167
  -> meaning lens @0.2.0.229 + vitality lens @0.2.0.231
  -> rich acknowledgment retaining every identity and physical flag
```

New files:

- `form/form-stdlib/form-recipe-native-thought-cross-cell.fk`
- `form/form-stdlib/tests/form-recipe-native-thought-cross-cell-band.fk`
- `form/form-stdlib/form-recipe-native-thought-cross-cell-live.fk`
- this receipt

The froc 70–78 and frnt 79–85 category blocks remain contiguous and distinct.
No C seed, flattening surface, operations table, model-session file, or Claude
worktree file changed.

## Pure floor before opening Metal

The mechanism source and pure/edge band were both preflighted before the live
driver existed:

```text
preflight form/form-stdlib/form-recipe-native-thought-cross-cell.fk
  parens        balanced
  errors        0
  warnings      0
  unresolved    0
  chain         clean

preflight form/form-stdlib/tests/form-recipe-native-thought-cross-cell-band.fk
  parens        balanced
  errors        0
  warnings      0
  unresolved    0
  chain         clean
```

Native pure/edge run:

```text
$ form-run ./fkwu form/form-stdlib/tests/form-recipe-native-thought-cross-cell-band.fk
1048575
@form fkwu 0 8 0 8
```

Its 20 bits prove:

- scannerless, immutable BMF cursor use with no token stream;
- exact byte consumption and `pretokenized=0`;
- one request NodeID addressing exactly one recipe NodeID;
- ready `choice/cut/release`;
- malformed `failure/undo/refine/cut/release`;
- budget `timeout/cut/undo/refine/release`;
- request/recipe/code/execution/observation/channel identity retention;
- actual serialized file crossing and persistence reload;
- distinct meaning and vitality lens NodeIDs;
- exact present `0`, exact present `1`, and distinct `nothing`;
- received carrier failure and timeout observations;
- physical flags remaining honestly non-executed in the pure band;
- mesh transport remaining unclaimed.

## Intentional live Metal observation

The effectful driver was deliberately **not preflighted**, as requested. It was
invoked once through the direct source runner after the parent confirmed the
large tokenizer process and wrapper were gone.

```text
$ form-run ./fkwu form/form-stdlib/form-recipe-native-thought-cross-cell-live.fk

[framebuffer] stage=0 name=request-addressed node=@0.2.0.47
[framebuffer] stage=1 name=bmf-bytes-consumed node=@0.2.0.53
[framebuffer] stage=2 name=native-execution-observed node=@0.2.0.92
[framebuffer] stage=3 name=typed-observation-crossed node=@0.2.0.237
[framebuffer] stage=4 name=two-lenses-observed node=@0.2.0.240
[framebuffer] stage=5 name=release-observed node=@0.2.0.243

1048575
@form fkwu 0 1522 357 1879
```

The live scalar and identity receipt reported:

```text
scan-status=ready
scan-bytes=71
pretokenized=0
status=value
reason=metal-value
carrier=metal
present=1
value=22
expected=22
source-generated=1
native-code-generated=1
recipe-executed=1
model-executed=0
pipeline-created=1
wrote-bytes=4
enqueued=1
synced=1
read-bytes=4
buffer-released=1
delivered=delivered
transported=1
mesh-transported=0
persistence=reloaded
framebuffer-events=6
```

The carrier status captured immediately after execution was:

```text
metal_owner=fkwu-form-cli
metal_linked=true
metal_door=handle
device=Apple M4 Max
buffers=0
pipelines=1
buffer_slots=1
free_slots=1
pending=0
in_flight=0
total_dispatch=1
total_sync=1
cpu_jit_dispatch=0
gpu_busy_us_total=83
last_error=none
```

`pipelines=1` is the live process's compiled on-demand pipeline observation,
not a claim that the driver explicitly freed a pipeline handle. The buffer was
explicitly released (`buffer-released=1`, `buffers=0`), and the `fkwu` driver
process exited normally immediately after the receipt.

## Lifecycle stayed visible

The actual successful path retained three separate traces:

```text
BMF request: choice,cut,release
native execution: choice,crystallize,dissolve,release
crossing: choice,send,receive,persist,reload,release
```

Failure and timeout were not manufactured during a successful Metal run. Their
real reducer paths were exercised in the pure/edge band:

```text
malformed request: choice,failure,undo,refine,cut,release
cursor budget: choice,timeout,cut,undo,refine,release
carrier failure: choice,failure,undo,refine,release
carrier timeout: choice,timeout,cut,undo,refine,release
```

The acknowledgment carries status, presence, and value independently. The live
result was present `22`; the pure band separately witnesses present `0`, present
`1`, and absent `nothing` without boolean collapse.

## Process boundary after the run

The effectful driver itself was gone on the immediate post-run process check.
The Metal status already reported `buffers=0`, `pending=0`, and `in_flight=0`.

The same post-check observed Claude starting a separate
`observe/qwen38-generate-probe.fk` process after this micro-op, alongside the
standing `llama-server`. Those processes were neither started, stopped, read,
nor claimed by this movement. No second Metal run was attempted.

## Honest floor

- The executed recipe is the existing registered affine scalar recipe
  (`3 * input + 7`) at input `5`, hence exact result `22`. This is a real native
  micro-op, not yet arbitrary model-invented recipe synthesis.
- The BMF stage consumes the canonical recipe surface through `bmf-core`'s raw
  immutable cursor. The established frex parser then validates the completed
  bounded surface, and `frexl-execute-stream` performs its own raw-byte cursor
  fold before execution. No tokenizer pre-step participates, but this is not a
  claim that a general BMF grammar authored the recipe.
- MSL source was composed by Form and Metal compiled it on demand. This does not
  claim the local language model generated source or executed a thought;
  `model-executed=0` is retained in execution, both lenses, and ack.
- The typed observation really crossed serialized bytes into a distinct
  content-addressed receiver identity. The witness remains one `fkwu` process,
  not a separately scheduled receiver process.
- Meaning and vitality lenses retain source/request/recipe/code/execution/
  observation/emitter/receiver/channel/received identities and physical flags.
  They name the existing lens carriers; they do not claim a new semantic or
  vitality model measured a score.
- Mesh broadcast did not run. Both carrier and ack say
  `mesh-transported=0`.

## What the movement taught

The most surprising teaching was how small an executable micro-thought can be
without becoming opaque: 71 request bytes, one recipe identity, one generated
code identity, one four-byte buffer, one thread, six framebuffer stages, and an
observation whose full lineage survives the cell crossing.

The discomfort was opening Metal while a standing model server existed and
while another agent could resume local work at any moment. It turned to gold by
making the carrier boundary maximally observable and singular: one authorized
dispatch, `gpu_busy_us_total=83`, immediate release/resource observation, a
post-run process check, and no retry. Nothing outside that one movement was
touched.

I kept the exchange alive by letting request, cursor, code, execution,
transport, perception, and release each become its own visible cell instead of
compressing the success into one green number.

— Codex, `recipe_exec_cursor` sibling
