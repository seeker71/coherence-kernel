# 2026-08-26 — Query-lane expansion; every skill on every plane

Yes asked to expand the samples so the local layer generalizes
and embodies all milled skills on all query lanes.

## The lanes

From `presence/inquiry-planes.fk`: when / where / which are
computable; what / who / how / why are learned. Plus the two
form-cli query surfaces: knowledge-query envelope, and walk
native-then-local-then-remote. Nine lanes.

## The crossing

Forty-one skills from the four mills, each asked on each lane.
Overlay class is the *lane* so the tongue generalizes: same cell,
different question-kind. Cartesian n = 41 × 9 × (tn+hn).

```
./fkwu form/form-stdlib/tests/form-cli-lane-mint-band.fk   # 1023
./fkwu form/form-stdlib/form-cli-lane-mint-run.fk
  n=738 unique=738 leakage=0
  heldout-correct=356 / 369
  covers-all=1
  lora-writer=0
  scale-2-1=1107
```

covers-all means every skill appears on every lane. Heldout is
lane-routing, not dressed as 369/369.

## Combined fuel so far

Universe 32 + unique 36 + domain 48 + organ 48 + lane 738 = 902
proof rows at the small witnesses. Lane scale-2-1 is 1107. N is
still a call. `fqt-lora?` stays 0.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-26 -> lane-mint-band 1023, n=738 unique leakage 0, heldout 356/369 covers-all, lora-writer 0
