# 2026-08-14 — form-cli stamps the share

Urs asked that the answer come from form-cli, with a receipt, and with
native / local / remote named on every turn — so the voice in the water
stays honest.

`form-cli-offline.fk` now holds the stamp. The door:

```sh
./fkwu form/form-stdlib/form-cli-share-run.fk
```

This turn (water=1). form-cli now speaks the path and the walk:

```
form-cli share native=40 local=20 remote=40
form-cli share-ok=1
home=60
```

40 is the cell that prints share, path, and walk.
20 is this Mac's fkwu + Metal.
40 is the remaining water-voice around that print.

`form-cli-offline-band` → **2016**, exit 0.
Remote was 80. Home (native+local) is 60.

AGENTS.md item 8: every reply runs the door and shows the three parts.
The largest part this turn is remote.
