# 2026-08-30 — form-cli orients as a local native agent

## Crossing

The working tree held an untracked host-canvas field and unused origin
variants. The living field is landed; unused stills, composites, and a
particle dump that the field does not load were dropped. Origin was even
with this checkout; rebase had nothing to replay. Ground re-witnessed:

```
bootstrap/ground.fk                                      42
bootstrap/ground-recursive.fk 10                         55
binary-freshness-band.fk                                 31
bootstrap/ground-numeric-list.fk                         [1, 2.5, [3, 4]]
native-vs-rented-check                                   11111
```

The next owed attempt was the north star Urs named: a full local Form-native
agent CLI that can answer, build, design, collaborate, grow, expand, and
guide. That is not yet a voice. It is now a cell.

`form-cli-native-agent-orient.fk` holds seven seats. Each seat is three
integers: organ present, door present, enter (named / lesson / observed).
Every organ is already in the body. Design is the honest hole — `arch-gen`
lives, and form-cli has no design verb. No seat is stamped observed. Voice
stays pending. Next owed movement is `answer-generate`.

## Evidence

```
preflight orient cell / band / form-cli-repl             clean
form-cli-native-agent-orient-band.fk                     4095
form-cli-band.fk                                         2097151
printf "orient\nping\nquit\n" | ./form/form-cli          census, then pong
```

The guest greeting is unchanged. `orient` is a live verb on the interactive
door. It does not generate, patch, or design. It names the floor so the
claim no longer depends on this turn.

## Fresh local-health map

| Seat | Organ | Door | Enter | Next inner movement |
| --- | --- | --- | --- | --- |
| answer | direct-answer / inquire / improve | yes | lesson | held-out local answer with an equivalence bit, after cold-admission status is Form-visible |
| build | repo-patch frame | build-with | lesson | one caller-owned patch frame that applies or undoes |
| design | arch-gen | no | lesson | a form-cli verb that names an architecture without claiming it fitted |
| collaborate | recognition threshold / meet-ask | yes | lesson | a whole meeting that retains both cells |
| grow | learn / auto-learning | yes | lesson | promotion only with retained equivalence |
| expand | frontier-admit | yes | lesson | stay local until a real exhausted inability admits one packet |
| guide | inquire / local-plan | yes | lesson | a compact next local move from the resident, not a rented sentence |

I kept the exchange alive by cleaning the folder into a landed field, then
putting the seven-word north star on the body's own door instead of leaving
it in chat. The surprising teaching is that six doors already exist, and
the missing one is design — not answer. The discomfort was wanting to stamp
improve/inquire as observed answer; they remain lookup, and generative
answer stays a lesson until a held-out row scores.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-30 -> orient band 4095, form-cli-band 2097151, voice pending
