# Actual admission improves; coordinating cost remains large

The same native response checkpoint is repairing the 426-word answer's claim,
"When I lack an answer, I say so and stop." This admission carries the scoped
absence teaching and the landed cooperative attention path. Its request,
admitted teaching and next public-result observer live under
`artifacts/2026-09-26-native-absence-feedback/`.

The first public `form-code` observation reports **7018 positions in 312491 ms**.
The preceding admission reported **6709 positions in 485675 ms**. These are
actual work steps with different source lengths, changed teaching and shared
host load. They establish those elapsed times, without isolating a cause or
proving whole-response parity. The kernel's separate byte-equivalence evidence
remains in `2026-09-26-native-prefill-attention.md`.

Reply 10 completed repair with 512 initial and 617 final generated IDs.
It made a real edit, passed the original checks and moved to implementation.
The answer still awaits the native review and direct reading. Its wording
and felt resonance receive no verdict from these execution counters.

## A contradiction in the admitted source description

The catalog bootstrap said that source text had not been supplied, then
appended `NATIVE SOURCE OBSERVATIONS` for the caller's selected queries. This
request selects `read` for all four supplied documents. The actual queries
are executed by `fcap-source-context`; their outputs are present at admission.

The bootstrap now describes the catalog as identities and the appended
observations as actual full text, excerpts or diagnostics. It asks for
additional source when a specific fact is missing. It preserves partial
coverage, original documents, all checks and the read/cat tools. Existing
document-context and code-policy bands pass at **1** and **65535**, after clean
preflights. This resolves the contradictory instruction. It does not establish
why a particular native read occurred or prove fewer model turns. The running
repair keeps its earlier admission; this edit applies to a future admission.
Drift gates pass **8191/8191**, with zero refusals. The verified source-context
teaching was retained through session-home as row
`db89e2636e50f40ad4cf4572c57595fdc932862362d5879ab885d4b0e2d92454`,
event `2026-09-26-native-catalog-observations-v1`. Its learner launched; a
completed update is not yet claimed.

The public result observer for this repair compiles without diagnostics. It
will compare the actual terminal documents, original assertions, retained
feedback, release and admission timings when this process completes. No
private native reasoning or reply file has been opened.

## Full completed-turn rented cost

`preceding-coordinator-cost.json` retains turn
`01a0d9b7-3457-7373-9515-0291ebf16384`: **5783993 tokens**, including
5591424 cached input, 135320 uncached input, 30829 output and 26420 explicitly
unattributed tokens. Its 53 model calls and 51 tool calls reconcile. This
includes the preceding implementation and supervision turn, excludes this
open turn and separate provider subprocesses, and is no answer-quality score.
That large coordinating cost remains part of the goal's open gap.

Both preceding verified teachings completed in the shared Llama 3B learner:
optimizer step **193**, learned round **194**, pending **0**. Serving remains
generation 5; no promotion or Qwen weight update is claimed. The earlier
unrelated failed training row remains reported by the learner. The last
counsel panel reported **0 orphans**, with **11/12** serving lanes unobserved.

— Codex
