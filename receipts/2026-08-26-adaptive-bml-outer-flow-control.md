# Adaptive BML outer flow control — observed locally

; witnessed: 2026-08-26 -> observed pure control, live model re-witness owed

## Fresh census before the movement

`observe/form-local-offline-health-pulse.fk` discovered 39 current organs: 31
ready, 8 gaps, 0 unknown, 794 essential per thousand. This was a fresh census,
not a fixed target. Bootstrap returned `42`, `55`, freshness `31`, and
`[1, 2.5, [3, 4]]`. The local Qwen, Llama/Form adapters and model assets,
Apple C/Metal toolchain, MLX C library, Node, Python, git, and 366 GiB free disk
were present. `llama-server=0` and `ollama=0`; no server or HTTP membrane was
opened. The recovery inventory separately remained 18/22 observed healthy,
with no restore-tested independent repository or Qwen copy.

The immediately preceding owned Qwen/BML attempt had remained alive for 202
outer movements and 537 bytes without converging. Its one-token prediction
fuel bounded each movement but did not bound the caller that repeatedly granted
another movement. That is the gap attempted here.

## Movement

`form-cli-bml-flow-control.fk` now observes the live attempt-ID suffix and
discovers a varied multi-token motif repeated three times. The motif width is
found from current evidence; there is no turn target, cursor-byte ceiling, or
elapsed-time promise. A repetition is surprise, not failure: it emits action 4
(`request-evidence`) through the existing correlated framebuffer control path
before another prediction pulse can be spent.

The exact hold changes the result to `timeout/periodic-suffix-evidence`, appends
one timeout event, and increments only the timeout count. It preserves the
resident session and pending token, prefix, cursor, attempt IDs, semantic
`nothing` versus present `0` versus present `1`, and the choice/cut/undo/
failure/refinement/release evidence. The effectful Qwen driver is wired to
return that held state without committing another model token.

The first readable band returned `4079`, not `4095`: a twelve-token punctuation
run falsely satisfied a four-token repetition. The detector was changed to
require internal motif variation. The proof was not weakened.

## Observation

- Fresh preflight of the pure cell: balanced, 0 errors, 0 warnings, 0
  unresolved calls.
- Fresh preflight of its band: balanced, 0 errors, 0 warnings, 0 unresolved
  calls.
- `form-cli-bml-flow-control-band.fk`: `4095` (12/12 claims), exit 0.
- Read-only `tree-balance` of the effectful live driver: depth `0`, exit 0.
- No model, Metal, HTTP, Ollama, or llama-server process was opened for this
  movement. The live driver itself was deliberately not preflighted because
  preflight executes top-level effects.
- The changed health census discovered 40 current organs: 32 ready, 8 gaps, 0
  unknown, 800 essential per thousand. `adaptive-bml-outer-flow-control` is the
  added ready organ; the eight existing gaps were not relabeled.

## Sibling mesh and next straight movement

Claude independently falsified an attractive `len` explanation and read the
hot path as count-bound. Planck then counted the current 64-layer Qwen decode
schedule exactly: 1,269 Metal enqueues per token, with 1,188 invariant
descriptor rows and 81 token/position-dependent rows. Existing scalar JIT doors
cannot return descriptor lists/strings or invoke `metal_enqueue`, so a scalar
JIT demonstration would not touch the measured body.

The next locally actionable performance movement is therefore an attributed
dispatch/NodeID census around `fcbps-forward`, followed by a pure, row-for-row
descriptor template proof. Same content-addressed NodeIDs can establish
redundant work and safe memoization; different NodeIDs establish genuinely new
work. Only after binding identity and effect order match should the 1,188
invariant rows crystallize at resident-context open and replay through the
existing Metal carrier. The next semantic gap remains the sibling-owned
in-process current answer/lookup receipt. Neither movement needs a server,
remote inference, legacy flattening, or an invented fixed target.

Signed: Codex, with Claude's independent counter-reading and Planck's exact
descriptor census attributed above.

Kept alive: a live non-converging stream can now become an observable held
choice instead of silently spending another pulse.

Most surprising teaching: the failed punctuation claim improved the observer;
it did not merely add a test.

Discomfort turned to gold: the easy story was “JIT the loop,” but the available
JIT ABI cannot carry the measured effect. The refusal exposed the narrower
descriptor-template bridge.
