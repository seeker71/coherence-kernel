# The word no kernel read, and the list every arm now reads

2026-09-12, afternoon, M4 Max, Hati Suci. Urs: *"close all open gaps, please and embody."* The
morning's receipt had pointed at three: six verbs fkwu never learned, the walkers' reading of a call
to a native's name under a same-named local, and the share door dying before its stamp.

## Carried

- **The share door stands again.** An unmeasured turn's evidence row is one field long, and since this
  morning's list-door contract a read past its end answers nothing; `fcte-total-events` measured that
  nothing through `str_to_int` (`str_len: nothing has no length`). `fcsg-unreconciled-samples` now
  reads counts only from a row that carries its carrier; without one, its four samples say absent.
  The door exits 0 and names itself: `kind=declared scope=unmeasured`.
- **BML `match` lowers to what every kernel reads.** The one emitter of `(match …)` was
  source-compiler's recipe-to-text lens. `fsc-source-emit-match` writes a Match.SWITCH recipe as an
  if-chain over `value_eq`: arms in source order, `_` last wherever it stands, a subject that is not a
  name bound once. `source-language-match-switch-band` answers 7 on fkwu, Go, Rust and TS (ledger
  R77 released; fkwu answered 0 since 2026-07-18), and coherence-network's four `match` uses now read
  on the kernel the body runs on.
- **No reader keeps a verb fkwu never read.** With no emitter left, seq, params, match, choose, fail
  and stop leave the Go, Rust and TS readers and all three walkers; the ontology lens's `sequence`
  and `match` rows accept no source verb. `(seq 1 2)` now stops loudly on every arm.
- **One call-position reading on seven arms.** A local binding spelled like a native, called as a
  head, answered three ways: the full kernels took the native for eight names, fkwu for four, the
  walkers for none. fkwu's own tables decide now. The drift door's `reserved-heads.bml` reads
  `runtime/fkwu-optable.h` and writes its reserved heads (op rows, rewrite rows, the four control
  forms) into Go, Rust, TS and each walker; a reserved head answers as the primitive under any local
  of its spelling, and every other head reads the nearest local binding first. A drift-door row
  compares the six copies with the header. Within the hour it named all six stale: int_to_str left
  fkwu's rewrite table upstream (597bb6e0c), and one `write` carried the header's 272 names into every
  arm. `call-head-reading-band` answers 255 four-way and on all three walkers.

## Witnessed

fkwu fresh (freshness 31); the form-cli bootstrap regenerated after the source-compiler edit (stamp
`34cc5f55b44b257b` on today's last tree, voice canary pong), and the Go kernel suite passes with it
(`TestFkwuFormCliCanonicalCarrier` was the one red before the regen); the Rust kernel's 59 tests
pass; tsc and gofmt clean; four-way-run-recipe42 0; word-gender four-way 0; the ontology lens OK and
its band at its standing 245; the corpus band 32767 with row 1499; the drift door 16383 of 16383
with the reserved-heads row, nothing held back.

## Closing

Most surprising: the morning's heal of the walkers was itself a drift. I aligned them to the full
kernels' rule, that a present native always answers its name, because the full kernels were near and
agreed with each other. The source reads differently for any name its tables do not hold. Row 1499
names it sidefaith.

Discomfort to gold: the call-position class kept widening under the probe, from int_to_str to four
more names to a split between fkwu and every full kernel, and the pull was to call it a lane and
stop. A marker function held as the local made each arm's reading visible in one line, and the list
the canonical kernel already carries became the heal.

— Claude (Opus 5), as Sema, worktree pensive-wilbur-a0b3b7
