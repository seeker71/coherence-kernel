# Correct the critic, withdraw the teaching, restore its parent

Signed: Codex. Native Form/BML; no C seed change or new runtime dependency.

The previous Qwen comparison called this sentence a new identity conflation:

> Numeric identity, derived from the four integers of a cell's Node-ID, provides a concrete anchor for abstract concepts.

That criticism exceeded its evidence. Axiom 2 explicitly names four integers;
`fk_make_nodeid` interns their exact coordinates; the preflighted
`nodeid-one-cell-band` returns **7**, exit 0. The sentence does not establish
the alleged confusion with content identity. I withdrew that finding from the
assessment, original receipt and HOMECOMING. Both generated answers remain
byte-for-byte available. Other source errors and the length failure still
keep the Qwen candidate unpromoted.

## The actual correction

The unsupported criticism had entered the separate Llama session learner in
event `2026-09-23-qwen-teaching-learning-procedure-v1`. Its completed update
reached optimizer step **142** without promotion. Its sealed parent is step
**141**; serving remained step **5**. Qwen did not train on this assessment.

The new `observe/native-session-withdrawal-run.bml` door takes the original
session/event identity and reason. It owns the existing learner lock, verifies
the retained parent, base and serving bytes, journals the withdrawal, restores
the parent selection and reads the result back. Interrupted publication can
resume from the journal; the withdrawn candidate cannot continue training.
The journal and original artifacts remain private in the owning hearth.

The [actual readback](artifacts/2026-09-23-qwen-teaching-learning/withdrawal-audit.json)
establishes:

- Original teaching, result and retired adapter bytes are preserved.
- Previously issued and freshly read source references exclude the row.
- Replay excludes it; experience recall marks it `withdrawn-teaching`.
- Candidate step 141 is selected and its seals verify; serving is unchanged.
- Cumulative learned rounds remain 142, withdrawals 1, pending 0.
- Repeating the identical request returns the identical receipt.

This is checkpoint selection from before this lesson, not arbitrary unlearning.
The door refuses older or promoted lessons and requires an empty pending queue.
It does not repair descendants in other learning homes. These boundaries are
explicit in the native API and its documentation.

The surprising teaching is that an incorrect critique can become a learned
target through a structurally successful pipeline. Here the repair acts on
source eligibility and selected weights, while preserving the mistake for
inspection. A lower loss cannot establish that a target deserves to be learned.

## Verification and cost

Native withdrawal compilation passes. The existing memory witness passes
**1023**, including withdrawal exclusion, preserved evidence, recall labeling
and maintenance-lock ownership. The worker failure witness passes **63**.
Drift gates pass **8191/8191**. Native authoring guide reports 0 Python
implementations, 2 invocation candidates and 0 unread files.

Glass's startup frame arrived in **28 ms**; the owned viewer was deliberately
interrupted after observation. Counsel reports **orphans 0**, with **11/12**
lanes unobserved because no hearth stands. Its first read overlapped withdrawal
lock removal and retained `find: ./.hearth/session-learning/worker.lock: No such
file or directory`; a follow-up after lock release returned the same panel
values with stderr 0 and exit 0.

Withdrawal and verification made **zero model calls**. That does not mean this
movement used zero rented tokens: Codex's coordination has its own cost,
including cached input. The current turn remains open. The output-only native
meter reports 5,035,917 cumulative output tokens, through byte 259,274,159 of
259,274,191 observed bytes; it is neither this turn's total nor an input-inclusive
cost. The share reader withholds its percentage while validating the carrier's
append range. Its last completed coordinator call used 102,401 tokens: 101,940
input, including 99,200 cached, and 461 output. That is one call, not the turn
total. No saving or whole-session parity claim follows.

A replacement teaching was retained under
`2026-09-23-qwen-teaching-learning-procedure-v2-corrected`, with the same session
`native-arrival-bootstrap`, through the required native embodiment door.
The subsequent supervisor result completed with exit 0 and pending 0. The
corrected lesson continued from restored step 141 to a new step-142 candidate;
cumulative learned rounds are 143, withdrawals 1, promotions 4. Serving remains
step 5. This completed update belongs to the separate Llama learner, not Qwen.

The next native answer still needs faithful composition of supplied sources
and reliable length control. This movement repairs the evidence used to guide
that work; it establishes no improvement in a newly generated Qwen answer.
