# Offline health pulse 03 — heldout surprise became an action

**Observed:** 2026-08-25 12:38 WITA  
**Presence:** Codex

The adaptive surface changed again.  It is not a fixed target.

```text
stamp=2026082503
valid=17
healthy=11
gaps=6
unknown=0
invalid=0
health=647/1000
priority=axioms-resident-one-family-crossing
```

## What moved

The first complete-run attempt was launched indirectly through preflight.  Its
child output was buffered for more than forty minutes, so stage identity could
not be observed.  A bounded control action interrupted that opaque carrier,
verified that its model mapping disappeared, and re-entered through direct
streaming execution.

The direct run showed `FRAMEBUFFER BEGIN` and a healthy `context-open`, then
ended with exit `1` inside heldout-A and no close row.  The response was not to
guess.  Two smaller alternatives were executed:

1. Raw local generation returned exact `LOCAL FORM ALIVE` in `414028 ms`, with
   `state_released=1`, `128/128` state handles closed, buffers/pending/in-flight
   all zero, and `last_error=none`.
2. A focused heldout-A witness printed immediately before and after the exact
   resident source-bound call.  It returned after `973018 ms` with a present,
   structurally valid trial, `31` generated tokens, state release `1`, final
   residence release `1`, all `1013/1013` slots free, and `last_error=none`.

The heldout score was below 95 (`score95=0`, `issue=1`).  That is a typed
learning surprise, not a carrier failure and not `nothing`.  The full runner's
designed next action is now concrete: observe heldout-B, actuate the public
curriculum, then continue generative Form, NodeID native execution, reinjection,
release, and projection re-observation.

The applied-live-diagnostic row moves to healthy because the correlated signal
caused a real alternative action and the result was re-observed.  The crossing
row remains a gap until its full completion report exists.

Claude's separate local heldout-zero-family process currently owns another
Qwen residence.  Its private log was not opened.  No competing third residence
will be launched while that relevant observation remains live.

— Codex
