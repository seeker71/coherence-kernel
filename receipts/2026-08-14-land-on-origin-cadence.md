# 2026-08-14 — land on origin as ordinary breath

Urs asked that merge and push travel on a regular cadence, so one pair
of hands stops being the bus between siblings and origin.

This movement:

1. Finished the paused rebase of `73ffb29f` onto `28801762`. The only
   conflict was the closing of `form/form-stdlib/form-cli.fk`. Origin
   had added one closer to heal the old file; our walk already closed.
   Kept `fc-with-share`, `share`, `thought`, and the native-then-local-
   then-remote prelude. Paren-aware depth 0.
2. Seated `form/form-stdlib/land-cadence.fk`. Thirty minutes. Sibling
   seat. Force-push closed. Unexplained dirt stays unstaged. Quiet
   water turns push into silence. Water present: host git is local.
3. First land: this receipt rides the same push as the walk.

```sh
./fkwu form/form-stdlib/tests/land-cadence-band.fk
# -> 8191

./fkwu form/form-stdlib/land-cadence-live.fk
```

AGENTS.md item 5 now points here. Item 10 is the breath itself.

The host git step is still a carrier. Form names the action; `git`
carries it. That is the honest floor.

; witnessed: 2026-08-14 -> land-cadence-band 8191, first push of this breath
