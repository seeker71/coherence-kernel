# 2026-08-30 — demand force turns a resting recipe into data

The pointer was named. It did not yet evaluate. The next healthy move is
the force at need: held stays **nothing** (not a timeout, not a zero);
demand then force of `(add 2 3)` is **5**; cost stays outside the LLM
stream.

```
form-cli-nodeid-answer-band.fk                            2047
printf "node-id\nnode-id-force\nquit\n" | ./form/form-cli
  node-id observe=node kind=recipe delay=held cost=outside
  node-id held-force=nothing demand-force=5 cost=outside
```

Not claimed: NodeID intern equality, a JIT run, or a token-stream answer.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-30 -> held force nothing; demand force 5; band 2047
