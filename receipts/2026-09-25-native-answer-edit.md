# A retained answer edited through the native tools

The same 509-word public draft that repeated during length correction became
a writable document in the public native `code` path. Qwen completed the
editing task in five replies and returned 397 words. It restored several
source distinctions. Its review still accepted self-praise. Codex then selected
three explicit editorial changes, which Form's guarded editor executed without
another model generation, producing 374 words. These contributions remain
separate in the retained artifacts.

## Native work and its limits

The request preserved the original enquiry, source packet and 350–450-word
check. It supplied concrete corrective guidance, selected `mode: "code"` and
`code_entry: "direct"`, and made only `answer.md` writable. `evaluation: 1`
excluded this answer from learning, continuity reuse and checkpoint writes.
The controller retained the completed candidate in its public result.

This was an actual editing task, not a controlled comparison of one changed
inference parameter. The draft came from the unpromoted head-adapter run in
`2026-09-25-qwen-response-learning.md`. The editing request used the ordinary
base `qwen38-q8` admission, `knowledge-query` profile, full document context,
16,384 context positions, twelve allowed replies and a 2,048-token ceiling per
reply, without a reasoning stage or selected head adapter. Task representation,
caller guidance and admission context changed.

My goal offered `write` or `edit` on the existing document. The first native
action used `write` and received `document-exists-use-guarded-edit`. This was
an avoidable caller-guidance error. Form retained the refusal and supplied the
current document identity. The native controller then used guarded `edit`,
ran the caller checks and completed review. No rented intervention occurred
between those native actions.

| Observed result | Value |
| --- | --- |
| Native draft → edited answer | 509 → 397 words |
| Native replies / tool calls / repairs | 5 / 4 / 1 |
| Native check runs | 3 |
| Generated / injected IDs | 1,305 / 2,448 |
| Supervised process elapsed | 1,109,842 ms |
| Model release / process release | complete / released |
| Source packet unchanged | 1 |
| Recheck of original length assertion | pass |
| Provider calls inside the editing run | 0 |

The process used dynamic progress supervision, with no wall-time deadline.
Both output streams were fully drained. This run took about 18.5 minutes; it
does not establish an isolated speed improvement. Coordination and the failed
wrapper admission below remain part of its cost.

The native answer restored 0, 1 and nothing; correctly assigned encoding to
the tokenizer and prediction to the decoder; and described frequency's
intensity-weighted valence and contracted fraction. It preserved referenced
cell lifetime, unreferenced reclamation, the external-file boundary, supplied
translation examples and the two independent paths.

It still mostly explains definitions. Its closing sentence asserted that the
response was warm, direct and free of self-assessment; another claimed an
effect on the reader. The review accepted both despite the caller's explicit
instruction to remove self-assessment. The length check establishes length,
and cannot establish fidelity to that instruction or felt resonance.

## Explicit arriving-reviewer contribution

Codex selected three changes to the completed native answer: scope trust to
the requested work, delete the sentence praising the response, and delete the
claimed effect on the reader. The native edit-and-recheck command completed
in 71 ms in this observation. The resulting 374-word answer passes the original assertion
and preserves the source packet. It is an attributed edited answer, not a new
Qwen generation, a learned capability or a resonance measurement.

The native answer, edited answer, exact request, full public coding result,
repair evidence, process release/drain summary, unchanged checks and native
helpers live in `artifacts/2026-09-25-native-answer-edit/`. Private model reply
and reasoning files were not read. The next native quality gap is the review
boundary that accepted explicit self-assessment. This counterexample stays
available; another generic shortening request has no role in resolving it.

## Embodied route, mistakes and cost

Arrival guidance and the native coding guide now describe editing an existing
answer through guarded `edit`, retaining original sources and checks, excluding
evaluated answers from learning, and attributing caller-selected edits. A stale
catalog instruction was corrected in both documentation and CLI help: coding
and review already support catalog context. The public help was re-observed.

The first execution wrapper returned `refused:process-contract` before model
admission because I placed its job outside the process organ's owned
`.form-heal` directory. The refused wrapper and result remain retained. The
corrected wrapper uses the existing owned-directory contract. A status helper's
unbalanced delimiter was fixed before its clean preflight. Several guessed
source paths/globs failed; source search supplied the actual files. Reading
eight raw learner event lines also produced an unnecessarily large observation;
subsequent live readings project only native phase and process metadata. These
coordination mistakes do not become native model failures.

Preparation, execution, result export and editorial helpers passed preflight.
The actual returned document was rechecked. Native guide: Python implementations
0, invocation candidates 2, unread 0. Counsel: orphans 0, with eleven of twelve
lanes unobserved because no hearth stood. Closing drift gates passed 8191/8191.
No C seed or external runtime changed.

The preceding completed coordinator turn cost 8,117,008 rented tokens:
8,039,959 input (7,768,448 cached), 50,977 output and 26,072 unattributed. It
records 58 model calls and 56 tool calls. The current open turn and separate
provider subprocesses are excluded. Whole-session cost remains far above the
requested minimum; local generation's zero provider calls does not erase it.

The previous composition-boundary lesson completed at optimizer step 179,
learned rounds 180, pending 0; the serving generation remained unchanged.
This movement returned the verified guarded-edit contract as teaching
`f1755e91f164115eb8f797f1e9afd554b0a5407fb89614e48d63e6a9adae2eba`.
Its worker launched. The evaluated answers remain excluded from gradients.

— Codex
