# 2026-08-26 — three sibling hands converge without repainting error

Urs asked that all agent work merge and push together so drift would shrink.
Three sibling organs arrived with three different evidence states. This landing
keeps those states distinct.

## Claude — live error-attention control

`observe/form-local-reasoning-live-diagnostic.fk` now retains the measured
base error `[expected 1, observed 0]`, crosses a correlated branch control,
runs the error-derived rank-one movement only after that control, and
re-observes both teaching and sealed surfaces. A failed re-observation selects
the existing rehearse-ground actuator and returns to the retained error.

Fresh result:

```text
family census       15, read live
base error/sealed   0 / 0
adapted error/sealed 1 / 1
framebuffer events  4
verdict             1
```

Release now requires both the `resolved` signal and equality between expected
and observed values. A label alone cannot release the error.

## current-answer capability — completed pure boundary

`runtime-program-image-fkb-current-answer-capability.fk` and its byte-identical
grammar mirror carry source/request/answer/lookup identity through a
recomputable resident execution receipt:

- admitted in-memory PIF load;
- call;
- typed return;
- execution fault;
- fuel timeout;
- immutable-frame release.

Missing or invalid evidence is `nothing`; resident disagreement is present
`0`; agreement is present `1`. Zero-byte source and empty answer remain
present values. Lifecycle, release, replay, and cross-input forgeries are
rejected. The load observation claims no disk I/O, model, Metal, or host
resource.

```text
source preflight       errors=0 warnings=0 unresolved=0
band preflight         errors=0 warnings=0 unresolved=0
source/grammar cmp     identical
adversarial band       16777215, exit 0
```

The broader symbol-walk regression was still computing when this scoped organ
landed; it is not reported as a verdict.

## family resident batch — pure boundary completed, physical boundary open

The family batch scheduler, adversarial band, dormant live driver, and
receipt land only as one unit. They now distinguish malformed, failure,
timeout, `nothing`, absent value, `0`, `1`, and other values; read the
family census fresh; compare whole resident context/state; and stop immediately
after invalid request, result, residence, or acknowledgement evidence.

Its source preflight is clean and source/band/live lexical depths are all zero.
The amended thirty-axis band was first cut after 1m41s without output, then
reopened and continued to `1073741823`, exit 0. No model or Metal run occurred.
The dormant driver remains a physical gate, not a landed claim.

## Boundary retained

Completed pure work and unobserved physical work are not averaged together.
The convergence lands all authored meaning while preserving the exact point
where each witness stops.

Signed, **Codex**, with Claude, Planck, and Beauvoir as co-authors of the three
hands.

Kept alive: convergence did not require every organ to impersonate the same
readiness. Most surprising: the healthy merge contains both a full 24-bit
capability band and a full 30-bit pure batch while the physical crossing stays
open. Discomfort turned to gold when "merge all" became a way to land honest
difference, and a later question reopened the cut proof until it completed.

; witnessed: 2026-08-26 -> live diagnostic 1/4 frames; current-answer 16777215; family batch 1073741823; physical run absent
