# record_new walks the direct-source lane

2026-09-02, worktree pensive-wilbur-a0b3b7, commit bb79473c.

## What was closed

The record organ's constructor was flatten-orphaned: fk_walk tag 64 fully
present in the kernel, no name reaching it from source. record-fold-band
answered 16 of 31 with four `[unresolved-call] 'record_new'` wounds, while
record_get/set/has/keys/blueprint all walked fine.

The door, opened through the body's own lanes:

- one flt-ops row — `(list "record_new" -1 64)` in flatten/form-flatten.fk,
  beside the record family it completes
- runtime/fkwu-optable.h regenerated by the two-call gen-source-walker
  recipe (splice, then combined driver — zero hand-edits of the generated
  header; the splice routed through the session scratchpad because the
  documented /tmp door was warm from a live sibling's hand)
- `fk_parse_record_new` in fk_sparse's variadic branch, building the exact
  entry packet flt-record-new emits: `((-1 bp) (k v) ..)` under one tag-64
  node — blueprint riding the -1 sentinel (fk_stri of a non-string answers
  -1), fields in source order

A dangling key now refuses loud at its own column — fields come in
(key value) pairs — instead of landing a partial record accepted as whole.

## Witnessed

- record-fold-band 31, rc 0; preflight: parens balanced, 0 errors,
  0 warnings, 0 unresolved
- record-band 176, record-blueprint 7, record-field-access 16,
  list-of-record-reduction 22, cache-phase 255 — all on declared verdicts
- grounded-cost-record-handler answers its frozen sample exactly
  (floats travel the packet door too)
- synthetic probes: key order `[a, b]` source-true; bare `(record_new bp)`
  is a keyless record; dangling key rc 1 with the wound named
- both manifest drift gates green, 149 rows aligned, before and after
- the row also flips fk_fkb_node_arity_for_tag(64) from 0 to 2, so a
  tag-64 node's packet child now remaps on .fkb import — a second seam
  the same row closes

Standing separately: method-band answers 0 on this arm — method_define /
method_invoke / method_has have no tags, no lowering, no fourth-arm claim.
An unbuilt organ, not a regression; named as its own task with this
landing as the pattern. record_new inside that same band resolves clean.

## The most surprising teaching

Both drift gates said "149 rows aligned" before the row landed and after —
and were incapable of saying anything else. Their glance reads rows with
digits only; an arity of -1 falls outside every mirror they hold. list and
print had lived there all along; record_new joins a family the gates were
sworn to see and structurally cannot. A gate's green is a claim about the
rows its glance can read, never about the table — the same teaching
preflight left about preludes it never loaded, now arriving from inside a
gate's own reading-shape. Corpus row 1222 carries it: gateshadow.

## Where discomfort became gold

Reaching for the regen recipe, /tmp/gen-source-walker-combined.fk was
already there — written one minute into this session by a sibling's live
hand. The pull split two ways: barge through the documented door anyway,
or stop and call the lane blocked. Witnessed instead: read the sibling's
combined file first. It carried no record_new — their work, not this work,
no duplicate landing in flight — and the splice then walked through the
session scratchpad, leaving their door untouched. The narrow shared path,
sat with rather than forced, turned into both a coordination fact and a
cleaner practice.

A smaller gold: the worry about stale ices replaying old refusals was
answered by the body before it was asked — "foreign .fkb (written by a
different fkwu build); rebuilding from source. The bytes of a source do
not fix its meaning — the binary that compiled them does." The kernel
already speaks the discipline this landing leaned on.
