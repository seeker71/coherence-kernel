# Merge, reunion at row 731, and the first innervation — 2026-07-16

## Ground

```sh
./fkwu --src bootstrap/ground.fk                               # 42
./fkwu --src form/form-stdlib/tests/binary-freshness-band.fk   # 15
```

## The merge

"merge and continue." The stewardship arc went to main as PR #260 (squash,
`a12afc0a8`). One live seam on the way: main had grown its own row 731
(**proleptic**, the plugin door's lesson) while this line grew 731–736. The
corpus's own reunion pattern (row 719, anastomosis) resolved it — every row
from both lines kept, this line's six renumbered 732–737, band restated to
138 rows and re-proven 511. The renumber note lives in row 732's comment.

## The continuation: first innervation

Corpus row 737 named the question the merge left behind: proven tissue is
still dark until wired into the live body — **innervate**. The first nerve is
now grown: `world-model-live-sense.fk` — the cell whose own header says
"continuous live witness loops are the arc" — carries `wmls-stewarded-grow`:
the same receipt→sense→grow walk, with the loop's ship/witness ledger read
first. An abreast ledger grows unattended; a lagged one reports instead of
growing; witness receipts restore; drift reports regardless. The trusted
core's follow/refuse stays its own. Band grown 19 → 22 claims, verdict
524287 → 4194303, four-way (Go = Rust = TS = fkwu); manifest row updated.

Still honest: the ledger values in the band are fixtures. The next arc is
feeding a real loop's ships and witnesses into that ledger — the fleet loops
live on CN ground, so that wiring crosses repos and waits for its own
grounded session.

## The most surprising teaching this work left behind

Two lines of one body independently reached for the corpus at the same hour
and the same row id — and the collision was not damage but the reunion
pattern exercising itself a second time, proving row 719 was a teaching and
not an anecdote. The body's history repeats its shapes, and the shapes hold.

## Where discomfort turned to gold

The merge failing right after "merge and continue" — the button refused, main
moving twice under my feet in one evening, a flicker of hurry to force it
through. Witnessed instead of rushed: the conflict was read as the corpus
asking for its own reunion law, the resolution came out cleaner than the
first founding's (ids contiguous, note in place), and the failed button turned
into the evening's best proof that append-only ledgers merge by renumbering,
not by fighting.
