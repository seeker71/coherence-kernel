# The full PIF crosses the content-addressed call boundary

The fresh pulse began at **58 observed / 43 ready / 15 gaps / 0 unknown / 0 invalid / 741 permille**. Its selected gap was `full-pif-content-addressed-call-adapter`; this movement changes only that already-censused row. The denominator was re-observed, not held as a target.

The companion adapter retains four things that the compact protocol alone could not honestly carry:

- the actual recursively interned PIF recipe in the binding, rather than a compact proxy;
- the receiver-held raw envelope and admission as authority distinct from content identity;
- the recursively typed OFFER value, including nested lists and exact nothing/0/1;
- the actual `rpsw-walk-symbol-with-kind` receipt beside an ACK that is accepted only when both the walk and ACK recompute from the receiver plus exact OFFER.

This exposes and heals the sharp seam: a successful full-PIF call may return typed `nothing` with output-count 1. That is now distinct from protocol `nothing` with output-count 0, as well as refusal, timeout, failure and choice. A malformed recursive kind is refused before any walk. The adapter adds no query-specific evaluator tag, global function/operation table, flattening path, C-seed meaning, host process, HTTP, model, or Metal ownership.

## Evidence

- adapter and focused-band preflight: balanced, compile errors 0, warnings 0, unresolved calls 0;
- focused adapter band: **262143**, exit 0;
- existing compact CALL/OFFER/ACK protocol band: **2097151**, exit 0;
- broad full-PIF symbol-walk regression: **17179869183**, exit 0; its first long rebuild was held under a correlated framebuffer `request-evidence` control, and the verdict was then re-run through a persistent captured channel rather than inferred from process completion;
- full form-cli band: **2097151**, exit 0; binary freshness: **31**, exit 0; existing route-evidence image-call band: **32767**, exit 0;
- health pulse after the one row changes: **58 observed / 44 ready / 14 gaps / 0 unknown / 0 invalid / 758 permille**.

The next selected local-only gap is `scannerless-request-image-birth`: consume one bounded raw BMF frame inside this admitted call, preserve malformed/incomplete/trailing/timeout and nothing/0/1 distinctions, and birth exact request category `31.2.0.98`.

Kept alive: content identity, receiver authority, recursively typed values and terminal signals remain independently observable. The most surprising teaching was that returned `nothing` was lost in the older ACK validator rather than in the full walker. Discomfort turned to gold when the tempting word “generic” exposed two false shortcuts—proxy binding and assumed composite node level—and both were removed instead of defended.
