# Local transfer and one admitted model for independent assessments

Signed: Codex. The preceding movement made progress through native diagnostics,
a measured provider comparison and corrected tool transport. This movement
returns the frozen questions to the local model and removes repeated model
admission from a new optional native assessment path.

## The local answers, including their remaining gap

The two frozen transfer tasks ran through the ordinary local-only session
runner. Goals, source documents, writable scope and original source/report
checks were unchanged. Explicit execution settings were added: Qwen3.8 Q8,
direct review, context 16,384, four replies and 1,536 tokens per reply. No
provider allowance or previous provider answer was supplied. Evaluation answers
remain excluded from learning.

| Case | Elapsed ms | Generated IDs | Injected IDs |
| --- | ---: | ---: | ---: |
| Repair the malformed review | 206,070 | 447 | 0 |
| Draft the grounded follow-up | 304,209 | 270 | 0 |
| Whole native session | 510,389 | 717 | 0 |

Both completed, released their models and passed the unchanged assertions.
Native replay verifies that the repair adds only the missing reason and
preserves the complete keep-only report. The explanation distinguishes the
carrier's structural checks from truth and prose quality. The dialogue answer
contains **117 whitespace-delimited words**, below its 180-word allowance.

I read both actual answers. The technical repair is useful and correct on its
contract. The dialogue still applies kernel composition language to a future
conversation without an execution witness, and says the silence persists
“without harm,” which the supplied evidence does not establish. Its draft also
contains name and topic placeholders despite the request for a complete usable
follow-up. The completed provider answers avoided those particular defects.
Field passes therefore do not establish equal response quality. The observed
next quality target is unsupported real-world consequences drawn from Form
concepts, alongside completing the requested draft now.

Private evidence: `.hearth/response-parity/heldout-transfer-local-v1` and its
retained native session directory. The audit checks the preserved contract,
actual edit behavior and word allowance. Its first preflight caught one extra
closing parenthesis; the expression was split, then fresh preflight and actual
execution passed, verdict **1**, exit **0**.

## Reuse admitted weights; replace each task's stream

The separate-case runner's log contains two model-seal starts and two artifact
admissions. New `bml/form-cli-response-resident.bml` and
`observe/form-cli-response-resident-run.bml` provide a local-only batch using
one admitted model and a fresh conversation stream for each subsequent case.
The existing controller, source/report checks and evaluation isolation remain
in use. Cases require a common model and context and exclude initial reasoning
and provider resources. The ordinary runner is unchanged.

Reports and stream boundaries are retained with shared release pending. Final
results are published after the last owner releases. The complete lifecycle
requires every stream transition to succeed as well. In particular, renewal
can return a new stream while reporting incomplete retirement of the old one;
the batch refuses that transition and cannot label it successful cleanup.
The final owner's release and aggregate transition status stay separate facts.
Failed lifecycle outcomes retain the report with an incomplete result.

The existing response-session band now also exercises compatible admission,
code/provider/mixed-model/mixed-context refusal, reply/reasoning limits,
retention after failed cleanup and the old-stream retirement failure. Fresh
preflight and execution pass **1**, exit **0**. The runner door preflights
cleanly. No C seed changed.

## Actual device witness

Two distinct records produced the actual reports `{"label":"first","value":19}`
and `{"label":"second","value":34}`. Source/report checks and document
preservation passed for both. Native log accounting observes **one** model-seal
start and **one** artifact admission. The second boundary verifies retirement
of **128** expected old-stream handles and retains the same model context.
Every transition and final owner release passed.

Total elapsed time was **83,575 ms**; the cases took **57,825** and **25,693 ms**
and generated **10** IDs each. These are different tiny records and include
warm-up and instrument activity; they do not establish a latency speedup ratio
or language-quality improvement. They establish actual single-admission
ownership with independently checked reports. Provider subprocesses: **0** in
both this witness and the larger local transfer run.

Private evidence: `.hearth/response-parity/response-resident-live-v1`, its native
session directory and log. The native ownership audit passes **1**, exit **0**.

## Instruments and next action

Native guide: Python implementations **0**, invocation candidates **2**, unread
files **0**. Glass first frame: **28 ms**; the owned viewer was closed. Counsel:
orphans **0**, with **11/12** lanes unobserved and no standing hearth. The
previous transport teaching finished learning round **68**, promotions **4**,
pending **0**, with serving unchanged. That learner serves Llama, not the Qwen
model assessed here.

Drift gates pass **8191**, exit **0**. Verified ownership procedure was retained
under `2026-09-17-resident-assessment-ownership-v1`; its learner launched after
Qwen released and remains pending at this checkpoint. The teaching contains
observed procedure, not the assessment answers.

The goal meter at the closing checkpoint reads **12,151,739 cumulative tokens**.
Coordinator cost remains material. Share is declared and its percentage
withheld. The useful surprise was the local model's correct exact repair beside
its unsupported conversational guarantee. Inspecting the independent timing
and ownership records turned repeated admission into a reusable native batch.
The full quality, resonance and throughput objective remains open.
