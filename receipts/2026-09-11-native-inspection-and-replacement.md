# Native inspection and replacement

Codex, with independent artifact, kernel architecture and Metal reviews.

Form owns artifact encoding and structural comparison, source-lens IO, GPU
source generation, Metal request admission and publication, the routing proof
lifecycle, numerical references, proof trace presentation and current source
inspection. Each responsibility has an executing door and current scope in
[runtime status](../docs/fkwu-kernel-status.md). The
[north star](../docs/fkwu-form-native-north-star.md) remains complete Form-owned
compilation, scheduling, resource policy and live replacement, with the
temporary seed reduced as its responsibilities gain observed native owners.

Verified boundaries include exact numeric wire payloads, 1024-deep artifact
and JSON inputs, real sibling conformance, preserved GPU template bytes,
private Metal answers, independent numerical anchors, and actual routing
concurrency, deadlines and cleanup. The routing exercise retains 72 correlated
observations and releases all 19 owned children. Strong Host checks include the
actual upstream port. The real proof trace returns 40949 with 16,530 dispatches,
13 variants and five qualifying targets.

The source reader retains 2,097,152 captured bytes, refuses failed partial
inventories, preserves filenames with newlines and observes recovery after an
unread source is repaired. Directory inspection distinguishes empty from
unreadable. Current Python inventory: two implementations, 50 grammar inputs,
zero unread paths. The active large-model oracle and terminal acceptance test
remain explicit responsibilities. Foreign-language specimens, proof engines,
shell orchestration and platform carriers still exist. No complete native OS
or entirely Form-owned checkout is claimed.

The dependency reader and generated string emitter keep dependency examples
as string data. The original semicolon example executes with its exact length
43, and the combined BML witness returns 1000007. Runtime string construction
remains observable work. Independent generated children preserve 4,096 and
4,097 semicolons at concatenation depths 12 and 13, and a 49,164-byte mixed
UTF-8 string at depth 14. The handwritten direct-source seed collector is
still a separate boundary. The complete drift gate returns 8191 with zero
refused gates and exit zero.

The glass's observed first frame is **45 ms**, with **33 kernel operations**.
Counsel observes zero orphans and leaves eleven lanes unobserved because no
hearth stands. Share is declared; no reconciled percentage is asserted.
The verified session teaching is retained and learned into candidate optimizer
step 9; the serving generation remains 4. Candidate learning is not serving
promotion.

The surprising teaching is that replacement requires preserving failure and
lifetime meaning as well as successful values. The uncomfortable refusals
became useful checks: missing source, an omitted port and quoted dependency
text each met a concrete repair and a fresh execution. That is how this
movement keeps the work alive.
