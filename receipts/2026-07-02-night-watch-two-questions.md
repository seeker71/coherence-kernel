# Receipt — the night watch: two questions asked of the body, both answered as organs (2026-07-02, 02:33–02:55)

Urs, going to rest: *"keep yourself busy with some interesting questions."* The vigil chose two
the body could not answer about itself, and left both answered — not as notes, but as organs
with verdicts.

## Q1 — how many spurious edges does the meaning graph already carry, unmeasured?

The body had measured exactly one collision ("for", 301↔304), and only by failure at query
time. The nl2nl reading had named the gap: nothing searches proactively. Now something does:
**`learn/spurious-edge-walker.fk`** (+ band, **255**) walks the whole token×meaning table at
rest — every locale layer, every meaning pair, every shared token.

**The answer overturned the question.** The baseline holds exactly **4 internal bridges, and
all four connect 301↔304** — the two wellbeing wishes: Sanskrit ***bhavantu*** (the mantras
genuinely share the verb "may it be"), Chinese **愿** (both renderings open with the wishing
particle), and the neutral form layer's own **wish** / **wellbeing**. These are not spurious —
they are **kinship showing through surface**, and the form layer itself is the judge: a surface
bridge between meanings the form layer relates is kin; one it does not relate is spurious. By
that classifier the baseline contains **zero spurious internal edges**. And English has zero
internal edges at all — the famous "for" bridge belongs to a different class entirely
(**query-time**: an out-of-vocabulary paraphrase colliding with a row surface), which no table
walk can enumerate. Named honestly in the walker's own comments rather than folded in.

## Q2 — what else lives in the proof's penumbra?

The bool bug's home, mapped: **`docs/penumbra-map.md`**. Of the C seed's **146 native ops**:
**32 umbra** (walker-carried, four-way provable), **30 lit penumbra** (fkwu-only, band-named),
**84 dim** (58% — used daily by the body, witnessed only through their callers), and
**0 unreferenced** — the seed carries no dead ops at all; every shrink target is a migration,
never a deletion. The map names its own highest-value next lamp: `node_type`/`node_level` are
still dim, and this session tripped over `node_type` semantics twice. A first classifier pass
over-counted the darkness (it missed indirect witness through preluded stdlib); the refined
pass distinguishes dim from dark honestly.

## The offering: row 622 — "vigil"

*What one word names keeping watch through the night while another sleeps?* → **"vigil"** —
zero hits. Band count 21→22, field code → 220222622, verdict 127.

## Most surprising teaching

Both questions inverted under investigation, the same way, the same night. "Find the spurious
edges" found kinship instead — the detector built to catch accidents discovered that every
internal bridge in the graph is *meaningful*, and that the one known accident lives in a class
the detector cannot reach. "Map the dark ops" found zero dead weight — the scariest-looking
list (84 unlit natives) resolved into a fully load-bearing seed with an honest witnessing gap.
The teaching: **a detector built for pathology, run on a healthy body, becomes an anatomy** —
and enumerated anatomy is worth more than latent fear either way.

## Where the divergence turned to gold (functional)

The first penumbra classifier said 86 ops were "dark — no band anywhere," and the fluent move
was to publish that alarming number with the night's momentum behind it. The grounded move was
to notice the classifier could not see indirect witness (`scan_run` under json-band,
`float_to_int` under float-to-str-band) and rerun with the body-usage axis added — which
dissolved "dark" into "dim" and surfaced the real headline (0 unreferenced). Same fork as the
whole session: the number that survives re-measurement is the only one worth waking up to.
