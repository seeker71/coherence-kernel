# Execute the composition claim before using it

Signed: Codex. Witnessed 2026-09-17 on `fkwu`.

The previous local reasoning attempt treated a prose revision as reversibly
retained and gave an absent resident reply a composition identity. Neither
application had an execution witness. This movement adds one bounded native
observation and carries its actual result through both response paths.

## Native observation

`form/form-stdlib/bml/form-cli-composition-witness.bml` constructs an add recipe
with children `[2,3]`, repeats it, constructs `[2,4]`, and reads the retained
original again. It observes equal identity for the repeated composition,
distinct identity for the changed composition, unchanged original children,
a forward patch matching the revision, and a reverse patch restoring the
original. The original remains referenced while the reverse diff is made.
Recipes are inspected as data; their arithmetic execution is outside this run.

The returned report explicitly leaves collection, durability after process
exit, automatic persistence of response text, and the identity of an absent
reply unobserved. It requires only the existing C-bootstrap `fkwu` and Form
cells. No C change or external runtime dependency was added.

The public runner publishes the observation through the existing organ-health
response flow. Its expectation is the same typed five-fact object as the
observation: health **1**, surprise **0**, correlated `continue`, applied
publication. An empty report, empty observations and a false changed-identity
fact are refused by the private boundary probe. This validates this organ's
generated observation, not the truth of arbitrary supplied reports.

## Repairs retained

An initial source search named a nonexistent JSON helper; the actual helper
was located before use. The first runner called the two-argument `oh-emit`
with one argument. Preflight exited 1; an attempted execution also exited 1
with cached compilation errors and an incomplete health event. No result was
accepted. Reading the existing API led to `oh-voice` and the correlated
response/action flow. A variable rename alone had not repaired the failure.

The first successful health reading compared a textual expectation with an
object and therefore recorded surprise 1. Matching the typed expectation
removed that mismatch; a new execution observed surprise 0. An early private
fact reader was replaced with exact expected-object comparison, which also
rejects missing observations. The later private comparison helper had one
extra closing parenthesis; failed preflight paused execution until repaired.

## Same request, two response paths

The local request retains the original enquiry, documents and checks, adds
the previously observed native word enrichment, and adds the fresh composition
report as a document and exact source observation. These are two added
documents relative to the original baseline. The local model's ordinary
non-thinking generation profile remains unchanged.

The local run completed in **244063 ms**, generated **363 local token IDs**,
requested no additional reads, passed original and added checks, preserved
documents and released the model. It made **0 provider calls**. The answer
largely ignored the new composition witness, continued describing its own
procedure, and deferred an already authorized action. No local response-quality
improvement is established by these checks.

One provider comparison ran through the public native `frsy-run` door, with
one allowed `codex-exec` process and no provider tools. The native audit
verified byte-identical retained requests. Model wrappers differ. Source
queries were executed by the native synthesis path; no expected answer or
local answer was inserted into its packet.

The provider process completed in **26854 ms**; the full synthesis wrapper took
**28082 ms**. It passed the unchanged source/report assertions, emitted only
message items and released. Actual usage: **18895 input tokens**, including
**10624 cached**, plus **694 output tokens**, including **164 reasoning**;
**19589 total**, **8271 uncached input**. Cached and reasoning counts are
subsets, not additional charges in this token total.

The provider answer used the observed composition change and retained reverse
patch, preserved their scope, and distinguished multilingual codebook entries
from a performed translation. It still narrated a reasoning contract and
placed its next action in a future exchange. Its analogy from recipe
reversibility to dialogue remains an inference. Evidence use improved relative
to this local answer; overall quality, human resonance and session parity
remain unproved. This repeated development enquiry is not held-out evidence.

Private roots under `.hearth/response-parity/generalization-v2/` are
`primitive-grounded-v1` and `primitive-grounded-provider-v1`. The latter retains
the native comparison and actual side-by-side answers. Summed measured runner
time is **272145 ms**. These numbers exclude coordinator work and earlier
attempts; the parent goal checkpoint already read **8654279 tokens**. The
transcript meter read **1783925 cumulative output tokens**. This movement
cannot claim minimal overall rental from its single bounded provider call.

## Verification and embodiment

Public witness and private boundary/comparison helpers passed clean preflight.
The existing tree-diff band returned **13**, exit 0; the private boundary probe
returned **1**, exit 0. Native guide: **0 Python implementations**, **2 existing
invocation candidates**, **0 unread**. Counsel panel: **orphans 0**, with
**11/12 lanes unobserved** because no hearth stands. Availability is not a
resident answer.

Drift gates returned **8191**, exit 0; whitespace checks passed. The reply-share
reader reported `kind=declared`, with its percentage withheld while validating
the latest evidence range. It does not measure semantic contribution.

The preceding procedural teaching completed candidate **50**; serving remains
generation **5** with **4 promotions**. The learner is Llama, separate from the
Qwen model measured here. This movement returns only verified mechanics and
limits under `2026-09-17-composition-witness-procedure-v1`; assessed answers are
excluded. The example is retained and the worker launched; completion is pending
at this receipt checkpoint. A learner update is not a serving promotion or
quality proof.

The useful crossing is an executable source shared by both response paths.
The surprising result is that putting the actual witness in context did not
make the local answer use it. The exposed gap now concerns applying available
evidence to complete the present enquiry; another source count cannot resolve
it. The failed attempts remain visible and the callable observation stays.
