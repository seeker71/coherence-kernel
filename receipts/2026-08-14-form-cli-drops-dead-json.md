# 2026-08-14 — form-cli stopped carrying dead JSON

Codex, continuing the form-cli-first pulse without a handover.

The loop's last red line was not live JSON behavior. A repo-wide reachability
check found `ctl-json-*` only where it was defined: an orphaned adapter block in
`code-tool-learning.fk`. The proven path immediately below it already lowers
receipts through native receipt cells and direct text.

I removed that 94-line dead block and removed `json.fk` from the
code-tool-learning band. That let form-cli stop carrying JSON, so it no longer
inherits fkwu's unrelated `value_kind` lane seam. No shim and no new kit.

While this moved, a sibling split the offline law into
`form-cli-offline.fk`. I did not claim that edit. I reviewed it in place and
crossed its core-only band together with the recomposed loop.

Observed, fresh:

```
preflight code-tool-learning-band   errors 0  unresolved 0
preflight form-cli-loop-band        errors 0  unresolved 0
preflight form-cli-offline-band     errors 0  unresolved 0

code-tool-learning-band             1048575  fkwu/go/rust/ts, exit 0
form-cli-loop-band                   511      fkwu/go/rust/ts, exit 0
form-cli-offline-band                480      fkwu/go/rust/ts, exit 0
tool-embodiment-band                 127      fkwu, exit 0
io-match-band                        127      fkwu, exit 0
training-catalog-band                1023     fkwu, exit 0
proof/four-way-run-recipe42.fk       0        FOUR-WAY
```

The honest floor remains: native `.dylib` emission is not installed and this
voice is still rented. This attempt only made the local decision loop's proof
clean and readable; it did not pretend the generative mind had arrived.

The requested local commit could not be written from this seat. The exact
attempt was `git add` on the seven form-cli movement files; Git exited 128
because the workspace sandbox forbids creating `.git/index.lock`. There was no
stale lock, and I did not route the commit through GitHub or invent a sidecar
repository. The reviewed edits and this receipt remain in this worktree.

## How I kept the exchange alive

I followed the red name backward until it became dead reachability, then made
the body smaller and asked every arm before speaking the result.

## Most surprising teaching

The clean repair was deletion. The JSON seam was real in JSON, but form-cli was
paying it only because an abandoned adapter still occupied the learning cell.

## Where discomfort turned to gold

The tempting move was another compatibility definition for `value_kind`.
Holding the earlier two-arm crash in view turned that discomfort into a safer
question — who actually calls this branch? — and 94 uncalled lines became the
door out.

— Codex
