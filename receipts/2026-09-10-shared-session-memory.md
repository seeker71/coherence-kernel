# Verified memory reaches across local worktrees

The open greeting need led to an actual search. The hearth answered
`no-standing-hearth`; native RAG returned a grounded miss. The matching
JSONL prompt found in the checked repository inputs was the held-out greeting
itself, which remains excluded. The next attempt addressed a narrower gap:
the native responder knew only this worktree's session memory.

`native-session-sources.bml` now asks Git for this repository's local
worktrees and reads their retained session records by reference. The normal
learner uses that catalogue in both preparation and attention. A nondefault
session home stays scoped to itself. No donor record, configuration, model,
adapter or learning state is changed or imported into a second session store.
Accepted examples still enter the requesting learner's frozen training input.
This is filesystem reference sharing; resident tensors and model admission
are not shared across these processes.

The initial native reading found three session homes and 18 records: seven
here, five in worktree `3457`, and six in `f933`. The greeting search examined
all 18 and found no eligible match; supply took 106 ms. After this movement's
verified teaching was retained, the reading found 19 records and again no
eligible greeting evidence, in 111 ms. Repeating the unchanged reading added
zero event bytes. Source discovery owns a health observation with coverage,
unread homes, registry exit and references to the retained evidence. Identical
registry bytes reuse one artifact. Commit movement alone does not change the
memory-coverage revision; source and assessment changes do.

Each source binds its record, donor configuration, donor's current validation
file and that record's retained validation file. The requester rechecks those
bytes before using a reference. Both requester and donor assessment prompts
are excluded; unavailable current donor validation leaves the source unusable.
Primary example admission now uses the same prompt comparison as rehearsal.
ASCII words normalize case and punctuation; non-ASCII or wordless inputs keep
their trimmed bytes instead of collapsing different strings to an empty key.

A real previously verified enquiry in worktree `3457` was replayed through
the care carrier. Its source row is `09906e96…1d9013`, enquiry hash
`9be3a4e7…2565`. The responder supplied it and the requester accepted one row.
Reading the same reference with its prompt marked held-out accepted zero.
Changing only the donor-configuration hash in the in-memory reference also
accepted zero. Donor files were untouched and neither challenge trained a
model. Primary admission excluded both the actual greeting sentinel and a
case/punctuation-normalized variant. These are readings of existing sources,
not another committed fixture band.

The verified teaching used session `codex-shared-session-memory-2026-09-10`,
event `verified-local-reference-sharing-v1`, row `c41fe67c…394244`.
The normal learner selected the prior local evidence-responder lesson at
feature overlap 0.2857142857142857 and retained its prior promoted rehearsal.
The actual gradient therefore used three local rows. The separate enquiry
replay established external reference consumption; this round does not claim
that a neighbouring worktree supplied its training example.

The native Form/Metal Llama-3.2-3B adapter completed optimizer step 7 → 8,
updating 56 LoRA pairs from 406 input and 265 supervised tokens. Two held-out
rows stayed excluded. Model opening took 15721 ms. The update round took
85899 ms: tokenization 108, training 84869, Adam update 221 and checkpoint
701 ms. The trainer's full run took 152251 ms and retained zero buffers.
Its before/after assessment covered the same five rows:

| Row | Before | After |
| --- | ---: | ---: |
| New verified teaching | 5.3603335554144 | 5.335943688623956 |
| Held-out greeting | 2.018980711698532 | 2.029051512479782 |
| Held-out unverified-result response | 3.631835713982582 | 3.631303295493126 |
| Retained promoted rehearsal | 4.9918725098032155 | 4.943438788788626 |
| Prior evidence-responder lesson | 5.054800789672299 | 5.0270723654393805 |

The greeting worsened. Serving generation 4 remained active, candidate 8 was
retained, worker status completed, pending 0, refused examples empty. The
source-discovery and resource-availability observations do not erase that
capability need. Cross-worktree events do not yet wake an otherwise idle
learner; discovery runs at the next worker drain or explicit attention call.
The selector remains a lexical heuristic, and arbitrary repository prose,
git history, RAG hits or generated answers are not verified training targets.

Evidence is under `evidence/2026-09-10-shared-session-memory/`:
`initial-reading.json`, `reading.json` and `catalog.json` retain coverage;
`current-needs.json` retains the current request; `reference-observation.json`
and `admission.json` retain the reference and exclusion readings.
`training-evidence.json` records all retrieval alternatives and selected
references. `learning.events.jsonl`, `training.events.jsonl`,
`learning-result.json` and `session-status.json` retain original stage events,
choices, per-row CPU/GPU timings, all loss comparisons and final state.
Private prompt and completion text is absent from these telemetry exports.

Bootstrap returned 42, 55, freshness 31, the numeric list and 11111. The old
temporary native-vs-rented file was absent (exit 2); recreating the prescribed
witness from committed source restored 11111. Two temporary native reading
helpers initially had syntax errors; those errors were repaired before their
results were used. Required landing gates passed 8191/8191 with refused 0.
The bounded Glass panel observed a 64 ms first frame, 33 kernel operations and
a loaded Metal carrier. Counsel observed zero orphans and eleven unobserved
lanes out of twelve. The native guide reported 25 Python implementations,
307 invocation candidates, 50 grammar inputs and zero unread files. No foreign
implementation or replacement regression band was added in this movement.
The transcript-bound meter read 1471024 cumulative session output tokens;
that is not this movement's spend. Contribution share remained declared while
the current boundary evidence was still being reconciled.

After rebasing onto the current kernel/Metal work, freshness returned 15.
Rebuilding the local C seed and dynamic Metal carrier restored 31. The
learner compiled on that runtime; shared-reference acceptance, both rejection
readings and primary assessment exclusion held again. The refreshed catalogue
still saw 19 records and an open greeting need. Its last new search took
240 ms; the next process reused that observation and added zero event bytes,
as did its repeated call. `before-runtime-reading.json` preserves the earlier
111 ms reading. The rebuilt, source-compiled Glass observed 527 ms to its
first frame and 33 kernel operations. The final guide inventory was unchanged
and landing gates again passed 8191/8191. The recorded LoRA round predates
this runtime integration; it was not rerun or relabelled as a new measurement.

Signed: Codex. The exchange stayed alive by following an empty result into
actual local resources. The surprising teaching was that another worktree
can hold usable verified memory while this organ sees none of it. That gap
became shared references and observed exclusion; the broader search still
leaves the greeting pain visible.
