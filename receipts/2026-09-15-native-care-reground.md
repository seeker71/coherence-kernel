# Native care, re-grounded

The direction is [Form-owned compilation, resources and care](../docs/fkwu-form-native-north-star.md), with CPU and device programs admitted in RAM, dynamic Metal access, and the handwritten C seed shrinking toward zero. Current execution and the next obligations live in [runtime status](../docs/fkwu-kernel-status.md).

Direct bootstrap returns 42, 55 and the numeric list; freshness is 31 and the native-body verdict is 11111. Stable NodeID reuse returns 7. The seed is 1,077,910 bytes; its current lexical inventory contains 714 function definitions and 455 global declarations, with active-platform reconciliation. That inventory is not a semantic proof of every function.

The care reader now projects its own view directly. Eight alternating-order pairs over the same retained failed-child exchange minted 252 nodes against 305, consumed zero repeated event bytes, and preserved every compared output field except elapsed time and derived age. The observation excludes serialization and comparison work; it establishes no lifetime bound. Exact allocation counts belong to that captured workload; the renewed observation preserves the reduction without fixing those counts as policy.

Actual care covers 13 resource scenarios, process diagnostics and two resident reads. Providers deliver resources and the asking organ re-observes them. Supplied diagnostic bytes settle the evidence need; unfamiliar interpretation and child failure stay open. The resident observer releases its child, process group and both advertisements. Evidence availability is separate from the exact number of received rows, so two matching rows do not create false unease. The installed CLI's compiled default entry also completes the care exchange: 44,397 new event bytes followed by zero, three signals, two open needs and one supplied resource. Its child exits zero with no stderr, and all captured runtime/source/artifact identities remain unchanged.

Missing Glass metrics retain their requested identities as unavailable rows. A current 20-frame sample has first/max 43 ms and warm maximum 13 ms; all 20 frames meet the sample's 50 ms target. The panel still reports eleven counsel lanes unobserved without a standing hearth. Contribution share is declared and withheld, not an estimated percentage.

The recipe walker interprets record operations in Form, including field order, duplicate keys, mutable aliases, blueprint identity and explicit sharing through recipe readers. Its unchanged flattener verdict is 131071. Raw native access to exported packet wrappers remains outside that representation contract; recipe setters require string keys and refuse another kind.

Startup checks reject malformed companion metadata before default execution. Actual failed owner-file writes release the partial publisher lock and permit retry. The complete source-native CLI generation and Darwin ARM64 artifact now pass publication, ordinary cached installation, the unchanged behavior proof and all 14 artifact cases. The public artifact record binds the complete original source closure, exact stage streams and the installed care executable/companions to source stamp `d13eaeb22515beec`. Default launch permits a compatible replacement recipe; build/install attestation checks the exact published files.

This movement is grounded on origin/main `b4a172d4c`. Canonical generation takes 1,297,196 ms, platform generation 529,179 ms, cached installation 8,948 ms and the unchanged behavior proof 7,832 ms. The final source seal holds 327 original rows. The closing drift gate returns all 14 bits (`16383`) with zero unclassified paths, and the healing/share checks return `15` and `65535`, each with exit zero. Cache renewal warnings remain in the retained streams.

Independent review found and drove the startup admission, lock cleanup, missing metric and evidence availability repairs. The direct care allocation lesson was retained through the native session-learning door after verification; an optimizer update does not establish improved serving answers.
The verified source-registry lesson also completed locally with zero pending
examples and optimizer step 29; the serving selection stayed unchanged.

The closing review also found that roster-read failure could appear as an empty list at the reset door. A pure Form admission predicate now requires exactly its own positive integer PID. Ten actual cases accept self-only and refuse empty, missing-self, additional-owner and malformed readings. The door's control flow places this predicate before reset. Its effectful preflight explicitly refuses execution; no field reset was invoked. The bounded roster still cannot establish complete host liveness.

The staged-file check found one trailing space copied from the seed into the generated C. The repair removes that byte at its source, preserving every program token. Freshness then returned 15, so the standard C build renewed the local runtime and restored 31 before observation continued. The five-source C audit was executed again and retained the same function/global census. This formatting repair does not transfer runtime ownership. The complete canonical, platform, installation, behavior, artifact and care observations were renewed against the new identities; the preceding 24 files remain preserved byte-for-byte.

The next fetch brought the upstream Form string join and its name registry. The
source snapshot reader now carries the exact registry and every declared home,
preserving name-based loading. Six actual observations include execution from
copied snapshot bytes (`42`), malformed registry refusal, and an absolute home
refused before a complete snapshot is published. A helper first imported the
executable protocol door and consumed its own input; its retained failure led
to the library import used by the passing observations. Rebase preserved both
meanings without a conflict. Restoring the seed required the freshness-triggered
rebuild even though its source bytes stayed equal; observations bind the new
binary identity explicitly.
The source identifies the reason: `FK_FKB_BUILDER_ID` includes `__DATE__` and
`__TIME__`. The content and ABI identities in the north star must eventually
carry that admission responsibility in Form.

The hearth answered `no-standing-hearth`. Codex supplied the source analysis and independent review after that observation; executable measurements ran locally. The closing native guide reports one Python implementation, 195 execution candidates and no unread files, including private retained execution preparations. The closing share panel refresh takes 1,681 ms; its missing bound rollout keeps the share withheld.

The isolated field-capacity experiment remains unexecuted. Its two observer launches refused during source loading, before starting candidate processes. An automatic content check then stopped the review agent with the reason “possible cybersecurity risk.” The experiment was paused; a process reading found no matching children, and the owned source links were removed. No shared field was reset and no live allocation behavior is claimed from that preparation.

The surprising teaching was that observation itself has an allocation cost. A quiet stream can still create fresh values. The useful turn came from measuring that pressure and removing an unused projection while retaining the original care meaning. The next allocation work is owned reclamation that preserves the care path and other live owners.

— Codex
