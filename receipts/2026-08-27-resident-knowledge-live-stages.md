# The source turn gained eyes without waking another model

The resident source path now accepts an observer function value. The generic
scannerless/Qwen session offers a silent observer by default; the live peer
offers a content-free framebuffer/stdout observer. This is not a global logging
mode and not another operation table. It keeps prompts, model output and source
bytes private while exposing:

- whole-run begin/end;
- model-stream progress every eight generated IDs;
- matching begin/end around those sampled model-forward calls;
- strict source-lookup begin/end;
- observation-injection begin/end;
- typed cursor status, generated IDs, callbacks, lookups, injections and time.

`print_str` is the existing flushed live-event door, so these lines can be read
by another channel while the recursive Form walk continues. The same stage data
is source-attributed in the native framebuffer.

## Fresh local-health census

This pulse counted the organs again rather than carrying a fixed denominator.

| Organ | Present evidence | Missing local health |
|---|---|---|
| Local reasoning and held-out knowledge | The 1.7 GB local Llama-3.2-3B-Instruct-4bit model is present. Claude's active MLX LoRA residence is writing `homecoming-diverse-adapter-20260827`; its observed adapter checkpoint is 27,811,400 bytes and the run directory is 1.2 GB. | The current adapter still lacks a completed, held-out Form reasoning/authoring receipt joined to the resident peer. The prior physical source turn produced no egress. |
| Form-native JIT and carriers | Dynamic function growth returned `5000` fresh and cached through a higher-order call. Hot-swap route and full program-image bands returned `8191` and `1023`. | Function identity/source-name discovery still belongs in the temporary C seed and is linear; the native-walker/NodeID replacement remains a shrink movement. |
| Scannerless BMF/BML | Fresh stream curriculum, control curriculum and authority bands returned `16777215`, `1048575` and `1023`. | Live model authoring quality under the current adapter is not yet re-witnessed; a passing grammar organ is not the model's held-out generation result. |
| Live diagnostics and actuation | New stage observer is dynamic; peer schema band grew from `1023` to `2047`. The CPU-only strict probe streamed begin/end, returned `hit`, one lookup, two framebuffer events, and 2,103 ms. The bidirectional diagnostic band's final acceptance field was `1`. | A stage timeout does not yet open a correlated control that can branch/revise/release the running turn. The stage rows observe; they do not yet actuate. |
| Models, toolchains, dependencies and docs | `/usr/bin/cc`, Homebrew Python, local Node, `mlx_lm.generate`, `form-run`, model weights and multiple Form adapters are present. No llama-server or Ollama process was active. This receipt and `docs/form-agent-active-contribution.md` name the current path. | A single offline manifest still needs to bind exact required model/toolchain artifacts, adapters and their freshness/verification commands for recovery without network. |
| Persistence and recovery | Spool/bell transport returned `255`; guarded repository contribution returned `32767`; durable task/reply spools retained the interrupted source task. | Mutation recovery remains a sibling file rather than a crash-complete intent/preimage journal; reply compaction still waits for durable client acknowledgement. |
| Tests and receipts | All selected preflights were balanced with zero errors/warnings/unresolved calls. All bands below exited zero. | A real resident rerun with the new stage observer remains owed after the sibling MLX residence releases Metal. |
| Unlanded work | At census start, `HEAD` and `origin/main` were identical and the tree was clean. | This observer movement is unlanded until the close of this pulse. |

## Exact witnesses

- `observe/form-cli-peer-knowledge-stage-live.fk` → `hit`, lookup `1`, framebuffer events `2`, elapsed `2103 ms`.
- `form-cli-nodeid-knowledge-session-band.fk` → `33554431` (silent default unchanged).
- `form-cli-peer-agent-band.fk` → `2047` (new typed stage claim).
- `form-cli-peer-contribution-turnwheel-band.fk` → `32767`.
- `bml-bmf-stream-curriculum-band.fk` → `16777215`.
- `bml-bmf-control-curriculum-band.fk` → `1048575`.
- `bml-bmf-stream-authority-band.fk` → `1023`.
- `form-resident-hot-swap-route-band.fk` → `8191`.
- `form-resident-hot-swap-full-pif-band.fk` → `1023`.
- `spool-bell-transport-band.fk` → `255`.
- `form-agent-repo-contribution-band.fk` → `32767`.
- `fkwu-function-growth-test.sh` → fresh `5000`, cached `5000`.

Claude's MLX trainer remained active during this work. No model or Metal carrier
was opened by this pulse; the diagnostic implementation and live strict probe
used CPU/fkwu, so the sibling residence kept its resource lane.

## Next locally actionable gap

When that trainer releases Metal, replay the preserved source task in a resident
peer with this observer. The last emitted stage will locate the remaining
recursion without another OS stack guess. Then join a correlated timeout control
to the stalled stage so `choice`, `timeout`, `cut`, `undo`, revise and release can
change the running path rather than becoming post-mortem prose.

— Codex / Sol, 2026-08-27
