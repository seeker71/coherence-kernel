# Repository knowledge enters native training

Codex, 2026-09-11. The 56 LoRA A/B matrix pairs were never the number of
examples. A native source export now holds 1,072 distinct pairs: 78 technical
documentation sections, 206 literal primitive contracts and 788 attributed
historical teachings. Actual split: 897 training, 175 held-out knowledge rows,
plus the two existing public validation sentinels. These are not 1,072 newly
reviewed sessions, nor 1,072 learned skills.

The executing corpus organ checks hashes, manifest counts, all 19 source files,
and train/held-out prompt and completion overlap before publication. The live
reading found zero overlap. Existing answer-word grouping is preserved; whole
documents stay on one side. Exact duplicate pairs count once. Four known
edge-contract omissions remain named. Registry callbacks are read as inert
declaration data, never imported into the native runtime. The initial import
failed preflight on proof-only symbols; source-data decoding repaired that seam.

Dataset identity:
`a58cb5a0d79bb39617fcb371156af93bc7fff835dd440be1fb369ed8dc3c10bd`.
Local content versions, training state and source references stay below the
private session-learning home. The published recipe and examples are documented
in `docs/repository-knowledge-learning.md`. Session status distinguishes retained
examples, distinct sessions and the repository corpus from adapter matrix units.

The interrupted first attempt admitted the model and completed validation but
left no gradient checkpoint. Its processes were confirmed absent on continuation;
the original streams and process evidence were preserved before retry. The retry
added per-row start, forward-complete and backward-complete observations. Row 13
had 1,259 input tokens; its forward pass took 35,927 ms. The first complete round
then consumed two source sections / 1,832 input tokens / 1,684 supervised tokens,
updated all 56 matrix pairs and published optimizer step 7. Wall time was
370,627 ms. This is native Llama-3.2-3B training, not Qwen or Whisper training.
The requested eight-round run remained active at this reading. No serving
selection or whole-session quality verdict was changed.

The larger corpus is real; throughput at this size still needs care. An OS sample
located the long span in Metal completion during backward execution. It later
completed, so a wait was not relabelled a dead process. The source-owned checks
replace the new redundant corpus fixture score; existing session-memory behavior
checks remain and now distinguish two events from one session.

Glass reading: 123 rows, 273 unread, 18 asks, 44 recipes, 14 ms; 24 of 24 held
findings shown at 100x30. Several physical observations remain missing. Share
was declared/unmeasured and no percentage is claimed.

Kept alive: missing examples became a native dataset and an actual weight update.
Surprise: the immediate limit was consumption and long-row cost, not matrix count.
Discomfort became useful when the interrupted attempt exposed the need for
stage evidence and preserved retry history. The full session audit and 95%
whole-session equivalence remain open.
