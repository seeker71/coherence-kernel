# BMF values reach linked PIF descriptors

Date: 2026-08-27
Movement: selected `generic-bmf-grammar-to-full-pif-recipe-emission` gap
Authors: Arendt and Codex, with Planck and Beauvoir as live adversarial co-creators

## Fresh census

The adaptive map was re-counted rather than inherited: **59 observed / 45 ready / 14 gaps / 0 unknown / 0 invalid / 762 permille**, selecting `generic-bmf-grammar-to-full-pif-recipe-emission`. PR #520's linker and this compiler stage are internal causal parts of that row, not new denominator entries.

The Mac retained the 27 GB Qwen Q8 GGUF, the 1.7 GB fused Form authoring checkpoint, `fkwu`, Metal and MLX carriers, `libmlxc`, the local compiler/toolchain surface and live diagnostic documentation. Strict repository fsck exited zero. No `llama-server`, Ollama or `mlx_lm` process was present, and this movement opened no model, Metal, HTTP or host execution lane.

## Co-authored implementation

`form-program-image-bmf-subset-lower.fk` accepts an explicit bounded grammar value and typed result templates. Before any IR escapes it validates:

- exact spec, rule, limits, byte-class and template shapes;
- integer-only positive limits beneath compiler-owned ceilings;
- unique and resolved rules/classes, an existing start rule and acyclic references;
- only `lit`, variable `seq`, ordered `alt`, `ref`, and `cap(run)` patterns;
- canonical byte ranges and `cap(run)` class resolution;
- per-rule definite captures, including intersection across alternatives;
- exact pattern/literal/capture/result and emitted row/string/function bounds.

The lowerer derives variable image-local function, node and string definitions from the data, including distinct entry and start roots, and sends them through the unchanged symbolic linker. Query-sequence and allow/deny-choice grammars produce different linked topology; a same-topology literal mutation changes the IR with no Form definition change. Exact replay recomputes both IR and linked numeric streams. Duplicate/unresolved/cyclic/unsupported/over-bound inputs and forged spec/IR wrappers refuse with IR=`nothing`.

## Observed surprise and actuation

The first expanded band returned **252**, not 255. Per-stage observation showed retained spec equality `1`, fresh result shape `1`, exact symbolic IR replay `1`, and generic equality over freshly realized linked numeric streams `0`. A correlated framebuffer control selected `revise` (`2`). The compiler gained recursive exact integer-stream equality rather than weakening the identity claim; the widened band then returned **65535**, was re-observed as `[65535,65535]`, and the next correlated control selected `continue` (`0`). Four outbound/inbound events retained the movement.

Fresh independent gates:

- source preflight: balanced, zero errors, warnings and unresolved calls;
- focused compiler-stage band: **65535**, exit zero;
- adjacent symbolic-linker band: **65535**, exit zero;
- independent component audit: no landing blocker.

## Changed health map and next gap

The map remains **59 / 45 / 14 / 0 / 0 / 762**. A health flip would still be false: the emitted program currently returns a recursively typed grammar/result descriptor. It does not yet consume raw bytes, advance a cursor, retain captures or execute semantic results; it also lacks the canonical symbol graph, content-vouched full envelope and admitted FPCPF execution.

The next local join is exact: lower the descriptor into `[bytes, at, captures] -> [code, at, captures, semantic]`. Ordered `alt` retries only on `no`; immutable original state supplies undo; ready supplies cut; incomplete, malformed and semantic timeout propagate; decision miss remains `nothing`, allow remains `1`, deny remains `0`; outer timeout remains output-count zero. After that, existing PIF symbol, byte-container, decoder and full-call organs can carry identity and execution without another health row.

Kept alive: a completed compiler stage and a completed raw parser remained separate observations.
Most surprising teaching: the grammar's missing serialization was already solved; the newly exposed seam is semantic state, especially definite captures across choice.
Discomfort turned to gold: `252` and the duplicate entry-root warning both became stronger identity structure before either could harden into a false `46/59`.
