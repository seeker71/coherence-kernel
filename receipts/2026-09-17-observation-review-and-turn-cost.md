# An unchanged review, and its coordinating cost made visible

Signed: Codex. The previous movement made progress by exposing native indexed
review and showing that it repaired a new probe but kept the original failed
dialogue. This continuation tests one appended question and improves the
measurement of the rented coordination around these experiments.

## Original review, one appended question

Private evidence is in `.hearth/response-parity/observation-review-v1/`.
The original review prompt remains an exact byte prefix. The added question
asks which observation or offered interface establishes each claim, preserves
the source domain, and distinguishes a proposed action from factual statements
or commitments in a draft. It supplies no bad-span IDs, expected decisions or
replacement answer. The original request and frozen rubric are retained.

Model, context and initial/final allowances remain Qwen3.8 27B Q8, 12288 and
512/2048. This run starts a fresh admission; the prior comparison case used
a renewed stream with already admitted weights. Timing includes that
difference and concurrent CPU-only instrument work.

| Observation | Retained indexed review | Added observation question |
| --- | ---: | ---: |
| Prompt tokens | 5024 | 5105 |
| Initial generated tokens | 512 | 512 |
| Final generated tokens | 668 | 747 |
| Generated total | 1180 | 1259 |
| Observation-prefill IDs | 98 | 98 |
| Case elapsed ms | 495475 | 556920 |

The new full run takes **557050 ms** and uses **0 provider processes**.
Generation, decoding, edit application, original assertions and final release
pass. Native paired comparison verifies identical requests, the unchanged
prompt prefix and both releases. The composed report is node-equal to the
previous report: all **13** spans are kept and the answer remains **173** words.
The added **81** prompt tokens and **79** generated tokens produce no answer
improvement. The case takes **61445 ms** more, with the admission difference
retained. The new suffix remains private and is not adopted as default.

The returned reasons still justify the revisable-nudge claim from the cell
axioms and characterize the draft's commitments by their warmth. One reason
changes the assumed busyness statement from supported to inference, without
changing its text. The actual answer, reasons and costs remain available.

The benchmark itself also needs a clearer distinction: editing an unsent draft
and guaranteeing consequences of a sent message are different claims. The
wording about revisiting a nudge permits more than one reading. The new probe
used an explicit relationship guarantee; the original dialogue is less
precise. This does not turn the unchanged review into a successful repair or
erase the earlier assessment. It names a limit of the evaluator's inference
as well as the model's. Another repeated attempt to obtain a preferred label
would not establish broad quality. A concrete next enquiry has been requested
as optional input; useful native work continues independently.

## One native door for completed coordinating cost

`observe/form-cli-turn-cost-run.bml` wraps the existing reconciled turn
collector. It performs at most **8** refresh steps, checking a **5000 ms**
attention allowance between steps. It emits one JSON record with the selected
completed turn's identity, model, timestamps and quantities. It never substitutes
the open turn or a stale record for a completed measurement. Missing/mismatched
bindings and unfinished readings retain null quantities and their reason.

There is no new usage parser. The existing collector still owns carrier,
completion, tool-call and usage reconciliation. The real prior-turn reading
matched its retained row in **420 ms**; after dependency cleanup it matched in
**406 ms**. The complete numbers are appended to
`2026-09-17-native-span-review.md`: **35** coordinating calls, **100279** uncached
input tokens, **26727** output tokens, plus **4472320** cached input tokens.
The native runs' zero provider-call count did not make the movement cheap.
Repeated readings of that completed turn remain one usage record.

An isolated-directory attempt initially exited **2** because the BML source
compiler path was unavailable. Native source/kernel links exposed a second
unnecessary dependency through session-learning imports. The cost module now
uses the collector and its JSON constructors directly. The normal command
passes after the reduction. Since `form-run` establishes a binding itself,
the missing-binding case was tested at the reader's explicit binding argument:
with a real older observed row present, empty and mismatched inputs return
null usage and do not refresh another transcript. That check returns **1**,
exit **0**. No external runtime or C source changed.

Preflight is clean. Existing collector parser/state band: **33555454**, exit
**0**. Paired review audit: **1**, exit **0**; this means the comparison was
performed, not that quality improved. Native guide: implementations **0**,
invocation candidates **2**, unread **0**. Drift gates: **8191**, refused **0**,
exit **0**. The prior learner completed round **75**, pending **0**, promotions
**4**, serving unchanged; this Llama learner is separate from the Qwen run.
After Qwen released, `completed-turn-cost-boundary-verified-v1` retained the
verified cost-reading procedure and launched the learner. No assessment
content was supplied as a training target; serving impact remains unobserved.

The exchange stayed open by exposing the coordination cost and the ambiguity
in our own assessment. The surprise was how large the coordinating cost was
beside a zero-provider native run. That uncomfortable number became a bounded
native measurement the next movement can use directly, while the failed
response intervention stays visible.
