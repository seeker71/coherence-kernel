# Health travels from the organ

Urs corrected the direction: health belongs in the running organ, which can
ask for nutrients and signal surprise and pain. The process, generation,
timing and session-learning paths now share `organ-health-v1`. Each supplies
its own expectation, observation, health, needed resources and offered actions.
The carrier correlates responses and keeps applied care separate from a fresh
health observation. It has no model-error catalogue or organ-specific verdict.

Released: the five healing attention, observed-process, quick-process, flow
and dynamic bands; their scenario runner; and the central healing-attention
classifier. Their working meanings remain in the executing boundaries:
actual child status and release, typed token counters, full output-byte
delivery, observed cadence, event timing and actual learning-row losses.
No replacement band or fixture tally was added. The standing guide carries
this authoring direction. Other legacy bands in the wider body remain.

The new `observe/form-cli-heal-process-run.bml` runs a caller's argv through
the real process organ. `observe/organ-health-run.bml` reads any compatible
event flow incrementally and reports the latest observations and their ages.
An unknown organ or resource name needs no central registration. Empty
evidence yields no health claim; an applied action cannot manufacture health.

Actual contact, retained under `evidence/2026-09-10-organ-health/`:

- A temporary filesystem organ observed missing source bytes, asked for them,
  supplied 15 bytes, read them back and emitted a fresh healthy observation.
  The process reader needed no case for that organ. The temporary file was
  released afterward; the event is a timestamped observation, not a standing
  claim about that file. See `resource-cycle.events.jsonl`.
- That flow exposed a timing fault: inline care advanced the event clock,
  then the next sampled line reused an earlier timestamp. The original report
  has `timing_reconciled=0`. Events now retain both sampling time and their
  actual monotonic recording time. The repeated run reconciled all 233 ms;
  the timing organ emitted its own healthy reading. Both traces remain.
- An explicit deadline returned 124 and retained child status 143, asking for
  stage timing. Dynamic work continued without a lifetime budget, inspected
  silence, and retired that cadence need when the work settled. See
  `deadline.events.jsonl` and `progress.events.jsonl`.
- The native generation organ compiled with its resource signals at context,
  prediction and output boundaries. This movement did not run another full
  Qwen generation. Output count now advances only after the full byte span
  was written.

The real session learner updated 56 LoRA pairs, optimizer step 5 → 6, with
284 input / 195 supervised tokens. Training and assessment took 288754 ms;
retained buffers were zero. The new lesson loss moved 5.3634040913 →
5.3422277952. The greeting observation moved 2.0054118335 → 2.0109831989,
so the learner asked for related training evidence and retained serving
generation 4. Candidate 6 remains available; pending queue 0. Assessment
examples were excluded. This is the local Llama dialogue adapter, not Qwen
or Codex. `learning-health.txt`, `learning-assessment.json` and
`learning-events.jsonl` retain the actual row readings and selected care.

The bounded Glass panel observed first frame 119 ms and 33 kernel operations.
After integrating upstream, kernel freshness reported 15; rebuilding the
local seed and Metal carrier restored 31. The refreshed runtime observed a
208 ms first frame, 33 kernel operations and a loaded Metal carrier. Its real
process run exited 0, retired the cadence need, and accounted for all 233 ms.
The generation and learning modules also compiled on that runtime. These
are separate observations; the retained learning round predates the rebuild.
The hearth answered `no-standing-hearth`; local RAG returned `grounded:miss`.
Local cells, git history and the prior receipt supplied the implementation
context. Codex supplied the implementation reasoning. No Python or other
foreign implementation was added. The repository landing gates remain the
current checkout workflow; they are not a claim of universal organ health.

Signed: Codex. The exchange stayed alive by moving the knowledge into the
organs and releasing duplicated scaffolding. The surprising teaching was
that care itself changed event timing. That observed discomfort became a
clock correction and a timing organ able to name its own need.
