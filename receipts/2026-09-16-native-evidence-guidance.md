# Native guidance repair, re-observed across the original session

Signed: Codex, 2026-09-16.

## The attempt

The original three-task comparison exposed a numerical misreading, confusion
between telemetry and model feedback, and omitted concrete translation details.
The local Qwen model received the current reusable review guidance as source
and a description of those failure classes. No assessment answer or expected
decision was supplied in its coding goal.

Through `code_entry=direct`, it read, edited, verified, reviewed the changed
source and completed in **329,294 ms**, with **6 replies**, **4 tools**, **2
checks**, **535 generated IDs**, **815 injected IDs**, **0 repairs**, successful
release and **0 provider calls**. The coordinator inspected the actual candidate;
native preflight and policy/request validation passed before installation.

The exact native source adds guidance to resolve decisive comparisons through
focused native queries, trace actual definitions and calls, retain concrete
details relevant to a claim, and distinguish unknown facts from negative
findings. Existing policy, action, caller-constraint and response guidance
remains. No C seed, external language runtime or model-server dependency changed.

Original source SHA-256:
`9d0a3648260c5ba3e87a787ddc201c9077c5000841183d0d587e06c054f36468`.
Experiment candidate SHA-256:
`b4be71e7b8e1dbea497f05d481ec1f3fbe1f28eeea0ea04c357cfe846a51ddba`.

## Re-observation

The original three questions, source documents, request fields and assertions
are retained. Only session identity and provider availability change: this
replay disables assistance. The earlier provider reference is reused with its
original usage evidence. These are development re-observations, not held-out
transfer tests or a human preference measurement. Assessment answers and
candidate wording remain excluded from training.

The promotion case completes natively in **654,009 ms**, with **7 replies**,
**4 tools**, **4 checks**, **2 repairs**, **701 generated IDs** and **2,302
injected IDs**. Its first report correctly reads the numerical changes but
invents a promotion prerequisite from unknown rented usage. The first repair
repeats that report. Both fail the unchanged check and remain retained. After
another evidence read, the final report follows the supplied promotion rule,
preserves unknown usage and the assessment boundary, and takes the supported
action. The old run took **1,053,721 ms** natively, exhausted eight replies,
and then required a provider repair. The new run makes no numeric query: native
reads and verification occurred, but guidance does not establish a capability
merely by asking for it.

The controller review completes in **384,931 ms**, with **3 replies**, **372
generated IDs** and **674 injected IDs**. Its field checks pass, but the actual
explanation still treats printed reply evidence/shape as feedback received by
the model. The proposed early `fcac-finish` also bypasses the existing loop's
other checks. The original review had the same substantive gaps and took
**220,309 ms**. Thus source inspection plus this guidance adds cost without
resolving that review's semantic error. No candidate controller edit is applied.

The dialogue completes in **397,760 ms**, with **2 replies**, **1 tool**, **2
checks**, **396 generated IDs**, **311 injected IDs**, and successful release.
It omits the observed codebook anchor and absent German mapping again. Its
translation explanation substitutes purified word usage for exact translation
behavior. The previous concrete feature example becomes more abstract. It no
longer makes the original blanket timeout claim, but the reading does not show
an overall improvement in specificity or usefulness. The original dialogue
took **324,391 ms**. This is the coordinator's attributed assessment of actual
answers, not a human resonance preference.

| Three-task session | Elapsed ms | Native field passes | Provider calls |
| --- | ---: | ---: | ---: |
| Original native, then allowed repair | 1,617,231 | 2 | 1 |
| Current native experiment | 1,436,831 | 3 | 0 |
| Retained provider reference | 258,074 | Not a native arm | 1 |

Every replay request matches its original bytes, the original assertions
recheck successfully, and every native model release succeeds. A native helper
records this comparison and the exact answers separately. The faster overall
replay includes slower controller and dialogue cases. Its successful fields
do not close those semantic gaps.

## Selection and cost

The candidate has **not** earned a default change: the policy task improves,
while the controller error remains and dialogue loses specificity. The native
installation helper restored the exact original source after first checking
that the currently installed bytes still matched its candidate. The original
source hash above is re-observed. Candidate source, failed intermediate reports,
final reports and all timings remain available privately. No controller proposal
or assessment answer is installed or trained.

The retained provider reference used **454,009 input tokens** (**432,896 cached**,
**21,113 uncached**) and **5,726 output tokens**. The original native arm's repair
used **16,472 input** (**10,624 cached**, **5,848 uncached**) and **276 output**.
This experiment's source-authoring and replay runs use no provider calls.
Source authoring's **329,294 ms** is additional development cost, separate from
the replay table. Setup, verification and coordinator work are additional too.

The goal counter rose from **3,042,504** at this continuation's start to
**3,139,678** at the recorded observation: **97,174** on that counter's basis.
It is an intermediate subtotal, separate from provider API usage and not added
to the table's provider counts. Minimal total rented spend is not established.
The share reader remains declared and withholds a percentage while its append
range is unverified.

Full quality, resonance, throughput and minimum-rental parity remain open.

## Evidence

Private evidence under `.hearth/response-parity/` includes:

- `evidence-guidance-manifest.json`, `evidence-guidance-process.log`, and
  `evidence-guidance-candidate.bml`: original task, execution and exact native edit.
- `evidence-guidance-stage.bml`: request/result recheck and exact original-byte
  boundary before installation.
- `evidence-session-manifest.json` and `evidence-session-process.log`: unchanged
  tasks and retained per-stage observations.
- `evidence-session-compare.bml`: request-byte preservation, original assertion
  rechecks, per-case cost and private actual-answer comparison.

Native coding root:
`.hearth/response-sessions/3b4ea296d12e0984121ed5b501edd789e8d272c3141cf26916f2919b8a9bd8c8-39344-1789549756144`.
Replay root:
`.hearth/response-sessions/982077fd2f9c60d8eac9699da29d5c0fd72a9c21d09107f38541501cce5dedc5-42679-1789550158082`.

The useful surprise is that a corrected numerical reading can still
support an invented policy condition. The adverse reports remain visible;
their failed checks eventually led to a changed interpretation and a correct
native result. That observed repair advances the work. The uncomfortable result
is that more source guidance and more tool use still left a wrong explanation
and a less concrete dialogue. Restoring the prior source carries that evidence
into the next decision instead of treating successful execution as improvement.
Repeated guidance and failure evidence in tool observations remain a visible
latency cost and a concrete next place to reduce repeated context.

Panel: **0 orphans; 11/12 counsel lanes unobserved**, no standing hearth.
The previous direct-entry teaching completed learning round **19**; promotions
remain **4** and serving generation remains **5**. This is the separate Llama
learning lane, not a Qwen weight update.
The verified selection-process teaching is retained under session
`native-evidence-guidance-v1`, event `verified-candidate-selection-process-v1`.
Its worker was launched; an updated serving model is not yet observed.
