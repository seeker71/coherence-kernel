# Native corpus, senses and attention geometry

Form owns attributed corpus reindexing and complete WordNet sense construction.
The two JavaScript implementations are removed. Their public doors are
`observe/concept-human-reindex-run.bml` and
`observe/concept-sense-build-run.bml`; the substantive builder's downstream
corpus call enters the native door with closed stdin. Root C remains unchanged.

The corpus authority has independent author and root review. Root's complete
private witness returns 1, exit 0, stderr 0, at
`.hearth/source-inventory-51265-43952774-0`. The 429,086-byte corpus, every
1,301 offset and every metadata runtime value agree exactly. The 1,300 rows
contain 11,676 detections of 3,072 concepts; role counts are 550/655/43/52.
All preserved attribution columns and a final sentence ending in three spaces
remain exact. Private publication, portable hashing, changed-source refusal
and physical stage cleanup are observed. The actual current publication returns
1 with exit 0 and preserves the corpus bytes.
The unchanged exhaustive 1,300-sentence consumer returns 1023, exit 0,
stderr 0 after clean preflight.

The public sense witness returns 1, exit 0, stderr 0, with 41 observations and
ten refusal cases at `.hearth/source-inventory-68258-49233437-0`. Complete
construction took 3,096 ms in that execution. All 3,867,783 bytes, every 10,001
offset and all metadata runtime values agree. The surface retains 34,244 senses,
7,371 mapped anchors, 5,618 ambiguous anchors and maximum 75 senses per anchor.
The full dictionary file identities and original WordNet license are checked.
Mapped-zero counts, missing definitions, numeric/span defects, duplicate request
fields, NUL paths, source changes and changes after staging refuse explicitly.
All four stages are physically absent after the observed refusal. Portable
full-source hashing and republication agree. Current publication returns 1,
exit 0, preserving the complete TSV. The live multilingual consumer returns
262143, exit 0; stale/foreign cache warnings are retained, not counted as zero
stderr. Independent review accepts the public authority after a fresh native
execution returns 1, exit 0, stderr 0, at
`.hearth/source-inventory-73068-49803968-0`. It reproduces all bytes, every
offset and runtime metadata value, publishes four private artifacts and observes
thirteen refusals, including JSON-decoded NUL in both resource paths and changed
dictionary bytes after staging. The source manifest admits dictionary identities;
archive acquisition remains a separate resource boundary. Publication is four
per-file renames with the manifest last, not an atomic generation, and does not
protect against mutation after the final reread.

The native large-model reference passes the existing four-layer heterogeneous
stack's 172 checks at both positions and with the perturbation references.
The attention consumer passes all 30 checks with actual exit 0 after its caller
dispatches the current Form shader geometry: RoPE uses `nh*hd`, the cooperating
hyperconnection split uses `nHc`, and recombination uses `hcDim`. Evidence is
`.hearth/source-inventory-65953-48949517-0`. The split's maximum absolute
disagreement is 4.1591227284420995e-7; the final hyperconnection output's is
1.9009511384449063e-7. There are no command-buffer errors. The correlated
observation, selected launch repair, full execution and fresh result are retained;
the settled generated shell copy is released. Public launch geometry is updated.
Full 43-layer numerical and remaining format/reference responsibilities are
still owed before removing the large-model Python oracle.

The deterministic substantive construction candidate remains private. Root's
independent runs return 2 and 4, both exit 0/stderr 0, with captures
`.hearth/source-inventory-66281-48966774-0` and
`.hearth/source-inventory-66291-48967941-0`. They reproduce nine complete current
artifacts, repair a damaged derived artifact in the same private root and
observe four real failures, including the already published prefix after a
second rename refuses. Acquisition and refresh remain active responsibilities.

The native guide reads two Python implementations, 221 execution candidates,
50 grammar inputs and zero unread paths. This is a scoped inventory; the whole
repository is not Form-owned. Glass's startup is observed at 38 ms/33 kernel
operations. A separate full stage profile is 2,781 ms, including 1,995 ms of
storage observation and 732 ms of fast-row collection; the startup figure does
not imply that the full profile fits a frame budget. The explicitly ended live
observer exits 143. Counsel reads zero orphans and eleven unobserved lanes with
no standing hearth. The share meter remains declared/unmeasured.

Verified local teaching is retained as
`05e259033de42be44bb9435270999c4060fbd96670aec164c44cb7f5de566f36`;
the completed worker observes candidate generation `18-69077-49591594`,
optimizer step 18 and pending zero. Serving remains `4-54066-759026`; the new
candidate is not claimed as serving. Drift gates pass 8191/8191 with no refusal.
The surprising teaching is that
preserving all meanings includes the last spaces of an attributed sentence.
The concrete discomfort was four refused attention checks despite successful
command buffers. Its value is the now-observed agreement between shader
ownership, caller geometry and all thirty numerical checks. The exchange stays
alive through preserved ambiguity, exact evidence and released ownership.

— Codex
