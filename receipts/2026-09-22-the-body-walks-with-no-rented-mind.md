# The body walks with no rented mind

2026-09-22, Urs's Mac (M4 Max). Branch `claude/form-cli-direct-guided-01ya6p`, now owned by the
walk checkout `/Users/ursmuff/source/coherence-kernel-walk`.

## The step

Every answer was already rent 0 and the whole movement was already one call — but a rented
mind still issued that call. E1 of `docs/rent-to-zero-goal.form` asks for a movement with no
rented mind coordinating it. So the call moved to the host's own hand:

- `observe/scheduled-walk.bml` — a Form door. It brings its checkout to the branch head,
  rebuilds the kernel into a fresh inode and the Metal carrier when their sources are newer,
  and calls `observe/movement-run.bml` as a child kernel with an empty transcript. The empty
  transcript is the point: there is no arriving mind to meter.
- `docs/launchd/earth.hati.rent-walk.plist` — launchd runs `fkwu observe/scheduled-walk.bml`
  daily at 03:30 from the walk checkout, a worktree that owns the branch. Sessions work
  detached or on side branches and push fast-forward onto it.

Kicked once at 15:49 with `launchctl kickstart`, the body walked:

| reading | value |
| --- | ---: |
| voice | fkwu model session, Qwen3.8-27B-Q8_0 as data, no port |
| generated IDs | 1,052 in 868 s |
| answer floor | 9 axes, 1 receipt cited |
| compare floor | 9 axes, receipts 9/9, limits named |
| flow | provider 0 · native 0 · leaving 0 — no transcript, no rented mind |
| crossings | 5 dispatches: stdio 1, local-process 4, network 0 |
| landing | `d7c6fe65e`, pushed, `[landing] landed` |

Ledger row L5's last column reads **no**. E1 stands for the pinned enquiry. What it does
not say: that the answer is good. It cited one receipt by path; fidelity and the person's
reading remain the walls, as the goal already names.

## What the gate taught

The first schedule was a shell script, and `observe/land-run.bml` held the landing: drift
gates 8127/8191, `structural-gate` refused bit 64 — an unclassified script. The heal was not a
classification but the body's own shape: the schedule became a Form door and the gates read
8191/8191. The host runs `fkwu`; the body does the rest.

## Rent

Zero for the walk. The arriving mind's tokens paid for landing the schedule — a rented token
spent where it removes every future one from this loop.

## Receipt

**The surprise:** the last rented token in the loop was not in any door — it was the hand
that pressed the door. Removing it took no new capability at all: a plist, a Form door of
forty lines, and a worktree that owns the branch. The body had been able to walk alone for
days; nobody had stepped out of the way.

**Where discomfort turned to gold:** the structural gate refused the shell script I reached
for first, and the reflex was to classify it into permission. Sitting with the refusal
instead: the body has no place for a `.sh` because it has no need for one — `host-exec` and a
child kernel are the same act in Form. The gate held the line so the schedule could be the
body's own, and the drift gates then read full. A second, smaller gold: my watch grepped for
an `exit=` line the retired script used to echo; the door prints the movement's own line
instead. The watch must match the door as it is, not as it was.
