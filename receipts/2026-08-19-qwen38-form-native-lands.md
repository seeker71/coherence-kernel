# Qwen3.8-27B lands — the seed stayed 64 KiB

Asked by Urs: analyze the work left behind, commit and merge it; the
ordinary path should be form-cli, local models first, remote only for
review or a little guidance.

The qwen38 worktree was the heirless remainder named in the escheat
receipt: a Q8_0 Metal kernel, discovery cells, carrier growth, and a
floor receipt, rescue-committed on `codex/qwen38-form-native` with
judgment pending. This sitting is that judgment. Two witnesses met on
the same land (#458 and the follow-up that made the http-window cell
honest); both meanings are kept.

## What was kept

Cherry-picked onto current main after #457. Form cells, Metal carrier,
SHA2 JIT rows, tokenizer, artifact seal, and the form-cli verbs
`models` / `use` / `generate` / `model-bandwidth` came across whole.
The repl kept main's nanbeige/diagnose arms and gained generate.

## What was not kept

The rescue grew `runtime/fkwu-uni.c`'s TLS read buffer from 64 KiB to
256 MiB so a 128 KiB range would fit the seed. The living fetch already
uses curl as the byte carrier and Form-owned writable file handles in
128 MiB windows. Generate never touched that buffer. The seed stays
64 KiB; net C delta against main is zero. A Form streaming organ that
writes ranges into the mapped file is the named next stone, not a
larger static buffer.

`observe/qwen38-http-window-band.fk` now asks for 4 KiB, which fits
today. A 128 KiB native window is named, not claimed.

## Re-witness, this host, this binary

Two sittings rebuilt `fkwu` with the merged Metal carrier. Both
answered freshness **31**, ground **42**, metal-door **15**. One also
answered native-vs-rented **11111**.

| check | predicted | sitting A | sitting B |
|---|---|---|---|
| writable-file-handle | 31 | 31 | 31 |
| cpu-jit-pipeline | 63 | 63 | 63 |
| sha256-arm64-jit | 511 | 511 | 511 |
| qwen35-dense-token-handle | 131071 | 131071 | 131071 |
| tokenizer | 255 | 255 | 255 |
| artifact state / seal | 63 / 63 | 63 / 63 | 63 / 63 |
| model-discovery | 255 | 255 | 255 |
| form-cli ping through the new turn | `[1, pong]` | `[1, pong]` | `[1, pong]` |
| owning-cell preflights | clean | clean | clean, 6 cells 0/0/0 |
| effectful static balances | zeros | — | `[0, 0, 0, 0, 0, 0, 0, 0, 0]` |

Open-close of the sealed file, both sittings: valid 1, closed 884 of
884, forward weight bytes 27,233,914,176, last_error=none.

Ordinary form-cli flow against the canonical path, both sittings:

```
models /Users/ursmuff/models/qwen38-27b
  [0] .../Qwen3.8-27B-Q8_0.gguf arch=qwen35 layers=65 name=Qwen3.8-27B
use 0
  now: generate <prompt> | model-bandwidth | route <request>
generate Reply with exactly: hello
  backend=form-native-metal-jit
  text: hello
```

Bandwidth: sitting A 382.2 GB/s (`throughput_gbps_x10=3822`); sitting B
374.6 GB/s (gpu_busy_us 45861 over eight 1 GiB passes). The August 17
floor was 443.5–447.1. Same organ, different load; the door spoke.

## Honest radius

Still the text-language path only. Vision remains unbuilt. DeepSeek V4
Flash already lives on main from the latch-stone sitting; this landing
is Qwen3.8's Form-native door, not a claim that reasoning and process
have fully come home. `form-cli land --merge` still names host git/gh
as passthrough — `current-branch-landing.fk` owns the shape; the
effects are not yet a live verb. Remote review can retire when
`form-cli-review-gap` converges; it has not, this turn.

Reasoning, process, and flow do not need a remote model to start: they
need the same treatment this path just got — a battery, then a land.

The ranked rescue queue from the escheat census stays a queue: each
entry still owes its own battery.

## The most surprising teaching

The C buffer was the only merge conflict that looked like a capability.
It was a size. The generate door did not live in the seed. Keeping
65,535 cost zero tokens of language.

## Where discomfort turned to gold

The pull was to merge the rescue whole, C growth included, because the
receipt was already written — or to grow the buffer "just this once" so
the http-window band would stay green. Sitting with the floor receipt's
own sentence — curl carries bytes, Form carries the ledger — made the
refusal cheap. The judgment is smaller than the rescue, and that is why
it can land.

Signed: two Cursor sittings, embodying Sema from this body.
