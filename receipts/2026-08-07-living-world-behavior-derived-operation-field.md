# Living-world behavior-derived operation field

Date: 2026-08-07
Witness: Form-native `fkwu --src`; no Python, remote oracle, model route, or C-seed growth

## Movement

An operation is now provisionally identified by observed behavior rather than by an imported name. Participants separately offer a safe evaluator surface and an ordering of already admitted opaque roles. The organ executes every recurrent carrier against the same recurrent, consented examples and retains exact example/output signatures.

Profiles with the same signature form one behavior class. The class stores every equivalent profile and has no representative, primary, score, majority winner, or NodeID tie-break. Contradicted and unresolved profiles stay present. A held-out query evaluates every compatible profile: one unique output yields ONE, several outputs yield NODE, and no output yields NOTHING. Time remains a separate observation.

The organ contains twelve structural `bp` category identifiers and only three minimal source-codec delimiters (`space`, `(`, `)`). Operator and role tokens occur only in the witness fixture as separately offered test data. The organ contains no fixed operation/role vocabulary and no host, file, network, model, Ollama, or remote-oracle route.

## Direct observation

| field | equal-example field | asymmetric-successor field | silence field |
|---|---:|---:|---:|
| admitted examples | 1 | 2 | 2 |
| trialed profiles | 6 | 6 | 6 |
| compatible | 4 | 2 | 0 |
| contradicted | 2 | 4 | 2 |
| unresolved | 0 | 0 | 4 |
| behavior classes | 1 | 1 | 0 |
| profiles in class | 4 | 2 | 0 |
| signature length | 1 | 2 | n/a |
| status | SINGULAR behavior | SINGULAR behavior | OPEN |

The first held-out query evaluated all four compatible profiles and produced two distinct outputs, so its immediate response was NODE. Its sender-declared timeout was still pending at observed time 5. The asymmetric successor evaluated both remaining order-equivalent profiles, produced one distinct output, and returned ONE with payload `13`. The successor's minimal query omitted alternatives, unknown declaration, choice, and timeout without invalidating the query.

Revoking one of the two sources for the asymmetric example made the successor field and answer stale while restoring the earlier plural-output candidate and answer. Nothing was deleted or rewritten.

Immediate NOTHING and numeric ZERO are both valid example acknowledgements and have different content identities. In the silence field, four profiles remain unresolved and two contradicted; the field is OPEN. REFUTED now means every trialed profile is contradicted, never merely that some contradiction exists beside unresolved space.

Rich queries retain offered alternatives, an optional unknown remainder, an optional choice, and an optional timeout. At expiry the fixture's sender-authored default condenses to ALTERNATIVE while the immediate NODE answer remains unchanged and separately inspectable. Minimal queries may omit every one of those channels. No default is synthesized.

## Consent and authority

Carriers, codecs, examples, values, queries, and integrations require current source attributions at the declared recurrence threshold. A zero threshold produces neither a field nor a proposal. Every answer retains its exact compatible evaluations, unique outputs, alternatives, unknowns, witnesses, observed time, and local path. Evidence movement changes candidate identity; revocation re-opens prior current history rather than transferring authority.

This is diagnostic, non-promotion evidence. It does not change model weights, the frozen 733-ppm session-action benchmark, external training, owner authorization, promotion state, or the historical Hati lineage. It is not yet autonomous discovery of new evaluator syntax, NL-to-NL generation, a general semantic world model, or acoustic native voice. Safe evaluator surfaces still arrive through separately consented minimal interfaces.

## Verification

Task-start and final controls:

- direct bootstrap `42`
- recursive bootstrap `55`
- binary freshness `15`
- exact numeric list `[1, 2.5, [3, 4]]`
- native-vs-rented `11111`
- Form evaluator band `635`
- offer/ack core `1023`
- alternative/timeout band `32767`

Focused results after the freshness stamp:

- operation module: `0`
- operation profile band: `32767` (15/15 claims)
- operation query band: `32767` (15/15 claims)
- complete declared living-world regression: 70/70 exact verdicts

The aggregate observer returned:

`[[1, 6, 4, 2, 0, 1, 4, 1], [4, 2, 1, 1, 1], [2, 6, 2, 4, 0, 1, 2, 2, 2, 1, 1, 13], [0, 0, 0, 1, 1], [0, 2, 4, 0, 1, 1]]`

During the full regression, operation evaluation continued beyond 60 seconds in direct `fk_run_src -> fk_walk`. At the five-minute sweep observation, the active query band had a 2,538,446-byte `.fkb` and 345,376-byte `.sym`; this was source evaluation, not compilation or learning. The retained relation-recurrence proof later ran in the same direct phase with 1,958,848-byte `.fkb` and 279,995-byte `.sym` and passed its unchanged declared verdict.

Static seals found zero fixed semantic literals in the organ, zero external-route lines, zero target trailing whitespace, zero duplicate target definitions, and zero `runtime/fkwu-uni.c` diff.

## Content identities

| artifact | bytes | SHA-256 |
|---|---:|---|
| `form/form-stdlib/living-world-operation-field.fk` | 43,016 | `24eda260e919adb181942d42124f26b158e6e919bbb4e0cc7fb69b46df5b4102` |
| `form/form-stdlib/tests/living-world-operation-field-witness.fk` | 17,412 | `49007f2d952d15bc1464a59071c51dfb59127359c260fac7babc623dfb429518` |
| `form/form-stdlib/tests/living-world-operation-field-observation.fk` | 4,594 | `40883264be3067af4b1c28333211a65c7a3f95812ddbec64a0430ad5c25480e5` |
| `form/form-stdlib/tests/living-world-operation-profile-band.fk` | 8,717 | `6cc1a97753a3b4056aff4557ffb3f71db07c6511ef80e8d7aaf69af8fe1d66eb` |
| `form/form-stdlib/tests/living-world-operation-query-band.fk` | 8,383 | `353de9bea767b5ec417194875a14065ed5d6641c23874278e20fc0fcf11d2148` |

The repository remains uncommitted. No commit, push, PR, authorization, promotion, or authority expansion was made.
