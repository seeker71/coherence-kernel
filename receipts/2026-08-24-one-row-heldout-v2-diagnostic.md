# One sealed row can be re-observed without spending fifteen

**Date:** 2026-08-24  
**Witness:** Codex (OpenAI)  
**Scope:** local-Qwen held-out diagnostic mechanism; no model invocation

## The named gap

The 15-family evaluator is the honest denominator for local reasoning credit,
but it is too expensive for a focused post-repair question such as “did h01 or
h03 change after the two-phase generation path landed?” Re-running one row must
not masquerade as repository coverage or ready95 evidence.

## The attempt

`form-knowledge-qwen-heldout-v2-diagnostic.fk` now defines a strict one-row
selector, consent bound to both the sealed dataset SHA-256 and row id, and a
generation-report allowlist. The effectful adjacent runner calls the stable
two-ledger current-source resident door once, scores in memory, releases the residence, and
prints only id, family, promotion, error, latency, and bounded scalar stage
metadata. It never calls the aggregate report path.

The extraction window begins at the native `backend=` report and ends before
`text:`. A synthetic proof placed prompt/answer-like secrets after that boundary;
none entered the rendered metadata. Missing two-ledger fields are named rather
than interpreted as zero. Non-scalar and over-limit values are redacted.

## Observation

Bootstrap/freshness answers were `42`, `55`, `31`, and `[1, 2.5, [3, 4]]`.
Preflight reported balanced forms, zero errors, zero warnings, and zero
unresolved calls for both the pure mechanism and its band. Then:

```text
./fkwu form/form-stdlib/tests/form-knowledge-qwen-heldout-v2-diagnostic-band.fk
8191
```

No Q8 weights were admitted. No live row was scored. This receipt therefore
changes diagnostic reach only: local-Qwen held-out promotion, all-family
coverage, and ready95 remain unobserved by this movement.

— Codex (OpenAI), 2026-08-24
