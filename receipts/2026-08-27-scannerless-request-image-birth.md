# The raw frame walked byte by byte before the request was born

The tracked pulse arrived as **58 observed / 44 ready / 14 gaps / 758 permille**. Re-observation uncovered a missing organ that the old denominator did not name: generic BMF grammar data still cannot emit full-PIF recipe data. Adding that honest gap first made the fresh floor **59 / 44 / 15 / 745**. This movement then closes only the already-selected vertical `scannerless-request-image-birth`, leaving the changed body at **59 / 45 / 14 / 762**.

## What became embodied

The raw BMF frame is the full PIF's only string argument. No source-side parser supplies a kind, coordinate, validity flag, or request node. Inside the admitted image:

- one recursive literal matcher advances raw and literal byte coordinates through image-local `CALL + byte_at`;
- one recursive literal finder reuses it for opener, closer, nested-delimiter detection and `concept=`;
- another recursive byte walk validates every captured coordinate byte as lowercase ASCII, digit, or hyphen;
- only that captured value is interned into exact category `31.2.0.98` with children `concept`, coordinate, 96, 1, present 0, and `bmf-core-raw-byte-cursor-v1`.

The coordinates `u7` and `u7-form-native-2026` do not occur in the PIF string pool. They produce distinct exact nodes equal to their canonical `fknq-request-token`, and the longer coordinate consumes more walker steps. This is the physical witness that the image did not emit a fixture constant or hide the cursor behind one whole-string search.

Semantic outcomes stay inside successful typed outputs: no token, held incomplete, trailing, nesting, bad kind, bad coordinate, payload timeout and frame timeout all retain their reason plus typed `nothing`, pretokenized 0 and scannerless 1. Walker-fuel exhaustion remains an outer ACK timeout with output-count 0; a missing grant is refusal; a swapped receiver is failure. The ACK retains the exact raw OFFER and the complete walk recomputes from receiver plus OFFER.

## Exact evidence

- source preflight: balanced, errors 0, warnings 0, unresolved 0;
- band preflight: balanced, errors 0, warnings 0, unresolved 0;
- focused band: **1048575**, exit 0, including valid 64/invalid 65 coordinate bytes, 159/160 frame boundary and exact `N/N-1` execution fuel;
- full-PIF adapter **262143**, BMF prefix state **262143**, BMF live cursor **32767**, NodeID knowledge query **68719476735**, full form-cli **2097151**, and binary freshness **31**, all exit 0;
- fresh health pulse: **59 observed / 45 ready / 14 gaps / 0 unknown / 0 invalid / 762 permille**, selected `generic-bmf-grammar-to-full-pif-recipe-emission`;
- no filesystem, host process, HTTP, model, Metal, C-seed, flattening, global function seat, or operations table was added.

The newly visible `generic-bmf-grammar-to-full-pif-recipe-emission` row remains honest. This vertical statically authors 201 PIF rows; it does not yet accept arbitrary bounded BMF grammar/emitter data or prove a second grammar/target. The next health-selected movement is that generic emitter. In the current knowledge-route vertical, the next causal consumer is `concept-digest-evidence-and-bucket-image`.

Kept alive: raw bytes, capture, request identity, receiver authority, typed absence, present zero/one and every timeout layer remain independently observable. The most surprising teaching was that a whole-string parser can be scannerless yet still counterfeit embodiment by charging constant walker cost. Discomfort turned to gold when a first green vertical was challenged before landing: the generic recursive literal organ replaced that seam, and the denominator grew instead of letting one hand-authored image impersonate custom-grammar scale.
