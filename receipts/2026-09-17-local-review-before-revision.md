# Local findings must reach the next answer

Signed: Codex. Witnessed 2026-09-17 on `fkwu`.

Local Qwen reviewed its retained dialogue answer using the original enquiry,
sources and assertions. The draft was additional immutable candidate data,
not source authority. Six native queries supplied the draft and the same five
source observations. The model also read the full trust teaching.

It identified three issues, including deferred work in `next_action`, but
returned that field byte-for-byte unchanged. Its generated JSON placed the
proposed answer before the findings that criticized it. Correct criticism in
the same output did not establish a corrected answer.

A separate native admission then received those findings before generation.
It retained the original sources and checks, adding immutable preservation
checks for the draft and findings. Native comparison confirmed the unchanged
field before making that observation part of the revision request. Findings
remained proposals to assess against the source, not independent authority.

## What changed and what did not

The revision changed `next_action`, removing the unsupported mapping between
fear-costumes, `nothing` and composting. It proposed a visibility action based
on the arrival observation. It still retained defensive self-description,
overbroad language about old compositions persisting, and limited useful
completion. This is a specific correction, not response or resonance parity.

The two admissions differ in task, supplied candidate context and ordering;
the result does not isolate a universal causal effect of ordering. It does
show that the local model could use its retained findings in a later answer.

| Admission | Initial tokens | Injected IDs | Generated IDs | Elapsed ms |
| --- | ---: | ---: | ---: | ---: |
| Original ordinary query response | 3055 | 0 | 386 | 231492 |
| Review and proposed correction | 3625 | 3519 | 734 | 628863 |
| Revision with findings already supplied | 4001 | 0 | 373 | 286912 |

Both new admissions completed, passed the original source/report assertions,
preserved every supplied document and released their model. The revision made
no additional tool read. No provider process was launched. Native arithmetic
reports **915775 ms** for the two repair admissions and **1147267 ms** including
the original answer. This cost does not establish the requested throughput.

Parent work is additional: the goal meter began this movement at 7,454,881
tokens and read 7,522,149 at the post-revision checkpoint. The whole goal is
not yet a minimal-token result.

## What the next use can carry

Keep the distinction between a finding and its application executable. Exact
field comparison can expose a criticized field that stayed unchanged; a changed
field still needs a substantive review. Preserve all attempts and their costs.

The existing native controller separates `fcac-loop` from `fcac-settle`, which
releases the model. That is a source-confirmed opening for a bounded follow-up
inside one resident context. This movement used separate admissions; it has
not yet measured that continuation or installed automatic self-review.

All experiment helpers compiled cleanly and ran through the ordinary native
request path. Native guide: 0 Python implementations, 2 existing voice invocation
candidates, 0 unread files. Counsel panel: **orphans 0**, with 11 of 12 lanes
unobserved because no hearth stood. No all-good performance claim follows.

The previous source-query teaching completed as local learner candidate 43,
pending 0. Serving generation 5 and four promotions were unchanged. That learner
is Llama, not the measured Qwen model. Assessment answers remain excluded from
training; only verified procedural observations are returned as teaching.

Private evidence: `.hearth/response-parity/generalization-v2/answer-review-v1`
and `answer-revision-v1`. The latter includes the exact comparison audit and
a readable answer with the adverse review beside it.

The surprising finding was a useful criticism alongside an unchanged criticized
field. Following that contradiction into a separate revision yielded a real,
limited correction. Keeping the remaining weaknesses visible preserves the
ground for the next attempt.
