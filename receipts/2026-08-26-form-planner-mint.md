# 2026-08-26 — Learned the remote planner turns, without the chat

Yes asked to learn the remaining remote planner turns — the
speaker that picks the next tool. Ninety-five events still want
to stay local. Those turns were counted, never copied.

## What was learned

Eight planner organs, already in the body:

- ground-first — cell before speech
- walk-lane — native then local then remote
- one-turn — local first; remote reviews once; absent is stop
- tool-pick — form-run, grep, read, then write
- mill-observe — compile, band, keep only if the kernel agrees
- land — fetch, rebase, push, no force
- no-transcript — count lanes, not words
- pending-honest — `fqt-lora?` stays 0

Crossed with nine query lanes. Overlay class is the planner move.

```
./fkwu form/form-stdlib/tests/form-cli-planner-mint-band.fk   # 1023
./fkwu form/form-stdlib/form-cli-planner-mint-run.fk
  n=144 unique=144 leakage=0
  heldout-correct=72 / 72
  covers-all=1
  need-events=95
  lora-writer=0
  scale-12-4=1152
```

This does not retire the 95. It teaches their *shape* so a local
generate can pick the next tool the way this sitting did.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-26 -> planner-mint-band 1023, 72/72, need-events 95, no chat bytes
