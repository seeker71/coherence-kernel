# 2026-08-30 — two surfaces rise, one twin refused, and the mirror learns what it lost

"Next with more BML." Two lifts landed on the airborne floor, one twin
of our own making was caught and removed, two seed seams were healed,
and the altitude mirror came back speaking a harder truth than before.

## Risen

- **The rest-answer surface** (`form-cli-rest-answer.bml`): split,
  locate, fresh word, registry — the answering half of the resting law
  authored at altitude, with `fra-agree` = 63 as its riseproof against
  the living organ (token-for-token split agreement, corpus words,
  exact nothings, pointer answers). Band 15 through the airborne floor,
  the .bml as its own prelude.
- **The altitude mirror rewired to its true surface**: the delegated
  lift discovered its "today" had drifted — a prior sibling lift had
  already raised the mirror into `form-cli-author-altitude.bml`, and
  the xtal sweep then removed the twin WITHOUT rewiring the door
  (runner rc=2, silently broken since). The fresh surface my agent
  authored turned out to be a second truth beside the sibling's — our
  own twin, refused the hour it was made: removed, and the runner,
  band, and surface now stand as one (runner speaks, band 783 at its
  declaration). The surface gained the one line its era predated: a
  carried `// preludes:` so it stands alone on the floor. The old
  file-lane compile door left the tree.

## Healed in the seed

- **The import probe stops poisoning the floor**: it compiled each
  prelude dep ALONE by design; for a .bml dep that means the raw brace
  surface — hundreds of honest unresolveds — and it wrote that refusal
  where the floor lane reads verdicts. A healthy door then served a
  **borrowedno** (corpus row 1174): a refusal inherited from a writer
  answering a different question. The probe now skips .bml deps.
- **The warm gate re-derives instead of trusting tombstones**: a
  refusal mark on the ice triggers a fresh re-lower; a .bml that truly
  refuses will refuse again with today's diagnostics.

Radius after both cuts: ground 42/31, rest-force 31, offer-ask 65535,
author-high 4095, organ 134217727, native-vs-rented 11111.

## The mirror's harder truth

```
before the sweeps:  32 pictures / 23 bound / 1455 cells
today, honest:      39 pictures /  2 bound / 1422 cells
```

Bound collapsed because the binding marks lived mostly in the xtal
twins the sweep removed — the mirror's own door was broken by the same
sweep, so nobody saw the fall until this lift healed the door. The
lens's next truthful widening is named, not forced: bindings must be
counted where truth now lives (the .bml surfaces), not only in .fk
cells.

## The most surprising teaching

The delegated lift's before-witness could not run (rc=2), and the
recovery was to resurrect the pre-lift cell from git history and run
the MEANING on today's tree — a baseline no header could have provided.
A witness line in a header can outlive the world it witnessed; only
re-running the meaning tells you which world you are in.

## Where discomfort turned to gold

Finding that my own delegated hand had minted a twin — hours after the
day's law named twins the wrong shape — stung precisely because the
agent did honest work on a drifted map. The gold is that the law held
against US without ceremony: the fresher, smaller surface was removed
and the sibling's fuller one kept, because the law binds by shape, not
by authorship or recency.

; witnessed: 2026-08-30 -> rest-answer band 15 (fra-agree 63);
; author-altitude band 783 + runner 390020001422; twin removed; import
; probe skips .bml deps; warm gate re-lowers refusal marks; corpus row
; 1174 borrowedno
