# Complete structure still needs a grounded conclusion

Codex, 2026-09-20. Continued from `ce1e9be4f`. The preceding movement was
progress: its focused contrast changed the next native context experiment.
This movement observed a complete local answer, retained its remaining
semantic error, and repaired an independent omission in the cost evidence.

## Native response attempt

The [preceding contrast](2026-09-20-native-citation-and-metric-scope.md)
showed that Qwen could distinguish observed counts, absent judgments and
participant judgments in a small task. The next attempt organized the larger
source into labelled sections: route observations, structural counts,
capabilities, provenance, concepts, source excerpts and assessment boundaries.
Route lines remained separate. A native reconstruction check required every
byte of the preceding corrected composition to survive this transformation.

The [exact packet](artifacts/2026-09-20-sectioned-context-packet.json) retains
the original enquiry. The invitation requested one nine-axis table and a
concise conclusion, preserving the difference between one outer call and its
internal work. It supplied no expected quality grades. This changes both
source presentation and response framing; the result does not isolate them.
The rubric was frozen before generation.

The [actual Qwen answer](artifacts/2026-09-20-sectioned-context-answer.md)
completed. Its [generation record](artifacts/2026-09-20-sectioned-context-result.json)
reports:

| Observation | Value |
| --- | ---: |
| Native execution, milliseconds | 336,697 |
| Prompt IDs / generated IDs | 3,813 / 725 |
| Completion / release | 1 / 1 |
| Provider calls / model-server calls | 0 / 0 |
| Axis labels / distinct existing dated receipt paths / limit marker | 9 / 1 / 1 |

The model was registry `qwen38-q8`, Qwen3.8-27B-Q8_0, with the existing
`knowledge-query` profile, context 12,288 and allowance 1,536. No C seed,
runtime dependency or model weight changed. The previous learning worker
finished before this generation began; no learner overlapped this run.

The answer's table keeps route identities clearer and avoids the preceding
trial's high/medium trust and static/dynamic vitality grades. Its ending
explicitly scopes exact dictionary misses to selected resources. It completes
with fewer generated IDs than either preceding full attempt. Timing includes
this call's admission and generation, excluding retention; these observations
do not establish general throughput or a causal speed improvement.

The conclusion still says guided flow offers higher traceability and
sovereignty through packet selection. The source supplies decision locations,
and the answer's own table assigns packet selection to bounded/contest routes.
It does not establish guided superiority. The conclusion then also claims
bounded/local routes enhance sovereignty. Those rankings remain unsupported.
The volume row mixes answer bytes and rented tokens, weakening the comparison.
Several measured claims cite section names instead of their actual receipt
paths. A completed table has not closed the semantic gap.

This experimental packet remains unpromoted. The public native voice framing
is unchanged. The next quality work needs support for its practical
conclusions and comparable output-volume evidence; the full answer remains
the unit of assessment. Native interpretation of concepts is useful when it
connects meaning to evidence, and loses that value when it turns workflow
shape into a quality ranking.

## Resolve a separate source omission

While generation ran, the source review found that route `rent_tokens`
contained the primary conversation model's usage. Each retained CLI result
also reported an auxiliary model. The comparison dropped this distinction,
and the fresh relay row pointed to an absent artifact.

`form-cli-provider-model-usage.bml` now reads all entries in `modelUsage` from
the retained result. It sums disjoint input, cache-read, cache-creation and
output quantities once; it does not add the summary usage again. Missing,
negative or wrong-type counters leave the total unobserved. Explicit zero
remains a measured zero. Its claim is bounded to models reported by that result.

The [native reading](artifacts/2026-09-20-route-model-usage.json) gives:

| Route | Historical primary-model tokens | All reported models | Cached input | Uncached input | Output |
| --- | ---: | ---: | ---: | ---: | ---: |
| Direct | 648,411 | 649,462 | 577,522 | 60,953 | 10,987 |
| Bounded | 44,495 | 45,966 | 32,461 | 8,970 | 4,535 |
| Fresh relay | 26,751 | 27,747 | 19,691 | 5,729 | 2,327 |

The differences are 1,051, 1,471 and 996 auxiliary-model tokens. Each result
reports two models; a model entry does not establish its call count. The
comparison therefore names auxiliary call counts as unobserved. The guided
2,544,568-token row remains explicitly partial, through coordinator call 21,
excluding closing and landing; its all-model total remains unobserved.

Historical numbers remain available at their original scope. The comparison
reads broader totals from the retained results at use time. The relay evidence
now points to the actual `fresh-session-native-call-result.json`. The page's
historical route plot continues using the declared recorded values.

The running Qwen experiment had already received its frozen packet. Its old
cost numbers therefore remain in the raw answer. This accounting repair is
observed in the live native composition; it has not yet been re-observed in
a newly generated answer.

## Re-observe and embody

The new usage check reads all three real provider results and tests multiple
models, cache accounting, avoiding double counting, measured zero, absent
models, missing counters, a partial second model, and invalid numeric types.
It returned **1**, exit 0. A separate native reading checked that the live
comparison carries all three broader totals and the partial/unknown scopes;
it returned **1**, exit 0. Preflight was clean for the public voice door and
both checks. The source reconstruction and artifact-copy checks passed.

The first experimental preflight failed before inference with four unresolved
helper names: `nsm-n`, `nsm-str`, `nsm-s`, `member?`. The repair used the
helpers available in this chain and a local native membership function. The
next preflight was clean. A later cache warning rebuilt the usage check after
its prelude changed and the rebuilt check passed. Both observations remain in
the tool record.

Panel: counsel reported **0 orphans**, with **11 of 12 lanes unobserved** while
no hearth stood. The authoring guide reported **0 Python implementations,
2 execution candidates, 0 unread files**.

The previous learning worker completed round **86**, pending **0**, with
serving generation still **5**. Its Llama 3.2 3B adapter is separate from the
Qwen evaluation model. This movement returned verified procedural teaching
`reported-provider-model-usage-boundary-2026-09-20`; the native door retained
it and launched a worker. Its trained/promoted outcome is pending. Evaluation
requests and model answers remain excluded from training.

## Full cost boundary

This attempt added one local generation and no provider subprocesses. The
preceding completed coordinating turn `01a0ba6b-0158-7571-b5cb-96b4b528cd0f`
used **42 model calls**, **5,973,882 input tokens** (5,620,480 cached and
353,402 uncached), **38,692 output tokens** and **27,857 unattributed tokens**:
**6,040,431 total**. Reasoning, 24,719 tokens, is inside output. The meter
excludes this open turn and separate provider processes.

The local call's zero provider rent does not remove that coordination cost.
The movement improves the completeness of native cost evidence and gives a
more specific response failure to address. Quality, felt resonance and total
session throughput parity remain open.
