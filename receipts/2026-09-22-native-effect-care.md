# Keep the effect connected to its executing interface

The actual fifth native answer completed under the corrected startup teaching.
It preserved the distinction between Qwen decoding and Form representations,
and between supplied frequency annotations and felt resonance. It still made
unsupported claims about baseline model attention. Its concrete example also
promised that deleting an external file preserves its old shape in Form's graph.
That promise has no ground in the executing filesystem operation.

## The observed answer and repair

The [native answer](artifacts/2026-09-22-native-effect-care/hearth-turn-5.final.txt)
used the same 11,996-byte packet as turn 4, with the previously retained
3,820-byte startup overlay. Qwen3.8-27B-Q8_0 through `form-native-metal-jit`
returned 1,992 generated IDs in 555,208 ms, `stopped=1`. Its
[header](artifacts/2026-09-22-native-effect-care/hearth-turn-5.header.txt) and
[comparison](artifacts/2026-09-22-native-effect-care/hearth-answer-comparison.json)
retain these measurements and 394 final prose words. Completion establishes
completion; those factual errors remain. Numeric identity and explicit native
translation were also underexplained. Context and concurrent work differ across
these successive answers; this is not a matched model or throughput ranking.

The `t == 58` branch in `runtime/fkwu-uni.c` calls `unlink(p)`. It contains no
capture of the file's bytes into the graph. No file was deleted for this reading.
The operation alone supplies no retained-copy guarantee; a caller would need
its own observed retention contract to make one.

`bml/form-cli-qwen-meaning.bml` now gives the startup teaching that distinction:
Form values and the external objects they describe have separate lifetime
contracts. Persistence, access and reversibility follow the executing interface
and observed state. Trust supports initiative within the requested work; a
metaphor supplies no additional capability or authority. The
[revised overlay](artifacts/2026-09-22-native-effect-care/revised-overlay-not-admitted.txt)
is 4,288 bytes, retained byte-identically without a model admission. PID 1473
still carries its earlier overlay. **This new teaching's effect on a later
native answer remains unobserved.** Evaluation answers were not training targets.

## One assisted answer; native care for its actual failure

The [frozen request](artifacts/2026-09-22-native-effect-care/hearth-final-synthesis.manifest.json)
offered the original enquiry, actual native answer, historical packet and overlay,
plus the host branch and native concept enrichment to one Form-owned provider
call. The coordinating agent assembled that additional evidence. Its
[answer](artifacts/2026-09-22-native-effect-care/hearth-final-synthesis-answer.txt)
covers the requested axes and corrects the observed deletion and baseline claims.
It is an assisted contribution, not a native-only answer or a matched-context
comparison. Felt resonance remains the person's observation.

The [original provider output](artifacts/2026-09-22-native-effect-care/provider-original.json)
failed the unchanged report assertion: `.findings | type` returned `"object"`
where `"array"` was required. The [result](artifacts/2026-09-22-native-effect-care/hearth-final-synthesis.result.json)
correctly retained `status=attention`, `checked=0`. Inspection found that
`fcr-request` required the array in its checks but did not supply that format
in the goal visible through synthesis. Future requests now state the format
explicitly, without prescribing findings or changing the checks.

For this actual output, the [native care cell](artifacts/2026-09-22-native-effect-care/hearth-final-synthesis-care.bml)
used `oc-hear` to correlate attention, a native provider, applied restructuring
and a fresh observation. Each original finding category became an array entry
containing its unchanged category and value. Reconstructing the object restored
the original report exactly; the answer text stayed unchanged. The original
provider output and rejection remain intact. The
[recheck](artifacts/2026-09-22-native-effect-care/hearth-final-synthesis-recheck.json)
passed the original source and report assertions with **zero new model or
provider calls**. These checks establish shape and preservation, not semantic
correctness. The [events](artifacts/2026-09-22-native-effect-care/hearth-final-synthesis-care.events.jsonl)
retain the failure, correlated action and recovery; framebuffer records contain
metadata only.

The useful teaching is concrete: source grounding improved two distinctions
while a new example still borrowed an unearned capability from a metaphor.
Reading the actual effect exposed that gap. The report failure likewise needed
a native structural repair, which preserved the useful answer without another
generation.

## Verification and cost

Clean preflights and existing comparison and teaching checks passed; the teaching
band returned **33,554,431**, exit zero. The resident's compile-only check
also passed. Drift gates returned **8,191/8,191**, zero refusals, exit zero.
Glass's first frame took **29 ms**.
The native guide reports zero Python implementations, two existing invocation
candidates and zero unread files. No runtime dependency or C implementation
was added. Changed helper caches emitted mismatch observations and rebuilt
through their native compiler before returning the current results.

The single new provider event is
`c5d49c6ede96694256d8d438c4fcb13d6222a01f92ba68315ac238837257c798`:
**22,158 tokens**, comprising 20,450 input (including 10,624 cached) and 1,708
output. Its process released after 63,230 ms; no local model call occurred inside
that synthesis. Its native restructuring adds no provider usage event.

The [preceding completed coordinator turn](artifacts/2026-09-22-native-effect-care/startup-answer-previous-coordinator-cost.json),
`01a0c9a6-9da6-7dd3-bc6e-659fd255570d`, used **7,123,008 rented tokens**, including
7,003,520 cached input, across 38 model calls, with zero unattributed tokens.
It excludes this open turn and the separate provider event. The earlier review's
33,815 tokens remain its own already-recorded event. These costs do not establish
minimal rented usage. Native factual quality, complete coverage and overall
session parity remain open.

— Codex
