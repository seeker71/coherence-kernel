# Resident stage egress reaches its own Form channel

**Date:** 2026-08-28  
**Movement:** scannerless resident knowledge stages can leave the active turn
while it is still moving, as content-free append-only Form frames.

## What crossed

`fknss-run-with-observer-context` carries caller-born relation data alongside
the existing observer function. The resident turnwheel creates that data from
its own egress path, task NodeID text, and replay key; the source-knowledge
route carries it unchanged to `fcpa-knowledge-stage-observer-with-context`.

Each live frame contains only route, structural task identity, stage/phase,
typed scanner status, counters, and a timestamp. It contains no task prose,
source byte, generated byte, prompt, or answer. It is appended through the
existing atomic byte carrier, not through HTTP, a server, a second model, a
global sink, or a recursive host process.

The stage uses `task-key`, not `exchange` and `turn`. Completed-contribution
recovery scans the latter pair; giving an intermediate observation those field
names could turn a progress event into false completion evidence. The portable
band builds a stage frame before recovery and proves that exactly the two real
completed task keys remain.

## Witnesses

```text
./fkwu observe/form-cli-peer-stage-egress-live.fk
form-peer stage route=source-knowledge stage=run phase=begin status=none
1

./fkwu form/form-stdlib/tests/form-cli-nodeid-knowledge-session-band.fk
33554431

cd form && ./validate.sh form-stdlib/tests/form-cli-peer-contribution-turnwheel-band.fk
→ 65535
→ fourth arm: 1 band(s) four-way (runtime fkwu source/JIT; no flatten gate)
→ 1 ok, 0 divergent
```

All changed Form cells preflighted with balanced parentheses, zero errors,
zero warnings, and zero unresolved calls before the witness run.

## The registration repair

The `form-cli-peer-contribution-turnwheel` registration named `16383`, from
commit `fdc22249`. Commit `1194cb9c` subsequently added the source-knowledge
claim that made the executable band `32767` but left the registration behind.
This movement adds the independent stage/replay-isolation claim, yielding
`65535`; `form/fourth-arm-bands.txt` now records the exact witnessed verdict.
That line is a proof registry entry, not a runtime function table or a limit.

## Honest present seam

The resident Qwen process already in memory was started from its earlier source
closure. It completed the first source-concept task and wrote its final frame,
but it cannot adopt these new Form functions until the resident has an
addressable program-image hot-swap crossing. This receipt does not call a
restart a hot swap, and it does not claim live stage delivery from that old
closure. The next direct build is a retained-node program-image replacement
that preserves the resident model/KV residence while exchanging the turnwheel
recipe, then one real source task observed through this stage channel.
