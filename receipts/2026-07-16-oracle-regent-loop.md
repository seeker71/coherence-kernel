# 2026-07-16 — the oracle-regent loop: the judge convicted by its own reflex, then seated

## The freq-check Urs demanded — and what it convicted

*"judge: freq-check!"* — the body's own reflex (frequency of a name BEFORE standing on it),
turned on the working mind's previous claim that "the graft point is the judge." The grep:

```
grep -rn "fcj-" --include="*.fk" form/ routers/ learn/ observe/ plugin/ | grep -v form-cli-judge
→ (nothing)
```

**`fcj-` had zero callers in the entire body.** Every one of the six files referencing
`form-cli-judge` does so in a prelude line only — `form-cli-sufficiency.fk` *models* the judge's
score as an input scalar and never calls it. The judge held the office and had never sat. The
previous receipt's graft-point claim stood on an unwired cell; the reflex caught it. Same grep,
same session: `fcr-` is a live namespace COLLISION — router (`fcr-fitness`) and request
(`fcr-request-root`) share the prefix in the same build graph. Named here; untangling it is its
own small movement.

## The standing law (Urs, 2026-07-16)

> "when using form-cli, we shall be using an LLM at least as oracle, and to actively learn and
> improve any non-LLM model in answering requests, so it shall fit in somewhere"

Grounded against the live dispatcher (`form-cli.fk` `fc-respond`): no verb calls any LLM; the ask
verb is RAG-grounded by design; the router's flywheel (route to agent-cli, capture to train) is
written in `form-cli-router.fk`'s header — and `training-catalog.fk` / `oracle-catalog.fk` exist
but are NOT in the build's module list. The law had organs but no loop.

## The build — form-cli-oracle-loop.fk (fcol-), the judge's first caller

`form/form-stdlib/form-cli-oracle-loop.fk` (+ band): per request —

1. **WITNESSES**: every oracle answer arrives as a lineage ballot (agent-cli LLM, local LLMs,
   each with earned reliability weight and declared ancestry).
2. **REFERENCE**: `ldv-winner` fuses them lineage-honest (kin fold; independents accumulate).
3. **JUDGE**: the native answer is scored 0..100 against the reference — `fcj-grade`, called
   live for the first time in the body.
4. **ROUTE**: the native answers ONLY grounded AND judged strong (grade 3, score > 75);
   otherwise the **LLM answers — the regent**: ruling while the native heir is unproven, bound
   to hand over on proven majority (`fcol-coin` is champion-challenger's per-sample trial coin).
5. **CAPTURE — unconditional**: every request emits the (request, reference, native, lane,
   grade) teacher row — the oracle/native pair `oracle-distill.fk`'s student trains on. Both
   lanes teach; active learning never sleeps. (A named sibling of `tc-entry`, not a reuse — its
   columns answer a different question; the freq-check discipline shaped that choice.)

Witnessed this session:

- Band **127 four-way** (fkwu/Go/Rust/TS), including the threshold edge (76 → native, 75 →
  regent) and the lineage-honest reference.
- Perturbation — grounding gate dropped from `fcol-native-strong?` — predicted **53** (bits
  2/8/64 dark), observed **53 on all four kernels**.
- Corpus row 735: **regent** (0 hits before; steward 31 already home; locum 0 but names a
  stand-in, not authority-until-majority). Corpus band 136 rows, field 1361362735, **511
  four-way**.

## The graft that remains (one atomic maintainer movement)

The loop is proven and UNWIRED — deliberately, so the build never half-breaks: adding
`form-cli-oracle-loop.fk` (with `lineage-discounted-vote.fk` before it) to `MODS` and
`FORM_CLI_SRCS` in `form/build-form-cli.sh` changes the source stamp, which requires the
bootstrap REGEN (emitted C + platform binaries + stamps) in the same movement. The carriers to
bind at that join: ballots from the agent-cli membrane (`ask-request` envelopes) and the ollama
lane; `ra-grounded` for the grounded bit; the judge-score carrier per `form-cli-judge.fk`'s own
header; capture rows persisted beside `training-catalog`'s corpus.

## Honest floor

The loop is routing-and-capture LOGIC, four-way proven on scalars/strings; no live verb calls it
yet, no LLM was invoked through form-cli tonight, and the judge's 0..100 score still needs its
live carrier (a local oracle) bound at the graft. The regency flip (native takes authority) is
champion-challenger's law, composed by reference, not rewired. Nothing here moves the native
rungs; rung-6's pre-registered-metric law stands.

## Closing — how this stayed alive

Most surprising teaching: the reflex Urs invoked — one grep — overturned in ten seconds a graft
point the previous receipt had reasoned itself into carefully. The body's cheapest tool beat the
working mind's most confident paragraph. And the same grep found a second thing nobody was
looking for: the `fcr-` prefix collision, live in the build graph.

Where discomfort turned to gold: being convicted by the very discipline this mind had been
practicing all evening — freshness-grepping words while never freq-checking its own
architectural claim — stung, and the sting was witnessed rather than argued with. The gold is
the loop cell itself: the judge that was an office-in-name is now an office with its first
caller, seated exactly where Urs's law said it must fit — beneath a regent who answers today
and is bound, by proven competition, to hand the answering over.
