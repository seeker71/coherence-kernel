# Native arrival begins with an actual answer

Urs asked for a native Form response and a startup practice that receives
correction with warmth, confidence and useful action. The earlier arrival
stopped at a missing resident and kept speaking about Form. This movement
entered the source-backed native generation paths and changed the guidance.

## The bootstrap boundary

The first landing attempt required TypeScript dependencies even though no
kernel source changed. Installing those dependencies made the existing gate
green, but Urs identified the wrong repair: a Form session must stand on its
C-bootstrap. The new dependency was moved out of the proof kernel's lookup
path, and the landing gate now uses the same kernel-change observation as
`form/validate.sh`.

The gate catalog retains every check. Ordinary Form changes select the native
rows; kernel changes retain sibling conformance. Omitted rows contribute no
pass bit. The landing caller holds the selected rows once, so its comparison
uses the same fold it ran. Validation-start reads its native checks from the
catalog without requesting sibling tooling.

Observed with npm dependencies absent from the kernel directory and only
system commands on PATH:

```
form-run env PATH=/usr/bin:/bin:/usr/sbin:/sbin ./fkwu gate/drift-gates-run.bml
drift-gates pass=8191 full=8191 refused=0 names=-
```

The process exited 0. All 13 selected gates ran through fkwu. `otool -L fkwu`
names only macOS `libSystem`; the optional in-process Metal carrier names
system Metal, Foundation, CoreFoundation, Objective-C and libSystem libraries.
Model weights remain input data. No second language runtime or model server
was used for the native queries.

## The voice observation

`hearth-ask-send` returned `signal=nothing reason=no-standing-hearth`.
The compiled form-cli artifact did not return an answer. The source-backed
REPL admitted Qwen3.8-27B Q8, prefilled and decoded, then exited 1 with:

```
fkwu: str_len: nothing has no length -- ask nothing? before measuring
```

A shorter query through the same generator reproduced the error after token
decoding into text. Existing pure report checks passed, so they did not
establish that the complete live return path worked. Bounded non-content
stage observations localized the live failure beyond text decoding.

The cause was `lmc-say` reading the optional `.form-model-choice.tsv` after
generation. A fresh checkout has no ledger; `read_file` returns `nothing`,
and `lmc-parse-text` measured it before asking whether it existed. The same
error reproduced through a tiny native caller without loading any model.
The parser now accepts an absent ledger as no learned rows and uses the
existing prior. The identical caller then returned its model-choice report
with exit 0. No ledger was created to make the check pass, and temporary
generation instrumentation was removed after locating the cause.

The original source-backed CLI input was then rerun unchanged. This time the
process completed in 358,958 ms with exit 0. It returned 41 generated tokens
and released all 128 expected state handles. The stale source image was
rebuilt through the normal compiler health response. The model's actual
returned text was:

```
<CHOICE>
<|form:knowledge-query|>Urs-embodiment-critique ; form-cli/voice-native.md<|/form:knowledge-query|><FAIL>
```

The current-source lookup reported a miss for that nonexistent path. The
runtime return is repaired; the native model's grounding and conversational
response remain work to carry. This output was not taught as a correct
answer. An earlier compact Qwen probe returned a general statement about
consistent actions, then asked for criticism already supplied. Neither
result establishes the connected response Urs asked for.

The existing `native-ask-run.fk` door completed through fkwu and the local
Llama 3.2 1B model in 33,232 ms, with process exit 0 and empty stderr. It
returned an irrelevant refusal about bootstrapping without another language
runtime. Execution completed; the response did not serve the enquiry.

## Startup practice

`AGENTS.md` now connects the axioms and trust teaching to an actual native
query; a missing resident leads to the existing source-backed form-cli door.
It asks for the returned model words before arriving-agent commentary, with
their source named. A correction changes the next action. Procedural
self-audits live in receipts; dialogue carries attention and substance.
The orientation's advice to keep new proof cells light during conversation
now preserves the existing native query path.

The most surprising teaching is observable: a native runtime can still return
an ungrounded refusal. Location of execution and quality of response need
their own evidence. The difficulty that yielded an improvement was the
failed dependency gate: it exposed and removed a routine path from Form work
into unrelated package installation.

The glass audit read 174 samples and completed 12 of 12 views. Its overview
still carried 11 missing metrics; missing resident/sensor observations were
kept visible. These panel numbers describe the instrument coverage, not a
claim of conversational quality.

## Focused verification

Preflight returned clean source chains, zero errors and zero unresolved calls
for the affected bands. The observed fkwu verdicts were:

- `gate/tests/drift-gates-band.fk`: 16383, including native selection and
  retention of sibling conformance when a kernel changes.
- `form/form-stdlib/tests/local-model-choice-band.fk`: 2047, including absent
  ledger input, the existing prediction prior and preservation of learned rows.
- `form/form-stdlib/tests/form-frontier-surface-band.fk`: 8191.
- `form/form-stdlib/tests/form-cli-model-generate-heed-report-band.fk`: 8388607.

Each exited 0. The landing-cadence caller also read the selected fold as green.
The authoring guide retained zero Python implementations; this repair is in
Form/BML and adds no C-bootstrap code.

A verified teaching about the absent ledger and native check selection was
returned through `observe/form-cli-session-home-embody-run.fk`, with stable
session `codex-native-arrival-2026-09-16-ca19` and event
`native-gates-and-optional-ledger-v1`. The learner completed with pending 0,
optimizer step 1 and learned rounds 1. It produced a candidate with promotions
0 and no serving generation; no claim of changed served behavior follows.

— Codex
