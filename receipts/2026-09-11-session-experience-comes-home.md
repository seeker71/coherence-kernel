# Session experience comes home

Codex, 2026-09-11. Failed attempts previously became a generic status example:
their original problem and attempted answer disappeared at the memory boundary.
Evaluation attempts returned without a retained record. Form now retains the
problem, attempt, outcome and evidence in private immutable experience, while
keeping the meaning of each observation explicit.

An unsuccessful attempt can train context to observed outcome. Its attempted
answer is input data, and the completion describes the actual outcome. Bound
outcome targets also participate in rehearsal. The original problem is checked
for assessment overlap even inside that context wrapper. Historical generic
status rows remain unchanged; a new side of memory cannot recreate information
that the old record never kept.

Native recall discovers local hearths, finds related problems with existing RAG
features, and brings the other observations from those sessions alongside them.
Source hashes are checked again when context is consumed. Consumption emits its
fresh health, selected action and actual received count. Missing or changed
experience remains an enquiry. The healer consults this context before model
routes, and can offer a remembered checked patch to the current caller's checker.
A changed experience context also permits a fresh session-adapter attempt.

The actual local recovery found 72 completed repair runs and retained 106
baseline/candidate observations. Repeating recovery of the selected run added
no record. The replay searched 125 source records, received 15 observations,
and found one previously checked patch for the positive-count case. This is
lexical retrieval and remembered-case practice, not evidence that all sessions
or lessons have been digested.

The grounded replay ran the original checker against the original and recalled
source. Both processes exited 0; the checker changed from 9 to 31. The replay
made zero native-model calls and zero remote-model calls. Recall took 17,921 ms;
the complete replay took 19,471 ms. The prior repair used a native Qwen model.
This comparison demonstrates avoided inference on that repeated case; it does
not measure a reduction in overall remote usage or unseen task performance.

A later replay exercised the healer's integrated recovery and recall entry while
the native learner ran. It examined 130 current source records, supplied 15
observations and one checked candidate, and again moved the checker from 9 to 31
with both exits 0 and no model calls. Recovery added no duplicate experience.
Recall took 2,521 ms and the entire observation took 4,290 ms. These are separate
readings under different running conditions, not an attributed speedup.

The first observation helper omitted the checker's core prelude. Both processes
exited 1 with an unresolved `nil?`; their printed 11 was not a verdict. Those
original files and outputs remain in the private hearth. Restoring the core
dependency made both chains pass preflight. The failed observation itself was
retained for contextual native learning, alongside the verified teaching.

Both full-gradient native Llama-3.2-3B rounds completed, restoring the preceding
Adam state and updating all 56 LoRA matrix pairs each time. Optimizer step moved
from 8 to 10. Across the two rounds the trainer consumed 998 input tokens and
549 supervised tokens, including rehearsal. These are token consumptions, not
unique sessions or skills. Original event and result files accompany this receipt.

| Observation | Step | Training rows | Input / supervised tokens | Primary loss before → after | Trainer wall ms |
| --- | ---: | ---: | ---: | --- | ---: |
| Verified experience teaching | 9 | 3 | 424 / 281 | 4.904538853879785 → 4.886844604538055 | 442408 |
| Actual missing-prelude failure | 10 | 2 | 574 / 268 | 3.441971133927453 → 3.405806002592911 | 703917 |

The greeting sentinel worsened across those rounds from 2.029051512479782 to
2.047746777534485, against serving generation 4's 2.002161681652069. Both
candidates remain retained, while serving stays at generation 4 and the learned
serving scope remains dialogue. The worker exited 0, pending is zero, and both
trainer exits reported zero retained buffers. Successful outcome learning does
not establish serving improvement. The failure row's forward and backward
spans were 66061 and 255514 ms; these actual costs need further performance work.

A one-second OS stack-sampling request did not return its report; that diagnostic
was terminated with exit 143. No stack attribution is claimed. Training continued
and the stage durations above come from its completed native events.

New doors: `observe/form-cli-heal-experience-run.bml` recovers completed repair
runs without repeating their execution; `observe/native-session-experience-run.bml`
returns the private recall reading. Ordinary healing performs recovery and recall
itself. Assessment experience is retained for practice, while independent
assessment receives no prior answer. Recovery does not schedule a gradient for
every historical observation. Full host-agent transcript digestion remains a
separate uncompleted surface.

Integration rebased through `15ad1e4c`, preserving `c6412754`'s repository-knowledge
path and session census, then its bidirectional historical descriptions. The
current local status reads 109 retained experiences under
73 session identities: 72 recovered repair runs and this coding movement. Those
are identity groups, not 73 digested host-agent conversations. Separately, there
are 10 retained training examples under nine teaching/session identities. The
withdrawn JSON-candidate lesson was retained as experience without claiming a
third gradient update.

The newly generated local repository corpus contains 1,857 source-attributed
examples from 1,069 source units: 1,556 training rows and 301 held-out rows, plus
two validation sentinels. Its 788 historical teachings also supply 788 reverse
description examples; these are additional directions, not independent lessons.
Its live witness rehashed 19 source files, confirmed dataset hashes and counts,
and found zero held-out prompt or completion overlap. This movement generated
that current dataset; its two measured gradient updates used the session rows
above, not those 1,556 repository rows. Four source edge-contract omissions remain
named by the corpus. `final-status.json` is the closing reading; the status files
inside individual round directories retain the state when each was exported.

Preflight passed the changed learner, healer, memory checks and the grounded
before/after checker. Existing memory, concurrent-journal, routing and code
checks passed at 1023, 31, 127 and 511, all exit 0. The native authoring guide
reported 25 existing Python implementations, 307 invocation candidates and zero
unread files; this movement added only Form/BML code and native temporary helpers.

The memory check changes a real private fixture record after recall. Consumption
then receives two of the previous three records, retains the remaining context,
and reports pain with a need for current evidence. An intermediate diagnostic
produced one JSON child from three logical list items. A native reproduction
initially repeated that result. A proposed constructor normalization subsequently
returned three, but a controlled rerun with the original constructors also
returned three. That candidate fix was withdrawn. The observation remains an
unisolated carrier/cache sensitivity; this movement does not claim to repair it.
The final memory check changes actual source bytes and does not depend on the
constructed-input diagnostic.

Panel: Glass reached its first frame in 82 ms, with 33 kernel operations and
Metal loaded. Counsel reported zero orphans and 11 of 12 lanes unobserved because
no hearth resident stood. These readings do not establish a hardware floor.
The share meter remained declared/unmeasured; no contribution percentage is
claimed.

Kept alive: discarded attempts became usable experience and a remembered repair
ran through its checker without model inference. Surprise: the missing memory
was at the collection boundary, before any gradient. Discomfort became useful
when the broken observation helper became evidence to learn from instead of a
number to explain away.
