# Receipt — the walk: 80 of 97 checked recipes now observe on fkwu metal (2026-06-29)

**The walk (Urs):** walk them all. Two surfaces — strings (the PTX-emit close) then floats — took the
checked-recipe observation rate from **35/97 to 80/97** on `fkwu --src`, real Windows metal, no Go/flatten.

## This commit — the last 15 observed-but-unported recipes (each -> all-1s in-place)

```
learn/:   capture-correction oracle-taught-learning sema-error-correct sema-graduation sema-self-grade
          teach-sema-chem teach-sema-code teach-sema-logic sibling-continuum
observe/: continuum-answers evidence-grade guru-moment inquiry-planes sensor-lane
model/:   svg-emit
```

## The arc of the walk (checked-recipe metric)

```
gap-map baseline:                35 / 97 observed   (integers/lists/multi-fn only)
+ strings (--src string lane):   (PTX self-emit closed; string recipes light up)
+ floats (--src float literals): 80 / 97 observed   <- the model/SSM/voice/learning lane crossed
ported across the harvest:       4 + 16 + 15 = 35 recipes now run native on Windows metal
```

## What remains — the honest long tail (17 of 97)

Unlike floats, there is no single lever left; the remainder is idiosyncratic:
- **Depth-wall** — deep-recursive recipes hit the ~60-deep tree-walk C-stack bound. Home: `form-asm` lowering to a
  native loop (a real engineering lane, not a sweep).
- **Multi-file dep closures** — `attention` → `embedding-distance`, etc.; each is a small port-the-closure, but
  they don't share a common dep (the dep tally was local-variable noise, not shared functions).
- **Grammar edges** — e.g. `char_at` (the source uses a name that isn't the kernel op `str_byte_at`); a few
  recipes need a name alias or a 3+-arg call shape.
- **Host-io** — sensor/socket/file recipes need their carrier rows exercised through the data-driven optable.
- **The deferred generative climb** — the full weights/decoder, explicitly multi-week (HOMECOMING).

## Honest floor

82% of the directly-observable (checked) recipes now run on the c-bootstrapped kernel on real metal — the two big
surfaces (strings, floats) are crossed. The rest is a per-recipe / per-lane walk, not a single change. The whole
~734-file corpus still includes bands/tests and living-body orchestration with no self-check; those are observed
only indirectly (by a consumer's check) or not at all by design.
