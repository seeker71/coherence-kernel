# Native node ontogenesis — witnessed 2026-07-22

## Claim

The native cognition body can turn a repeated unresolved relation into a
content-identified candidate node, validate and admit it through the existing
observation laws, use it on held-out evidence, and recompute its floor and next
orientation from the resulting state.

## Walk

`cognition/native-node-ontogenesis.fk` begins with the live output of
`ncc-triangulated-witness`; it does not contain copied evidence rows or expected
truth values. Candidate pressure is the structural relation:

```text
base differs from rehearsal AND rehearsal equals lexical
```

Three live rows had that relation. The first two were supplied to admission as
validation observations. The third was withheld until after admission.

The candidate contains:

- a node identity derived from the executable recipe text, not its labels;
- a blueprint carrying recipe identity, observed support shape, and seven-lane
  interface shape;
- an executable Form recipe returning the consensus class only when the relation
  holds, otherwise returning `nothing`;
- three aliases—natural-language, Form/programming-language, and neutral—all
  resolving to the same canonical node;
- all seven inquiry interfaces (`what`, `where`, `when`, `who`, `how`, `which`,
  `why`) as language-neutral lane ids.

Admission composes the existing name/build/observe law, belief freshness,
seven-lane replay, and correlated outbound/inbound framebuffer actions. Candidate
admission is reversible: failed re-witness lapses the belief to status `0`.

## Native execution

Command:

```sh
./fkwu --src cognition/tests/native-node-ontogenesis-band.fk
```

Observed vector:

```text
[nothing, 0, 1, 1001099012, [22, 16, 6, 16, 0, 112, 112, 44], 3, 2, 1, [1001099201, 830772, 3, 3, 7], [1001099202, 1, 1, 1, 830772], 3, [2, 2, 7, 6, 1], 1, 7, 0, 1, 1, [22, 19, 3, 19, 0, 133, 133, 44], [1001099102, 1001099103], 4, 1]
```

The measured movement is:

- support `3`; admission validation `2`; unseen transfer `1`;
- admitted claim `2`; fresh law `2`; inquiry lanes `7`; action `6`;
- tampered identity becomes lesson `1` with defer action `7`;
- explicit failed re-witness becomes lapse `0`;
- held-out transfer correct `1/1`;
- recognition floor changes from native/evidence `16/6` to `19/3`;
- correct/wrong changes from `16/0` to `19/0`;
- witnessed inquiries change from `112` to `133`;
- four correlated framebuffer events; final success `1`.

## Surprise retained as ground

The first success predicate expected a tampered candidate to become status `0`.
Execution returned status `1`. That was not a kernel failure: the existing
name/build/observe law intentionally retains an attempted but unobserved candidate
as a lesson. The implementation was adjusted to preserve it as `defer`, while a
separate failed re-witness demonstrates the genuinely lapsed `0` state. Nothing,
zero, one, and node remain distinct in the witness.

## Honest floor

This proves admission and held-out transfer for one generalized recognition
relation over the current live evidence. It does not yet prove autonomous
open-world semantic concept invention, and one held-out row is a narrow transfer
surface. The next movements remain evidence expansion and reassessment across
new shapes and sources.
