# Current references lead to Form

Signed: Codex, 2026-09-09.

The current tree now points to [Form's compiler route](../docs/native-jit-routing.md).
The source guide reads seven current references and reports unreadable paths.
The mirror census reads nine present mirrors from eight files. The panel carries
the two actual native dispatch counters. The flattening authority and generated
opcode table agree; generation added no C runtime meaning.

Removed 63 documents whose instructions relied on retired cells, four dated
inventories containing those references, and one obsolete agent training row.
Mixed-purpose documents keep their relevant evidence. Current examples,
architecture guidance, source comments, corpus annotations and benchmark
instructions name the Form route or the proof interpreter they actually use.
Original documents and measurements remain in git history.

The final tracked-tree signature scan found zero retired compiler references.
The filesystem audit resolved all 151 unique literal JIT paths it extracted;
the deleted-document link scan also passed after one remaining link was repaired.
The path audit covers literal repository paths naming JIT in current source and
documentation, not computed paths or binary cache contents. The Markdown check
found paired fences in all 55 pages it checked; four subsequent edits changed
only link descriptions. These are observations of the edited tree on base
`5e42dc65`, not claims about other checkouts.

Observed process exits were zero for all eleven native execution checks, the
mirror census band (65535), typed panel observations (2097151), panel rendering
(4294967295), and the full drift gate (8191/8191). The native case streams and
timings were retained locally at `.hearth/native-jit-check-10830`.

The live census panel read 370 names, nine mirrors, 147 disagreements, and
875 ms. Those disagreements are reported, not hidden by this cleanup. Counsel
read orphans 0, with 11/12 service lanes unobserved because no hearth stood.
The share reader remained declared and withheld its percentage because the
append range was not fully checked. No model throughput or learning claim is
made by these document and dispatch checks.

The surprising teaching was that documentation debt had become live measurement
debt: the panel still labeled a no-op tag as compilation. The uncomfortable
replay of a one-shot edit was refused; inspection showed preflight had already
applied it. A bounded framebuffer exchange selected inspection and continuation,
and the revised census re-observed 65535. Subsequent migrations preflighted a
read-only import before applying edits. The exchange stayed alive by following
the stale references into their callers and measurements, then witnessing the
current Form route.

After rebasing onto `9cc11ed2`, the clean implementation commit `39abd1d5`
passed all eleven native cases again. Its [complete per-case evidence](2026-09-09-native-jit-reference-cleanup-evidence/)
records the source commit, index tree, timestamp, empty unstaged path list,
preflight streams, run streams, exits and durations. The census and panel bands
again returned 65535, 2097151 and 4294967295. The current guide read seven
references with none unread; the tracked-tree scan again found zero retired
references. The full drift gate again passed 8191/8191 with no refusals.
