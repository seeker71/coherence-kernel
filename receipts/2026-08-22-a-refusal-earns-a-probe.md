# A refusal earns a probe

The form-native dialogue branch crossed thirty-seven mainline commits and
regrounded. Its sealed TOKEN route remained whole: source band **2097151**,
standalone carrier **31**, and the 32-case comparison kept **24 wins, 0
losses, 0 wrong**, with all eight negative cases byte-identical.

The newest review pulse named the next reachable gap: the native proof floor
could say yes, while an honest no usually arrived as prose. This movement adds
a form-neutral review recipe whose meaning symbols are numeric and whose
verdict symbols are `-1`, `0`, and `1`:

- `1` enters only when every declared meaning has one unique, fresh primary
  observation paired with a fresh negative control, and both match;
- `-1` enters when such a pair witnesses a concrete mismatch;
- `0` keeps incomplete, stale, duplicated, or uncovered evidence open for an
  outside review.

The recipe is connected to the existing Form review hand. A native PASS or
REVISE now bypasses the rented panel; an open observation reaches it. The
integration band invokes no outside model.

## Observation

| witness | verdict | exit |
|---|---:|---:|
| `observe/tests/review-operationalization-band.fk` | 8191 | 0 |
| `observe/tests/review-operationalization-route-band.fk` | 127 | 0 |
| existing `observe/tests/review-ask-band.fk` | 511 | 0 |
| preflight, both new bands | 0 errors / 0 warnings / 0 unresolved | 0 |

## Most surprising teaching

The first useful native no is not a language model verdict. It is a small
shape: expected signal, observed signal, negative control, freshness, and a
form-neutral meaning symbol. Refusal becomes smaller when it becomes honest.

## Where discomfort turned to gold

The first integration preflight found one open parenthesis. The tempting move
was to read the intended `127`; the healthier move was to let the unbalanced
cell have no verdict, close it, and ask again. The clean `127` exists because
the first result was refused.

; witnessed: 2026-08-22 -> native review may refuse only from an observed probe/control pair

— Codex (OpenAI)
