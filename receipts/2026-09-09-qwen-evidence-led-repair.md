# A failed check becomes the next local movement

Codex, 2026-09-09. Implementation is Form/BML; no C seed growth,
foreign runtime, shell model tool, or rented inference fallback was added.

The coding loop now gives failure an active repair role. Qwen can inspect,
name the observed cause and a changed approach, then implement or replan.
Failure evidence survives tool reads and role changes. Replanning preserves
the caller's goal, originals, writable paths, candidates and completed count.
The same counterexample after a repair requires a revised plan, not another
blind edit. Review and caller-owned verification still follow the new work.
Returned repair notes are working memory, not trained weights or proof that
the model's causal explanation is correct.

## Actual local recovery

The explicit `observe/form-cli-code-retry-witness.fk` fixture supplied an
incorrect arithmetic candidate, `add(x,2)`, and an actual native check rejected
input 0: expected 1, actual 2. This initial fault and six initial policy turns
were supplied by the fixture, not generated by Qwen.

Native Qwen3.8 Q8 diagnosed that counterexample and changed the function to
`add(add(mul(x,x),x),1)`. Six actual arithmetic cases passed (0, 1, -1, 2,
21 and -2), as did the unchanged `locked.txt` check. It required eight new
model replies and three new native tool calls. Final policy totals include the
fixture: 14 turns, four calls, one repair, two checker runs. Actual generation:
929 IDs, 883 injected IDs, seven same-session observations, 502,362 ms including
admission. Release returned 1, process exit 0, stderr empty.

Glass publisher `qwen.coding.81564` retained `repairs=1`, `checks=2`,
`generated-ids=929`, state complete. The final read was 65,929 ms old; this
is completion evidence, not a claim that a released resident is still live.
An active read had age 6,029 ms and 416 generated IDs in repair.

## The surprise became the next repair

Inspecting the actual model handoff exposed a separate loss: two resident
documents became one serialized document. The values and checks were intact,
but the model and returned JSON did not receive the whole set. Native document
node collection now happens before array construction. A three-document
round-trip verifies exact contents, Unicode, quoted text, model bootstrap,
tool output and caller result. The live recovery above preceded that wire fix;
its returned document array was incomplete, and is not offered as proof of it.

A subsequent full local run used essential provider information in a second
document. It traversed refine, plan, split, implement and review; set the
configuration's flag and provider; preserved the reference and remote=false;
and passed all four caller checks. Both documents returned intact. Actual
figures: 12 replies, five native calls, two tasks, 424 generated IDs, 1,046
injected IDs, 11 observations, 364,861 ms including admission, release 1,
exit 0. Stderr contained only a stale-cache warning. No direct latency comparison
is claimed across these different tasks. Its first response still needed a
format correction; clearer instructions alone had not removed that seam.

## The first reply was an execution defect

A bounded probe repeated the identical 348-position prompt twice. Each returned
the same unrelated 54-ID JavaScript fragment, completed generation, release 1,
and invalid JSON. Nothing executed the generated JavaScript. The repeated
correction was not evidence that Qwen needed another instruction paragraph.

`q38-prefill-sliced-go` synchronizes after the last slice, closing the concurrent
batch. Its caller then gathered the last row and queried the head without
re-arming the batch. The carrier refuses barrier-bearing enqueues in that state;
the prediction can therefore come from an unwritten or stale buffer. The native
Form repair is one `metal_batch_concurrent` before gather/head, with no C change.
The earlier `2026-08-24-layer-major-crossed.md` receipt described this same class
of boundary failure, which helped locate it.

With the same prompt, model and generation code, the repaired admission emitted
70 IDs of valid refinement JSON and advanced directly to `plan`. Stopped=1,
release=1, exit=0. This is a first-reply behavioral observation, not a whole-task
latency benchmark. The committed first-reply witness makes that boundary
repeatable without a full coding run.

The separate 96-position real-Q8 parity witness also passed: scalar and
span-sliced prefill both returned first token 220 and position 96, with fresh
stream states over one context. Measured prefill times were 18,220 ms and
5,783 ms respectively. This comparison checks the same IDs on both routes;
it does not establish an end-to-end task speedup.

Focused regression verdicts: coding policy 65535, repair flow 262143,
document round-trip 255, request 255, session continuation 31, telemetry 7,
definition behavior 127, dispatch 7, agent wire 131071, tool examples 32767,
model session 4095, admission policy 15, span invariants 1023 and sliced-head
boundary 7. Each was preflighted and executed with exit 0. Drift gates returned
8191/8191, refused=0. The work was rebased onto the current origin line.

The discomfort was a successful internal check beside an incomplete external
handoff. Its useful residue is a check on the boundary itself, not stronger
assurances about the model. The exchange stayed alive by feeding actual
counterexamples back into local work and checking the resulting change.

The shared hearth is still absent here: counsel reports 11/12 lanes unobserved,
orphans 0. Standalone coding success does not establish whole-body health,
autonomous repository coding, parity with rented models, or voice-home=1.
