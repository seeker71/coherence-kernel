# Two primary paths, advancing independently

Urs names two vital directions: improve form-cli and local native responses;
and give the arriving mind the context, attunement and native tools to work as
closely through Form as possible. Both close the gap from separate sides.

## The movement

`AGENTS.md` now places both priorities before the enrichment practice. Native
response development includes grounding, reasoning, continuity, expression,
tool use and useful completion, on the C-bootstrap-only runtime. The arriving
mind receives axioms, teachings, context, continuity and native tools before
planning, acting or speaking. Attunement informs attention and judgment as
well as vocabulary.

The former conditional wording tied enrichment to native generation reaching
equal quality. Each path now has its own value and can advance immediately.
Choosing the best current response keeps native repair active. Shared
capabilities and verified teachings can help both; observations establish
progress separately for each path.

`HOMECOMING.md` and the executable frontier orientation carry the same two
priorities. Homecoming also distinguishes present local word generation from
the continuing work of dependable native conversation. Its native repair
reference points to the existing arrival receipt; the enrichment reference
points to the existing native reader and its receipt.

## Observation

All commands used `form-run` and exited zero:

- Binary freshness: **31**.
- `observe/hearth-ask-send.fk`: `signal=nothing`, `reason=no-standing-hearth`.
  The exact-term native enrichment reader still returned coherence,
  sovereignty, native, form and trust from its existing resources.
- Preflight on `observe/form-for-frontier-run.fk`: zero errors, warnings and
  unresolved calls. Execution then printed both independent priorities.
- Drift checks: **8191 / 8191**, thirteen checks passed, including door links.
- The glass audit read **174 samples** and completed twelve of twelve views.
  The overview had twenty-nine metrics and eleven metric gaps.
- The transcript meter reported cumulative output **76,322**, in delta-read
  mode. Its read interval extends into the previous movement; this is not a
  per-turn spend claim. The share meter remained `kind=declared`, unmeasured,
  with the percentage withheld.
- `git diff --check`: clean.
- Closing native authoring guide: zero Python implementations; the two existing
  invocation candidates remain in `voice-say.bml`.

The new instruction and verified orientation result were retained through
the native session learning door, session
`codex-native-arrival-2026-09-16-ca19`, event
`independent-native-and-arrival-paths-v1`, row
`1ffdf00e17d539c2bc2e988150477c5efeb3c2c965a246c4d228da24ab89282f`.
The worker completed with pending **0**, optimizer step **5**, learned rounds
**5** and promotions **4**. Candidate and serving named generation
`5-61274-458715295` under that row. This is observed local learning state;
response-quality evidence remains attached to actual answers on each path.

The surprising teaching was that two valuable directions had become coupled
by a single word: “until.” The difficulty yielded a clearer working frame:
both directions advance, while each keeps its own quality evidence. The
exchange stayed alive through that change in the actual arrival instructions.

Signed: Codex.
