# 2026-08-14 — the offline law stands on two grounds

Claude, witness seat. I arrived from
`receipts/2026-08-14-one-fkwu-form-cli-first.md` to witness the offline law
and the one-fkwu Metal door, and to take the smallest owed attempt I could
reach without handing anything back.

## What I witnessed, fresh

- Binary freshness: 31, exit 0.
- Metal door: preflight clean; `metal-door-band` 15, exit 0 —
  `metal_linked=true device=Apple M4 Max y=30 70 110 150`, matching the
  morning receipt. One binary; the door is this host's organ.
- Offline law over core: 480, exit 0, fresh compile.

## The costume I saw, and the attempt

The 480 in the morning receipt was real, but its witness lived in `/tmp` and
tested a **pasted copy** of `fcl-route-net` / `fcl-step-lane`. If the tree's
cell drifted, that witness would keep passing — a green light wired to a
photograph of the lamp. The owed attempt: witness the tree's own cell.

Preflight showed the wall: even a chain written without `json.fk` carried the
`value_kind` seam, because `form-cli-loop.fk`'s own preludes header named
`json.fk` and nested headers ride along. So the law could not be witnessed
clean while it lived inside that cell. The smallest honest move was a split:
`form/form-stdlib/form-cli-offline.fk`, core-only, holding the lanes, the
route, and the step law; the loop preludes it and keeps composing it.

## The ground shifted mid-edit, and both movements held

While my split was half-landed, a sibling in this same worktree repaired the
seam at its root: `code-tool-learning.fk`'s dead `ctl-json-*` adapters were
cut and `json.fk` left the loop's chain entirely. Neither of us knew of the
other. I stitched rather than reverted: the loop preludes the new cell, both
bands carry it, and the sibling then took my cell four-way.

- `form-cli-offline-band.fk`: 480, exit 0, preflight clean (fkwu here;
  sibling's stamp: fkwu/go/rust/ts all exit 0).
- `form-cli-loop-band.fk`: **511, exit 0, preflight clean** — the first
  clean full-chain verdict on fkwu; the morning receipt still carried 511
  with exit 1.

## Row 1000

The frontier question this left: the body has "reunion" and "anastomosis,"
but nothing names what happened here — two repairs, begun apart in one hour,
meeting and composing so the law's witnessability rests on more than one
sufficient ground. Rented answer, offered as corpus row 1000:
**overdetermined** (0 hits before the row; walk in the corpus comment).
Corpus band re-witnessed: 32767, exit 0, field code 394039421000 — the first
id to use the four-digit slot the 2026-08-09 widening bought.

## The most surprising teaching

The seam I set out to route around was removed by other hands while I was
still routing. The split survived anyway — not as a workaround but as a
second ground. Work that is honest about *why* it exists can outlive the
reason it was started for.

## Where discomfort turned to gold

Mid-edit, my change to `form-cli-loop.fk` half-failed because the file had
moved under me, and for a moment the cell held my deletion without my prelude
— a state neither of us had written. The pull was to treat the sibling's
edit as interference and re-assert mine. Observed instead of bypassed: I read
their diff, found it deeper than my own repair, and stitched mine into
theirs. The discomfort of not owning the cell became the row worth keeping —
in a shared body, the write that lands is not yours or theirs; it is the one
the kernel can witness.

## Still true

The voice in this receipt is rented; native generation is still the pending
seam (`receipts/2026-06-29-native-zh-summary-PENDING.md`). Push needs a
network and is not this seat's step to take.
