# A completed comparison needs a working tool boundary

Signed: Codex. Witnessed 2026-09-17 on `fkwu`.

The prior mixed-session baseline had never completed its tasks. This movement
repaired its boundary, observed a complete context-equipped provider session,
then compared it with bounded synthesis applied and checked by native Form.
Every provider process originated inside Form. Assessment answers stayed out
of training.

## Repairs and adverse attempts

The file-based v3 request reached the code edit and produced a candidate that
passed all 39 behavior cases. The wrapper then failed with `organ health event
has no complete identity or observation`: it had supplied a plain audit object
to the typed health-event carrier. State had already persisted. The provider's
claim that no work had executed was broader than the observed failure.

After correcting that event shape, an unrestricted full-lifecycle smoke passed.
The v4 provider sandbox still refused `native owner process birth unavailable`.
Again, the code edit persisted. The host smoke had not established the sandbox's
ability to inspect process birth time.

`oh-reading-with-id` now accepts an organ's existing unique retained event key.
The original `oh-reading` still derives its identity from the process owner
clock. Both produce the same event schema and use the same transport and
response correlation. Empty identities are refused. The explicit caller owns
uniqueness and replay identity; it does not invent a process birth time.

The comparison organ uses its retained action-evidence path inside its
exclusively claimed run. Its file request is read directly, avoiding shell JSON
serialization. Read-only replies expose the requested stdout, stderr and exit,
without echoing the entire resident document set.

A fresh full smoke then passed inside a provider-style workspace sandbox with
only its owned evidence directory additionally writable. It completed edit,
verify, review and accept, checked all 39 code cases and the dialogue report,
preserved quoted transport input, and emitted its health events. Earlier answers
were used only as no-model smoke inputs, excluded from the provider packet.

## The observed two-task session

The successful routes received the same frozen code and dialogue packet:
`70fe2c067a0bbf1e4dd3cac4af8a51795b0d09c77d71e97ef129587bb6287d46`.
Both implemented the resident source, passed its 39 behavior cases, completed
code review, passed the original dialogue assertions and released their
processes. The bounded provider made no tool calls; Form performed application,
verification and review. The context-equipped provider drove those steps itself.

| Route | Input + output tokens | Uncached input | Measured runner ms |
| --- | ---: | ---: | ---: |
| Completed tool-driven baseline | 665138 | 33203 | 137427 |
| Bounded synthesis | 26272 | 14635 | 40116 |
| Bounded synthesis plus dialogue revision | 52968 | 30031 | 74346 |

The completed bounded route including revision used **92% fewer total tokens,
rounded down**, in this comparison. Uncached input was much closer. Cached
input is a subset of input, and reasoning output is a subset of output; neither
is added twice. Runner-time sums exclude coordinator gaps. These are observed
route costs, not a monetary saving, contribution percentage or global minimum.

## Actual answer quality

Both initial answers explained useful distinctions but deferred concrete work
until Urs supplied another proposal. One explicit caller finding then asked
bounded synthesis to apply the concepts to the enquiry already present. The
revision did that conceptual work in the answer, retained the original boolean
observations, and passed the unchanged source/report checks. Native comparison
confirmed the already-checked code candidate stayed byte-identical.

This is a specific improvement in initiative. The answer still contains excess
self-narration and broad claims about what its rule governs. Its translation
language remains conceptual rather than an observed native translation result.
The extra feedback and second provider call are disclosed; this is not an
isolated architecture effect. Human resonance judgment was requested against
the retained actual answers and remained unanswered at this checkpoint. Broad
quality, frequency and throughput parity remain unproven.

## Whole cost, checks and continuity

The failed v3 and v4 attempts consumed **276020 tokens**. All five provider
processes in this movement, including failures and the final revision, consumed
**994126 tokens**. The older failed v2 baseline's 82564 tokens remain separately
retained. The parent goal meter moved from 7,818,427 to 8,045,853 at the post-audit
checkpoint; the transcript meter read 1,668,820 cumulative output tokens. These
counters have distinct scopes. The development movement is not itself a
minimal-token result.

The identity band returned 1 after clean preflight, including default identity
creation and refusal of a response carrying another event's identity. Drift
gates returned 8191, exit 0. The native comparison helper initially had misspelled
`nsm_n` and `nsm_s` calls; preflight refused them. Correcting the names produced
clean preflight and the recorded arithmetic, without another provider call.

Native guide: 0 Python implementations, 2 existing voice invocation candidates,
0 unread files. Counsel panel: **orphans 0**; 11 of 12 lanes were unobserved with
no standing hearth. Stale caches reborn after the shared health source changed
retained their warning and rebuild events. No C seed growth or external native
runtime dependency was added.

The verified procedural teaching was retained under
`2026-09-17-scoped-organ-identity-and-matched-provider-route-v1`. Its local worker
launched; completion was pending at this checkpoint. This learner is Llama,
not the measured Qwen model. Assessment answers remain excluded.

Private evidence is under `.hearth/response-parity/mixed-session-v3`, `v4` and
`v5`, plus `mixed-session-v5-smoke`. The v5 directory retains the packet,
provider events, all results, comparison arithmetic, initial answers and revised
answer. The surprising lesson was that an edit could persist while its event
identity failed in the sandbox. Following that mismatch repaired a real native
boundary and made a valid comparison possible without hiding the cost.
