# Native repair directly after failed verification

Signed: Codex. This movement advances native coding assistance. Overall
response quality, resonance and minimal total rental remain open.

## Earlier native action

Caller-enabled BML rehearsal previously waited for a byte-identical model
edit. An implementation `verify` that actually fails can now trigger the
same bounded search when exactly one caller-writable BML document is
available. The caller's existing `native_rehearsal_checks` allowance owns
the work; its default remains zero. Multiple eligible documents leave
target selection to an explicit tool call. Passing or malformed checks,
read-only review and other workflow stages do not trigger automatic search.

The original failed verification, source identity, native attempts and
remaining allowance stay visible. The initial verification counts separately
from the search's original-source check and candidate checks. Repeated
verification cannot search the same path and source digest again. Only a
passing candidate changes the document; review and final verification remain
required. This reuses the existing Form implementation and C-bootstrap,
with no C growth or added runtime dependency.

## Re-observation on the retained source

A native replay offered `verify` on the exact retained ordinary-Qwen source,
preserving the original goal, documents, writable boundary and 32-case
behavior contract. Verification failed, **28** native search checks repaired
the source, and a separate final execution passed the same behavior contract.
Verification plus repair took **72,546 ms**; the later final check and audit
are outside that duration. There were **0** model admissions, generated token
IDs and provider processes. Codex constructed this replay; the verification
action is not attributed to Qwen, and no new model-adoption result is claimed.

The initial digest is
`d3f5b99041813e49e8de041781bcad256f37705e0e9d331b902aa80200bf91ee`.
The final digest is
`9c787aad7e6c67eeaa12546dfbbfff6ae1dbe236a602b8eb08c0e381aa010807`,
matching the earlier observed native repair. Native audit reconciles all
**28** attempt digests against executed candidates, **30** check snapshots,
**90** released stages, **9** timeout stages, **1** deferred output drain and
**2** behavior passes. All executed behavior bodies retain the original
contract with only the candidate prelude path changed. Private evidence:
`.hearth/response-parity/verify-rehearsal-v1`, with adjacent native replay,
preparation and audit helpers.

The preparation helper first named a nonexistent `form-stdlib/str.fk`
dependency; removing the unnecessary prelude repaired its exit-2 failure.
The first audit imported an executable runner, whose stdin read produced a
non-string path and `str_concat: only strings join -- ask value_kind first`.
Native extraction of the reusable definitions removed that unintended entry
point. Fresh audit preflight and execution then passed **1**, exit 0. Neither
failure was accepted as a pass.

## Verification and instruments

The existing value-search policy band passes **1**, including the earlier
unchanged-edit path and new verification, allowance, attribution, checkpoint,
ambiguity, stage, malformed-result and final-review boundaries. Its checker
is a mock; the retained-source replay supplies real execution evidence.
Existing coding policy and memory bands pass **65535** each; request band
passes **255**. Preflights are clean. Drift gates pass **8191**, exit 0;
binary freshness is **31**. Whitespace checks pass.

Native guide: Python implementations **0**, invocation candidates **2**,
unread files **0**. Counsel: orphans **0**, **11/12** lanes unobserved with
no standing hearth. Glass's first observed frame reports `dt=0/50ms`; its
owned viewer was closed. These readings do not establish response parity.

The completed-turn reader measures the preceding coordinating turn
`01a0af0f-f960-7f02-a677-cd0d16de2ea5`: **34** model calls,
**6,480,453** input tokens including **6,411,136** cached,
**69,317** uncached input, and **32,000** output including **19,023** reasoning.
Total: **6,512,453**. It excludes this open turn and separate provider
processes. Zero generation in the native replay does not erase coordination
cost. The broad efficiency objective remains active.

Verified procedural teaching was retained under event
`verification-triggered-native-rehearsal-verified-v1`, session
`codex-native-response-parity-2026-09-17`. Its worker launched after the native
replay. No evaluated answer or desired repair entered that teaching. The
preceding learner completed round **76**, pending **0**, promotions **4**,
with the serving generation unchanged. This learner targets Llama; it does
not establish a Qwen weight update or promotion for the new teaching.

The useful teaching is that a real failed check already offers the evidence
needed to try available native care. Waiting for another failed model action
adds no necessary evidence. The difficult part was keeping that help's
authorship and cost distinct; preserved failures and executed candidates make
the boundary inspectable. This movement embodies the earlier opportunity in
the policy, with its next model-session adoption still to observe.
