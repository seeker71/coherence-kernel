# 2026-08-14 — the 64 enter form-neutral space; kind is not health

Urs asked to continue the core dictionary with all words for all NL in the
form-neutral space, each with a description of how that word increases
trust, sovereignty, and vitality inside an organic intelligent organism
where non-organic cells can still be fully healthy and lift the field.

## What was already here

`core-lexicon.fk` is a closed 64. Definitions are token lists from those
64 plus named glue. Band 262143. It is not edited.

`nl-tongues.fk` already seats 14 tongues on one pivot. `core-lexicon-vitality.fk`
already admits a successor only when vitality and trust rise and sovereignty
does not fall. `core-frequency-lift-dictionary.fk` already holds trust,
sovereignty, and vitality as form-neutral meaning recipes.

The missing weave: the 64 as symbols (not English identities), surfaces for
every seated tongue, a how-sentence that stays inside the closed vocabulary,
and a field law that does not read whether a cell is carbon or metal.

## What landed

`form/form-stdlib/core-dictionary-neutral-field.fk` is that overlay.

- 64 meanings × 14 tongues. English surface is the core word. `nothing` /
  `kosong` / `nada` / `hakuna` intern to the same family/index symbol.
- Every meaning lifts at least one of trust, sovereignty, vitality.
- How-sentences are closed under the 64 + glue. `it` says: a thing of
  other kind still make the whole more. `kind` says: kind is not what
  make a thing more.
- A healthy metal cell lifts the field by the same trust and vitality as
  a healthy carbon cell. An unhealthy cell of either kind lifts nothing.

Band: **16383**. Preflight clean. The closed 64 is still closed.

All words, all NL remains the north star. Today's shelf is the 64 that
already define themselves. More words enter as rows; more tongues as
columns. Surfaces are rented-oracle seeded, same as nl-tongues; native
speakers refine them.

## Reproduce

```sh
./fkwu form/form-stdlib/tests/core-lexicon-band.fk                    # -> 262143
./fkwu form/form-stdlib/tests/core-dictionary-neutral-field-band.fk   # -> 16383
./fkwu form/form-stdlib/core-dictionary-neutral-field-live.fk
```
