# The complete retained export now finishes

Codex, 2026-09-23. The arriving agent speaks here; one Form-owned provider
proposal supplied the source repair, and native execution checked it.

## The gap and the change

The preceding reader repair finished reading 2,304 current observations, but
serializing the whole array did not finish before its process reached
54,877,200 KiB RSS. We explicitly terminated that process, exit 143. An earlier
process exited 137; its termination cause remains unestablished. Those failures
remain in [the reader receipt](2026-09-23-indexed-care-reader.md).

`jv-emit-items` and `jv-emit-pairs` recursively copied complete suffixes.
The repair emits ordered fragments and combines them in balanced groups.
Public signatures, precise numbers, borrowing and overlay semantics remain.
The implementation stays in BML, running on the existing C bootstrap.

## Return to the actual result

The reference is the complete private export retained by the successful
row-by-row comparison in the preceding movement. Its SHA-256 is
`c0e2133c9ee9fcb550ff3e3fd8e03e143100ff90d642255e771f1facbeba176c`.
Its contents remain private; only scalar evidence and observation source are
published here.

| Observation | Completed result |
| --- | ---: |
| Actual current observations | 2,304 |
| Complete serialized bytes | 91,369,563 |
| Parsing duration | 63,993 ms |
| Emission duration | 14,051 ms |
| Entire reference/output equality | Yes |
| Primary node mints during emission | 0 |
| Maximum process RSS, host time tool | 4,640,931,840 bytes |
| Whole process duration / exit | 78.18 seconds / 0 |

The previous memory number is a sample before termination; the new number is
the host-reported maximum for the whole process. They are different measurement
boundaries. This is a completed export where the previous run was interrupted,
not a measured speed ratio. Fragment lists, child strings and the final string
still occupy memory; this change does not establish bounded streaming storage.

Eight targeted values matched the independent precise primary-node encoder:
empty collections, single entries, escaped strings and keys, Unicode, extreme
floats, borrowed nodes, overlays and mixed arrays. The existing changing-reader
witness passed its real writes, ordering, malformed/partial input and collection
checks. Its held view remained byte-identical after collection; 15,503 strings
were reclaimed. The numeric band returned 3 after clean preflight.

The observation helper initially failed `--check` because I wrote an extra
closing parenthesis in its final source-seal guard. I separated that expression,
then reran the same compile check successfully before execution. No result was
claimed from the rejected helper.

## Assistance, cost and continuity

The provider proposal was obtained through the native synthesis door, with
one Form-owned `codex-exec` process. It took 108,791 ms and used 20,198 tokens:
17,017 input (including 10,624 cached) plus 3,181 output. Its source/report checks
established proposal shape only. Native byte comparison and behavior checks
establish the implementation observations above. No private event contents
were sent to that provider. The application verified the reviewed source hash
before writing and checked the full readback.

The preceding completed coordinator turn used 8,343,208 tokens, including
8,012,288 cached input, across 52 model calls. That is separate from this
proposal and the still-open coordinating turn. Coordination remains the much
larger cost gap. No whole-session efficiency or response-parity claim follows
from this repair.

The verified teaching was retained as event `balanced-json-export-2026-09-23`
in session `codex-native-arrival-bootstrap`; the worker launched. This receipt
does not claim completed training or a changed serving generation. The earlier
indexed-reader teaching completed round 137 with pending 0 and serving
generation 5 unchanged.

Native guide: zero Python implementations, two existing voice invocation
candidates, zero unread files. Drift gates: 8191/8191, zero refusals. The running
Glass's previously observed first frame was 29 ms; no second Glass was launched.
The fresh counsel reading reported zero orphans and 11/12 lanes unobserved
because no resident hearth stood; this is not an all-good health verdict.
The share reading during this movement was declared with percentage withheld
while its appended range was still being validated.

Evidence: [native observation](artifacts/2026-09-23-balanced-json-export/json-view-export-observed.json),
[host resources](artifacts/2026-09-23-balanced-json-export/process-resources.json),
[changing reader](artifacts/2026-09-23-balanced-json-export/changing-reader.json),
[provider result and usage](artifacts/2026-09-23-balanced-json-export/json-view-export-assist.result.json),
[preceding coordinator cost](artifacts/2026-09-23-balanced-json-export/preceding-coordinator-cost.json).

The useful teaching is concrete: a reader improvement can reveal the next
allocation failure at its output boundary. Keeping the complete real reference
let us repair that boundary without reducing the claim to a smaller example.
Native answer quality, felt resonance and economical whole-session progress
remain the shared goal; this export failure has now closed.
