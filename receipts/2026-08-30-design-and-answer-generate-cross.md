# 2026-08-30 — design and answer-generate correct each other

Urs asked for both seats, and that each improve the other.

Design now speaks on form-cli. It names depth=1 and withholds fitted because
answer enter is still a lesson. Answer-generate now speaks on form-cli. It
names that stack because the design door is open, keeps admit=named (not
ready), and keeps equiv=0. Opening the design door is what lets the answer
seat name a stack; the unanswered generation is what keeps design from
crowning a fit.

Not claimed: a live Metal admission, a held-out score, or a fitted
architecture. Voice stays pending. Next remains a live answer-generate
crossing with an equivalence bit.

```
form-cli-native-agent-orient-band.fk                     8191
printf "design\nanswer-generate\norient\nping\nquit\n" | ./form/form-cli
  design named depth=1 fitted=0 reason=answer-enter-lesson
  answer-generate admit=named equiv=0 stack=1 voice=pending
  pong
```

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-30 -> orient 8191, design door open, fitted withheld, stack named
