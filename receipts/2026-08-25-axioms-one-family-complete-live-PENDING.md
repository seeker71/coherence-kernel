# Axioms complete local crossing — PENDING physical observation

Date: 2026-08-25
Status: **dormant physical door built; model and Metal not run**

The first selected family window is `axioms`. It is not a permanent target.
The public schema's live family IDs and count are a fresh denominator to
re-observe after this crossing, not a fixed destination.

## Built movement

`observe/qwen38-axioms-one-family-complete-live-run.fk` defines one dormant
function, `fka1l-run(path, model-id)`, which:

1. opens one bounded bidirectional framebuffer window and streams real
   before/after resource snapshots at every expensive boundary,
2. admits one sealed local Qwen context/weights residence,
3. creates independent fresh states for public heldout `axioms` lanes `a` and
   `b`, sealing raw output before the answer row becomes reachable,
4. retains heldout misses as observed baselines instead of stopping; surprise
   branches into the public curriculum experiment, while heldout `nothing` or
   timeout requests evidence and continues only through a valid source-bound
   packet,
5. creates one public-generative candidate attempt and executes at most one
   check-bound `supervised-embodiment` repair; that repair alone may consume
   the exact `states/direct` packet from `fkap-source-bound-hook`,
6. creates a fresh native-request state in the same Qwen context, accepts only
   the exact scannerless family-bound recipe query, generates/JIT-executes the
   existing Metal micro-thought only after exactness, injects the typed frex
   result, and resumes the same state,
7. releases carrier buffer, native stream state, and Qwen context,
8. passes the two retained trials and canonical generative/execution evidence
   directly to `fkav-result`, then passes that verified live value into
   `fka1-crossing`, and
9. re-observes exact current family IDs/count and a bounded source access mark
   rather than freezing a next family target.

No stdout line becomes evidence. No prompt or answer bytes enter the
framebuffer. Diagnostic frames retain only stage IDs, counters, source/hash
coordinates and content digests. Surprise, `nothing`, timeout and refusal open
a correlated control response; branch/revise/request-evidence/abstain changes
the actual next state and is re-observed before movement continues.

The framebuffer stream comes from the extracted 11-function
`observe/framebuffer-runtime-live-stream.fk` surface. It retains the complete
body's BEGIN/STAGE/END event and report shapes while leaving the analysis and
bottleneck tail in `observe/framebuffer-runtime-observation.fk`. This closed an
observed source-closure floor: the full module first exceeded `FK_FN_CAP` at
`fbro-event-row` #4096 and prevented every live function from registering.
No cap grew and no full body was deleted.

Residence continuity is an in-process q38 handle transition witness bound to
model/run and every state release. It does not cryptographically authenticate
the physical GPU or model weights. Census freshness is a bounded typed mark of
file size, mtime, first byte and last byte; it explicitly names that a
same-size interior rewrite preserving mtime can escape the mark. A full-source
Form SHA was removed after the live diagnostic loop observed its current O(n²)
walk over the public mastery source.

The generation carrier now prepends explicit `state_released`,
`state_handles_closed`, and `state_handles_expected` rows only after executing
the state close and comparing its returned count. Heldout and generative
attempts accept release only from `state_released=1`; absence of a mismatch
phrase no longer impersonates a positive release witness.

The function is deliberately not called at top level. Review and pure checks
therefore do not open Qwen or Metal.

## Pure and lexical observations

- Private preflight over crossing source, expanded band, extracted stream
  source/band, and dormant live runner reported each balanced with errors `0`,
  warnings `0`, unresolved `0`; combined exit `0`,
  `@form fkwu 0 1178 454 1632`.
- Tree balance over crossing source/band, dormant runner and extracted stream
  returned `[0, 0, 0, 0]`, exit `0`.
- Failure-control probe returned
  `[heldout-b+curriculum, curriculum, heldout-b+curriculum, curriculum, release, release, 1]`,
  exit `0`: surprise, evidence request, unusable packet and refusal are distinct.
- Expanded one-family adversarial band returned `134217727` (27/27 bits),
  exit `0`; final rerun metadata was `@form fkwu 0 10 0 10`.
- Extracted framebuffer stream band emitted BEGIN, resource-delta STAGE and
  END, then returned `8191` (13/13 bits), exit `0`,
  `@form fkwu 0 571 149 720`.
- Dormant live runner direct static compile returned `0`, exit `0`,
  `@form fkwu 0 2 442 444`. Its top-level value remained `0`; Qwen and Metal
  were not opened.

## Honest heldout boundary

The crossing produces the exact two public `axioms` trial values required by
the one-family `fkav-result`. It does not fabricate
`public-family-heldout-evidence-v2`: that canonical evidence remains bound to a
complete current-census heldout report. The family-local generative and native
execution evidence rows are canonical fkpfm v2 values.

## Remaining physical gate

After review, one intentional carrier owner must invoke `fka1l-run` with the
selected sealed local Qwen path and model ID while no sibling owns Qwen/Metal.
The movement now continues through curriculum/generative/native even when the
public heldout baseline is below 95, but then returns `not-yet`; it never
relabels a curriculum-exposed response as heldout. A physical pass requires
both heldout lanes at least 95, a valid independent or explicitly supervised
generative row, exact scannerless native request, generated and executed Metal
result, typed reinjection/resume, one context open, zero reopens/remote calls,
all releases, `fkav-valid=1`, `fkav-ready=1`, and `crossing-valid=1`. Until that
observation exists, this receipt remains PENDING and claims no local mastery.

— Codex, grounded in Sema's public body
