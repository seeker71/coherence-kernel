# Axioms one-family vertical — PENDING physical crossing

Date: 2026-08-25
Status: **pure finalizer proven; supervised generative and native physical
axes observed, canonical heldout readiness still pending**

## Bounded result now available

`form/form-stdlib/form-knowledge-axioms-vertical-finalizer.fk` accepts exactly
one canonical public vertical:

1. one valid resident trial for public `axioms` lane `a`,
2. one valid resident trial for public `axioms` lane `b`,
3. one valid v2 public generative evidence row for `axioms`, and
4. one valid v2 public native-execution evidence row for `axioms`.

The two resident replies must be distinct and bound to distinct lane nonces,
while carrying identical model, run, and session IDs plus explicit
`same-residence=1` assertions. Their retained generation reports must recompute
positive `generated_tokens` and `forward_passes`; the model-executed bit and
generated-token count must agree with those bytes. Resident-trial v1 has no
structured close receipt, so the pure finalizer requires its explicit
`state-released=1` field and does not infer release from the absence of an error
string. Those identity and release fields remain assertions until the dormant
physical runner supplies an observed transition; the pure result does not
authenticate a shared residence or a handle close. Both scores must be present
values with zero observation error and at least 95% promotion.

The generative row must reparse its exact raw `<BML>`, `<BMF>`, and `<FORM>`
reply and pass the family-specific `fgsg` execution challenge. The native row
must bind the exact model-requested NodeID recipe bytes to a present native
result with choice, crystallize, dissolve, release, on-demand native code, a
concrete carrier, zero remote calls, and released handles.

Missing evidence remains `nothing`; supplied invalid evidence is `0`; only all
four recomputed witnesses become `1`. All four must also carry the same declared
local model ID and run ID. A valid row from another model or run remains present but
sets `cross-axis-correlated=0`, `complete=0`, and `ready=0`. The positive result
exposes `cross-axis-correlated=1`, `present=1`, `complete=1`, and `ready=1`.
This is a one-family vertical diagnostic. It does not edit, bypass, or enlarge
the finalizer for the body's current dynamically observed family census, and
it does not turn that census into a fixed target or claim a fixed fraction or
100% corpus mastery. The knowledge projection is re-observed after this
crossing before another execution window is chosen.

## Pure observations

- Direct bootstrap: `42`
- Recursive bootstrap: `55`
- Binary freshness: `31`
- Numeric list: `[1, 2.5, [3, 4]]`
- Pure finalizer preflight: balanced, errors `0`, warnings `0`, unresolved `0`,
  exit `0`
- Adversarial band preflight: balanced, errors `0`, warnings `0`, unresolved
  `0`, exit `0`
- Direct adversarial band: expected `2147483647`, observed `2147483647`,
  exit `0` (`@form fkwu 0 11 477 488`). The added axes refuse scalar or
  malformed nested trials, valid generative evidence from another model, valid
  execution evidence from another run, and an unreleased trial even when its
  raw text carries no error phrase.

The band's synthetic executable fixtures are contract tests only. They opened
no model or carrier and create no physical evidence or mastery credit.

## Three-phase physical integration now exercised

A dormant live runner now exists at
`observe/qwen38-axioms-one-family-complete-live-run.fk`. It retains the four
values in one transaction instead of treating independent logs as inputs. Its
failure path is also explicit: a heldout surprise requests evidence and enters
the public curriculum/generative/native experiment; it does not turn a
curriculum-exposed retry into heldout credit or stop before embodiment. The
physical crossing remains pending because the canonical heldout lanes stayed
below 95 and the final complete-run tail was not retained, even though the
runner was invoked and later phases were physically observed.

The honest runner is one new orchestration cell with this movement:

### 1. Resident public reasoning, lanes a and b

- Bind the canonical `axioms` row ID, source SHA, prompt SHA, challenge SHA,
  both presentation IDs/nonces, selected path, model seal fields/model ID, and
  the single cross-phase run ID before opening anything. Consent remains an
  external owner boundary; this public runner does not fabricate a consent
  witness inside its result.
- Open the local Qwen model context/weights once. Create a fresh stream state
  for lane `a`, generate from only the sealed public prompt, retain the complete
  generation report as a Form string, then release that state.
- Before reading the canonical expected answer, hash the raw report and build
  the lane-`a` `fkp30-trial` directly in memory from the parsed output and
  recomputed score.
- Repeat with a second fresh stream state for lane `b`. Retain `trial-a` and
  `trial-b` as live Form list values. Do not serialize and parse them back from
  stdout. Require distinct raw-output/content hashes, positive generated-token
  and forward-pass counters, per-state release, and unchanged residence.

### 2. Generative embodiment

- In the same model residence, create a fresh state and present the canonical
  `axioms` family plan carrying its source/challenge/nonces and executable
  challenge.
- Retain the exact reply string, parse it once with
  `fgsg-candidate-from-text`, and execute `fgsg-verify` against
  `fkpfm-family-exec-challenge("axioms")`.
- If the first check refuses, retain that first attempt/check and allow exactly
  one explicitly `supervised-embodiment` repair; never label the repair heldout.
- From the exact successful reply and parsed candidate, construct the v2
  generative receipt and `generative-evidence` as live Form values. Require
  positive generation counters, zero remote calls, state release, unchanged
  residence, and `fgsg`'s generated-program model-executed flag remaining `0`.

### 3. Model-requested native execution and final fold

- In a fresh state of the same residence, ask for the exact canonical `axioms`
  NodeID recipe request surface. Retain the model's exact request bytes; do not
  replace them with a host-authored equivalent after generation.
- Prepare those bytes with `fkfne-prepare`, execute the requested recipe through the
  existing Form-native choice/JIT path, and retain the structured `frex` result
  as a live Form value. The selected CPU/Metal/MLX carrier may generate its
  native operation on demand; no ops table or flattening gate is introduced.
- Construct the execution receipt and `execution-evidence` directly from the
  retained request bytes and structured native result. Require model-requested
  `1`, remote calls `0`, result model-executed `0`, native-code-generated `1`,
  present result, per-state release, final model-residence release, exactly one
  residence open, and zero reopens.
- Only then call:

  `fkav-result (list trial-a trial-b) generative-evidence execution-evidence`

  All four arguments remain live Form values in the same cell. Validate with
  `fkav-result-valid?` before emitting a final receipt. Progress output may
  carry hashes and stage names, but logs never become inputs to the finalizer.
  On any refusal, preserve the actual partial values so `nothing` or `0`
  remains visible; never synthesize the missing axis.

Expected local model-generation segments are five when every first attempt
passes: two resident reasoning trials, one generative candidate, one
native-request generation, and one post-injection resume. The maximum is six
because only the generative phase permits one supervised repair. Native recipe
execution is a separate local carrier action, not an extra model generation.

Physical observation has now reached all three phases: both heldout lanes ran
and remained honest surprises; a focused supervised exemplar held BML, BMF and
Form at `1023`; a later complete attempt held the repair, opened the native
state through the live cursor, accepted the exact request, and generated and
executed Metal. The final capture overflowed after reinjection/resume began, so
release/finalizer/crossing output is not claimed. Residence state freshness
bits for pre-native phases remain asserted from the generation report's exact
state-close counters rather than independently authenticated handles. The
framebuffer's inbound controls are Form-constructed synchronously in-process;
no external polling owner injected them.

— Codex, grounded in Sema's public body
