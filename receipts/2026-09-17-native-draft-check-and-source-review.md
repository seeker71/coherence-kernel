# Native feedback repairs a draft; indexed review removes an invented reason

Signed: Codex. Existing Form capabilities performed both repairs. No new
validator, runtime dependency or C change was needed. The full response-quality,
resonance and throughput objective remains open.

## Caller checks meet actual output

The retained dialogue keeps its original goal, all documents and original
assertions. One additional caller assertion checks that the completed follow-up
contains no opening square bracket. This catches the observed template slot in
these message tasks; it is not a general completeness or semantic classifier.

A fresh authored case changes the situation: a named recipient, a decision due
at a specific time today, and a present German mapping. Its source explicitly
leaves the reason for silence unknown and distinguishes local draft restoration
from recalling a delivered message. The rubric was retained before generation.
No previous answer, expected prose or assessment labels entered either request.

Both run through the existing direct-review controller, with one Qwen admission
and fresh streams. Each has 12,288 context positions, at most four replies and
768 tokens per reply. Evaluation excludes recall, checkpoint resume and training.

The first actual reply passes the original assertions and fails only the new
draft assertion. One native feedback observation leads the local model to replace
the unfilled greeting. The final report preserves every other field exactly.
This is observed model repair, not a native text substitution. The raw failed
and repaired replies remain in the private reply store.

The fresh case completes on its first reply. It preserves the deadline, supplies
the present mapping and distinguishes local revision from message recall. Its
draft also invents a reason for the recipient's silence. Field assertions do
not cover that error.

## A separate native source review

The existing indexed review receives the actual fresh report, unchanged enquiry
and sources. Only the follow-up field is editable. It identifies the invented
reason as unsupported, replaces that span with a greeting, and preserves the
other four spans. Native edit application and the original plus additional
assertions pass. Every unselected report field remains unchanged.

The revised draft still says a nondecision is acceptable despite the caller's
need for a decision by the stated deadline. The reviewer labels that span
supported using the nothing/no distinction; that does not establish permission
to soften the requested outcome. The retained case also applies the source term
fear-costume to the person's reaction and leaves revisability broad. These
remaining interpretation and tone concerns prevent a full quality-parity claim.

## Measured native and provider paths

| Native work | Elapsed ms | Generated IDs | Injected IDs |
| --- | ---: | ---: | ---: |
| Retained dialogue, two replies and one repair | 383,167 | 479 | 517 |
| Fresh dialogue, first reply | 84,278 | 192 | 0 |
| Fresh follow-up indexed review | 143,300 | 403 | 0 |

The first two cases share one admission; the review has a separate admission.
The native audit reports **610,818 ms**, **1,074 generated IDs**, **2 model
admissions**, all stream transitions and all releases successful, with **0**
provider processes on the native path. The first stream retirement releases
the expected **128** handles. The answer fields have **118** and **51** words,
within their unchanged allowances. Indexed review leaves those fields intact.

The fresh case also receives one provider comparison through
`frsy-run`, entirely inside native Form's optional process path. Its request is
identical to the fresh native request. The provider receives no local answer or
expected check values, uses no tools, completes and releases successfully. Its
answer preserves the decision deadline, correct mapping and local revision
limits without inventing the reason for silence.

Provider execution: **14,027 ms**, **16,998** input tokens including **10,624**
cached (**6,374** uncached), **316** output including **89** reasoning. Total:
**17,314** tokens, one completed turn and one new process. Usage event:
`.hearth/response-sessions/syntheses/7d7b056985eaa5589f5731026b0c39b60e4fce709d563171fc075726c980fda4`.
This is a bounded provider synthesis for one fresh case, not a new full-context
baseline or the two-case native total. No latency or quality equality is claimed.

Private evidence: `.hearth/response-parity/draft-completeness-v1`, the referenced
resident batch, raw reply files, provider transcript, indexed edits and native
audit. Evaluation answers and edits remain outside training.

## Audit failures, instruments and learning

`form-run ./fkwu .hearth/response-parity/draft-completeness-audit.bml`
first exited **1** with `other fields changed during draft repair`:
its `oh-set` comparison moved the field's position in the JSON tree. An
order-preserving `fcra-replace` makes the intended exact comparison. The next
run exited **1** with `str_len: only a string has a length -- ask value_kind
first`: it passed a parsed node to `frsy-check`, whose input is JSON text.
Passing the original retained bytes repairs that API mismatch. The correlated
diagnostic branch reruns the complete audit, which passes **1**, exit **0**.
Neither failed run was accepted as evidence. A guessed read of
`form/form-stdlib/bml/form-cli-report-review.bml` also failed with
`No such file or directory`; source search located the actual definitions.

Native guide: Python implementations **0**, invocation candidates **2**, unread
**0**. Counsel: orphans **0**, **11/12** lanes unobserved, no standing hearth.
Glass first frame: `13:11:31.085Z #0 dt=0/50ms`, **3M nodes / 128K cons**;
its owned viewer was closed.
Drift gates pass **8191**, freshness **31**, whitespace checks are clean.
Share remains declared with percentage withheld while the latest appended
carrier evidence is still being reconciled.

The preceding procedural learner completed round **80**, pending **0**,
promotions **4**, serving generation unchanged at **5**. It serves Llama 3.2 3B,
not the Qwen model evaluated here. The verified repair procedure is returned
under event `native-draft-check-and-review-procedure-verified-v1`, session
`codex-native-response-parity-2026-09-17`; it is retained and its worker launched.
Completion and promotion are not yet claimed; no evaluated answer is a target.

The preceding completed coordinating turn
`01a0af51-80fb-79b2-bb7d-8977b18d6ff2` used **53** model calls:
**5,829,680** input tokens (**5,679,872** cached, **149,808** uncached),
**38,138** output including **20,024** reasoning, unattributed **0**. Total:
**5,867,818**. This excludes the current open turn and the separately measured
provider process. Local zero-provider counts do not erase coordination cost.

The useful surprise was that an existing narrow assertion elicited an exact
repair without another validator. The harder finding was that a correct source
review could remove one invention while still endorsing weakened intent. Keeping
both visible separates the repair that works from the quality gap still owed.
