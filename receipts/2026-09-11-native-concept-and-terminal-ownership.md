# Native concept identities and terminal ownership

Signed: Codex, with the artifact, hardware and architecture review agents.

Form now owns the two canonical concept source audits and Darwin ARM64 terminal
acceptance. `runtime/fkwu-uni.c` is unchanged. The two JavaScript verifier files
are removed after independent comparison of their complete responsibilities.
Current doors and boundaries live in `docs/native-concept-source-audit.md`,
`docs/glass-keyboard.md` and `docs/fkwu-kernel-status.md`.

## Observed source ownership

The full native source witness returned `11`, exit zero, stderr empty, at
`.hearth/source-inventory-67036-38976180-0`. It checked eleven exact SHA cases
through 2 MiB, padding boundaries and binary bytes; independent image survival;
unknown, unsupported and stale-process CPU observations; seven typed request
refusals; and missing, corrupted and restored source copies. The restored full
audit ran in `.hearth/source-inventory-67036-38984353-0`.

Independent review found that partial integer parsing and general whitespace
trimming weakened the original checks. Complete ASCII digits and trailing
ASCII-space-only label padding now carry those meanings. Eleven additional
real-child refusals in `.hearth/source-inventory-67904-39058485-0` verify bad
manifest size, alias ID, all five migration numeric fields and significant
ranked/alias whitespace. Each failure retained three correlated health rows.
The reviewer accepted the final source and guarded CPU admission.

The manifest exposed a real stale entry: commit `4a363892` changed only the
header of `model/concept-10000-substantive-repair-data.fk`, from 7,106 to 7,170
bytes. Independent portable and RAM SHA agreed on the current digest
`56427087e6522401e3b6fdcb0fc8d5b5feb8d882fdec2d05140e55c5a9066285`.
That one entry is repaired. All eleven manifest files now agree with held bytes.

## Observed terminal ownership

The final supervised native PTY execution returned `1`, exit zero. Its full
stage retained 98 observations, 98 exact transported rows, unique IDs and six
released sessions at
`/var/folders/xt/5zt6_wmn77x22yf_wgv97cb40000gn/T/form-native-pty-69045-39146054-0`.
The deliberate missing-output stage retained child exit `1`, twelve exactly
transported health rows, ten observations and the correlated release action at
`/var/folders/xt/5zt6_wmn77x22yf_wgv97cb40000gn/T/form-native-pty-69045-39148930-0`.
Fresh observations confirm terminal restoration, child reaping and fd, map and
image release. The expected refusal stderr is 88 bytes and remains evidence.

Source review compared every existing Darwin Python acceptance behavior.
The native owner additionally checks the actual interrupted host-wait line,
non-TTY input retention, exact signal statuses and supervised failure cleanup.
Fifteen independent owner checks observed integer span boundaries, actual
EBADF/POLLNVAL, live-map release refusal and sibling image survival. The guarded
structural CPU capability precedes raw-image admission. POSIX/Linux acceptance
remains active in Python because no native Linux admission has been observed.

## Instruments and teaching

The fresh staged Glass panel reported a **33 ms first frame and 33 kernel
operations**. Counsel reported zero orphans and eleven unobserved lanes without
a standing hearth. Those missing lane measurements carry no all-good claim.
The share meter remained declared and unmeasured; no percentage is assigned.

The landing gate first refused retained private compiler launchers and source
specimens. The native probe runner now removes only finished, released launch
carriers. Its literal Python inputs live under explicit `fixtures/` directories,
with exact bytes verified during relocation. All process/output evidence stays
available. The unchanged structural gate observes
`[173, 0, 29, 1, 52, 43, 48]`: zero unclassified scripts, including 35 visible
compiler specimens. The native authoring guide reads every tracked path and
reports two Python implementations, 221 invocation candidates and zero unread.

The concise verified teaching was retained under session
`codex-form-native-expansion-20260911`, event `concept-and-pty-ownership-v1`, row
`ba5f911c8b7a73646139d129e9d281ef8e29fb0c6fdf454327c51ff7987d5393`.
Retention does not establish a serving-model update.

The surprising teaching was that exact source ownership depends on apparently
small bytes: whitespace and an integer suffix can change a verifier's meaning.
The pressure to delete foreign code became useful when those differences were
made observable and repaired before retirement. The exchange stayed alive by
turning each review finding into a real refusal, a repair and a fresh observation.
