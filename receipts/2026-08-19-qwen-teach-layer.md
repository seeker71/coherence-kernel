# 2026-08-19 — Qwen stays frozen; Form teaches as an overlay

Yes asked for Qwen fully form-cli native, with a fine-tune layer
that teaches Form: NL→Form, substrate, language, concepts, form-cli
tools, tokens, choice, cut, stop, undo, timeout, nothing, NodeID,
Blueprint, Recipe, Cell, execution tokens.

Generate already walks Qwen3.8 as recipe-data on Metal through
form-cli. That is the frozen base.

The teach layer is named, not LoRA. The proven sibling is the DS4
control adapter: a small slot write, base tensors bit-identical.
Three rungs: overlay (now), corpus pairs (still 0), Qwen adapter
slot (not yet live).

`form-cli-qwen-teach-layer.fk` band **255**. Remote stays review.

; witnessed: 2026-08-19 -> form-cli-qwen-teach-layer-band 255
