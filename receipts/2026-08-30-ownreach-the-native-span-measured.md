# 2026-08-30 — ownreach: the native span, expanded and then measured

Asked: focus on form-cli native expansion and vitality; see how much it
can do with minimal remote tokens. The answer is a number the body now
computes about itself, and a live turn that needed no remote at all.

## Expanded

- **The corpus became an answering organ.** `kra-corpus-word` in
  `knowledge-rest-answer.fk`: a query surface splits to tokens (trailing
  ? stripped), locates through the corpus's own exact-match door, and
  the fresh word comes home — a lookup, never a classifier; unknown
  questions answer exact nothing. Wired as the second rest lane of
  `fcmg-heed-rest-honor`, behind the pointer registry and before any
  source search. Band 511 on fkwu AND all three walkers — the corpus
  lane is four-way, 12k-line chain and all.
- **Radius held at declared verdicts:** heed-report 2097151, live 255.

## Measured

- **ownreach** (`observe/native-reach-run.fk`, embodied and re-runnable):
  every corpus question round-tripped through the live selfheed door —
  **555/555 reach, 0 misses**, plus 3 registry queries, 9 catalog
  surfaces, unbounded paren arithmetic. Fold: 555000309. Zero model
  decode, zero remote tokens. The number can only grow by landing rows
  that ANSWER; it shrinks the day a door regresses — a vitality reading,
  not a completion metric.
- **The pulse's second verdict: 6** (trajectory 3 lying → 2 honest → 6).
  T2 answered natively through the catalog with zero decode. T3 is the
  historic one: the model emitted a CLOSED envelope
  (`<|form:knowledge-query|>bootstrap/ground.fk ...`), the lookup was
  honored through the SOURCE lane, and the model read the injected
  evidence, decoded 42, and stopped on purpose — a fact it does not hold
  in weights, answered correctly by asking its own body, with zero
  remote tokens. The envelope-close budget was the binding repair; the
  kra/corpus lanes now sit ahead of it as the cheaper paths.
- **The embody census** (re-run live): 1214 proof cells, 57 organs, 45
  teach-overlay pairs, session-ppm 894153.
- T1 (axioms) stays dark and named: its free-form query surface reaches
  neither the registry nor a valid source path — the axioms family
  remains the curriculum target it was in v3.

## What "minimal remote" measures now

Three nested spans, each with its own meter: what the BODY answers alone
(ownreach, 555000309 today); what the LOCAL MODEL answers grounded by the
body (pulse, 6 of 7 bits); what still needs the rented mind (this voice,
stamped per turn by the share meter — this session's walk order remains
native, then local, then remote). The remote share shrinks by growing the
first two numbers, and both are now organs any session can re-run.

## The most surprising teaching

The historic T3 turn was won by the least glamorous change in the stack:
not the JIT, not the pointers, not the registry — a budget that finally
let the model FINISH ITS OWN SENTENCE. The deepest lanes only matter
after the shallowest courtesy is paid.

## Where discomfort turned to gold

The reach meter's first design counted rows on the shelf; the discomfort
was knowing 555 would then be a vanity number the day a door broke.
Rebuilding it to round-trip every question through the live door cost an
O(n²) walk and bought the only property vitality needs: the number is
falsifiable by regression, so its growth means something.

; witnessed: 2026-08-30 -> kra band 511 four-way; reach 555000309
; (555/555, 0 miss); pulse verdict 6 with closed-envelope self-grounded
; 42; heed-report 2097151 + live 255; census 1214/57/45/894153;
; corpus row 1164 ownreach
