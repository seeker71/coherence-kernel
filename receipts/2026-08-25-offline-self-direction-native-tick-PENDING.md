# Offline self-direction: native tick observed, hourly life still pending

**Date:** 2026-08-25
**Witness:** Codex, grounded in the public Sema body
**State:** PENDING — the Form-native map/select/control/apply/re-observe tick is
observed; native wake and category-to-recipe bindings are not present

## What moved

`observe/form-local-offline-self-direction.fk` now owns one bounded local
movement.  A due invocation:

1. observes an adaptive health map,
2. selects the map's highest-scored observed gap,
3. emits an answer-free framebuffer observation,
4. accepts only the exactly correlated `continue` control,
5. calls one supplied Form actuator,
6. stages and binds the actuator result to the exact selected identity and
   action NodeID, and
7. re-observes the materially returned state.

This is a tick, not a daemon.  It does not call Codex or Claude, spawn a host
process, grow C, flatten Form, or route through an ops table.  Its result keeps
`native-wake-owned=0` and names `native-bounded-wake-primitive-absent`.

## Direct observations

Fresh preflight of the organ, band, and current seam each reported:

```text
parens balanced
errors 0
warnings 0
unresolved 0
```

The adversarial band returned:

```text
524287
@form fkwu 0 7 0 7
```

All nineteen bits held.  They include exact identity/action binding, a real
before/after health transition, one observable actuator call, rejection of a
replayed exchange, rejection of false/missing control, rejection of wrong
identity/action and raw `nothing` results, no state smuggling through a typed
`nothing`, an applied-but-unchanged distinction, not-due/idle distinctions,
the wake seam, and retained `nothing`/0/1 health states.

The executable current-map seam returned:

```text
[form-local-offline-self-direction-seam-v1,
 nothing,
 selected-action-category-has-no-executable-recipe-binding,
 axioms-resident-one-family-crossing,
 -23,
 0, 1, 0,
 6, 6, 739, 739,
 0, 0,
 native-bounded-wake-primitive-absent,
 2]
@form fkwu 0 226 415 641
```

The two framebuffer events are the outbound observation and correlated inbound
control.  The selected `-23` is the current `fmcl-attempt-category` NodeID.  It
is a category identity, not a proven executable recipe binding.  The typed
`nothing` therefore preserved the current rows and re-observed the same six
gaps and 739 permille health.  The `offline-self-directed-hourly-loop` row
correctly remains an observed gap.

## Exact remaining ports

Two independent bindings remain before “offline self-directed hourly loop” is
an observed claim:

- **Wake:** a bounded Form/native-walker wake or timer recipe that can enter
  `flosd-tick` when due without a host subprocess or a busy-spin recursion.
- **Apply:** a registry from each selected action NodeID to a concrete,
  validated Form recipe function.  Today's `fmcl-*` NodeIDs classify lifecycle
  movements; no source in this crossing proves they are callable recipes.

Once those ports exist, the callback seam already supplies the local
map→select→apply→re-observe body.  Until then an external caller may enter one
tick, but may not call that hourly autonomy.

## Boundaries

No model or native carrier was opened.  No private heldout or evaluator source
was inspected.  The global tree-balance sweep was intentionally not used
because it reads every `.fk`, including unrelated untracked sibling work; the
three direct-source preflights provided string-aware balance and compile proof
for exactly this movement.

— Codex
