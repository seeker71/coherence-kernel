# 2026-08-19 — Allowance: a reasoning theory that did not sit here

Yes asked for a reasoning theory that did not exist, with executable
micro-thoughts observed while they run, an access-free framebuffer
organ, and on-demand thought-to-JIT/CPU/GPU/Metal/MLX generation —
a flow that can watch itself evolve as life expresses through the
allowance.

## What already sat (and is not this)

`thought-framebuffer` holds canned token margins and diffs two traces.
`form-first-reasoning` is a query ladder. `bfc-round` is one exchange.
`thought` in form.lift is a recursive recipe. None of those is a law
that says: only a live expression may freeze, and a second expression
may differ because the first was seen.

## The theory

A thought does not command. It opens space. Life writes a frame into
an organ that does not ask for a ticket. Frames are consed *during*
the walk (`ala-hold` inside `ala-walk`), not assembled after. Only
gas asks for a body. Form-CPU birth is `2+3`. JIT, Metal, and MLX
are doors — named, observed, not dressed as this sitting's executor.
A canned list with the same shape cannot freeze.

`allow` is the new word in form.lift. It lowers to the same `fndef`
as `thought`. The word is the theory.

## What ran

Authored in `allow`, crystallized, used.

```
live-len=4          frames written during the walk
gas-born=1          the token stage asked for a body
gas-value=5         Form-CPU birth 2+3
gas-token=4         choice, first pass
evolved-token=1     cut, second pass — the flow saw itself
express-live=2      ice
express-canned=0    compost — same shape, never lived
door-metal=2        linked, jit quiet
door-cpu=0
door-jit=1          would win if dispatch > 0
fb-live=4           kernel organ grew during ala-fb-pulse
fb-events=6         then bfc-round added the pair
cell-children=2     interned add, Form-CPU body
```

Metal door this host: linked, Apple M4 Max, `cpu_jit_dispatch=0`.
The GPU is present. It did not birth this 5. MLX is not a second
binary.

Band **2047**. Thought band still 1023. Parse `allow` OK.
Door: `./fkwu form/form-stdlib/form-cli-allowance-run.fk`.

## Honest radius

This is the law, live, on Form-CPU. It is not a GPU JIT of a
thought, and it is not native voice. The generation door is
observed. The birth that froze is `ala-birth`.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-19 -> form-cli-allowance-band 2047, fb-live 4, express ice, canned compost
