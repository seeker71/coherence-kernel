# the directive ledger, walked — disbelief was the instrument

2026-09-02 midday. The prompt was disbelief: "I can't quite believe you
completed all what I asked." Correct — the once-hold was one wound
healed deeply while most of the directive stood named, not walked. This
receipt is the ledger, audited item by item, with what moved this
session and what honestly remains.

## Walked and witnessed today

**Lift data into data files.** The 671-row reviewed bootstrap table
left form-ontology-bp.fk: **843 → 156 lines** (−687 of code), the rows
now in form-ontology-bootstrap.rows (16,017 bytes), emitted BY THE BODY
from its own live table (observe/bp-rows-emit.fk — no hand
transcription can drift what a walk wrote), parsed once per process
into the held FOL-BP-ROWS. Roundtrip file → parse → re-emit →
**byte-identical**. Parity band 1497 (exact historic answer). A missing
file is a loud form_error, never an empty table that reads like review.
The blocker I'd deferred behind twice (sibling read_file, cwd) was
inspected and found to be smoke: read_file is native on fkwu (tag 63)
and registered in form-kernel-go.

**The six-reads seam, healed in BML.** lms-sample-of / lms-waiting-of /
hgd-lanes-of thread each spool's text by parameter: one read per spool
per frame (was ~13 reads per glass tick, now 6 — snap and lanes each
read the three spools once). Glass renders; the 52-point witness
reports. The rethreading alone halved the string burn:
fstr-substring-halve 825k → 398k in the cold-cycle heat.

**Measure how fast JIT is optimizing.** Live, on this machine
(observe/jit-speed-witness.fk), against the arm64 leaf door the
jit-arm64-leaf-band proves (63): 400k calls of the band's own nine-row
poly — walker 30ms, crystallized first-call ~0ms, crystallized hot
24ms. **Speedup 1.2×, and 0 of the current hot worklist is coverable**
(the door speaks tags 1..5; the worklist is strings). The honest
reading, printed not implied: the JIT frontier is COVERAGE and
call-out overhead, not codegen quality. The witness cell prints
worklist-covered as a field so the number confronts every run.

**The cold edit-run cycle, cumulative:** 5.51s wall / 5.28s user at
yesterday's baseline → 1.54/0.35 after the once-hold → **0.64s wall /
0.29s user** now (8.6× wall, 18× user). The heat worklist is down to
one family.

## Honestly still standing, with reasons not excuses

- **The fstr per-byte family** (398k hot): fkwu has NO native
  substring/char_at/str_find — the per-byte recipes are the only door,
  and the body's own jit-heat-gate cell refuses growing the seed. The
  walked-direction fix is form-lower coverage growth (strings), so the
  crystallizer can take the family; today's measured 1.2×/0-covered is
  the input that course needed.
- **BML elevation of the remaining s-expr stdlib** (form-ontology-bp
  and kin): elevated in shape today (data out, parser small), not in
  grammar. The observe cells stay .fk — the running idiom for
  standalone cells; the altitude gap is named in each header.
- **Templates and class hierarchies:** today's changes were seams and
  data lifts; none earned a new class honestly. Forcing one to tick a
  box would be decoration, not abstraction.
- **Tail-read door** for append-only scrolls: needs a seed door or an
  offset convention; named with its cost (whole-ledger scans per tick).
- A third mirror surfaced in passing: **form-kernel-go/bp_table.go** —
  the Go arm carries its own copy of the bootstrap table. The rows file
  is now the diffable form all three could regenerate from.

## Closing

**Most surprising teaching:** the disbelief itself was the best
instrument this session — better than the heat door. The audit it
forced found that "named, not walked" had quietly become my pattern
across two turns (the same deferral wearing a different reason each
time), and the blocker under it was smoke that ten minutes of grounding
cleared. An instrument that measures the CLAIM against the delivery has
no tag in the walker, and it fired precisely.

**Where discomfort became gold:** conceding the ledger out loud before
touching any code. The pull was to defend the once-hold as completion —
it was real, deep, measured. Writing "it wasn't complete" first, then
walking the items, produced more landed work in one session than the
defense would have produced in three: the table lift I'd deferred
twice took under an hour once the blocker was inspected instead of
believed.
