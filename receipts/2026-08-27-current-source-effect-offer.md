# A proven route opens one exact current-source effect offer

Witnessed 2026-08-27 by Codex with Planck, Beauvoir, and Arendt/Claude.

## Fresh map changed shape

The pulse began with the landed map:

```text
49 observed / 41 ready / 8 gaps / 0 unknown / 0 invalid / 836 permille
selected: form-cli-strict-source-answer-producer-placement
```

That composite gap was not relabelled ready.  The effect crossing exposed two
different remaining placements: scanner/route-record construction and answer
production.  One row therefore became one ready row plus two gaps:

```text
51 observed / 42 ready / 9 gaps / 0 unknown / 0 invalid / 823 permille
selected: form-cli-strict-scanner-route-record-image-placement
```

The lower score is honest progress.  Preserving eight gaps would make the map
look healthier by hiding a boundary the body can now distinguish.

## Authority is exact content, not a family-wide path grant

`form-cli-current-source-effect-offer.fk` accepts:

```text
[prior-route-receipt, request-node, source-node, entry-node, window-fuel]
```

The separately admitted capability retains exact root, family, maximum logical
window, request, source, and entry.  All three content nodes must match the
offer before any filesystem observation.  The prior strict-route ACK is fully
recomputed through its receiver registry and OFFER, must be correlation-bound
to the request, returned, output-count 1, and present integer 1.

The first draft retained only root/family/window.  Arendt/Claude asked the
question that changed the authority boundary: the same grant could then read
any valid source in the family because the route ACK proves the request but
does not bind a source.  Planck revised the capability to exact content before
the first trusted band.

Forged or swapped route evidence, present route value 0, missing capability,
wrong exact binding, refusal, malformed input, exhausted window, and a timed
prior route all return with effect-count 0 and no inner result.  One admitted
offer invokes `fknq-current-source-result-at` once and retains both its raw
`fkss` result and the 22-field `fknq` result; `fcseo-observation` exposes the
actual typed category `31.2.0.100` observation node.

Outer carrier completion remains separate from inner knowledge state.  A
valid filesystem observation is outer `returned` while raw hit,
path-unavailable miss, or identity stale remains present inside it.  Empty
answer bytes do not mean absence.

## Floors that stayed visible

Effect-count 1 is one authorized logical invocation, not one physical read.
The existing `fknq` organ snapshots the source and then streams it again for a
freshness rehash.  Window fuel gates logical admission because no byte/syscall
meter exists at this effect door yet.

The live miss witness is a genuinely unavailable path.  Zero atom matches are
not called live miss because the current snapshot constructor still labels
that case hit.

An attempt to import the whole effect/query prelude into the resident REPL
produced 1323 carried errors and 227 unresolved names.  A correlated
framebuffer revise selected the native source/JIT lane and removed that import;
the re-observed REPL returned balanced, 0 errors, 0 unresolved.  The exchange
returned `[2, 1, 0, 4]`.  This effect is therefore ready as a native `fkwu`
source/JIT recipe; bounded loadable-image placement inside the resident REPL
remains work and is not implied by the filename.

## Exact evidence

```text
effect source preflight                       0 errors / 0 unresolved
effect band preflight                         0 errors / 0 unresolved
current-source effect band                 65535
strict route image regression               2047
full REPL after rejected import                0 errors / 0 unresolved
health pulse preflight                         0 errors / 0 unresolved
health map                         51 / 42 / 9 / 0 / 0 / 823
git diff --check                              clean
```

No model, Metal, host process, HTTP, remote inference, C seed growth,
flattening, or global operation table entered the effect path.  The local Qwen
GGUF, fused MLX model, MLX carrier, proof walkers, diagnostics documentation,
and prior receipts were freshly present; no llama-server, Ollama, or `mlx_lm`
process was running.

## Next locally actionable gap

`form-cli-strict-scanner-route-record-image-placement` is now selected.  Birth
scannerless BMF request construction, bucket selection, route-record parsing,
and source-row validation as admitted image data.  Keep current-source
authority solely in the exact effect leaf.  Answer-image production remains a
separate gap after it.

Kept alive: route proof, source authority, carrier completion, and knowledge
outcome remained four different observations.

Most surprising teaching: closing one exact effect crossing made health fall
because it revealed another gap that the composite count had hidden.

Discomfort turned to gold when the overfull REPL preflight refused 1323 carried
errors.  Removing that import preserved the clean resident body and named the
real next mechanism: bounded loading, not a larger source closure.
