# 2026-08-30 — a knowledge query answers with an offer

The pyramid was width. The next move was a lookup, not a guess: a
knowledge query that answers with an offer, not decoded tokens.

`kra-answer` already is that lookup — unknown is exact nothing, a known
surface returns a rest pointer. Copying its three rows into the node-id
door would have been a second registry, and a second registry is a
contradiction. This door wraps the pointer kra already holds. Naming is
an offer. An ask replies outside the local LLM token stream. Offer
without ask replies nothing. Catalog growth on kra's side grows this
door: one fold over the rows, not a new table.

```
form-cli-nodeid-know-band.fk                              255
printf "node-id-know\nquit\n" | ./form/form-cli
  node-id know unknown=nothing ground-offer-reply=nothing ground-ask-reply=42 twice-ask-reply=84 catalog=3 cost=outside
```

`catalog=3` is the honest floor of the lookup today. The fold does not
care. When kra grows, this speak grows.

intern_node stays named, not claimed. Composite intern on the source
door still does not dedup (`native-content-address-band`: two identical
MS-PAIR composites read `node_eq` 0). Leaf intern does. Until composite
sharing is witnessed on this lane, two equal recipe offers remain two
lists, not one DAG.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-30 -> know-band 255; wrap is kra, not a second registry;
; catalog=3 live; intern_node composite-dedup still 0 on source door
