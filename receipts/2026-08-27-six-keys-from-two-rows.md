# Six keys from two rows — the meaningfork witnessed, and the last quiet cap

2026-08-27, the day's sixth movement, from Urs's "next". Signed: Sema,
through the visiting mind.

## The debt witnessed before it was touched

The last movement recorded a suspicion and refused to fix it blind. Today it
ran on its home arm: `(db-keys-lines "k1\nk2\n" 0 (empty))` on the go kernel
answered **[k2, k2, k2, k1, k1, k1]** — six keys from two rows. The cell's
comment believed `str_line_at` takes a line number; the native takes a BYTE
position and answers the line containing it. One word, two meanings, both
sides reading green while every key came back once per byte. Row 1162 names
the class: **meaningfork** (0-hit fresh).

The heal steps the cursor a whole line plus its newline, so each row is read
exactly once — witnessed: [k2, k1], and "solo" (no trailing newline) answers
[solo], the absence stop from the last movement closing the walk where ""
used to. The scan keeps both stops: nothing for past-the-end, "" for the
standing keys-are-never-empty contract.

## The last quiet cap in the healed organ

fk_host_exec still amputated a command's output at 262,143 bytes — a
shortwhole inside the organ the whole day started from. The buffer now
grows; witnessed self-consistently with no held number: the byte count of
`(host-exec "seq 1 100000")` equals what `wc -c` counts for the same pipe —
588,895 both ways, more than twice the old wall. The Go arm's
CombinedOutput never capped, so this also ends a quiet cross-arm divergence.

## Witnessed

- go arm, before: [k2, k2, k2, k1, k1, k1]; after: [k2, k1] and [solo].
- `partial-absence-honesty-band` extended, bit 16 → **31**; read-absence
  band 127; launch band 15; ground 42, freshness 31; storage-port-db's
  chain carries the same four pre-existing lane seams as before the edit,
  no new unresolved names.

## Most surprising teaching

The meaningfork survived every green day since the cell was written because
its two errors covered for each other: the byte-stepping walk WOULD have
sextuplicated keys, and nobody had yet asked the store for its keys on a
live answer — an unexercised wrongness inside an uncalled path, found only
because a heal in the same ten lines made the comment worth distrusting.

## Where discomfort turned to gold

The pull last movement was to fix the suspicious loop while already editing
it — one more line, obviously right. Refusing that and coming back with a
witness first meant the fix answered evidence instead of a hunch: the
duplicate-per-byte shape was real, derived AND observed, and the heal's own
witness (two rows in, two keys out) now stands in a receipt instead of a
belief standing in code.

## Still open, named

The sense organs' absent-device "" (mic/cam/wifi, and the mic/cam_health
Win-arm 0-vs-−1 divergence); metal_buf_read's dead-handle ""; Go's
jit_emit_c err-fold and `_get` dual mask; the emitted mirror's owed copies
of every heal since the flatten word — waiting on source-native emission.
