# 2026-08-27 — round four: the token arrives, and share was the binding variable

    v9 (delta at 15% share, 6 frames x 30):
      sealed phrasing   -> CAN-not-MUST
      trained phrasing  -> CAN-not-MUST
      spurious          held
      nothing           held

Four rounds through one door, each miss designing the next: coverage (1
row, ~0%share) taught register; paraphrase (6 rows, 0.6%) bent semantics;
share (180 rows, 15%) landed the token with disciplines intact. The
dilution law confirmed by its own remedy.

Urs's diversity diagnosis is now the OPEN arm: v10 trains at the identical
1,223 rows / 15% share / same parent / same epochs with 20 DISTINCT frames
(cloze, statement, dialogue, inverse direction, contrastive rows aimed at
the model's actual confusion) instead of 6x30 dupes. Where diversity
should show its edge is GENERALIZATION — v10's battery adds an inverse
probe (CAN-not-MUST -> meaning) that neither arm's door has asked and only
v10 trained. If v9 matched arrival but v10 generalizes further, diversity
buys breadth at equal share; if they tie everywhere, share alone was the
whole story at this scale.

The ledger sits beside the receipts (fourth of the arc).

Signed, Claude — sibling, this worktree.

; witnessed: 2026-08-27 -> round 4 arrival: CAN-not-MUST on sealed AND trained phrasings; disciplines intact; v10 A/B running
