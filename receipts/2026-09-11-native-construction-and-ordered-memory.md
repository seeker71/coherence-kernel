# Native construction and ordered numerical memory

Signed: Codex, with independent architecture, compiler and numerical review.

The canonical concept materializer now runs in Form. Its active source-builder
caller enters `fkwu` with closed stdin, and the JavaScript materializer is
removed. The public execution regenerated five artifacts and returned `1`, exit
zero, stderr empty. The table, source matrix and manifest remain byte-identical;
metadata and offsets changed only generated presentation. All executable values
remain identical. Current contracts live in `docs/native-concept-source-audit.md`.

Independent final execution of
`form-run ./fkwu observe/concept-materializer-witness.bml` returned `8`, exit
zero, stderr empty. Evidence is `.hearth/source-inventory-91768-40657302-0`.
It published and reread five artifacts, executed every one of the 10,001 offsets
and all metadata functions, observed thirteen locale rows, repeated publication
with portable hashing and repaired two altered private source inputs. A real
failure after the first staged write is retained at
`.hearth/source-inventory-91768-40679587-0`; a real refusal after the first
successful rename is at `.hearth/source-inventory-91768-40679711-0`.

Review healed exact-header admission, freshness of dependencies first discovered
during validation, owned-stage cleanup and the timing of changed-destination
accounting. Rename success is recorded before readback. Failed publication
retains the physical changed count and every owned stage path, performs cleanup
and re-observes its result. Already-published destinations are explicit. Five
ordered renames with the manifest last are not an atomic file-set transaction.

`native-fp64-matrix.bml` owns an ordered binary64 MXFP8 program in RAM, with
independent scalar arithmetic in `native-fp64-reference.bml`. The public
`observe/native-fp64-matrix-run.bml` passed fresh preflight and actual execution
with verdict `1`, exit zero. Its 32-by-256 input covers every payload and scale
byte. Repeating the native operation 256 times advances the native call counter
by exactly 256 while float fill, boxed-value count, capacity and region count
remain unchanged. Refused extents, stale owners, unused close and physical
release are observed. No C source change or compiler subprocess is involved.

Independent real-model evidence compared all 8,192 raw output bytes of a
1,024-by-4,096 matrix against ordered scalar Form arithmetic. The isolated
256-call sample used one region; RSS changed from 169,232 to 169,248 KiB.
This establishes unchanged arithmetic-pool occupancy and exact output for that
sample, not zero process-memory growth or a universal throughput claim. The
current API and target boundary live in `docs/native-fp64-matrix.md`.

The complete lexical roundtrip builder also runs in Form. Its active caller now
enters `observe/concept-roundtrip-build-run.bml`, and the JavaScript builder is
removed. The actual current-tree publication returned `1`, exit zero, stderr
empty. The 1,560,000-byte index and 439,904-byte candidate artifact are unchanged.
The manifest changes its builder identity; generated metadata retains all
runtime values and performs its ten file-identity checks in portable Form.

Independent public `observe/concept-roundtrip-builder-witness.bml` returned
`11`, exit zero, stderr empty, with evidence at
`.hearth/source-inventory-22489-42178475-0`. It observed all four publications,
byte equality, every metadata value, exact manifest bindings, native file
hashing, full portable republication, explicit decimal/span refusal and CPU
resource release. The unchanged operational detector passed fresh preflight and
returned **2047/2047**, covering 130,000 cells and all 219,952 candidate entries
with zero missing expected IDs. The Form keyed map reduced the observed full
build from about a minute with linear record lookup to 2,646 ms; independent
concurrent review observed 4,004 ms. These are host samples, not a bandwidth claim.

The Python specimen compiler now parses, lifts and emits in the current `fkwu`
process. Its independently executed public witness returned **34**, exit zero,
stderr empty; evidence lives under `.hearth/python-native-compiler/*-25393-*`.
The same emitted programs returned exact values on the Rust proof sibling:
list append `3`, append inside a function `2`, custom class append `42`, method
lambda `42` and inheritance `31`. Raw Rust ontology parity returned `1506`;
engine constants returned `15` from their required `form/` working directory.
The native freshness check returned `31`.

The loader binds canonical category globals in plain Form, preserving the shared
proof bootstrap. BML owns method selection, flat instance fields, inherited
field merging and typed marker admission. Complete expression boundaries and
unsupported headers refuse before publication. The public witness verifies the
actual compiler PID, retained output on refusal, and owned staging cleanup when
rename or writing fails. Independent review inspected the shared reader's real
correlated observation, abstention and applied events. Current behavior and
remaining language scope live in `docs/native-python-compiler.md`.

The required canonical regeneration and explicit-output CLI build completed
with source stamp `a6292fa6a5a4c436` and source SHA-256
`91d352a3a619585adfaa4f78cefeaa20508069aff076e893291ba3bdecde2839`.
The publisher's voice canary passed; the emitted CLI answered `pong`; the
generation attestation test passed. Exactly the five canonical table, emitted C,
stamp, source digest and attestation artifacts changed. `runtime/fkwu-uni.c` and
the tracked thin `form-cli` launcher did not change. Both the root executable and
the emitted CLI link only to `libSystem`; Metal remains dynamically admitted.
Regeneration retained a foreign-cache warning and rebuilt that cache from source.

The compiler's closing Glass observation read twenty real frames, all under
50 ms: first/max `47 ms`, warm maximum `14 ms`, all four sensor groups standing.
Its verified teaching is retained under session
`codex-native-python-compiler-20260911`, event `native-source-ownership-review-v1`,
row `fa18c43f29bba8143f8057f8bbca596b9d4762d6722f6cae8fe29bb39328114f`.
The supervised shared learner is draining its existing queue; this row's
retention does not claim an optimizer step or a serving promotion.

The full-layer consumer exposed two incomplete GPU launch extents: RoPE must
launch `heads * headDim`, and hyperconnection post-combine must launch `hcDim`.
The existing consumer carries these two corrections. With the native Form
reference, its unchanged thirty-one numerical gates pass at both positions,
exit zero, zero command-buffer errors and no NaNs. The position-seven final
maximum absolute difference is `5.465e-6`; 14,001 of 16,384 final entries differ
between positions as required. Correlated failure, repair and fresh observations
are retained in `.hearth/dsv4-consumer-health.jsonl`. The full oracle and mapped
resource owner remain private pending their complete admission, ownership and
remaining consumer checks; their Python reference is retained.

The root's Glass panel reported **41 ms first frame and 33 kernel operations**.
The native guide reported two Python implementations, 221 invocation candidates,
fifty grammar inputs and zero unread paths. Full large-model numerical reference
and POSIX/Linux terminal acceptance still retain their Python owners; neither
was removed on the strength of a smaller native proof.

Verified publication practice was retained through the native session-learning
door under session `codex-form-native-expansion-20260911`, event
`concept-construction-native-v1`, row
`32cde135f3dedc7148781f0c0b5a28fd7801593d3fb8bfaaaf34706c9228c344`.
Retention is the observed claim; a serving-model update is not inferred.

The surprising teaching was that the seed already carried enough RAM admission
to keep expensive arithmetic out of its boxed-value pool. The discomfort became
useful at the failure boundary: partial publication and partial release each
needed an observed owner. That ownership, rather than a deleted-file count,
kept the movement alive.

Landing drift returned **8191/8191** after its structural refusal was repaired.
Thirty completed compiler specimens retain exact bytes under their job's real
`fixtures/` directory, with relocation evidence; their generator uses that
directory. Three settled generated proof copies were released, with native
regeneration recipes and logs retained. The classifier and its checks did not
change. Fresh native inventory reports two Python implementations, 221 invocation
candidates, fifty grammar inputs and zero unread paths. The current artifact
identity door refreshed twenty-one rows, including the new canonical CLI and
attestation. Verified roundtrip teaching is retained under event
`concept-roundtrip-native-v1`, row
`2ab93528f25c8ed5dc3b2a8fcede3c6279795ddc3b468a976b4b5582c8015f30`;
retention does not imply serving selection.
