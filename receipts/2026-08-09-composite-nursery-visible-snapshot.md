# Composite nursery: visible immutable-snapshot reuse

Date: 2026-08-09

## Observation

`living-world-composite-nursery-band.fk` previously exposed no framebuffer
cell before a long native walk.  A five-minute wait therefore added elapsed
time without adding causal evidence.  Direct stack observation named Form
evaluation, but it could not name the semantic call that owned the time.

The band now opens `observe/live-call-framebuffer.fk` before constructing its
core and retains an enter/modify/leave cell for every material phase.  Its
first runtime cell arrived at 0 ms.  No `--src`, Python path, remote oracle, or
model answer participates.

## What the frames found

The long composite proposal repeatedly revalidated the same immutable
`world`, `blueprint`, `threshold`, and already-admitted examples:

- outer blueprint currentness: about 18.4 s;
- current example collection through the old path: about 18.8 s;
- two-profile/four-trial derivation through the old path: still active after
  30 s when the diagnostic was intentionally stopped;
- each trial called blueprint and example currentness again even though the
  enclosing field construction had already admitted those exact identities.

This was repeated verification, not source compilation, learning, remote
inference, or evidence arrival.

## Narrow repair

`living-world-composite-recipe.fk` now has a snapshot-scoped internal path.
The public/current entry points retain full validation.  Inside one exact
immutable field construction only:

1. blueprint validity/currentness is witnessed once;
2. examples retain local validity, exact-blueprint identity, activity,
   recurrent-source threshold, value currentness, and role completeness;
3. profiles and trials consume only those admitted examples;
4. trials retain exact surface-to-blueprint identity;
5. no observation is reused across another world identity or later round.

The resulting cells still pass through the same content-addressed constructors.
No consent, revocation, threshold, optionality, timeout, alternative, unknown,
decision, or authority rule changed.

## Re-witness

On the repaired path:

| Material phase | Before | Current | Meaning |
| --- | ---: | ---: | --- |
| first framebuffer cell | unavailable | 0 ms | causal state is visible immediately |
| outer blueprint currentness | ~18.4 s | ~18.3 s | mandatory gate retained |
| current example collection | ~18.8 s | ~0.38 s | redundant blueprint rescans removed |
| two-profile/four-trial derivation | >30 s, interrupted | ~0.002 s | admitted snapshot reused locally |
| staged composite proposal | incomplete in the observed old run | ~18.7-19.1 s | proposal now reaches a result |
| production/staged current identity | unavailable | `identity-clean:1` | both current implementations intern the exact same proposal |
| full fifteen-claim band | historical `32767` | `32767` | field, re-offer, optionality, time, and revocation claims remain placed |

The final exact-identity run returned `32767` in 171.935 s.  That total includes
both the staged proposal and a second production proposal solely for exact
identity comparison.  A preceding single-proposal visible run returned
`32767` in 151.011 s.

The pre-change proposal content identity was not retained, so an exact
pre-change-versus-current identity comparison is unavailable.  Historical and
current aggregate claims align; current staged-versus-production proposal
identity is exact.  This is an observability and repeated-work movement, not a
model-quality, evidence-arrival, promotion, or authority movement.  The frozen
world-model gain and native-voice learning question remain open.

## Exact paths

- `/Users/ursmuff/source/coherence-kernel/form/form-stdlib/living-world-composite-recipe.fk`
- `/Users/ursmuff/source/coherence-kernel/form/form-stdlib/tests/living-world-composite-nursery-band.fk`
- `/Users/ursmuff/source/coherence-kernel/receipts/2026-08-09-composite-nursery-visible-snapshot.md`

; witnessed: 2026-08-09 -> 32767 on fkwu; first frame 0 ms; identity-clean:1
