# Complete native source admission

Codex, with independent compiler, artifact and grammar review.

The north star is Form-owned meaning: complete programs, compilation, resource
policy and version lifetimes, with CPU and device programs admitted in RAM.
The compiler repair was first observed with the handwritten C seed and root
executable unchanged. Integration also carries the current upstream seed.
Its obsolete parameter guard is removed: the native census observes 4,106
fewer C bytes, 55 fewer lines and one fewer function definition. Freshness
returned 15 after integration, required the standard rebuild, and then returned
31. The 32 admission cases and seven supervisor controls were re-observed on
that rebuilt runtime; their preceding private reports and runtime bytes remain
retained under their original identities.

Form now walks statement, declaration and body boundaries by their actual
lexical extent. Inline and multiline bodies retain later effects, lexical
bindings, final values and failures. Function and class suffixes stay visible.
Unconsumed expressions and unknown class members refuse admission. BML has one
executable source owner; the cursor grammar remains a separate proof surface
whose checks require complete parsing and exact executable trees.

The [direct cache observation](../docs/evidence/fkwu/bml-cache-renewal.json)
holds an unchanged source and its old image, invokes the ordinary runner, and
observes the complete result after cache renewal. Its runtime identities match
the retained pre-repair DSV4 identities. The original BML baseline did not
record runtime hashes; this receipt does not supply retrospective ones.

The [held-source observation](../docs/evidence/fkwu/dsv4-held-source-rewitness.json)
walks 81 source rows in order twice and refuses a deliberately changed last
hash. The existing numerical executions keep their original identities.
The source inspection read 6,357 Git-listed Form/BML files completely; its
2,120 flagged physical lines were lexical candidates, not 2,120 execution
defects. Quoted semicolons accounted for 1,202 of those lines.

Independent review found two boundaries that the initial admission examples
did not establish. Custom BMF rule data must keep `do` and `end` as rule names;
its section reader now observes quote, comment and brace boundaries without
inventing executable BML scope. The BML reader distinguishes an adjacent
`do(...)` call from a whitespace-separated `do (...) ... end` block. Both
receive actual child executions in the public admission witness.
Its final 32 cases pass with 24 source/runtime identities unchanged after every
case and the final reread. Successful child streams have no stderr; the outer
runner's cache-renewal warning is retained separately.

The [integration evidence](../docs/evidence/fkwu/bml-admission-integration.json)
retains ten fresh preflights and exact successful outputs. Source health now
reads current files and observes ontology, round-trip and text-emission
behavior; source markers are explicitly source-presence observations. It
does not issue a universal healthy verdict. The import/ref witness uses a
real imported class. The native text boundary check follows the current
loader's native binding contract.

The required local learning return exposed an extra closing parenthesis in
the session-source reader. Preflight reported depth -1 and refused the chain;
the repaired source passes a fresh preflight. The teaching contains the
verified ownership practice, with no evaluation inputs or answers.
Its ordinary retry completed optimizer step 26, settled with no pending rows
and no retained buffers, and kept the serving generation unchanged.
The [supervisor-exit repair](../docs/evidence/fkwu/session-supervisor-exit.json)
also retains a known failure when the supervisor exits before writing its
terminal record. Its shared home exit policy refuses that actual failed child.
Unknown waits remain indeterminate; a saved failure cannot become a successor
worker's status. Pending rows and the retained diagnostic keep their bytes.

The final cursor witness returns 105 with exit 0 on Go, Rust, TypeScript and
fkwu under the validator's unchanged-tree seal. The native leg retains its
foreign-build cache warning and successful source rebuild on the integrated
runtime. The canonical Form-generated CLI answers `pong`; root and CLI both
link only libSystem on this host. Generation and current artifact identities
are retained with the committed bootstrap artifacts. The earlier interrupted
generation was deliberately stopped before publication when review required
a compiler correction; its nonzero status remains a stopped attempt.

My Glass reading observed a 118 ms first frame and 163 ms admission. I ended
only my sampled process group; the pre-existing Glass remained alive. Counsel
reported zero orphans and eleven unobserved lanes out of twelve with no
standing hearth. Share remained declared and unmeasured, with percentage
withheld; the token meter had no bound transcript path.

The surprising teaching was that an evidence traversal could itself discard
its next step. The discomfort became useful when that claim met all 81 rows
and an actual refusal at the last one. I kept the movement alive by repairing
the owner, returning through its ordinary door, and preserving the earlier
observations under the identities that produced them.
