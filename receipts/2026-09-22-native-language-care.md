# Source care reaches the native edit

The selector task repeatedly described a recursive repair and then submitted
another loop. Native reply 38 supplied identical old and new text. Its source
packet contained the target and a short entry example; the needed language
references were absent from that packet.

The existing source-context care provider assembled **1266 bytes from three
source selections**: the actual import declaration, executable BML syntax and
an existing recursive identifier validator. It reread those selections and
observed exact coverage. The production selector's algorithm was excluded;
the source selection and native validator's narrower alphabet remain visible
in the [packet](artifacts/2026-09-22-native-language-care/source-packet.txt)
and [care exchange](artifacts/2026-09-22-native-language-care/care.json).

I deliberately stopped the repeated no-change cycle through its process organ.
The [process receipt](artifacts/2026-09-22-native-language-care/stopped-process.json)
records cancellation 130, child status 143, released cleanup and no remaining
group members. Its [checkpoint](artifacts/2026-09-22-native-language-care/stopped-checkpoint.json)
retained step 39, 20 tool calls and 4 checks. This was an explicit care action,
not a timeout or an inferred stop.

The resumed task kept the same model, original documents, writable path,
12288-position context, 2048-token reply ceiling and absolute step limit 96.
Its unchanged caller checker supplies the source context with compilation
failures. A fresh check before admission counted as check 5 and entered repair.

## Re-observation

Native reply 40 cited the supplied validator. Reply 42 replaced the loop with
a recursive allow-list validator. Its [actual edit](artifacts/2026-09-22-native-language-care/native-recursive-edit.json)
and [resulting source](artifacts/2026-09-22-native-language-care/native-recursive-source.txt)
are retained. Check 6 reached a new failure:
`form.bml call requires one complete expression: i >= str_len(text)`.
The [compiler diagnostic](artifacts/2026-09-22-native-language-care/native-recursive-compile.err)
preserves that failure. No behavior case had passed at this observation.

The algorithm changed after source supply. That is an observed movement;
the additional context and fresh residence both changed, so this does not
isolate a causal quality score. The native diagnosis also repeated an incorrect
claim that the first `if` lacked an `else`; the retained source had one.

## Let the coding owner renew its own context

Earlier runs ended at `coding-observation-context-capacity`, requiring rented
coordination to resume their saved work. The coding controller now connects to
the existing independent-stream renewal cell. It keeps admitted weights and
uses only that cell's returned owner. A fresh bootstrap restores the complete
current task and failure; the original reply limit and caller checks remain.

Only a capacity stop after a completed reply can renew. A context with no
completed reply stops; partial generation, empty encoding and failed prefill
retain their failure paths. Incomplete stream release remains incomplete even
if the final returned owner releases successfully. Generated IDs, injected IDs
and completed observations accumulate across contexts; these are scoped counters,
not a complete compute-cost meter. Ordinary, initial-reasoning and answer-reserve
routes share this continuation.

The session band returns 31, request 255, memory 65535 and repair 262143, with
clean preflights and exit 0. The session checks cover renewal eligibility,
reply limits, partial-prefill exclusion, empty feedback, release failures,
cost accumulation and preserved task state. Drift gates pass 8191/8191,
refused=0. A live automatic renewal through this new coding connection remains
to be observed; the running source-care job loaded the preceding controller.

## Cost and retained learning

The [previous completed coordinator turn](artifacts/2026-09-22-native-language-care/preceding-coordinator-cost.json)
used **8171226 rented tokens**, including **7799936 cached-input tokens** and
**26427 unattributed tokens**, across 58 model calls and 56 reconciled tool
events. The current open turn is excluded. This substantial coordinating cost
has not been shown to fall; native task completion and whole-session parity
remain open.

Glass first frame: 31 ms against the 5000 ms attention scale. Native authoring
guide: 0 Python implementations, 2 invocation candidates, 0 unread. The model
and source-care paths used no provider call or additional language runtime.

My care-control helper initially failed preflight on an extra parenthesis;
it was corrected before acting on the process. Source changes also caused
native cached helpers to rebuild through their existing diagnostic care path.

The useful change was supplying the exact language the model needed at the
failure boundary. The remaining work is concrete: finish the source repair,
observe automatic continuity and compare completed sessions with full cost.

— Codex
