# The native answer completes; useful composition remains the gap

The retained native review completed its original source and report checks at
**447 words**. Model release is confirmed. This residence and its renewal used
**5,013 generated IDs**, **5,152 injected IDs**, **2,979,127 ms** and zero provider
calls. The checkpoint ended at ten cumulative replies and nine cumulative
check runs. Earlier interrupted and completed residences retain their separate
costs; these are not whole-goal totals.

The original process handle completed with exit 0, and its PID was absent.
The independent re-observer confirmed source preservation and the original
report assertions again. Its answer and report are retained in the artifacts.

## What the returned words establish

The final answer restores “referenced” in the persistence statement and removes
the unsupported automatic frequency-response behavior. It keeps all six axes,
the actual translation surfaces, missing-code distinction, external-file
lifetime boundary and separation of graph identity from model decoding.

It also removes several practical interaction examples introduced by prior
amendments. Most sections again inventory definitions. Its unchanged findings
claim those concrete response decisions were added, although several no longer
appear in the final answer. Passing shape and word-count checks did not make
those findings current or establish a useful, resonant response.

The Form-owned assisted answer remains a different, attributed 445-word answer;
the latest context-equipped answer is 404 words. Neither was supplied to the
native review as a target. The earlier generic re-observer also lists historical
444-word assisted and 407-word context-equipped answers; its labels and hashes
describe those older artifacts, not the latest pair.

## Compose from the sources and the actual conversation

The next native request retains every original document, source assertion and
report assertion. It uses the existing catalog and native source-query path:
the full enquiry and supplied source text are delivered by native `cat`;
the earlier draft stays available through tools but its prose is absent from
the initial model context. The preparation verified both conditions.

The caller goal now includes Urs's actual corrections about a distant,
cautious answer and the two independent paths. It asks for practical response
decisions in this exchange and for findings written from the final answer.
This is an explicit context change, not a matched one-variable comparison or
a claim about hidden model mechanisms.

The model, 16,384-position context and 512/1,024 stage allowances remain.
The fresh response has two completed replies available; evaluation exclusion
prevents its outputs from becoming training targets. Bootstrap size is
**24,481 bytes**. No generated replacement answer or provider resource is
supplied. The original owner completed before this request was admitted.
The request ended at `attention`: its second final generation used all
**1,024 final-stage IDs** without a completed response. It emitted no report,
ran no report checks, and released the model. Total cost was **935,119 ms**,
**1,777 generated IDs**, **2,179 injected IDs**, and zero provider calls.
Process exit 0 records an orderly return; it does not make this a completed
answer. There is no new answer quality to compare. Private generation files
remain private.
The independent re-observation confirms unchanged documents and passing source
assertions; the absent report fails the answer-type assertion.

Its first public action explicitly reads `answer.md`. The earlier draft is
therefore present after this tool call. Initial-context exclusion did not keep
the draft out of the subsequent conversation, and any later improvement cannot
be attributed to draft absence. The actual action is retained alongside the
preparation evidence.

## Resolve the observed final-output ceiling

The next request increases the final allowance from 1,024 to **2,048 IDs** and
raises its per-stage cap accordingly. Native preparation read the actual second
stage metadata, confirmed exhaustion and release, and verified that every other
request field is identical. The question, sources, checks, Qwen model,
512-token reasoning allowance and two-reply limit remain the same. Evaluation
exclusion still applies. The health flow correlates this observation with the
capacity correction.

That request completed at `.hearth/native-final-capacity-2026-09-23` in
**989,309 ms**, using **1,687 generated IDs**, **2,179 injected IDs**, one
tool read and one check run. The final stage stopped after **1,095 IDs**;
release and the original source/report assertions passed. Provider calls were
zero. Increasing the ceiling allowed this report to complete; its answer
quality has separate evidence below.

The actual Qwen tokenizer encodes the retained 447-word answer in **609 IDs**
and its complete report submission in **809 IDs**. This report retains its
stale findings; it is a sizing observation, not a correct-answer target. Both
the reference and indexed native encoders returned these counts and roundtripped
the exact text. A complete report therefore can fit the former 1,024-ID ceiling.
Exhaustion establishes what this generation used, not that the required report
shape inherently needed more space. The larger allowance remains a recovery
measure whose result must be examined.

The sizing helper initially selected the slow reference encoder. It completed
before an attempted stop: `ps` found no process and `kill` returned “No such
process”; its original handle then returned exit 0. The helper now uses the
existing indexed native cursor. The answering-model process was unchanged.

## The review sees the gap and leaves the response undone

The returned 447-word answer copies the supplied draft exactly after trimming
outer whitespace. Its report carries `verdict: reject` and eight findings. The
native model names the missing connection to Urs's correction, the missing
correction-to-action example, and the two independent paths. Its proposed next
move is useful: lead with the gap and let the six axes serve that connection.
The report also retains the draft's unsupported claim that traceability changes
resonance, while one finding says no such unsupported claim is made.

The review controller accepted the report's shape and length; its `complete`
status did not approve the candidate. Its contract permits negative findings
inside a completed report. We routed composition through that review workflow.
The most useful native contribution here was its criticism; the requested
response still needed to be composed.

The current repair uses the ordinary source-backed CLI's `generate` path.
`generate --prompt-file PATH` now reads a complete context file after the
generation options, preserves its bytes, and diagnoses unreadable, empty or
size-mismatched reads before model admission. Ordinary inline prompts retain
their behavior. The existing reasoning/generation band passed **1**, exit 0,
after a clean preflight; its file case compares actual source bytes and all
allowances. No C seed or external runtime changed.

The prepared **15,518-byte** prompt retains the original enquiry/source text
verbatim, includes the conversation correction and the native review's own
actionable reason, and asks for a prose answer. The earlier draft is absent.
Preparation verifies that the file-backed CLI request equals the exact inline
request. This changes the response path and context arrangement; it is not a
matched one-variable comparison.

The real CLI listed Qwen Q8 at index **0**, then selected that observed index
and ran the file-backed generation with 2,048 answer tokens, 512 reasoning
tokens and the same 350–450 word range. Its prompt lives at
`.hearth/native-compose-2026-09-23/prompt.txt`. The original review artifacts
and checks remain retained. The generator owns response completion and one
native length correction. Its final answer, quality and cost are still pending.

## Spend and instruments

The preceding completed coordinator turn used **7,365,112 rented tokens**:
7,325,827 input, including **7,148,288 cached input**, plus 39,285 output.
Thirty-nine model calls and 37 tool calls reconcile; unattributed usage is zero.
The separate Form-owned provider repair used 21,078 tokens and remains its own
event. These different work scopes establish no savings ratio or parity.
Coordination is still too expensive.

The next completed coordinator turn, `01a0ce41-5bee-70e1-bddc-5c5c58c2ab04`,
used **4,585,991 rented tokens**, including **4,416,640 cached input** and
**25,683 unattributed tokens**. It records 44 model calls and 42 tool calls.
The retained cost reader reconciles that completed turn and includes the
unattributed quantity; it excludes the current open turn and separate provider
subprocesses. This is further coordination cost, not a matched-route saving.

Native guide: zero Python implementations, two invocation candidates, zero
unread files. The latest counsel panel reports **orphans 0** and eleven of
twelve lanes unobserved without a standing hearth.

Drift gates passed **8191/8191**, exit 0. The verified teaching was retained as
`2ff1d22fe661109d3d5d5178ffcec22a74141ed49e46f270595bbb5eb37e3c05`.
Retention establishes no serving-model update.
The worker subsequently completed with this teaching as the latest candidate;
the serving generation remained earlier. This worker uses the separate Llama
learning path, so it establishes no update to the answering Qwen model.
The observed capacity failure and correction were also retained as teaching
`44b74465311ffbdfe272e5dd60b43fef0e55ac32feca7e16518ffaa82da7def1`;
its worker was launched, with no serving update claimed.
The verified distinction between review completion and response composition,
and the file-backed generation checks, were retained as
`d26be9aca80213678ec301b7f42e5818138b277d600666212ec428e7837b5faa`.

— Codex
