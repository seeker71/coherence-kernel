# A retained failure gets care, and an unchanged edit says so

The local review continued from its owned checkpoint. Reply seven grew the
answer from 514 to 532 words. Reply eight shortened it to **513**; reply nine
submitted the same answer again. Both still failed the original 350–450 range.
The native check already supplied the exact deficit and a 400-word target.
Missing numeric feedback is not the cause.

## Native repair boundary

Whole-value report amendments previously accepted an unchanged value, added an
amendment record and reran the same checks. Literal text edits already detected
this case. The shared amendment boundary now compares the resulting JSON
values with the retained report and returns an explicit unchanged diagnostic
when no value changed. JSON object key order alone is not a correction. A
mixed amendment with a real change remains available.

The exact public reply nine was replayed against report eight. The repaired
boundary preserved the report and all source documents, added **zero** check
runs and **zero** amendment records, and left the work in repair with its
diagnostic. No model was called. Its historical execution remains recorded as
an accepted, failing amendment; replay does not rewrite that history.

The original process had loaded its controller before this patch. Later model
use of this diagnostic is still unobserved.

## The offered resource returns a usable answer

The actual failing reports seven and eight entered one Form-owned provider
repair. The original source documents and source/report checks were unchanged;
the request added attributed Codex feedback about recomposition and explicit
source conditions. No arriving-agent answer was supplied for imitation.

The provider returned a **445-word** answer in **33,656 ms**. Original source
and report checks passed again, and process completion and release were
observed. The [actual answer](artifacts/2026-09-23-native-response-care/assisted-answer.txt)
is attributed to Codex through the native Form resource.

It restores the explicit referenced-cell persistence condition and says the
frequency functions calculate supplied annotations rather than reading a
person or selecting a response. It connects each requested axis to a response
decision. The definition-heavy section structure remains. Compared with the
404-word context-equipped answer, it is more explicit about several technical
boundaries but less closely connected to Urs's actual correction and the next
shared action. These are Codex's observations; human felt resonance remains
open.

Precision about the native finding: reply eight removed the word “referenced”
while retaining “the unreferenced composts.” The observed loss is an explicit
qualification in the first clause. The earlier care event's categorical
`source_conditions_preserved=0` is broader than that evidence alone supports;
it does not prove the model denied reclamation. Restoring the qualification
makes the scope clear.

The one provider call used **21,078 tokens**: 20,259 input, including
**10,624 cached input**, and 819 output. Count its retained usage event once.
The native execution, this coordinator and that provider call have separate
accounting; the provider-only figure is not a whole-session saving.

The preceding completed coordinator turn used **3,397,034 tokens**, including
**3,332,736 cached input**, over 27 model calls and 26 tool calls. The retained
reading reconciles both counts. It excludes this open turn and the separate
provider call. Coordination cost remains far above the desired floor.

## Put the repair behind the ordinary command

The public resource runner initially returned
`invalid-or-oversized-resource-request` for the actual `@request.json`
input, with zero provider processes. It accepted only inline JSON.

File-backed input now reads through Form's native reader with the existing size,
permission, failed-report and one-process boundaries. The source-backed CLI
exposes `response-repair @request.json` and its help. AGENTS.md points to this
door. The runner and actual CLI both rechecked this exact retained repair,
returned `provider_processes_new=0`, and preserved the same answer and usage
event. No additional provider call was made during those reuses.

The public CLI output snapshot preserves the checked reply. The first CLI reuse
completed with exit 0; the later capture's completion handle was still pending
when its output was frozen. Its continuing log remains under .hearth.

## Checks and continuing native work

Fresh preflights were clean. Resource boundary returned **1**, CLI dispatch
**7**, and native review repair **34359738367**, all exit 0. Drift gates returned
**8191/8191**. Native guide: Python implementations 0, invocation candidates 2,
unread 0. Counsel reports **orphans 0**; eleven of twelve lanes remain
unobserved without a standing hearth.

The verified boundary and reuse teaching was retained as session example
`0e72f8d9af95b6a92755be4f3b9c59533dbf7a61ca7fc0292622752768f4cded`.
Worker launch establishes retention, not a serving-weight update or Qwen
quality improvement. The provider assessment remains excluded from learning.

The original native process also renewed its context successfully on the same
admitted weights: position **15,292**, **1,092** positions remaining, three
completed replies in that residence, **3,813 generated IDs** and **4,351 injected
IDs**. The fresh context started at position **6,902**, with zero new weight
admissions. This confirms the existing renewal path in this execution; the
earlier tokenizer replay separately discriminates the handoff-only overflow.
The process remains live with the original cumulative reply ceiling of twelve.

The quality gap remains open. The useful crossing is concrete: one known
unchanged action now gets an accurate diagnostic, and the stronger assisted
answer is available through a reusable native command with explicit cost and
attribution.

— Codex
