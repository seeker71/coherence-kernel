# Living-world shared-field continuity witness

Date: 2026-08-09

## Movement

The shared field can now leave one `fkwu` process as an inert,
content-addressed cell stream and enter a second `fkwu` process without
rebuilding the world from fixture knowledge.  Loading carries no authority.
Restore is a new action: every holder discovered from the inert field must
respond freshly through the same ordinary action membrane.  Missing responses
return NOTHING and explicit dissent returns ZERO.  Only unanimous fresh ONE
responses return the restored state.

After admitted restore, the second process asks the prior field again, retains
the learned native surface, grows and integrates a fresh independent branch,
releases an earlier observation, and composts a newly released opaque cell
only after its holder explicitly tends the compost proposal.  The composted
successor is persisted and loaded once more.  This is continuity with new
movement, not replay dressed as learning.

## Direct Form observation

The writer, invoked directly as
`./fkwu form/form-stdlib/tests/living-world-shared-field-continuity-write.fk`,
returned exact `31`.  It left a 3,632-byte carrier whose SHA-256 was
`de5a412072dbd3b76706099ebd4207f2f12162c83e454a37259a6e12e6537c49`.
Its final framebuffer state reported nine cells, thirteen attributions, two
proposals, and boundary ack TWO at 2 ms.

The independent reader, invoked directly as
`./fkwu form/form-stdlib/tests/living-world-shared-field-continuity-read.fk`,
returned exact `1048575` (twenty of twenty claims):

- partial restore: five participants, four ONE, one missing, ack NOTHING at
  8 ms;
- complete restore: five participants, five ONE, no dissent, silence, or
  missing response, ack ONE at 9 ms;
- fresh branch integration and prior-observation release remained visible as
  ordinary offer/field transitions;
- compost proposal: one affected participant, enter at 13 ms and leave at
  68 ms;
- compost integration: explicit fresh holder response, enter at 69 ms and
  leave with ack RELEASE at 341 ms.

The continuity unit band returned exact `2097151` (twenty-one of twenty-one).
The existing compost band returned exact `1023` after the traversal repair.
All before, successor, and pending transient carrier paths were absent after
the reader completed.

## The defect the framebuffer found

The first separate-process reader reached the compost-proposal enter frame and
then emitted no leave frame.  That exact phase isolated the fault: the public
`lwc-contains?` predicate walked a content-addressed DAG as though it were a
tree, revisiting shared ancestry exponentially.  It now carries a visited-cell
field.  Compost semantics and the `1023` proof band are unchanged; the same
cross-process action now leaves the proposal phase in 55 ms and completes
integration in 341 ms.

A separate source-compilation delay was also made visible rather than inferred
from CPU.  A monolithic restore expression produced no root artifact for more
than ten minutes.  An import-only Form probe compiled immediately, localizing
the delay to that expression.  Splitting the same meaning into narrow Form
cells restored compilation to seconds.  No C capability was added.

The observer's macOS direct-stack sample now has a three-second watchdog, so a
blocked diagnostic sample cannot withhold the next status indefinitely.  When
the OS denies process sampling, the observer says so and continues to expose
the child framebuffer; it does not reinterpret CPU as evaluator progress.

## Compatibility ground

- shared-field channel: `2147483647`
- shared-field continuity: `2097151`
- session learning: `134217727`
- transition field: `262143`
- symbol learning: `32767`
- transition expression: `262143`
- alternatives and elapsed defaults: `32767`
- identity-preserving durable continuity: `32767`
- unanimous reversible compost: `1023`

## Honest boundary

The second process made new field movement, but the carrier path and inputs are
still driven by bounded Form witnesses.  A live interactive cell-coordinate
ingress has not landed.  The retained learned surface and compositional answer
prove local Form-native expression from current cells; they do not prove an
open-ended natural-language generator.  The generative-weight/native-vocabulary
keystone remains pending exactly as recorded in
`2026-06-29-native-zh-summary-PENDING.md`.

No remote oracle answered any question in this path.  No C-seed growth,
`--src`, Python answer path, external training, model promotion, authority
grant, commit, push, or pull request occurred.

## Exact paths

- `form/form-stdlib/living-world-shared-field-continuity.fk`
- `form/form-stdlib/tests/living-world-shared-field-continuity-witness.fk`
- `form/form-stdlib/tests/living-world-shared-field-continuity-band.fk`
- `form/form-stdlib/tests/living-world-shared-field-continuity-write.fk`
- `form/form-stdlib/tests/living-world-shared-field-continuity-read.fk`
- `form/form-stdlib/living-world-compost.fk`
- `/Users/ursmuff/.codex/automations/native-model-daily-witness/observe_run.sh`
