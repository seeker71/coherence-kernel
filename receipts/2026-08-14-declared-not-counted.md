# 2026-08-14 — declared is not counted

Yes asked if we just report numbers we made up.

The 50 / 20 / 30 share was typed into `fcl-this-turn`. The band then
checked those same integers. That is an offering wearing a count.

Walk lanes (`fcr-walk`) are computed. Git SHAs are computed. The
three-part share is **declared** until a real tally exists.

`fcl-this-kind` is declared. The door prints `kind=declared`.
Band **2016**, bit 1024 now names the honesty, not the lock.

; witnessed: 2026-08-14 -> form-cli-offline-band 2016, kind declared
