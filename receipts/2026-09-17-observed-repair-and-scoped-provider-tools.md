# Specific observations let the native repair move

Signed: Codex. Witnessed 2026-09-17 on `fkwu`, C seed unchanged.

## What changed in the body

The caller-bound expression evaluator now explains the first refused syntax
node. A native coding attempt had supplied five arguments to binary `and`;
the old observation named only the expression lane. The new observation names
the binding, expected arity and actual arity. Admission still uses the same
validator, and diagnostics never execute proposed callbacks.

Given that observation, local Qwen repaired the retained source, passed all
39 caller-owned behavior cases, and completed review with model release.
The first failed attempt remains part of the cost. This was a second admission
on an already seen task; it is evidence of repair, not a new generalization test.

| Native code attempt | Elapsed ms | Generated IDs | Injected IDs | Behavior |
| --- | ---: | ---: | ---: | --- |
| Initial | 207973 | 528 | 935 | Failed |
| Repair | 156917 | 308 | 688 | 39 cases passed |
| Combined | 364890 | 836 | 1623 | Completed review |

Neither admission called a provider. Initial prompt tokens are additional to
the generated/injected columns. The proposed predicate remains assessment
evidence; it has not been installed as a production source-query feature.

The provider process carrier now observes resource release separately from
successful exit. An interrupted process can have released its resources while
its task remains unsuccessful. The optional internal
`frr-execute-tool-session` adds the caller-owned evidence directory to the
provider's isolated writable workspace. Ordinary resource calls retain their
existing scope. A no-model sandbox probe observed: inside write 19 bytes,
outside write -1, outside file absent, native parser admitted 1, pass 1.

## Focused source observations

On the repeated corrected dialogue assessment, five coordinator-selected native
source queries supplied exact excerpts while preserving access to the full,
unchanged sources. Local Qwen performed one further document read.

| Native dialogue admission | Initial tokens | Injected IDs | Generated IDs | Elapsed ms |
| --- | ---: | ---: | ---: | ---: |
| Document catalog, then full reads | 1072 | 10409 | 678 | 1214038 |
| Selected source observations | 3026 | 1789 | 399 | 347252 |

The focused run saved 866786 ms (71% at integer precision) and 8620 injected
IDs. Both runs preserved source documents, passed the original assertions,
released the model and made zero provider calls. This demonstrates a useful
retrieval direction, not autonomous query selection or whole-session parity.
The focused answer still used defensive disclaimers and deferred its useful
action to another reading. No frequency or resonance score was inferred.

## Mixed code and dialogue observation

Both provider arms received the same frozen two-task packet, SHA-256
`70fe2c067a0bbf1e4dd3cac4af8a51795b0d09c77d71e97ef129587bb6287d46`.
The code task required editing resident source and preserving its contract;
the dialogue task retained all original sources and assertions. Expected
answers and earlier model answers were excluded. Every provider process was
started inside native Form.

| Provider arm | Elapsed ms | Input | Cached subset | Output | Total | Outcome |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Context-equipped native tool session | 50057 | 81026 | 63744 | 1538 | 82564 | Malformed JSON tool requests; neither task completed |
| Bounded synthesis, native apply/check/review | 26253 | 25259 | 10624 | 1021 | 26280 | Code and dialogue assertions passed; review completed |

The tool-session model over-escaped its JSON in both shell attempts. It called
that an infrastructure failure, but the retained requests show invalid JSON;
the native tool correctly refused admission. This failed baseline does not
establish comparative quality. The synthesis process made no tool calls;
Form applied a guarded source edit and performed the checks and review. Both
processes released their resources. Cached input is included in input, and
reasoning output is included in output; neither is added again.

An earlier provider attempt used an insufficient writable scope, attempted
unnecessary runtime copying and was terminated with status 143. Its 160209 ms
and retained failure remain separate. Actual cleanup was released; the old
result's release=0 conflated cleanup with success. Completed provider usage was
absent and remains unknown, not zero. The owned temporary copy was removed.

Local dialogue plus both local coding admissions took 712142 ms, with no
provider call inside those executions. These were separate local admissions,
not a single resident mixed session. Parent coordination and development costs
are additional: this goal had already consumed 7,179,209 provider tokens at
the movement's resumed checkpoint. That cost is not a minimal-token result.

Reading the actual bounded answer also leaves initiative unresolved: its next
action waits for Urs to name a decision. Structural passes do not settle
overall usefulness, voice quality or resonance. The goal remains open.

## Witnesses and embodiment

- Review execution: `33554431`; response resource: `1`.
- Downstream synthesis: `1` (21 boundaries); response session: `1`;
  review search: `511`.
- Preflight reported no errors, warnings or unresolved calls; all listed
  checks exited zero.
- Native authoring guide: 0 Python implementations, 2 existing voice invocation
  candidates, 0 unread files.
- Counsel panel: **orphans 0**; 11 of 12 lanes unobserved because no hearth
  stood. No all-good performance verdict follows from that panel.

Private evidence remains under `.hearth/response-parity/`: `query-authoring-v1`,
`generalization-v2/focused-v2`, and `mixed-session-v1`/`mixed-session-v2`.
Assessment answers remain excluded from learning. The verified teaching carries
the admission diagnostic and release/success distinction, not those answers.

The surprising teaching was practical: one precise failure observation let the
native model finish a repair it had previously circled. The difficult finding
was the provider's expensive transport failure. Keeping its requests and costs
visible made the next movement concrete: improve the boundary and observe the
actual outcome. The exchange stayed alive through that repair, with the voice
and initiative gaps still available for examination.
