# Fresh native responses with an independent final allowance

Signed: Codex. The previous movement reserved a final response after bounded
deliberation. This movement returns to the original two transfer enquiries,
without a supplied draft, a previous critique or a provider answer.

## Original questions, fresh streams

The private `direct-transfer-bounded-v1` run uses the exact original manifest
from `heldout-transfer-v1`. Native comparison verifies node equality. The
goals, documents, source assertions, report assertions and frozen rubric are
unchanged. These are repeated assessment tasks, not new unseen cases.

One Qwen3.8 27B Q8 admission serves both cases at 12288 context positions.
The second case retires the first stream's 128 expected handles and opens a
fresh stream with the same weights. Each prompt carries only the original
goal and documents, with a short instruction to answer directly in the
requested JSON form. It carries no coding-controller bootstrap, prior answer,
expected checker values, retrieval of assessment history or training target.

Both cases use `knowledge-query-reasoning` and `frbg-generate` with initial
allowance 512 and separate final allowance 1536. Each reaches the controller
final stage through the truthful runtime observation introduced in the
previous movement. No new domain evidence arrives through that observation.

| Observation | Review repair | Live dialogue |
| --- | ---: | ---: |
| Prompt tokens | 1944 | 3884 |
| Initial generated tokens | 512 | 512 |
| Final generated tokens | 244 | 286 |
| Total generated tokens | 756 | 798 |
| Observation-prefill IDs | 98 | 98 |
| Completion / JSON decode | 1 / 1 | 1 / 1 |
| Original source / report assertions | 1 / 1 | 1 / 1 |
| Elapsed ms | 243828 | 330845 |

The batch takes **574737 ms**, generates **1554** tokens and uses **0 provider
processes**. Final owner release and all stream transitions each return **1**.
Per-case boundary records correctly retain shared release as pending; the
aggregate receipt supplies the later completed lifecycle observation.

The same native checker used by the retained provider arms passes. Applying
the proposed edit through the actual native carrier passes its contract,
preserves the complete keep-only report and changes only the missing reason.
The native audit returns **1**, exit **0**. Dialogue length is **126** words
against the original maximum of **180**.

## Actual answer quality

Codex read both returned reports. The review locates the malformed edit,
repairs its missing reason and explains why a structurally valid edit does
not establish truth or prose quality. Its actual application supports the
claimed repair.

The dialogue gives a complete usable follow-up, distinguishes an absent reply
from an explicit rejection, preserves the null German mapping and keeps the
short codebook anchor separate from full kernel identity and human intention.
It also carries unnecessary technical exposition into the personal response.
More significantly, it extends cell-history preservation into an assurance
that reaching out loses nothing. A storage property cannot establish the
consequences of a human interaction. The original rubric's grounding criterion
therefore remains unmet despite the structural passes.

The actual native and retained provider dialogue answers are kept together in
`direct-transfer-bounded-v1/actual-dialogue-answers.md`. The retained provider
answers avoid this consequence claim and connect revisability to updating
one's reading or plan. That is Codex's assessment of these texts. Human felt
resonance remains unmeasured; no semantic parity score is manufactured.

## Cost comparison and limits

| Retained execution arm | Elapsed ms | Provider input | Cached subset | Provider output |
| --- | ---: | ---: | ---: | ---: |
| Fresh native pair | 574737 | 0 | 0 | 0 |
| Completed provider baseline | 37535 | 64898 | 52864 | 1421 |
| Form-grounded provider synthesis | 19068 | 21009 | 10624 | 588 |

The first baseline attempt also took **300242 ms**, exited **124**, and has
unknown usage because it never emitted a completed-turn usage record. It
remains in the comparison. Input includes its cached subset; output includes
reasoning. These are execution measurements from retained runs, not a
concurrent hardware trial or monetary prices. Coordination, development and
the failed native experiments in earlier receipts remain additional costs.
Zero provider generation in this native pair does not make the parent session
free. This run is slower than both successful provider executions and does
not establish parity in throughput or overall quality.

The direct prompt policy is still a private assessment. The public resident
assessment runner continues to use its existing review controller. The public
final-stage API is available independently; two completed tasks do not justify
silently replacing the ordinary response path.

## Attempted scope repair, still open

A second native run, `direct-scope-bounded-v1`, repeats only the original
dialogue. It adds one general instruction: keep each claim within the domain
its evidence supports, and preserve uncertainty when applying a technical
principle elsewhere. It supplies no replacement answer, identified bad span
or assessment target. Native comparison verifies the original request is
unchanged and the retained prompt contains exactly the proposed instruction.

The model, context and 512/1536 stage allowances stay the same. Unlike the
earlier dialogue's renewed stream, this run starts a fresh model admission;
its time therefore includes that different admission cost. The case takes
**377105 ms**, the full run **377161 ms**. Prompt tokens are **3919**; initial
and final generation are **512 / 322**, total **834**. Observation-prefill is
**98**. Completion, decode, release, original source/report checks and the
**173 / 180** word limit pass. Provider processes remain **0**. Native paired
comparison returns **1**, exit **0**; generation grows by **36** tokens and
case elapsed time by **46260 ms**, with the admission difference retained.

The actual answer does not resolve the semantic error. It changes the
unsupported assurance into a claim that a nudge can be revisited without
breach. The draft also adds an unprovided absence of a deadline and presumes
things have piled up. Those claims are not established by the supplied
material. Technical exposition grows rather than becoming a more useful
human explanation. This attempt does not earn an improvement claim.

The candidate instruction briefly entered shared review guidance for a
compile/workflow check, then was removed after the answer was read. The
default review guidance is byte-for-byte unchanged in the final diff. The
private paired answers, prompts and failed instruction remain available in
`direct-scope-bounded-v1`. Neither answer nor assessment input enters learning.

The next discriminating question is whether a native review can identify an
unsupported consequence in a new situation and preserve the useful action.
The generic instruction alone has now failed that practical purpose here;
another unchanged retry would provide little new evidence.

## Instruments and retained teaching

Freshness is **31**. The owned glass's first tick showed **3M nodes / 184K
cons**; it closed with exit **130**. Counsel reported **0** orphans and
**11/12** lanes unobserved. There is no standing hearth resident. The native
guide reports **0** Python implementations, **2** invocation candidates and
**0** unread files. Share remains **declared**, with percentage withheld while
the selected previous turn is not fully traversed.

The existing workflow band passes **65535**, exit **0**, including after the
candidate is removed. Its stale local cache was observed, rebuilt through the
compiler's care flow and then re-observed cleanly. Drift gates pass **8191**,
refused **0**, exit **0**. No kernel source or external runtime dependency
changed. Public changes retain the findings and the assessment practice.

Closing snapshots: **2721187** cumulative transcript output tokens and
**13461091** cumulative goal tokens. These have different scopes; neither is
the local inference cost. After the model released, the procedural teaching
`direct-transfer-evidence-separation-verified-v1` was retained under
`codex-native-response-parity-2026-09-17` and a learner launched. It covers
verified comparison practice, not assessment answers. Its serving effect is
unobserved. The previous learner completed round **73**, pending **0**,
promotions **4**, with serving unchanged; this is separate from Qwen quality.

The exchange stayed open by following the actual answer past its passing
checks. The most surprising teaching was that an instruction about evidence
domains still produced another unsupported consequence. The uncomfortable
result became useful when it prevented an ineffective prompt change from
becoming a default. The successful edit repair, completed generation and
remaining dialogue gap each keep their own evidence.
