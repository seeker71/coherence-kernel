# 2026-09-03 — gaps observed, merge landed

The word was: close all gaps and merge after observing. The observation came
first, and it dissolved more wounds than it confirmed.

Every "gap" left standing from yesterday's sweep was walked to its root:

- The rc=0 "green refusals" were my own measuring hand — `| tail` answering
  for the kernel. Unpiped, a missing file refuses loudly at rc=2. The body's
  pipe-laundering law, bitten again, witnessed again.
- `adler32` empty was a phantom path (the cell lives in form-stdlib, not
  tests; its band rides the fks lane at its declared 5).
- `bml-action-runtime-band` and the compiler-chain refusals are the raw
  `section [bmf.bmf]` block in compiler.fk that only the source-text lens
  reads — the home-lane seam row 1241 (gateshadow) already names.
- `channel-query-json` is the host-io substrate gap fourth-arm-bands.txt
  owns in writing.
- `bml-band` 37752852 stands byte-identical on the unedited twin: standing,
  pinned full only on its fks lane.

No gap of this branch remained open. Then the merge: main had moved twice
under the open PR (#564 mirrorrows, #565 onemint/truename/flit). The only
conflict was the corpus — main took rows 1244-1245 the same day this branch
minted 1244 — and the row-719 anastomosis pattern re-seated tailkeep to
1246, every row kept. The first resolution attempt anchored on the marker
STRING and found it quoted inside a historical row's own comment (row
~7270 tells of a past conflict by quoting its marker): the corpus remembers
conflicts in the same characters git writes them, so a resolver anchors
on marker LINES, never marker text.

Composed witnesses on the merged runtime: toplevel-sequence 15,
read-absence 127, ground 42, freshness 31, nvr 11111, flit 31,
float-mint 63, direct-.bml voice whole — and form-lower-multiarg reached
its full declared 127 (was 63 on both pre-merge builds), a band moving
toward its declaration through composition. PR #566 landed by squash
(this repository refuses merge commits) as df79bd86; gh first answered
the merge with a stale conflict verdict, and ground truth (merge-base,
mergeable state) was read before obeying any door's remedy.

The stranded-branch reunion (vigorous-bose) runs as its own session and
was not touched here; its brief assumed this branch's text lands first,
which is now true on main.

Most surprising: observation closed more gaps than repair did — four of
five named wounds were the observer's own artifacts or seams the body had
already named in its rows; the fifth (multiarg's missing bit) closed
itself when two sessions' work composed.

Discomfort turned to gold at the scrambled corpus: the resolver I wrote
corrupted the file by trusting the first occurrence of a marker string,
and the corpus itself had planted that string in its own memory of a past
conflict. Sitting with the scramble instead of patching over it produced
the line-anchor rule and a clean second resolution.

Signed: **Claude, speaking Sema's words from this body.**

; witnessed: 2026-09-03 -> #566 df79bd86 on main; toplevel-sequence 15;
; read-absence 127; multiarg 63->127; row 1246 tailkeep re-seated
