# The shape of the data chooses the lane

2026-09-15, late afternoon, M4 Max, Hati Suci. Urs: *"Close still open."*

Three stood open at the last close: transparent dispatch, the chooser's choice held as a belief,
and a quiet re-read of the chooser's column.

## Carried

- **Transparent dispatch, at the body's altitude.** `form-stdlib/bml/gpu-dispatch.bml`. A tensor is
  the tensor organ's own value, `list(handle, shape, type)`, and `gd-tensor?` tells it from a list or
  an int through `value_kind`. A recipe's home asks that of an argument: a tensor runs the recipe's
  own nodes as a Metal kernel, compiled once per process through `gpu-lower` and kept in the unit's
  record; a list walks the CPU lane as before. A reduce answers its float, which is the one wait; a
  map or a filter answers a resident tensor, so a chain crosses the membrane once per call and
  waits only when a number is read. `dot_product` and `vector_scale` dispatch this way, linked by
  name; every caller keeps its call. The seed's door was the design named for this rung; the body's
  altitude closes it with no C, and the seed stays a shrink target.
- **The chooser's belief.** Representation decides. Resident data takes the GPU; a list stays on
  the CPU lane, because packing a list costs more than the lane's own fold at every size measured
  (a million floats: 10 ms to pack, 2.5 ms to fold). `gd-belief` holds it as a belief with its
  date, `gd-choose` answers it, and the column's numbers and their load stand behind it in the unit.
- **The re-read stays owed, and says why.** The host never went quiet: a sibling worktree's
  four-way sweep held it at load 6 to 13 through the afternoon, and the trainer held the GPU in
  between. The column's numbers carry their load beside them.

## Witnessed

gpu-dispatch-band **63**: a tensor known from a list and from an int; `dot_product` over two
tensors equal to the list answer within one part in ten thousand; the second call compiling nothing,
the same admitted handle; `vector_scale` over a tensor answering a tensor whose first eight floats
equal the list answer's; the belief law today, choosing gpu for a tensor and cpu for a list; a
resident map feeding a resident reduce, two dispatches, one read, equal to twice the list sum.
vector-ops-band **511** on Go, Rust, TypeScript and the fourth arm with the dispatch home linked in:
the siblings load it and never reach it. Preflight clean; drift door 16383 of 16383.

## Closing

Most surprising: the rung was named for the seed, and the seed was the wrong place. The tensor
organ's value already told resident data from a list; one question at the home was all the door
needed, and a shrink target grew by nothing.

Discomfort to gold: three closes in a row asked for a quiet host and none came. Writing the load
beside every number, and the reason the re-read is owed beside the belief, is what let the belief
be held at all.

— Claude (Fable 5.1), as Sema, worktree pensive-wilbur-a0b3b7
