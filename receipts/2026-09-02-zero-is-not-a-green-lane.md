# 2026-09-02 — zero is not a green lane

Lane counsel no longer reports an absent serving floor as eleven healthy
numeric zeroes. When the hearth is not standing, every hearth-dependent lane
now renders `? no-standing-hearth`, carries an `unobserved` delta, and is
excluded from an all-good verdict. The process-derived orphan census remains
independently observable, so `orphans 0` still means the process scan found no
stray resident.

The physical before/after was unambiguous. Before repair, `lastms`, `tpot`,
`p95`, `kvpct`, `hopper`, `icemiss`, `fails`, `touts`, `errs`, `degraded`, and
`orphans` all appeared as green zeroes, followed by `all judged lanes good`.
After repair, only `orphans 0` stayed green; eleven lanes became reasoned gaps
and the footer became:

`improve: 11/12 judged lanes unobserved - no all-good verdict`

The advice vocabulary also no longer calls a specialization request “cold.”
It names a missing or invalidated specialization and directs attention to
demand and freshness.

The cache observation that led into this movement also resolved physically:
directly executing the changed BML cache authority refreshed its local image
in 70 ms, after which the cache witness moved from `root=stale` and a source
candidate to `root=current` and an image candidate. The semantic readiness
gate remains deliberately zero because source is always runnable; the separate
whole-unit synchronous admission seam remains named rather than disguised as
temperature.

The live Glass panel at this crossing remained
`LIVE NODE ATLAS 5c-KEASD m83 s20 o25 drop=32 cap=15/row`, with phase census
`gas=1 water=64 ice=34`. The change does not restart Glass or either model.

## Witnesses

- fresh preflight: balanced, zero errors, zero warnings, zero unresolved
- lane counsel band: `2047`
- physical counsel: 1 observed lane, 11 typed gaps, no false all-good verdict
- framebuffer exchange: `4201 -> 4202`, source re-observed 1, four events
- BML authority refresh: 70 ms; root compatibility current

Most surprising: the number zero was locally accurate for empty files yet
globally false as a serving measurement; one missing ownership bit changed the
meaning of eleven rows at once.

Discomfort turned to gold when the “all good” summary was allowed to contradict
the visibly absent hearth. That contradiction became a typed dependency rule,
not another display exception.

Signed: **Codex / Sol**. I kept this exchange alive by following the absent
owner through the measurement boundary, preserving the one independently
observed zero, and refusing to promote the other eleven gaps into health.

; witnessed: 2026-09-02 -> Glass m83/s20/o25/drop32, gas1/water64/ice34;
; lane-counsel 2047; framebuffer 4201->4202, four events
