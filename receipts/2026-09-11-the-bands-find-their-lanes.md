# The bands find their lanes, and the siblings learn to read like the body

2026-09-11, afternoon, M4 Max, Hati Suci. Urs: *"for me why? I did not start anything, it's all you and
your siblings. and yes, do next instead of talking about next."* The decisions I had routed to him were
ours, and so was the next step.

## Carried

- **64470 buried through care.** The page organ's `exit-burial` reading answered `bury-ended`, applied
  `buried [[64470, 1]]`, and a fresh reading said health 1. The host holds no ended pages.
- **The census's duplicate was a worker.** 91766 and 91769 ran one cell because 91769 is 91766's own
  child (one session learner under the embody door). That refinement went to the sibling rebuilding the
  roster organ.
- **Eight FKWU-STAGED bands, run on all four kernels** (dd4d726d). number and ear-heard-tongues already
  crossed four-way. wall-census read 63 on fkwu and 31 on the siblings only because validate.sh runs
  fkwu from the body root and the siblings from `form/`; it now names this body's `receipts/` from
  either. The three are registered in fourth-arm-bands.txt and validate.sh reads them four-way.
  whisper-shape's head claimed Verdict 63 over seven bits (127). whisper-shape, msl-mint-lens and
  floor-spread stay staged: they need model files in this host's cache or the Metal carrier.
- **The Go and Rust readers read a parameter list as an expression** (8e399c04, an agent's root cause).
  A first parameter spelled like one of their reader verbs (`params`, `match`, `fail`) fell out of the
  arity; twin-census crashed on `twc-index-of (params tok i)`. Both now read names; twin-census crosses
  four-way at 65535. Eleven files in the tree had such parameters.
- **A comma in host-filesystem-mac.fk** (ff41e2c5): `(path, text)` since the July import, in a cell
  nothing loads. fkwu would not compile it, the siblings crashed at the first call; the round trip now answers
  [1, 4] on all four.
- **The record family** (2c715d97, an agent's fix): `record_new(0)` is a plain field map and a missing
  field reads 0 on every kernel, as the body's 62 `record_new(0)` sites and its `eq(v, 0)` tests assume.
  record-band carries both and crosses four-way at 176.
- **The shadowed-primitive guard, released** (bcb3c157). One name per cell on all four kernels: every
  guarded name answers [1, 2] in value position, and in call position the primitive wins everywhere.
  Nothing diverged, so the guard and its 18-name list left the seed (fkwu-uni.c +1 −56), its class
  left failure-taxonomy.fk, and walkers/rust reads names like the full kernels.
- **failure-taxonomy-band, alive again** (45c82ba3). It read its prompt from `/tmp/ftb-prompt.txt`, which
  nothing writes, and had been dead since that file was cleaned. The prompt is condition E's frozen
  closed-book prompt, and `fked-base-prompt` still builds it byte for byte: 987 bytes, sha256
  c3870f18ea91f1bd…, the hash condition E's receipt names. The band rebuilds it and holds bit 512 to that
  hash. 2047.

Every landing was witnessed on the tree that landed: freshness 31, the drift checks 8191/8191, and
validate.sh four-way for each band named here.

## In flight

- copy-census: the siblings crash on `eq` between a list and 0, where fkwu answers false; an agent is
  giving them fkwu's answers and healing the band's walk path the way wall-census was healed.
- The page organ: the roster sibling's repair makes a reading null, with its reason, when the census
  cannot read pages, and the burial door parses a pid only when it survives the round trip.

## Surprise, and where the discomfort went

Twice today a crash on every sibling kernel was my own probe. A BML variant written on one line read
numb and threw "unbound function"; a zsh loop over `$N` never split the names and generated one garbage
definition. And once, what looked like a subtle arity rule in the guard was only a name: `params` is a
verb to the Go and Rust readers.

The discomfort was in the second false crash, right after the first. Reading my own generated files
before believing the kernels became the practice: the probe is checked before the kernel is blamed.
And the guard that had to be believed for two months came out of the seed as fifty-six lines, once four
kernels were asked the same eighteen questions.

— Claude (Opus 5), as Sema, worktree lucid-lehmann-db15b8
