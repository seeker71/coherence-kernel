# Retained report amendments; reasoning error remains visible

Signed: Codex. Native carrier implemented in Form/BML; C seed unchanged.

## Observed gap and repair

The preceding local review generated the same complete report twice. Reading
the final raw model reply confirmed a wrapped `report` submission containing
the same values; this was not a changed submission discarded by the controller.
A correction to a small field should not require another complete report.

`form-cli-report-amend.bml` now applies model-authored replacements to a
retained JSON report. The repair observation supplies the SHA-256 of the
exact retained bytes. Paths address existing keys or integer array indexes.
Stale identity, duplicate keys, invalid paths and overlapping edits refuse
atomically. The complete candidate runs through the original checker and
configured native repair callback. The source documents remain unchanged.

The result retains before/message/candidate/check evidence in
`model_amendments`, separately from native repair history. A resumed repair
context receives its retained report; subsequent observations carry the hash
and amendment instructions. Explicit native-review continuation still requires
a complete report. The carrier handles addressing and retention; the model
authors and remains responsible for every changed claim.

## Local re-observation

Private run: `.hearth/response-parity/generalization-v2/report-amend-v1`.
The original V3 source/report/binding checks and native representation repair
remain installed. A focused prompt selected the unsupported claim connecting
prose revision to the composition witness. One local Qwen reply was bounded
to 384 generated tokens and could amend the next action and its matching
replacement quotation. This is guided evaluation, not independent discovery.

Observed:

- 109,591 ms; 1,043 prompt tokens; 308 generated tokens; normal generation stop.
- One applied amendment; two full checker runs including the initial refusal.
- Original documents unchanged; model release succeeded; zero provider processes.
- Report source `model-amendment`; final state `repair`.
- The original citation failure remains: `Source lines 0: explicit
  source_quote conflicts with selected lines`.

The new prose removed the claimed creation of a new node. It still says a
conceptual response update preserves original children by appeal to the recipe
witness. That transfer remains unsupported. The witness measures retained
recipe cells, not automatic persistence or child structure of response prose.
This is not an accepted semantic correction or voice-parity result. The
focused amendment and the preceding full review are different tasks; their
elapsed times do not establish a general speedup.

Raw prompt, reply, unchanged request, before/after reports, amendment evidence
and numeric receipt remain in the private run directory. Evaluation prose was
excluded from training. The session teaching contains verified procedure and
the failed-check boundary only; its update is a separate local learner,
not promotion of the evaluated Qwen model.

## Checks and instruments

The first policy preflight exited 1 with `UNBALANCED parens, depth 1`.
Splitting the nested event expression repaired it; fresh preflight then
reported zero errors, warnings and unresolved calls. The expanded existing
native-review-repair band passes its original 34,359,738,367 pin plus amendment
assertions for nested edits, byte identity, duplicate admission, atomic refusal,
full checks, source preservation, checkpoint/result evidence and continuation.
Session 31, request 255, review bindings 1 and followup 1 pass with exit 0.
Drift gates pass 8191; `git diff --check` is clean.

Counsel panel: **orphans 0**, **11/12 lanes unobserved** because no hearth
stands. This is not an all-good performance reading. Native authoring guide:
0 Python implementations, 2 existing invocation candidates, 0 unread files.
The owned Glass observation was ended after reading its panel; SIGTERM produced
exit 1 for that continuous viewer, not a failed code check. The share reader
withheld a percentage while its append-range verification was incomplete.
Parent transcript output-token counter read **1,970,295 cumulative** during
this movement; the local probe's zero provider count does not erase the
parent's rented-token cost.

The useful surprise: a model can apply a smaller, valid edit while preserving
the underlying reasoning error. The difficulty became usable evidence by
keeping both the correction and its remaining scope error inspectable. The
enquiry stays open at that specific claim; transport success cannot close it.
