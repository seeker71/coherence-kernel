# Native substantive workflow

Form owns the deterministic substantive build and its three downstream
generators in one process. The public door is
`observe/concept-substantive-build-run.bml`. The existing JavaScript command
delegates its ordinary entry to that door and preserves the actual child
status. Its fresh acquisition branch remains active with its source selection,
translation and proposed-generation responsibilities. Root C is unchanged.

Independent root source review and the complete public workflow witness accept
the movement. Root capture `.hearth/source-inventory-90388-51053413-0` returns
1, exit zero, stderr zero, retaining 446,830 stdout bytes. The public child
publishes 21 exact prepared artifacts into its private snapshot, then the
portable hash policy reconstructs an actually damaged alias carrier and
reproduces all 21 artifacts. Evidence for that portable child is
`.hearth/source-inventory-90391-51078298-0`.

The generated Form files execute every one of the 10,000 rank and repaired-ID
queries, three outside-domain queries, all 111 ID/rank entries, all path doors
and all 112 translation offsets against independently read evidence. Current
publication returns 1, exit zero, stderr zero. Its 539,734 stdout bytes retain
all publication and source observations. The semantic consumer returns 4095
after clean preflight. The current runtime witness also returns 1 after fresh
preflight with zero diagnostics. Its preceding cache-renewal warning is
retained; it is not described as a zero-stderr execution.

The six substantive data carriers remain byte-identical. Generated Form
descriptions carry current meaning, and the rank argument is `current-rank`.
Their two manifest entries bind the new bytes. The public request rejects
malformed, duplicate, wrongly typed, empty and NUL-bearing paths, unknown
fields and unknown hash policies. Each publication owner preserves its
completed prefix and stage disposition on refusal. The 21-file workflow is
not an atomic generation.

The native guide observes two Python implementations, 221 execution candidates,
50 grammar inputs and zero unread files. Glass's bounded full stage profile is
180 ms: storage 138 ms, fast collection 35 ms and render construction 1 ms,
with 168 samples and five publishers. Drift gates return 8191/8191 with no
refusal. The share meter is declared/unmeasured; no percentage is inferred.

Verified teaching is retained as
`f8f6dc457783d7860f1086560cef264cdb46724e42b61d8d0f295c584cd42ed8`.
Retention does not attest model promotion.

The surprising teaching is that the command's meaning includes its downstream
generators. The concrete discomfort was that fresh evidence cannot honestly
pass a manifest describing the preceding evidence. Its value is a precise
admission boundary: ordinary reconstruction is native now; fresh acquisition
must establish its proposed projection before it can replace current truth.
The exchange stays alive through exact execution, a shared process and an
explicitly owned next attempt.

— Codex
