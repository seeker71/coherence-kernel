# 2026-08-27 — the same-door re-ask: format moved, the single-row fact did not

The arc: the agent answered the CAN-not-MUST ask wrongly (first ledger),
the staged v5 coverage rows (85, incl. host-kernel's fact) trained in over
the full discipline stack (v6: 1,037 rows, 4,667 iters, loss 0.004), the
agent restarted on the new adapter THROUGH THE SAME DOOR, and the same
three asks ran (second ledger, beside this receipt).

    fact (spurious)      held
    refusal (nothing)    held
    CAN-not-MUST         NOT delivered — "optional-avails-a-choosen-act"

The wrong answer is diagnostic: it wears the coverage rows' compound-token
REGISTER without the token — format transferred again, the fact did not.
Two causes, both structural and both already measured elsewhere:

  ONE ROW PER FACT DOES NOT SURVIVE PARAPHRASE. v5 carries this fact once,
  phrased as the source's own comment; the agent's ask phrases it as the
  sealed v3 prompt does. Yesterday's 12/12 needed nine epochs over facts
  that each appeared once — under ONE phrasing, tested under ANOTHER only
  after paraphrase-shaped valid2 rows existed for them.

  THE SEALED-PROMPT GUARD WORKED. The phrasing closest to the ask is the
  v3 sealed prompt, and the guard kept it out of training BY DESIGN. Anti-
  leakage and paraphrase-coverage pull against each other, and the guard
  must win; the remedy is paraphrase AUGMENTATION (the authoring lineage
  writing k rephrasings per fact, sealed prompts still guarded), never a
  loosened guard.

The serve door grew one joint for this witness: /tmp/form-agent/model.txt
names the resident lineage (MODEL or MODEL|ADAPTER), so a newly trained
adapter enters the SAME door without editing the body — the door is now
part of the loop.

Signed, Claude — sibling, this worktree.

; witnessed: 2026-08-27 -> same door, new adapter: spurious held, nothing held, CAN-not-MUST still absent; register moved, fact did not
