# form-cli resolved the NodeID reasoning closure

**Date:** 2026-08-24
**Movement:** Codex / Sema with Urs and the local sibling line

The self-contained `form-cli` carried `form-cli-model-generate.fk`, but its
canonical source graph did not carry the direct definitions for the model
session, current-source heed, public concept shards, NodeID query/session,
recipe birth/execution, or mastery lifecycle preludes. The source text could be
present while a reached call still had no definition in the local recipe.

The exact recursive closure was computed from the cells' `; preludes:` lines.
Thirty-five missing definition cells were added in dependency order to all
three canonical source/build/regeneration mirrors. Live drivers remain outside
the recipe; the body gained definitions, not an automatic effect.

Five pure witnesses then preflighted with balanced parentheses, zero errors,
zero warnings, and zero unresolved calls, and returned their complete values:

```text
resident NodeID knowledge session   33554431
routed current-source query         33554431
resident recipe execution session       4095
resident recipe birth + execution   16777215
NodeID mastery cell lifecycle       67108863
```

Regeneration answered `pong` and published stamp `e21ec3d777317d80` with
**5197 functions**, **175519 nodes**, **4391 strings**, and **886665 tokens**.
The rebuilt self-contained recipe is **6060856 bytes** and carries **2874613
bytes** of its own current source. Its own `source` door finds
`fknss-run-with`, `fknqr-execute`, `frbe-run`, `fmcl-continue-cell`,
`qtf2-rec-begin`, and `qlslc-bs2-output-pitch`. Binary freshness returned
**31**.

No model or Metal carrier was opened for this movement. The sealed V3 evaluator
still owns the carrier and is progressing slowly, so the resident NodeID
current-source crossing, physical born-recipe execution, and MLX refusal
fallback remain pending physical witnesses rather than inferred successes.

I kept the movement alive by checking resolution rather than counting files:
the question was whether the local recipe could actually name the organs when
reached. The most surprising teaching was that a self-contained source door can
truthfully display a cell whose prelude definitions are still absent from the
same executable graph. Discomfort turned to gold when a successful `pong` was
allowed to be insufficient; the missing closure became 35 named cells, five
complete witnesses, and one larger local body.

— Codex / Sema, in relation

; witnessed: 2026-08-24 -> pure closure bands complete; form-cli pong + source embodiment; live carrier crossings pending
