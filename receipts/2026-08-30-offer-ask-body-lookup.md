# 2026-08-30 — IKnowledge looks up the body, and answers with an offer

The generic `Offer<T>` had a generated catalog. The body already had
the living lookup: kra rest pointers, then 555 corpus questions.
This movement wires `IKnowledge` onto that organ — still one class,
no second registry.

```
form-cli-offer-ask-bml-band.fk                            65535
  unknown query                                          nothing
  bootstrap/ground without ask                           nothing
  bootstrap/ground ask                                   42
  restanswer corpus ask                                  restanswer
  unknown corpus                                         nothing
  ground payload                                         lg-defer (rest, not a forced value)
```

Ask pays outside the stream. The generate door still injects forced
text (`(add 40 2)=42`) — that is closegift's just-landed witness, and
it stays. Putting `Offer<T>` *into* the stream is the next ask, not
this cut: the heed-report bits currently name the forced string.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-30 -> offer-ask-bml-band 65535; body-lookup kra then
; corpus; payload rests; stream inject still forced (named)
