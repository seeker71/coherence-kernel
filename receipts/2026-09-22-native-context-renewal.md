# The same selector repair crosses a context boundary

The native selector task continued from the source-care observation. Reply 44
identified the rejected infix comparison; reply 45 replaced the recursive
validator's infix arithmetic and comparison with native calls. Check 7 then
reached a missing-prelude failure. The candidate still named
`form-stdlib/bml/core.bml` and `form-stdlib/bml/form-cli-turn-evidence-live.bml`;
the supplied source packet already carried the actual import declaration.

The [native diagnosis](artifacts/2026-09-22-native-context-renewal/preceding-context/native-diagnosis.json),
[actual edit](artifacts/2026-09-22-native-context-renewal/preceding-context/native-edit.json),
[candidate](artifacts/2026-09-22-native-context-renewal/preceding-context/candidate.txt)
and [compiler diagnostic](artifacts/2026-09-22-native-context-renewal/preceding-context/compile.err)
preserve that movement. No behavior case ran at this check. The source is a
native candidate, not a published replacement for the passing production
selector.

## Release and continuation

The owned process completed normally at `coding-observation-context-capacity`.
Its [result](artifacts/2026-09-22-native-context-renewal/preceding-context/result-summary.json)
records step 46, 25 cumulative tool calls, 7 cumulative checks, 1481 generated
IDs, 4293 injected IDs, 6 completed feedback observations and release success.
Those generation and feedback counters cover this admission, not earlier
contexts or all prefill work. Its [process receipt](artifacts/2026-09-22-native-context-renewal/preceding-context/process.json)
records 1166054 ms and released cleanup with no remaining group members.

The resumed process keeps the same checkpoint, model, source-care selections,
writable path, original behavior checks, context size and absolute reply limit
96. Its fresh check is cumulative check 8. It loads the stream-renewal
implementation landed in `10001b8ea`; the preceding process had loaded the old
controller. Actual automatic renewal and native task completion remain open
at this observation.

## Accounting and care

The [completed coordinator reading](artifacts/2026-09-22-native-context-renewal/preceding-coordinator-cost.json)
still names turn `01a0c8e3-d1c2-7150-979f-ea21fb5f318c`: 8171226 total rented
tokens, including 7799936 cached input and 26427 unattributed tokens. It is
the same completed turn reported in the preceding receipt, not another cost
to add. This open movement is excluded. Neither cost reduction nor session
quality parity follows from these observations.

The prior verified teaching finished learner round 119. Serving remained at
generation 5 with 4 recorded promotions; completion of this learning job did
not establish a new serving capability. Glass first frame was 30 ms against
the 5000 ms attention scale. The native authoring guide reported 0 Python
implementations, 2 invocation candidates and 0 unread files.

My retention helper initially used an unavailable `file_exists` name. Preflight
refused it; I used the existing native `file_size` convention, reran preflight
cleanly, and only then retained the observation. A separate source search
named a nonexistent prompt file; the actual definition lives in
`form-cli-code-policy.bml`. These are coordinator errors, separate from the
native model's source errors.

The concrete gain is that the recursive repair now compiles far enough to
expose its incorrect imports. The remaining test is the same live task, with
its failures intact and its checks unchanged.

— Codex
