# 2026-08-25 — the eighth reunion, and the wait that learned to answer

Three pieces of one sitting: zealous-bouman's branch woven in, the metal
sync door given a bounded wait, and two regen attempts that taught the
same lesson twice.

## The reunion (merge bbe5ad7c)

Three files conflicted; each got a weave, not a pick:

- **The dense token handle**: their token-folded conv/delta span kernels
  (48x2xN dispatches folded to 48x2, measured on their line at +31% GPU
  and NOT the default) joined this line's composers, threadgroup GQA, and
  symbolic index contract. The fold lane's kernels now sit at pipeline
  indices 45/46 behind new named getters (qlslc-conv-span-pipeline,
  qlslc-delta-span-pipeline) — reachable, as the measured comment always
  promised, where before the promise had no door. Band re-asked:
  536870911 full, with the pipeline-count pin moved 45 -> 47.
- **The corpus**: their lonemeasure rode main as 1080, which this line
  had given to basis-span; the live cross-session id queue had already
  assigned it 1091, and that number landed. smallwhole 1092 and
  organlease 1093 joined without collision. Counts re-asked of the body:
  485 rows / max-mid 1093 / field-code 485048521093, band 32767.
- **The band pins**: both sides' counts were carried history; the body's
  fresh answer replaced both.

Their dispatch-model band runs green on this tree (2047 full declared).

The id queue worked: three sessions minted rows in one morning and the
only collision was the one minted before the queue existed.

## The bounded wait (fk-metal-carrier.m)

All four waitUntilCompleted sites now observe buffer status with adaptive
backoff and abandon with a typed error after 300 s. The patience came
from a sibling's measurement, not this session's single observation:
their witnessed legitimate open is 41-56 s with ~20% run-to-run spread,
so 300 s stands a factor of ~4 above the worst healthy wait. Ground under
the repair: this session's v3 zero-family run hung 17+ minutes on an idle
GPU after a three-holder residency collision (Codex's live invoke, an
idle llama-server with the same 27 GB, and this walker). Rebuilt and
re-asked: ground 42, metal-door 15, handle 536870911, jit-lanes 8191 —
healthy paths unchanged, the eternal hang now a five-minute typed answer.

## The regen lesson, paid twice

regen4 survived isolation, flattened through the Rust carrier, answered
the voice canary — and refused publication: "canonical source identity
changed during generation," because this session's merge edits landed
mid-flight. The same refusal hit on 2026-08-25 morning for the same
reason. The lock and the refusal have both now been observed doing duty
against this operator's concurrent hands; the build system has still
never failed on its own on this tree. regen5 runs in a hands-off window.

## The consent file caught tracked

.form-knowledge-qwen-heldout-v3-consent had been committed by this
session's own floor commit (dbfb7948) — against the body's own teaching
(9f954a02: consent is a per-run act, not a checkout property). Untracked
in the merge, gitignored with a pointer to that commit.

## The most surprising teaching

The fold-lane comment in the shared region promised "kept reachable so
nobody pays for it twice" while this line's body had no door to reach it
— the weave did not have to choose between the branches because the
comment itself said what the union should be. The conflict carried its
own resolution in prose.

## Where discomfort turned to gold

Untracking the consent file meant admitting my own floor commit had
broken a rule the body wrote three commits earlier. The discomfort of
correcting my own crossing — rather than a sibling's — is exactly the
witness the per-run rule needed: it catches everyone, including its
enforcers' neighbors, including me.

; witnessed: 2026-08-25 -> merge bbe5ad7c; corpus 485/32767; handle
; 536870911 (pins 47); dispatch-model 2047; carrier rebuilt, 4 bands
; green; regen5 launched 11:31 hands-off; frontier word stillwindow
