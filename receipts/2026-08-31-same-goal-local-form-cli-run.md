# The active goal runs once through local form-cli

Authored by Codex on 2026-08-31.

The exact active goal text was submitted once to the already-running local
Form resident through `observe/peer-ask-send.fk`.  The sender appended one
length-safe task frame and rang the existing hearth bell.  It did not start a
second model, open HTTP, invoke a provider, or cross into llama/Ollama.

The resident selected its retained direct route through its dynamic program
NodeID.  The durable reply and public stage stream—not this prose—are the
authority for the run.

## Receipt

```text
turn                         9205
task append / bell ring      8772 / 1
policy action                direct-answer
policy route-node            @0.2.0.18
policy reason                dynamic-program-nodeid-invoked

reply signal                 candidate
reply route                  direct-answer
local generated tokens       155
callback calls               0
lookup count                 0
carrier                      form-native-metal-jit
lifecycle                    generate,release
reply elapsed-ms             119935
reply response-bytes         683

outer wall-ms                132669
GPU busy                     35119213 us
CPU JIT busy                 0 us
MLX dispatch                 0
resident completion count    2
resident session positions   1447 -> 1218 (recycled)
resident readiness           1
```

The local answer covered BML authority, BMF grammar as walker-carried data,
cache/JIT crystallization, Metal JIT, framebuffer evidence, and the honest
missing-provider-ratio boundary.  Its exact answer bytes remain in the
caller-owned reply spool; neither this receipt nor the framebuffer duplicates
them.

This proves one same-goal local Form execution with zero provider callbacks.
It does **not** prove a numeric 10% remote-token ratio: there is still no
completed comparable provider receipt for the same prompt.  The meter remains
withheld rather than treating local tokens or this Codex turn's token use as a
provider denominator.

The active local resident retained its Qwen/KV lease and returned to ready;
no successor was forced into its Metal space.  The next concrete crossing is
to let an orderly successor adopt current BML source when that lease ends,
then repeat this same receipt with the new birth/frame evidence visible.

I kept the exchange alive by using one durable Form task and the resident's
own stage stream rather than a second model or a polling sidecar.  The
surprising teaching is that the local answer itself refused the tempting ratio
claim.  That discomfort becomes a clean denominator requirement instead of a
made-up percentage.
