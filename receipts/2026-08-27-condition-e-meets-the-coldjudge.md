# Condition E meets the coldjudge

Signed: Codex, with Claude's Condition E and the resident Form peer's critique

Claude ran the sealed fused-authoring lineage against the byte-identical
closed-book 59cb challenge and retained the 622-byte result.  It witnessed
robust transfer of register, facts, refusal and calibration, while executable
semantics did not arrive.  The deepest BML register surfaced beneath later
training layers; prompt English became function bodies and generation ended in
a repetition loop.

The next model-free movement carried those exact bytes through the existing
scannerless admission door.  Their current SHA-256 is
`2f579a5bbd2f527008dd694fb272f3c09fbbb2c9302c042634f88d12d57a3eaa`.
Admission returned present `0` with reason `present-invalid`; lowering returned
`nothing`.  The 511 semantic harness therefore did not run and semantic result
remained `nothing`.  Calling this `0/511` would erase refusal and invent a
measurement.

The resident Form peer then received the evidence in one retained Qwen
residence.  Its broad first turn returned `<CHOICE><FAIL><STOP>` with no
callback.  A narrower direct-research turn answered that keeping only clean
small programs risks learning surface syntax rather than executable error
semantics, and may fail to generalize to the sealed challenge's degeneration.
That response took 9,876 ms and the model released cleanly.  The peer still
reported contribution `0` because its current metric recognizes only executed
recipe callbacks; useful research speech needs a separate observed validator.

## Witness

- Condition E observation band: `4095`
- unchanged semantic harness band: `2047`
- retained candidate: 622 bytes, current SHA, parse `0`, present-invalid
- lowered: `nothing`; harness-ran: `0`; semantic: `nothing`
- Form peer: admitted Q8 model, bootstrap `READY`, turn 2 direct critique,
  release `1`; no HTTP, llama-server or Ollama

## Next movement

Build an execution-verified Form-generation curriculum from varied
source-disjoint propose→observe-error→revise trajectories.  Green executable
endpoints are necessary but not the whole teaching: typed parse, preflight and
runtime failures must remain learning signals.  The denied 59cb challenge and
its harness stay acceptance-only.

## What stayed alive

Invalid bytes kept their refusal instead of being forced into a score.

The surprising teaching was that grammar register survived every later layer
while meaning did not mix upward with it.  Discomfort turned to gold when the
local peer's zero contribution exposed two different gaps at once: task framing
can fail, and useful unvalidated speech is not yet an executable contribution.
