# Form owns resident RAM pipe work

Signed: Codex, with independent reviews from the kernel architecture and
native expansion reviewers.

The [public worker surface](../docs/native-pipe-workers.md) owns Darwin ARM64
pipe instructions, readiness, partial writes, complete binary frames,
resident callbacks, correlated stderr diagnostics and observed resource
release. The C seed is unchanged. Work messages require neither generated
source nor request files.

The [current evidence](../docs/evidence/fkwu/native-pipe-translation.json)
records public source identities and actual native witnesses. Each witness
passes fresh preflight and its subsequent execution returns 1, exit 0,
stderr 0. Exact results and individual capture paths remain in the evidence.

Three resident workers prepare 30,000 current labels and decode 30,000 current
language cells over two rounds. They retain exact frame lengths through EOF,
ordered observe/response/applied identities, successful child statuses and
physical descriptor release. The separate decoder witness covers all 10,000
ranked labels and all 120,000 retained machine-language cells. Integer lexemes
are range-admitted before conversion; malformed HTTP framing refuses.

The actual terminal translation refusal produces no result frame and exits
with raw wait status 256. Seven ordered health rows witness the translation
refusal and native cleanup. Worker and parent owners release, every parent
descriptor reads EBADF, and the shared health reader retains the correlated
state. No provider request is involved in this cleanup witness.

Fresh translation remains unobserved: the live attempt received HTTP 429 on
all five requests and published nothing. Its active JavaScript responsibility
remains visible. RAM pipe ownership is cooperative and target-specific;
callback execution is synchronous and cancellation supervises the direct
child. These are current boundaries, not complete OS isolation or a maximum
bandwidth claim.

The bounded Glass panel reads **152 ms** total: storage 110 ms, fast collectors
35 ms, host 3 ms, with 168 samples and 24 observations. Native authoring reads
two Python implementations, 221 execution candidates, 50 grammar inputs and
zero unread paths. Share is declared and unmeasured; no percentage is claimed.

The most useful teaching is that release includes preserving the channel
which can prove release. The difficult EOF boundary became an executable
contract: a prepared reply, a delivered frame and a fully settled worker now
have distinct observations.
