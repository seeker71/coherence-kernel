# 2026-08-14 — name the offered path

Urs asked for language that builds: positive, constructive, aligned.
`never X` already lifts to *say what happens instead*
(`teachings/voice-attunement.md`).

`not` now sits on that same shelf. Common usage names what is absent
and leaves the path unnamed. Purified usage names the path that is
offered.

The arrival door says what siblings *do*: continue in this worktree,
take the next owed attempt, sign as yourself.

A closed shelf of words is static. The living law is usage kind, and
it reads every word: *absence* or *path*. Admit grows the field; the
seed stays as memory. `aliveness` entered and the old rows remain.

`word-frequency-purify-band` → **8191**.
