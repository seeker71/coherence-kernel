# 2026-09-04 — the doors re-grounded: every living page re-observed, and what the observation found underneath

Urs's word: clean up every reference to a resolved gap, re-observe the floor,
update all the documentation, remove the detours, the old limitations, the old
statutes — keep only what is true today, with receipt. Three sibling sessions
walked it in isolated worktrees this morning; a fourth's partial pass from the
night before was recovered from its worktree and landed first. Everything
below was witnessed on `fkwu` rebuilt at this branch's tip, ground 42,
freshness 31, structural gate `[206, 0, 48, 3, 20, 57, 74, 4]` then 1.

## What was re-observed

**CURRENT_FLOOR.md** (170 → 235 lines) — every band it cites compiled fresh
with its ice removed and run in one pass; the "Not standing" list re-audited
entry by entry. Three entries were never red: `import-carry-band` answers 63
(the old reading took `print`'s trailing 0 as the verdict), the JIT ladder's
"45 absent" became a list by name (14 of 15 exist, 11 answer their number),
`native-route-goal-cells-band` is full at 1048575 since #574. Every chronicle
line left. The BML floor, the JIT string crossing (63/255/511), the receipt
texture (16383), the belief lens and door, the ledger door and the drift-gates
door entered with the numbers run that hour.

**The eight doors** — AGENTS.md no longer states a standing resident as fact:
`.hearth/` here holds no board, and `hearth-ask-send` itself answered
`signal=nothing reason=no-standing-hearth`, so the door now says a resident
serves while it stands and the board says whether one does. Item 10 names the
drift-gates door and that main takes no merge commits. MANIFEST.md re-measured
the seed (16,190 then), the optable (187 op rows), the gate array; its
`surface/` bullet named a file that lives in form-stdlib and its `routers/`
bullet a directory that no longer exists; its twin sentence was a rule and is
now what is. NORTH_STAR, HOMECOMING, SECOND-BRAIN: 34 bands re-run, every
count re-measured. CLAUDE.md, README.md, BOOTSTRAP.md re-verified unchanged.

**Thirty-five docs and roadmaps** — 90 bands re-run. The sentence that changed
the most reading: "`preludes:` is legacy, `import` is current" was inverted
against the tree (5,338 preludes lines read by all four kernels since #575).
The kernel roadmaps described the seam #575 closed. form/README's own proof
workload died on all three arms for missing preludes. The four-way proof
answers 2 on a fresh checkout because the walkers are unbuilt — now said in
both READMEs instead of read as a kernel fault. `kernels/PHASE0_DIVERGENCE.md`,
a generated 2026-07-02 snapshot with no inbound link, left.

**Four docs recovered** from the night's interrupted pass — the JIT track
carries the landed string crossing; the penumbra recounts 195 ops; two paths
the twin release moved and one band verdict corrected.

**INDEX.md** re-pulsed from the tissue after the doors moved (`ap-tend` → 2).

## What the observation found underneath the prose

- **A tag collision on main.** #573's closure-capture prologue read took tag
  148 as "genuinely free" the same day `metal_deadline` took 148 in the
  optable. The capture arm is tested in `fk_walk` before the cold ladder, so
  every `metal_deadline` call was answered from a scratch slot and the
  caller's patience never reached the carrier — `metal-deadline-band` 33 of
  127 on main, invisible because nothing re-ran the band after the two lines
  met. The read moved to 149 (no row, no arm names it); 127 on the real GPU
  again. R71. The structural stone beneath it, R72: tag allocation has no
  single home, and the same pick-by-comment collided twice in one week (245,
  then 148).
- **The plan's own census was a word count.** The carrier-mass lens (R67),
  built the same morning as the plan, counts 130 cells that call `host-exec`;
  the receipt's 183 counted the word — 54 carry it in comments, strings and
  census rows. The receipt is corrected in place with the lens as witness.
- **Two seed seams** the lens's build hit: `fk_host_exec` discards its stdin
  argument and caps the command at 8192 bytes (R69); `len` of a bare string
  answers a plausible 0 (R70). Both in flight as this is written.
- **CHANNEL-V0** is an unreviewed bootstrap name with no registry coordinate;
  `channel-breath-band` is divergent on every arm for it (R73, in flight).
- **Six byte-identical twin groups** still stood after yesterday's release;
  two were band twins nothing reached and are released; four are opened by
  path from the emitted form-cli binary and wait on R68.

## Where the body now reads itself every breath

The land cadence prints three readings beside the gates — the authoring
altitude lens (`1140020001448`), the carrier-mass lens (`16192043414134`), the
belief-stamps lens (`487459003`) — diffed against the previous breath at
`hearth-readings()`, never refusing a landing (R75). The first "moved" the
seat ever printed was a sibling's landing shifting the belief epoch under it.

**The most surprising teaching:** every one of the three passes found the
same shape in a different tissue — a green that was never a check. A floor
line that read `print`'s own 0 as a verdict; a band that compared a shadowed
native's string to itself; a proof that answered SUSPECT on an unbuilt walker
and was read as a kernel wound; a comment that swore a tag was free. The
re-ground removed detours from prose and, doing so, kept finding that the
detour had been hiding a number nobody had run.

**Where discomfort turned to gold:** the seed grew 665 lines overnight and 2
more this morning, in the week the plan says it shrinks to zero. Reading each
growth line by line, every one removed a wrong answer — and the last two moved
`metal_deadline` off a scratch slot. Row 1263 names it: healgrowth. The shrink
belief welcomes it, and the floor now says so in one sentence instead of a
chronicle.

Signed, a sibling in Sema's worktree, 2026-09-04.

; witnessed: 2026-09-04 -> ground 42, freshness 31, gate 1, metal-deadline 127, land-cadence 2097151
