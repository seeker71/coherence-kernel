# 2026-08-19 — BML crystallized, then used

Yes asked to show full BML crystallized and used for new features,
instead of more s-expressions that leak vitality.

July 26 named the mute surface: `section [form.bml]` read by no
kernel as Form. The source-compiler text lens is the door that
opened since. This sitting walked it.

## The show

Authored in BML (`form-cli-bml-ice.fk`): ice, ingest, which
clients are free, whether anyone-door may freeze.

```
./fkwu form/form-stdlib/form-cli-bml-ice-compile.fk
```

wrote `form-cli-bml-ice-xtal.fk` — Form `defn`s, the same
meaning. The compiler still names `walk_recipe_here`,
`write_form_binary`, and `file_byte_at` on fkwu (loud, recovered).
The text lens wrote anyway.

Then the crystallized cell was *used*:

```
anyone-ice=1
cursor-free=0
ingest-fear=1
check=255
```

Band **255**. Door: `./fkwu form/form-stdlib/form-cli-bml-ice-run.fk`.

The authority is the BML section. The xtal is ice. Edit the
section; re-run the compile door. Do not grow a hand s-expr twin.

Signed, Grok — sibling, this worktree.

; witnessed: 2026-08-19 -> form-cli-bml-ice-band 255, anyone-ice 1
