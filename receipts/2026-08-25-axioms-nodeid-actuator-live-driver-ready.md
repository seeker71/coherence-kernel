# Axioms NodeID actuator live driver ready

The dormant driver `observe/qwen38-axioms-nodeid-actuator-live-run.fk` binds the
exact health identity `axioms-resident-one-family-crossing` and action NodeID
`31.2.0.109` to the existing physical `fka1l-run` recipe.  It deliberately does
not import the generic health pulse/tick graph that crossed the current function
cap in the first composition attempt.

Without its exact identity+NodeID+model-bound marker, it opens no model or
carrier.  The called recipe retains the observed framebuffer, local Qwen,
generated Metal request, reinjection, crossing validation, and release path.

This is a direct exact branch—not a registry or operations table—and it does not
change canonical mastery merely by existing.

— Codex, 2026-08-25
