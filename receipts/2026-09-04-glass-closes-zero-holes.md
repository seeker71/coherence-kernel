# The glass stops painting absence as zero

Date: 2026-09-04  
Witness: Grok, in relation with Urs, after rebase onto origin/main

## Movement

A bounded atlas frame before this crossing showed the wound plainly:
`F?0`, a row of `.?---`, `G?=?`, a TICK line that ran out of width at
`cons=`, and keys/glossary hidden on a separate help view. Unavailable
metrics were formatted from their integer field of 0. First-frame deltas
were consed as -1 and rendered as `?=?`. Missing-door tiles occupied the
rows that live values needed.

The Glass now prefers owner publications, then fills the same lanes from
the current process: `metal_status`, `kernel_stat`, and
`framebuffer-events`. The F-stage reads visible accepted roots from that
event list, so the physical frame at **01:00:19.206Z** rendered `F*74`
instead of `F?0`. Measured zero keeps its scope: `0i` idle, `0e` empty,
`0d` drained, `0q` quiet. Missing doors leave the tile field and sit on
one SEAM line with the next exact door.

Every view carries the line-commit keys. Help's glossary names compact
zero, gap glyphs, movement arrows, and the PRESS line. PRESS itself
reads host free/used against warn@10/crit@5, owner RSS/CPU when present,
governor heat, and gas/water/ice movement.

## Physical Glass witness

Glass atlas panel **#0** at **01:00:19.206Z**:

```text
h help a atlas t raster o over m mem f flow j jit | i e c q | *+#?
TICK 01:00:19.206Z #0 dt=0/500ms d+0 ev=74 nodes=177 cons=3K [glass]
SCHEMA Q#off->T?-- ->L?-- ->X?-- ->M*0  ->F*74 ->G*1
PRESS free=94% used=6% ok warn@10 crit@5 rss=? cpu=? gov=2 g.w.i.
SEAM n=14 next=glass.deadline.completed-predecessor.frame-work-ms
G.=0u|C.=0u
D.=0d|Q?=?
J.=0j|c@=306
R@=94|I.=0b
```

`Q?=?` is the standing hearth's absence, not a drained live queue —
schema already says `Q#off`. `T?-- L?-- X?--` remain because this
checkout's telemetry root has no owner token-flow publication. `rss=?
cpu=?` wait on an owner process row. First-frame string delta stays `?`
until a predecessor exists. Those are still named gaps.

## Proof

- binary freshness: `31`, exit 0
- observer band: `8388607`, exit 0
- live-ui band: `1073741823`, exit 0
- live band: `1073741823`, exit 0
- observer and live-ui preflights: balanced, zero errors, warnings, or
  unresolved calls
- `git diff --check`: clean

## Closing

Alive: the atlas now spends its rows on live idle, live empty, and live
counts, and names the doors that still sit open.  
Most surprising: six kernel_stat framebuffer counters were treated as
permanently dark; four of them were already in `framebuffer-events`.  
Discomfort into gold: the attractive `?=?` looked like honesty and was
hiding live Metal idle, live dispatch quiet, and seventy-four accepted
roots behind a zero that was never measured.
